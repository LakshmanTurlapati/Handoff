{
  "name": "@achilles/achilles-skill",
  "version": "0.1.0",
  "private": true,
  "description": "Embedded companion system prompt for the Achilles voice loop. Ships the single source-of-truth markdown body (skill/prompts/companion.md) consumed by both the npm-CLI launch path via --append-system-prompt-file and the Phase 13 Claude Code skill body. Exposes resolved absolute paths only; no runtime logic.",
  "files": [
    "dist",
    "skill"
  ],
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist .tsbuildinfo && tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(lint placeholder for packages/achilles-skill)\"",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "5.7.3",
    "vitest": "2.1.8"
  }
}
