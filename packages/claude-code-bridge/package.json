{
  "name": "@achilles/claude-code-bridge",
  "version": "0.1.0",
  "private": false,
  "description": "Subprocess wrapper around `claude -p --output-format stream-json` for Achilles: NDJSON parsing, typed event union, authoritative success/failure signal, session-id resume, SIGINT cancellation primitive, and ack + spoken-summary extractor scaffolding for the voice loop.",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist .tsbuildinfo && tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(lint placeholder for packages/claude-code-bridge)\"",
    "prepack": "npm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "4.3.6"
  },
  "devDependencies": {
    "typescript": "5.7.3",
    "vitest": "2.1.8"
  }
}
