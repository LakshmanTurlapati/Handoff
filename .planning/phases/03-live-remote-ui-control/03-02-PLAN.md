---
phase: 03-live-remote-ui-control
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/protocol/src/live-session.ts
  - packages/protocol/src/bridge.ts
  - packages/protocol/src/session.ts
  - packages/protocol/src/index.ts
  - packages/protocol/package.json
  - apps/relay/src/browser/browser-registry.ts
  - apps/relay/src/browser/session-buffer.ts
  - apps/relay/src/browser/session-router.ts
  - apps/relay/src/routes/ws-browser.ts
  - apps/relay/src/server.ts
  - apps/web/app/api/sessions/route.ts
  - apps/web/app/api/sessions/[sessionId]/connect/route.ts
  - apps/web/app/api/sessions/[sessionId]/command/route.ts
  - apps/relay/tests/unit/ws-browser.test.ts
  - apps/web/tests/unit/session-connect-route.test.ts
autonomous: true
requirements:
  - LIVE-01
  - SESS-04
  - SESS-05
must_haves:
  truths:
    - "The browser can obtain a short-lived connect payload for one remote session without exposing long-lived credentials."
    - "The relay accepts browser live connections only after ws-ticket validation and routes them to the owning bridge/session."
    - "History, live deltas, approvals, prompt/steer commands, and interrupt requests all travel through explicit typed contracts."
    - "Reconnect has a backfill story instead of forcing a full timeline reset."
  artifacts:
    - path: "packages/protocol/src/live-session.ts"
      provides: "typed browser-relay live session envelopes and command schemas"
      exports: ["LiveSessionEventSchema", "SessionConnectResponseSchema", "TurnInterruptParamsSchema"]
    - path: "apps/relay/src/routes/ws-browser.ts"
      provides: "browser WebSocket upgrade route with ws-ticket validation"
      contains: "verifyWsTicket"
    - path: "apps/web/app/api/sessions/[sessionId]/connect/route.ts"
      provides: "authenticated connect boot payload for the browser"
      exports: ["POST"]
    - path: "apps/web/app/api/sessions/[sessionId]/command/route.ts"
      provides: "structured command ingress for prompt, steer, approval, and interrupt actions"
      exports: ["POST"]
  key_links:
    - from: "apps/web/app/api/sessions/[sessionId]/connect/route.ts"
      to: "apps/relay/src/routes/ws-browser.ts"
      via: "connect route returns the relay URL and ws-ticket used by the browser route"
      pattern: "relayUrl|ticket"
    - from: "apps/relay/src/routes/ws-browser.ts"
      to: "apps/relay/src/browser/session-router.ts"
      via: "validated browser socket is registered with the session router"
      pattern: "sessionRouter"
    - from: "apps/web/app/api/sessions/[sessionId]/command/route.ts"
      to: "packages/protocol/src/live-session.ts"
      via: "route validates structured command payloads before forwarding"
      pattern: "Command"
---

<objective>
Implement the live browser transport and structured control plane for Phase 3.

Purpose: Provide the authenticated browser-to-relay session channel, typed protocol contracts, and command ingress required for live output, steering, approvals, and interrupt without violating the trust boundary or leaking credentials.

Output: Shared browser-live protocol schemas, browser relay route + routing helpers, connect/list/command web routes, and automated coverage for upgrade/auth behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-live-remote-ui-control/03-CONTEXT.md
@.planning/phases/03-live-remote-ui-control/03-RESEARCH.md
@docs/adr/0001-phase-1-trust-boundary.md
@packages/protocol/src/bridge.ts
@packages/protocol/src/session.ts
@packages/auth/src/ws-ticket.ts
@apps/relay/src/routes/ws-bridge.ts
@apps/relay/src/server.ts
@apps/bridge/src/daemon/relay-connection.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed browser live-session contracts and explicit interrupt semantics</name>
  <files>packages/protocol/src/live-session.ts, packages/protocol/src/bridge.ts, packages/protocol/src/session.ts, packages/protocol/src/index.ts, packages/protocol/package.json</files>
  <read_first>packages/protocol/src/bridge.ts, packages/protocol/src/session.ts, packages/auth/src/ws-ticket.ts, .planning/phases/03-live-remote-ui-control/03-RESEARCH.md</read_first>
  <action>Create `packages/protocol/src/live-session.ts` with zod schemas and exported types for: session list items safe for the browser, connect boot payloads `{ relayUrl, ticket, expiresAt, sessionId }`, typed live activity envelopes keyed by `kind`, reconnect cursor fields, structured command payloads for prompt and steer, approval responses, and an explicit interrupt params schema named `TurnInterruptParamsSchema`. Update `packages/protocol/src/index.ts` and `packages/protocol/package.json` exports so the new module is consumable from `@codex-mobile/protocol`. Extend `packages/protocol/src/bridge.ts` only where bridge-facing messages need concrete additions for cursor, event typing, or interrupt forwarding; do not leave interrupt as an untyped `payload: unknown` convention.</action>
  <verify>rg -n 'LiveSessionEventSchema|SessionConnectResponseSchema|TurnInterruptParamsSchema|cursor|mode|interrupt' packages/protocol/src/live-session.ts packages/protocol/src/bridge.ts packages/protocol/src/session.ts packages/protocol/src/index.ts packages/protocol/package.json</verify>
  <acceptance_criteria>
    - `packages/protocol/src/live-session.ts` exists and exports `LiveSessionEventSchema`
    - `packages/protocol/src/live-session.ts` exports `SessionConnectResponseSchema`
    - `packages/protocol/src/live-session.ts` exports `TurnInterruptParamsSchema`
    - `packages/protocol/src/index.ts` exports `./live-session`
    - `packages/protocol/package.json` exposes the `./live-session` subpath
  </acceptance_criteria>
  <done>The browser-live protocol is explicit, typed, and exportable across web, relay, and bridge code.</done>
</task>

<task type="auto">
  <name>Task 2: Add the relay browser WebSocket route, ownership router, and reconnect buffer</name>
  <files>apps/relay/src/browser/browser-registry.ts, apps/relay/src/browser/session-buffer.ts, apps/relay/src/browser/session-router.ts, apps/relay/src/routes/ws-browser.ts, apps/relay/src/server.ts</files>
  <read_first>apps/relay/src/routes/ws-bridge.ts, apps/relay/src/server.ts, packages/protocol/src/bridge.ts, packages/protocol/src/live-session.ts, .planning/phases/03-live-remote-ui-control/03-RESEARCH.md</read_first>
  <action>Create `apps/relay/src/browser/browser-registry.ts` to track browser sockets per `deviceSessionId` and `sessionId`. Create `apps/relay/src/browser/session-buffer.ts` to keep a bounded in-memory recent-event buffer keyed by session for reconnect/backfill. Create `apps/relay/src/browser/session-router.ts` that validates browser commands, resolves the owning bridge connection, forwards `session.attach`, `turn.send`, `approval.respond`, and explicit interrupt messages, and fans `session.history` plus typed live events back to the browser with cursor values. Create `apps/relay/src/routes/ws-browser.ts` as the browser-facing WebSocket upgrade route that validates ws tickets with `verifyWsTicket`, rejects reused tickets, and registers the socket with the session router. Update `apps/relay/src/server.ts` to register the new route alongside the existing bridge route without removing bridge behavior.</action>
  <verify>rg -n 'verifyWsTicket|cursor|interrupt|session.attach|session.history|turn.send|approval.respond|registerBrowser|sessionBuffer' apps/relay/src/browser/browser-registry.ts apps/relay/src/browser/session-buffer.ts apps/relay/src/browser/session-router.ts apps/relay/src/routes/ws-browser.ts apps/relay/src/server.ts</verify>
  <acceptance_criteria>
    - `apps/relay/src/routes/ws-browser.ts` contains `verifyWsTicket`
    - `apps/relay/src/routes/ws-browser.ts` contains `ws-ticket already used` or equivalent replay rejection
    - `apps/relay/src/browser/session-router.ts` references `session.history`
    - `apps/relay/src/browser/session-router.ts` references `turn.send` and `approval.respond`
    - `apps/relay/src/browser/session-router.ts` references an interrupt method name explicitly
    - `apps/relay/src/browser/session-buffer.ts` contains a bounded buffer constant or eviction logic
  </acceptance_criteria>
  <done>The relay can authenticate browser live connections, route them to the correct bridge session, and support reconnect backfill.</done>
</task>

<task type="auto">
  <name>Task 3: Add authenticated web routes for session listing, connect boot, and structured commands with unit coverage</name>
  <files>apps/web/app/api/sessions/route.ts, apps/web/app/api/sessions/[sessionId]/connect/route.ts, apps/web/app/api/sessions/[sessionId]/command/route.ts, apps/relay/tests/unit/ws-browser.test.ts, apps/web/tests/unit/session-connect-route.test.ts</files>
  <read_first>apps/web/auth.ts, apps/web/lib/device-session.ts, packages/protocol/src/live-session.ts, apps/relay/src/routes/ws-browser.ts, .planning/phases/03-live-remote-ui-control/03-VALIDATION.md</read_first>
  <action>Create `apps/web/app/api/sessions/route.ts` to return the currently available remote sessions for the authenticated paired device using only server-side credential checks. Create `apps/web/app/api/sessions/[sessionId]/connect/route.ts` as a `POST` route that validates the browser session, mints a fresh ws-ticket, resolves the relay target for the session, and returns `{ relayUrl, ticket, expiresAt, sessionId }`. Create `apps/web/app/api/sessions/[sessionId]/command/route.ts` as a `POST` route that validates structured command payloads from `packages/protocol/src/live-session.ts` and forwards prompt, steer, approval, and interrupt actions through the relay router when WebSocket-originated commands are not used directly. Add `apps/relay/tests/unit/ws-browser.test.ts` to cover ticket validation, replay rejection, and owner routing. Add `apps/web/tests/unit/session-connect-route.test.ts` to assert connect payload shape, auth rejection, and no URL credential leakage.</action>
  <verify>rg -n 'relayUrl|ticket|expiresAt|sessionId|interrupt|approval|user_mismatch|Authorization|describe\\(|it\\(' apps/web/app/api/sessions/route.ts apps/web/app/api/sessions/[sessionId]/connect/route.ts apps/web/app/api/sessions/[sessionId]/command/route.ts apps/relay/tests/unit/ws-browser.test.ts apps/web/tests/unit/session-connect-route.test.ts</verify>
  <acceptance_criteria>
    - `apps/web/app/api/sessions/[sessionId]/connect/route.ts` exports `POST`
    - `apps/web/app/api/sessions/[sessionId]/connect/route.ts` contains `relayUrl`, `ticket`, `expiresAt`, and `sessionId`
    - `apps/web/app/api/sessions/[sessionId]/command/route.ts` references `interrupt`
    - `apps/web/tests/unit/session-connect-route.test.ts` asserts no query-string credential transport
    - `apps/relay/tests/unit/ws-browser.test.ts` contains a replay or reused-ticket rejection assertion
  </acceptance_criteria>
  <done>The browser has authenticated server routes for session discovery and live-session boot, with relay/browser auth behavior covered by tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `vitest run --project phase-01-unit apps/relay/tests/unit/ws-browser.test.ts apps/web/tests/unit/session-connect-route.test.ts` succeeds
- [ ] `rg -n 'relayUrl|ticket|TurnInterruptParamsSchema|LiveSessionEventSchema' packages/protocol/src/live-session.ts apps/web/app/api/sessions/[sessionId]/connect/route.ts` returns matches
- [ ] Browser live auth uses ws-ticket semantics and no query-param credential transport
</verification>

<success_criteria>

- Browser-to-relay live transport is authenticated and typed
- Reconnect/backfill has an explicit contract
- Prompt, steer, approval, and interrupt actions are inspectable structured commands
- The relay stays aligned with the outbound-only bridge trust boundary
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-remote-ui-control/03-02-SUMMARY.md`
</output>
