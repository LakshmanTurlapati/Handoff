# Phase 3 Questions (Bulk Mode)

**Instructions:** Answer each question (numbered responses). Reply with "q1: answer" format. Say "done" when finished.

## q1-3: Timeline Structure
q1. Compact layout preference (vertical stack / card carousel / infinite scroll)?
q2. Event types priority (agent text > tools > approvals > system)?
q3. Truncation rules (fold old turns / search / jump-to-turn)?

## q4-6: Streaming Transport
q4. Update granularity (full re-render / diff patches / optimistic UI)?
q5. Interrupt semantics (clear queue / cancel token / soft abort)?
q6. Backpressure handling (pause stream / queue events / drop old)?

## q7-9: Controls UX
q7. Prompt input (bottom bar / slide-up / full-screen)?
q8. Steer button (during agent response / always visible / context-menu)?
q9. Interrupt placement (fab / header / gesture swipe)?

## q10-12: Polish
q10. Gestures (swipe to approve / long-press copy / tap expand)?
q11. Loading/error states (spinner types / retry logic / offline queue)?
q12. Accessibility (voiceover / high-contrast / screen size breakpoints)?

**Reply e.g. "q1: vertical stack" or "done"**
