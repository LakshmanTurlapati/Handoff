---
phase: 03-live-remote-ui-control
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - apps/web/app/session/[sessionId]/session-shell.tsx
  - apps/web/components/session/composer.tsx
  - apps/web/components/session/activity-card.tsx
  - apps/web/components/session/approval-card.tsx
  - apps/web/components/session/reconnect-banner.tsx
  - apps/web/components/session/jump-to-live.tsx
  - apps/web/lib/live-session/transport.ts
  - apps/web/lib/live-session/reducer.ts
  - apps/web/package.json
  - apps/web/tests/setup.ts
  - apps/web/tests/unit/session-shell.test.tsx
  - apps/web/tests/live-session-mobile.spec.ts
  - vitest.workspace.ts
autonomous: true
requirements:
  - SESS-04
  - SESS-05
  - LIVE-01
  - LIVE-04
must_haves:
  truths:
    - "The live mobile shell actually consumes the transport layer instead of remaining fixture-driven."
    - "Send Prompt, Steer, Interrupt, and approval actions all surface pending, success, and failure states in-context."
    - "Reconnect keeps the timeline visible, inserts a Reconnected marker, and restores live follow only when the user asks."
    - "The mobile UI remains readable and tappable on a phone-sized viewport while a live turn is running."
  artifacts:
    - path: "apps/web/app/session/[sessionId]/session-shell.tsx"
      provides: "integrated live session UI using transport and reducer state"
      contains: "Reconnected"
    - path: "apps/web/components/session/approval-card.tsx"
      provides: "inline approval request card with approve/deny/abort actions"
      contains: "Waiting for approval"
    - path: "apps/web/components/session/reconnect-banner.tsx"
      provides: "subtle reconnect state banner"
      contains: "Live connection lost. Reconnecting now."
    - path: "apps/web/lib/live-session/transport.ts"
      provides: "browser live-session connection management"
      exports: ["connectLiveSession", "sendSessionCommand"]
    - path: "apps/web/tests/live-session-mobile.spec.ts"
      provides: "mobile smoke coverage for the live session shell"
      contains: "Interrupt"
    - path: "vitest.workspace.ts"
      provides: "jsdom-capable test project for React session-shell tests"
      contains: "jsdom"
  key_links:
    - from: "apps/web/app/session/[sessionId]/session-shell.tsx"
      to: "apps/web/lib/live-session/transport.ts"
      via: "session shell boots and controls the live transport"
      pattern: "connectLiveSession"
    - from: "apps/web/components/session/approval-card.tsx"
      to: "apps/web/lib/live-session/transport.ts"
      via: "approval card dispatches structured decisions"
      pattern: "sendSessionCommand"
    - from: "apps/web/components/session/jump-to-live.tsx"
      to: "apps/web/lib/live-session/reducer.ts"
      via: "jump control restores follow mode"
      pattern: "followMode"
---

<objective>
Integrate the live transport into the mobile session shell and finish the phone-specific interaction contract for Phase 3.

Purpose: Turn the session shell and transport foundation into the actual remote-control experience promised by the roadmap: readable live progress, prompt/steer/interrupt controls, inline approvals, reconnect continuity, and small-screen usability.

Output: Connected session shell, transport hook, approval/reconnect/jump components, and automated coverage for the end-to-end mobile interaction surface.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-live-remote-ui-control/03-CONTEXT.md
@.planning/phases/03-live-remote-ui-control/03-RESEARCH.md
@.planning/phases/03-live-remote-ui-control/03-UI-SPEC.md
@.planning/phases/03-live-remote-ui-control/03-01-PLAN.md
@.planning/phases/03-live-remote-ui-control/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the session shell to the live transport and reconnect lifecycle</name>
  <files>apps/web/app/session/[sessionId]/session-shell.tsx, apps/web/lib/live-session/transport.ts, apps/web/lib/live-session/reducer.ts, apps/web/components/session/reconnect-banner.tsx, apps/web/components/session/jump-to-live.tsx</files>
  <read_first>apps/web/app/session/[sessionId]/session-shell.tsx, apps/web/lib/live-session/reducer.ts, packages/protocol/src/live-session.ts, .planning/phases/03-live-remote-ui-control/03-UI-SPEC.md, .planning/phases/03-live-remote-ui-control/03-02-PLAN.md</read_first>
  <action>Create `apps/web/lib/live-session/transport.ts` exporting `connectLiveSession` and `sendSessionCommand` helpers that boot the live session from `/api/sessions/[sessionId]/connect`, open the WebSocket, dispatch validated relay messages into the reducer, and reconnect with a fresh boot payload when the socket closes unexpectedly. Update `apps/web/app/session/[sessionId]/session-shell.tsx` to replace fixture-only state with the real transport, render `ReconnectBanner` while reconnecting, preserve the timeline during reconnect, and insert the literal separator label `Reconnected` when backfilled events arrive after a successful reconnect. Create `apps/web/components/session/reconnect-banner.tsx` and `apps/web/components/session/jump-to-live.tsx` to render the exact reconnect/error copy from `03-UI-SPEC.md` and restore follow mode only when the user taps `Jump to live`.</action>
  <verify>rg -n 'connectLiveSession|sendSessionCommand|Reconnected|Jump to live|Live connection lost\\. Reconnecting now\\.|followMode|reconnecting' apps/web/app/session/[sessionId]/session-shell.tsx apps/web/lib/live-session/transport.ts apps/web/lib/live-session/reducer.ts apps/web/components/session/reconnect-banner.tsx apps/web/components/session/jump-to-live.tsx</verify>
  <acceptance_criteria>
    - `apps/web/lib/live-session/transport.ts` exports `connectLiveSession`
    - `apps/web/lib/live-session/transport.ts` exports `sendSessionCommand`
    - `apps/web/app/session/[sessionId]/session-shell.tsx` contains `Reconnected`
    - `apps/web/components/session/reconnect-banner.tsx` contains the exact sentence `Live connection lost. Reconnecting now.`
    - `apps/web/components/session/jump-to-live.tsx` contains the exact label `Jump to live`
  </acceptance_criteria>
  <done>The session page consumes live relay data, survives reconnects, and respects user-controlled follow mode.</done>
</task>

<task type="auto">
  <name>Task 2: Finish prompt, steer, interrupt, approval, and error affordances for phone use</name>
  <files>apps/web/components/session/composer.tsx, apps/web/components/session/activity-card.tsx, apps/web/components/session/approval-card.tsx, apps/web/app/session/[sessionId]/session-shell.tsx</files>
  <read_first>.planning/phases/03-live-remote-ui-control/03-CONTEXT.md, .planning/phases/03-live-remote-ui-control/03-UI-SPEC.md, apps/web/components/session/composer.tsx, apps/web/components/session/activity-card.tsx</read_first>
  <action>Update `apps/web/components/session/composer.tsx` so the sticky composer expands upward for long text, submits `Send Prompt`, submits `Steer` as a distinct action without hiding it in a menu, and flips `Interrupt` into a pending label such as `Interrupting...` until the reducer receives a terminal interrupt outcome. Create `apps/web/components/session/approval-card.tsx` with inline approve, deny, and abort actions and the state-first header `Waiting for approval`. Update `apps/web/components/session/activity-card.tsx` so failure events render expanded error cards with retry/context actions when command metadata provides them. Keep all critical actions label-first, minimum 44px high, and readable on a narrow mobile viewport.</action>
  <verify>rg -n 'Send Prompt|Steer|Interrupt|Interrupting|Waiting for approval|retry|abort|deny|approve|44' apps/web/components/session/composer.tsx apps/web/components/session/activity-card.tsx apps/web/components/session/approval-card.tsx apps/web/app/session/[sessionId]/session-shell.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/session/composer.tsx` contains the exact labels `Send Prompt`, `Steer`, and `Interrupt`
    - `apps/web/components/session/composer.tsx` contains an interrupt pending label such as `Interrupting`
    - `apps/web/components/session/approval-card.tsx` contains the exact header `Waiting for approval`
    - `apps/web/components/session/approval-card.tsx` contains approve, deny, and abort controls
    - `apps/web/components/session/activity-card.tsx` references retry or context actions for errors
  </acceptance_criteria>
  <done>All remote-control actions are available in-context on the phone and expose honest pending/error states.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated coverage for connected session shell behavior on unit and mobile smoke paths</name>
  <files>apps/web/package.json, apps/web/tests/setup.ts, apps/web/tests/unit/session-shell.test.tsx, apps/web/tests/live-session-mobile.spec.ts, vitest.workspace.ts</files>
  <read_first>apps/web/app/session/[sessionId]/session-shell.tsx, apps/web/lib/live-session/transport.ts, apps/web/components/session/approval-card.tsx, vitest.workspace.ts, apps/web/package.json, .planning/phases/03-live-remote-ui-control/03-VALIDATION.md</read_first>
  <action>Update `apps/web/package.json` devDependencies to include `jsdom`, `@testing-library/react`, and `@testing-library/jest-dom` so `.tsx` session-shell tests can run locally. Add `apps/web/tests/setup.ts` importing `@testing-library/jest-dom/vitest`. Update `vitest.workspace.ts` to add a second project dedicated to Phase 3 web UI tests with `environment: "jsdom"` and an include glob that picks up `apps/web/tests/unit/**/*.test.tsx`. Create `apps/web/tests/unit/session-shell.test.tsx` to cover reconnect banner visibility, `Jump to live` follow-mode restoration, approval card placement near the live turn, and interrupt pending-state behavior using React Testing Library rather than raw reducer-only assertions. Create `apps/web/tests/live-session-mobile.spec.ts` as a Playwright mobile smoke spec that loads the session page, verifies the sticky composer controls are visible on a phone viewport, confirms historical turns stay readable while a live turn is active, and checks that reconnect and approval copy are not hidden behind desktop-only layout assumptions. Reuse the existing Playwright style from `apps/web/tests/auth-pairing.spec.ts` rather than inventing a new harness.</action>
  <verify>rg -n 'jsdom|@testing-library/react|@testing-library/jest-dom|Jump to live|Waiting for approval|Interrupt|reconnect|phone|viewport|describe\\(|test\\(' apps/web/package.json apps/web/tests/setup.ts apps/web/tests/unit/session-shell.test.tsx apps/web/tests/live-session-mobile.spec.ts vitest.workspace.ts</verify>
  <acceptance_criteria>
    - `apps/web/package.json` contains `jsdom`
    - `apps/web/package.json` contains `@testing-library/react`
    - `vitest.workspace.ts` contains `environment: "jsdom"`
    - `apps/web/tests/setup.ts` imports `@testing-library/jest-dom/vitest`
    - `apps/web/tests/unit/session-shell.test.tsx` contains `Jump to live`
    - `apps/web/tests/unit/session-shell.test.tsx` contains `Waiting for approval`
    - `apps/web/tests/unit/session-shell.test.tsx` asserts interrupt pending behavior
    - `apps/web/tests/live-session-mobile.spec.ts` contains `Interrupt`
    - `apps/web/tests/live-session-mobile.spec.ts` contains a mobile viewport assertion or setup
  </acceptance_criteria>
  <done>The connected mobile experience has regression coverage for the most failure-prone live interaction behaviors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `vitest run --project phase-01-unit --project phase-03-web apps/web/tests/unit/session-shell.test.tsx apps/web/tests/unit/live-session-reducer.test.ts` succeeds
- [ ] `npx playwright test apps/web/tests/live-session-mobile.spec.ts` succeeds or is documented behind the same live-server gate pattern used by existing mobile specs
- [ ] A phone-sized viewport shows Send Prompt, Steer, Interrupt, Jump to live, reconnect copy, and inline approval content without horizontal overflow
</verification>

<success_criteria>

- The live session page is connected to the relay and remains usable during reconnects
- Prompt, steer, interrupt, and approval are all available from the phone UI with honest state transitions
- The active turn, historical compression, and reconnect affordances match the approved design contract
- The phase outcome is a real remote-control experience rather than a static timeline
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-remote-ui-control/03-03-SUMMARY.md`
</output>
