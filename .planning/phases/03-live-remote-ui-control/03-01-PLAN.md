---
phase: 03-live-remote-ui-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/page.tsx
  - apps/web/app/session/[sessionId]/page.tsx
  - apps/web/app/session/[sessionId]/session-shell.tsx
  - apps/web/components/session/session-list.tsx
  - apps/web/components/session/turn-card.tsx
  - apps/web/components/session/activity-card.tsx
  - apps/web/components/session/composer.tsx
  - apps/web/lib/live-session/session-model.ts
  - apps/web/lib/live-session/reducer.ts
  - apps/web/tests/unit/live-session-reducer.test.ts
autonomous: true
requirements:
  - LIVE-02
  - LIVE-04
must_haves:
  truths:
    - "The authenticated mobile landing screen exposes available remote sessions without desktop-style chrome."
    - "The session view is turn-grouped and visually distinguishes assistant, tool, command, approval, system, and error activity."
    - "The active turn dominates the viewport while historical turns remain compressed but readable."
    - "The session shell includes a sticky bottom control rail with Send Prompt, Steer, and Interrupt labels even before live wiring lands."
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "authenticated session landing / chooser screen"
      contains: "No remote session yet"
    - path: "apps/web/app/session/[sessionId]/session-shell.tsx"
      provides: "client-side mobile live session shell"
      contains: "Jump to live"
    - path: "apps/web/components/session/activity-card.tsx"
      provides: "typed activity renderer for assistant/tool/command/approval/system/error cards"
      contains: "approval"
    - path: "apps/web/lib/live-session/reducer.ts"
      provides: "turn-centric reducer and follow-mode state transitions"
      exports: ["createInitialLiveSessionState", "liveSessionReducer"]
  key_links:
    - from: "apps/web/app/session/[sessionId]/page.tsx"
      to: "apps/web/app/session/[sessionId]/session-shell.tsx"
      via: "server-auth page renders the client live shell"
      pattern: "SessionShell"
    - from: "apps/web/app/session/[sessionId]/session-shell.tsx"
      to: "apps/web/lib/live-session/reducer.ts"
      via: "session shell derives UI state from the reducer"
      pattern: "liveSessionReducer"
    - from: "apps/web/components/session/turn-card.tsx"
      to: "apps/web/components/session/activity-card.tsx"
      via: "turn cards render typed activity cards"
      pattern: "ActivityCard"
---

<objective>
Build the authenticated mobile session shell, timeline structure, and structured activity renderer for Phase 3.

Purpose: Establish the phone-sized UI architecture before live transport lands so the transport work can plug into a stable reducer, card system, and session route shape instead of improvising UI structure afterward.

Output: Authenticated session landing screen, session page shell, turn-centric reducer, typed activity cards, and a sticky control rail scaffold that matches the approved UI contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-live-remote-ui-control/03-CONTEXT.md
@.planning/phases/03-live-remote-ui-control/03-RESEARCH.md
@.planning/phases/03-live-remote-ui-control/03-UI-SPEC.md
@apps/web/app/sign-in/page.tsx
@apps/web/app/pair/[pairingId]/page.tsx
@apps/web/app/pair/[pairingId]/pairing-claim-flow.tsx
@apps/web/auth.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the authenticated session landing page and session route shells</name>
  <files>apps/web/app/page.tsx, apps/web/app/session/[sessionId]/page.tsx, apps/web/components/session/session-list.tsx</files>
  <read_first>AGENTS.md, .planning/phases/03-live-remote-ui-control/03-CONTEXT.md, .planning/phases/03-live-remote-ui-control/03-UI-SPEC.md, apps/web/app/sign-in/page.tsx, apps/web/app/pair/[pairingId]/page.tsx, apps/web/auth.ts</read_first>
  <action>Create `apps/web/app/page.tsx` as the authenticated device landing screen that uses the same server-side auth gate pattern as the pairing route and renders a phone-first list of available remote sessions using a `SessionList` component. Use the exact empty-state heading `No remote session yet` and the exact body copy from `03-UI-SPEC.md`. Create `apps/web/app/session/[sessionId]/page.tsx` as a server component that validates the browser session, reads the `sessionId` param, and renders `SessionShell` with the session identifier plus an initial `connecting` state. In `apps/web/components/session/session-list.tsx`, render large-tap session rows with title, model, status, and turn count in a single-column mobile layout with no desktop sidebar or table chrome.</action>
  <verify>rg -n 'No remote session yet|Open Codex on your laptop|SessionShell|turnCount|model' apps/web/app/page.tsx apps/web/app/session/[sessionId]/page.tsx apps/web/components/session/session-list.tsx</verify>
  <acceptance_criteria>
    - `apps/web/app/page.tsx` contains the exact heading `No remote session yet`
    - `apps/web/app/page.tsx` contains the exact sentence `Open Codex on your laptop, connect the bridge, then choose a session to continue here.`
    - `apps/web/app/session/[sessionId]/page.tsx` contains `SessionShell`
    - `apps/web/components/session/session-list.tsx` contains `turnCount`
    - `apps/web/components/session/session-list.tsx` contains `model`
  </acceptance_criteria>
  <done>The web app has authenticated session entry points with phone-sized routing and empty-state behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Build the turn-centric session reducer and structured card system</name>
  <files>apps/web/app/session/[sessionId]/session-shell.tsx, apps/web/components/session/turn-card.tsx, apps/web/components/session/activity-card.tsx, apps/web/components/session/composer.tsx, apps/web/lib/live-session/session-model.ts, apps/web/lib/live-session/reducer.ts</files>
  <read_first>.planning/phases/03-live-remote-ui-control/03-RESEARCH.md, .planning/phases/03-live-remote-ui-control/03-UI-SPEC.md, apps/web/app/pair/[pairingId]/pairing-claim-flow.tsx</read_first>
  <action>Create `apps/web/lib/live-session/session-model.ts` with typed UI models for `assistant`, `tool`, `command`, `approval`, `system`, and `error` activity kinds plus per-turn state labels. Create `apps/web/lib/live-session/reducer.ts` exporting `createInitialLiveSessionState` and `liveSessionReducer` with explicit fields for `followMode`, `pendingInterrupt`, `connection`, `liveTurnId`, and ordered turns. In `apps/web/components/session/activity-card.tsx`, render distinct visual treatments keyed by activity kind, with approval and error states called out explicitly. In `apps/web/components/session/turn-card.tsx`, render state-first headers and collapsed historical turns that show only header plus preview while the active turn is expanded. In `apps/web/components/session/composer.tsx`, render a sticky bottom rail with the literal labels `Send Prompt`, `Steer`, and `Interrupt`, with `Interrupt` styled as destructive and minimum 44px control height. Wire these pieces together in `apps/web/app/session/[sessionId]/session-shell.tsx` using fixture-backed reducer state only; do not add live transport in this task.</action>
  <verify>rg -n 'assistant|tool|command|approval|system|error|followMode|pendingInterrupt|Send Prompt|Steer|Interrupt|Jump to live' apps/web/app/session/[sessionId]/session-shell.tsx apps/web/components/session/turn-card.tsx apps/web/components/session/activity-card.tsx apps/web/components/session/composer.tsx apps/web/lib/live-session/session-model.ts apps/web/lib/live-session/reducer.ts</verify>
  <acceptance_criteria>
    - `apps/web/lib/live-session/session-model.ts` contains all six activity kind literals `assistant`, `tool`, `command`, `approval`, `system`, and `error`
    - `apps/web/lib/live-session/reducer.ts` contains `followMode` and `pendingInterrupt`
    - `apps/web/components/session/composer.tsx` contains the exact labels `Send Prompt`, `Steer`, and `Interrupt`
    - `apps/web/app/session/[sessionId]/session-shell.tsx` contains `Jump to live`
    - `apps/web/components/session/turn-card.tsx` contains a state-first header field such as `stateLabel`
  </acceptance_criteria>
  <done>The session shell can render realistic structured turns and controls from local state without any transport dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit coverage for reducer grouping, follow mode, and pending interrupt transitions</name>
  <files>apps/web/tests/unit/live-session-reducer.test.ts</files>
  <read_first>apps/web/lib/live-session/reducer.ts, apps/web/lib/live-session/session-model.ts, .planning/phases/03-live-remote-ui-control/03-RESEARCH.md, .planning/phases/03-live-remote-ui-control/03-VALIDATION.md</read_first>
  <action>Create `apps/web/tests/unit/live-session-reducer.test.ts` covering these exact reducer behaviors: creating an active turn from a history snapshot, appending typed activities to the correct turn, pausing auto-follow when the user scrolls away, restoring follow mode when `Jump to live` is triggered, and leaving the turn in a pending-stop state until a terminal interrupt event is reduced. Use Vitest and import the real reducer exports rather than duplicating logic in test fixtures.</action>
  <verify>rg -n 'pending-stop|Jump to live|follow mode|pendingInterrupt|history snapshot|Vitest|describe\\(|it\\(' apps/web/tests/unit/live-session-reducer.test.ts</verify>
  <acceptance_criteria>
    - `apps/web/tests/unit/live-session-reducer.test.ts` contains `describe(`
    - `apps/web/tests/unit/live-session-reducer.test.ts` references `pendingInterrupt`
    - `apps/web/tests/unit/live-session-reducer.test.ts` contains `Jump to live`
    - `apps/web/tests/unit/live-session-reducer.test.ts` imports `liveSessionReducer`
    - `apps/web/tests/unit/live-session-reducer.test.ts` asserts that follow mode can pause and resume
  </acceptance_criteria>
  <done>The UI state model has automated coverage for the core phone-first timeline behaviors before transport is attached.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `vitest run --project phase-01-unit apps/web/tests/unit/live-session-reducer.test.ts` succeeds
- [ ] `rg -n 'Send Prompt|Steer|Interrupt|Jump to live' apps/web/app/session/[sessionId]/session-shell.tsx apps/web/components/session/composer.tsx` returns matches
- [ ] Historical turns render as collapsed previews while the active turn stays expanded
</verification>

<success_criteria>

- The app has an authenticated session landing flow and a stable session route shape
- The timeline is turn-grouped and activity-type aware
- The mobile shell already matches the approved control labels and hierarchy
- UI state is reducer-driven rather than assembled ad hoc in components
</success_criteria>

<output>
After completion, create `.planning/phases/03-live-remote-ui-control/03-01-SUMMARY.md`
</output>
