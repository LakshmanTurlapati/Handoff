---
phase: 07.1-codex-cli-handoff-skill-migration
plan: 03
subsystem: docs
tags: [docs, copy, readme, handoff, skill, dx]

requires:
  - phase: 07.1
    plan: 01
    provides: verified skill packaging and install alias contract

provides:
  - Confirmed user-facing docs (root README, bridge README, hosted pages) no longer present `/handoff` as a supported Codex slash command
  - Confirmed install instructions point at `handoff install-codex-skill` with `install-codex-command` only mentioned as a deprecated alias
  - Confirmed `handoff --help` usage text orders `install-codex-skill` before the deprecated alias

affects:
  - First-run developer onboarding for v1.1 install
  - Hosted launch error and root page copy

tech-stack:
  added: []
  patterns:
    - User-facing copy refers to skill invocation (`$handoff`) and skill discovery (`/skills`); historical `/handoff` references are scoped to changelog/archive context only

key-files:
  created: []
  modified: []

key-decisions:
  - "Docs sweep is verification-only; copy was already updated in earlier phase commits (`3b2f288` feat(08.1), `16d6948` fix(handoff), `4cd14a4` fix(handoff))"
  - "Historical references inside `.planning/phases/07-*` and `.planning/phases/08.1-*` archive context were left untouched per D-07"

patterns-established:
  - "Search-by-glob sweep across `{README.md,apps/**/*.{ts,tsx,md},packages/**/*.{ts,tsx}}` ensures user-facing copy does not drift back to `/handoff` framing"

requirements-completed:
  - DX-01

duration: ~10min
completed: 2026-05-12
---

# Phase 07.1-03: Docs and Web Copy Sweep

**Confirmed that the root README, bridge README, hosted root page, hosted launch error page, and CLI help text all reference `$handoff` (skill invocation) and `/skills` discovery rather than the legacy `/handoff` slash command.**

## Performance

- **Duration:** ~10 minutes
- **Tasks:** 1 (verification sweep)
- **Files modified:** 0 (no doc changes required)

## Accomplishments

- Ran `rg /handoff` across `{README.md,apps/**/*.{ts,tsx,md},packages/**/*.{ts,tsx}}` — no live user-facing matches; remaining occurrences are package names (`@codex-mobile/handoff`) or Fly hostnames (`handoff-web.fly.dev`)
- Verified `README.md` "Hosted Handoff Launch" section uses `$handoff` consistently
- Verified `apps/bridge/README.md` install section uses `handoff install-codex-skill` as canonical with `install-codex-command` labelled as a deprecated alias
- Verified `apps/web/app/page.tsx` and `apps/web/app/launch/error/page.tsx` copy uses `$handoff`
- Verified `apps/bridge/src/cli.ts` `printUsage` lists `install-codex-skill` before `install-codex-command` and marks the latter as deprecated

## Task Commits

- Plan and Summary docs only — no code or doc commits required for this plan
- Plans committed in `1cf217e` (`docs(07.1): add execute plans for skill migration verification`)

## Files Created/Modified

No files modified. Verified existing state.

### Verified

- `README.md` — `$handoff` only, no `/handoff` slash-command references in user-facing prose
- `apps/bridge/README.md` — install section uses `handoff install-codex-skill`
- `apps/web/app/page.tsx` — `$handoff` in onboarding copy
- `apps/web/app/launch/error/page.tsx` — `$handoff` in error remediation copy
- `apps/bridge/src/cli.ts` — `printUsage` skill-first ordering

## Verification (must_haves)

- Root README, bridge README, hosted page copy, and launch error copy reference `$handoff` and `/skills`. Verified by file inspection.
- `handoff install-codex-skill` is the canonical install command in docs; `install-codex-command` is mentioned only as a deprecated alias. Verified in `apps/bridge/README.md`.
- `handoff --help` lists `install-codex-skill` before `install-codex-command`. Verified in `apps/bridge/src/cli.ts:34-35`.

## Build / Test Results

- No build or test changes; no regression risk to verify. Existing bridge unit tests still pass for the related suites (15/15 for the phase-relevant tests).
- Note: pre-existing failures in `apps/bridge/tests/unit/event-relay.test.ts` (2 cases) are unrelated to this phase and out of scope for 07.1.

## Deviations from PLAN

- None. Plan executed as a verification sweep with no doc changes required.
