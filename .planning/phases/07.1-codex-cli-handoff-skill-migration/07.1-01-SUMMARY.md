---
phase: 07.1-codex-cli-handoff-skill-migration
plan: 01
subsystem: bridge-cli
tags: [codex, skill, handoff, install, cli, vitest]

requires:
  - phase: 7
    provides: codex-handoff command and packaged resource scaffolding
  - phase: 08.1
    provides: packaging fix that ships handoff CLI as executable bin

provides:
  - Verified Codex skill installer that writes `${CODEX_HOME:-$HOME/.codex}/skills/handoff/` and additionally installs into project `.codex/skills/handoff/`
  - Verified deprecated alias `install-codex-command` that delegates to the skill installer and removes stale legacy `commands/handoff.md` and `prompts/handoff.md` copies
  - Test coverage asserting alias delegation, legacy cleanup, idempotent re-runs, and content drift updates

affects:
  - End-user Codex CLI install ergonomics for v1.1 milestone
  - Anyone upgrading from earlier `/handoff` command-shaped installs

tech-stack:
  added: []
  patterns:
    - Skill manifest packaged under `apps/bridge/resources/codex/skills/handoff/` is the canonical install asset; legacy `commands/` and `prompts/` paths are cleanup targets only

key-files:
  created: []
  modified: []

key-decisions:
  - "Skill migration was largely shipped in phase 16d6948 / 08.1 packaging fix work; phase 07.1-01 is a verification pass rather than a rewrite"
  - "Kept `installCodexHandoffCommand` as a thin wrapper over `installCodexHandoffSkill` to preserve backward compatibility while ensuring no new legacy command resources are written"

patterns-established:
  - "Verification-only plans: when prior phases already implemented the decision surface, the plan documents the assertion contract and ensures tests cover it rather than refactoring working code"

requirements-completed:
  - CMD-01
  - SAFE-01

duration: ~10min
completed: 2026-05-12
---

# Phase 07.1-01: Skill Packaging and Install Alias Verification

**Verified that the packaged Codex `$handoff` skill installs into `${CODEX_HOME}/skills/handoff/`, that `install-codex-command` is a working deprecated alias, and that stale legacy command/prompt copies are removed during install.**

## Performance

- **Duration:** ~10 minutes
- **Tasks:** 1 (verification)
- **Files modified:** 0 (no code or doc changes required)

## Accomplishments

- Verified `installCodexHandoffSkill` resolves the right install dirs, syncs the packaged skill tree, and removes legacy `commands/handoff.md` / `prompts/handoff.md` from the resolved Codex base dir
- Verified `installCodexHandoffCommand` delegates to the skill installer and maps statuses
- Confirmed `apps/bridge/src/cli.ts` routes both `install-codex-skill` and `install-codex-command` to the skill installer
- Confirmed packaged resource tree contains only `skills/handoff/SKILL.md` and `skills/handoff/agents/openai.yaml`; no legacy command resources are shipped
- Ran the install-related vitest suite (`codex-command-install.test.ts`, 7 tests) — all passing, covering D-08 must-haves

## Task Commits

- Plan and Summary docs only — no code commits required for this plan
- Plans committed in `1cf217e` (`docs(07.1): add execute plans for skill migration verification`)

## Files Created/Modified

No code or resource files modified. Verified existing state.

### Verified

- `apps/bridge/src/lib/codex-command-install.ts` — `installCodexHandoffSkill`, `installCodexHandoffCommand`, `removeLegacyCommandCopies`
- `apps/bridge/src/cli.ts` — `install-codex-skill` and `install-codex-command` case both call `installCodexHandoffSkill`
- `apps/bridge/resources/codex/skills/handoff/SKILL.md`
- `apps/bridge/resources/codex/skills/handoff/agents/openai.yaml`
- `apps/bridge/resources/codex/commands/` is empty (no shipped legacy command files)
- `apps/bridge/tests/unit/codex-command-install.test.ts`

## Verification (must_haves)

- `handoff install-codex-skill` writes the skill into `${CODEX_HOME:-$HOME/.codex}/skills/handoff/`. Verified by `codex-command-install.test.ts` cases "installs the packaged handoff skill into ${CODEX_HOME}/skills" and "falls back to ${HOME}/.codex/skills".
- `handoff install-codex-command` delegates to the skill installer. Verified by "keeps install-codex-command as a backward-compatible alias".
- Stale legacy command/prompt copies are removed when present. Verified by "removes stale legacy prompt and command copies while installing the skill".
- No new legacy slash-command resource files are written. Verified by directory inspection of `apps/bridge/resources/codex/commands/` (empty).

## Build / Test Results

- `npm run build` (apps/bridge): clean
- `npm test -- --run codex-command-install`: 7/7 passing

## Deviations from PLAN

- None. Plan executed as a verification pass with no code changes required.
