---
phase: 07.1-codex-cli-handoff-skill-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bridge/src/cli/codex-handoff.ts
  - apps/bridge/src/cli.ts
  - apps/bridge/tests/unit/codex-handoff-command.test.ts
  - apps/bridge/tests/unit/codex-handoff-safety.test.ts
autonomous: true
requirements:
  - CMD-02
  - SAFE-01
must_haves:
  truths:
    - "`handoff codex-handoff` derives the active thread from `CODEX_THREAD_ID` (or explicit `--thread-id` flag) with no session-picker fallback."
    - "When the thread context cannot be resolved the command returns `missing_active_thread_context` exit code 1 with the repair guidance constant."
    - "Session id is derived from `CODEX_SESSION_ID` when present, otherwise falls back to the thread id (no new bridge state, no external lookups)."
  artifacts:
    - path: "apps/bridge/src/cli/codex-handoff.ts"
      provides: "thread-bound codex-handoff runner"
      contains: "missing_active_thread_context"
    - path: "apps/bridge/tests/unit/codex-handoff-command.test.ts"
      provides: "CODEX_THREAD_ID env-binding coverage"
      contains: "falls back to CODEX_THREAD_ID"
    - path: "apps/bridge/tests/unit/codex-handoff-safety.test.ts"
      provides: "fail-closed coverage for missing thread context"
      contains: "No session picker fallback"
---

<objective>
Verify the `handoff codex-handoff` command resolves the active thread and session entirely from supported environment bindings (CODEX_THREAD_ID, CODEX_SESSION_ID) plus explicit CLI flags, and that it fails closed with `missing_active_thread_context` when neither is available. This covers decisions D-04, D-05, and D-09. No new bridge state is introduced; the code already implements this contract — the plan asserts it stays implemented and is covered by tests.
</objective>

<verification>
- [ ] `apps/bridge/src/cli/codex-handoff.ts` reads `process.env.CODEX_THREAD_ID` / `process.env.CODEX_SESSION_ID` via `readContextValue` and returns `missing_active_thread_context` when no thread is resolvable.
- [ ] `HANDOFF_FAILURE_GUIDANCE.missing_active_thread_context` retains "No session picker fallback" repair guidance.
- [ ] `apps/bridge/src/cli.ts` codex-handoff branch passes `process.env.CODEX_THREAD_ID` and `process.env.CODEX_SESSION_ID` (with `--thread-id` / `--session-id` flag override) into the runner.
- [ ] Unit coverage exists for: (a) CODEX_THREAD_ID-only env path, (b) CODEX_SESSION_ID preference over thread id, (c) missing-thread fail-closed path.
- [ ] `npm run build` (apps/bridge) succeeds.
- [ ] `npm test` (apps/bridge) passes for `codex-handoff-command.test.ts` and `codex-handoff-safety.test.ts`.
</verification>
