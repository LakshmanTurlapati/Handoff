---
phase: 07.1-codex-cli-handoff-skill-migration
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - README.md
  - apps/bridge/README.md
  - apps/web/app/page.tsx
  - apps/web/app/launch/error/page.tsx
  - apps/bridge/src/cli.ts
autonomous: true
requirements:
  - DX-01
must_haves:
  truths:
    - "User-facing docs (root README, bridge README, hosted page copy, launch error copy) reference `$handoff` and `/skills`, not `/handoff` as a supported slash command."
    - "Install instructions point at `handoff install-codex-skill` as the canonical command, with `install-codex-command` only mentioned as a deprecated alias."
    - "`handoff --help` usage text reflects skill-first install ordering."
  artifacts:
    - path: "README.md"
      provides: "root project README"
      contains: "$handoff"
    - path: "apps/bridge/README.md"
      provides: "bridge CLI install + usage docs"
      contains: "install-codex-skill"
    - path: "apps/web/app/page.tsx"
      provides: "hosted root page copy"
      contains: "$handoff"
    - path: "apps/web/app/launch/error/page.tsx"
      provides: "hosted launch error copy"
      contains: "$handoff"
---

<objective>
Sweep user-facing docs and web copy to ensure none of them promise `/handoff` as the supported Codex slash command. Reference `$handoff` (skill invocation) and `/skills` discovery wherever an install or invocation surface is described. This covers decisions D-06 and D-07. Historical references inside changelog/archive context (under `.planning/phases/07-*`, `.planning/phases/08.1-*`) may stay untouched per D-07.
</objective>

<verification>
- [ ] `README.md` has no remaining references to `/handoff` as a Codex slash command in user-facing prose.
- [ ] `apps/bridge/README.md` install section uses `handoff install-codex-skill` as the canonical command and labels `install-codex-command` as a deprecated alias.
- [ ] `apps/web/app/page.tsx` and `apps/web/app/launch/error/page.tsx` user-facing copy uses `$handoff`.
- [ ] `apps/bridge/src/cli.ts` `printUsage` lists `install-codex-skill` before `install-codex-command` and marks the latter as deprecated.
- [ ] `rg -n "/handoff[^-]"` (excluding `.planning/`, build outputs, package names like `@codex-mobile/handoff`, and Fly hostnames like `handoff-web.fly.dev`) returns no live user-facing matches.
</verification>
