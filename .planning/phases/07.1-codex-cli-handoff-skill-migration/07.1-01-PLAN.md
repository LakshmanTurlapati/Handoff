---
phase: 07.1-codex-cli-handoff-skill-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bridge/src/lib/codex-command-install.ts
  - apps/bridge/src/cli.ts
  - apps/bridge/resources/codex/skills/handoff/SKILL.md
  - apps/bridge/resources/codex/skills/handoff/agents/openai.yaml
  - apps/bridge/tests/unit/codex-command-install.test.ts
autonomous: true
requirements:
  - CMD-01
  - SAFE-01
must_haves:
  truths:
    - "`handoff install-codex-skill` writes the packaged handoff skill to `${CODEX_HOME:-$HOME/.codex}/skills/handoff/`."
    - "`handoff install-codex-command` remains a working deprecated alias that delegates to the skill installer."
    - "Stale legacy `commands/handoff.md` and `prompts/handoff.md` copies are removed during install when present."
    - "No new legacy slash-command resource files are written by the installer."
  artifacts:
    - path: "apps/bridge/resources/codex/skills/handoff/SKILL.md"
      provides: "packaged Codex skill manifest"
      contains: "Use this skill only when the user explicitly invokes"
    - path: "apps/bridge/src/lib/codex-command-install.ts"
      provides: "skill installer with legacy cleanup"
      contains: "removeLegacyCommandCopies"
    - path: "apps/bridge/tests/unit/codex-command-install.test.ts"
      provides: "skill install + legacy cleanup + alias coverage"
      contains: "removes stale legacy prompt and command copies"
---

<objective>
Verify the install path packages the explicit `$handoff` Codex skill, keeps `install-codex-command` working as a deprecated alias that delegates to the skill installer, and removes any stale legacy command/prompt copies. This is a verification + small-edit pass over code that already targets the skill surface (decisions D-01, D-02, D-03, D-08).
</objective>

<verification>
- [ ] `apps/bridge/resources/codex/skills/handoff/SKILL.md` exists and references `$handoff` and `handoff codex-handoff --format json`.
- [ ] `apps/bridge/resources/codex/commands/` contains no `handoff.md` (no legacy command resources shipped).
- [ ] `installCodexHandoffSkill` writes the skill into `${CODEX_HOME}/skills/handoff/` and project `.codex/skills/handoff/` and calls `removeLegacyCommandCopies` for the codex base dir.
- [ ] `installCodexHandoffCommand` delegates to `installCodexHandoffSkill` and maps statuses (`skill_installed -> command_installed`, etc.).
- [ ] CLI handler maps both `install-codex-skill` and `install-codex-command` cases to the skill installer.
- [ ] Unit tests cover: skill written, alias delegation, legacy prompt/command files removed.
- [ ] `npm run build` (apps/bridge) succeeds.
- [ ] `npm test` (apps/bridge) passes for `codex-command-install.test.ts`.
</verification>
