---
phase: 19-distribution-publishing-skill-rewire
plan: 04
type: execute
wave: 4
depends_on: [19-01, 19-02, 19-03]
files_modified:
  - apps/achilles-terminal/scripts/check-deletion-reachability.sh
  - apps/achilles/ (DELETE - 132 files Electron tree)
  - apps/achilles-cli/src/commands/launch.ts (DELETE)
  - apps/achilles-cli/src/commands/launch.test.ts (DELETE)
  - apps/achilles-cli/ (DELETE - entire workspace after reachability passes)
autonomous: false
requirements: [DIST-03, DIST-04]
tags: [post-publish, cleanup, electron-tree-delete, publish-then-cut]

must_haves:
  truths:
    - "LOOP-02 byte-for-byte invariant: voice packages + companion.md unchanged"
    - "INIT-07 static-import budget held: cli.ts top-level static imports stay {node:fs/promises, node:url, node:path}"
    - "No emojis (CLAUDE.md global)"
    - "Publish-then-cut ordering enforced: apps/achilles + apps/achilles-cli/src/commands/launch.ts deletion lands in commit B AFTER npm view achilles@1.3.0 confirms publish success (D-08/D-09)"
    - "Reachability check passes: zero importers of apps/achilles-cli/** remain from outside the directory (RESEARCH §Pitfall 10)"
    - "Atomic deletion: commit B is a pure deletion commit with no edits; rollback story is git revert HEAD"
    - "Operator confirmation gate: Plan 04 runs ONLY after operator confirms npm view achilles@1.3.0 + npm view @achilles/cli-linux-x64@1.3.0 + npm view @achilles/cli-linux-arm64@1.3.0 + npm view @achilles/cli-win32-x64@1.3.0 + npm view @achilles/achilles-skill@1.3.0 all succeed"
    - "Full workspace typecheck + lint + test green after deletion (no broken imports)"
  artifacts:
    - path: "apps/achilles-terminal/scripts/check-deletion-reachability.sh"
      provides: "Pre-deletion 4-variant grep + root package.json check (RESEARCH §Pitfall 10)"
      min_lines: 40
    - path: "(deletion target)"
      provides: "apps/achilles/ directory ABSENT - Electron tree 132 files removed"
    - path: "(deletion target)"
      provides: "apps/achilles-cli/src/commands/launch.ts + launch.test.ts ABSENT"
    - path: "(deletion target)"
      provides: "apps/achilles-cli/ directory ABSENT (if reachability check passes)"
  key_links:
    - from: "apps/achilles-terminal/scripts/check-deletion-reachability.sh"
      to: "Plan 04 commit B precondition"
      via: "exit 0 required before deletion proceeds"
      pattern: "exit \"\\$EXIT\""
    - from: "operator publish-success verification"
      to: "Plan 04 execution"
      via: "checkpoint:human-verify gate; operator confirms 5 npm view assertions pass"
      pattern: "npm view achilles@1.3.0"
---

<objective>
Execute the publish-then-cut deletion (commit B per D-08/D-09) AFTER the operator confirms npm view achilles@1.3.0 + the 4 sub-packages (@achilles/cli-linux-x64, @achilles/cli-linux-arm64, @achilles/cli-win32-x64, @achilles/achilles-skill) succeed on the registry. Creates the reachability audit script (scripts/check-deletion-reachability.sh per RESEARCH §Pitfall 10) that 4-variant-greps for any remaining importers of apps/achilles-cli/** from outside the directory; atomically deletes apps/achilles/ (Electron tree, 132 files) + apps/achilles-cli/src/commands/launch.ts + launch.test.ts; if the reachability script exits 0, atomically deletes the entire apps/achilles-cli/ workspace; commit lands as a SEPARATE pure-deletion commit so a publish failure does not orphan the workspace.

Purpose: Close the publish-then-cut sequencing per CONTEXT.md D-08 + D-09 + RESEARCH §Pattern 1 (two commits, two distinct work bodies). Without this, the v1.2 Electron .app tree + the v1.2 detached-stdio launcher path remain in the monorepo - confusing future readers and providing a recoverable artifact path that contradicts the v1.3 architecture. Plan 04 is the operator-triggered closure: it's the one place in Phase 19 that requires HUMAN VERIFICATION (autonomous: false) because the operator must confirm the npm registry state before deletion proceeds.

Output: 1 NEW script (check-deletion-reachability.sh), 1 commit (commit B per RESEARCH §Pattern 1) deleting apps/achilles/ + launch.ts + launch.test.ts + (conditional on reachability) apps/achilles-cli/. The plan is NOT autonomous: the first task is a checkpoint:human-verify gate where the operator confirms publish success via npm view assertions; only then does the deletion sequence proceed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-CONTEXT.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-VALIDATION.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-01-SUMMARY.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-02-SUMMARY.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-03-SUMMARY.md

<interfaces>
<!-- The 4-variant grep recipe + deletion targets -->

From RESEARCH §Pitfall 10 (the reachability check script recipe):
```
grep -rn "from ['\"]\\.\\./achilles-cli" --include="*.ts" --include="*.tsx" --include="*.js" --include="*.mjs" .
grep -rn "from ['\"]@achilles/achilles-cli" --include="*.ts" --include="*.tsx" --include="*.js" --include="*.mjs" .
grep -rn "import.*achilles-cli" --include="*.ts" --include="*.tsx" --include="*.js" --include="*.mjs" .
grep -rn "require.*achilles-cli" --include="*.ts" --include="*.tsx" --include="*.js" --include="*.mjs" .
# PLUS: grep root package.json for residual apps/achilles-cli/ references
```

From PATTERNS.md check-deletion-reachability.sh section (full skeleton lines 242-289 verbatim):
- Single bash script
- set -euo pipefail
- cd to git rev-parse --show-toplevel
- 4 grep variants + root package.json grep
- EXIT=0 baseline; bump to 1 if any grep returns matches
- Final echo + exit "$EXIT"

Deletion targets per CONTEXT.md D-08 + RESEARCH §State of the Art deprecated list:
- apps/achilles/ (entire Electron tree, 458 total tree files / 95 src files per CONTEXT.md "458 files"; per ROADMAP "132 files" - the lower count is the production src, the higher is total tree including build artifacts that gitignore covers but the directory does exist)
- apps/achilles-cli/src/commands/launch.ts (179 LOC)
- apps/achilles-cli/src/commands/launch.test.ts
- apps/achilles-cli/ (entire workspace, contingent on reachability)

Post-deletion verification suite:
- npm test --workspaces --if-present (all workspace tests still green)
- npm run typecheck --workspaces --if-present (no broken imports)
- npm run lint --workspaces --if-present (no broken eslint configs)
- npm run build --workspaces --if-present (no broken builds)

Operator publish-confirmation commands (the checkpoint:human-verify gate):
- npm view achilles@1.3.0 version
- npm view @achilles/cli-linux-x64@1.3.0 version
- npm view @achilles/cli-linux-arm64@1.3.0 version
- npm view @achilles/cli-win32-x64@1.3.0 version
- npm view @achilles/achilles-skill@1.3.0 version
All 5 must return non-empty "1.3.0" strings.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator publish-success gate (CHECKPOINT)</name>
  <what-built>
    Plan 01 + Plan 02 + Plan 03 produced "commit A" - the publish-ready state including the achilles-release.yml workflow. By the time Plan 04 executes, the operator has either pushed the `v1.3.0` tag OR triggered workflow_dispatch in the GH Actions UI, and the workflow has run to completion against commit A's HEAD SHA. The workflow:
    - Built 3 platform-binary tarballs on matrix.os runners
    - Ran the 2 gate scripts (check-source-of-truth.mjs + check-tarball-no-secrets.mjs)
    - Published 5 packages sequentially (3 platform siblings + achilles-skill + parent achilles LAST)
    - Waited 30s for CDN propagation
    - Verified all 5 packages on the registry via npm view assertions
    - Ran the macOS smoke job: `bunx achilles@1.3.0 --version` exited 0

    This checkpoint EXISTS because the deletion in Task 2/3 below is irreversible mid-Phase. The operator MUST verify the registry state on the operator's own terminal before deletion proceeds.
  </what-built>
  <how-to-verify>
    Run all 5 of the following commands from your local shell (NOT inside the running Phase 19 session). Each must return a non-empty "1.3.0" string:

    1. `npm view achilles@1.3.0 version` -> expected output: `1.3.0`
    2. `npm view @achilles/cli-linux-x64@1.3.0 version` -> expected output: `1.3.0`
    3. `npm view @achilles/cli-linux-arm64@1.3.0 version` -> expected output: `1.3.0`
    4. `npm view @achilles/cli-win32-x64@1.3.0 version` -> expected output: `1.3.0`
    5. `npm view @achilles/achilles-skill@1.3.0 version` -> expected output: `1.3.0`

    Additionally confirm the macos-smoke job in the GH Actions UI for the most recent achilles-release.yml run shows a green checkmark.

    If ANY of the 5 commands returns 404 or empty, the publish did not fully succeed and you MUST NOT proceed with deletion. Instead:
    - Inspect the GH Actions workflow logs for which publish step failed
    - Decide: (a) rerun the workflow (idempotent; the JS-DevTools/npm-publish action or the raw `npm publish` step both soft-error on republish of an already-published version), or (b) rollback via `npm deprecate achilles@1.3.0 "v1.3.0 published in error - use 1.3.1"` (npm policy: unpublish is only allowed within 72h with no dependents)
    - Address the failure; then re-run this checkpoint

    Per CONTEXT.md D-09: a publish failure leaves the workspace at commit A's HEAD with the Electron tree still in place as a recoverable artifact - DO NOT proceed with deletion in that case.

    PER CLAUDE.md global: do not run applications automatically. The npm view commands are pure registry lookups (no application launch), so they're appropriate to run manually as part of this verification.
  </how-to-verify>
  <resume-signal>Type "publish-confirmed" once all 5 npm view assertions succeed AND the GH Actions macos-smoke job shows green. Type "publish-failed" with a brief failure summary if any assertion fails (Plan 04 will halt).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create check-deletion-reachability.sh + run it to gate the deletion</name>
  <files>apps/achilles-terminal/scripts/check-deletion-reachability.sh</files>
  <read_first>
    - .planning/phases/19-distribution-publishing-skill-rewire/19-CONTEXT.md (D-08 single-commit cleanup; D-09 separate commit; reachability check via grep)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md (§Pitfall 10 4-variant grep recipe; §Open Question 6 commit B reachability + dynamic check)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md (check-deletion-reachability.sh section - full skeleton lines 242-289 verbatim)
    - Plan 03 SUMMARY (confirms root package.json scripts were repathed to apps/achilles-terminal/scripts/ - so no residual apps/achilles-cli/scripts/ references should remain)
    - apps/achilles-cli/src/ directory listing (verify what currently lives there - per CONTEXT.md "init.ts, install-skill.ts, latency.ts, transcripts.ts and paired test files are likely all dead post-v1.3" - the reachability script confirms)
  </read_first>
  <behavior>
    - check-deletion-reachability.sh exits 0 when zero importers of achilles-cli remain anywhere in the repo
    - Script runs 5 grep variants per RESEARCH §Pitfall 10:
      1. Relative imports: `from "../achilles-cli"`
      2. Scoped imports: `from "@achilles/achilles-cli"`
      3. Dynamic imports: any `import.*achilles-cli`
      4. CommonJS require: any `require.*achilles-cli`
      5. Root package.json: any line containing `apps/achilles-cli`
    - Script aggregates exit codes (EXIT=0 baseline; bump to 1 on any match)
    - Script outputs structured status lines (no emojis) so the operator can read them
  </behavior>
  <action>
    Per PATTERNS.md check-deletion-reachability.sh section lines 242-289 + RESEARCH §Pitfall 10.

    1. Create apps/achilles-terminal/scripts/check-deletion-reachability.sh with the verbatim skeleton from PATTERNS.md lines 242-289:
       - Shebang: `#!/usr/bin/env bash`
       - `set -euo pipefail`
       - `cd "$(git rev-parse --show-toplevel)"`
       - EXIT=0 baseline
       - Echo + grep + EXIT bump for each of the 5 variants
       - Final echo + exit "$EXIT"

    2. Make executable: `chmod +x apps/achilles-terminal/scripts/check-deletion-reachability.sh`.

    3. Run the script: `bash apps/achilles-terminal/scripts/check-deletion-reachability.sh`. Capture the exit code.

       Two possible outcomes:
       - **Exit 0:** zero importers remain. apps/achilles-cli/ is fully reachable-dead. Proceed to Task 3 with the full deletion (Plan 04 Task 3 deletes apps/achilles + launch.ts + the full apps/achilles-cli/ workspace).
       - **Exit 1+:** at least one importer remains. Inspect the grep output to identify the residual reference. Two possible remediations:
         - (a) The residual reference is in a file that should ALSO be deleted (e.g., a test file referencing the v1.2 launcher) - resolve by extending Task 3's deletion list
         - (b) The residual reference is in a file that should STAY (e.g., a doc page or generated artifact) - resolve by adding a follow-up task to repath the import to use the new apps/achilles-terminal path

       Per CONTEXT.md D-09: if the reachability check fails AND the resolution path is ambiguous, halt Plan 04 and surface the failure to the operator for guidance.

    4. NO emojis in the script output (CLAUDE.md global). Use ASCII status prefixes like "[reachability] OK" or "[reachability] FAIL".
  </action>
  <verify>
    <automated>bash apps/achilles-terminal/scripts/check-deletion-reachability.sh</automated>
    File exists + executable: `[[ -x apps/achilles-terminal/scripts/check-deletion-reachability.sh ]]` returns 0.
    5 grep variants present: `grep -cE '\\.\\./achilles-cli|@achilles/achilles-cli|import\\..*achilles-cli|require.*achilles-cli|apps/achilles-cli' apps/achilles-terminal/scripts/check-deletion-reachability.sh` returns >=5.
    Script structure: `grep -c 'set -euo pipefail' apps/achilles-terminal/scripts/check-deletion-reachability.sh` returns 1.
    No emoji: `LC_ALL=C grep -rP '[\x80-\xff]' apps/achilles-terminal/scripts/check-deletion-reachability.sh` returns nothing.
  </verify>
  <done>
    - apps/achilles-terminal/scripts/check-deletion-reachability.sh created >=40 LOC.
    - Script is executable.
    - Script exits 0 OR the failure is captured and resolved before Task 3 proceeds.
    - If exit 0: full apps/achilles-cli/ deletion is sanctioned for Task 3.
    - If exit non-zero: operator-resolvable diagnostic printed; Plan 04 halts pending guidance.
    - No emojis.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atomic deletion of apps/achilles + launch.ts + (conditional) apps/achilles-cli (commit B)</name>
  <files>apps/achilles/ (DELETE recursive), apps/achilles-cli/src/commands/launch.ts (DELETE), apps/achilles-cli/src/commands/launch.test.ts (DELETE), apps/achilles-cli/ (DELETE recursive IF Task 2 reachability check exited 0)</files>
  <read_first>
    - .planning/phases/19-distribution-publishing-skill-rewire/19-CONTEXT.md (D-08 single cleanup commit; D-09 separate from commit A; rollback story via git revert HEAD)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md (§Pattern 1 publish-then-cut diagram; §Runtime State Inventory rows - confirms no live consumers post-cut)
    - Plan 04 Task 2 output (the reachability script exit code + any failure diagnostic)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md (Pattern Assignments DELETED section line 30 - the 4 deletion targets)
    - apps/achilles/ directory listing (confirm Electron tree presence - 132 src files per ROADMAP; 458 tree files per CONTEXT.md including build artifacts)
    - apps/achilles-cli/ directory listing (confirm 14 src files per CONTEXT.md + paired tests + scripts/)
  </read_first>
  <behavior>
    - After Task 3, apps/achilles/ directory is absent
    - After Task 3, apps/achilles-cli/src/commands/launch.ts + launch.test.ts are absent
    - IF Task 2 exited 0: after Task 3, apps/achilles-cli/ directory is absent in its entirety
    - IF Task 2 exited non-zero: Task 3 deletes only apps/achilles + launch.ts + launch.test.ts (the unambiguous deletion targets); apps/achilles-cli remainder is documented for follow-up
    - Post-deletion workspace tests are GREEN (no broken imports - verified by Plan 03 Task 2's root package.json repath PLUS Plan 02 Task 3's install-skill port PLUS the reachability audit)
    - Post-deletion typecheck + lint + build all pass on the surviving workspaces
    - The commit is a PURE DELETION commit with NO file edits (per RESEARCH §Pattern 1 diagram - "M (none -- pure deletion commit, no edits)")
  </behavior>
  <action>
    Per CONTEXT.md D-08/D-09 + RESEARCH §Pattern 1 + PATTERNS.md DELETED section. This task lands as a SEPARATE git commit from commit A's earlier plans.

    1. Verify pre-state: Plan 04 Task 1 (operator gate) returned "publish-confirmed". Plan 04 Task 2 reachability script exit code is captured.

    2. Atomic deletions (pure deletion commit; no edits):
       - `git rm -r apps/achilles/` (the Electron tree - 132 src files / 458 tree files including build artifacts)
       - `git rm apps/achilles-cli/src/commands/launch.ts`
       - `git rm apps/achilles-cli/src/commands/launch.test.ts`
       - IF Task 2 exited 0: `git rm -r apps/achilles-cli/` (the entire workspace)
       - IF Task 2 exited non-zero: STOP HERE; do NOT delete apps/achilles-cli/. Surface a Plan 04 SUMMARY note that the workspace remainder requires follow-up resolution.

    3. Verify the post-deletion state is clean:
       - `npm ci --include=optional --force` exits 0 (workspace resolution still valid)
       - `npm run typecheck --workspaces --if-present` exits 0 (no broken imports anywhere)
       - `npm run lint --workspaces --if-present` exits 0 (no broken eslint configs)
       - `npm test --workspace apps/achilles-terminal -- --pool=forks` full suite exits 0 (~127 tests with Plan 02 additions; should be exactly the same surface as before deletion since nothing the surviving terminal workspace depends on is being deleted)
       - `npm run check:dist` exits 0 (verifies the root scripts repath from Plan 03 still resolves; the source-of-truth + secret-scan scripts in apps/achilles-terminal/scripts/ are unaffected by the deletion)
       - `npm run build --workspaces --if-present` exits 0 (no broken builds)

    4. Commit B is a PURE DELETION commit. The commit message follows the v1.3 phase convention: `chore(19): publish-then-cut delete v1.2 Electron tree + launcher (post-publish cleanup per D-08/D-09)`. The commit body references:
       - Plan 04 Task 1 operator-confirmed publish success
       - Plan 04 Task 2 reachability check result (exit code 0 -> full apps/achilles-cli/ delete; exit non-zero -> partial delete only)
       - Rollback story: `git revert HEAD` undoes the whole cut atomically per RESEARCH §Pattern 1

    5. If any post-deletion verification command fails: HALT, do NOT push the commit, surface the failure to the operator. The recovery path is `git reset --hard HEAD~` to restore the deleted files and investigate the missed importer.

    6. Per CLAUDE.md global: never run applications automatically. The verification commands (npm test, typecheck, lint, build, check:dist) are NOT application launches - they're verification gates and they're appropriate to run automatically as part of this task.

    7. NO emojis in commit message or any file output.
  </action>
  <verify>
    <automated>npm test --workspace apps/achilles-terminal -- --pool=forks</automated>
    apps/achilles absent: `[[ ! -d apps/achilles ]]` returns 0.
    launch.ts absent: `[[ ! -f apps/achilles-cli/src/commands/launch.ts ]]` returns 0.
    launch.test.ts absent: `[[ ! -f apps/achilles-cli/src/commands/launch.test.ts ]]` returns 0.
    Conditional apps/achilles-cli absence: if Task 2 exited 0, then `[[ ! -d apps/achilles-cli ]]` returns 0.
    Typecheck: `npm run typecheck --workspaces --if-present` exits 0.
    Lint: `npm run lint --workspaces --if-present` exits 0.
    Build: `npm run build --workspaces --if-present` exits 0.
    Check:dist still works: `npm run check:dist` exits 0 (Plan 03's repath survives the deletion).
    LOOP-02 invariant: `git diff HEAD~ -- packages/voice-protocol packages/voice-stt packages/voice-tts packages/claude-code-bridge packages/achilles-skill/skill/prompts/companion.md` returns empty (deletion did not touch voice packages or companion.md).
    INIT-07 invariant: top-level static imports in apps/achilles-terminal/src/cli.ts unchanged (no cli.ts changes in Plan 04).
    Pure deletion commit: `git show HEAD --stat | grep -cE '^\\s+[0-9]+ files? changed'` returns 1 AND `git show HEAD --stat | grep -cE '\\+\\+\\+' | head -1` returns 0 (no inserted lines - only deletions).
  </verify>
  <done>
    - apps/achilles/ directory deleted (Electron tree gone).
    - apps/achilles-cli/src/commands/launch.ts + launch.test.ts deleted.
    - IF Task 2 reachability passed: apps/achilles-cli/ workspace fully deleted.
    - npm ci + typecheck + lint + test + build + check:dist all GREEN on the surviving workspaces.
    - Commit is a PURE DELETION commit with zero edits per RESEARCH §Pattern 1.
    - Commit message references operator publish confirmation + reachability check result + rollback path.
    - LOOP-02 + INIT-07 invariants unchanged.
    - M-10 satisfied: publish-then-cut ordering enforced (deletion lands AFTER npm view confirms publish).
    - D-08/D-09 satisfied: single cleanup commit landed as SEPARATE commit (not the same as commit A).
    - No emojis.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> deletion sequence | Plan 04's deletion is irreversible mid-Phase; operator confirmation gate (Task 1) is the trust handoff between the auto + manual stages |
| pre-deletion grep -> deletion authorization | The 4-variant grep + root package.json grep in check-deletion-reachability.sh decides whether the apps/achilles-cli workspace gets fully deleted vs partial; a missed grep variant could mask a live importer |
| commit B -> commit A | Plan 04 commit B MUST land AFTER commit A's publish completes (D-08); ordering violation would orphan the workspace on publish failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Tampering | check-deletion-reachability.sh misses a live importer due to grep pattern blind spot (RESEARCH §Pitfall 10) | mitigate | 4-variant grep covers relative imports, scoped imports, dynamic imports, CommonJS require; PLUS root package.json grep; PLUS belt-and-braces dynamic check via `npm run typecheck --workspaces --if-present` + `npm run build --workspaces --if-present` as post-deletion verification (Pitfall 10 dynamic-check recommendation) |
| T-19-04-02 | Repudiation | Plan 04 executes BEFORE operator confirms publish success | mitigate | Task 1 is checkpoint:human-verify gate="blocking"; operator must type "publish-confirmed" before Task 2 proceeds; per D-08/D-09 a publish failure leaves the Electron tree in place as recoverable artifact |
| T-19-04-03 | Tampering | Plan 04 commit modifies workspace edges that break v1.3 builds | mitigate | Post-deletion verification suite (typecheck + lint + build + test on all surviving workspaces) gates the commit; any failure halts Plan 04 and reverts via `git reset --hard HEAD~`; pure-deletion commit shape (no edits) per RESEARCH §Pattern 1 keeps the rollback story atomic (`git revert HEAD`) |
| T-19-04-04 | DoS | Deletion of apps/achilles-cli/scripts/ breaks root package.json check:dist alias | mitigate | Plan 03 Task 2 repathed root scripts to apps/achilles-terminal/scripts/ in commit A; verified before commit A merged; Plan 04 Task 3 verifies via `npm run check:dist` exits 0 post-deletion |
| T-19-04-05 | Information Disclosure | Lingering test fixtures in apps/achilles-cli/ contain example keys or secrets | accept | Plan 03 Task 1 tarball secret-scan (TNS6 case) already verified the SKILL.md + companion.md are clean; any secret-shaped fixture in apps/achilles-cli/ tests would also fail the secret-scan; deletion eliminates the exposure surface entirely |
| T-19-04-06 | Elevation of Privilege | Plan 04 runs in autonomous mode without operator gate | mitigate | `autonomous: false` frontmatter; Task 1 explicitly checkpoint:human-verify gate="blocking"; execute-phase pauses here until operator confirms |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | Plan 04 installs zero new packages; the reachability script uses bash-native grep (no external deps) |
</threat_model>

<verification>
- Operator confirmed "publish-confirmed" before Task 2 proceeded
- apps/achilles-terminal/scripts/check-deletion-reachability.sh exists, is executable, exits with the captured outcome
- apps/achilles/ directory absent post-Plan 04
- apps/achilles-cli/src/commands/launch.ts absent post-Plan 04
- apps/achilles-cli/src/commands/launch.test.ts absent post-Plan 04
- IF reachability passed: apps/achilles-cli/ directory absent post-Plan 04
- `npm test --workspace apps/achilles-terminal -- --pool=forks` exits 0 (~127 tests still green - no surviving consumer of the deleted code)
- `npm run typecheck --workspaces --if-present` exits 0 (no broken imports)
- `npm run lint --workspaces --if-present` exits 0
- `npm run build --workspaces --if-present` exits 0
- `npm run check:dist` exits 0 (root scripts repath from Plan 03 survives)
- Commit B is a PURE DELETION commit (no inserted lines)
- LOOP-02 invariant: voice packages + companion.md unchanged
- INIT-07 invariant: cli.ts top-level static imports unchanged
- No emojis anywhere
</verification>

<success_criteria>
- M-10 met: publish-then-cut ordering enforced; deletion lands in commit B AFTER operator confirms npm view achilles@1.3.0 succeeds
- M-11 met (Plan 04 contribution): LOOP-02 invariants held; voice packages + companion.md unchanged across the deletion
- M-12 met (Plan 04 contribution): INIT-07 static-import budget held; no cli.ts changes
- D-08 satisfied: single cleanup commit AFTER npm publish succeeds for achilles@1.3.0 + 4 sub-packages
- D-09 satisfied: deletion lands as SEPARATE commit from commit A; rollback via git revert HEAD
- DIST-03 / DIST-04 satisfied at the architecture level: post-Plan 04 monorepo state matches the v1.3 terminal-only model with no Electron-era surface remaining
- Phase 19 complete: 4/4 plans land; Phase 20 (RBS asciicasts) can begin
</success_criteria>

<output>
Create `.planning/phases/19-distribution-publishing-skill-rewire/19-04-SUMMARY.md` when done capturing:
- Operator publish-confirmation outcome (the 5 npm view assertion results)
- check-deletion-reachability.sh exit code + any residual importer notes
- Final deletion list (apps/achilles + launch.ts + launch.test.ts + conditional apps/achilles-cli/)
- Diff stats for commit B (files deleted; zero files modified or added beyond the new script)
- Post-deletion verification suite outcomes (test + typecheck + lint + build + check:dist all GREEN)
- LOOP-02 + INIT-07 invariants held confirmation
- Phase 19 wrap: 4 plans complete; Phase 20 RBS asciicast gate ready to begin
</output>
