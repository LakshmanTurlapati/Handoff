---
phase: 19-distribution-publishing-skill-rewire
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified:
  - apps/achilles-terminal/scripts/check-tarball-no-secrets.mjs
  - apps/achilles-terminal/scripts/check-tarball-no-secrets.test.mjs
  - apps/achilles-terminal/tests/check-source-of-truth.test.ts
  - apps/achilles-terminal/package.json
  - package.json
  - .github/workflows/achilles-release.yml
autonomous: true
requirements: [DIST-03, DIST-04, DIST-06]
tags: [distribution, ci-publish, source-of-truth, secret-scan]

must_haves:
  truths:
    - "LOOP-02 byte-for-byte invariant: voice packages + companion.md unchanged"
    - "INIT-07 static-import budget held: cli.ts top-level static imports stay {node:fs/promises, node:url, node:path}"
    - "TDD RED+GREEN per task (S-1 pattern, except YAML workflow which uses shape-validation)"
    - "No emojis (CLAUDE.md global)"
    - "Option 3 macOS distribution shape (no codesign, no Apple Dev ID, no compiled darwin binary, no @achilles/cli-darwin-* publish)"
    - "Sequential publish order: 3 platform siblings FIRST, then @achilles/achilles-skill, then parent achilles LAST (RESEARCH Pattern 2)"
    - "SHA-256 source-of-truth check passes against the new publish layout (companion.md byte-for-byte equal between source and bundled-in-tarball)"
    - "Tarball secret-scan with 7 regex patterns empty against parent + 3 platform sibling + achilles-skill tarballs"
    - "macOS smoke job runs `bunx achilles@1.3.0 --version` against published artifact (DIST-06 CI gate; full voice round-trip is Phase 20 GATE-01 RBS-1 scope)"
    - "--provenance flag set on every npm publish step (Sigstore attestation per RESEARCH §Security Domain row V2)"
    - "NODE_AUTH_TOKEN sourced from GH Actions secret NPM_PUBLISH_TOKEN (never logged, never committed)"
    - "prepublishOnly hook in apps/achilles-terminal/package.json chains source-of-truth + secret-scan scripts"
    - "Root package.json scripts repath from apps/achilles-cli/scripts/ to apps/achilles-terminal/scripts/ (commit A; before Plan 04 deletes apps/achilles-cli/)"
  artifacts:
    - path: "apps/achilles-terminal/scripts/check-tarball-no-secrets.mjs"
      provides: "v1.2 port: 7 regex patterns scan parent + sibling + skill tarballs for embedded secrets"
      min_lines: 250
    - path: "apps/achilles-terminal/scripts/check-tarball-no-secrets.test.mjs"
      provides: "TNS1-TNS6 paired test cases ported from v1.2"
      min_lines: 100
    - path: "apps/achilles-terminal/tests/check-source-of-truth.test.ts"
      provides: "wider arm test verifying Phase 17 source-of-truth script against new publish layout"
      min_lines: 30
    - path: ".github/workflows/achilles-release.yml"
      provides: "matrix build + sequential publish + macOS smoke + --provenance"
      min_lines: 150
    - path: "apps/achilles-terminal/package.json"
      provides: "prepublishOnly hook chain"
      contains: '"prepublishOnly":'
    - path: "package.json"
      provides: "root scripts repathed to apps/achilles-terminal/scripts/"
      contains: "apps/achilles-terminal/scripts/check-source-of-truth.mjs"
  key_links:
    - from: "apps/achilles-terminal/package.json prepublishOnly"
      to: "apps/achilles-terminal/scripts/check-source-of-truth.mjs"
      via: "npm prepublishOnly hook execution"
      pattern: "check-source-of-truth\\.mjs.*check-tarball-no-secrets\\.mjs"
    - from: ".github/workflows/achilles-release.yml publish job"
      to: "npm registry"
      via: "sequential npm publish with siblings-first order"
      pattern: "npm publish --access public --provenance"
    - from: ".github/workflows/achilles-release.yml macos-smoke job"
      to: "Bun runtime on macos-14 runner"
      via: "bunx achilles@1.3.0 --version after publish"
      pattern: "bunx achilles@"
---

<objective>
Wire the publish-ready CI release workflow + the script-level gates that protect it. Port apps/achilles-cli/scripts/check-tarball-no-secrets.mjs (v1.2 canonical, 306 lines, 7-regex set) to apps/achilles-terminal/scripts/ with the only path change being `REPO_ROOT` -> `apps/achilles-terminal` directory; add a wider-arm vitest test for Phase 17's existing check-source-of-truth.mjs to verify it works against the new publish layout (DIST-03); create .github/workflows/achilles-release.yml that builds 3 platform binaries on matrix.os runners + downloads the artifacts + runs the 2 gate scripts + publishes 4 sub-packages sequentially (siblings FIRST, then achilles-skill, then parent achilles LAST per RESEARCH §Pattern 2) with --provenance + NODE_AUTH_TOKEN + 30s sleep for CDN propagation + npm view verification + macOS smoke job that runs `bunx achilles@1.3.0 --version` (DIST-06 CI gate); wire prepublishOnly hook in apps/achilles-terminal/package.json chaining the 2 scripts; repath root package.json scripts from apps/achilles-cli/scripts/ to apps/achilles-terminal/scripts/ (commit A safety - Plan 04 deletes apps/achilles-cli/).

Purpose: This is the workflow that runs at v1.3.0 release time. After commit A merges, the operator either pushes a `v1.3.0` tag or triggers workflow_dispatch; the workflow builds and publishes 4 npm packages atomically and the macOS lane confirms the JS-fallback resolution works. Without this, there is no publish.

Output: 4 NEW files (release workflow + secret-scan script + paired test + source-of-truth wider-arm test), 3 MODIFIED files (terminal package.json prepublishOnly, root package.json scripts repath). The plan is autonomous: every gate script has a vitest paired test; the workflow YAML is shape-validated via `yamllint` + GitHub Actions schema; no runtime apps are launched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-CONTEXT.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-VALIDATION.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-01-SUMMARY.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-02-SUMMARY.md

<interfaces>
<!-- v1.2 port sources + CI scaffold + interface contracts -->

From apps/achilles-cli/scripts/check-tarball-no-secrets.mjs (v1.2 canonical, 306 lines - port source):
- 7 frozen KEY_PATTERNS regex set (lines 58-95):
  1. elevenlabs-sk_ -- /sk_[a-f0-9]{48,}/
  2. elevenlabs-xi-api-key -- /xi-api-key:/
  3. elevenlabs-xi_api_key-assignment -- /xi_api_key=/
  4. elevenlabs-env-assignment -- /ELEVENLABS_API_KEY=/
  5. anthropic-sk-ant -- /sk-ant-[a-zA-Z0-9_-]{12,}/
  6. github-pat -- /ghp_[a-zA-Z0-9]{36}/
  7. github-fine-grained-pat -- /github_pat_[a-zA-Z0-9_]{82,}/
- SCANNABLE_EXTENSIONS (10): .md, .txt, .js, .mjs, .cjs, .json, .html, .css, .ts, .tsx
- walkFiles + truncateMatch helpers (lines 110-147)
- defaultScannerSeam (lines 152-203) -- uses `npm pack --pack-destination` + `tar -xzf` + `mkdirSync(recursive: true)` per CR-04 cross-platform fix
- runTarballSecretScan async function (lines 213-269)
- Invocation guard IIFE (lines 272-305)
- PATH ADJUSTMENT: change `const cliDir = resolve(REPO_ROOT, "apps/achilles-cli")` -> `const cliDir = resolve(REPO_ROOT, "apps/achilles-terminal")`
- REPO_ROOT computation `resolve(HERE, "..", "..", "..")` is identical (relative depth matches)

From apps/achilles-cli/scripts/check-tarball-no-secrets.test.mjs (v1.2 canonical - port source):
- node:test + node:assert imports
- buildBuffer + buildScannerSeam helpers (in-memory file-map scanner seam)
- TNS1-TNS6 cases (TNS6 reads packages/achilles-skill/skill/SKILL.md + companion.md via REPO_ROOT and asserts the rewritten SKILL.md still passes the 7-regex scan)

From apps/achilles-terminal/scripts/check-source-of-truth.mjs (Phase 17 ported - single-arm form):
- Reads packages/achilles-skill/skill/prompts/companion.md
- Computes SHA-256
- Compares against embedded constant in some module (Phase 17 produced this single-arm shape)
- Phase 19 ADDS a vitest wider-arm test that runs the script + asserts it exits 0 against the new layout where:
  - companion.md is at packages/achilles-skill/skill/prompts/companion.md (LOOP-02 invariant)
  - the embedded hash lives wherever Phase 17 placed it (verify via Phase 17 SUMMARY)
  - the script handles the case where @achilles/achilles-skill is a public dep (not bundled)

From apps/achilles-cli/package.json (v1.2 prepublishOnly hook pattern, line 27):
```
"prepublishOnly": "node scripts/check-source-of-truth.mjs && node scripts/check-tarball-no-secrets.mjs"
```

From .github/workflows/achilles-terminal-ci.yml (Phase 15 CI scaffold, 308 lines - the analog):
- name + on.pull_request.paths + on.push.branches + concurrency block (lines 38-62)
- permissions: contents: read (lines 67-68) -- Phase 19 adds id-token: write for --provenance
- actions/checkout@v4 + actions/setup-node@v4 (node-version: "22") + oven-sh/setup-bun@v2 (bun-version: "1.3.14")
- `npm ci --include=optional --force` (D-15-02 mandatory cross-platform shape)
- matrix.os runners: ubuntu-latest (x64), ubuntu-22.04-arm (linux-arm64), windows-2022 (win32-x64)
- compile-binaries job (lines 233-307) -- Phase 19 trims to 3 targets (drop darwin)
- bun build --compile --target=${{ matrix.target.bunTarget }} --outfile=../${{ matrix.target.out }} --minify

From RESEARCH §Code Example 1 lines 537-642 (the full release workflow shape):
- publish job needs[build-linux-x64, build-linux-arm64, build-win32-x64]
- ubuntu-latest runner
- permissions: contents: read + id-token: write
- 4 actions/download-artifact@v4 steps (one per sibling binary)
- node + node scripts (source-of-truth + tarball secret-scan)
- 5 sequential npm publish steps (3 siblings + achilles-skill + parent) with --access public --provenance + NODE_AUTH_TOKEN env from secrets.NPM_PUBLISH_TOKEN
- sleep 30 for CDN propagation
- 4 npm view assertions
- macos-smoke job needs: publish; macos-14 runner
- macos-smoke runs `npm install -g achilles@1.3.0` then `achilles --version` (Node lane) then `bunx achilles@1.3.0 --version` (Bun lane per DIST-06)

From root package.json (HEAD lines 31-33):
```
"check:source-of-truth": "node apps/achilles-cli/scripts/check-source-of-truth.mjs",
"check:tarball:secrets": "node apps/achilles-cli/scripts/check-tarball-no-secrets.mjs",
"check:dist": "npm run check:source-of-truth && npm run check:tarball:secrets",
```
Target shape (RESEARCH §Open Question 6): replace `apps/achilles-cli` -> `apps/achilles-terminal`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port check-tarball-no-secrets.mjs + paired test + wider-arm check-source-of-truth test</name>
  <files>apps/achilles-terminal/scripts/check-tarball-no-secrets.mjs, apps/achilles-terminal/scripts/check-tarball-no-secrets.test.mjs, apps/achilles-terminal/tests/check-source-of-truth.test.ts</files>
  <read_first>
    - .planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md (§Pattern 3 Sequential publish discussion; §Pitfall 3 bundledDependencies carryover; §Pitfall 4 dissolved by Option 3; §Don't Hand-Roll secret detection; §Code Example 1 release workflow)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md (check-tarball-no-secrets.mjs + .test.mjs MOD sections - verbatim port discussion; only `cliDir` line changes)
    - apps/achilles-cli/scripts/check-tarball-no-secrets.mjs (FULL FILE - v1.2 canonical 306 lines - the port source)
    - apps/achilles-cli/scripts/check-tarball-no-secrets.test.mjs (FULL FILE if exists - v1.2 test patterns)
    - apps/achilles-terminal/scripts/check-source-of-truth.mjs (FULL FILE - Phase 17 single-arm shape; verify how it computes/compares the SHA-256)
    - packages/achilles-skill/skill/SKILL.md (HEAD AFTER Plan 01 rewrite - rewritten body must still pass the 7-regex scan)
    - packages/achilles-skill/skill/prompts/companion.md (LOOP-02 locked - DO NOT touch; read to confirm presence)
  </read_first>
  <behavior>
    - check-tarball-no-secrets.test.mjs ports TNS1-TNS6 cases:
      - TNS1: in-memory file map with synthesized ElevenLabs sk_<48 hex chars> -- assert leak detected
      - TNS2: in-memory file map with synthesized xi-api-key: header -- assert leak detected
      - TNS3: in-memory file map with synthesized xi_api_key= shell assignment -- assert leak detected
      - TNS4: in-memory file map with ELEVENLABS_API_KEY= or sk-ant- or ghp_/github_pat_ patterns -- assert leak detected
      - TNS5: empty tarball + clean prose -- assert zero leaks
      - TNS6: real packages/achilles-skill/skill/SKILL.md (Plan 01 rewrite) + companion.md scanned -- assert zero leaks (the rewritten body must remain clean)
    - check-source-of-truth.test.ts wider-arm test asserts:
      - script exits 0 when run against the current monorepo (post-Plan 01 state)
      - script reads companion.md from packages/achilles-skill/skill/prompts/companion.md (LOOP-02 invariant path)
      - script does NOT attempt to extract a bundled tarball arm (Pitfall 3 - bundledDependencies dissolved; @achilles/achilles-skill is now a public dep)
      - script exits non-zero on a synthesized companion.md drift (use a fixture or mock)
  </behavior>
  <action>
    Per RESEARCH §Pattern 3 + §Pitfall 3 + PATTERNS.md check-tarball-no-secrets.mjs + .test.mjs MOD sections. RED commits first (2 test files), then GREEN commits (1 script port + 1 test port + 1 wider-arm test).

    1. Write apps/achilles-terminal/scripts/check-tarball-no-secrets.test.mjs by porting the v1.2 file verbatim from apps/achilles-cli/scripts/check-tarball-no-secrets.test.mjs. The TNS1-TNS6 cases all port unchanged. Path resolution is identical (`REPO_ROOT = resolve(HERE, "..", "..", "..")` matches: apps/achilles-cli/scripts/X.mjs <-> apps/achilles-terminal/scripts/X.mjs depth). TNS6 specifically reads packages/achilles-skill/skill/SKILL.md (after Plan 01 rewrite) + companion.md via REPO_ROOT and asserts the 7-regex scan finds zero leaks. NO emoji.

    2. Write apps/achilles-terminal/tests/check-source-of-truth.test.ts as a vitest test (NOT node:test). Use execFileSync or child_process.spawnSync to invoke `node apps/achilles-terminal/scripts/check-source-of-truth.mjs`; assert exit code 0 against the current monorepo state. Then write a second case that creates a temp companion.md fixture with a SHA-256 mismatch and asserts exit code != 0. Per RESEARCH §Pitfall 3 + §Open Question 2: the test does NOT require a tarball extraction arm; the single-arm form (source-vs-embedded-hash) is sufficient and the test verifies this.

    3. Port apps/achilles-terminal/scripts/check-tarball-no-secrets.mjs from apps/achilles-cli/scripts/check-tarball-no-secrets.mjs verbatim per PATTERNS.md section. The ONLY content change is the `cliDir` line:
       - v1.2: `const cliDir = resolve(REPO_ROOT, "apps/achilles-cli");`
       - v1.3: `const cliDir = resolve(REPO_ROOT, "apps/achilles-terminal");`
       Everything else - the 7 KEY_PATTERNS regex set, SCANNABLE_EXTENSIONS, walkFiles + truncateMatch helpers, defaultScannerSeam (npm pack + tar -xzf + mkdirSync recursive), runTarballSecretScan async function, invocation guard IIFE - ships verbatim. Per PATTERNS.md stdio caveat: the script uses `stdio: ["ignore", "pipe", "pipe"]` and `stdio: "ignore"` internally; this is OK because the ESLint rule scope (Plan 02 Task 2) only fires on src/**/*.ts files, not scripts/*.mjs. Confirm by running lint after the port.

    4. Verify test infrastructure: `node --test apps/achilles-terminal/scripts/check-tarball-no-secrets.test.mjs` exits 0 (the test uses node:test - same as v1.2 pattern). The new check-source-of-truth.test.ts uses vitest (different runner).

    5. NO emojis (CLAUDE.md global).
  </action>
  <verify>
    <automated>node --test apps/achilles-terminal/scripts/check-tarball-no-secrets.test.mjs</automated>
    <automated>npm test --workspace apps/achilles-terminal -- --pool=forks tests/check-source-of-truth.test.ts</automated>
    Script file present: `[[ -f apps/achilles-terminal/scripts/check-tarball-no-secrets.mjs ]]` returns 0.
    Path adjustment verified: `grep -c 'apps/achilles-terminal' apps/achilles-terminal/scripts/check-tarball-no-secrets.mjs` returns >=1 AND `grep -c 'apps/achilles-cli' apps/achilles-terminal/scripts/check-tarball-no-secrets.mjs` returns 0.
    7-regex KEY_PATTERNS present: `grep -c 'elevenlabs-sk_\|elevenlabs-xi-api-key\|elevenlabs-xi_api_key\|elevenlabs-env-assignment\|anthropic-sk-ant\|github-pat\|github-fine-grained-pat' apps/achilles-terminal/scripts/check-tarball-no-secrets.mjs` returns >=7.
    Lint pass: `npm run lint --workspace apps/achilles-terminal -- --max-warnings 0` exits 0 (the scripts/*.mjs files are excluded from the launch-path src/**/*.ts coverage).
    No emoji: `LC_ALL=C grep -rP '[\x80-\xff]' apps/achilles-terminal/scripts/check-tarball-no-secrets.mjs apps/achilles-terminal/scripts/check-tarball-no-secrets.test.mjs apps/achilles-terminal/tests/check-source-of-truth.test.ts` returns nothing.
  </verify>
  <done>
    - check-tarball-no-secrets.mjs >=250 LOC ported from v1.2 with cliDir path change.
    - check-tarball-no-secrets.test.mjs ports TNS1-TNS6 cases (all 6 assertions cover the 7-regex set + clean prose + real SKILL.md/companion.md).
    - check-source-of-truth.test.ts (vitest) asserts the Phase 17 script exits 0 against the post-Plan 01 layout AND non-zero on synthesized companion.md drift.
    - All 3 test files green.
    - Lint pass (scripts/*.mjs excluded from src/**/*.ts ESLint rule scope).
    - No emojis.
    - One RED commit + one GREEN commit per task (S-1 TDD pattern).
  </done>
</task>

<task type="auto">
  <name>Task 2: prepublishOnly hook + root package.json scripts repath</name>
  <files>apps/achilles-terminal/package.json, package.json</files>
  <read_first>
    - .planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md (§Pitfall 3 prepublishOnly hook; §Open Question 6 commit A path-rewrite safety)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md (apps/achilles-terminal/package.json MOD prepublishOnly hook section; Root package.json MOD scripts paths section)
    - apps/achilles-terminal/package.json (post-Plan 01 state - HEAD lines 23-31 scripts block)
    - package.json (HEAD root - lines 31-33 the existing check:source-of-truth + check:tarball:secrets + check:dist aliases)
    - apps/achilles-cli/package.json (v1.2 line 27 prepublishOnly pattern - the chain)
  </read_first>
  <behavior>
    - apps/achilles-terminal/package.json adds prepublishOnly entry chaining the 2 scripts
    - Root package.json scripts repath from apps/achilles-cli/scripts/ to apps/achilles-terminal/scripts/
    - `npm run check:dist` exits 0 after the repath (verifies the new paths actually exist - Plan 01 + this task's Task 1 created them)
  </behavior>
  <action>
    Per RESEARCH §Open Question 6 + PATTERNS.md package.json MOD sections.

    1. Edit apps/achilles-terminal/package.json scripts block: ADD a new entry:
       ```
       "prepublishOnly": "node scripts/check-source-of-truth.mjs && node scripts/check-tarball-no-secrets.mjs"
       ```
       Insertion site: inside the existing scripts object, alphabetical position. Per PATTERNS.md note: the first script (check-source-of-truth.mjs) already exists (Phase 17 substrate); the second (check-tarball-no-secrets.mjs) is the Plan 03 Task 1 port. Both scripts must exist at this point because Task 1 ran before this task.

    2. Edit root package.json scripts block per PATTERNS.md Root package.json MOD section. Three changes:
       - Line 31: `"check:source-of-truth": "node apps/achilles-cli/scripts/check-source-of-truth.mjs"` -> `"check:source-of-truth": "node apps/achilles-terminal/scripts/check-source-of-truth.mjs"`
       - Line 32: `"check:tarball:secrets": "node apps/achilles-cli/scripts/check-tarball-no-secrets.mjs"` -> `"check:tarball:secrets": "node apps/achilles-terminal/scripts/check-tarball-no-secrets.mjs"`
       - Line 33 `check:dist` alias stays unchanged (it chains the 2 updated commands).

       Per PATTERNS.md "Why this change happens in commit A": once Task 1 created the ported scripts in apps/achilles-terminal/scripts/, the root aliases MUST switch to point at them BEFORE Plan 04 deletes apps/achilles-cli/. Doing this in Plan 03 (commit A) avoids a window where running `npm run check:dist` fails between Plan 03 and Plan 04.

    3. Verify root scripts work: `npm run check:dist` from the repo root exits 0.

    4. NO emojis. Per CLAUDE.md global: never run applications automatically - the check:dist run is a verification step that the new paths resolve, not an "application launch".
  </action>
  <verify>
    <automated>jq -r '.scripts.prepublishOnly' apps/achilles-terminal/package.json | grep -c 'check-source-of-truth.mjs.*check-tarball-no-secrets.mjs'</automated>
    Root path change: `grep -c 'apps/achilles-terminal/scripts/check' package.json` returns 2 AND `grep -c 'apps/achilles-cli/scripts/check' package.json` returns 0.
    Hook chain works: `npm run check:dist` from repo root exits 0.
    No emoji: `LC_ALL=C grep -rP '[\x80-\xff]' apps/achilles-terminal/package.json package.json` returns nothing.
  </verify>
  <done>
    - apps/achilles-terminal/package.json scripts block adds prepublishOnly entry chaining source-of-truth + tarball-no-secrets.
    - Root package.json scripts.check:source-of-truth + check:tarball:secrets repathed to apps/achilles-terminal/scripts/.
    - npm run check:dist exits 0 (verifies both ports resolve and run correctly).
    - One commit per file edit (mechanical metadata change; TDD RED/GREEN pattern is not applicable here since the runtime is the existing ported scripts which already have paired tests from Task 1).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create .github/workflows/achilles-release.yml (matrix build + sequential publish + macOS smoke)</name>
  <files>.github/workflows/achilles-release.yml</files>
  <read_first>
    - .planning/phases/19-distribution-publishing-skill-rewire/19-CONTEXT.md (CONTEXT.md Claude's Discretion: single workflow with matrix.os; release workflow shape)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md (§Pattern 1 publish-then-cut; §Pattern 2 siblings-first publish order; §Don't Hand-Roll multi-package publish orchestration + `npm deprecate` rollback; §Pitfall 5 macOS publishes nothing; §Pitfall 9 npm view CDN cache lag; §Code Example 1 FULL workflow YAML lines 537-642; §Architecture diagram lines 102-150)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md (achilles-release.yml MOD section - all sub-sections)
    - .github/workflows/achilles-terminal-ci.yml (FULL FILE - 308 lines - the analog for matrix.os + Bun/Node setup + compile-binaries job pattern)
    - apps/achilles-terminal/scripts/check-source-of-truth.mjs (Phase 17 - the CI gate script invoked from the workflow)
    - apps/achilles-terminal/scripts/check-tarball-no-secrets.mjs (Plan 03 Task 1 port - the CI gate script invoked from the workflow)
  </read_first>
  <behavior>
    - YAML lint passes
    - Workflow has correct triggers (workflow_dispatch + push.tags: ["v*"])
    - Workflow has concurrency block with cancel-in-progress: false (release runs MUST complete)
    - Workflow has permissions: contents: read + id-token: write (for --provenance)
    - matrix.os build job covers EXACTLY 3 targets (linux-x64, linux-arm64, win32-x64) -- NO darwin targets per D-01/D-02
    - publish job needs[3 build jobs] and runs on ubuntu-latest
    - publish job runs the 2 gate scripts BEFORE any npm publish
    - publish job has 5 sequential npm publish steps in correct order: 3 platform siblings FIRST, then @achilles/achilles-skill, then parent achilles LAST (RESEARCH §Pattern 2)
    - every npm publish step uses --access public --provenance and env NODE_AUTH_TOKEN sourced from secrets.NPM_PUBLISH_TOKEN
    - publish job has sleep 30 + npm view assertions for 5 packages (RESEARCH §Pitfall 9 CDN lag)
    - macos-smoke job needs: publish; runs on macos-14; installs achilles@1.3.0 globally; verifies `achilles --version` (Node lane) AND `bunx achilles@1.3.0 --version` (Bun lane per DIST-06)
    - macos-smoke job publishes NOTHING platform-specific (Pitfall 5)
  </behavior>
  <action>
    Per RESEARCH §Pattern 1 + §Pattern 2 + §Code Example 1 + PATTERNS.md achilles-release.yml MOD section. The YAML workflow is shape-validated via yamllint + GitHub Actions schema; no TDD RED/GREEN cycle (YAML is config, not testable behavior).

    1. Create .github/workflows/achilles-release.yml using the structure from RESEARCH §Code Example 1 lines 537-642 + PATTERNS.md achilles-release.yml MOD section. The full workflow has 4 jobs:

       **(a) Header + triggers (PATTERNS.md "Workflow header / triggers" section):**
       ```
       name: achilles-release
       on:
         workflow_dispatch: {}
         push:
           tags:
             - "v*"
       concurrency:
         group: ${{ github.workflow }}-${{ github.ref }}
         cancel-in-progress: false   # release runs MUST complete
       permissions:
         contents: read
         id-token: write   # for npm provenance (--provenance flag)
       ```

       **(b) Build jobs (3 instances of compile-binaries job - one per surviving platform):**
       Copy SHAPE from achilles-terminal-ci.yml lines 233-307. Trim matrix.target from 5 entries to EXACTLY 3 (drop darwin-arm64 + darwin-x64 per D-01):
       - linux-x64 -- runs-on: ubuntu-latest -- bunTarget: bun-linux-x64
       - linux-arm64 -- runs-on: ubuntu-22.04-arm -- bunTarget: bun-linux-arm64
       - win32-x64 -- runs-on: windows-2022 -- bunTarget: bun-windows-x64

       Each job: actions/checkout@v4 + actions/setup-node@v4 (node-version: "22") + oven-sh/setup-bun@v2 (bun-version: "1.3.14") + `npm ci --include=optional --force` (D-15-02 mandatory shape) + `bun build src/cli.ts --compile --target=${{ matrix.target.bunTarget }} --outfile=../${{ matrix.target.out }} --minify` + actions/upload-artifact@v4 with name: cli-${{ matrix.target.name }}-binary (per PATTERNS.md "Build-artifact upload" section).

       **(c) Publish job (RESEARCH §Code Example 1 lines 538-622):**
       - needs: [build-linux-x64, build-linux-arm64, build-win32-x64]
       - runs-on: ubuntu-latest
       - permissions: contents: read + id-token: write
       - Setup: actions/checkout@v4 (fetch-depth: 0) + actions/setup-node@v4 (node-version: "22" + registry-url: "https://registry.npmjs.org") + npm ci --include=optional --force
       - Build the parent JS-fallback bundle: working-directory: apps/achilles-terminal -- run: npm run build
       - 3x actions/download-artifact@v4 (one per sibling: name: cli-linux-x64-binary -> path: apps/cli-linux-x64/bin/, repeat for linux-arm64 + win32-x64)
       - Gate 1: `node apps/achilles-terminal/scripts/check-source-of-truth.mjs`
       - Gate 2: `node apps/achilles-terminal/scripts/check-tarball-no-secrets.mjs`
       - 5 sequential npm publish steps in PATTERN 2 order (siblings FIRST):
         1. Publish @achilles/cli-linux-x64 (working-directory: apps/cli-linux-x64; env NODE_AUTH_TOKEN from secrets.NPM_PUBLISH_TOKEN; `npm publish --access public --provenance`)
         2. Publish @achilles/cli-linux-arm64 (same shape)
         3. Publish @achilles/cli-win32-x64 (same shape)
         4. Publish @achilles/achilles-skill (working-directory: packages/achilles-skill; same npm publish line; uses publishConfig.access:public flipped in Plan 01)
         5. Publish achilles (parent; working-directory: apps/achilles-terminal; same npm publish line; LAST in order)
       - Wait for CDN: `run: sleep 30`
       - Verify 5 packages: 5x `npm view <pkg>@1.3.0 version`

       **(d) macos-smoke job (RESEARCH §Code Example 1 lines 624-642):**
       - needs: publish
       - runs-on: macos-14
       - Setup: actions/setup-node@v4 (node-version: "22") + oven-sh/setup-bun@v2 (bun-version: "1.3.14")
       - Install published parent: `npm install -g achilles@1.3.0`
       - Smoke under Node: `achilles --version` (verifies bin shim resolves the JS-fallback bundle - DIST-06 partial)
       - Smoke under Bun: `bunx achilles@1.3.0 --version` (DIST-06 Bun lane per Open Question 7 recommendation)
       - Per Pitfall 5: macos-smoke publishes NOTHING; it only verifies the JS-fallback resolution.

    2. Use exact YAML indentation (2 spaces). NO emojis (CLAUDE.md global). All `run:` blocks use bash on linux/macos and powershell on windows where appropriate (achilles-terminal-ci.yml lines 280-307 demonstrate this).

    3. Document at the top of the file (YAML comments) that the workflow:
       - is triggered manually (workflow_dispatch) OR by tag push (v*) per RESEARCH §Open Question 5
       - publishes only 3 compiled platform siblings + skill + parent (NO darwin per Option 3 lock)
       - rolls back via `npm deprecate achilles@1.3.0 "..."` (RESEARCH §Standard Stack `npm deprecate` row) - this is operator-triggered, NOT in the workflow
       - depends on secret NPM_PUBLISH_TOKEN being provisioned in repo settings before run

    4. Verify YAML structure:
       - Use any available YAML lint command (e.g., `yq eval '.' .github/workflows/achilles-release.yml`) to confirm valid YAML
       - Verify GitHub Actions schema via the `gh workflow view` command if available, OR via the `actionlint` linter if available
       - Manual review: confirm 3 matrix.os entries (no darwin), 5 publish steps in correct order, --provenance on every publish step
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const w=fs.readFileSync('.github/workflows/achilles-release.yml','utf8');const expected=['linux-x64','linux-arm64','win32-x64'];const forbidden=['darwin'];for(const e of expected)if(!w.includes(e))throw new Error('missing target '+e);for(const f of forbidden)if(w.includes(f+'-arm64')||w.includes(f+'-x64'))throw new Error('forbidden target '+f);const pubOrder=['@achilles/cli-linux-x64','@achilles/cli-linux-arm64','@achilles/cli-win32-x64','@achilles/achilles-skill','achilles'];let idx=0;for(const p of pubOrder){const i=w.indexOf('npm publish',idx);if(i<0)throw new Error('missing publish step '+p);idx=i+1;}console.log('ok');"</automated>
    File exists: `[[ -f .github/workflows/achilles-release.yml ]]` returns 0.
    YAML valid: `node -e "const y=require('fs').readFileSync('.github/workflows/achilles-release.yml','utf8');console.log(y.length)"` returns >0.
    matrix.os has 3 build jobs: `grep -c 'bun-linux-x64\|bun-linux-arm64\|bun-windows-x64' .github/workflows/achilles-release.yml` returns >=3.
    NO darwin targets: `grep -c 'darwin' .github/workflows/achilles-release.yml` returns 0.
    Sequential publish order (siblings FIRST, parent LAST): grep finds publish steps in order @achilles/cli-linux-x64, @achilles/cli-linux-arm64, @achilles/cli-win32-x64, @achilles/achilles-skill, achilles.
    --provenance flag present: `grep -c '\-\-provenance' .github/workflows/achilles-release.yml` returns >=5 (one per publish step).
    NODE_AUTH_TOKEN secret reference: `grep -c 'NODE_AUTH_TOKEN.*secrets\.NPM_PUBLISH_TOKEN' .github/workflows/achilles-release.yml` returns >=5.
    Sleep 30 for CDN lag (Pitfall 9): `grep -c 'sleep 30' .github/workflows/achilles-release.yml` returns >=1.
    macOS smoke job: `grep -c 'macos-14\|bunx achilles@' .github/workflows/achilles-release.yml` returns >=2.
    macOS smoke publishes nothing (Pitfall 5): the macos-smoke job block has no `npm publish` line; verify via line-range inspection or extract the job block.
    No emoji: `LC_ALL=C grep -rP '[\x80-\xff]' .github/workflows/achilles-release.yml` returns nothing.
  </verify>
  <done>
    - .github/workflows/achilles-release.yml created >=150 LOC.
    - matrix.os covers EXACTLY 3 compiled targets (linux-x64, linux-arm64, win32-x64); NO darwin.
    - publish job runs the 2 gate scripts (source-of-truth + tarball-no-secrets) BEFORE any npm publish.
    - 5 sequential publish steps in PATTERN 2 order (siblings FIRST, achilles-skill, parent LAST).
    - Every npm publish uses --access public --provenance + NODE_AUTH_TOKEN from secrets.NPM_PUBLISH_TOKEN.
    - sleep 30 + 5 npm view assertions for CDN propagation lag (Pitfall 9).
    - macos-smoke job runs `achilles --version` AND `bunx achilles@1.3.0 --version` against the published artifact (DIST-06 CI gate).
    - macos-smoke publishes NOTHING platform-specific (Pitfall 5).
    - YAML is valid; no emojis; concurrency block prevents in-flight cancellation.
    - One commit (the YAML file creation - no test cycle for config files).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GH Actions Secrets -> npm registry | NODE_AUTH_TOKEN (from secrets.NPM_PUBLISH_TOKEN) crosses this boundary; compromise of the token grants publish access to all 5 packages |
| Build artifact -> publish job | Per-OS binaries built on matrix.os runners flow to the publish job via actions/upload-artifact/download-artifact; supply-chain tamper risk if the artifact transport is compromised |
| Tarball -> public registry | npm publish makes the tarball externally readable; any embedded secret in any of 5 packages becomes externally readable |
| companion.md -> bundled artifact | The SKILL.md (Plan 01) + companion.md (LOOP-02 locked) live in achilles-skill package; drift between source and tarball is a Tampering threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-V2-01 | Spoofing | GH Actions NPM_PUBLISH_TOKEN secret | mitigate | Repo-scoped GH Actions secret; never logged (only referenced via ${{ secrets.NPM_PUBLISH_TOKEN }}); rotated annually outside Phase 19 scope; --provenance flag adds Sigstore attestation to every tarball binding it to the workflow + commit SHA |
| T-19-V4-01 | Information Disclosure | Tarball-embedded secret (ElevenLabs API key / Anthropic key / GitHub PAT) | mitigate | check-tarball-no-secrets.mjs runs as prepublishOnly hook + CI gate before any npm publish; 7-regex set covers the known secret shapes; TNS6 verifies the rewritten SKILL.md + companion.md stay clean |
| T-19-V6-01 | Tampering | companion.md drift between packages/achilles-skill/skill/prompts/companion.md (source) and bundled-in-tarball | mitigate | SHA-256 source-of-truth check (Phase 17 ported) runs as prepublishOnly + CI gate; wider-arm vitest test verifies the script against new layout; single-arm form is sufficient per RESEARCH §Open Question 2 (bundledDependencies dissolved - achilles-skill is now a public dep, not bundled) |
| T-19-V8-01 | Tampering | Compromised platform binary uploaded as artifact between build + publish jobs | accept | actions/upload-artifact@v4 + actions/download-artifact@v4 are first-party GitHub-maintained; transport is HTTPS; npm registry verifies tarball SHA-512 after publish |
| T-19-V13-01 | Elevation of Privilege | Workflow YAML allows arbitrary commands via tag push trigger | mitigate | concurrency block + cancel-in-progress: false + workflow_dispatch + push.tags only; no PR triggers; secret NPM_PUBLISH_TOKEN only accessible to publish job, not build jobs |
| T-19-V13-02 | Repudiation | Sequential publish leaves intermediate state on failure (e.g., 3 siblings published, parent failed) | mitigate | Pattern 2 siblings-first ordering eliminates the window where users see "ERESOLVE: optional dep not found"; on failure, operator runs `npm deprecate achilles@1.3.0 "..."` per RESEARCH §Standard Stack (deprecate is idempotent; unpublish is only within 72h) |
| T-19-V14-01 | DoS | npm view CDN cache lag causes spurious 404 on post-publish verify | mitigate | sleep 30 between publish + verify (RESEARCH §Pitfall 9); falls within npm's documented 15-45s propagation window |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | Plan 03 installs zero new runtime packages; the workflow uses pinned action versions (actions/checkout@v4, actions/setup-node@v4, oven-sh/setup-bun@v2, actions/upload-artifact@v4, actions/download-artifact@v4) - all first-party / well-vetted; package-legitimacy audit in RESEARCH §Package Legitimacy Audit confirms zero new packages |
</threat_model>

<verification>
- `npm test --workspace apps/achilles-terminal -- --pool=forks tests/check-source-of-truth.test.ts` exits 0
- `node --test apps/achilles-terminal/scripts/check-tarball-no-secrets.test.mjs` exits 0
- `npm run check:dist` from repo root exits 0 (verifies both ported scripts resolve and run)
- `.github/workflows/achilles-release.yml` exists, is valid YAML, has 3 matrix.os build jobs (no darwin), 5 sequential publish steps in Pattern 2 order, --provenance + NODE_AUTH_TOKEN on every publish step, sleep 30 + npm view assertions
- macos-smoke job exists and runs `bunx achilles@1.3.0 --version` (DIST-06 Bun lane CI gate)
- LOOP-02 invariant: `git diff HEAD~ -- packages/voice-protocol packages/voice-stt packages/voice-tts packages/claude-code-bridge packages/achilles-skill/skill/prompts/companion.md` returns empty
- INIT-07 invariant: cli.ts top-level static imports unchanged (no cli.ts changes in this plan)
- No emojis anywhere
- prepublishOnly hook in apps/achilles-terminal/package.json chains the 2 gate scripts
- Root package.json scripts repathed to apps/achilles-terminal/scripts/
</verification>

<success_criteria>
- M-01 met: Parent achilles + 3 platform sibling packages + achilles-skill ready to publish to npm via the workflow (DIST-04 CI scaffold)
- M-03 met: companion.md byte-for-byte equal between source and tarball - prepublishOnly hook runs source-of-truth check before publish
- M-11 met (Plan 03 contribution): LOOP-02 invariants held; voice packages + companion.md unchanged
- M-12 met (Plan 03 contribution): INIT-07 invariant held; no cli.ts changes
- DIST-03 SHA-256 source-of-truth verification part covered (the install-skill subcommand part lives in Plan 02; the SKILL.md rewrite part lives in Plan 01)
- DIST-04 CI publish workflow covered (NODE_AUTH_TOKEN, --provenance, matrix.os, sequential publish)
- DIST-06 CI smoke gate covered (macos-14 runner runs `bunx achilles@1.3.0 --version`; full RBS-1 asciicast verification is Phase 20 GATE-01 scope)
</success_criteria>

<output>
Create `.planning/phases/19-distribution-publishing-skill-rewire/19-03-SUMMARY.md` when done capturing:
- Diff stats for check-tarball-no-secrets.mjs port (lines of code transferred + the cliDir line change)
- Diff stats for check-source-of-truth.test.ts (wider-arm test coverage)
- prepublishOnly hook addition to apps/achilles-terminal/package.json
- Root package.json scripts repath diff
- achilles-release.yml structure: 3 build jobs, publish job with 5 sequential npm publish steps, macos-smoke job
- Confirmation of LOOP-02 + INIT-07 invariants held
- Wave 3 completes commit A's "publish-ready state" - operator can now push the v1.3.0 tag to trigger the release workflow
</output>
