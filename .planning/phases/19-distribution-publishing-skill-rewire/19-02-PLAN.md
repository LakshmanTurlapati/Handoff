---
phase: 19-distribution-publishing-skill-rewire
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - apps/achilles-terminal/src/ui/Banner.tsx
  - apps/achilles-terminal/src/error-classifier.ts
  - apps/achilles-terminal/src/ui/VoiceShell.tsx
  - apps/achilles-terminal/src/state/useAchillesState.ts
  - apps/achilles-terminal/src/session.ts
  - apps/achilles-terminal/src/install-skill.ts
  - apps/achilles-terminal/src/skill-symlink.ts
  - apps/achilles-terminal/src/cli.ts
  - apps/achilles-terminal/eslint.config.js
  - apps/achilles-terminal/tests/ui/banner.test.tsx
  - apps/achilles-terminal/tests/error-classifier.test.ts
  - apps/achilles-terminal/tests/session-err03-wiring.test.ts
  - apps/achilles-terminal/tests/session-err08-wiring.test.ts
  - apps/achilles-terminal/tests/eslint-stdio-ignore.test.ts
  - apps/achilles-terminal/tests/install-skill.test.ts
  - apps/achilles-terminal/tests/cli-install-skill.test.ts
autonomous: true
requirements: [ERR-01, ERR-03, ERR-08, DIST-03]
tags: [runtime-hardening, error-ux, watchdog, logger, eslint, install-skill]

must_haves:
  truths:
    - "LOOP-02 byte-for-byte invariant: voice-protocol, voice-stt, voice-tts, claude-code-bridge, companion.md untouched"
    - "INIT-07 static-import budget held: cli.ts top-level static imports stay {node:fs/promises, node:url, node:path}; install-skill is the 6th dynamic-import gate inside main()"
    - "TDD RED+GREEN per task (S-1 pattern)"
    - "No emojis (CLAUDE.md global)"
    - "ARIA priority: Banner uses aria-role compatible with Ink 7 (timer, per RESEARCH §Assumption A8) so the screen-reader announcement fires distinct from the polite status row"
    - "ERR-01 banner is one-line red row ABOVE the status row; visible simultaneously with status row during cascading failures (D-10)"
    - "ERR-01 banner auto-dismisses after 8s OR on next successful event (whichever comes first)"
    - "ERR-03 watchdog respawns sox + ffplay bounded (3-in-10s); cap-exceeded stays in error state (not process exit) - typed-input fallback still works (Phase 18 ERR-04 carryover)"
    - "ERR-08 createStructuredLogger called unconditionally at runVoice() entry, run_voice_start log line written before any other side effect"
    - "ESLint stdio:ignore forbid rule active in apps/achilles-terminal/eslint.config.js only; NOT root, NOT other workspaces"
    - "install-skill subcommand registers skill via Bash symlink (or copy on Windows) at ~/.claude/skills/achilles/"
  artifacts:
    - path: "apps/achilles-terminal/src/ui/Banner.tsx"
      provides: "ERR-01 inline error banner Ink component (D-10 layout, D-11 aria-live=assertive)"
      min_lines: 40
      exports: ["Banner", "BannerProps", "BANNER_AUTO_DISMISS_MS"]
    - path: "apps/achilles-terminal/src/error-classifier.ts"
      provides: "Maps SessionErrorClassification union -> ClassifiedBanner{class, suggestedAction} - 8 classes"
      min_lines: 30
      exports: ["classifyForBanner", "ClassifiedBanner"]
    - path: "apps/achilles-terminal/src/install-skill.ts"
      provides: "DIST-03 install-skill command action (port from v1.2 apps/achilles-cli/src/commands/install-skill.ts)"
      min_lines: 100
      exports: ["installSkillCommand", "InstallSkillCommandOptions"]
    - path: "apps/achilles-terminal/src/skill-symlink.ts"
      provides: "symlink primitive with Windows EPERM fallback (port from v1.2 apps/achilles-cli/src/skill-symlink.ts)"
      min_lines: 200
      exports: ["installSkillSymlink", "ExistingDestinationConflictError", "SymlinkNotPermittedError"]
    - path: "apps/achilles-terminal/eslint.config.js"
      provides: "GATE-04 no-restricted-syntax rule activated against literal { stdio: 'ignore' } shape"
      contains: "no-restricted-syntax"
  key_links:
    - from: "apps/achilles-terminal/src/ui/VoiceShell.tsx"
      to: "apps/achilles-terminal/src/ui/Banner.tsx"
      via: "import Banner; render as first child of root <Box flexDirection='column'>"
      pattern: "import \\{ Banner \\}"
    - from: "apps/achilles-terminal/src/session.ts"
      to: "apps/achilles-terminal/src/child-exit-watchdog.ts"
      via: "createChildExitWatchdog calls in wireAudioBridges for sox + ffplay"
      pattern: "createChildExitWatchdog\\(\\{[\\s\\S]*?label: ['\"](sox|ffplay)['\"]"
    - from: "apps/achilles-terminal/src/session.ts runVoice entry"
      to: "apps/achilles-terminal/src/structured-logger.ts"
      via: "createStructuredLogger + logger.info('run_voice_start', ...)"
      pattern: "logger\\.info\\(\"run_voice_start\""
    - from: "apps/achilles-terminal/src/cli.ts"
      to: "apps/achilles-terminal/src/install-skill.ts"
      via: "dynamic import in main() install-skill branch"
      pattern: "await import\\(['\"]\\./install-skill"
---

<objective>
Wire all v1.3 runtime hardening behind the publish-ready artifact: ERR-01 inline error banner (Banner.tsx + error-classifier.ts + VoiceShell.tsx integration), ERR-03 sox/ffplay watchdog wiring in session.ts (Phase 17 substrate exists at child-exit-watchdog.ts; this plan adds the construction calls in wireAudioBridges), ERR-08 unconditional structured logger fan-out at runVoice() entry, ESLint stdio:"ignore" forbid rule activation in apps/achilles-terminal/eslint.config.js only (GATE-04 lint half), and the install-skill subcommand port from v1.2 (DIST-03 install path) added as the 6th dynamic-import gate in cli.ts.

Purpose: Close the v1.2 silent-launch failure shape (ERR-08 + GATE-04 prevent the detached-stdio regression), make cascading failures user-visible (ERR-01 banner above status row), and bounded-respawn audio devices on hot-swap / suspend resume (ERR-03 watchdog).

Output: 4 NEW source files (Banner.tsx, error-classifier.ts, install-skill.ts, skill-symlink.ts), 5 MODIFIED source files (VoiceShell.tsx, useAchillesState.ts, session.ts, cli.ts, eslint.config.js), 7 NEW test files. INIT-07 invariant: install-skill is the 6th dynamic-import gate inside main() - cli.ts's top-level static-import budget stays at {node:fs/promises, node:url, node:path}. The plan is autonomous: every new behavior is testable end-to-end with vitest + Ink testing library + grep verification of session.ts wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-CONTEXT.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-VALIDATION.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-01-SUMMARY.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md
@.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-CONTEXT.md

<interfaces>
<!-- Key contracts extracted from Phase 17/18 + v1.2 port sources. -->
<!-- Executor uses these directly; no codebase exploration needed. -->

From apps/achilles-terminal/src/session-events.ts (Phase 17 lines 44-52):
```
export type SessionErrorClassification =
  | "network"
  | "auth"
  | "rate_limit"
  | "server"
  | "unknown"
  | "mic_unavailable"
  | "playback_lost"
  | "claude_failed";
```

From apps/achilles-terminal/src/structured-logger.ts (Phase 17 lines ~144 + 364-367):
```
export function createStructuredLogger(opts?: StructuredLoggerOpts): StructuredLogger;
interface StructuredLogger {
  info(event: string, fields: Record<string, unknown>): void;
  warn(event: string, fields: Record<string, unknown>): void;
  error(event: string, fields: Record<string, unknown>): void;
  child(scope: string): StructuredLogger;
  flush(): Promise<void>;
  dispose(): void;
}
// DEFAULT_REDACT_PATTERNS exported (7-regex set including xi_ prefix)
```

From apps/achilles-terminal/src/child-exit-watchdog.ts (Phase 17 lines ~90-140, 60):
```
export function createChildExitWatchdog<TChild>(opts: {
  readonly label: "sox" | "ffplay";
  readonly child: TChild;
  readonly respawnFactory: () => TChild;
  readonly onError: (msg: string) => void;
  readonly logger: StructuredLogger;
}): ChildExitWatchdog;
export const AUDIO_DEVICE_LOST_MESSAGE: string; // "Audio device lost -- restart Achilles"
```

From apps/achilles-cli/src/commands/install-skill.ts (v1.2 lines 39-60, 82-92, 99-125, 158-219 - port source):
```
// Imports include SKILL_PROMPTS_DIR from "@achilles/achilles-skill" and skill-symlink primitives
// Port adjustment: change "../skill-symlink.js" -> "./skill-symlink.js" (flat layout vs v1.2 commands/ subdir)
export interface InstallSkillCommandOptions {
  readonly force: boolean;
  readonly stdout: NodeJS.WritableStream;
  readonly stderr: NodeJS.WritableStream;
  readonly processExitImpl: (code: number) => void;
  // 5 optional seams (fs, logger, sourceProvider, etc.)
}
export function installSkillCommand(opts: InstallSkillCommandOptions): void;
```

From apps/achilles-cli/src/skill-symlink.ts (v1.2 lines 1-323 - port source):
```
// 9 exported surfaces, all port verbatim (no path changes):
export interface InstallSkillSymlinkFs { ... 6-method node:fs subset ... }
export interface InstallSkillSymlinkLogger { info, warn }
export interface InstallSkillSymlinkOptions { 6-field option bag }
export type InstallSkillSymlinkResult = "symlink" | "copy" | "already-installed";
export class ExistingDestinationConflictError extends Error { ... }
export class SymlinkNotPermittedError extends Error { ... }
export function getErrorCode(err: unknown): string | undefined;
export const WINDOWS_FALLBACK_CODES: Set<string>;
export function installSkillSymlink(opts: InstallSkillSymlinkOptions): InstallSkillSymlinkResult;
```

From apps/achilles-terminal/src/cli.ts (HEAD lines 92-189, the 5 existing dynamic-import gates):
- voice gate
- init gate
- config gate (insertion-site model at lines 134-140)
- transcripts gate (line 164)
- latency gate

INIT-07 invariant: top-level static imports MUST stay {node:fs/promises, node:url, node:path}

From apps/achilles-terminal/src/ui/ScreenReader.tsx (Phase 16 lines 14-24, 56-61 - aria-role precedent):
- Uses aria-role="timer" because "status" was not in Ink 7's role enum (D-16-03-02 deviation)
- Phase 19 Banner inherits this precedent (RESEARCH §Assumption A8)

From apps/achilles-terminal/src/ui/VoiceShell.tsx (HEAD lines 130-142):
```
return (
  <Box flexDirection="column">
    {sr ? (<ScreenReader state={state} />) : (<><Blob amplitude={amp} /><Sparkline ring={ring} writeIndex={writeIndex} /></>)}
    <StatusRow state={state} transcript="" transcriptsActive={false} />
  </Box>
);
```
Target: insert <Banner ... /> as FIRST child of the root Box.

From apps/achilles-terminal/eslint.config.js (HEAD lines 27-41 - prepared slot in comment):
```
// "no-restricted-syntax": [
//   "error",
//   {
//     selector: "ObjectExpression > Property[key.name='stdio'][value.value='ignore']",
//     message: "stdio:'ignore' is forbidden on the launch path (GATE-04)",
//   },
// ],
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ERR-01 Banner component + error-classifier + VoiceShell wiring</name>
  <files>apps/achilles-terminal/src/ui/Banner.tsx, apps/achilles-terminal/src/error-classifier.ts, apps/achilles-terminal/src/ui/VoiceShell.tsx, apps/achilles-terminal/src/state/useAchillesState.ts, apps/achilles-terminal/tests/ui/banner.test.tsx, apps/achilles-terminal/tests/error-classifier.test.ts</files>
  <read_first>
    - .planning/phases/19-distribution-publishing-skill-rewire/19-CONTEXT.md (D-10 pre-empt layout, D-11 screen-reader aria-live=assertive vs polite)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md (§Pattern 3 Banner code, §Code Example 3 error-classifier table, §Pitfall 7 errorNonce-in-deps guard, §Assumption A8 aria-role timer)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md (Banner.tsx + error-classifier.ts + VoiceShell.tsx MOD sections; S-6 layout pattern; S-7 readonly fields; S-8 chalk via colors.ts)
    - apps/achilles-terminal/src/ui/ScreenReader.tsx (full file - aria-role=timer precedent, useEffect-cleanup pattern, header docstring format)
    - apps/achilles-terminal/src/ui/VoiceShell.tsx (full file - HEAD JSX layout at lines 130-142)
    - apps/achilles-terminal/src/state/useAchillesState.ts (full file - existing hook patterns for useAchillesState, useAmplitude, useRingBuffer)
    - apps/achilles-terminal/src/session-events.ts (lines 44-52 SessionErrorClassification union; lines 208-223 SessionEvent discriminated union)
    - apps/achilles-terminal/src/circuit-breaker.ts (lines 83-88 ClassifiedErrorKind mapping pattern as analog)
  </read_first>
  <behavior>
    - banner.test.tsx: render Banner with vi.useFakeTimers(); assert NULL when classification is null; assert renders red text with "[error] <class> -- <suggestedAction>" when classification is non-null; assert auto-dismisses after 8000ms; assert dismisses early when successNonce bumps; assert visible state resets cleanly when errorNonce bumps mid-display (Pitfall 7 guard); assert aria-role="timer" + aria-label includes class + suggestedAction (D-11 screen-reader announcement; A8 timer-vs-status)
    - error-classifier.test.ts: assert classifyForBanner(c) returns the locked mapping for each of the 8 SessionErrorClassification union members (network, auth, rate_limit, server, mic_unavailable, playback_lost, claude_failed, unknown) per RESEARCH §Code Example 3 lines 715-748
    - Integration assertion in banner.test.tsx: VoiceShell + Banner together render banner as FIRST child of root <Box flexDirection="column"> per D-10 pre-empt layout (use ink-testing-library render + frame inspection)
  </behavior>
  <action>
    Per D-10/D-11 and RESEARCH §Pattern 3 + §Code Example 3. RED commits first (2 test files), then GREEN commits (4 source files).

    1. Write apps/achilles-terminal/tests/error-classifier.test.ts: imports SessionErrorClassification from session-events.js; calls classifyForBanner(c) for each of the 8 union members; asserts return shape matches the table from RESEARCH §Code Example 3 lines 715-748 (network->"network"/retrying...; auth->"auth"/check ELEVENLABS_API_KEY; rate_limit->"rate-limit"/ElevenLabs rate limit -- retrying in 30s; server->"server"/...; mic_unavailable->"sox"/Audio device lost -- restart Achilles; playback_lost->"ffplay"/...; claude_failed->"claude"/...; unknown->"unknown"/see ~/.achilles/achilles.log).

    2. Write apps/achilles-terminal/tests/ui/banner.test.tsx using ink-testing-library render() per Phase 16 test patterns. Use vi.useFakeTimers(). Tests:
       - T-BAN-01: renders nothing when classification=null
       - T-BAN-02: renders "[error] sox -- Audio device lost -- restart Achilles" when classification.class="sox" + suggestedAction="Audio device lost -- restart Achilles"
       - T-BAN-03: renders red color (Text color="red") and aria-role="timer" + aria-label contains class + suggestedAction
       - T-BAN-04: auto-dismisses after BANNER_AUTO_DISMISS_MS (8000ms) - vi.advanceTimersByTime(8000) makes component return null
       - T-BAN-05: dismisses immediately on successNonce bump (vi.advanceTimersByTime(4000) then bump successNonce - component returns null without waiting for 8000ms)
       - T-BAN-06: timer resets when errorNonce bumps mid-display (Pitfall 7 guard) - second error fires at t=4s, expects 8s more before dismiss (total t=12s)
       - T-BAN-07: full VoiceShell integration via render(<VoiceShell session={mockSession} />) where mockSession.emit("event", {type:"error",...}) fires; assert Banner is FIRST child of root Box, Status row is LAST child (D-10 pre-empt layout)

    3. Implement apps/achilles-terminal/src/error-classifier.ts per RESEARCH §Code Example 3 lines 707-755 + PATTERNS.md error-classifier.ts section. Export ClassifiedBanner interface with readonly fields (S-7 pattern). Export classifyForBanner(c) function. Use the table from RESEARCH §Code Example 3 verbatim (8 entries covering every SessionErrorClassification union member). Header docstring follows circuit-breaker.ts SHAPE per PATTERNS.md (phase/plan/task header, what-it-is, what-it-isn't, threat model, "No emojis (CLAUDE.md global)" line). Cite session-events.ts as the SOURCE-OF-TRUTH union.

    4. Implement apps/achilles-terminal/src/ui/Banner.tsx per RESEARCH §Pattern 3 lines 251-310 + PATTERNS.md Banner.tsx section. Use the imports from PATTERNS.md (JSX + useEffect/useState + Box/Text + SessionErrorClassification + ClassifiedBanner types). Component signature: `export function Banner({ classification, errorNonce, successNonce }: BannerProps): JSX.Element | null`. Constants: `BANNER_AUTO_DISMISS_MS = 8_000`. Three useEffect blocks per Pattern 3 (show on errorNonce bump; auto-dismiss on 8s timer with errorNonce in deps per Pitfall 7; dismiss on successNonce bump). Render path: early return null when !visible || classification === null; else `<Box aria-label={...} aria-role="timer"><Text color="red">{...}</Text></Box>` per A8 (timer not status). Header docstring follows ScreenReader.tsx shape with the Phase 19 Plan 02 Task 1 header + the Pitfall 7 reference + "No emojis (CLAUDE.md global)" line.

    5. Add `useErrorBanner(session)` hook to apps/achilles-terminal/src/state/useAchillesState.ts next to existing hooks (mirror useAchillesState / useAmplitude / useRingBuffer shapes). Returns `{ errorClass: ClassifiedBanner | null; errorNonce: number; successNonce: number }`. Subscribes to `session.on("event", evt => ...)`: on `evt.type === "error"` -> map evt.payload.classification through classifyForBanner -> set errorClass + bump errorNonce; on any other typed event -> bump successNonce. Cleanup via session.off in useEffect return.

    6. Modify apps/achilles-terminal/src/ui/VoiceShell.tsx per D-10 + PATTERNS.md VoiceShell.tsx MOD section + RESEARCH §Pattern 3 lines 314-334. Import Banner from "./Banner.js". Call `const { errorClass, errorNonce, successNonce } = useErrorBanner(session)` near the existing hook calls. Insert `<Banner classification={errorClass} errorNonce={errorNonce} successNonce={successNonce} />` as the FIRST child of the existing `<Box flexDirection="column">`. Leave the `{sr ? ... : ...}` ternary + `<StatusRow ... />` unchanged in their existing positions. Pattern S-6 (root Box layout) and S-8 (Ink Text color="red" not chalk import) both apply.

    7. NO emojis anywhere (CLAUDE.md global, also called out in every existing UI file's header per PATTERNS.md S-5). Use ASCII double-hyphen "--" in classifier messages (matches AUDIO_DEVICE_LOST_MESSAGE in child-exit-watchdog.ts:60 shape).
  </action>
  <verify>
    <automated>npm test --workspace apps/achilles-terminal -- --pool=forks tests/error-classifier.test.ts tests/ui/banner.test.tsx</automated>
    Type-check pass: `npm run typecheck --workspace apps/achilles-terminal` exits 0.
    Banner import in VoiceShell: `grep -c 'import.*Banner.*from.*Banner' apps/achilles-terminal/src/ui/VoiceShell.tsx` returns 1.
    useErrorBanner hook in useAchillesState: `grep -c 'export function useErrorBanner' apps/achilles-terminal/src/state/useAchillesState.ts` returns 1.
    aria-role="timer" + Pitfall 7 guard: `grep -cE 'aria-role="timer"|errorNonce.*lastErrNonce' apps/achilles-terminal/src/ui/Banner.tsx` returns >=2.
    classifyForBanner has 8 mapping entries: `grep -cE '^\s*(network|auth|rate_limit|server|mic_unavailable|playback_lost|claude_failed|unknown):' apps/achilles-terminal/src/error-classifier.ts` returns 8.
    No emoji: `LC_ALL=C grep -rP '[\x80-\xff]' apps/achilles-terminal/src/ui/Banner.tsx apps/achilles-terminal/src/error-classifier.ts` returns nothing.
  </verify>
  <done>
    - 2 test files green (banner.test.tsx with T-BAN-01..07; error-classifier.test.ts with 8 mapping assertions).
    - Banner.tsx >=40 LOC; error-classifier.ts >=30 LOC; both with header docstrings per project convention.
    - VoiceShell.tsx renders Banner as FIRST child of root Box (D-10 layout) verified via snapshot or grep.
    - useErrorBanner hook in useAchillesState.ts subscribes to session "event" channel and maps errors via classifyForBanner.
    - aria-role="timer" + aria-label contains class + suggestedAction (D-11 + A8 honored).
    - Pitfall 7 errorNonce-in-deps guard present in Banner.tsx useEffect.
    - Type-check pass; lint pass; no emoji anywhere.
    - One RED commit + one GREEN commit per task (S-1 TDD pattern).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ERR-03 watchdog wiring + ERR-08 unconditional logger fan-out + GATE-04 ESLint activation</name>
  <files>apps/achilles-terminal/src/session.ts, apps/achilles-terminal/eslint.config.js, apps/achilles-terminal/tests/session-err03-wiring.test.ts, apps/achilles-terminal/tests/session-err08-wiring.test.ts, apps/achilles-terminal/tests/eslint-stdio-ignore.test.ts</files>
  <read_first>
    - .planning/phases/19-distribution-publishing-skill-rewire/19-CONTEXT.md (CONTEXT.md Claude's Discretion stay-in-error-state on cap-exceeded; single-archive logger rotation; ESLint rule scope apps/achilles-terminal/ only)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md (§Pattern 4 ERR-03 wiring; §Pattern 5 ERR-08 wiring; §Pattern 6 ESLint activation; §Pitfall 8 AST selector limitation; §Code Example 4 ESLint slot; §Code Example 5 logger fan-out)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md (session.ts MOD section - VERIFY-then-extend; eslint.config.js MOD section - uncomment prepared slot; S-3 logger child(scope) fan-out; S-4 SessionEvent emit shape)
    - apps/achilles-terminal/src/session.ts (HEAD - especially constructor at line ~352, wireAudioBridges at lines 449-542, handleSoxExit at line 936-962, runVoice action handler around line 1100, this.stop() at lines 594-630, error emit call sites at lines 766-770, 805-814, 890-907)
    - apps/achilles-terminal/src/child-exit-watchdog.ts (full file - createChildExitWatchdog signature, AUDIO_DEVICE_LOST_MESSAGE constant, 3-in-10s sliding window, dispose method)
    - apps/achilles-terminal/src/structured-logger.ts (full file - createStructuredLogger signature, child(scope) method, DEFAULT_REDACT_PATTERNS export, flush + dispose methods, 10MB rotation strategy)
    - apps/achilles-terminal/eslint.config.js (full file - HEAD lines 27-41 prepared slot in comment block)
  </read_first>
  <behavior>
    - session-err03-wiring.test.ts: parse session.ts source (fs.readFileSync) and assert: (a) createChildExitWatchdog is called with label:"sox" at least once; (b) createChildExitWatchdog is called with label:"ffplay" at least once; (c) each onError callback emits a SessionEvent of type "error" with classification "mic_unavailable" OR "playback_lost"; (d) wiring happens inside wireAudioBridges (or equivalent). This is a regression guard against the single-arm wiring shape.
    - session-err08-wiring.test.ts: parse session.ts source and assert: (a) `createStructuredLogger` is called at runVoice entry; (b) `logger.info("run_voice_start", ...)` is called BEFORE any other side-effecting call; (c) `await logger.flush()` and `logger.dispose()` are wired into graceful-shutdown registration; (d) fan-out via `logger.child(scope)` is wired into mic-sox, tts-playback, stt-bridge, claude-bridge (per Pattern S-3).
    - eslint-stdio-ignore.test.ts: programmatic ESLint API (ESLint constructor + lintText) runs against a synthetic fixture string `spawn(cmd, args, { stdio: "ignore" })` against apps/achilles-terminal/eslint.config.js. Assert at least 1 message with rule id `no-restricted-syntax`. Then run against `spawn(cmd, args, { stdio: "inherit" })`; assert 0 messages from `no-restricted-syntax`. This validates the rule fires on the forbidden shape and stays silent on the sanctioned shape.
  </behavior>
  <action>
    Per RESEARCH §Pattern 4 + §Pattern 5 + §Pattern 6 + CONTEXT.md Claude's Discretion. RED commits first (3 test files), then GREEN commits (2 source files).

    1. Write apps/achilles-terminal/tests/session-err03-wiring.test.ts: use fs.readFileSync to load session.ts source; use regex assertions (grep-style) to confirm both `createChildExitWatchdog({\s*label:\s*["']sox["']` and `createChildExitWatchdog({\s*label:\s*["']ffplay["']` appear; confirm onError callbacks include `classification:\s*["'](mic_unavailable|playback_lost)["']`.

    2. Write apps/achilles-terminal/tests/session-err08-wiring.test.ts: same fs.readFileSync approach; assert: `createStructuredLogger()` appears at runVoice() entry (before the apiKey resolve); `logger.info("run_voice_start"` literal appears; `await logger.flush()` + `logger.dispose()` appear inside graceful-shutdown callback or registerShutdownHook block; fan-out scopes appear: `logger.child("sox")` or `logger.child("mic-sox")`, `logger.child("tts")`, `logger.child("stt")`, `logger.child("claude")`, `logger.child("sox-watchdog")`, `logger.child("ffplay-watchdog")` (at least 4 child scopes present).

    3. Write apps/achilles-terminal/tests/eslint-stdio-ignore.test.ts: import ESLint from "eslint"; instantiate with overrideConfigFile pointing at apps/achilles-terminal/eslint.config.js; lintText on the two fixtures (forbidden + sanctioned). Forbidden fixture must include `{ stdio: "ignore" }` literal. Sanctioned fixture uses `{ stdio: "inherit" }` literal. Assert message count > 0 for forbidden, == 0 from no-restricted-syntax for sanctioned.

    4. Implement ERR-03 wiring in apps/achilles-terminal/src/session.ts per RESEARCH §Pattern 4 lines 352-383 + PATTERNS.md session.ts MOD section. In wireAudioBridges (around lines 449-542): after both micSox + ttsPlayback handles exist, construct two `createChildExitWatchdog` calls:
       - sox: `{ label: "sox", child: this.micSox.child, respawnFactory: () => { this.micSox = createMicSox(<same opts captured in closure>); return this.micSox.child; }, onError: (msg) => this.emit("event", { type: "error", payload: { classification: "mic_unavailable", message: msg }, timestamp: Date.now() }), logger: this.logger.child("sox-watchdog") }`
       - ffplay: mirror shape with label:"ffplay", classification:"playback_lost", respawnFactory closing over ttsDeps + createTtsPlayback, logger:this.logger.child("ffplay-watchdog")
       Store both watchdog handles as `this.soxWatchdog` + `this.ffplayWatchdog`. Extend `this.stop()` (lines 594-630) to call `this.soxWatchdog?.dispose()` and `this.ffplayWatchdog?.dispose()`. Per CONTEXT.md Claude's Discretion: onError callback does NOT process.exit; the cap-exceeded path stays in error state so Phase 18 ERR-04 typed-input fallback continues to work.

    5. Implement ERR-08 wiring in apps/achilles-terminal/src/session.ts per RESEARCH §Pattern 5 lines 388-402 + §Code Example 5 lines 781-816 + PATTERNS.md session.ts MOD section. Confirm line 352 already constructs `this.logger = opts.logger ?? createStructuredLogger({})` UNCONDITIONALLY in Session constructor (Phase 17 substrate; PATTERNS.md says it's already there - VERIFY-then-extend). At runVoice() entry around line ~1100 (BEFORE the apiKey resolve): add `logger.info("run_voice_start", { pid: process.pid, argv, nodeVersion: process.version })`. In the graceful-shutdown registration: add `await logger.flush(); logger.dispose();`. Fan out via `logger.child(scope)` to mic-sox, tts-playback, stt-bridge, claude-bridge (per S-3 - some of these are already wired in Phase 17 lines 471-479 + 506-507; extend any missing). The new watchdog scopes from step 4 fall under this fan-out.

    6. Activate ESLint rule in apps/achilles-terminal/eslint.config.js per RESEARCH §Pattern 6 + §Code Example 4 + PATTERNS.md eslint.config.js MOD section. Uncomment the prepared slot at HEAD lines 27-41. Final shape inside the `rules:` block:
       ```
       "no-restricted-syntax": ["error", { selector: "ObjectExpression > Property[key.name='stdio'][value.value='ignore']", message: "stdio:'ignore' is forbidden on the launch path (GATE-04)" }]
       ```
       Per PATTERNS.md eslint.config.js note: keep the comment above the rule documenting Pitfall 8 (selector is literal-only; variable indirection + array form are accepted false-negatives). Per CONTEXT.md Claude's Discretion: this rule lives in apps/achilles-terminal/eslint.config.js ONLY - do not modify root eslint.config.js or any other workspace config.

    7. Verify post-activation `npm run lint --workspace apps/achilles-terminal -- --max-warnings 0` exits 0 (no source under apps/achilles-terminal/src/ currently uses `stdio: "ignore"` per PATTERNS.md note).
  </action>
  <verify>
    <automated>npm test --workspace apps/achilles-terminal -- --pool=forks tests/session-err03-wiring.test.ts tests/session-err08-wiring.test.ts tests/eslint-stdio-ignore.test.ts</automated>
    Lint pass: `npm run lint --workspace apps/achilles-terminal -- --max-warnings 0` exits 0.
    Watchdog presence: `grep -cE 'createChildExitWatchdog\(\{' apps/achilles-terminal/src/session.ts` returns >=2.
    Logger entry: `grep -c 'logger\.info."run_voice_start"' apps/achilles-terminal/src/session.ts` returns >=1.
    Logger fan-out scopes: `grep -cE 'logger\.child\("(mic-sox|tts|stt|claude|sox-watchdog|ffplay-watchdog)"\)' apps/achilles-terminal/src/session.ts` returns >=4.
    ESLint rule activated: `grep -c 'no-restricted-syntax' apps/achilles-terminal/eslint.config.js | head -1` shows the rule is present, not commented out (verify with: `node -e "import('./apps/achilles-terminal/eslint.config.js').then(c => console.log(JSON.stringify(c.default[0].rules)))"` shows the rule).
  </verify>
  <done>
    - 3 test files green: session-err03-wiring + session-err08-wiring + eslint-stdio-ignore.
    - apps/achilles-terminal/src/session.ts wireAudioBridges constructs both sox + ffplay watchdogs with onError emitting SessionEvent type:"error" + classification:"mic_unavailable"|"playback_lost".
    - apps/achilles-terminal/src/session.ts runVoice entry calls createStructuredLogger + logger.info("run_voice_start", {pid, argv, nodeVersion}) BEFORE any other side effect.
    - graceful-shutdown chain calls await logger.flush(); logger.dispose() (also disposes both watchdogs).
    - Logger fan-out to >=4 child scopes via logger.child(scope) per S-3.
    - apps/achilles-terminal/eslint.config.js no-restricted-syntax rule is ACTIVE (uncommented; not in a block comment).
    - npm run lint exits 0 (no existing src under apps/achilles-terminal violates the rule).
    - CONTEXT.md Claude's Discretion respected: cap-exceeded stays in error state (no process.exit); typed-input fallback covers user's path forward.
    - One RED commit + one GREEN commit per task (S-1 TDD pattern).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: install-skill subcommand port + cli.ts 6th dynamic-import gate (DIST-03)</name>
  <files>apps/achilles-terminal/src/install-skill.ts, apps/achilles-terminal/src/skill-symlink.ts, apps/achilles-terminal/src/cli.ts, apps/achilles-terminal/tests/install-skill.test.ts, apps/achilles-terminal/tests/cli-install-skill.test.ts</files>
  <read_first>
    - .planning/phases/19-distribution-publishing-skill-rewire/19-CONTEXT.md (D-03 SKILL.md is rewritten in Plan 01; INIT-07 invariant maintained)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md (§Open Question 1 ADD install-skill to apps/achilles-terminal; §Architectural Responsibility Map row 5 install-skill ports from v1.2; §Don't Hand-Roll skill discoverability filesystem convention)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md (install-skill.ts + skill-symlink.ts + cli.ts MOD sections; S-2 dynamic-import gate pattern)
    - apps/achilles-cli/src/commands/install-skill.ts (FULL FILE - v1.2 canonical 221 lines - the port source)
    - apps/achilles-cli/src/skill-symlink.ts (FULL FILE - v1.2 canonical 324 lines - the port source; module is pure, only imports node:path)
    - apps/achilles-cli/src/commands/install-skill.test.ts if exists (test patterns to mirror)
    - apps/achilles-terminal/src/cli.ts (FULL FILE - HEAD shape with 5 existing dynamic-import gates at lines 92-189; insertion site between transcripts and voice; INIT-07 top-level static imports at lines 1-15)
    - apps/achilles-terminal/tests/cli.test.ts (existing T8 test for INIT-07 invariant assertion)
    - apps/achilles-terminal/tests/integration/init-07-invariant.test.ts (cited in cli.ts header line 19 - the existing static-import budget assertion)
    - packages/achilles-skill/package.json (after Plan 01 - version 1.3.0 with publishConfig.access:public; the SKILL_PROMPTS_DIR exports stays unchanged)
  </read_first>
  <behavior>
    - install-skill.test.ts: full port from v1.2 install-skill.test.ts behaviors - test the symlink path (creates symlink at ~/.claude/skills/achilles/), the copy fallback path (Windows EPERM), the --force overwrite path, the already-installed idempotent path, the conflict error path, the not-permitted error path. Mock fs via the productionFs seam. Use the InstallSkillCommandOptions option bag for test injection.
    - cli-install-skill.test.ts: assert (a) cli.ts top-level static imports COUNT is exactly 3 (node:fs/promises, node:url, node:path - INIT-07 invariant); (b) the `install-skill` argv[0] branch uses `await import("./install-skill.js")` (dynamic import gate); (c) the branch calls installSkillCommand with the correct options bag including `force: argv.includes("--force")`; (d) the branch exits the process after the call (`return` or `process.exit(0)` to prevent fall-through to voice).
  </behavior>
  <action>
    Per RESEARCH §Open Question 1 (recommend ADD install-skill to apps/achilles-terminal) + PATTERNS.md install-skill.ts + skill-symlink.ts + cli.ts MOD sections + S-2 dynamic-import gate.

    RED commits first (2 test files), then GREEN commits (3 source files).

    1. Write apps/achilles-terminal/tests/install-skill.test.ts: port the v1.2 test patterns from apps/achilles-cli/src/commands/install-skill.test.ts. Tests cover:
       - T-IS-01: successful symlink creation at default destination (~/.claude/skills/achilles/) returns "symlink"
       - T-IS-02: idempotent already-installed path - second invocation returns "already-installed"
       - T-IS-03: --force overwrites an existing destination
       - T-IS-04: ExistingDestinationConflictError raised when destination exists, --force not passed
       - T-IS-05: SymlinkNotPermittedError raised on macOS/Linux when symlink permission denied
       - T-IS-06: Windows EPERM fallback - WINDOWS_FALLBACK_CODES handles EPERM/EACCES/EISDIR by copying instead

    2. Write apps/achilles-terminal/tests/cli-install-skill.test.ts: parse cli.ts source (fs.readFileSync) and assert:
       - INIT-07 invariant: top-level static import lines (lines 1-15 or until first non-import code) reference EXACTLY 3 modules: node:fs/promises, node:url, node:path
       - install-skill branch present: `argv[0] === "install-skill"` literal appears in main()
       - dynamic import gate: `await import("./install-skill.js")` appears INSIDE the install-skill branch
       - --force parse: `argv.includes("--force")` appears in the install-skill branch
       - return statement: the install-skill branch ends with `return;` or `process.exit(0)` (prevents fall-through)

    3. Port apps/achilles-terminal/src/skill-symlink.ts from apps/achilles-cli/src/skill-symlink.ts per PATTERNS.md skill-symlink.ts section - FULL VERBATIM PORT. The module is pure (no relative imports beyond node:path). All 9 exported surfaces port unchanged: InstallSkillSymlinkFs interface, InstallSkillSymlinkLogger interface, InstallSkillSymlinkOptions interface, InstallSkillSymlinkResult type (discriminated union), ExistingDestinationConflictError class, SymlinkNotPermittedError class, getErrorCode helper, WINDOWS_FALLBACK_CODES constant (Set with EPERM, EACCES, EISDIR), installSkillSymlink function. Header docstring: replace v1.2 references to `Plan 13-02` with `Phase 19 Plan 02 Task 3` and references to `apps/achilles-cli/src/commands/install-skill.ts` with `apps/achilles-terminal/src/install-skill.ts`. NO emoji.

    4. Port apps/achilles-terminal/src/install-skill.ts from apps/achilles-cli/src/commands/install-skill.ts per PATTERNS.md install-skill.ts section. Imports per PATTERNS.md install-skill.ts MOD section - the ONLY port-time path adjustment is `from "./skill-symlink.js"` (v1.3 flat layout) NOT `from "../skill-symlink.js"` (v1.2 commands/ subdirectory layout). Keep `import { SKILL_PROMPTS_DIR } from "@achilles/achilles-skill"`. Keep the InstallSkillCommandOptions interface, productionFs constant, defaultSkillSourceProvider helper (resolves SKILL_PROMPTS_DIR, ".."), buildStdoutLogger helper, installSkillCommand action handler verbatim. The action handler has three error branches: ExistingDestinationConflictError, SymlinkNotPermittedError, generic Error - all port unchanged. Header docstring updates per PATTERNS.md. NO emoji.

    5. Modify apps/achilles-terminal/src/cli.ts per PATTERNS.md cli.ts MOD section + S-2 dynamic-import gate. Insert a new branch AFTER the transcripts gate (line 164) and BEFORE the voice gate (line 176). The new branch is the SHAPE of the config gate (lines 134-140):
       ```
       // Phase 19 Plan 02 (DIST-03): install-skill subcommand dynamic-import gate.
       // Symlinks (or on Windows, copies) the skill bundle into ~/.claude/skills/achilles/.
       // INIT-07: install-skill.ts and its @achilles/achilles-skill import load only
       // here; cli.ts's static-import budget stays at {node:fs/promises, node:url, node:path}.
       if (argv[0] === "install-skill") {
         const force = argv.includes("--force");
         const { installSkillCommand } = await import("./install-skill.js");
         installSkillCommand({
           force,
           stdout: process.stdout,
           stderr: process.stderr,
           processExitImpl: (code) => process.exit(code),
         });
         return;
       }
       ```
       DO NOT touch top-level static imports (INIT-07 invariant). DO NOT modify the existing 5 gates.

    6. Verify INIT-07 invariant via cli-install-skill.test.ts (the existing tests/integration/init-07-invariant.test.ts continues to pass; the new test ADDS a positive assertion for install-skill).

    7. NO emojis (CLAUDE.md global). Use ASCII double-hyphen "--" in any error messages or log lines.
  </action>
  <verify>
    <automated>npm test --workspace apps/achilles-terminal -- --pool=forks tests/install-skill.test.ts tests/cli-install-skill.test.ts</automated>
    Type-check pass: `npm run typecheck --workspace apps/achilles-terminal` exits 0.
    Lint pass: `npm run lint --workspace apps/achilles-terminal -- --max-warnings 0` exits 0.
    INIT-07 regression test still passes: `npm test --workspace apps/achilles-terminal -- --pool=forks tests/integration/init-07-invariant.test.ts` exits 0.
    install-skill source surface: `[[ -f apps/achilles-terminal/src/install-skill.ts ]] && [[ -f apps/achilles-terminal/src/skill-symlink.ts ]]` returns 0.
    cli.ts gate presence: `grep -c 'argv\[0\] === "install-skill"' apps/achilles-terminal/src/cli.ts` returns 1.
    Dynamic-import gate: `grep -c 'await import.*install-skill' apps/achilles-terminal/src/cli.ts` returns 1.
    Top-level static-import count remains 3: count via `head -15 apps/achilles-terminal/src/cli.ts | grep -cE '^import .* from "node:(fs/promises|url|path)"'` returns 3.
    Top-level import budget held: `head -15 apps/achilles-terminal/src/cli.ts | grep -cE '^import .* from "(?!node:(fs/promises|url|path)").*"'` returns 0.
  </verify>
  <done>
    - 2 test files green: install-skill.test.ts (6 cases T-IS-01..06) + cli-install-skill.test.ts (4 assertions).
    - apps/achilles-terminal/src/install-skill.ts >=100 LOC ported from v1.2 with path adjustment to flat layout.
    - apps/achilles-terminal/src/skill-symlink.ts >=200 LOC fully verbatim port from v1.2 (9 exported surfaces).
    - apps/achilles-terminal/src/cli.ts has the 6th dynamic-import gate for install-skill inserted between transcripts and voice gates.
    - INIT-07 invariant held: cli.ts top-level static imports stay {node:fs/promises, node:url, node:path}; install-skill loaded only via dynamic import inside main().
    - DIST-03 install-skill command path covered (the SHA-256 source-of-truth + SKILL.md rewrite parts land in Plan 01 + Plan 03).
    - Type-check + lint pass.
    - No emojis (CLAUDE.md global).
    - One RED commit + one GREEN commit per task (S-1 TDD pattern).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user terminal -> session event channel | Banner subscribes to session "event" stream; malformed payload could crash Ink render or leak sensitive details to UI |
| user terminal -> structured logger sink | ERR-08 unconditional logger writes every session to disk; key/secret in event payload would persist on user's filesystem |
| OS file system -> install-skill target | Symlink creation at ~/.claude/skills/achilles/ - path traversal risk if SKILL_PROMPTS_DIR provider were user-controlled |
| Claude Code skill discovery -> apps/achilles-terminal/src/install-skill.ts | The install action must not create symlinks outside ~/.claude/skills/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Information Disclosure | apps/achilles-terminal/src/structured-logger.ts log file (ERR-08) | mitigate | DEFAULT_REDACT_PATTERNS (7 regex set from Phase 18) applies to every log line; the logger constructor enforces this unconditionally (Phase 17 substrate); ERR-08 wiring does NOT bypass the redaction pipeline (V7 mitigation) |
| T-19-07 | Repudiation | apps/achilles-terminal/src/session.ts launch path with stdio:"ignore" hides failures (v1.2 silent-launch shape) | mitigate | GATE-04 ESLint no-restricted-syntax rule activated at apps/achilles-terminal/eslint.config.js literal { stdio: "ignore" } shape forbidden on launch path; CI fails build on violation; Pitfall 8 documented accepted false-negative for variable indirection + array form |
| T-19-08 | Denial of Service | apps/achilles-terminal/src/session.ts watchdog respawn could infinite-loop on persistent device failure | mitigate | 3-in-10s bounded sliding window (Phase 17 substrate at child-exit-watchdog.ts:60); cap-exceeded stays in error state per CONTEXT.md Claude's Discretion (typed-input fallback still works); does NOT process.exit (PR4 carries the user's path forward) |
| T-19-09 | Tampering | apps/achilles-terminal/src/install-skill.ts symlink target outside ~/.claude/skills/ | accept | productionFs seam + SKILL_PROMPTS_DIR resolution uses path.resolve (no string concatenation); v1.2 port has been audit-clean; no user-controllable input crosses the boundary |
| T-19-10 | Information Disclosure | apps/achilles-terminal/src/ui/Banner.tsx renders raw error message to terminal | mitigate | error-classifier.ts maps raw classification to canned suggestedAction strings; the raw exception text never reaches the banner (banner displays only the classified summary); aria-label uses the same canned summary so screen reader receives no raw error details |
| T-19-11 | Elevation of Privilege | apps/achilles-terminal/src/cli.ts install-skill branch could execute arbitrary code if argv parsed incorrectly | mitigate | INIT-07 invariant: install-skill loaded via dynamic import gate inside main(); top-level static-import budget stays at 3 (node:fs/promises, node:url, node:path); the gate ONLY fires when argv[0]==="install-skill" exact match - no shell interpolation |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | Plan 02 installs zero new npm packages (RESEARCH §Package Legitimacy Audit confirms); ink + react + ESLint already vetted in Phase 15-18 |
</threat_model>

<verification>
- `npm test --workspace apps/achilles-terminal -- --pool=forks` full suite GREEN (existing 120 tests + 7 new Plan 02 tests = 127 total)
- `npm run lint --workspace apps/achilles-terminal -- --max-warnings 0` exits 0
- `npm run typecheck --workspace apps/achilles-terminal` exits 0
- LOOP-02 invariant: `git diff HEAD~ -- packages/voice-protocol packages/voice-stt packages/voice-tts packages/claude-code-bridge packages/achilles-skill/skill/prompts/companion.md` returns empty
- INIT-07 invariant: top-level static imports in apps/achilles-terminal/src/cli.ts remain 3 (node:fs/promises, node:url, node:path); install-skill is the 6th dynamic-import gate
- D-10/D-11 visual contract: Banner renders as FIRST child of root <Box> in VoiceShell.tsx with aria-role="timer" (A8) and aria-label="error <class> <action>"
- D-discretion respected: ERR-03 cap-exceeded stays in error state (no process.exit); Phase 18 ERR-04 typed-input fallback continues to function
- No emojis anywhere: `LC_ALL=C grep -rP '[\x80-\xff]' apps/achilles-terminal/src/ui/Banner.tsx apps/achilles-terminal/src/error-classifier.ts apps/achilles-terminal/src/install-skill.ts apps/achilles-terminal/src/skill-symlink.ts` returns nothing
</verification>

<success_criteria>
- M-06 met: ERR-01 banner pre-empts above status row; both visible during cascading failures; aria-live priority honored
- M-07 met: ERR-03 watchdog respawns bounded; cap-exceeded transitions to error state with "Audio device lost -- restart Achilles" while typed-input fallback stays active
- M-08 met: ERR-08 unconditional structured logger writes to ~/.achilles/achilles.log on every run; keys redacted via 7-regex; rotates at 10MB
- M-09 met: ESLint stdio:"ignore" forbid rule active in apps/achilles-terminal/eslint.config.js only; CI fails build if pattern appears on launch path
- M-11 met (Plan 02 contribution): LOOP-02 invariants held; voice packages + companion.md unchanged
- M-12 met (Plan 02 contribution): INIT-07 static-import budget held; install-skill is the 6th dynamic-import gate
- DIST-03 install-skill subcommand path covered (combined with Plan 01 SKILL.md rewrite + Plan 03 SHA-256 check)
- ERR-01, ERR-03, ERR-08 fully satisfied
- GATE-04 lint half active (CI gate validated in Plan 03 release workflow)
</success_criteria>

<output>
Create `.planning/phases/19-distribution-publishing-skill-rewire/19-02-SUMMARY.md` when done capturing:
- Banner.tsx render structure + auto-dismiss timer guard against Pitfall 7
- error-classifier.ts mapping table (8 classes covered)
- session.ts wireAudioBridges new construction calls (sox + ffplay watchdogs)
- session.ts runVoice entry logger fan-out summary (which scopes wired)
- eslint.config.js rule activation diff
- install-skill.ts + skill-symlink.ts port deltas from v1.2 (path adjustments)
- cli.ts new 6th dynamic-import gate insertion line range
- 7 test file pass output
- LOOP-02 + INIT-07 invariants held confirmation
</output>
