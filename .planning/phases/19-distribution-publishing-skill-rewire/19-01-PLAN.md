---
phase: 19-distribution-publishing-skill-rewire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/achilles-terminal/package.json
  - packages/achilles-skill/package.json
  - packages/achilles-skill/skill/SKILL.md
  - apps/achilles-terminal/tests/package-json-shape.test.ts
  - apps/achilles-terminal/tests/skill-md-contract.test.ts
  - apps/achilles-terminal/tests/achilles-skill-publish-config.test.ts
autonomous: true
requirements: [DIST-03]
tags: [distribution, skill-manifest, pre-publish]

must_haves:
  truths:
    - "LOOP-02 byte-for-byte invariant: packages/voice-protocol/, voice-stt/, voice-tts/, claude-code-bridge/, packages/achilles-skill/skill/prompts/companion.md untouched"
    - "INIT-07 static-import budget held: cli.ts top-level static imports stay {node:fs/promises, node:url, node:path}"
    - "No emojis anywhere (CLAUDE.md global)"
    - "Option 3 macOS distribution shape (no codesign, no Apple Dev ID, no compiled darwin binary)"
    - "apps/cli-darwin-arm64/ and apps/cli-darwin-x64/ directories absent from monorepo"
    - "@achilles/achilles-skill package.json shows version 1.3.0 + private:false + publishConfig.access:public"
    - "packages/achilles-skill/skill/SKILL.md describes terminal-only model with 8 narrowed allowed-tools entries"
    - "SKILL.md body contains literal BASH_MAX_TIMEOUT_MS=86400000 documentation prominently at top"
    - "SKILL.md body does NOT contain any of: Electron, floating UI, systemPreferences.askForMediaAccess, X-forwarding, Achilles.app, renderer process"
    - "apps/achilles-terminal/package.json optionalDependencies has exactly 3 entries (linux-arm64, linux-x64, win32-x64)"
    - "apps/achilles-terminal/package.json dependencies @achilles/achilles-skill pinned to 1.3.0"
    - "D-01: Drop the 2 darwin sibling packages entirely (no no-op shim) per the macOS Option 3 lock — bin shim falls through to JS bundle deterministically"
    - "D-02: Darwin sibling drop happens as PRE-publish work in Plan 01 (NOT in Plan 04's publish-then-cut deletion commit B); the 2 directories were never published so deleting them now is the only place that touches them"
    - "D-03: SKILL.md gets a FULL rewrite to the terminal-only model (not a one-line `achilles launch` -> `achilles voice` diff); the v1.2 manifest lies about the product and must be replaced wholesale"
    - "D-04: SKILL.md frontmatter allowed-tools narrowed to exactly 8 entries (5 subcommand patterns + 3 `which` invocations) matching Phase 18's wired subcommands"
    - "D-05: SKILL.md body contains BASH_MAX_TIMEOUT_MS=86400000 documentation prominently at the TOP of the body so operators do not get blindsided by the 600s Bash default cutting off long voice sessions"
    - "D-06: SKILL.md body shells out to `achilles voice` (the `#!/usr/bin/env node` shebang on dist/cli.js dispatches to Node 22+); Bun is documented as the preferred runtime on macOS for sub-500ms cold-start but NOT a prerequisite"
    - "D-07: SKILL.md rewrite drops ALL references to Electron, the floating window, Achilles.app, systemPreferences.askForMediaAccess, renderer process, X-forwarding requirements, and SSH-without-X-forwarding caveats; replaces with sox + ffmpeg + Node 22 prerequisites + parent-terminal-emulator TCC remediation note (cite Phase 18 INIT-06) + `achilles init` linear wizard walk"
  artifacts:
    - path: "apps/achilles-terminal/package.json"
      provides: "trimmed optionalDependencies (3 entries) + bumped @achilles/achilles-skill dep to 1.3.0"
      contains: '"@achilles/cli-linux-x64": "1.3.0"'
    - path: "packages/achilles-skill/package.json"
      provides: "publish-ready manifest with version 1.3.0, private:false, publishConfig.access:public"
      contains: '"private": false'
    - path: "packages/achilles-skill/skill/SKILL.md"
      provides: "terminal-only skill manifest with narrowed allowed-tools"
      contains: "BASH_MAX_TIMEOUT_MS=86400000"
    - path: "apps/achilles-terminal/tests/package-json-shape.test.ts"
      provides: "D-01 darwin sibling removal regression guard"
      min_lines: 25
    - path: "apps/achilles-terminal/tests/skill-md-contract.test.ts"
      provides: "D-04 frontmatter allowed-tools contract assertion (exactly 8 entries)"
      min_lines: 40
    - path: "apps/achilles-terminal/tests/achilles-skill-publish-config.test.ts"
      provides: "DIST-03 achilles-skill publish-config shape assertion"
      min_lines: 20
  key_links:
    - from: "apps/achilles-terminal/package.json"
      to: "packages/achilles-skill/package.json"
      via: "dependencies version pin"
      pattern: '"@achilles/achilles-skill": "1.3.0"'
    - from: "packages/achilles-skill/skill/SKILL.md"
      to: "Claude Code skill discovery"
      via: "frontmatter allowed-tools comma-separated string"
      pattern: "^allowed-tools: Bash\\(achilles voice"
---

<objective>
Pre-publish metadata + docs preparation for v1.3.0 release. Drops the 2 darwin sibling packages from the monorepo and from optionalDependencies (D-01/D-02), flips @achilles/achilles-skill from private:true / version:0.1.0 to private:false / version:1.3.0 with publishConfig.access:public (RESEARCH §Pitfall 2), and fully rewrites packages/achilles-skill/skill/SKILL.md from the v1.2 Electron-era 49-line manifest to the v1.3 terminal-only model per D-03/D-04/D-05/D-06/D-07.

Purpose: Lay the publish-ready foundation that Wave 2 (runtime hardening) and Wave 3 (publish workflow) build on. Without the darwin drop, optionalDependencies references non-existent packages. Without the achilles-skill flip, npm publish fails with EPRIVATE. Without the SKILL.md rewrite, the published manifest lies to operators about the Electron architecture.

Output: 3 modified files (package.json shapes + SKILL.md), 0 runtime/code changes, 3 NEW contract tests (Wave 0) acting as regression guards. No darwin directories. The plan is autonomous (no checkpoints) because every change is mechanical metadata / docs rewriting with deterministic verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-CONTEXT.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md
@.planning/phases/19-distribution-publishing-skill-rewire/19-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor uses these directly; no codebase exploration needed. -->

From apps/achilles-terminal/package.json (HEAD lines 46-52, current optionalDependencies):
- "@achilles/cli-darwin-arm64": "1.3.0"  -- DROP
- "@achilles/cli-darwin-x64": "1.3.0"    -- DROP
- "@achilles/cli-linux-arm64": "1.3.0"   -- KEEP
- "@achilles/cli-linux-x64": "1.3.0"     -- KEEP
- "@achilles/cli-win32-x64": "1.3.0"     -- KEEP

From apps/achilles-terminal/package.json (HEAD dependencies line ~33):
- "@achilles/achilles-skill": "0.1.0"    -- BUMP to 1.3.0 in this plan

From packages/achilles-skill/package.json (HEAD lines 1-10):
- "name": "@achilles/achilles-skill"
- "version": "0.1.0"        -- BUMP to "1.3.0"
- "private": true           -- FLIP to false
- (no publishConfig)        -- ADD: "publishConfig": { "access": "public" }

From packages/achilles-skill/skill/SKILL.md (HEAD lines 1-49, v1.2 Electron-era):
- Frontmatter: name, description (Electron-era), allowed-tools: Bash (broad)
- Body: 43 lines describing Electron + floating UI + systemPreferences.askForMediaAccess + X-forwarding

D-04 locked 8 allowed-tools entries (comma-separated single line per RESEARCH §Pitfall 6):
1. Bash(achilles voice *)
2. Bash(achilles init *)
3. Bash(achilles transcripts *)
4. Bash(achilles config *)
5. Bash(achilles latency *)
6. Bash(which achilles)
7. Bash(which sox)
8. Bash(which ffmpeg)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Darwin sibling drop + achilles-skill publish-config flip</name>
  <files>apps/achilles-terminal/package.json, packages/achilles-skill/package.json, apps/cli-darwin-arm64/ (DELETE), apps/cli-darwin-x64/ (DELETE), apps/achilles-terminal/tests/package-json-shape.test.ts, apps/achilles-terminal/tests/achilles-skill-publish-config.test.ts</files>
  <read_first>
    - .planning/phases/19-distribution-publishing-skill-rewire/19-CONTEXT.md (D-01, D-02, RESEARCH §Pitfall 2)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md (§Standard Stack table "Installation"; §Pitfall 2 EPRIVATE flag)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md (apps/achilles-terminal/package.json MOD section + packages/achilles-skill/package.json MOD section)
    - apps/achilles-terminal/package.json (HEAD state - especially optionalDependencies lines 46-52 + dependencies @achilles/achilles-skill pin)
    - packages/achilles-skill/package.json (HEAD state - confirm version:0.1.0 + private:true + absent publishConfig)
    - apps/cli-darwin-arm64/package.json (confirm not published, only scaffolded in Phase 15)
    - apps/cli-darwin-x64/package.json (confirm not published, only scaffolded in Phase 15)
  </read_first>
  <behavior>
    - package-json-shape.test.ts asserts apps/achilles-terminal/package.json optionalDependencies has EXACTLY 3 entries: @achilles/cli-linux-arm64, @achilles/cli-linux-x64, @achilles/cli-win32-x64
    - package-json-shape.test.ts asserts NO darwin entry appears in optionalDependencies (regex: /darwin/)
    - package-json-shape.test.ts asserts apps/achilles-terminal/package.json dependencies @achilles/achilles-skill pin equals "1.3.0"
    - achilles-skill-publish-config.test.ts asserts packages/achilles-skill/package.json has private:false, version "1.3.0", and publishConfig.access "public"
    - Filesystem assertions confirm apps/cli-darwin-arm64/ and apps/cli-darwin-x64/ directories are absent
  </behavior>
  <action>
    Per D-01/D-02: write the 2 NEW test files first (RED commits). Tests assert the target shape via JSON parse + filesystem existence checks. Use the existing test patterns in apps/achilles-terminal/tests/ for fs.readFileSync + JSON.parse + assert. Then apply the implementation (GREEN commits):

    1. Edit apps/achilles-terminal/package.json optionalDependencies block: REMOVE the 2 lines "@achilles/cli-darwin-arm64" and "@achilles/cli-darwin-x64"; keep the 3 surviving entries in alphabetical order.
    2. Edit apps/achilles-terminal/package.json dependencies: change "@achilles/achilles-skill" pin from "0.1.0" to "1.3.0".
    3. Edit packages/achilles-skill/package.json: change "version" from "0.1.0" to "1.3.0"; change "private" from true to false; ADD a "publishConfig" field with value { "access": "public" } per RESEARCH §Pitfall 2 + PATTERNS.md achilles-skill/package.json MOD section.
    4. Delete the entire apps/cli-darwin-arm64/ directory (rm -rf). Delete the entire apps/cli-darwin-x64/ directory (rm -rf). Use git rm -r if tracked.
    5. Verify monorepo lockfile + workspace install still resolves: `npm ci --include=optional --force` exits 0. Note: the actual install command runs once during verification; do not run apps automatically.
  </action>
  <verify>
    <automated>npm test --workspace apps/achilles-terminal -- --pool=forks tests/package-json-shape.test.ts tests/achilles-skill-publish-config.test.ts</automated>
    Filesystem check: `[[ ! -d apps/cli-darwin-arm64 ]] && [[ ! -d apps/cli-darwin-x64 ]]` returns 0.
    JSON shape check: `jq '.optionalDependencies | keys | length' apps/achilles-terminal/package.json` returns 3.
    JSON shape check: `jq '.private' packages/achilles-skill/package.json` returns `false`.
    JSON shape check: `jq -r '.publishConfig.access' packages/achilles-skill/package.json` returns `public`.
    Workspace resolution: `npm ci --include=optional --force` exits 0 (no broken workspace edges).
  </verify>
  <done>
    - Tests added: package-json-shape.test.ts (>=25 lines) + achilles-skill-publish-config.test.ts (>=20 lines).
    - Both tests are GREEN.
    - apps/cli-darwin-arm64/ and apps/cli-darwin-x64/ directories are absent (filesystem).
    - apps/achilles-terminal/package.json optionalDependencies has exactly 3 platform entries.
    - apps/achilles-terminal/package.json dependencies @achilles/achilles-skill pinned to 1.3.0.
    - packages/achilles-skill/package.json shows version 1.3.0, private:false, publishConfig.access:public.
    - npm ci --include=optional --force exits 0.
    - One RED commit + one GREEN commit per the TDD pattern (S-1 from PATTERNS.md).
  </done>
</task>

<task type="auto">
  <name>Task 2: Full SKILL.md rewrite to v1.3 terminal-only model + contract test</name>
  <files>packages/achilles-skill/skill/SKILL.md, apps/achilles-terminal/tests/skill-md-contract.test.ts</files>
  <read_first>
    - .planning/phases/19-distribution-publishing-skill-rewire/19-CONTEXT.md (D-03, D-04, D-05, D-06, D-07)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-RESEARCH.md (§Code Example 2 lines 644-702 the v1.3 SKILL.md target body; §Pitfall 6 allowed-tools delimiter; §Security Domain V13 skill API contract)
    - .planning/phases/19-distribution-publishing-skill-rewire/19-PATTERNS.md (packages/achilles-skill/skill/SKILL.md MOD section; section-by-section content table)
    - packages/achilles-skill/skill/SKILL.md (HEAD - all 49 lines of v1.2 content - read in full to understand what is being replaced)
    - packages/achilles-skill/skill/prompts/companion.md (LOOP-02 locked - DO NOT TOUCH; read first 5 lines to confirm the file exists, no edit)
  </read_first>
  <behavior>
    - skill-md-contract.test.ts parses frontmatter via gray-matter or manual YAML parse and asserts:
      - allowed-tools field is a SINGLE-LINE COMMA-SEPARATED STRING (not a YAML list)
      - allowed-tools value contains EXACTLY 8 patterns in order: Bash(achilles voice *), Bash(achilles init *), Bash(achilles transcripts *), Bash(achilles config *), Bash(achilles latency *), Bash(which achilles), Bash(which sox), Bash(which ffmpeg)
      - body contains the literal string "BASH_MAX_TIMEOUT_MS=86400000"
      - body does NOT contain any of: "Electron", "floating UI", "systemPreferences.askForMediaAccess", "X-forwarding", "Achilles.app", "renderer process"
      - body contains terminal-only language: "achilles voice", "sox", "ffmpeg", "Node 22"
  </behavior>
  <action>
    Per D-03 (full rewrite, not one-line diff): write skill-md-contract.test.ts first (RED commit) per RESEARCH §Wave 0 Gaps + PATTERNS.md SKILL.md MOD section. The test asserts the 8 locked allowed-tools entries, the body BASH_MAX_TIMEOUT_MS callout (D-05), and the absence of Electron-era language (D-07).

    Then implement (GREEN commit): replace packages/achilles-skill/skill/SKILL.md HEAD lines 7-49 (the Electron-era body) with the v1.3 terminal-only body from RESEARCH §Code Example 2 lines 653-702. Preserve the YAML triple-dash frontmatter shape (3 fields: name, description, allowed-tools).

    Frontmatter changes per D-04:
    - Keep "name: achilles" (unchanged)
    - Replace the description with the terminal-only model description from RESEARCH §Code Example 2 line 649 (single paragraph; mentions sox + Scribe v2 + claude -p subprocess + ElevenLabs Flash v2.5 via ffplay; cites achilles init for ELEVENLABS_API_KEY hierarchy)
    - Replace allowed-tools with the SINGLE-LINE COMMA-SEPARATED STRING (RESEARCH §Pitfall 6): "Bash(achilles voice *), Bash(achilles init *), Bash(achilles transcripts *), Bash(achilles config *), Bash(achilles latency *), Bash(which achilles), Bash(which sox), Bash(which ffmpeg)"

    Body changes per D-05/D-06/D-07 (use the 7-section structure from PATTERNS.md SKILL.md MOD section):
    1. H1 "# Achilles voice companion for Claude Code"
    2. BASH_MAX_TIMEOUT_MS=86400000 callout at TOP of body (D-05; one paragraph naming the 24-hour cap, the 600s default risk, and the consequence of leaving sox/ffplay/claude orphans)
    3. "## What it does" section (terminal TUI + sox + Scribe v2 + claude -p + Flash v2.5 via ffplay)
    4. "## Prerequisites" section (npm install -g achilles, Bun preferred on macOS, sox + ffmpeg per-platform install lines, ELEVENLABS_API_KEY via achilles init, macOS parent-terminal TCC note citing Phase 18 INIT-06)
    5. "## How to launch" section (achilles voice foreground; NOT detached; Bash tool waits for exit; BASH_MAX_TIMEOUT_MS reminder; missing binary diagnostics)
    6. "## How the spoken interaction works" section (same companion.md contract as v1.2; references companion.md)
    7. "## When the run fails" section (failure-override phrase + ERR-01 inline red banner)
    8. "## Privacy" section (key in keychain or encrypted file; tarball secret-scan; 7-regex log redaction; ELEVENLABS-only outbound; transcripts opt-in; ~/.achilles/achilles.log per ERR-08)

    Use ASCII double-hyphen "--" (NOT em-dash) per PATTERNS.md Pattern S-5. NO EMOJIS anywhere (CLAUDE.md global). LOOP-02 invariant: DO NOT touch packages/achilles-skill/skill/prompts/companion.md.
  </action>
  <verify>
    <automated>npm test --workspace apps/achilles-terminal -- --pool=forks tests/skill-md-contract.test.ts</automated>
    Frontmatter shape: `grep -c '^allowed-tools:' packages/achilles-skill/skill/SKILL.md` returns 1.
    8-entry count: `head -10 packages/achilles-skill/skill/SKILL.md | grep -o 'Bash([^)]*)' | wc -l` returns 8.
    Body assertion: `grep -c 'BASH_MAX_TIMEOUT_MS=86400000' packages/achilles-skill/skill/SKILL.md` returns >=1.
    Negative assertion: `grep -cE 'Electron|floating UI|systemPreferences|X-forwarding|Achilles\.app|renderer process' packages/achilles-skill/skill/SKILL.md` returns 0.
    LOOP-02 invariant: `git diff HEAD~ -- packages/achilles-skill/skill/prompts/companion.md` returns empty (companion.md unchanged).
    No emoji check: file is ASCII-only when checked with `file packages/achilles-skill/skill/SKILL.md` (or LC_ALL=C grep -P '[\x80-\xff]' returns nothing).
  </verify>
  <done>
    - skill-md-contract.test.ts written (>=40 lines) and GREEN.
    - packages/achilles-skill/skill/SKILL.md fully rewritten per D-03/D-04/D-05/D-06/D-07.
    - Frontmatter allowed-tools is single-line comma-separated string with exactly 8 entries per D-04.
    - Body contains BASH_MAX_TIMEOUT_MS=86400000 prominently at top per D-05.
    - Body contains no Electron / floating UI / systemPreferences / X-forwarding / Achilles.app / renderer process references per D-07.
    - packages/achilles-skill/skill/prompts/companion.md unchanged (LOOP-02 invariant held).
    - No emojis anywhere (CLAUDE.md global).
    - Contract test asserts all 4 categories: allowed-tools shape + 8-entry list + BASH_MAX_TIMEOUT_MS body + negative-Electron-language.
    - One RED commit (test only) + one GREEN commit (SKILL.md rewrite); per PATTERNS.md S-1 the docs-only rewrite is the ONE exception that doesn't strictly need RED/GREEN but gets a contract test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> skill manifest | The SKILL.md frontmatter allowed-tools defines what Claude Code is permitted to invoke; broadening accidentally grants more capability than intended |
| operator -> npm publish surface | The achilles-skill package becomes a public publish target via private:false flip; any secret/PII in that package's files becomes externally readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | packages/achilles-skill/skill/SKILL.md allowed-tools field | mitigate | D-04 locks 8 specific patterns; skill-md-contract.test.ts asserts exact match against the locked list; CI fails build if list drifts |
| T-19-02 | Elevation of Privilege | overly broad Bash() pattern in allowed-tools (e.g. broad "Bash" matching `achilles voice; rm -rf /`) | mitigate | Narrow patterns use `Bash(achilles voice *)` form; the `*` glob is whitespace-bounded per Claude Code's allowed-tools semantics (RESEARCH §Security Domain Known Threat Patterns row 2) |
| T-19-03 | Information Disclosure | packages/achilles-skill/skill/SKILL.md leaks Electron-era prerequisites that no longer apply | mitigate | D-03/D-07 full rewrite drops all Electron / floating UI / systemPreferences / X-forwarding language; contract test enforces absence via negative-substring assertions |
| T-19-04 | Spoofing | apps/cli-darwin-* npm package squat after directory deletion | accept | @achilles scope is private (CONTEXT.md D-01 reasoning); no squat risk on unpublished sub-package names |
| T-19-05 | Tampering | accidental edit to packages/achilles-skill/skill/prompts/companion.md during SKILL.md rewrite | mitigate | LOOP-02 invariant guard: contract test runs `git diff HEAD~ -- companion.md` shows zero diff; Phase 17 source-of-truth check covers byte-for-byte equality at publish time |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | Plan 01 installs zero new npm packages; no slopcheck triggers |
</threat_model>

<verification>
- npm test --workspace apps/achilles-terminal -- --pool=forks tests/package-json-shape.test.ts tests/skill-md-contract.test.ts tests/achilles-skill-publish-config.test.ts
- npm ci --include=optional --force exits 0 (workspace install resolves with the 3 surviving siblings)
- Filesystem absence: `[[ ! -d apps/cli-darwin-arm64 ]] && [[ ! -d apps/cli-darwin-x64 ]]` returns 0
- LOOP-02 invariant: `git diff HEAD~ -- packages/achilles-skill/skill/prompts/companion.md packages/voice-protocol packages/voice-stt packages/voice-tts packages/claude-code-bridge` returns empty
- INIT-07 invariant: `head -10 apps/achilles-terminal/src/cli.ts` shows static imports are still {node:fs/promises, node:url, node:path} (no cli.ts changes in this plan)
- No emojis: `LC_ALL=C grep -rP '[\x80-\xff]' packages/achilles-skill/skill/SKILL.md` returns nothing (excluding the em-dash check)
</verification>

<success_criteria>
- M-05 met: SKILL.md frontmatter allowed-tools has exactly 8 entries matching the locked list (D-04 contract test passes)
- M-11 met (Plan 01 contribution): LOOP-02 invariants held; voice packages + companion.md unchanged
- M-12 met (Plan 01 contribution): INIT-07 static-import budget held; cli.ts untouched in this plan
- D-01 satisfied: 2 darwin sibling directories deleted; optionalDependencies trimmed to 3
- D-02 satisfied: darwin drop lands in Plan 01 (PRE-publish), not Plan 04 (post-publish cleanup)
- D-03/D-04/D-05/D-06/D-07 satisfied: SKILL.md fully rewritten to terminal-only model with narrowed allowed-tools + BASH_MAX_TIMEOUT_MS callout
- DIST-03 partially satisfied (skill manifest part covered; install-skill subcommand in Plan 02; SHA-256 check in Plan 03)
</success_criteria>

<output>
Create `.planning/phases/19-distribution-publishing-skill-rewire/19-01-SUMMARY.md` when done capturing:
- Final shape of apps/achilles-terminal/package.json optionalDependencies
- Final shape of packages/achilles-skill/package.json publish-config block
- Diff stats for SKILL.md rewrite (lines removed / lines added)
- Confirmation of LOOP-02 + INIT-07 invariants held
- 3 contract test pass output
</output>
