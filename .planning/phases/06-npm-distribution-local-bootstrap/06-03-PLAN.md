---
phase: 06-npm-distribution-local-bootstrap
plan: 03
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - apps/bridge/src/cli.ts
  - apps/bridge/src/cli/daemon.ts
  - apps/bridge/src/cli/launch.ts
  - apps/bridge/src/daemon/daemon-manager.ts
  - apps/bridge/src/daemon/relay-connection.ts
  - apps/bridge/src/lib/local-state.ts
  - apps/bridge/tests/unit/daemon-manager.test.ts
  - apps/bridge/tests/unit/launch-command.test.ts
  - apps/bridge/tests/unit/relay-connection.test.ts
autonomous: true
requirements:
  - DIST-03
  - LAUNCH-04
must_haves:
  truths:
    - "The normal daemon path starts from saved local bootstrap state rather than raw `CODEX_MOBILE_USER_ID` or `CODEX_MOBILE_DEVICE_SESSION_ID` env wiring."
    - "Repeated local handoff invocations reuse one outbound-only bridge daemon instead of spawning duplicates."
    - "Bridge reconnects fetch fresh hosted connect tickets instead of storing or reusing a local server signing secret."
    - "Phase 06 ends with a local `handoff launch` seam that Phase 07 can call from Codex."
  artifacts:
    - path: "apps/bridge/src/daemon/daemon-manager.ts"
      provides: "single-daemon reuse and stale-lock handling"
      exports: ["ensureDaemonRunning", "readDaemonStatus", "startDetachedDaemon"]
    - path: "apps/bridge/src/cli/launch.ts"
      provides: "user-facing local launch/reuse command"
      contains: "daemon_reused"
    - path: "apps/bridge/src/daemon/relay-connection.ts"
      provides: "ticket-provider-based relay reconnects"
      contains: "ticketProvider"
    - path: "apps/bridge/src/cli/daemon.ts"
      provides: "local-state-driven daemon startup"
      contains: "--unsafe-env-override"
  key_links:
    - from: "apps/bridge/src/cli/launch.ts"
      to: "apps/bridge/src/daemon/daemon-manager.ts"
      via: "launch uses the daemon manager to reuse or start the background bridge"
      pattern: "ensureDaemonRunning"
    - from: "apps/bridge/src/cli/daemon.ts"
      to: "apps/bridge/src/daemon/relay-connection.ts"
      via: "daemon startup injects a hosted connect-ticket provider rather than raw env credentials"
      pattern: "ticketProvider"
    - from: "apps/bridge/src/daemon/daemon-manager.ts"
      to: "apps/bridge/src/lib/local-state.ts"
      via: "daemon lifecycle metadata persists through the local state files"
      pattern: "daemon.json"
---

<objective>
Replace the normal env-driven daemon path with reusable local launch orchestration.

Purpose: After the install surface and bootstrap state exist, the final Phase 06 step is making bridge startup ergonomic and stable. The local user should be able to start handoff without re-entering credentials and without accidentally spawning multiple competing bridge daemons.

Output: A daemon manager with PID and lock tracking, a state-driven daemon command that fetches hosted connect tickets, and a `handoff launch` command that reuses or starts the bridge automatically.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-npm-distribution-local-bootstrap/06-RESEARCH.md
@.planning/phases/06-npm-distribution-local-bootstrap/06-VALIDATION.md
@.planning/phases/06-npm-distribution-local-bootstrap/06-02-PLAN.md
@apps/bridge/src/cli.ts
@apps/bridge/src/cli/daemon.ts
@apps/bridge/src/daemon/relay-connection.ts
@apps/bridge/src/lib/local-state.ts
@apps/bridge/src/cli/pair.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add daemon PID, lock, and stale-process management</name>
  <files>apps/bridge/src/daemon/daemon-manager.ts, apps/bridge/src/lib/local-state.ts, apps/bridge/tests/unit/daemon-manager.test.ts</files>
  <read_first>.planning/phases/06-npm-distribution-local-bootstrap/06-RESEARCH.md, apps/bridge/src/lib/local-state.ts, apps/bridge/src/daemon/bridge-daemon.ts, resources/gsd-2/packages/pi-coding-agent/src/core/auth-storage.ts</read_first>
  <action>Create `apps/bridge/src/daemon/daemon-manager.ts` exporting these exact functions: `ensureDaemonRunning`, `readDaemonStatus`, and `startDetachedDaemon`. Use the exact state file `daemon.json` and the exact lock file `daemon.lock` under the XDG state dir from `local-state.ts`. Track these exact daemon status values: `starting`, `running`, `stale`, and `stopped`. Detect live processes with the exact pattern `process.kill(pid, 0)` and treat `ESRCH` as stale. Update `apps/bridge/src/lib/local-state.ts` so it can read and write the daemon status payload and the lock file path. Add `apps/bridge/tests/unit/daemon-manager.test.ts` covering a live PID, a stale PID, and lock reuse without duplicate spawns.</action>
  <verify>rg -n 'ensureDaemonRunning|readDaemonStatus|startDetachedDaemon|daemon.lock|daemon.json|starting|running|stale|stopped|process.kill\\(pid, 0\\)|ESRCH|describe\\(|it\\(' apps/bridge/src/daemon/daemon-manager.ts apps/bridge/src/lib/local-state.ts apps/bridge/tests/unit/daemon-manager.test.ts</verify>
  <acceptance_criteria>
    - `apps/bridge/src/daemon/daemon-manager.ts` exports `ensureDaemonRunning`
    - `apps/bridge/src/daemon/daemon-manager.ts` exports `startDetachedDaemon`
    - `apps/bridge/src/daemon/daemon-manager.ts` contains the exact file name `daemon.lock`
    - `apps/bridge/src/daemon/daemon-manager.ts` contains the exact expression `process.kill(pid, 0)`
    - `apps/bridge/src/daemon/daemon-manager.ts` contains the exact status `stale`
    - `apps/bridge/tests/unit/daemon-manager.test.ts` contains `describe(`
  </acceptance_criteria>
  <done>The CLI now has a single-daemon coordination layer instead of blind process spawning.</done>
</task>

<task type="auto">
  <name>Task 2: Make the daemon command and relay reconnect path boot from stored bridge state</name>
  <files>apps/bridge/src/cli/daemon.ts, apps/bridge/src/daemon/relay-connection.ts, apps/bridge/src/lib/local-state.ts, apps/bridge/tests/unit/relay-connection.test.ts</files>
  <read_first>apps/bridge/src/cli/daemon.ts, apps/bridge/src/daemon/relay-connection.ts, apps/bridge/src/lib/local-state.ts, apps/web/app/api/bridge/connect-ticket/route.ts, .planning/phases/06-npm-distribution-local-bootstrap/06-02-PLAN.md</read_first>
  <action>Update `apps/bridge/src/cli/daemon.ts` so the normal daemon path no longer requires `CODEX_MOBILE_USER_ID`, `CODEX_MOBILE_DEVICE_SESSION_ID`, or `CODEX_MOBILE_WS_TICKET_SECRET`. Instead, load the saved bootstrap config and credentials from `local-state.ts`, call the hosted `/api/bridge/connect-ticket` route, and construct `RelayConnection` from that response. Add an exact escape hatch flag `--unsafe-env-override` that preserves the old env-driven path only for tests or emergency debugging. Update `apps/bridge/src/daemon/relay-connection.ts` so `RelayConnectionOptions` accepts an exact async `ticketProvider` callback returning `{ relayUrl, ticket }`. Use that callback for the initial connect and every reconnect attempt instead of minting tickets locally. Extend `apps/bridge/tests/unit/relay-connection.test.ts` so reconnect behavior now asserts the `ticketProvider` is called twice and no local secret-mint path is used.</action>
  <verify>rg -n 'unsafe-env-override|ticketProvider|/api/bridge/connect-ticket|relayUrl|ticket|CODEX_MOBILE_USER_ID|CODEX_MOBILE_DEVICE_SESSION_ID|CODEX_MOBILE_WS_TICKET_SECRET|toHaveBeenCalledTimes\\(2\\)' apps/bridge/src/cli/daemon.ts apps/bridge/src/daemon/relay-connection.ts apps/bridge/tests/unit/relay-connection.test.ts</verify>
  <acceptance_criteria>
    - `apps/bridge/src/cli/daemon.ts` contains the exact flag `--unsafe-env-override`
    - `apps/bridge/src/daemon/relay-connection.ts` contains the exact option name `ticketProvider`
    - `apps/bridge/src/cli/daemon.ts` references the exact route `/api/bridge/connect-ticket`
    - `apps/bridge/src/daemon/relay-connection.ts` no longer references `mintWsTicket`
    - `apps/bridge/tests/unit/relay-connection.test.ts` asserts `toHaveBeenCalledTimes(2)`
    - The normal daemon path can resolve startup tickets without persisted `WS_TICKET_SECRET`
  </acceptance_criteria>
  <done>The daemon now starts and reconnects from stored bridge state and hosted ticket minting instead of local secret-based env wiring.</done>
</task>

<task type="auto">
  <name>Task 3: Add `handoff launch` and automatic daemon reuse for local startup</name>
  <files>apps/bridge/src/cli.ts, apps/bridge/src/cli/launch.ts, apps/bridge/src/daemon/daemon-manager.ts, apps/bridge/tests/unit/launch-command.test.ts, apps/bridge/tests/unit/daemon-manager.test.ts</files>
  <read_first>apps/bridge/src/cli.ts, apps/bridge/src/cli/daemon.ts, apps/bridge/src/daemon/daemon-manager.ts, .planning/phases/06-npm-distribution-local-bootstrap/06-RESEARCH.md, .planning/phases/06-npm-distribution-local-bootstrap/06-VALIDATION.md</read_first>
  <action>Create `apps/bridge/src/cli/launch.ts` and register a new top-level command `launch` in `apps/bridge/src/cli.ts`. Implement this exact behavior: if the daemon manager reports a live daemon with the stored `bridgeInstanceId`, print the exact status line `daemon_reused` and exit `0`; otherwise start a detached background `handoff daemon`, wait up to `5000` ms for `daemon.json` to report `status = "running"`, print the exact status line `daemon_started`, and exit `0`. Keep the bridge outbound-only; `launch` must not open a local inbound port or local HTTP control server. Add `apps/bridge/tests/unit/launch-command.test.ts` covering the `daemon_reused` and `daemon_started` paths plus the missing-bootstrap failure path.</action>
  <verify>rg -n 'launch|daemon_reused|daemon_started|5000|status = \"running\"|ensureDaemonRunning|describe\\(|it\\(' apps/bridge/src/cli.ts apps/bridge/src/cli/launch.ts apps/bridge/src/daemon/daemon-manager.ts apps/bridge/tests/unit/launch-command.test.ts</verify>
  <acceptance_criteria>
    - `apps/bridge/src/cli.ts` registers the exact command `launch`
    - `apps/bridge/src/cli/launch.ts` contains the exact output `daemon_reused`
    - `apps/bridge/src/cli/launch.ts` contains the exact output `daemon_started`
    - `apps/bridge/src/cli/launch.ts` waits up to the exact value `5000`
    - `apps/bridge/tests/unit/launch-command.test.ts` references `daemon_reused`
    - `apps/bridge/tests/unit/launch-command.test.ts` references `daemon_started`
  </acceptance_criteria>
  <done>Phase 06 now has a local launch seam that ensures one outbound-only bridge is available for later `/handoff` integration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `vitest run apps/bridge/tests/unit/daemon-manager.test.ts apps/bridge/tests/unit/launch-command.test.ts apps/bridge/tests/unit/relay-connection.test.ts` succeeds
- [ ] `rg -n 'ticketProvider|unsafe-env-override|daemon_reused|daemon_started|daemon.lock' apps/bridge/src/cli/daemon.ts apps/bridge/src/cli/launch.ts apps/bridge/src/daemon/daemon-manager.ts apps/bridge/src/daemon/relay-connection.ts` returns matches
- [ ] Re-running `handoff launch` reuses one daemon instead of spawning duplicates
</verification>

<success_criteria>

- The normal daemon path is driven by saved bridge bootstrap state instead of raw identity envs
- Bridge reconnects fetch fresh hosted connect tickets
- `handoff launch` automatically starts or reuses the local bridge without opening inbound ports
- Phase 07 can call a stable local launch seam instead of recreating daemon bootstrap logic
</success_criteria>

<output>
After completion, create `.planning/phases/06-npm-distribution-local-bootstrap/06-03-SUMMARY.md`
</output>
