---
phase: 18-init-wizard-config-transcripts-single-instance-lock
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - apps/achilles-terminal/src/init/preflight.ts
  - apps/achilles-terminal/src/init/install-suggestions.ts
  - apps/achilles-terminal/src/init/ambient-calibration.ts
  - apps/achilles-terminal/src/init/parent-terminal.ts
  - apps/achilles-terminal/src/init/marker.ts
  - apps/achilles-terminal/src/lock-file.ts
  - apps/achilles-terminal/src/structured-logger.ts
  - apps/achilles-terminal/tests/init/preflight.test.ts
  - apps/achilles-terminal/tests/init/install-suggestions.test.ts
  - apps/achilles-terminal/tests/init/ambient-calibration.test.ts
  - apps/achilles-terminal/tests/init/parent-terminal.test.ts
  - apps/achilles-terminal/tests/init/marker.test.ts
  - apps/achilles-terminal/tests/lock-file.test.ts
  - apps/achilles-terminal/tests/structured-logger.test.ts
autonomous: true
requirements:
  - INIT-03
  - INIT-04
  - INIT-06
  - SAFE-04
  - SAFE-01

must_haves:
  truths:
    - "preflight.ts runs which sox + which ffmpeg + which claude AND a real 1-second sox device-open smoke; returns a typed result table identifying every missing/failing binary"
    - "install-suggestions.ts returns the per-platform install line (brew/apt/choco) for every detected missing binary AND can invoke the package manager subprocess when which brew/apt/choco succeeds"
    - "ambient-calibration.ts spawns mic-sox for 5 seconds, accumulates 250 frames of RMS, picks the 10th percentile as the noiseFloor EWMA seed, and writes it to ~/.achilles/settings.json"
    - "parent-terminal.ts resolves process.ppid -> ps -p PPID -o comm= -> emulator string; the per-emulator remediation table covers VS Code/Cursor/iTerm2/Terminal.app/Ghostty/Warp/WezTerm/unknown"
    - "marker.ts read/write of ~/.achilles/init.json — achilles voice reads it via marker.hasInitMarker() to know whether to skip the wizard"
    - "lock-file.ts acquireLock() refuses to start when ~/.achilles/voice.lock contains a live PID (process.kill(pid, 0) succeeds); cleans up stale locks (kill returns ESRCH); releaseLock() unlinks the file idempotently"
    - "structured-logger.ts gains a 7th regex (xi_-prefix ElevenLabs new shape) added to DEFAULT_REDACT_PATTERNS; existing 6 regexes unchanged"
  artifacts:
    - path: "apps/achilles-terminal/src/init/preflight.ts"
      provides: "checkPreflight() returning { sox, ffmpeg, claude } with which-path + device-open verdict each"
      exports: ["checkPreflight", "PreflightResult", "BinaryCheck"]
      min_lines: 80
    - path: "apps/achilles-terminal/src/init/install-suggestions.ts"
      provides: "suggestInstallCommand(platform, missing[]) and invokePackageManager(platform, missing[])"
      exports: ["suggestInstallCommand", "invokePackageManager", "INSTALL_COMMANDS"]
      min_lines: 50
    - path: "apps/achilles-terminal/src/init/ambient-calibration.ts"
      provides: "calibrateAmbient(durationMs?) -> { noiseFloor, samples } and writeNoiseFloorToSettings()"
      exports: ["calibrateAmbient", "writeNoiseFloorToSettings", "DEFAULT_CALIBRATION_DURATION_MS"]
      min_lines: 60
    - path: "apps/achilles-terminal/src/init/parent-terminal.ts"
      provides: "resolveParentEmulator() and getRemediationHint(emulator)"
      exports: ["resolveParentEmulator", "getRemediationHint", "ParentEmulator", "REMEDIATION_TABLE"]
      min_lines: 60
    - path: "apps/achilles-terminal/src/init/marker.ts"
      provides: "hasInitMarker(), writeInitMarker(record), readInitMarker()"
      exports: ["hasInitMarker", "writeInitMarker", "readInitMarker", "INIT_MARKER_PATH", "InitMarker"]
      min_lines: 40
    - path: "apps/achilles-terminal/src/lock-file.ts"
      provides: "acquireLock(): {ok, runningPid?}; releaseLock(): void; — wraps Phase 17's LOCK_FILE constant"
      exports: ["acquireLock", "releaseLock", "isPidAlive", "LockState"]
      min_lines: 70
  key_links:
    - from: "ambient-calibration.ts"
      to: "audio/mic-sox.ts (Phase 16)"
      via: "createMicSox call with onFrame handler accumulating RMS samples"
      pattern: "createMicSox.*onFrame"
    - from: "parent-terminal.ts"
      to: "child_process execSync for `ps -p $PPID -o comm=`"
      via: "execSync(`ps -p ${ppid} -o comm=`, { encoding: 'utf8' })"
      pattern: "ps -p \\$\\{?ppid|process\\.ppid"
    - from: "lock-file.ts"
      to: "resume-session.ts LOCK_FILE constant (Phase 17)"
      via: "import { LOCK_FILE, ACHILLES_HOME } from './resume-session.js'"
      pattern: "LOCK_FILE"
---

<objective>
Land the six Wave 2 building-block modules that the wizard (Plan 03) composes into a linear flow: preflight (INIT-03), ambient calibration (INIT-04 half), parent-terminal detection (INIT-06), init-marker idempotency (INIT-05 substrate), single-instance lock enforcement (SAFE-04), and the structured-logger 7th regex (SAFE-01 hardening). All six modules are independent of each other and only depend on Phase 17's existing primitives (mic-sox, resume-session's LOCK_FILE constant, structured-logger's DEFAULT_REDACT_PATTERNS).

Purpose: Wave 2 turns the locked decisions in 18-CONTEXT.md into individually testable units before Plan 03 composes them under @clack/prompts. The wizard is a UX shell; the verifiable behaviour lives here. Splitting the modules across one wave keeps the executor context cost per task ~10-15% (small focused modules with strong test coverage).

Output: Six new source modules, one extension to structured-logger.ts, twelve unit-test files (one per source module + structured-logger reused), and zero file conflicts with Plan 01 or future Plan 03 modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-CONTEXT.md
@.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-VALIDATION.md
@.planning/research/PITFALLS.md
@apps/achilles-terminal/src/audio/mic-sox.ts
@apps/achilles-terminal/src/audio/vad-energy.ts
@apps/achilles-terminal/src/structured-logger.ts
@apps/achilles-terminal/src/resume-session.ts
@apps/achilles-terminal/src/graceful-shutdown.ts

<interfaces>
<!-- Types this plan creates that Plan 03 (wizard + transcripts + typed-input + config) and Plan 04 (cli.ts) consume. -->

From src/init/preflight.ts (NEW):
```
export type BinaryStatus = "ok" | "missing" | "device-failed";

export interface BinaryCheck {
  readonly name: "sox" | "ffmpeg" | "claude";
  readonly status: BinaryStatus;
  readonly path: string | null;        // resolved via `which`
  readonly stderr?: string;            // captured if device-open smoke failed
}

export interface PreflightResult {
  readonly sox: BinaryCheck;
  readonly ffmpeg: BinaryCheck;
  readonly claude: BinaryCheck;
  readonly allOk: boolean;
}

export function checkPreflight(deps?: PreflightDeps): Promise<PreflightResult>;
```

From src/init/install-suggestions.ts (NEW):
```
export interface InstallCommand {
  readonly cmd: string;       // e.g. "brew install sox ffmpeg"
  readonly canAutoInvoke: boolean; // true if `which brew`/`apt`/`choco` succeeds
}

export const INSTALL_COMMANDS: Readonly<Record<NodeJS.Platform, (missing: string[]) => string>>;

export function suggestInstallCommand(platform: NodeJS.Platform, missing: ReadonlyArray<"sox" | "ffmpeg" | "claude">): InstallCommand;
export function invokePackageManager(cmd: string, deps?: InvokeDeps): Promise<{ exitCode: number; stderr: string }>;
```

From src/init/ambient-calibration.ts (NEW):
```
export interface CalibrationResult {
  readonly noiseFloor: number;     // 10th-percentile RMS over the window
  readonly sampleCount: number;    // typically 250 (5s @ 20ms/frame)
  readonly durationMs: number;
}

export const DEFAULT_CALIBRATION_DURATION_MS = 5000;

export function calibrateAmbient(deps?: CalibrationDeps): Promise<CalibrationResult>;
export function writeNoiseFloorToSettings(noiseFloor: number, deps?: SettingsWriteDeps): Promise<void>;
```

From src/init/parent-terminal.ts (NEW):
```
export type ParentEmulator =
  | "iTerm2"
  | "Terminal"
  | "VSCode"
  | "Cursor"
  | "Ghostty"
  | "WezTerm"
  | "Warp"
  | "unknown";

export const REMEDIATION_TABLE: Readonly<Record<ParentEmulator, string>>;

export function resolveParentEmulator(deps?: ParentTerminalDeps): ParentEmulator;
export function getRemediationHint(emulator: ParentEmulator): string;
```

From src/init/marker.ts (NEW):
```
export interface InitMarker {
  readonly initializedAt: string;  // ISO timestamp
  readonly version: string;        // achilles-terminal package version at init time
  readonly apiKeySource: "env" | "keychain" | "encrypted-file";
}

export const INIT_MARKER_PATH: string; // path.join(ACHILLES_HOME, "init.json")

export function hasInitMarker(): boolean;
export function readInitMarker(): InitMarker | null;
export function writeInitMarker(marker: InitMarker): void;
```

From src/lock-file.ts (NEW — wraps Phase 17's LOCK_FILE constant):
```
export type LockState =
  | { readonly ok: true }
  | { readonly ok: false; readonly runningPid: number; readonly emulator?: ParentEmulator };

export function acquireLock(deps?: LockFileDeps): LockState;
export function releaseLock(deps?: LockFileDeps): void;
export function isPidAlive(pid: number, killImpl?: (pid: number, sig: number) => boolean): boolean;
```

From src/structured-logger.ts (EXISTING — Plan 02 ADDS one regex):
```
const DEFAULT_REDACT_PATTERNS: ReadonlyArray<RegExp> = [
  // existing 6 regexes unchanged
  /Bearer\s+[A-Za-z0-9._-]+/g,
  /[a-zA-Z0-9_-]{32,}\.eyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+/g,
  /sk-[a-zA-Z0-9]{16,}/g,
  /xi-[a-zA-Z0-9-]{20,}/g,
  /ELEVENLABS_API_KEY=\S+/g,
  /[a-f0-9]{64,}/g,
  /xi_[a-zA-Z0-9]{40,}/g,  // NEW — ElevenLabs new key shape (xi_ underscore, 40+ chars)
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: preflight.ts + install-suggestions.ts (which + device-open smoke + platform commands)</name>
  <files>
    apps/achilles-terminal/src/init/preflight.ts
    apps/achilles-terminal/src/init/install-suggestions.ts
    apps/achilles-terminal/tests/init/preflight.test.ts
    apps/achilles-terminal/tests/init/install-suggestions.test.ts
  </files>
  <read_first>
    - apps/achilles-terminal/src/audio/mic-sox.ts (the sox command argv shape: `rec -q -t raw -r 16000 -b 16 -e signed -c 1 - trim 0 1` on POSIX; `sox.exe -q -d ...` on win32)
    - .planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-CONTEXT.md (Preflight INIT-03 decision block; install lines: `brew install sox ffmpeg`, `sudo apt install sox ffmpeg`, `choco install sox.portable ffmpeg`)
    - apps/achilles-terminal/src/circuit-breaker.ts (pattern reference: deps interface shape; classifyError seam)
  </read_first>
  <behavior>
    - checkPreflight runs `which sox` via execSync, captures stdout path; status=missing if exit!=0
    - When sox is found, checkPreflight spawns `sox -q -n -t raw -r 16000 -b 16 -e signed -c 1 - trim 0 1` (POSIX; win32 uses `sox.exe -q -d ... trim 0 1`) and waits up to 3 seconds for exit; status=device-failed if exit !=0 OR timeout — and captures stderr into BinaryCheck.stderr (the macOS EPERM string lives here, consumed by parent-terminal.ts in Plan 03)
    - Identical pattern for ffmpeg (`ffmpeg -version` only — ffmpeg lacks a 1-second device-open smoke that does not require a device; the version check suffices because PLAY-01 fails LOUDLY via Phase 17 child-exit-watchdog if ffmpeg runtime breaks)
    - claude: `claude --version`, exit 0
    - checkPreflight returns the full PreflightResult; allOk is true iff all three statuses are "ok"
    - The factory accepts execImpl + spawnImpl seams (tests mock both)
    - suggestInstallCommand("darwin", ["sox"]) returns { cmd: "brew install sox", canAutoInvoke: <which brew result> }
    - suggestInstallCommand("linux", ["sox", "ffmpeg"]) returns { cmd: "sudo apt install -y sox ffmpeg" }
    - suggestInstallCommand("win32", ["sox", "ffmpeg"]) returns { cmd: "choco install -y sox.portable ffmpeg" }
    - invokePackageManager(cmd) spawns a shell with the command and resolves with exit code + stderr
  </behavior>
  <action>
    Create `apps/achilles-terminal/src/init/preflight.ts`:

    1. Top docblock — Phase 18 Plan 02 Task 1 attribution. Cite INIT-03 ("which + real device-open smoke"). Note PITFALLS.md §1: the device-open smoke is what catches the "binary on PATH but mic disabled" silent-failure shape; without it, Phase 18 would inherit the v1.2 silent-launch class.

    2. Imports: `execSync, spawn, type SpawnOptions from "node:child_process"`. Declare `DEFAULT_DEVICE_OPEN_TIMEOUT_MS = 3000`.

    3. Internal helper `whichSync(bin: string, execImpl): string | null` — try `execImpl("which " + bin + " 2>/dev/null", { encoding: "utf8" })`; return trimmed stdout if string + non-empty; null on any throw or empty output. Windows fallback: `where.exe ${bin}`.

    4. Internal helper `runDeviceOpenSmoke(cmd: string, args: string[], spawnImpl, timeoutMs): Promise<{ exitCode: number | null; stderr: string }>`:
       - spawn child with stdio ["ignore", "ignore", "pipe"]
       - Accumulate stderr chunks
       - setTimeout to SIGTERM the child at timeoutMs; resolve with { exitCode: null, stderr: capturedStderr } if it fires
       - On natural exit resolve with { exitCode: code, stderr }
       - Use Phase 16 mic-sox.ts's exact argv shape for the sox smoke; for ffmpeg use `["-version"]` (no device touch — see Behavior note)

    5. Export `checkPreflight(deps)`:
       - For each of sox/ffmpeg/claude: whichSync; if null -> status="missing"
       - If found: run smoke
         - sox: `rec -q -t raw -r 16000 -b 16 -e signed -c 1 - trim 0 1` (POSIX) or `sox.exe -q -d ...` (win32) — match mic-sox.ts argv
         - ffmpeg: `ffmpeg -version` (no device test — keep INIT-03's "real device-open smoke (catches binary on PATH but device unavailable)" applies only to sox per CONTEXT.md decision block; ffmpeg failure modes surface at PLAY-01 runtime via Phase 17 watchdog)
         - claude: `claude --version`
       - On exit code 0 -> "ok"; non-zero or timeout -> "device-failed" with captured stderr
       - Set allOk = all three === "ok"

    Create `apps/achilles-terminal/src/init/install-suggestions.ts`:

    1. Top docblock — INIT-03 install-line lookup table.

    2. Export INSTALL_COMMANDS keyed by platform: darwin -> "brew install sox ffmpeg" (subset filtered by `missing`); linux -> "sudo apt install -y sox ffmpeg"; win32 -> "choco install -y sox.portable ffmpeg" (per CONTEXT.md "specifics" + research v1.3-terminal-pivot.md §10.3). For "claude" missing on any platform, the install line is "https://docs.claude.com/code" (claude has no package-manager line — it's a separate installer).

    3. Export suggestInstallCommand(platform, missing[]): filter the platform's command line to only the missing binaries; for "claude" missing, return the docs URL (canAutoInvoke=false).

    4. canAutoInvoke check: try execSync("which brew") / "which apt-get" / "where choco" and return true on exit 0.

    5. Export invokePackageManager(cmd, deps): spawn a shell with the command, return { exitCode, stderr }. On Linux this typically requires sudo; document that the wizard surfaces the sudo prompt — do NOT auto-elevate.

    Tests in `apps/achilles-terminal/tests/init/preflight.test.ts`:

    - "checkPreflight returns sox.status='ok' when whichSync resolves AND device-open smoke exit code is 0"
    - "checkPreflight returns sox.status='missing' when whichSync returns null"
    - "checkPreflight returns sox.status='device-failed' with stderr when device-open smoke exits non-zero"
    - "checkPreflight returns sox.status='device-failed' with stderr when the device-open smoke times out at 3s"
    - "checkPreflight returns allOk=false when any of sox/ffmpeg/claude is not ok"
    - "checkPreflight's allOk=true only when every binary reports ok"
    - "checkPreflight captures EPERM stderr verbatim into BinaryCheck.stderr (fixture: 'rec FAIL formats: can't open input ...permission denied')"

    Tests in `apps/achilles-terminal/tests/init/install-suggestions.test.ts`:

    - "suggestInstallCommand('darwin', ['sox']) returns 'brew install sox'"
    - "suggestInstallCommand('darwin', ['sox', 'ffmpeg']) returns 'brew install sox ffmpeg'"
    - "suggestInstallCommand('linux', ['sox', 'ffmpeg']) returns 'sudo apt install -y sox ffmpeg'"
    - "suggestInstallCommand('win32', ['sox', 'ffmpeg']) returns 'choco install -y sox.portable ffmpeg'"
    - "suggestInstallCommand('darwin', ['claude']) returns a docs URL string and canAutoInvoke=false"
    - "invokePackageManager spawns the supplied command via the injected spawnImpl seam and resolves with { exitCode, stderr }"

    Both source modules: explicit `stdio` arg on every spawn call MUST be either `"inherit"` or `["ignore", "pipe", "pipe"]` — NEVER `"ignore"` everywhere. The PITFALLS §1 / D-19 lint rule applies. No emojis.
  </action>
  <verify>
    <automated>npx vitest run apps/achilles-terminal/tests/init/preflight.test.ts apps/achilles-terminal/tests/init/install-suggestions.test.ts --pool=forks</automated>
  </verify>
  <acceptance_criteria>
    - Both source files exist with min LOC budgets met
    - `grep -E "rec -q -t raw -r 16000" apps/achilles-terminal/src/init/preflight.ts | wc -l` >= 1 (sox argv matches mic-sox.ts)
    - `grep -E "brew install|sudo apt|choco install" apps/achilles-terminal/src/init/install-suggestions.ts | wc -l` >= 3 (all three platforms covered)
    - `grep -vE "^\\s*\\*|^\\s*//" apps/achilles-terminal/src/init/preflight.ts | grep -E "stdio:\\s*[\"']ignore[\"']" | wc -l` == 0 (no full-ignore stdio on the launch path; PITFALLS §1)
    - Tests pass: `npx vitest run apps/achilles-terminal/tests/init/preflight.test.ts apps/achilles-terminal/tests/init/install-suggestions.test.ts --pool=forks` exits 0
    - Lint clean: `npm run lint --workspace apps/achilles-terminal -- apps/achilles-terminal/src/init/preflight.ts apps/achilles-terminal/src/init/install-suggestions.ts apps/achilles-terminal/tests/init/preflight.test.ts apps/achilles-terminal/tests/init/install-suggestions.test.ts` exits 0
  </acceptance_criteria>
  <done>preflight + install-suggestions modules shipped with 13 passing tests; sox argv matches Phase 16; no full-ignore stdio; all three platform install lines covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ambient-calibration.ts (5s mic capture -> 10th-percentile RMS -> settings.json)</name>
  <files>
    apps/achilles-terminal/src/init/ambient-calibration.ts
    apps/achilles-terminal/tests/init/ambient-calibration.test.ts
  </files>
  <read_first>
    - apps/achilles-terminal/src/audio/mic-sox.ts (createMicSox surface — onFrame, onExit, stop; the 640-byte/320-sample frame contract)
    - apps/achilles-terminal/src/audio/vad-energy.ts (DEFAULT_VAD_CONFIG; alpha=0.05; voiceThresholdRatio=3; initialNoiseFloor=0.005)
    - apps/achilles-terminal/src/store-stub.ts (Phase 16 stub — confirm Plan 02 is NOT replacing it; only ADDING a writer; Plan 03's config-menu owns the full loader rewrite)
    - .planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-CONTEXT.md (Ambient calibration block: 5s, 10th percentile, EWMA seed)
  </read_first>
  <behavior>
    - calibrateAmbient default 5000ms / 250 frames @ 20ms hop
    - Each frame's RMS computed inline (sqrt(mean(squared_int16_samples)) / 32768) so it lives in [0, 1] range matching the VAD's expected scale
    - At end: sort the RMS array ascending; 10th percentile = sorted[floor(0.10 * (n - 1))]; return as noiseFloor
    - The result is written to ~/.achilles/settings.json under `vad.initialNoiseFloor` (overrides the DEFAULT_VAD_CONFIG.initialNoiseFloor of 0.005 with the calibrated value)
    - settings.json is created/merged idempotently — never blow away the user's existing other settings
    - 0o600 perms enforced on settings.json
    - The factory accepts a `micSoxFactory` seam — tests inject a fake that emits synthetic frames at a controlled rhythm so the test runs deterministically without a real mic
    - The function is cancellable via the standard SIGINT path (rejected promise with reason "calibration_cancelled") so a user pressing Ctrl-C during the 5-second window does not hang the wizard
  </behavior>
  <action>
    Create `apps/achilles-terminal/src/init/ambient-calibration.ts`:

    1. Top docblock — Phase 18 Plan 02 Task 2. Cite INIT-04 (half: the calibration half; Plan 03's smoke test is the other half). Reference vad-energy.ts's DEFAULT_VAD_CONFIG.initialNoiseFloor (the value Phase 16 uses as a fallback when the user hasn't run init yet).

    2. Imports:
       - `import { createMicSox, type MicSoxHandle, type MicSoxOptions } from "../audio/mic-sox.js";`
       - `import { existsSync, readFileSync, writeFileSync, mkdirSync, chmodSync } from "node:fs";`
       - `import { homedir } from "node:os";`
       - `import { join } from "node:path";`

    3. Export `DEFAULT_CALIBRATION_DURATION_MS = 5000`.

    4. Internal helper `computeRms(frame: Int16Array): number`:
       - let sumSq = 0; for each sample s in frame: sumSq += (s * s)
       - rms = Math.sqrt(sumSq / frame.length) / 32768
       - return rms (range [0, 1])

    5. Internal helper `percentile10(samples: number[]): number`:
       - sorted = [...samples].sort((a, b) => a - b)
       - idx = Math.floor(0.10 * (sorted.length - 1))
       - sorted[idx] ?? 0.005 (default fallback matches DEFAULT_VAD_CONFIG.initialNoiseFloor)

    6. Export interface `CalibrationDeps`:
       - `durationMs?: number`
       - `micSoxFactory?: (opts: MicSoxOptions) => MicSoxHandle` (defaults to createMicSox)
       - `clockImpl?: () => number` (Date.now seam)
       - `setTimeoutImpl?: (cb: () => void, ms: number) => unknown` + `clearTimeoutImpl?`
       - `onProgress?: (elapsedMs: number, sampleCount: number) => void` (Plan 03 wizard surfaces progress via @clack/prompts spinner)
       - `signal?: AbortSignal` (Ctrl-C path)

    7. Export `async function calibrateAmbient(deps?: CalibrationDeps): Promise<CalibrationResult>`:
       - Open mic via micSoxFactory with onFrame collecting RMS into an array
       - Schedule a timeout for durationMs to stop the mic and resolve
       - On abort signal: stop mic + reject with "calibration_cancelled"
       - Periodically call onProgress (every 250ms)
       - When duration elapses: stop mic; compute 10th percentile; return { noiseFloor, sampleCount, durationMs }

    8. Export interface `SettingsWriteDeps` with `homedirImpl?: () => string`, `readFileImpl?`, `writeFileImpl?`, `chmodSyncImpl?` (all defaulted to node:fs).

    9. Export `async function writeNoiseFloorToSettings(noiseFloor: number, deps?: SettingsWriteDeps): Promise<void>`:
       - Resolve settingsPath = join(homedirImpl(), ".achilles", "settings.json")
       - mkdirSync parent { recursive: true, mode: 0o700 }
       - Read existing JSON (if any); parse; preserve top-level keys
       - Set `settings.vad = { ...settings.vad, initialNoiseFloor: noiseFloor }`
       - writeFileSync with mode: 0o600
       - chmodSync(settingsPath, 0o600) (explicit — umask defeat)

    Create `apps/achilles-terminal/tests/init/ambient-calibration.test.ts`:

    - "calibrateAmbient with a fake micSoxFactory emitting 250 frames of low-amplitude noise returns noiseFloor between 0.001 and 0.05 (typical quiet office)"
    - "calibrateAmbient with a fake emitting silent (all zeros) frames returns noiseFloor close to 0"
    - "calibrateAmbient duration defaults to 5000ms; sampleCount matches durationMs / 20ms hop"
    - "calibrateAmbient onProgress callback fires at least 19 times across 5 seconds (every 250ms)"
    - "calibrateAmbient rejects with 'calibration_cancelled' when abort signal fires before duration elapses"
    - "calibrateAmbient stops the micSoxHandle when duration elapses (assert handle.stop called)"
    - "writeNoiseFloorToSettings creates settings.json with { vad: { initialNoiseFloor: 0.012 } } when the file does not exist"
    - "writeNoiseFloorToSettings PRESERVES other top-level keys when the file already has them"
    - "writeNoiseFloorToSettings PRESERVES other vad keys when settings.vad already has voice_threshold etc."
    - "writeNoiseFloorToSettings enforces 0o600 perms via chmodSync"
    - "percentile10 helper returns sorted[floor(0.1 * (n - 1))] (assert against fixture of 100 values)"

    Inject all fs seams via deps so no test ever touches the real ~/.achilles/settings.json.
  </action>
  <verify>
    <automated>npx vitest run apps/achilles-terminal/tests/init/ambient-calibration.test.ts --pool=forks</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `apps/achilles-terminal/src/init/ambient-calibration.ts` (min 60 LOC)
    - `grep -E "from\s+\"../audio/mic-sox.js\"" apps/achilles-terminal/src/init/ambient-calibration.ts | wc -l` >= 1
    - `grep -E "0\\.10|0\\.1\\b" apps/achilles-terminal/src/init/ambient-calibration.ts | wc -l` >= 1 (10th percentile in source)
    - `grep -E "chmodSync.*0o600" apps/achilles-terminal/src/init/ambient-calibration.ts | wc -l` >= 1
    - `npx vitest run apps/achilles-terminal/tests/init/ambient-calibration.test.ts --pool=forks` exits 0; all 11 tests pass
    - Lint clean
  </acceptance_criteria>
  <done>Ambient calibration shipped with 11 passing tests; 10th-percentile RMS algorithm correct; settings.json merge preserves existing keys; 0o600 perms enforced; abort signal honored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: parent-terminal.ts + marker.ts (TCC emulator detection + init.json marker)</name>
  <files>
    apps/achilles-terminal/src/init/parent-terminal.ts
    apps/achilles-terminal/src/init/marker.ts
    apps/achilles-terminal/tests/init/parent-terminal.test.ts
    apps/achilles-terminal/tests/init/marker.test.ts
  </files>
  <read_first>
    - .planning/research/PITFALLS.md §3 (macOS TCC parent-emulator block in full — every emulator string this task must detect)
    - .planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-CONTEXT.md (Parent terminal detection decision: process.ppid -> ps -p PPID -o comm=; the 7-emulator remediation table)
    - apps/achilles-terminal/src/resume-session.ts (ACHILLES_HOME constant — INIT_MARKER_PATH derives from this)
  </read_first>
  <behavior>
    - resolveParentEmulator inspects process.ppid + execSync(`ps -p ${ppid} -o comm=`) on POSIX
    - On Windows the parent detection currently falls through to "unknown" (Windows TCC analog is N/A — INIT-06 explicitly targets macOS)
    - Emulator-string match table (case-insensitive, last-segment-of-path):
      - "iTerm" -> "iTerm2"
      - "Terminal" -> "Terminal"
      - "Code Helper" or "Code" or "VSCode" -> "VSCode"
      - "Cursor" or "Cursor Helper" -> "Cursor"
      - "ghostty" -> "Ghostty"
      - "wezterm" -> "WezTerm"
      - "Warp" or "stable" (Warp embeds itself as "stable") -> "Warp"
      - Anything else -> "unknown"
    - getRemediationHint(emulator) returns the locked CONTEXT.md "per-emulator remediation table" message per emulator
    - VSCode + Cursor emit the explicit "open Terminal.app once and re-run achilles init" hint per microsoft/vscode#307364
    - All other emulators emit "Open System Settings -> Privacy & Security -> Microphone and enable {emulator}"
    - The factory accepts a `execSyncImpl` seam + a `ppidImpl` seam so tests run hermetically
    - hasInitMarker() returns true iff ~/.achilles/init.json existsSync; readInitMarker returns parsed contents or null on parse failure
    - writeInitMarker writes JSON at 0o600 + ensures the parent directory mode is 0o700
  </behavior>
  <action>
    Create `apps/achilles-terminal/src/init/parent-terminal.ts`:

    1. Top docblock — Phase 18 Plan 02 Task 3. Cite INIT-06 + PITFALLS.md §3. State the VS Code special case (microsoft/vscode#307364: "VS Code does not propagate mic permission. Open Terminal.app once and run achilles init, then return to VS Code.").

    2. Imports: `execSync from "node:child_process"`. No others.

    3. Export type `ParentEmulator = "iTerm2" | "Terminal" | "VSCode" | "Cursor" | "Ghostty" | "WezTerm" | "Warp" | "unknown"`.

    4. Export the REMEDIATION_TABLE as a frozen `Record<ParentEmulator, string>`:
       - iTerm2 / Terminal / Ghostty / WezTerm / Warp: "Open System Settings -> Privacy & Security -> Microphone and grant {name} access. Restart the terminal after granting."
       - VSCode: "VS Code does not propagate microphone permission to its integrated terminal (microsoft/vscode#307364). Open Terminal.app once, run `achilles init`, then return to VS Code."
       - Cursor: "Cursor does not propagate microphone permission to its integrated terminal. Open Terminal.app once, run `achilles init`, then return to Cursor."
       - unknown: "Open System Settings -> Privacy & Security -> Microphone and enable your terminal emulator. If your terminal is not listed, try iTerm2 or Terminal.app as a known-working alternative."

    5. Internal map `EMULATOR_PATTERNS: ReadonlyArray<{ pattern: RegExp; emulator: ParentEmulator }>` — ordered most-specific first so e.g. "Code Helper" matches VSCode before a future emulator with "Code" in the name.

    6. Export `resolveParentEmulator(deps?)` — execSync(`ps -p ${ppid} -o comm=`, { encoding: "utf8" }).trim(); loop EMULATOR_PATTERNS; first match wins; default to "unknown" on any throw or non-match.

    7. Export `getRemediationHint(emulator) -> REMEDIATION_TABLE[emulator]`.

    Create `apps/achilles-terminal/src/init/marker.ts`:

    1. Top docblock — Phase 18 Plan 02 Task 3 (continuation). Cite INIT-04 ("writes ~/.achilles/init.json marker so `achilles voice` skips the wizard on next run") + INIT-05 (idempotent re-run reads the existing marker for "keep current" defaults).

    2. Imports: `existsSync, readFileSync, writeFileSync, mkdirSync, chmodSync from "node:fs"`; `homedir from "node:os"`; `join from "node:path"`.

    3. Export `INIT_MARKER_PATH = join(homedir(), ".achilles", "init.json")`.

    4. Export interface `InitMarker { readonly initializedAt: string; readonly version: string; readonly apiKeySource: "env" | "keychain" | "encrypted-file"; }`.

    5. Export hasInitMarker() -> existsSync(INIT_MARKER_PATH).

    6. Export readInitMarker() -> JSON.parse(readFileSync(...)) wrapped in try/catch -> null on any failure.

    7. Export writeInitMarker(marker) -> ensure parent dir 0o700; writeFileSync at 0o600; chmodSync explicit.

    Tests `apps/achilles-terminal/tests/init/parent-terminal.test.ts`:

    - "resolveParentEmulator maps 'iTerm2' execSync output to 'iTerm2'"
    - "resolveParentEmulator maps 'Code Helper' to 'VSCode'"
    - "resolveParentEmulator maps 'Cursor Helper' to 'Cursor'"
    - "resolveParentEmulator maps 'ghostty' (lowercase) to 'Ghostty'"
    - "resolveParentEmulator maps 'wezterm' to 'WezTerm'"
    - "resolveParentEmulator maps 'stable' (Warp's embedded shape) to 'Warp'"
    - "resolveParentEmulator maps unknown string 'kitty' to 'unknown'"
    - "resolveParentEmulator returns 'unknown' when execSync throws"
    - "getRemediationHint('VSCode') contains the substring 'microsoft/vscode#307364'"
    - "getRemediationHint('Cursor') contains the substring 'Cursor does not propagate'"
    - "getRemediationHint('unknown') suggests trying iTerm2 or Terminal.app as a known-working alternative"
    - "REMEDIATION_TABLE has an entry for every ParentEmulator value (no missing case)"

    Tests `apps/achilles-terminal/tests/init/marker.test.ts`:

    - "hasInitMarker returns false when the file does not exist"
    - "writeInitMarker -> readInitMarker round-trips a valid InitMarker"
    - "readInitMarker returns null when JSON.parse throws on corrupt content"
    - "writeInitMarker enforces 0o600 perms via chmodSync"
    - "writeInitMarker creates the parent ~/.achilles dir with 0o700 perms if missing"

    Both tests use tmpdir + homedirImpl injection; no real ~/.achilles touch.
  </action>
  <verify>
    <automated>npx vitest run apps/achilles-terminal/tests/init/parent-terminal.test.ts apps/achilles-terminal/tests/init/marker.test.ts --pool=forks</automated>
  </verify>
  <acceptance_criteria>
    - Both source files exist with min LOC budgets
    - `grep -E "microsoft/vscode#307364" apps/achilles-terminal/src/init/parent-terminal.ts | wc -l` >= 1 (VSCode hint cites the issue)
    - `grep -c "iTerm2\|Terminal\|VSCode\|Cursor\|Ghostty\|WezTerm\|Warp" apps/achilles-terminal/src/init/parent-terminal.ts` >= 7 (all 7 emulators present)
    - `grep -E "process\\.ppid|ps -p" apps/achilles-terminal/src/init/parent-terminal.ts | wc -l` >= 1
    - Tests pass: 17 cases total (12 + 5) green
    - Lint clean
  </acceptance_criteria>
  <done>Parent-terminal detection + init marker shipped; 17 passing tests; all 7 emulator strings mapped; VSCode + Cursor cite the upstream issue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: lock-file.ts (single-instance enforcement at startup) + structured-logger 7th regex</name>
  <files>
    apps/achilles-terminal/src/lock-file.ts
    apps/achilles-terminal/src/structured-logger.ts
    apps/achilles-terminal/tests/lock-file.test.ts
    apps/achilles-terminal/tests/structured-logger.test.ts
  </files>
  <read_first>
    - apps/achilles-terminal/src/resume-session.ts (Phase 17 LOCK_FILE = path.join(ACHILLES_HOME, "voice.lock"); the file already exists as a constant — Plan 02 wraps it with conflict-resolution semantics)
    - apps/achilles-terminal/src/graceful-shutdown.ts (process.once("exit") last-chance unlinkSync of LOCK_FILE — Plan 02's releaseLock MUST be idempotent with that path)
    - apps/achilles-terminal/src/structured-logger.ts (DEFAULT_REDACT_PATTERNS at lines 167-174; the 6-regex set Plan 02 extends to 7)
    - apps/achilles-terminal/tests/structured-logger.test.ts (existing test layout — Plan 02 adds new cases without breaking existing)
    - .planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-CONTEXT.md (Single-instance lock decision block + the 7th regex specifics block)
  </read_first>
  <behavior>
    - acquireLock() reads ~/.achilles/voice.lock; if absent, write `{ pid: process.pid, startTime: Date.now() }` and return { ok: true }
    - If lock exists: parse PID; isPidAlive(pid) -> true means another instance is running -> return { ok: false, runningPid }
    - If lock exists but isPidAlive(pid) -> false (process gone, file is stale) -> unlink + retry the acquire path
    - releaseLock() unlinkSync wrapped in try/catch (idempotent — ENOENT is expected when graceful-shutdown.ts already released it)
    - isPidAlive(pid) uses process.kill(pid, 0) — throws ESRCH if dead, throws EPERM if alive-but-not-ours (rare; treat as "alive" for safety), returns true on no-throw
    - structured-logger.ts's DEFAULT_REDACT_PATTERNS list grows from 6 to 7 entries; the new entry is `/xi_[a-zA-Z0-9]{40,}/g` (NEW ElevenLabs key shape: xi UNDERSCORE prefix, 40+ alphanumeric chars; distinct from the existing xi-hyphen pattern)
    - The 6 existing regexes are UNTOUCHED — byte-for-byte identical
    - A new structured-logger test asserts the 7th regex redacts a synthetic `xi_abc...` key
    - All other structured-logger tests still pass
  </behavior>
  <action>
    Create `apps/achilles-terminal/src/lock-file.ts`:

    1. Top docblock — Phase 18 Plan 02 Task 4. Cite SAFE-04 ("Single-instance lock at ~/.achilles/voice.lock with PID liveness check (kill -0); refuses to start second instance"). Cite Phase 17's resume-session.ts for the LOCK_FILE constant and graceful-shutdown.ts for the process.once("exit") last-chance cleanup. Note: lock-file.ts is the STARTUP-side enforcement; releaseLock is also called from gracefulShutdown step 7.

    2. Imports:
       - `import { existsSync, readFileSync, writeFileSync, unlinkSync, mkdirSync } from "node:fs";`
       - `import { LOCK_FILE, ACHILLES_HOME } from "./resume-session.js";`
       - (Optionally) `import type { ParentEmulator } from "./init/parent-terminal.js";` — the LockState may carry an emulator hint for the conflict message

    3. Export type `LockState`:
       ```
       | { readonly ok: true }
       | { readonly ok: false; readonly runningPid: number; readonly startTime?: number }
       ```

    4. Export interface `LockFileDeps`:
       - `lockFilePathImpl?: string` (tests inject tmp paths)
       - `killImpl?: (pid: number, sig: number) => boolean` (defaults to wrapping process.kill)
       - `pidImpl?: () => number` (defaults to () => process.pid)
       - `clockImpl?: () => number` (Date.now seam)

    5. Export `isPidAlive(pid: number, killImpl?: (pid: number, sig: number) => boolean): boolean`:
       - try killImpl(pid, 0); return true (no throw means process exists OR we lack perms — both treated as alive)
       - catch e where e.code === "ESRCH" -> return false (process gone, stale lock)
       - catch e where e.code === "EPERM" -> return true (alive, not ours; defensive)
       - catch all -> return true (default to "lock held" on unknown error — fail closed)

    6. Export `acquireLock(deps?)`:
       - mkdirSync(ACHILLES_HOME, { recursive: true, mode: 0o700 })
       - lockPath = deps?.lockFilePathImpl ?? LOCK_FILE
       - If !existsSync(lockPath) -> writeFileSync(JSON.stringify({ pid, startTime }), { mode: 0o600 }) -> return { ok: true }
       - existsSync: parse content; if invalid JSON -> treat as stale -> unlink -> retry
       - If isPidAlive(pid) true -> return { ok: false, runningPid: pid, startTime }
       - If false -> unlinkSync (stale) -> retry the write path

    7. Export `releaseLock(deps?)`:
       - try unlinkSync(lockPath); catch ENOENT -> no-op (idempotent with graceful-shutdown.ts's process.once("exit") unlink)

    Modify `apps/achilles-terminal/src/structured-logger.ts`:

    - EDIT only the DEFAULT_REDACT_PATTERNS array (lines 167-174 in the current file).
    - APPEND one new entry: `/xi_[a-zA-Z0-9]{40,}/g`
    - DO NOT touch any other line in the file
    - Update the docblock at lines 23-31 ("Default redaction patterns") to list the new entry below the existing six

    Tests `apps/achilles-terminal/tests/lock-file.test.ts`:

    - "acquireLock returns ok=true when lock file does not exist"
    - "acquireLock writes JSON { pid, startTime } with 0o600 perms"
    - "acquireLock returns ok=false with runningPid when lock exists AND isPidAlive(pid)==true"
    - "acquireLock unlinks stale lock and proceeds to acquire when isPidAlive(pid)==false"
    - "acquireLock treats malformed JSON content as stale and proceeds to acquire"
    - "releaseLock unlinks the lock file"
    - "releaseLock is idempotent — calling on a missing file does not throw"
    - "isPidAlive returns false when killImpl throws ESRCH"
    - "isPidAlive returns true when killImpl throws EPERM (alive, not ours)"
    - "isPidAlive returns true when killImpl returns without throw"

    Add to `apps/achilles-terminal/tests/structured-logger.test.ts` (DO NOT modify existing test cases — APPEND new ones):

    - "DEFAULT_REDACT_PATTERNS now contains 7 entries (one more than Phase 17 baseline of 6)"
    - "redaction of an xi_ underscore-prefixed 40+ char key is applied (fixture: 'xi_aBcDeFgHiJkLmNoPqRsTuVwXyZ0123456789AbCdEfGh' becomes [REDACTED] in the log line)"
    - "redaction of the EXISTING xi- hyphen-prefixed key is STILL applied (regression test — 7th regex did not break the 4th)"
    - "the new xi_ regex does NOT match short strings like 'xi_abc' (under 40 chars after the underscore)"

    Use tmpdir for the structured-logger tests' logDir injection so the assertions run hermetically.
  </action>
  <verify>
    <automated>npx vitest run apps/achilles-terminal/tests/lock-file.test.ts apps/achilles-terminal/tests/structured-logger.test.ts --pool=forks</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `apps/achilles-terminal/src/lock-file.ts` (min 70 LOC)
    - `grep -E "LOCK_FILE|ACHILLES_HOME" apps/achilles-terminal/src/lock-file.ts | wc -l` >= 2 (reuse Phase 17 constants — not duplicate)
    - `grep -E "isPidAlive|process\\.kill\\(.*,\\s*0\\)|killImpl\\(.*,\\s*0\\)" apps/achilles-terminal/src/lock-file.ts | wc -l` >= 2
    - structured-logger.ts grew by exactly 1 regex entry: `grep -cE "/xi_\\[a-zA-Z0-9\\]\\{40,\\}/g" apps/achilles-terminal/src/structured-logger.ts` >= 1
    - All 6 existing regexes still present (regression check): `grep -cE "Bearer\\\\s|eyJ|sk-\\[|xi-\\[|ELEVENLABS_API_KEY=|a-f0-9" apps/achilles-terminal/src/structured-logger.ts` >= 6
    - Tests pass: lock-file (10 cases) + structured-logger (existing + 4 new = at least baseline + 4) all green
    - Lint clean across lock-file.ts and the edited structured-logger.ts
  </acceptance_criteria>
  <done>Single-instance lock conflict resolution shipped; structured-logger gained the 7th regex without touching the existing 6; 14+ tests pass (10 new + existing structured-logger suite still green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.ppid -> ps -p PPID -o comm= command-substitution | Plan 02 reads the parent process name; an attacker could in principle race-modify the parent process, but the attack value is purely the wrong remediation hint (no privilege gained). |
| ~/.achilles/voice.lock content -> isPidAlive(parsed_pid) | If an attacker forges a lock file with a PID that happens to be alive but is NOT actually achilles, the second instance refuses to start — accept (this is fail-closed: worst case the user has to delete the lock file manually). |
| ambient calibration RMS -> ~/.achilles/settings.json | A malicious mic feed could nudge the noise floor up to mute the user's voice. Out of threat scope — same exposure as voice.lock content. |
| structured-logger log file -> reader on multi-user system | T-17-LG already mitigated via 0o600 perms. Plan 02's 7th regex narrows the exfiltration window further (catches the xi_-underscore shape). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Information Disclosure | xi_-prefix ElevenLabs key leaked to ~/.achilles/achilles.log | mitigate | New 7th regex `/xi_[a-zA-Z0-9]{40,}/g` added to DEFAULT_REDACT_PATTERNS in structured-logger.ts; test fixture proves the redaction; existing 6 regexes unchanged (regression test) |
| T-18-08 | Denial of Service | Stale lock file blocks legitimate restart after crash | mitigate | acquireLock unlinks the file and proceeds when isPidAlive(parsed_pid) returns false (ESRCH) |
| T-18-09 | Denial of Service | Two real instances both attempt mic capture | mitigate | acquireLock returns { ok: false, runningPid } when the parsed PID is alive; cli.ts (Plan 04) exits with the explicit message |
| T-18-10 | Tampering | An attacker forges a malformed JSON lock file to bypass the check | mitigate | acquireLock parses content try/catch; malformed JSON treated as stale -> unlinked -> retry. Net effect: an attacker can only force a stale-lock retry, not a permanent DoS. |
| T-18-11 | Information Disclosure | settings.json world-readable | mitigate | writeNoiseFloorToSettings enforces 0o600 via writeFileSync mode + explicit chmodSync; parent dir at 0o700 |
| T-18-12 | Spoofing | A malicious process names itself "iTerm2" to receive the iTerm2 remediation hint | accept | The remediation hint is informational only — no security action is gated by it. Worst case the user reads the wrong hint and switches terminal emulators (no privilege change). |
| T-18-13 | Information Disclosure | Init marker init.json reveals user uses Achilles + which key source | accept | The marker contains version + apiKeySource enum (3 values) + timestamp. Same exposure as the directory existing at all. 0o600 still applied. |
</threat_model>

<verification>
- Six new source files under `apps/achilles-terminal/src/init/` + `apps/achilles-terminal/src/lock-file.ts`
- Six new test files: 13 + 11 + 17 + 14 = 55 new test cases across Plan 02
- structured-logger.ts edit is one-line append + docblock update; existing test suite still green
- No file conflict with Plan 01: zero overlap in files_modified
- No file conflict with future Plan 03: Plan 02 writes settings.json via writeNoiseFloorToSettings; Plan 03's config-menu reads + extends but does NOT replace
- LOOP-02 unaffected: zero changes anywhere under `packages/voice-*`, `packages/claude-code-bridge/`, or `packages/achilles-skill/skill/prompts/companion.md`
- INIT-07 unaffected: cli.ts NOT modified (Plan 04 owns that)
- Phase 17 graceful-shutdown.ts NOT modified: the lock-file release path remains compatible with the existing process.once("exit") last-chance unlink
</verification>

<success_criteria>
Plan 02 succeeds when:
- [x] All six modules ship with the listed exports and pass min-LOC budgets
- [x] structured-logger.ts goes from 6 to 7 default-redaction regexes; the existing 6 are byte-for-byte unchanged
- [x] 55 new test cases pass under `npx vitest run apps/achilles-terminal/tests/init/ apps/achilles-terminal/tests/lock-file.test.ts apps/achilles-terminal/tests/structured-logger.test.ts --pool=forks`
- [x] Lint clean for all new + edited files
- [x] Phase 17 baseline tests still pass after the structured-logger edit
- [x] D-15-01 + D-15-02 + INIT-07 + LOOP-02 invariants preserved
</success_criteria>

<output>
Create `.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-02-SUMMARY.md` when done with:
- File-by-file LOC + test count + key behaviors implemented
- Confirmation of the 7th regex insertion + the 6 existing regexes unchanged
- The emulator-string mapping fixture coverage (all 7 + unknown)
- Confirmation the lock-file releaseLock path is idempotent with graceful-shutdown.ts's process.once("exit") unlinkSync
</output>
