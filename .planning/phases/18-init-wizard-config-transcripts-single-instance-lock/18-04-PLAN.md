---
phase: 18-init-wizard-config-transcripts-single-instance-lock
plan: 04
type: execute
wave: 4
depends_on:
  - 18-01
  - 18-02
  - 18-03
files_modified:
  - apps/achilles-terminal/src/cli.ts
  - apps/achilles-terminal/tests/cli.test.ts
  - apps/achilles-terminal/tests/integration/loop-02-host-allowlist.test.ts
  - apps/achilles-terminal/tests/integration/init-07-invariant.test.ts
autonomous: true
requirements:
  - INIT-01
  - INIT-04
  - SAFE-02
  - SAFE-03
  - SAFE-04
  - ERR-04
  - ERR-07

must_haves:
  truths:
    - "cli.ts extended with init / config / transcripts (list|purge) / latency (--report) subcommands; INIT-07 preserved (top-level static imports unchanged from Phase 15/16/17)"
    - "All new subcommands use `await import(...)` dynamic-import gates inside main(), entered AFTER argv parse"
    - "achilles --version still works without API key, sox, ffmpeg (INIT-07 T8 invariant)"
    - "achilles voice (Phase 17) still works unchanged; lock-file acquireLock added at startup with explicit conflict message on collision (SAFE-04 wire-up)"
    - "SAFE-03 (ElevenLabs-only host allowlist) verified via assertion test that packages/voice-protocol/src/transport.ts:assertElevenLabsHost still exists and accepts the allowed host + rejects an arbitrary one"
    - "Phase 17 baseline tests + Phase 18 Plans 01/02/03 tests all green"
    - "ROADMAP.md Phase 18 entry updated with Plans: list and 'Complete' status"
  artifacts:
    - path: "apps/achilles-terminal/src/cli.ts"
      provides: "Four new subcommand branches dynamically importing Plans 02/03 modules"
      contains: "init|config|transcripts|latency"
      min_lines: 200
    - path: "apps/achilles-terminal/tests/cli.test.ts"
      provides: "T10-T20 new test cases verifying each new subcommand routes correctly + INIT-07 invariant T8 still green"
    - path: "apps/achilles-terminal/tests/integration/loop-02-host-allowlist.test.ts"
      provides: "SAFE-03 assertion that assertElevenLabsHost is still present + functional"
      exports: []
    - path: "apps/achilles-terminal/tests/integration/init-07-invariant.test.ts"
      provides: "T8-equivalent assertion that cli.ts's top-level static imports remain { node:fs/promises, node:url, node:path }"
      exports: []
  key_links:
    - from: "cli.ts"
      to: "Plans 01-03 modules (api-key, preflight, wizard, transcripts/cli, config-menu, latency-report, lock-file)"
      via: "await import('./init/wizard.js'), etc. — dynamic gates after argv parse"
      pattern: "await import\\(\"\\./"
    - from: "cli.ts voice branch"
      to: "lock-file.ts acquireLock"
      via: "Invoked at the very start of the voice subcommand (after argv parse, before session.ts dynamic import)"
      pattern: "acquireLock|isPidAlive"
---

<objective>
Wire all Plans 01/02/03 modules into the cli.ts entry point under the strict INIT-07 invariant (top-level static imports stay `{ node:fs/promises, node:url, node:path }`; every new subcommand uses `await import(...)` AFTER argv parse). Add the SAFE-04 lock acquisition to the voice branch with the explicit conflict-message exit. Run two integration tests that prove the LOOP-02-locked SAFE-03 allowlist still works and the INIT-07 invariant is preserved. Update ROADMAP.md with the Phase 18 Plans list and mark Phase 18 complete.

Purpose: cli.ts is the gateway. Every Plan 01/02/03 module is dead code without this wiring. The four new subcommands (`init`, `config`, `transcripts list|purge`, `latency --report`) become the surfaces a user actually invokes. The voice branch gains the lock acquisition because SAFE-04 must fire BEFORE the pipeline boots — not after.

Output: cli.ts grows by 4 subcommand branches; tests cover each branch via spawnSync against tsx (matching the Phase 15/17 pattern); two integration tests prove the LOOP-02 + INIT-07 invariants; ROADMAP updated; Phase 18 closes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-CONTEXT.md
@.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-01-PLAN.md
@.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-02-PLAN.md
@.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-03-PLAN.md
@apps/achilles-terminal/src/cli.ts
@apps/achilles-terminal/tests/cli.test.ts
@packages/voice-protocol/src/transport.ts

<interfaces>
<!-- The cli.ts existing shape (Phase 15/16/17) — Plan 04 EXTENDS without touching INIT-07 invariant -->

Current cli.ts main() branch order (DO NOT REORDER):
1. argv parse for --version / -v -> print package.json version, exit 0
2. argv check for --latency-probe -> measure + print, exit 0
3. argv[0] === "latency" branch (Phase 17 Plan 04) — currently `latency --report` only
4. argv[0] === "voice" branch (Phase 16 Plan 04 / Phase 17 Plan 04) — dynamic import session.ts
5. default -> stderr "unknown command", exit 1

Plan 04 EXTENDS by inserting NEW branches BEFORE branch 5 (the default fallthrough):
- argv[0] === "init" -> dynamic import init/wizard.js
- argv[0] === "config" -> dynamic import config-menu.js
- argv[0] === "transcripts" + argv[1] ∈ ["list", "purge"] -> dynamic import transcripts/cli.js
- argv[0] === "voice" branch — REPLACED to acquire the lock BEFORE awaiting session.ts (SAFE-04 wire-up)

INIT-07 invariant (asserted in tests/cli.test.ts T8 + the new init-07-invariant.test.ts):
```
import { readFile } from "node:fs/promises";
import { fileURLToPath } from "node:url";
import { dirname, join } from "node:path";
```
NO other top-level static imports may be added. All new subcommand bodies live in `await import("./...")` dynamic gates inside main().

From src/lock-file.ts (Plan 02):
```
export function acquireLock(deps?): LockState;
export function releaseLock(deps?): void;
```

From src/init/wizard.ts (Plan 03):
```
export function runInitWizard(deps?: WizardDeps): Promise<WizardOutcome>;
```

From src/config-menu.ts (Plan 03):
```
export function runConfigMenu(deps?: ConfigMenuDeps): Promise<void>;
```

From src/transcripts/cli.ts (Plan 03):
```
export function transcriptsList(deps?): Promise<void>;
export function transcriptsPurge(deps?): Promise<void>;
```

From src/latency-report.ts (Plan 03):
```
export function runLatencyReport(deps?): Promise<string>;
```

From packages/voice-protocol/src/transport.ts (LOOP-02 locked; verified by grep at planning time at line 94):
```
export function assertElevenLabsHost(url: string | URL): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend cli.ts with init / config / transcripts / latency subcommands + voice lock acquisition</name>
  <files>
    apps/achilles-terminal/src/cli.ts
    apps/achilles-terminal/tests/cli.test.ts
  </files>
  <read_first>
    - apps/achilles-terminal/src/cli.ts (Phase 17 final shape — every existing branch must be preserved byte-for-byte; this task ADDS branches without reordering)
    - apps/achilles-terminal/tests/cli.test.ts (existing T1-T9 cases — Plan 04 adds T10+ without modifying T1-T9)
    - .planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-CONTEXT.md (INIT-07 invariant block — TOP-LEVEL STATIC IMPORTS stay { node:fs/promises, node:url, node:path })
  </read_first>
  <behavior>
    - `achilles init` runs the wizard; the cli branch does `const { runInitWizard } = await import("./init/wizard.js"); const outcome = await runInitWizard(); if (outcome.completed) process.exit(0); else if (outcome.cancelled) { process.stderr.write("achilles init: cancelled.\n"); process.exit(130); } else { process.exit(1); }`
    - `achilles config` runs `runConfigMenu()` then exits 0 on success, 1 on error
    - `achilles transcripts list` invokes transcriptsList(); `achilles transcripts purge` invokes transcriptsPurge(); both exit 0 on success
    - `achilles transcripts` (no subcommand) prints "achilles transcripts: try list or purge." to stderr and exits 1
    - `achilles latency --report` continues to invoke renderLatencyReport (Phase 17 path) — Plan 04 keeps the existing branch; alternative: replace the existing dynamic-import target to runLatencyReport (Plan 03 module). Decision: KEEP the Phase 17 branch (renderLatencyReport already does the work; runLatencyReport is the wrapper for Plan 04 cli.ts to dynamic-import — use runLatencyReport going forward to maintain a stable consumer surface)
    - `achilles voice` branch — INSERT lock acquisition: `const { acquireLock } = await import("./lock-file.js"); const lockState = acquireLock(); if (!lockState.ok) { process.stderr.write(\`Another achilles voice session is running (pid ${lockState.runningPid}). Press Ctrl-C in that terminal first.\n\`); process.exit(1); return; }` — THEN await runVoice as today
    - INIT-07 preserved: NO new top-level static import lines added. All new module references are via `await import(...)` inside main() AFTER the argv parse
    - `achilles unknown-cmd` still hits the default fallthrough and exits 1 with "unknown command" stderr (no regression to T4)
    - Each subcommand exits with proper code: 0 success / 1 error / 130 user-cancel
  </behavior>
  <action>
    Modify `apps/achilles-terminal/src/cli.ts` by inserting branches between the existing `voice` branch (currently at lines 102-106) and the default-fallthrough stderr write (lines 108-109).

    BEFORE EDITING: re-read the existing cli.ts file once (Read tool) so the exact line numbers + brace structure are clear. The shebang at line 1 + the top-level static imports at lines 34-36 + the uncaughtException/unhandledRejection registrations at lines 117-126 + the main().catch tail at lines 128-131 are LOAD-BEARING and stay byte-for-byte unchanged.

    EDIT plan:

    1. Update the docblock (lines 1-33) — add a paragraph describing the Plan 04 extension: "Phase 18 Plan 04 adds the `init`, `config`, `transcripts`, and `voice` lock-acquisition branches. Every new subcommand uses `await import(\"./...\")` dynamic gates inside main() so INIT-07's top-level static-import budget remains exactly { node:fs/promises, node:url, node:path }."

    2. In `main()`, before the `if (argv[0] === "voice")` block at line 102, ADD these branches in this order:

    ```
    if (argv[0] === "init") {
      const { runInitWizard } = await import("./init/wizard.js");
      const outcome = await runInitWizard();
      if (outcome.completed) {
        process.exit(0);
      } else if (outcome.cancelled) {
        process.stderr.write("achilles init: cancelled.\n");
        process.exit(130);
      } else {
        process.exit(1);
      }
      return;
    }

    if (argv[0] === "config") {
      const { runConfigMenu } = await import("./config-menu.js");
      await runConfigMenu();
      process.exit(0);
      return;
    }

    if (argv[0] === "transcripts") {
      const sub = argv[1];
      if (sub === "list") {
        const { transcriptsList } = await import("./transcripts/cli.js");
        await transcriptsList();
        process.exit(0);
        return;
      }
      if (sub === "purge") {
        const { transcriptsPurge } = await import("./transcripts/cli.js");
        await transcriptsPurge();
        process.exit(0);
        return;
      }
      process.stderr.write("achilles transcripts: try list or purge.\n");
      process.exit(1);
      return;
    }
    ```

    3. REPLACE the latency branch's dynamic-import target. Currently (line 83):
    ```
    const { renderLatencyReport } = await import("./latency-probe.js");
    ```
    becomes:
    ```
    const { runLatencyReport } = await import("./latency-report.js");
    ```
    and `const report = await renderLatencyReport();` becomes `const report = await runLatencyReport();`. Plan 03's runLatencyReport is the thin wrapper; the underlying renderLatencyReport still does the work.

    4. WRAP the existing `if (argv[0] === "voice")` block with the lock acquisition. Currently:
    ```
    if (argv[0] === "voice") {
      const { runVoice } = await import("./session.js");
      await runVoice(argv.slice(1));
      return;
    }
    ```
    becomes:
    ```
    if (argv[0] === "voice") {
      const { acquireLock } = await import("./lock-file.js");
      const lockState = acquireLock();
      if (!lockState.ok) {
        process.stderr.write(
          `Another achilles voice session is running (pid ${lockState.runningPid}). Press Ctrl-C in that terminal first.\n`,
        );
        process.exit(1);
        return;
      }
      const { runVoice } = await import("./session.js");
      await runVoice(argv.slice(1));
      return;
    }
    ```
    NOTE: releaseLock is owned by graceful-shutdown.ts's process.once("exit") (Phase 17). DO NOT add a releaseLock call here — it would be redundant and could race with the graceful-shutdown handler.

    5. DO NOT modify lines 1, 34-36 (the static imports), 38-39 (HERE constant), 117-131 (handlers + main().catch). The lint rule + the test T8 invariant assertion will fail if these lines drift.

    Modify `apps/achilles-terminal/tests/cli.test.ts` — APPEND new test cases (do NOT modify T1-T9):

    - T10 "achilles init: invokes runInitWizard via dynamic import" — spawnSync against tsx with argv [init]; assert exit code matches the wizard outcome. Since the real wizard is interactive, this test sends an immediate SIGINT and asserts the wizard cancels cleanly with exit 130 + the "cancelled" stderr message
    - T11 "achilles config: invokes runConfigMenu via dynamic import" — same SIGINT pattern; assert exit 130 OR 0 (config-menu may exit immediately on no-op)
    - T12 "achilles transcripts list: when ~/.achilles/transcripts/ is empty prints 'No transcripts on disk.' and exits 0" — set HOME to a tmpdir; spawnSync; assert exit 0 + stdout substring
    - T13 "achilles transcripts (no subcommand): prints try-list-or-purge stderr + exits 1"
    - T14 "achilles transcripts bogus: same fallback (exit 1)"
    - T15 "achilles latency --report: from a tmp HOME with empty latency/ dir prints 'samples=0' and exits 0" (cover the runLatencyReport delegation path)
    - T16 "achilles voice: respects lock file. With a stale lock (PID=999999 not alive), voice proceeds; with a live lock (parent test PID), voice exits 1 with the expected stderr substring 'Another achilles voice session is running'"
    - T17 "achilles --version still works after Plan 04 (INIT-07 invariant)" — same shape as T1 but with the Plan 04 file in place; explicit smoke test
    - T18 "achilles -v still works after Plan 04" — same as T3 with Plan 04 in place
    - T19 "achilles bogus-cmd still falls through to 'unknown command' stderr + exit 1" — T4 regression check after the new branches
    - T20 "achilles init: when readInitMarker returns null (no prior init), the wizard's idempotency defaults are the Plan 01/02 module defaults" — already covered by wizard.test.ts but adds the spawnSync-level integration check

    For tests requiring HOME isolation, set `process.env.HOME = tmpDir` in the spawn env so the test never touches the real user home. For SIGINT-driven tests, use the existing spawn helper at the bottom of cli.test.ts (the `sigintDelayMs` shape Plan 16/17 added).

    All new tests use spawnSync or the spawn-with-SIGINT helper. None mock @clack/prompts — the wizard's bail-on-Ctrl-C path runs through the real lib in these spawn-based tests; the wizard.test.ts unit tests (Plan 03) already covered every prompt seam.
  </action>
  <verify>
    <automated>npx vitest run apps/achilles-terminal/tests/cli.test.ts --pool=forks</automated>
  </verify>
  <acceptance_criteria>
    - cli.ts grew by ~70-100 LOC (4 new branches + 1 voice-branch lock wrapper)
    - Top-level static imports unchanged: `grep -E "^import\\s" apps/achilles-terminal/src/cli.ts | wc -l` == 3 (the three node: imports — readFile, fileURLToPath, dirname/join)
    - `grep -E "^import\\s+\\{[^}]+\\}\\s+from\\s+\"node:fs/promises\"" apps/achilles-terminal/src/cli.ts | wc -l` == 1
    - `grep -E "^import\\s+\\{[^}]+\\}\\s+from\\s+\"node:url\"" apps/achilles-terminal/src/cli.ts | wc -l` == 1
    - `grep -E "^import\\s+\\{[^}]+\\}\\s+from\\s+\"node:path\"" apps/achilles-terminal/src/cli.ts | wc -l` == 1
    - `grep -c "await import" apps/achilles-terminal/src/cli.ts` >= 6 (latency-report + lock-file + wizard + config-menu + transcripts/cli x2 + session)
    - `grep -E "acquireLock|lockState" apps/achilles-terminal/src/cli.ts | wc -l` >= 2 (lock acquisition wired in voice branch)
    - `grep -E "argv\\[0\\] ===\\s*\"(init|config|transcripts|voice|latency)\"" apps/achilles-terminal/src/cli.ts | wc -l` >= 5 (all 5 subcommand branches)
    - All existing T1-T9 tests still pass (no regression)
    - New T10-T20 tests pass (11 new cases)
    - `npm run lint --workspace apps/achilles-terminal -- apps/achilles-terminal/src/cli.ts apps/achilles-terminal/tests/cli.test.ts` exits 0 (no new lint errors in either file)
    - `npm run typecheck --workspace apps/achilles-terminal` exits 0
  </acceptance_criteria>
  <done>cli.ts grew by 4 subcommand branches + 1 lock wrapper; INIT-07 invariant preserved (top-level static imports byte-for-byte unchanged); 11 new cli.test.ts cases pass; existing T1-T9 still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SAFE-03 + INIT-07 invariant integration tests</name>
  <files>
    apps/achilles-terminal/tests/integration/loop-02-host-allowlist.test.ts
    apps/achilles-terminal/tests/integration/init-07-invariant.test.ts
  </files>
  <read_first>
    - packages/voice-protocol/src/transport.ts (assertElevenLabsHost at line 94 — verified by grep at planning time)
    - apps/achilles-terminal/src/cli.ts (post-Task-1 final shape — three top-level static imports unchanged)
    - apps/achilles-terminal/tests/integration/ (existing integration test layout — if dir does not exist, create it; mirror the tests/audio/ shape)
    - .planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-CONTEXT.md (SAFE-03 block — "ElevenLabs-only outbound network allowlist enforced at packages/voice-protocol/src/transport.ts:assertElevenLabsHost (carryover from v1.2, no change)")
  </read_first>
  <behavior>
    - loop-02-host-allowlist.test.ts asserts assertElevenLabsHost is still importable from "@achilles/voice-protocol" with its expected signature, accepts an api.elevenlabs.io host string, and throws on an arbitrary host (example.com)
    - init-07-invariant.test.ts is the file-level grep assertion (Phase 15 T8-style) — reads cli.ts as text and asserts the top-level static imports MATCH the exact 3 lines and NOTHING ELSE
  </behavior>
  <action>
    Create `apps/achilles-terminal/tests/integration/loop-02-host-allowlist.test.ts`:

    1. Top docblock — Phase 18 Plan 04 Task 2. Cite SAFE-03 + LOOP-02 invariant. State this test does NOT modify packages/voice-protocol — it imports + asserts. If the import fails, the LOOP-02 boundary has shifted and Phase 19 needs to address it.

    2. Imports: `import { describe, it, expect } from "vitest"; import { assertElevenLabsHost } from "@achilles/voice-protocol";` (the package is already a dependency — verify via package.json read in the test if the import errors)

    3. Test cases:
       - "assertElevenLabsHost is exported from @achilles/voice-protocol" — assert typeof === "function"
       - "assertElevenLabsHost('wss://api.elevenlabs.io/v1/...') returns the host without throwing" — assert result === "api.elevenlabs.io"
       - "assertElevenLabsHost('wss://example.com/v1/...') throws" — assert .toThrow()
       - "assertElevenLabsHost('https://malicious.io/abc') throws" — assert .toThrow()
       - "assertElevenLabsHost is invoked with the same surface v1.2 used (no signature drift)" — read transport.ts source via fs.readFileSync; grep for the export signature; assert it matches `export function assertElevenLabsHost(url: string | URL): string`

    Create `apps/achilles-terminal/tests/integration/init-07-invariant.test.ts`:

    1. Top docblock — Phase 18 Plan 04 Task 2. Cite INIT-07 (Phase 15 invariant + Phase 18 preservation contract). State this test enforces the file-level invariant: cli.ts top-level static imports must remain EXACTLY the three node: imports listed in Phase 15 Plan 01.

    2. Imports: `import { describe, it, expect } from "vitest"; import { readFileSync } from "node:fs"; import { fileURLToPath } from "node:url"; import { dirname, join, resolve } from "node:path";`

    3. Test cases:
       - "cli.ts top-level static imports are exactly the three node: imports" — read cli.ts; split into lines; count the top-level `^import\s` matches BEFORE the `async function main` definition; assert count === 3; assert each line matches one of the 3 expected patterns
       - "cli.ts contains no top-level `import .* from \"\\./` lines (no relative static imports)" — assert the grep count === 0
       - "cli.ts contains at least 6 `await import(` occurrences (the dynamic gates for init/config/transcripts/latency/voice/session)" — assert grep count >= 6
       - "cli.ts shebang line is exactly '#!/usr/bin/env node'" — Phase 15 T5 regression check at Plan 04
       - "cli.ts does NOT import from @clack/prompts, @napi-rs/keyring, @stablelib/nacl at top level (T-18-21 mitigation: these libs must not load before --version exits)" — assert grep count for each is 0 at top level (they MAY appear inside async function bodies via await import, which is fine)
       - "INIT-07 spawn smoke: `achilles --version` exits 0 in <500ms in a clean env (no ELEVENLABS_API_KEY, no PATH entries for sox/ffmpeg/claude)" — spawnSync with a stripped env; assert exit 0 + elapsed <500ms
  </action>
  <verify>
    <automated>npx vitest run apps/achilles-terminal/tests/integration/ --pool=forks</automated>
  </verify>
  <acceptance_criteria>
    - Both test files created under `apps/achilles-terminal/tests/integration/`
    - All 5 cases in loop-02-host-allowlist.test.ts pass
    - All 6 cases in init-07-invariant.test.ts pass
    - The grep-based file-level assertions match exactly (3 static imports; 0 top-level relative imports; 0 top-level @clack/prompts | @napi-rs/keyring | @stablelib/nacl imports)
    - The 500ms --version cold-start budget holds (sanity check against DIST-05's 200ms JS-fallback budget — JS fallback has 200ms; tsx-driven test allows 500ms to absorb tsx compile overhead in dev)
  </acceptance_criteria>
  <done>11 integration test cases prove the LOOP-02 host allowlist + INIT-07 invariant survive Phase 18.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run the full Phase 18 suite + update ROADMAP.md + write Phase 18 SUMMARY</name>
  <files>
    .planning/ROADMAP.md
    .planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-04-SUMMARY.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (lines 79-91: Phase 18 section needs Plans list + Completed status)
    - All Plan 01/02/03/04 SUMMARYs once they exist on disk (the executor writes 18-01-SUMMARY.md, 18-02-SUMMARY.md, 18-03-SUMMARY.md after their respective plans complete; this task aggregates)
  </read_first>
  <behavior>
    - Full suite green: `npm test --workspace apps/achilles-terminal` exits 0 with the Phase 17 baseline + all Phase 18 Plans 01-04 tests passing
    - Typecheck + lint green for every Phase 18 file added or modified
    - ROADMAP.md Phase 18 entry updated:
      - "Plans: 4 plans" (was "TBD")
      - Plans list with 4 entries each with [x] (Complete) and brief objective
      - Phase 18 row in the Progress table changes from "0/TBD Not started -" to "4/4 Complete YYYY-MM-DD"
    - 18-04-SUMMARY.md written with: cli.ts grew by ~80 LOC; all 12 phase requirements addressed; integration tests pass; LOOP-02 + INIT-07 invariants preserved; Phase 19 entry conditions met
  </behavior>
  <action>
    1. Run the full suite assertion:
       ```
       npm test --workspace apps/achilles-terminal
       npm run typecheck --workspace apps/achilles-terminal
       npm run lint --workspace apps/achilles-terminal -- $(git diff --name-only main..HEAD apps/achilles-terminal/src apps/achilles-terminal/tests | tr "\n" " ") 2>/dev/null || echo "lint with delta-only ran"
       ```
       All three must exit 0. If any fail, STOP and fix before proceeding to ROADMAP update.

    2. Update `.planning/ROADMAP.md` — Phase 18 section (currently lines 79-91 per planning context):
       - Change `**Plans**: TBD` (line 89) to `**Plans**: 4 plans`
       - APPEND below it the Plans list block:
         ```
         **Plans**: 4 plans
           - [x] 18-01-PLAN.md — Foundation: install @clack/prompts + @napi-rs/keyring + @stablelib/nacl; build env -> keychain -> encrypted-file API key resolver with 0o600 enforcement (INIT-02, SAFE-01)
           - [x] 18-02-PLAN.md — Modules: preflight + install-suggestions + ambient calibration + parent-terminal TCC detection + init.json marker + single-instance lock + structured-logger 7th regex (INIT-03, INIT-04, INIT-06, SAFE-04, SAFE-01)
           - [x] 18-03-PLAN.md — User surfaces: @clack/prompts wizard composing Plans 01/02 + smoke test + transcripts store/retention/CLI + typed-input fallback + config menu + latency-report wrapper (INIT-01, INIT-04, INIT-05, SAFE-02, ERR-04, ERR-07)
           - [x] 18-04-PLAN.md — cli.ts extension: init/config/transcripts/latency subcommand branches + voice lock acquisition + SAFE-03 + INIT-07 invariant integration tests + Phase 18 wrap (INIT-07 preserved; SAFE-03 verified; ROADMAP closed)
         ```
       - Update the Progress table row for Phase 18 (line 139 today):
         FROM: `| 18. Init Wizard + Config + Transcripts | v1.3 | 0/TBD | Not started | - |`
         TO:   `| 18. Init Wizard + Config + Transcripts | v1.3 | 4/4 | Complete   | YYYY-MM-DD |`
         (substitute today's actual date)

    3. Write `.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-04-SUMMARY.md` with:
       - Phase 18 outcome: 4/4 plans complete
       - Aggregate file count: ~14 new source files + ~14 new test files + 1 edited source (structured-logger.ts +1 regex +1 export keyword) + 1 edited source (cli.ts extension)
       - Aggregate test count: ~150+ new test cases (Plan 01: 25; Plan 02: 55; Plan 03: 52; Plan 04: 11 cli + 11 integration ≈ 22) = ~154 cases
       - All 12 requirement IDs covered (INIT-01..06, SAFE-01..04, ERR-04, ERR-07); INIT-07 preserved as cross-cutting invariant
       - Invariants honored: LOOP-02 (packages/voice-*, claude-code-bridge/, achilles-skill/skill/prompts/companion.md byte-for-byte unchanged); INIT-07 (cli.ts top-level static imports unchanged); D-15-01 (package name unchanged); D-15-02 (--include=optional --force install verified)
       - SKILL.md was NOT modified in Phase 18 (defer to Phase 19's "SKILL.md diff" task per CONTEXT.md recommendation)
       - Pre-existing Phase 17-01 lint debt (63 errors per deferred-items.md) is OUT OF SCOPE for Phase 18 and remains for Phase 19 to address
       - Phase 19 entry conditions: Apple Developer ID acquisition decision needed before Phase 19 starts; everything else (wizard, transcripts, config, lock-file, latency-report) is on disk and tested
       - Pending tech debt: SKILL.md update + the lint debt above; both Phase 19 work
  </action>
  <verify>
    <automated>npm test --workspace apps/achilles-terminal 2>&amp;1 | tail -10 &amp;&amp; grep -E "^\\s*-\\s*\\[x\\]\\s+18-0[1-4]-PLAN" .planning/ROADMAP.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace apps/achilles-terminal` exits 0 with at least Phase 17 baseline + Phase 18 Plans 01-04 cases (~150+ new cases) all green
    - `npm run typecheck --workspace apps/achilles-terminal` exits 0
    - ROADMAP.md Phase 18 entry shows "Plans: 4 plans" + 4 [x] entries
    - ROADMAP.md Progress table row for Phase 18 shows "4/4 Complete YYYY-MM-DD"
    - `.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-04-SUMMARY.md` exists with the required content
    - `grep -E "LOOP-02|INIT-07" .planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-04-SUMMARY.md | wc -l` >= 2 (invariants explicitly named)
    - `grep -RE "modified" packages/voice-protocol/src/transport.ts packages/voice-stt/ packages/voice-tts/ packages/claude-code-bridge/ packages/achilles-skill/skill/prompts/companion.md 2>/dev/null | wc -l` (verify file mtimes pre + post Phase 18 are within ~the planning-window — no diff against the Phase 17 frozen state); the audit script can use `git diff --stat main..HEAD packages/voice-* packages/claude-code-bridge/ packages/achilles-skill/skill/prompts/companion.md` and confirm the output is empty
  </acceptance_criteria>
  <done>Full Phase 18 suite green; ROADMAP closed with 4/4 Complete; 18-04-SUMMARY.md written; LOOP-02 + INIT-07 + D-15-01 + D-15-02 invariants confirmed preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cli.ts argv parse -> dynamic-import gates | The argv-string is user-supplied (or test-supplied via spawnSync). The dynamic-import gates execute MODULE TOP-LEVEL CODE on first reference — a malicious import path injection would be a serious vector, but argv is fully under our control (no string interpolation into the await import target). |
| ~/.achilles/voice.lock content -> cli.ts displays the PID in stderr | If the lock contains a forged PID, the stderr message displays that PID verbatim. Accept (worst case: misleading message to the user; user can manually delete the lock). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-21 | Information Disclosure | INIT-07 regression: a Plan 04 edit accidentally adds a top-level static import of @clack/prompts, causing the wizard library + its 30+ transitive deps to load on every `achilles --version` invocation, blowing the DIST-05 cold-start budget | mitigate | init-07-invariant.test.ts asserts the grep count for top-level @clack/prompts / @napi-rs/keyring / @stablelib/nacl imports is exactly 0; failure here flags the regression before merge |
| T-18-22 | Denial of Service | Two real instances both attempt mic capture | mitigate | cli.ts voice branch acquires the lock BEFORE the session.ts dynamic import; conflict path exits 1 with the explicit "Another achilles voice session is running (pid X)" message |
| T-18-23 | Tampering | Lock file forged with PID of a running unrelated process | mitigate via accept | isPidAlive returns true on EPERM (not our process); the user can manually delete the lock — this is the intentional fail-closed behavior. The risk of a real attacker is low for a single-user dev tool. |
| T-18-24 | Tampering | SAFE-03 LOOP-02 boundary violated by an accidental edit to packages/voice-protocol/src/transport.ts | mitigate | loop-02-host-allowlist.test.ts asserts assertElevenLabsHost is still exported and functional; the SHA-256 source-of-truth check (Phase 17 carryover) runs in CI per LOOP-02 |
| T-18-25 | Repudiation | The Phase 18 wrap claims completion without actually running the full test suite | mitigate | Task 3's verify command runs `npm test` AND counts the four [x] ROADMAP entries; a missing test or missing entry fails the gate |
</threat_model>

<verification>
- cli.ts grew by 4 new subcommand branches + 1 voice-branch lock wrapper; top-level static imports unchanged (verified at lint, typecheck, and test levels)
- 11 new cli.test.ts cases + 11 integration test cases pass
- Full suite green: Phase 17 baseline + Plans 01/02/03 + Plan 04 all pass
- ROADMAP.md Phase 18 entry shows Plans list with 4 entries (all [x]) + Progress table row "4/4 Complete YYYY-MM-DD"
- LOOP-02 invariant: zero diff in `packages/voice-*`, `packages/claude-code-bridge/`, `packages/achilles-skill/skill/prompts/companion.md`
- INIT-07 invariant: cli.ts top-level static imports unchanged (file-level grep asserted in init-07-invariant.test.ts)
- D-15-01: package.json `name` field still "achilles-terminal"
- D-15-02: install across the full workspace used `--include=optional --force`
- SKILL.md (`packages/achilles-skill/skill/SKILL.md`) NOT modified in Phase 18 per CONTEXT.md recommendation (defer to Phase 19)
- Pre-existing Phase 17-01 lint debt (per `.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/deferred-items.md`) remains as documented — out of scope for Phase 18
</verification>

<success_criteria>
Plan 04 succeeds when:
- [x] cli.ts has 4 new subcommand branches (init, config, transcripts, lock-wrapped voice); INIT-07 invariant preserved
- [x] 22 new tests pass (11 cli + 11 integration)
- [x] Full phase 18 suite green (~154 cases total + Phase 17 baseline)
- [x] ROADMAP.md Phase 18 entry closed with 4/4 Complete + the four Plans listed
- [x] 18-04-SUMMARY.md written with the aggregate Phase 18 outcome
- [x] LOOP-02 + INIT-07 + D-15-01 + D-15-02 invariants verifiably preserved
- [x] No emojis anywhere in the new source or documentation files
</success_criteria>

<output>
Create `.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-04-SUMMARY.md` when done. This is also the Phase 18 closing artifact (the SUMMARY of the final plan within the phase).
</output>
