---
phase: 18-init-wizard-config-transcripts-single-instance-lock
plan: 03
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified:
  - apps/achilles-terminal/src/init/wizard.ts
  - apps/achilles-terminal/src/init/smoke-test.ts
  - apps/achilles-terminal/src/transcripts/store.ts
  - apps/achilles-terminal/src/transcripts/retention.ts
  - apps/achilles-terminal/src/transcripts/cli.ts
  - apps/achilles-terminal/src/typed-input.ts
  - apps/achilles-terminal/src/config-menu.ts
  - apps/achilles-terminal/src/latency-report.ts
  - apps/achilles-terminal/tests/init/wizard.test.ts
  - apps/achilles-terminal/tests/init/smoke-test.test.ts
  - apps/achilles-terminal/tests/transcripts/store.test.ts
  - apps/achilles-terminal/tests/transcripts/retention.test.ts
  - apps/achilles-terminal/tests/transcripts/cli.test.ts
  - apps/achilles-terminal/tests/typed-input.test.ts
  - apps/achilles-terminal/tests/config-menu.test.ts
  - apps/achilles-terminal/tests/latency-report.test.ts
autonomous: true
requirements:
  - INIT-01
  - INIT-04
  - INIT-05
  - SAFE-02
  - ERR-04
  - ERR-07

must_haves:
  truths:
    - "wizard.ts orchestrates @clack/prompts steps linearly: API key resolution -> preflight -> ambient calibration -> smoke test -> marker write; idempotent re-run with 'keep current' defaults"
    - "Summary diff prints before any write; user must confirm before persistence"
    - "smoke-test.ts uses Phase 17 session.ts (createSession + runVoice path) for ONE round-trip with NO mocking; respects CLAUDE.md no-auto-running rule (only fires inside the wizard, never from vitest)"
    - "transcripts/store.ts writes JSONL at ~/.achilles/transcripts/<sid>.jsonl with secret redaction via structured-logger's 7-regex pattern; default OFF; opt-in via --save-transcripts"
    - "transcripts/retention.ts deletes files older than 30 days at startup"
    - "transcripts CLI handlers list (filename + first user-line preview) and purge (interactive delete-all / delete-older-than-N / cancel)"
    - "typed-input.ts wires @clack/prompts.text() as the STT-circuit-open fallback; the typed string flows through the same commitTranscript path as voice transcripts"
    - "config-menu.ts @clack/prompts.select menu reads + edits ~/.achilles/settings.json; preserves existing keys"
    - "latency-report.ts calls renderLatencyReport from Phase 17's latency-probe.ts; this module is the thin CLI-side wrapper Plan 04 dynamic-imports"
  artifacts:
    - path: "apps/achilles-terminal/src/init/wizard.ts"
      provides: "runInitWizard() main entry point composing all Plan 02 modules under @clack/prompts"
      exports: ["runInitWizard", "WizardOutcome", "WizardStep"]
      min_lines: 150
    - path: "apps/achilles-terminal/src/init/smoke-test.ts"
      provides: "runSmokeTest() one-utterance round-trip exercising Phase 17 session.ts"
      exports: ["runSmokeTest", "SmokeTestResult"]
      min_lines: 60
    - path: "apps/achilles-terminal/src/transcripts/store.ts"
      provides: "TranscriptStore — opt-in JSONL writer with redaction"
      exports: ["createTranscriptStore", "TranscriptEntry", "TranscriptStoreHandle"]
      min_lines: 80
    - path: "apps/achilles-terminal/src/transcripts/retention.ts"
      provides: "cleanupOldTranscripts(days?) deletes files older than threshold"
      exports: ["cleanupOldTranscripts", "DEFAULT_RETENTION_DAYS"]
      min_lines: 40
    - path: "apps/achilles-terminal/src/transcripts/cli.ts"
      provides: "transcriptsList() and transcriptsPurge() — the subcommand handlers Plan 04 dynamic-imports"
      exports: ["transcriptsList", "transcriptsPurge"]
      min_lines: 80
    - path: "apps/achilles-terminal/src/typed-input.ts"
      provides: "createTypedInputFallback(circuitBreaker, onTyped) — attaches the @clack/prompts.text() prompt when circuit opens"
      exports: ["createTypedInputFallback", "TypedInputHandle"]
      min_lines: 50
    - path: "apps/achilles-terminal/src/config-menu.ts"
      provides: "runConfigMenu() interactive settings editor"
      exports: ["runConfigMenu", "ConfigField"]
      min_lines: 80
    - path: "apps/achilles-terminal/src/latency-report.ts"
      provides: "runLatencyReport() — thin wrapper around Phase 17's renderLatencyReport for CLI subcommand"
      exports: ["runLatencyReport"]
      min_lines: 25
  key_links:
    - from: "wizard.ts"
      to: "Plan 01 (api-key.ts) + Plan 02 (preflight, ambient-calibration, parent-terminal, marker)"
      via: "Sequential @clack/prompts steps composing the Plan 01/02 modules"
      pattern: "resolveApiKey|writeApiKey|checkPreflight|calibrateAmbient|writeInitMarker"
    - from: "smoke-test.ts"
      to: "Phase 17 session.ts createSession"
      via: "Direct import; one-utterance round trip"
      pattern: "createSession|runVoice"
    - from: "typed-input.ts"
      to: "Phase 17 circuit-breaker.ts (the STT breaker's status() reports 'open')"
      via: "Polling or event subscription on circuit-breaker"
      pattern: "circuitBreaker|circuit-breaker|status\\(\\)"
    - from: "transcripts/store.ts"
      to: "structured-logger.ts redaction patterns (Plan 02's 7-regex set)"
      via: "Apply DEFAULT_REDACT_PATTERNS to every line before write"
      pattern: "DEFAULT_REDACT_PATTERNS|applyRedactions"
---

<objective>
Compose Plan 01 + Plan 02 modules into the four user-facing surfaces: the init wizard (INIT-01, INIT-04, INIT-05), opt-in transcripts (SAFE-02), typed-input fallback (ERR-04), config menu, and the latency-report CLI wrapper (ERR-07). The wizard is the cold-start experience; the rest are the runtime surfaces.

Purpose: All eight modules here are user-visible. The wizard MUST handle idempotency, summary diff, "keep current" defaults, and the smoke-test path that exercises the real Phase 17 voice loop end-to-end (only when the operator triggers init — NEVER from vitest, per CLAUDE.md). The other modules are smaller but each addresses one explicit requirement: SAFE-02 (transcripts), ERR-04 (typed-input), and ERR-07 (latency-report + the --debug flag wiring deferred to Plan 04's cli.ts extension).

Output: Eight new source modules + eight test files; all consume Plan 01/02's interfaces; zero modifications to Phase 17's session.ts, latency-probe.ts, circuit-breaker.ts, or structured-logger.ts; zero modifications to packages/voice-* (LOOP-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-CONTEXT.md
@.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-01-PLAN.md
@.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-02-PLAN.md
@apps/achilles-terminal/src/session.ts
@apps/achilles-terminal/src/circuit-breaker.ts
@apps/achilles-terminal/src/latency-probe.ts
@apps/achilles-terminal/src/structured-logger.ts

<interfaces>
<!-- The eight modules' public surface — Plan 04 (cli.ts) dynamic-imports these. -->

From src/init/wizard.ts (NEW):
```
export type WizardStep =
  | "welcome"
  | "api-key"
  | "preflight"
  | "ambient-calibration"
  | "smoke-test"
  | "summary"
  | "marker";

export interface WizardOutcome {
  readonly completed: boolean;
  readonly stepsCompleted: ReadonlyArray<WizardStep>;
  readonly apiKeySource: ApiKeySource | null;
  readonly noiseFloor: number | null;
  readonly smokeTestPassed: boolean;
  readonly cancelled: boolean;
}

export interface WizardDeps {
  // All Plan 01/02 module functions exposed as injection seams for tests
  resolveApiKeyImpl?: typeof resolveApiKey;
  writeApiKeyImpl?: typeof writeApiKey;
  checkPreflightImpl?: typeof checkPreflight;
  calibrateAmbientImpl?: typeof calibrateAmbient;
  writeNoiseFloorImpl?: typeof writeNoiseFloorToSettings;
  resolveParentEmulatorImpl?: typeof resolveParentEmulator;
  writeInitMarkerImpl?: typeof writeInitMarker;
  readInitMarkerImpl?: typeof readInitMarker;
  runSmokeTestImpl?: typeof runSmokeTest;
  // @clack/prompts seams for hermetic tests — see Task 1
  promptText?: (msg: string, opts?: unknown) => Promise<string | symbol>;
  promptSelect?: (msg: string, opts: unknown) => Promise<string | symbol>;
  promptConfirm?: (msg: string, opts?: unknown) => Promise<boolean | symbol>;
  noteImpl?: (msg: string, title?: string) => void;
  spinnerImpl?: () => { start: (msg?: string) => void; stop: (msg?: string) => void };
}

export function runInitWizard(deps?: WizardDeps): Promise<WizardOutcome>;
```

From src/init/smoke-test.ts (NEW):
```
export interface SmokeTestResult {
  readonly passed: boolean;
  readonly elapsedMs: number;
  readonly failureReason?: string;
}

export function runSmokeTest(deps?: SmokeTestDeps): Promise<SmokeTestResult>;
```

From src/transcripts/store.ts (NEW):
```
export interface TranscriptEntry {
  readonly t: number;
  readonly type: "user" | "assistant" | "system";
  readonly text?: string;
  readonly event?: string;
  readonly session_id?: string;
}

export interface TranscriptStoreHandle {
  append(entry: TranscriptEntry): void;
  dispose(): void;
  readonly filePath: string;
}

export function createTranscriptStore(sessionId: string, deps?: TranscriptStoreDeps): TranscriptStoreHandle;
```

From src/transcripts/retention.ts (NEW):
```
export const DEFAULT_RETENTION_DAYS = 30;
export function cleanupOldTranscripts(days?: number, deps?: RetentionDeps): Promise<{ deletedCount: number; keptCount: number }>;
```

From src/transcripts/cli.ts (NEW):
```
export function transcriptsList(deps?: TranscriptsCliDeps): Promise<void>;
export function transcriptsPurge(deps?: TranscriptsCliDeps): Promise<void>;
```

From src/typed-input.ts (NEW):
```
export interface TypedInputHandle {
  dispose(): void;
}

export function createTypedInputFallback(
  circuitBreaker: CircuitBreaker,
  onTyped: (transcript: string) => Promise<void>,
  deps?: TypedInputDeps
): TypedInputHandle;
```

From src/config-menu.ts (NEW):
```
export interface ConfigField {
  readonly key: string;          // "vad.voiceThresholdRatio"
  readonly currentValue: unknown;
  readonly label: string;        // human-readable label for @clack/prompts.select
  readonly validator?: (v: unknown) => string | null;
}

export function runConfigMenu(deps?: ConfigMenuDeps): Promise<void>;
```

From src/latency-report.ts (NEW):
```
export function runLatencyReport(deps?: LatencyReportDeps): Promise<string>;
```

From Phase 17 src/circuit-breaker.ts (EXISTS — Plan 03 imports CircuitBreaker; Plan 03 does NOT modify):
```
export interface CircuitBreaker {
  attempt<T>(fn: () => Promise<T> | T): Promise<AttemptOutcome<T>>;
  status(): CircuitStatus;
}
```

From Phase 17 src/latency-probe.ts (EXISTS — Plan 03 uses renderLatencyReport unchanged):
```
export async function renderLatencyReport(dir?: string): Promise<string>;
export const LATENCY_DIR: string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wizard.ts + smoke-test.ts (the linear @clack/prompts flow with idempotency + summary diff)</name>
  <files>
    apps/achilles-terminal/src/init/wizard.ts
    apps/achilles-terminal/src/init/smoke-test.ts
    apps/achilles-terminal/tests/init/wizard.test.ts
    apps/achilles-terminal/tests/init/smoke-test.test.ts
  </files>
  <read_first>
    - .planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-CONTEXT.md (Wizard step ordering; idempotency: "keep current" defaults; summary diff before write)
    - apps/achilles-terminal/src/session.ts lines 1-110 (runVoice entry point — smoke-test invokes a 1-utterance variant)
    - apps/achilles-terminal/src/init/api-key.ts (Plan 01 — the wizard's first step)
    - apps/achilles-terminal/src/init/preflight.ts (Plan 02 — the wizard's second step)
    - apps/achilles-terminal/src/init/ambient-calibration.ts (Plan 02 — the wizard's third step)
    - apps/achilles-terminal/src/init/parent-terminal.ts (Plan 02 — used when preflight.sox.status === "device-failed" with EPERM)
    - apps/achilles-terminal/src/init/marker.ts (Plan 02 — the wizard writes init.json at the end)
  </read_first>
  <behavior>
    - runInitWizard() executes steps in order: welcome -> api-key -> preflight -> ambient-calibration -> smoke-test -> summary -> marker
    - At each step the prompt's default value is derived from the existing settings/init.json (idempotency for INIT-05); if marker is absent, all defaults are the Plan 01/02 module defaults (e.g. DEFAULT_VAD_CONFIG.initialNoiseFloor)
    - api-key step: calls resolveApiKey first; if source === "env" -> prompt "ELEVENLABS_API_KEY is set in env (xxxx...xxxx); keep using env? (Y/n)"; if source === "missing" -> prompt for the key text + ask where to store (keychain default; encrypted-file fallback)
    - preflight step: calls checkPreflight; if any binary missing/device-failed -> calls suggestInstallCommand + prints the line + offers invokePackageManager (if canAutoInvoke); on sox device-failed AND macOS -> calls resolveParentEmulator + getRemediationHint and PRINTS the remediation hint via @clack/prompts.note
    - ambient-calibration step: shows a 5-second spinner; calls calibrateAmbient with onProgress wired to spinner.message; on completion shows the computed noiseFloor + the prior value (for the diff)
    - smoke-test step: calls runSmokeTest; on pass -> "Smoke test passed: heard ack + spoken-summary within 30s"; on fail -> "Smoke test failed: <reason>; you can still proceed but the loop may not work end-to-end"
    - summary step: prints a diff table of every changed setting (label: old -> new) via @clack/prompts.note; calls @clack/prompts.confirm "Save these changes? (Y/n)"
    - marker step: writeInitMarker({ initializedAt: iso, version, apiKeySource }) + writeNoiseFloorToSettings(...) only on confirm; on rejection -> outcome.completed = false but cancelled = false (the user opted out, not aborted)
    - At any point Ctrl-C / Esc cancels the wizard cleanly; outcome.cancelled = true; nothing is written
    - The wizard imports @clack/prompts BUT all prompts go through the deps injection seams (promptText, promptSelect, promptConfirm, noteImpl, spinnerImpl) so tests run hermetically without touching stdin/stdout
    - runSmokeTest spawns a TEMPORARY Phase 17 Session via createSession from session.ts, awaits ONE round-trip (speech_start -> claude_done -> tts_playback_complete -> drained), then disposes the session
    - runSmokeTest accepts a `sessionFactoryImpl` seam so tests pass a fake that emits the round-trip events synthetically
  </behavior>
  <action>
    Create `apps/achilles-terminal/src/init/wizard.ts`:

    1. Top docblock — Phase 18 Plan 03 Task 1. Cite INIT-01 (linear @clack/prompts flow), INIT-04 (smoke test exercises Phase 17 loop), INIT-05 (idempotent + keep-current defaults + summary diff). Reference CLAUDE.md no-auto-running rule: the smoke-test step is invoked ONLY here, ONLY when the operator runs `achilles init` interactively — NEVER from vitest. Tests use the sessionFactoryImpl injection seam to mock the round-trip.

    2. Imports (all `.js` specifiers, all from Plan 01/02 modules):
       - api-key.ts: resolveApiKey, writeApiKey, ApiKeySource
       - preflight.ts: checkPreflight, PreflightResult
       - install-suggestions.ts: suggestInstallCommand, invokePackageManager
       - ambient-calibration.ts: calibrateAmbient, writeNoiseFloorToSettings, DEFAULT_CALIBRATION_DURATION_MS
       - parent-terminal.ts: resolveParentEmulator, getRemediationHint
       - marker.ts: writeInitMarker, readInitMarker, hasInitMarker
       - smoke-test.ts: runSmokeTest
       - Type-only import: `import type { text, select, confirm, note, spinner } from "@clack/prompts";` — used only in default-impl fallback that lazily imports the real @clack/prompts inside the function (so tests can run without touching the lib)

    3. Default @clack/prompts impl loader (lazy): the deps interface lets tests pass synthetic prompt impls; production wires real ones by `await import("@clack/prompts")` INSIDE runInitWizard so the import does not trigger on test-side wizard.ts file load.

    4. Export `runInitWizard(deps?)`:
       - Resolve all deps to either supplied seams or production defaults
       - stepsCompleted = []
       - Wrap the whole flow in try/catch; on cancel symbol (clack's isCancel) -> outcome.cancelled = true, return early
       - Step welcome: noteImpl("Welcome to Achilles. This wizard configures your microphone, API key, and the voice loop. Press Ctrl-C at any time to cancel."); stepsCompleted.push("welcome")
       - Step api-key: resolveApiKey first; branch on source; promptText / promptSelect for storage target; writeApiKey if user supplied a new value; stepsCompleted.push("api-key")
       - Step preflight: checkPreflight; if !allOk -> for each missing/failed: suggestInstallCommand + noteImpl the line + promptConfirm "Run this now? (y/N)" + if confirmed AND canAutoInvoke -> invokePackageManager. If sox.status === "device-failed" AND process.platform === "darwin" -> resolveParentEmulator + getRemediationHint -> noteImpl. stepsCompleted.push("preflight")
       - Step ambient-calibration: spinnerImpl().start("Calibrating microphone (5s)..."); call calibrateAmbient with onProgress wired to spinner; on completion spinner.stop with `noiseFloor=${noiseFloor.toFixed(4)}`; stepsCompleted.push("ambient-calibration")
       - Step smoke-test: promptConfirm "Run a 1-utterance smoke test now? (Y/n)"; if confirmed -> runSmokeTest; on pass noteImpl "Smoke test passed"; on fail noteImpl with the reason; stepsCompleted.push("smoke-test")
       - Step summary: build diff array from { apiKeySource, noiseFloor, apiKey written? }; noteImpl with the diff table; promptConfirm "Save these changes? (Y/n)"; on reject -> outcome.completed = false; return
       - Step marker: writeInitMarker + writeNoiseFloorToSettings (if changed); stepsCompleted.push("marker")
       - Return { completed: true, stepsCompleted, apiKeySource, noiseFloor, smokeTestPassed, cancelled: false }

    Create `apps/achilles-terminal/src/init/smoke-test.ts`:

    1. Top docblock — Phase 18 Plan 03 Task 1. Cite INIT-04 ("1-utterance smoke test exercising the full mic -> STT -> claude -> TTS -> ffplay path"). State CLAUDE.md no-auto-running rule: this function is invoked ONLY from the wizard, NEVER from vitest. Tests use sessionFactoryImpl to mock the round-trip events.

    2. Imports: `import { createSession, type Session } from "../session.js";` — the Phase 17 composition root. Type-only for the test seam.

    3. Export interface `SmokeTestDeps`:
       - `sessionFactoryImpl?: () => Session` (defaults to () => createSession())
       - `timeoutMs?: number` (defaults to 30000 — INIT-04 spec)
       - `setTimeoutImpl?, clearTimeoutImpl?` (clock seams)

    4. Export `async function runSmokeTest(deps?): Promise<SmokeTestResult>`:
       - session = sessionFactoryImpl()
       - Promise.race([round-trip listener, timeout]):
         - Round-trip listener: subscribe to session "event" stream; resolve when both "claude_done" with success AND "tts_drained" have fired (the half-duplex tail completes the loop)
         - Timeout: setTimeoutImpl resolves with { passed: false, failureReason: "timeout_30s" }
       - finally: session.dispose() — even on timeout, do not leak the session
       - Return { passed, elapsedMs, failureReason? }

    Tests `apps/achilles-terminal/tests/init/wizard.test.ts` (use ALL injection seams; never touch real prompts):

    - "runInitWizard executes 7 steps in order when all inputs succeed"
    - "runInitWizard.outcome.cancelled === true when the api-key promptText returns the clack cancel symbol"
    - "runInitWizard.outcome.completed === false (not cancelled) when the summary confirm returns false"
    - "runInitWizard does NOT writeInitMarker when summary confirm returns false"
    - "runInitWizard SKIPS the api-key write step when resolveApiKey returns source='env' AND user confirms 'keep'"
    - "runInitWizard calls suggestInstallCommand + invokePackageManager when preflight.sox.status === 'missing' AND user confirms install"
    - "runInitWizard calls resolveParentEmulator AND getRemediationHint when preflight.sox.status === 'device-failed' AND platform === 'darwin'"
    - "runInitWizard does NOT call resolveParentEmulator on linux even when sox.status === 'device-failed'"
    - "runInitWizard's summary diff includes 'noiseFloor: 0.005 -> 0.012' when calibrateAmbient returns 0.012"
    - "runInitWizard's idempotency: when readInitMarker returns a prior marker, the api-key default is the marker's apiKeySource"
    - "runInitWizard writeInitMarker is called with the resolved apiKeySource + a string version + an ISO timestamp"

    Tests `apps/achilles-terminal/tests/init/smoke-test.test.ts`:

    - "runSmokeTest passes when the injected session emits claude_done(success) AND tts_drained"
    - "runSmokeTest fails with failureReason='timeout_30s' when no events fire within 30 seconds"
    - "runSmokeTest disposes the session on pass"
    - "runSmokeTest disposes the session on timeout"
    - "runSmokeTest accepts a custom timeoutMs"

    Use vitest fake timers + a synthetic session event-emitter for all tests. Zero real session spawning.
  </action>
  <verify>
    <automated>npx vitest run apps/achilles-terminal/tests/init/wizard.test.ts apps/achilles-terminal/tests/init/smoke-test.test.ts --pool=forks</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/achilles-terminal/src/init/wizard.ts` (min 150 LOC, 7 stepsCompleted strings present in source)
    - File `apps/achilles-terminal/src/init/smoke-test.ts` (min 60 LOC)
    - `grep -E "createSession|Session " apps/achilles-terminal/src/init/smoke-test.ts | wc -l` >= 1 (uses Phase 17 session)
    - `grep -E "stepsCompleted|WizardStep" apps/achilles-terminal/src/init/wizard.ts | wc -l` >= 7
    - `grep -E "resolveApiKey|checkPreflight|calibrateAmbient|writeInitMarker|runSmokeTest|resolveParentEmulator" apps/achilles-terminal/src/init/wizard.ts | wc -l` >= 6 (composes all six Plan 01/02 modules)
    - `grep -E "promptText|promptSelect|promptConfirm" apps/achilles-terminal/src/init/wizard.ts | wc -l` >= 3 (seam-driven prompts)
    - Tests pass: 16 cases total (11 wizard + 5 smoke-test) green
    - Lint clean
  </acceptance_criteria>
  <done>Wizard composes Plan 01/02 modules into 7 linear steps with idempotent defaults, summary diff, smoke test (operator-only, never from vitest), and graceful cancel; 16 passing tests use only deps-injection seams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: transcripts (store + retention + CLI handlers) — opt-in SAFE-02</name>
  <files>
    apps/achilles-terminal/src/transcripts/store.ts
    apps/achilles-terminal/src/transcripts/retention.ts
    apps/achilles-terminal/src/transcripts/cli.ts
    apps/achilles-terminal/tests/transcripts/store.test.ts
    apps/achilles-terminal/tests/transcripts/retention.test.ts
    apps/achilles-terminal/tests/transcripts/cli.test.ts
  </files>
  <read_first>
    - .planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-CONTEXT.md (Transcripts block: JSONL, 30-day retention, list/purge subcommands, system-line types for session boundaries)
    - apps/achilles-terminal/src/structured-logger.ts (DEFAULT_REDACT_PATTERNS — Plan 03 imports the 7-regex set updated by Plan 02 and applies before write)
    - apps/achilles-terminal/src/resume-session.ts (ACHILLES_HOME constant for the transcripts dir derivation)
  </read_first>
  <behavior>
    - createTranscriptStore("sid-abc") writes to ~/.achilles/transcripts/sid-abc.jsonl at 0o600 with 0o700 parent dir
    - Each append() applies the structured-logger DEFAULT_REDACT_PATTERNS to the text + event fields BEFORE writing
    - JSONL format: one JSON object per line, ending with \n
    - The store accepts system entries: { t, type: "system", event: "session_start" | "session_end", session_id }
    - dispose() writes a final session_end system entry then closes the handle
    - cleanupOldTranscripts(30) reads ~/.achilles/transcripts/, statSyncs each file's mtime, deletes those older than (Date.now() - 30 * 24 * 3600 * 1000)
    - cleanupOldTranscripts returns { deletedCount, keptCount }
    - transcriptsList() reads every JSONL in the dir; for each: print filename + the first entry with type === "user" (text truncated to 80 chars); print "No transcripts on disk." if empty
    - transcriptsPurge() @clack/prompts.select with 4 options: "Delete all", "Delete older than 7 days", "Delete older than 30 days", "Cancel"; on Delete all -> rm all jsonl files; on N-days -> cleanupOldTranscripts(N); on Cancel -> noop
    - All three modules accept fs + clock seams for hermetic tests
  </behavior>
  <action>
    Create `apps/achilles-terminal/src/transcripts/store.ts`:

    1. Top docblock — Phase 18 Plan 03 Task 2. Cite SAFE-02 ("transcripts off by default; --save-transcripts opt-in writes JSONL with secret redaction"). State that Plan 04's cli.ts is the place where --save-transcripts flag wires this to session.ts as a subscriber.

    2. Imports: `appendFileSync, mkdirSync, chmodSync from "node:fs"`; `homedir from "node:os"`; `join from "node:path"`. Plus the redaction patterns from `../structured-logger.js` (import them — but the existing export shape is INTERNAL; OPTION: re-export DEFAULT_REDACT_PATTERNS from structured-logger.ts as a public export OR duplicate the array literal here. Decision: re-use the patterns by EXPORTING DEFAULT_REDACT_PATTERNS from structured-logger.ts. This is a one-line `export` keyword addition to structured-logger.ts, NOT a logic change — Plan 02 already touched the file's regex list; this task adds the export keyword to the existing const).

    3. Export interface TranscriptEntry as shown in <interfaces>.

    4. Internal helper `applyRedactionsToLine(line: string, patterns: ReadonlyArray<RegExp>): string` — mirrors the structured-logger.ts function (could be hoisted as a shared util; for now copy-implement to avoid coupling).

    5. Export `createTranscriptStore(sessionId, deps?)`:
       - Resolve path = join(homedir(), ".achilles", "transcripts", `${sessionId}.jsonl`)
       - mkdirSync parent { recursive: true, mode: 0o700 }
       - chmodSync file at 0o600 after first append
       - Return handle with append(entry) -> redact + serialize + appendFileSync; dispose() -> append session_end entry + mark disposed (subsequent appends no-op)

    Create `apps/achilles-terminal/src/transcripts/retention.ts`:

    1. Top docblock — Phase 18 Plan 03 Task 2. Cite SAFE-02 ("30-day retention default").

    2. Export `DEFAULT_RETENTION_DAYS = 30`.

    3. Export `cleanupOldTranscripts(days = 30, deps?)`:
       - dir = ~/.achilles/transcripts
       - readdir; for each .jsonl: statSync; if mtimeMs < cutoff -> unlinkSync + deletedCount++; else keptCount++
       - Return { deletedCount, keptCount }

    Create `apps/achilles-terminal/src/transcripts/cli.ts`:

    1. Top docblock — Phase 18 Plan 03 Task 2. Cite SAFE-02 ("achilles transcripts list / purge subcommands"). State this module is the body that Plan 04's `await import("./transcripts/cli.js")` invokes.

    2. Imports: node:fs (readdir + readFile + unlink + statSync), node:os homedir, node:path join. Lazy-import @clack/prompts inside transcriptsPurge for the select() call.

    3. Export `transcriptsList(deps?)`:
       - dir = ~/.achilles/transcripts
       - If !existsSync -> process.stdout.write("No transcripts on disk.\n")
       - Else readdirSync; for each .jsonl:
         - readFileSync; split on \n; find first JSON line with type === "user"; print `${filename}: ${text.slice(0, 80)}`
         - If no user line, print `${filename}: (no user transcripts)`

    4. Export `transcriptsPurge(deps?)`:
       - clack = await import("@clack/prompts")
       - selection = await clack.select({ message: "Purge transcripts?", options: [{ value: "all", label: "Delete all" }, { value: "30d", label: "Delete older than 30 days" }, { value: "7d", label: "Delete older than 7 days" }, { value: "cancel", label: "Cancel" }] })
       - On "all" -> readdir + unlink every .jsonl; print deletedCount
       - On "30d" -> cleanupOldTranscripts(30); print result
       - On "7d" -> cleanupOldTranscripts(7); print result
       - On "cancel" or isCancel(selection) -> print "Cancelled."

    Tests `apps/achilles-terminal/tests/transcripts/store.test.ts`:

    - "createTranscriptStore writes JSONL with one object per line"
    - "createTranscriptStore.append applies the xi_-prefix redaction (Plan 02 7th regex) to text"
    - "createTranscriptStore.append applies the existing sk- redaction"
    - "createTranscriptStore enforces 0o600 perms via chmodSync"
    - "createTranscriptStore creates ~/.achilles/transcripts/ with 0o700 perms"
    - "createTranscriptStore.dispose appends a session_end system entry"
    - "createTranscriptStore.append after dispose is a no-op (does not throw)"
    - "createTranscriptStore.filePath includes the supplied sessionId"

    Tests `apps/achilles-terminal/tests/transcripts/retention.test.ts`:

    - "cleanupOldTranscripts(30) deletes files with mtime older than 30 days"
    - "cleanupOldTranscripts(30) keeps files newer than 30 days"
    - "cleanupOldTranscripts returns { deletedCount, keptCount } accurate counts"
    - "cleanupOldTranscripts when dir does not exist returns { deletedCount: 0, keptCount: 0 } without throwing"
    - "cleanupOldTranscripts with custom days threshold (7) uses the custom value"

    Tests `apps/achilles-terminal/tests/transcripts/cli.test.ts`:

    - "transcriptsList prints 'No transcripts on disk.' when dir absent"
    - "transcriptsList prints filename + first-user-line preview per file"
    - "transcriptsList truncates user text to 80 chars"
    - "transcriptsPurge with select returning 'all' deletes every .jsonl"
    - "transcriptsPurge with select returning '30d' calls cleanupOldTranscripts(30)"
    - "transcriptsPurge with select returning 'cancel' is a no-op"
    - "transcriptsPurge with isCancel(selection) is a no-op"

    All tests use a tmpdir + homedirImpl injection.

    ALSO: Modify `apps/achilles-terminal/src/structured-logger.ts` ONE line — add the `export` keyword to the `DEFAULT_REDACT_PATTERNS` const (line 167 currently `const DEFAULT_REDACT_PATTERNS` -> `export const DEFAULT_REDACT_PATTERNS`). This is the ONLY change to structured-logger.ts in Plan 03; do NOT modify the array entries — Plan 02 already added the 7th regex.
  </action>
  <verify>
    <automated>npx vitest run apps/achilles-terminal/tests/transcripts/ apps/achilles-terminal/tests/structured-logger.test.ts --pool=forks</automated>
  </verify>
  <acceptance_criteria>
    - Three source files exist with min LOC budgets
    - `grep -c "appendFileSync\|0o600\|0o700" apps/achilles-terminal/src/transcripts/store.ts` >= 3
    - `grep -E "DEFAULT_REDACT_PATTERNS|applyRedactions" apps/achilles-terminal/src/transcripts/store.ts | wc -l` >= 1
    - `grep -E "^export const DEFAULT_REDACT_PATTERNS" apps/achilles-terminal/src/structured-logger.ts | wc -l` == 1 (the one-keyword change)
    - `grep -c "DEFAULT_REDACT_PATTERNS" apps/achilles-terminal/src/structured-logger.ts` >= 2 (export + usage)
    - The 7 regex entries still byte-for-byte intact (Plan 02's work preserved): `grep -E "/xi_\\[a-zA-Z0-9\\]\\{40,\\}/g" apps/achilles-terminal/src/structured-logger.ts | wc -l` == 1
    - Tests pass: 8 + 5 + 7 + structured-logger existing = 20 transcripts cases + structured-logger suite still green
    - Lint clean
  </acceptance_criteria>
  <done>Transcripts store with redaction + 30-day retention + list/purge CLI shipped; 20 passing tests; DEFAULT_REDACT_PATTERNS now exported (one-keyword change); existing structured-logger tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: typed-input.ts + config-menu.ts + latency-report.ts (runtime user surfaces)</name>
  <files>
    apps/achilles-terminal/src/typed-input.ts
    apps/achilles-terminal/src/config-menu.ts
    apps/achilles-terminal/src/latency-report.ts
    apps/achilles-terminal/tests/typed-input.test.ts
    apps/achilles-terminal/tests/config-menu.test.ts
    apps/achilles-terminal/tests/latency-report.test.ts
  </files>
  <read_first>
    - apps/achilles-terminal/src/circuit-breaker.ts (CircuitBreaker.status() returns { state: "open" | "half-open" | "closed", ... }; Plan 03 attaches a status poller because the v1.2 incident-detection.ts does NOT emit events — pure-functional read model)
    - apps/achilles-terminal/src/latency-probe.ts (renderLatencyReport import — Plan 03 latency-report.ts is the CLI-side wrapper)
    - apps/achilles-terminal/src/audio/vad-energy.ts (DEFAULT_VAD_CONFIG — the 4 config fields the menu exposes: voiceThresholdRatio, voiceHoldMs, silenceHoldMs, minUtteranceMs)
    - apps/achilles-terminal/src/store-stub.ts (Phase 16 settings loader stub — the config-menu effectively replaces this stub's loader role; Plan 04 wires the real loader in)
    - .planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-CONTEXT.md (Typed-input fallback block; Config menu block; Latency report block; --debug flag block)
  </read_first>
  <behavior>
    - createTypedInputFallback watches the supplied circuitBreaker.status(); when state === "open", presents an @clack/prompts.text() prompt; on user input -> calls onTyped(transcript); after onTyped resolves, polls the breaker again — if state has returned to "closed" the fallback dismisses, otherwise re-prompts
    - The handle exposes dispose() to stop polling
    - runConfigMenu reads settings.json (or returns defaults if absent); shows @clack/prompts.select listing each configurable field with its current value; on selection prompts for new value via @clack/prompts.text with validator; on save -> merge + writeFileSync at 0o600; on cancel -> no-op
    - Configurable fields (per CAP-04 + the v1.4 deferrals): vad.voiceThresholdRatio (number, range 1.5-5), vad.voiceHoldMs (number, range 20-200), vad.silenceHoldMs (number, range 100-1000), vad.minUtteranceMs (number, range 100-1000), save_transcripts (boolean, default false), debug_mode (boolean, default false). Language picker is OUT — deferred to v1.4 per CONTEXT.md deferred block
    - runLatencyReport delegates to Phase 17's renderLatencyReport (LATENCY_DIR default) and returns the formatted string; Plan 04's cli.ts writes it to stdout
  </behavior>
  <action>
    Create `apps/achilles-terminal/src/typed-input.ts`:

    1. Top docblock — Phase 18 Plan 03 Task 3. Cite ERR-04 ("typed-input fallback via inline @clack/prompts.text() activates when STT circuit breaker opens; typed transcript flows through the same sandwich-wrap single-pipeline entry as voice transcripts").

    2. Imports: `import type { CircuitBreaker, CircuitStatus } from "./circuit-breaker.js";`

    3. Export interface `TypedInputHandle { dispose(): void; }`

    4. Export interface `TypedInputDeps`:
       - `pollIntervalMs?: number` (default 1000)
       - `promptText?: (msg: string) => Promise<string | symbol>` (lazy default to @clack/prompts.text)
       - `isCancel?: (v: unknown) => boolean` (lazy default to @clack/prompts.isCancel)
       - `setIntervalImpl?, clearIntervalImpl?`

    5. Export `createTypedInputFallback(circuitBreaker, onTyped, deps?)`:
       - Set up a setIntervalImpl that polls breaker.status() every pollIntervalMs
       - When status.state === "open" AND no active prompt -> open promptText("STT unavailable — type your message:"); on result -> if !isCancel -> await onTyped(result)
       - After onTyped: do not auto-dismiss the fallback — let the next poll observe the breaker state. If state has returned to "closed", the next iteration is a no-op.
       - Return { dispose: () => clearIntervalImpl + cancel any in-flight prompt }

    Create `apps/achilles-terminal/src/config-menu.ts`:

    1. Top docblock — Phase 18 Plan 03 Task 3. Cite CONTEXT.md Config menu decision block. Reference CAP-04 (the 4 VAD knobs become user-configurable here).

    2. Imports: node:fs (existsSync, readFileSync, writeFileSync, chmodSync, mkdirSync); node:os homedir; node:path join.

    3. Internal helper `loadSettings(homedirImpl)` — reads ~/.achilles/settings.json + returns parsed or {} on absent/invalid.

    4. Define CONFIGURABLE_FIELDS as a frozen array of ConfigField objects:
       - { key: "vad.voiceThresholdRatio", label: "VAD voice threshold ratio (default 3, range 1.5-5)", validator: (v) => { n = Number(v); if (!Number.isFinite(n) || n < 1.5 || n > 5) return "Must be a number in [1.5, 5]"; return null; } }
       - Similar entries for voiceHoldMs (20-200), silenceHoldMs (100-1000), minUtteranceMs (100-1000)
       - { key: "save_transcripts", label: "Save transcripts to disk (default off)", validator: boolean coercion }
       - { key: "debug_mode", label: "Verbose debug logging (default off)", validator: boolean coercion }

    5. Export `runConfigMenu(deps?)`:
       - Load current settings
       - Build select() options from CONFIGURABLE_FIELDS with current values shown
       - On select -> promptText with the field's validator; on submit -> merge into settings + writeFileSync at 0o600
       - Loop until user selects "Save and exit" or "Cancel"

    Create `apps/achilles-terminal/src/latency-report.ts`:

    1. Top docblock — Phase 18 Plan 03 Task 3. Cite ERR-07 ("achilles latency --report prints rolling-window P50/P95 ..."). Note that Phase 17's latency-probe.ts already exports `renderLatencyReport()`; this module is the thin CLI-side wrapper that Plan 04 dynamic-imports.

    2. Imports: `import { renderLatencyReport, LATENCY_DIR } from "./latency-probe.js";`

    3. Export `runLatencyReport(deps?)`:
       - dir = deps?.dirOverride ?? LATENCY_DIR
       - return renderLatencyReport(dir)

    (NOTE: this module is intentionally tiny — Phase 17 already shipped the implementation. We keep this wrapper as the Plan 04 dynamic-import target so the cli.ts subcommand branch doesn't need to know about Phase 17 internal paths; Plan 04 calls `runLatencyReport()` and writes the result to stdout.)

    Tests `apps/achilles-terminal/tests/typed-input.test.ts`:

    - "createTypedInputFallback does NOT prompt while breaker.status().state === 'closed'"
    - "createTypedInputFallback prompts via promptText when status transitions to 'open'"
    - "createTypedInputFallback calls onTyped with the user-typed string"
    - "createTypedInputFallback does NOT call onTyped when promptText returns isCancel symbol"
    - "createTypedInputFallback.dispose() stops the poller"
    - "createTypedInputFallback re-prompts on the next interval if breaker is still 'open' after onTyped resolves"

    Tests `apps/achilles-terminal/tests/config-menu.test.ts`:

    - "CONFIGURABLE_FIELDS contains all 4 VAD knobs + save_transcripts + debug_mode"
    - "CONFIGURABLE_FIELDS validator for voiceThresholdRatio rejects 6 (out of range)"
    - "CONFIGURABLE_FIELDS validator for voiceThresholdRatio accepts 2.5"
    - "runConfigMenu writes the supplied value to settings.json at 0o600"
    - "runConfigMenu PRESERVES other unrelated keys in settings.json"
    - "runConfigMenu with select returning 'cancel' does NOT write"
    - "runConfigMenu loads current settings as defaults"

    Tests `apps/achilles-terminal/tests/latency-report.test.ts`:

    - "runLatencyReport returns 'samples=0' formatted string when LATENCY_DIR is empty"
    - "runLatencyReport returns a string containing 'P50' and 'P95' when sample files exist"
    - "runLatencyReport accepts a dirOverride and reads from there instead of LATENCY_DIR"

    Use tmpdir + injected dirOverride for the latency tests so they reuse Phase 17's renderLatencyReport implementation without touching the real ~/.achilles/latency/.
  </action>
  <verify>
    <automated>npx vitest run apps/achilles-terminal/tests/typed-input.test.ts apps/achilles-terminal/tests/config-menu.test.ts apps/achilles-terminal/tests/latency-report.test.ts --pool=forks</automated>
  </verify>
  <acceptance_criteria>
    - Three source files exist with min LOC budgets
    - `grep -E "circuitBreaker\\.status|status\\(\\)" apps/achilles-terminal/src/typed-input.ts | wc -l` >= 1
    - `grep -E "CONFIGURABLE_FIELDS|voiceThresholdRatio|voiceHoldMs|silenceHoldMs|minUtteranceMs" apps/achilles-terminal/src/config-menu.ts | wc -l` >= 5 (all 4 VAD knobs + the table)
    - `grep -E "renderLatencyReport" apps/achilles-terminal/src/latency-report.ts | wc -l` >= 1 (delegates to Phase 17)
    - Tests pass: 6 + 7 + 3 = 16 cases green
    - Lint clean
  </acceptance_criteria>
  <done>Typed-input fallback wired to circuit-breaker.status() polling; config-menu exposes 4 VAD knobs + 2 booleans with validators; latency-report delegates to Phase 17's renderLatencyReport; 16 passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-typed text -> structured-logger redaction -> JSONL file | If a user accidentally types their API key into the typed-input fallback, the transcripts store applies DEFAULT_REDACT_PATTERNS (now 7 regexes) before write. Defence in depth. |
| @clack/prompts user input -> writeApiKey / writeNoiseFloorToSettings | The wizard accepts user input and routes it to Plan 01/02 writers. The writers themselves enforce 0o600 perms on output. |
| settings.json read by Phase 16 vad-energy loader (via Plan 04's runtime loader rewrite) | A maliciously-crafted settings.json could ship with very-low voiceThresholdRatio (e.g. 0.1) to silently broaden the VAD's mic gate. Mitigated by the config-menu validators when the user EDITS via runConfigMenu — but raw file edits bypass. Accept (single-user dev tool, user is the security boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-14 | Information Disclosure | API key typed into typed-input fallback ends up in transcripts JSONL | mitigate | createTranscriptStore.append applies DEFAULT_REDACT_PATTERNS (7 regexes, includes the xi_ and xi- shapes); test fixture proves redaction on the synthetic key |
| T-18-15 | Information Disclosure | API key bytes in wizard summary diff sent to @clack/prompts.note (renders to stdout/TTY) | mitigate | The wizard summary diff displays the apiKeySource enum + a length annotation; never the key bytes (test assertion: the note fixture text has no substring match against the test key) |
| T-18-16 | Tampering | An attacker swaps the user's @clack/prompts module via PATH manipulation | accept | @clack/prompts is exact-pinned in dependencies; npm install resolves to the verified copy. PATH attacks require host compromise — out of scope for a single-user tool. |
| T-18-17 | Denial of Service | typed-input poll interval too aggressive (CPU burn) | mitigate | Default pollIntervalMs = 1000 (1s) — minimum perceptible to the user; tests assert the seam is configurable but not below 100ms |
| T-18-18 | Information Disclosure | settings.json world-readable | mitigate | runConfigMenu writes at 0o600 + chmodSync; identical contract to Plan 02's writeNoiseFloorToSettings |
| T-18-19 | Information Disclosure | transcripts/ directory world-readable | mitigate | createTranscriptStore enforces 0o700 on the parent dir + 0o600 on each file |
| T-18-20 | Tampering | Config-menu input bypasses validator (TypeScript-level coerce edge case) | mitigate | Validators are runtime functions invoked before merge+write; tests assert rejection of out-of-range values |
</threat_model>

<verification>
- Eight new source files; eight new test files
- 16 (wizard+smoke) + 20 (transcripts) + 16 (typed-input+config+latency) = 52 new test cases
- structured-logger.ts grows by exactly ONE keyword (`export` on DEFAULT_REDACT_PATTERNS); the 7-regex array is byte-for-byte unchanged from Plan 02
- No file conflict with Plan 04 (cli.ts); Plan 03 does NOT touch cli.ts
- LOOP-02 preserved: zero changes anywhere under `packages/voice-*`, `packages/claude-code-bridge/`, or `packages/achilles-skill/skill/prompts/companion.md`
- INIT-07 preserved: cli.ts not touched
- Phase 17 internals not modified: session.ts, latency-probe.ts, circuit-breaker.ts, graceful-shutdown.ts, resume-session.ts byte-for-byte unchanged
- The smoke-test module respects CLAUDE.md no-auto-running rule: every test injects a synthetic session via sessionFactoryImpl; no test spawns a real Phase 17 session
</verification>

<success_criteria>
Plan 03 succeeds when:
- [x] Wizard composes Plan 01/02 modules into 7 linear steps with idempotency + summary diff + the operator-only smoke test
- [x] Transcripts store applies the 7-regex redaction + 0o600 perms; retention deletes >30-day files; list + purge CLI handlers work via select prompt
- [x] typed-input.ts polls the circuit breaker status() and routes typed strings through onTyped; no real circuit breaker construction in tests
- [x] config-menu reads + writes settings.json with validators for the 4 VAD knobs + 2 booleans
- [x] latency-report.ts is a thin wrapper around Phase 17's renderLatencyReport
- [x] 52 new test cases pass under `npx vitest run apps/achilles-terminal/tests/init/wizard.test.ts apps/achilles-terminal/tests/init/smoke-test.test.ts apps/achilles-terminal/tests/transcripts/ apps/achilles-terminal/tests/typed-input.test.ts apps/achilles-terminal/tests/config-menu.test.ts apps/achilles-terminal/tests/latency-report.test.ts --pool=forks`
- [x] Lint clean for all new files + the one-keyword change to structured-logger.ts
- [x] Phase 17 baseline tests still pass
</success_criteria>

<output>
Create `.planning/phases/18-init-wizard-config-transcripts-single-instance-lock/18-03-SUMMARY.md` when done with:
- File-by-file LOC + test count + key behaviors
- Confirmation the wizard's smoke test fires ONLY from the wizard (never from vitest) — every test uses sessionFactoryImpl
- Confirmation the transcripts redaction covers the xi_ Plan 02 7th regex (fixture proof)
- Confirmation runConfigMenu validators reject out-of-range VAD values
- Confirmation runLatencyReport delegates to Phase 17 unchanged (one-line wrapper)
</output>
