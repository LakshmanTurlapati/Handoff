---
phase: 02-bridge-codex-session-adapter
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - apps/bridge/src/daemon/bridge-daemon.ts
  - apps/bridge/src/daemon/session-manager.ts
  - apps/bridge/src/daemon/message-router.ts
  - apps/bridge/src/cli/daemon.ts
  - apps/bridge/package.json
  - apps/bridge/tests/session-flow.test.ts
  - apps/bridge/tests/event-relay.test.ts
  - apps/relay/tests/ws-bridge.test.ts
autonomous: true
requirements:
  - SESS-02
  - SESS-03
  - SEC-02

must_haves:
  truths:
    - "The bridge exposes a session list to the relay from real local Codex thread data."
    - "Only one remote-controlled session can be attached at a time, and attach/resume behavior preserves the existing conversation history."
    - "Relay commands for prompt, steer, interrupt, detach, and approval response are validated and routed through the bridge rather than bypassing Codex safeguards."
    - "Local crashes, detachs, and ended sessions emit honest `session.ended` or error signals instead of leaving stale live state behind."
  artifacts:
    - path: "apps/bridge/src/daemon/session-manager.ts"
      provides: "single-active-session guard plus session metadata cache"
      exports: ["SessionManager"]
    - path: "apps/bridge/src/daemon/message-router.ts"
      provides: "bridge-relay JSON-RPC method routing for list, attach, history, prompt, steer, approval, and interrupt"
      exports: ["BridgeMessageRouter"]
    - path: "apps/bridge/src/daemon/bridge-daemon.ts"
      provides: "top-level orchestration between relay connection, Codex adapter, and session manager"
      exports: ["BridgeDaemon"]
    - path: "apps/bridge/tests/session-flow.test.ts"
      provides: "integration coverage for session listing and attach/history flow"
      contains: "session.attach"
    - path: "apps/bridge/tests/event-relay.test.ts"
      provides: "integration coverage for normalized event fanout and failure handling"
      contains: "session.ended"
  key_links:
    - from: "apps/bridge/src/daemon/message-router.ts"
      to: "packages/protocol/src/bridge.ts"
      via: "all relay-bridge JSON-RPC methods validate against shared schemas before routing"
      pattern: "SessionAttachParamsSchema"
    - from: "apps/bridge/src/daemon/bridge-daemon.ts"
      to: "apps/bridge/src/daemon/codex-adapter.ts"
      via: "daemon attaches real local Codex sessions and relays normalized events"
      pattern: "CodexAdapter"
    - from: "apps/bridge/src/daemon/bridge-daemon.ts"
      to: "apps/bridge/src/daemon/relay-connection.ts"
      via: "daemon owns the outbound relay channel and reconnect behavior"
      pattern: "RelayConnection"
---

<objective>
Finish Phase 2 by wiring the bridge daemon, session manager, and relay method routing around the real Codex adapter so remote session list and attach/resume behavior actually works end to end.

Purpose: Plan 02-02 establishes the local Codex boundary. This plan completes the product-facing behavior for Phase 2: session discovery, attach/resume, one-active-session enforcement, and local failure handling that preserves Codex sandbox and approval semantics.

Output: Session manager, message router, daemon entry point, and integration tests for list/attach/history/event flows.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bridge-codex-session-adapter/02-CONTEXT.md
@.planning/phases/02-bridge-codex-session-adapter/02-RESEARCH.md
@.planning/phases/02-bridge-codex-session-adapter/02-02-PLAN.md
@packages/protocol/src/bridge.ts
@packages/protocol/src/live-session.ts
@apps/bridge/src/daemon/relay-connection.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the bridge daemon and session manager around one-active-session semantics</name>
  <files>apps/bridge/src/daemon/bridge-daemon.ts, apps/bridge/src/daemon/session-manager.ts, apps/bridge/src/cli/daemon.ts, apps/bridge/package.json</files>
  <read_first>.planning/phases/02-bridge-codex-session-adapter/02-CONTEXT.md, .planning/phases/02-bridge-codex-session-adapter/02-RESEARCH.md, apps/bridge/src/daemon/relay-connection.ts</read_first>
  <action>Create `apps/bridge/src/daemon/session-manager.ts` to cache discovered sessions, track the currently attached remote-controlled session, and enforce the locked rule that only one session may be remotely attached at a time. Create `apps/bridge/src/daemon/bridge-daemon.ts` to own the `RelayConnection`, `CodexAdapter`, and `SessionManager`, start the local Codex boundary, refresh session metadata, and surface session-ended/error transitions. Add `apps/bridge/src/cli/daemon.ts` (and package entry updates if needed) so the bridge can run as a long-lived daemon instead of only exposing the pairing CLI.</action>
  <verify>rg -n 'SessionManager|BridgeDaemon|attachedSessionId|one active|daemon' apps/bridge/src/daemon/bridge-daemon.ts apps/bridge/src/daemon/session-manager.ts apps/bridge/src/cli/daemon.ts apps/bridge/package.json</verify>
  <acceptance_criteria>
    - `apps/bridge/src/daemon/session-manager.ts` exports `SessionManager`
    - `apps/bridge/src/daemon/bridge-daemon.ts` exports `BridgeDaemon`
    - `apps/bridge/src/daemon/session-manager.ts` enforces one active attached session
    - `apps/bridge/src/cli/daemon.ts` exists as a daemon entry point
  </acceptance_criteria>
  <done>The bridge has a long-running daemon model with explicit session ownership and attach-state management.</done>
</task>

<task type="auto">
  <name>Task 2: Route relay JSON-RPC methods through the session manager and Codex adapter</name>
  <files>apps/bridge/src/daemon/message-router.ts, apps/bridge/src/daemon/bridge-daemon.ts, apps/relay/tests/ws-bridge.test.ts</files>
  <read_first>packages/protocol/src/bridge.ts, apps/bridge/src/daemon/codex-adapter.ts, apps/bridge/src/daemon/session-manager.ts, apps/relay/src/routes/ws-bridge.ts</read_first>
  <action>Create `apps/bridge/src/daemon/message-router.ts` to parse relay JSON-RPC requests and notifications using the shared bridge schemas, handle `session.list`, `session.attach`, `session.detach`, `turn.send`, `turn.steer`, `approval.respond`, and `turn.interrupt`, and send structured responses/notifications back through the relay connection. Update the daemon so attach triggers `thread/resume`, history load, `session.history`, and subsequent live event fanout, while detach or local termination emits `session.ended`. Validate commands against the active session and sandbox/approval constraints before forwarding them to Codex.</action>
  <verify>rg -n 'session.list|session.attach|session.detach|turn.send|turn.steer|approval.respond|turn.interrupt|session.history|session.ended' apps/bridge/src/daemon/message-router.ts apps/bridge/src/daemon/bridge-daemon.ts</verify>
  <acceptance_criteria>
    - `apps/bridge/src/daemon/message-router.ts` handles `session.attach`
    - `apps/bridge/src/daemon/message-router.ts` handles `turn.send`
    - `apps/bridge/src/daemon/message-router.ts` handles `approval.respond`
    - `apps/bridge/src/daemon/bridge-daemon.ts` emits `session.history`
    - `apps/bridge/src/daemon/bridge-daemon.ts` emits `session.ended`
  </acceptance_criteria>
  <done>The bridge can expose sessions, attach to one, and route remote commands/events through the real local Codex adapter.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration coverage for session list, attach/history, and local failure handling</name>
  <files>apps/bridge/tests/session-flow.test.ts, apps/bridge/tests/event-relay.test.ts, apps/relay/tests/ws-bridge.test.ts</files>
  <read_first>apps/bridge/src/daemon/bridge-daemon.ts, apps/bridge/src/daemon/message-router.ts, apps/bridge/src/daemon/session-manager.ts, .planning/phases/02-bridge-codex-session-adapter/02-RESEARCH.md</read_first>
  <action>Create `apps/bridge/tests/session-flow.test.ts` for session listing and attach/history orchestration with mocked relay and Codex adapter boundaries. Create `apps/bridge/tests/event-relay.test.ts` for command/event fanout, one-active-session rejection, and local termination handling. Extend `apps/relay/tests/ws-bridge.test.ts` only where relay-side bridge route expectations need to know about the richer session/list/attach traffic or local failure signaling.</action>
  <verify>rg -n 'describe\\(|session.attach|session.history|session.ended|one active|approval.respond|turn.interrupt' apps/bridge/tests/session-flow.test.ts apps/bridge/tests/event-relay.test.ts apps/relay/tests/ws-bridge.test.ts</verify>
  <acceptance_criteria>
    - `apps/bridge/tests/session-flow.test.ts` contains `session.attach`
    - `apps/bridge/tests/session-flow.test.ts` contains `session.history`
    - `apps/bridge/tests/event-relay.test.ts` contains `session.ended`
    - `apps/bridge/tests/event-relay.test.ts` covers one-active-session rejection
    - `apps/relay/tests/ws-bridge.test.ts` still passes with the richer bridge traffic
  </acceptance_criteria>
  <done>Phase 2 ends with regression coverage for list, attach/resume, and bridge-local failure cases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run --project phase-01-unit apps/bridge/tests/session-flow.test.ts apps/bridge/tests/event-relay.test.ts apps/relay/tests/ws-bridge.test.ts` succeeds
- [ ] `rg -n 'session.attach|session.history|session.ended|turn.send|approval.respond' apps/bridge/src/daemon/message-router.ts apps/bridge/src/daemon/bridge-daemon.ts` returns matches
- [ ] Attempting to attach a second remote session is rejected without corrupting the active one
</verification>

<success_criteria>

- Real local Codex sessions are visible to the relay
- Remote attach/resume preserves existing conversation history
- Only one remote-controlled session can be active per bridge instance
- Local session failures and detachs produce honest end/error signals
</success_criteria>

<output>
After completion, create `.planning/phases/02-bridge-codex-session-adapter/02-03-SUMMARY.md`
</output>
