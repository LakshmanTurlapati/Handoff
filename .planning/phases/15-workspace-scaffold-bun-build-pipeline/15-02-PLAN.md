---
phase: 15-workspace-scaffold-bun-build-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/cli-darwin-arm64/package.json
  - apps/cli-darwin-arm64/README.md
  - apps/cli-darwin-arm64/.gitignore
  - apps/cli-darwin-x64/package.json
  - apps/cli-darwin-x64/README.md
  - apps/cli-darwin-x64/.gitignore
  - apps/cli-linux-x64/package.json
  - apps/cli-linux-x64/README.md
  - apps/cli-linux-x64/.gitignore
  - apps/cli-linux-arm64/package.json
  - apps/cli-linux-arm64/README.md
  - apps/cli-linux-arm64/.gitignore
  - apps/cli-win32-x64/package.json
  - apps/cli-win32-x64/README.md
  - apps/cli-win32-x64/.gitignore
autonomous: true
requirements:
  - DIST-02

must_haves:
  truths:
    - "Five platform-binary sibling packages exist under apps/ with matching os/cpu filter pairs"
    - "Each sibling package.json declares correct os field (darwin/linux/win32) and cpu field (arm64/x64) per Node's process.platform / process.arch enum"
    - "No sibling package declares a bin field (only the parent package owns bin: { achilles: ... } per RESEARCH.md Pattern 2)"
    - "Windows variant declares files entry for bin/achilles.exe (with .exe suffix); UNIX variants declare bin/achilles"
    - "bin/ directory in each sibling is gitignored (CI-generated artifact, never committed)"
    - "Each sibling README is a 5-line shared template directing users to install the parent package"
  artifacts:
    - path: "apps/cli-darwin-arm64/package.json"
      provides: "darwin/arm64 platform-binary manifest"
      contains: "@achilles/cli-darwin-arm64"
    - path: "apps/cli-darwin-x64/package.json"
      provides: "darwin/x64 platform-binary manifest"
      contains: "@achilles/cli-darwin-x64"
    - path: "apps/cli-linux-x64/package.json"
      provides: "linux/x64 platform-binary manifest"
      contains: "@achilles/cli-linux-x64"
    - path: "apps/cli-linux-arm64/package.json"
      provides: "linux/arm64 platform-binary manifest"
      contains: "@achilles/cli-linux-arm64"
    - path: "apps/cli-win32-x64/package.json"
      provides: "win32/x64 platform-binary manifest with bin/achilles.exe"
      contains: "achilles.exe"
  key_links:
    - from: "apps/achilles-terminal/package.json"
      to: "apps/cli-darwin-arm64/package.json (and 4 siblings)"
      via: "optionalDependencies block — npm resolver filters by sibling's os/cpu at install time"
      pattern: "@achilles/cli-(darwin|linux|win32)-(arm64|x64)"
    - from: "Each sibling package.json"
      to: "apps/cli-<plat>-<arch>/bin/achilles[.exe]"
      via: "files: ['bin/achilles'] (or 'bin/achilles.exe' on win32) — what the tarball ships"
      pattern: "files.*bin/achilles"
---

<objective>
Create the five platform-binary sibling packages under `apps/cli-<platform>-<arch>/`. Each sibling has a package.json with the correct `os` + `cpu` filter pair (so npm install silently skips non-matching siblings on each developer's machine), a 5-line shared-template README directing users to install the parent `achilles` package, and a .gitignore that ignores the CI-generated `bin/` directory.

Purpose: Satisfies DIST-02 by establishing the sibling-package shape that the parent's `optionalDependencies` references and the shim resolves at runtime. The actual compiled binaries (one per sibling, ~60-100 MB each) are produced by Plan 04's CI workflow on native-OS runners — they are NOT committed to git.
Output: 15 files (3 per sibling × 5 siblings). Parallel-safe with Plan 01 (zero file overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-workspace-scaffold-bun-build-pipeline/15-CONTEXT.md
@.planning/phases/15-workspace-scaffold-bun-build-pipeline/15-RESEARCH.md
@.planning/phases/15-workspace-scaffold-bun-build-pipeline/15-PATTERNS.md

<interfaces>
<!-- Sibling-package shape contract from RESEARCH.md §Pattern 2. -->

From RESEARCH.md §Pattern 2 (lines 315-326) — verbatim reference for each sibling package.json:
```
{
  "name": "@achilles/cli-<plat>-<arch>",
  "version": "1.3.0",
  "description": "Bun-compiled achilles binary for <plat> <arch>.",
  "license": "MIT",
  "os": ["<plat>"],
  "cpu": ["<arch>"],
  "files": ["bin/achilles"],
  "publishConfig": { "access": "public" }
}
```

Field substitution table per platform:

| Directory | name | os | cpu | files |
| `apps/cli-darwin-arm64/` | `@achilles/cli-darwin-arm64` | `["darwin"]` | `["arm64"]` | `["bin/achilles"]` |
| `apps/cli-darwin-x64/` | `@achilles/cli-darwin-x64` | `["darwin"]` | `["x64"]` | `["bin/achilles"]` |
| `apps/cli-linux-x64/` | `@achilles/cli-linux-x64` | `["linux"]` | `["x64"]` | `["bin/achilles"]` |
| `apps/cli-linux-arm64/` | `@achilles/cli-linux-arm64` | `["linux"]` | `["arm64"]` | `["bin/achilles"]` |
| `apps/cli-win32-x64/` | `@achilles/cli-win32-x64` | `["win32"]` | `["x64"]` | `["bin/achilles.exe"]` |

Critical invariants from RESEARCH.md §Pattern 2 + Pitfall 3:
- NO `bin` field on platform packages (only parent has bin per RESEARCH.md line 335)
- `os` accepts ONLY: "darwin", "linux", "win32" (Node's process.platform enum — NOT Bun target strings like "windows")
- `cpu` accepts ONLY: "arm64", "x64" (Node's process.arch enum)
- Windows variant's files array uses "bin/achilles.exe" (NOT "bin/achilles") — Pitfall 3 says forgetting the .exe suffix is the single most common cross-platform CLI bug

Shared 5-line README template (per RESEARCH.md Open Question #2 + PATTERNS.md):
```
# @achilles/cli-<plat>-<arch>

This is a platform-binary package consumed by the `achilles` npm CLI.
Do NOT install directly. Install the parent package:

    npm install -g achilles
```

Shared .gitignore (per RESEARCH.md §Runtime State Inventory):
```
bin/
```
The bin/ directory is where Plan 03's build-binaries.mjs (run in Plan 04 CI) deposits the bun-compiled binary. The artifact is reproducible from source so it is never committed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 5 platform-binary sibling packages (package.json + README.md + .gitignore each)</name>
  <files>
    apps/cli-darwin-arm64/package.json,
    apps/cli-darwin-arm64/README.md,
    apps/cli-darwin-arm64/.gitignore,
    apps/cli-darwin-x64/package.json,
    apps/cli-darwin-x64/README.md,
    apps/cli-darwin-x64/.gitignore,
    apps/cli-linux-x64/package.json,
    apps/cli-linux-x64/README.md,
    apps/cli-linux-x64/.gitignore,
    apps/cli-linux-arm64/package.json,
    apps/cli-linux-arm64/README.md,
    apps/cli-linux-arm64/.gitignore,
    apps/cli-win32-x64/package.json,
    apps/cli-win32-x64/README.md,
    apps/cli-win32-x64/.gitignore
  </files>
  <read_first>
    .planning/phases/15-workspace-scaffold-bun-build-pipeline/15-RESEARCH.md,
    .planning/phases/15-workspace-scaffold-bun-build-pipeline/15-PATTERNS.md,
    apps/achilles-cli/package.json
  </read_first>
  <action>
    Create 15 files: one package.json + one README.md + one .gitignore in each of the five sibling directories. The package.json shape is the verbatim 9-field block from RESEARCH.md §Pattern 2 (lines 315-326), substituting name + os + cpu + files per the platform table in the interfaces block above.

    For each of the 5 directories (apps/cli-darwin-arm64, apps/cli-darwin-x64, apps/cli-linux-x64, apps/cli-linux-arm64, apps/cli-win32-x64):

    1. package.json: JSON object with exactly these top-level keys in this order: name, version, description, license, os, cpu, files, publishConfig. name = "@achilles/cli-<plat>-<arch>" (e.g., "@achilles/cli-darwin-arm64"). version = "1.3.0" (matches the parent in Plan 01). description = "Bun-compiled achilles binary for <plat> <arch>." (substitute readable platform name — e.g., "Bun-compiled achilles binary for macOS arm64." or "Bun-compiled achilles binary for Linux x64." or "Bun-compiled achilles binary for Windows x64."). license = "MIT". os = array with single string from Node's process.platform enum ("darwin", "linux", or "win32" — NOT "windows" or "macos"). cpu = array with single string from Node's process.arch enum ("arm64" or "x64"). files = ["bin/achilles"] for UNIX variants, ["bin/achilles.exe"] for the win32-x64 variant (Pitfall 3 — .exe suffix is required on Windows). publishConfig = { "access": "public" } (matches parent — locks the publish shape for Phase 19). DO NOT add a bin field (RESEARCH.md line 335 — bin lives only on the parent so PATH points to one consistent place — the shim). DO NOT add engines (sibling packages ship only a binary; no JS execution; engines floor is enforced at the parent level only). DO NOT add a main or exports field. DO NOT add devDependencies or dependencies (the binary is self-contained per bun --compile semantics).

    2. README.md: 5-line shared template (per RESEARCH.md Open Question #2 recommendation):
       Line 1: "# @achilles/cli-<plat>-<arch>" (substitute the directory's platform-arch slug; matches the package name)
       Line 2: blank
       Line 3: "This is a platform-binary package consumed by the `achilles` npm CLI."
       Line 4: "Do NOT install directly. Install the parent package:"
       Line 5: blank
       Line 6: "    npm install -g achilles" (4-space indent for code-block rendering)
       Total file is exactly these 6 lines; CLAUDE.md global rule applies — NO emojis.

    3. .gitignore: single line "bin/" (the CI-generated binary directory; never committed per RESEARCH.md §Runtime State Inventory).

    The platform mapping (use EXACT strings — Pitfall 3 + Pattern 2):
    - apps/cli-darwin-arm64/ → name="@achilles/cli-darwin-arm64", os=["darwin"], cpu=["arm64"], files=["bin/achilles"], description references "macOS arm64"
    - apps/cli-darwin-x64/ → name="@achilles/cli-darwin-x64", os=["darwin"], cpu=["x64"], files=["bin/achilles"], description references "macOS x64"
    - apps/cli-linux-x64/ → name="@achilles/cli-linux-x64", os=["linux"], cpu=["x64"], files=["bin/achilles"], description references "Linux x64"
    - apps/cli-linux-arm64/ → name="@achilles/cli-linux-arm64", os=["linux"], cpu=["arm64"], files=["bin/achilles"], description references "Linux arm64"
    - apps/cli-win32-x64/ → name="@achilles/cli-win32-x64", os=["win32"], cpu=["x64"], files=["bin/achilles.exe"], description references "Windows x64"

    The root package.json's workspaces glob "apps/*" already covers these directories (per PATTERNS.md "Workspace glob inheritance") — DO NOT modify the root package.json.

    NO sibling references @achilles/voice-*, claude-code-bridge, or companion.md (LOOP-02 invariant — these are pure binary-only packages).
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; node -e "const fs = require('fs'); const path = require('path'); const expected = [['cli-darwin-arm64','darwin','arm64','bin/achilles'],['cli-darwin-x64','darwin','x64','bin/achilles'],['cli-linux-x64','linux','x64','bin/achilles'],['cli-linux-arm64','linux','arm64','bin/achilles'],['cli-win32-x64','win32','x64','bin/achilles.exe']]; for (const [dir, os, cpu, binFile] of expected) { const pkg = JSON.parse(fs.readFileSync(path.join('apps', dir, 'package.json'), 'utf8')); if (pkg.name !== '@achilles/' + dir) throw new Error('name mismatch: ' + dir); if (pkg.version !== '1.3.0') throw new Error('version mismatch: ' + dir); if (JSON.stringify(pkg.os) !== JSON.stringify([os])) throw new Error('os mismatch: ' + dir); if (JSON.stringify(pkg.cpu) !== JSON.stringify([cpu])) throw new Error('cpu mismatch: ' + dir); if (JSON.stringify(pkg.files) !== JSON.stringify([binFile])) throw new Error('files mismatch: ' + dir); if (pkg.bin !== undefined) throw new Error('bin field present (forbidden): ' + dir); if (!pkg.publishConfig || pkg.publishConfig.access !== 'public') throw new Error('publishConfig mismatch: ' + dir); if (!fs.existsSync(path.join('apps', dir, 'README.md'))) throw new Error('README missing: ' + dir); if (!fs.existsSync(path.join('apps', dir, '.gitignore'))) throw new Error('.gitignore missing: ' + dir); const gi = fs.readFileSync(path.join('apps', dir, '.gitignore'), 'utf8').trim(); if (gi !== 'bin/') throw new Error('.gitignore content mismatch: ' + dir + ' (got: ' + gi + ')'); } console.log('All 5 sibling packages validated.');"</automated>
  </verify>
  <acceptance_criteria>
    - 5 directories exist: apps/cli-darwin-arm64/, apps/cli-darwin-x64/, apps/cli-linux-x64/, apps/cli-linux-arm64/, apps/cli-win32-x64/
    - Each directory has package.json + README.md + .gitignore (15 files total)
    - Each package.json has the 8 expected fields (name, version, description, license, os, cpu, files, publishConfig) and NO bin field
    - os field uses Node's process.platform enum: "darwin" | "linux" | "win32" (not "windows" or "macos")
    - cpu field uses Node's process.arch enum: "arm64" | "x64"
    - apps/cli-win32-x64/package.json files entry is ["bin/achilles.exe"] with .exe suffix (Pitfall 3)
    - Each .gitignore is exactly "bin/" (single line)
    - Each README.md follows the 5-line shared template and contains "npm install -g achilles"
    - npm install at the repo root succeeds after this task (workspaces glob auto-picks up the 5 new directories)
    - grep -rE "voice-protocol|voice-stt|voice-tts|claude-code-bridge|companion.md" apps/cli-*/ returns 0 lines (LOOP-02)
    - No emoji characters in any file (grep returns 0)
  </acceptance_criteria>
  <done>
    Fifteen files created across 5 sibling directories. The parent's optionalDependencies block (set in Plan 01) now resolves at install time — npm filters by each sibling's os/cpu and silently skips non-matching ones. The bin/ subdirectory of each sibling stays empty (and gitignored); Plan 04's CI workflow populates it via bun build --compile on native-OS runners.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm publish (CI → registry) | Each sibling tarball is the surface npm consumers download; Phase 19 publishes; Phase 15 locks the shape |
| npm install resolver (registry → developer machine) | os/cpu filter pair is the install-time gate that picks the right binary per machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-supply-chain | Tampering (supply chain — sibling packages) | apps/cli-*/package.json publish shape | mitigate | Five sibling packages have NO dependencies, NO devDependencies, NO postinstall, NO scripts of any kind — the tarball contains exactly one binary file plus the package.json + README. The publish-time tarball secret scan (Phase 19) and the per-binary `--version` smoke (Plan 04 CI) are the runtime mitigations; Plan 02 prevents a tampering surface from existing in the first place by minimizing the package shape. |
| T-15-platform-filter-bypass | Tampering (resolver mismatch) | os / cpu filter pair on sibling package.json | mitigate | The os/cpu enum is verified character-for-character against Node's process.platform / process.arch values (the verify command parses the JSON and asserts the expected strings). A wrong string (e.g., "macos" instead of "darwin", or "windows" instead of "win32") would silently cause the resolver to install the wrong binary on the wrong machine — caught at the package.json validation step in this plan, then re-verified by the per-OS native runner in Plan 04 CI. |
</threat_model>

<verification>
After this plan's single task completes:
- All 15 files exist with expected shapes (verified by the Task 1 automated command)
- `npm install` at the repo root succeeds (workspaces auto-glob picks up 5 new directories)
- `npm pack --dry-run --workspace apps/cli-darwin-arm64` (and 4 siblings) exits 0 — confirms tarball shape is valid for Phase 19 publish
- No sibling has a `bin` field (would conflict with parent's bin per RESEARCH.md)
- `apps/cli-win32-x64/package.json` files entry uses `.exe` suffix
</verification>

<success_criteria>
DIST-02 (sibling-package half): 5 platform-binary packages exist with correct os/cpu filter pairs and minimal-shape package.json that npm can publish + npm install can filter. The shim's `import.meta.resolve("@achilles/cli-<plat>-<arch>/package.json")` in Plan 03 will succeed on a matching-platform install (the sibling will be present in node_modules) and fail on a non-matching install (the sibling will be silently skipped by the resolver — exactly the shim's fallback trigger).
</success_criteria>

<output>
Create `.planning/phases/15-workspace-scaffold-bun-build-pipeline/15-02-SUMMARY.md` summarizing the 15 files created across 5 sibling directories, the DIST-02 sibling-package contract that's now locked, and any RESEARCH.md spec deviations encountered.
</output>
