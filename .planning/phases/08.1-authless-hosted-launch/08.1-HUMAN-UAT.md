---
status: partial
phase: 08.1-authless-hosted-launch
source: [08.1-01-PLAN.md, .continue-here.md]
started: 2026-05-12T00:00:00Z
updated: 2026-05-12T00:00:00Z
---

## Current Test

[awaiting human bootstrap pairing]

## Tests

### 1. One-time hosted pairing bootstrap
expected: Running `handoff pair --base-url https://handoff-web.fly.dev`, completing the browser sign-in, and approving the verification phrase populates `~/.config/handoff` so subsequent CLI calls reach real bootstrap/runtime logic instead of failing closed.
result: pending

### 2. `/handoff` slash command end-to-end in real Codex thread
expected: After Codex is restarted (if needed) and the local `handoff` bin is installed, invoking `/handoff` from an active Codex thread returns a real handoff payload (not "command unrecognized" and not a fail-closed install error).
result: pending

### 3. Push `4cd14a4` upstream (optional, for shared distribution)
expected: The CLI packaging fix is published so other installs receive an executable `handoff` bin without requiring a local rebuild.
result: pending

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
