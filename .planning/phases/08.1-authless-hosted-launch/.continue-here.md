---
phase: 08.1-authless-hosted-launch
task: 4
total_tasks: 4
status: blocked
last_updated: 2026-04-20T15:33:33.808Z
---

<current_state>
Phase 08.1 is functionally implemented. The hosted web app is deployed on Fly with the authless `/launch/<publicId>` flow, and the local `handoff` executable now works again. The stop point is no longer a code bug; it is the one-time bootstrap pairing step that still needs a real signed-in browser session so `~/.config/handoff` can be populated.
</current_state>

<completed_work>

- Task 1: Ship the authless hosted launch flow and durable device-session browser auth - Done in `3b2f288`
- Task 2: Fix Fly public redirect handling for `/launch` URLs - Done in `131451e`
- Task 3: Repair `handoff` CLI executable packaging and local install - Done in `4cd14a4`
- Verified `handoff --help`, `validate:handoff-pack`, and `validate:handoff-publish-install` after the packaging fix
- Reinstalled the local global helper and confirmed the packaged `/handoff` prompt files under `~/.codex/prompts` and `~/.codex/commands`
</completed_work>

<remaining_work>

- Task 4: Complete the one-time hosted pairing/bootstrap in a real browser session
- Restart Codex if the currently running app still says `/handoff` is unrecognized
- Run `/handoff` from the intended Codex thread and verify the helper reaches real bootstrap/runtime logic instead of fail-closed install errors
- Push `4cd14a4` if the CLI packaging fix should be shared remotely
</remaining_work>

<decisions_made>

- Kept `/launch/<publicId>` authless while leaving the legacy `/pair/<pairingId>` bootstrap flow behind GitHub OAuth because only the generated handoff URL needed the OAuth removal
- Fixed the CLI problem at the packaging layer by marking `dist/cli.js` executable in the bridge build instead of patching the local install by hand
- Strengthened the validators to execute the packed/installed `handoff` bin directly so future regressions fail before publish
</decisions_made>

<blockers>
- Human action required: the hosted pairing page still redirects anonymous requests to `/sign-in?callbackUrl=/pair/...`, so a real browser session must complete sign-in and verification
- Potential app-cache issue: the Codex app may need a restart once to reload the already-installed `/handoff` command files
</blockers>

<context>
The code state is ahead of `STATE.md`: phase 08.1 is implemented and deployed, and the local CLI packaging bug is fixed locally in commit `4cd14a4`, but the operational verification loop is incomplete. `~/.config/handoff` exists but is empty, which explains the remaining bootstrap failure. I started a fresh `handoff pair --base-url https://handoff-web.fly.dev` run, opened the hosted URL, confirmed the route still requires sign-in, then interrupted the waiting terminal session so no background pair process is left running.
</context>

<next_action>
Start with: `handoff pair --base-url https://handoff-web.fly.dev`, finish the browser sign-in and local verification-phrase approval, then restart Codex if needed and run `/handoff` from the target thread.
</next_action>
