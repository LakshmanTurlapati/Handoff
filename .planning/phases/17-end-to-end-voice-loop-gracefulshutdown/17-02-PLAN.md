---
phase: 17
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - apps/achilles-terminal/src/audio/tts-playback.ts
  - apps/achilles-terminal/src/audio/stt-bridge.ts
  - apps/achilles-terminal/src/stuck-thinking-watchdog.ts
  - apps/achilles-terminal/src/child-exit-watchdog.ts
  - apps/achilles-terminal/tests/audio/tts-playback.test.ts
  - apps/achilles-terminal/tests/audio/stt-bridge.test.ts
  - apps/achilles-terminal/tests/stuck-thinking-watchdog.test.ts
  - apps/achilles-terminal/tests/child-exit-watchdog.test.ts
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - LOOP-01
  - ERR-05
  - ERR-06
must_haves:
  truths:
    - "TTS playback subprocess accepts MP3 chunks via stdin and signals drain when ffplay exits"
    - "Half-duplex SPEAKING_DEBOUNCE_MS=300 tail enforced after TTS drain"
    - "STT bridge constructs a voice-stt realtime client via webSocketCtor DI seam and forwards Int16Array frames"
    - "Stuck-thinking watchdog fires onTimeout after 60s without a Claude progress event"
    - "Child-exit watchdog respawns sox/ffplay up to 3 times within a 10s sliding window then transitions to error"
  artifacts:
    - path: "apps/achilles-terminal/src/audio/tts-playback.ts"
      provides: "ffplay child spawn + voice-tts events$ consumer + drain detection + half-duplex tail debounce"
      exports: ["createTtsPlayback", "TtsPlaybackHandle", "FFPLAY_ARGS"]
    - path: "apps/achilles-terminal/src/audio/stt-bridge.ts"
      provides: "voice-stt realtime-client factory wrapper consuming Int16Array frames from mic-sox"
      exports: ["createSttBridge", "SttBridgeHandle"]
    - path: "apps/achilles-terminal/src/stuck-thinking-watchdog.ts"
      provides: "ERR-05 stuck-thinking watchdog port from v1.2 (60s no-progress trigger)"
      exports: ["createStuckThinkingWatchdog", "StuckThinkingWatchdog", "STUCK_THINKING_DEFAULT_TIMEOUT_MS", "STUCK_THINKING_ANNOUNCEMENT"]
    - path: "apps/achilles-terminal/src/child-exit-watchdog.ts"
      provides: "ERR-03/ERR-06 sox/ffplay child-exit watchdog with bounded respawn"
      exports: ["createChildExitWatchdog", "ChildExitWatchdog"]
  key_links:
    - from: "apps/achilles-terminal/src/audio/tts-playback.ts"
      to: "packages/voice-tts (via @achilles/voice-tts events$ async iterable)"
      via: "for-await loop pushing TtsChunk.bytes into ffplay.stdin.write"
      pattern: "events\\$|ffplay|stdin\\.write"
    - from: "apps/achilles-terminal/src/audio/stt-bridge.ts"
      to: "packages/voice-stt createRealtimeSttClient"
      via: "webSocketCtor DI seam (globalThis.WebSocket in production)"
      pattern: "createRealtimeSttClient|webSocketCtor|globalThis\\.WebSocket"
    - from: "apps/achilles-terminal/src/child-exit-watchdog.ts"
      to: "process exit-code observer"
      via: "3-in-10s sliding window respawn cap"
      pattern: "respawn|sliding window|RESPAWN_MAX|RESPAWN_WINDOW_MS"
---

<objective>
Wave 2 — port the four per-bridge runtime modules that Plan 04's session.ts composes: TTS playback subprocess wrapper (PLAY-01 + PLAY-02), STT bridge factory (LOOP-01 STT half), stuck-thinking watchdog (ERR-05), and child-exit watchdog (ERR-03/ERR-06). These four files have zero file-overlap with Plan 03 (which owns claude-bridge.ts + sandwich-defence.ts) so this plan and Plan 03 run in parallel under Wave 2.

Purpose: each module owns one concern; the seams are stable so Plan 04 can wire them via constructor injection without modifying their internals. The TTS playback wrapper is the load-bearing PLAY-01 piece (ffplay flag set + backpressure + drain detection); STT bridge is the thinnest possible adapter around voice-stt's existing webSocketCtor seam; the two watchdogs are ports of v1.2's existing modules with Phase 17's "no Electron" adaptations.

Output: 4 new source files, 4 new test files. ZERO modifications under packages/voice-*, packages/claude-code-bridge/, or packages/achilles-skill/skill/prompts/companion.md (LOOP-02 invariant).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-VALIDATION.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-01-PLAN.md
@.planning/research/PITFALLS.md
@apps/achilles/src/main/stuck-thinking-watchdog.ts
@apps/achilles-terminal/src/audio/mic-sox.ts
@apps/achilles-terminal/src/audio/vad-energy.ts
@apps/achilles-terminal/src/state/constants.ts
@packages/voice-stt/src/realtime-client.ts
@packages/voice-tts/src/stream-client.ts
@packages/voice-protocol/src/tts-events.ts

<interfaces>
<!-- Key contracts. Extracted from the codebase. -->

From packages/voice-stt/src/realtime-client.ts (DI seams at lines 95-98):
```
export type SttWebSocketCtor = (url: string, protocols?: string | string[]) => SttWebSocketLike;
export interface CreateRealtimeSttClientOptions {
  getToken: () => Promise<{ token: string; expiresAt: string }> | { token: string; expiresAt: string };
  webSocketCtor?: SttWebSocketCtor;
  url?: string;
}
export function createRealtimeSttClient(opts: CreateRealtimeSttClientOptions): RealtimeSttClient;
// RealtimeSttClient exposes: start(), stop(), write(frame: Int16Array), commit(), events$: AsyncIterable<SttEvent>
```

From packages/voice-tts/src/stream-client.ts (DI seam at line 92):
```
export interface CreateTtsStreamClientOptions {
  keySource: KeySource;            // () => Promise<string> | string
  voiceId: string;
  webSocketCtor?: TtsWebSocketCtor;
  outputFormat?: TtsOutputFormat;  // default "mp3_44100"
}
export function createTtsStreamClient(opts: CreateTtsStreamClientOptions): TtsStreamClient;
// TtsStreamClient exposes: open(), appendText(text), flush(), close(), events$: AsyncIterable<TtsEvent>
```

From packages/voice-protocol/src/tts-events.ts:
```
type TtsChunk = { type: "chunk"; chunk: { seq, mime: "audio/mpeg"|"audio/pcm", bytes: ArrayBuffer, isFinal: boolean } }
type TtsStreamComplete = { type: "complete" }
type TtsEvent = TtsChunk | TtsStreamComplete
```

From apps/achilles-terminal/src/state/constants.ts:
- `SPEAKING_DEBOUNCE_MS = 300` — half-duplex tail (PLAY-02)

From .planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md `<decisions>` row "ffplay TTS playback":
- ffplay command line: `ffplay -loglevel quiet -nodisp -autoexit -fflags +nobuffer -flags +low_delay -framedrop -probesize 32 -analyzeduration 0 -i -`
- stdin pipe carries MP3 chunks; drain detection via ffplay process exit
- Half-duplex enforced: mic frames during speaking state are dropped at the VAD layer; 300ms playback-tail debounce prevents listening transition before TTS audio fully drains

From .planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md `<decisions>` row "Child-exit watchdog":
- sox + ffplay child-exit watchdog: max 3 respawns in 10-second sliding window
- Cap exceeded → transition state machine to error with "Audio device lost — restart Achilles"

From .planning/research/PITFALLS.md Pitfall 7 — ffplay backpressure:
- Use `stdin.write(chunk, callback)` and await the callback before continuing the for-await loop
- Catch EPIPE; if ffplay dies mid-stream → mark "speaking_failed" + emit error event
- Send EOF via `stdin.end()` on stream_complete (do NOT kill ffplay; -autoexit + EOF drains cleanly)

From apps/achilles/src/main/stuck-thinking-watchdog.ts (the v1.2 port source — 301 LOC):
- Public surface: `createStuckThinkingWatchdog({ onTimeout, timeoutMs?, setTimeoutImpl?, clearTimeoutImpl?, nowImpl?, logger? }): StuckThinkingWatchdog`
- Handle exposes: `armForTurn(), observeProgress(), clearForTurn(), dispose()`
- Locked constants: `STUCK_THINKING_DEFAULT_TIMEOUT_MS = 60_000`, `STUCK_THINKING_ANNOUNCEMENT = "Claude is still working — I'll let you know when it's done."`
- Threat T-14-20: logger sees waitedMs only, never transcript fragments
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TTS playback module (PLAY-01 + PLAY-02) + STT bridge factory (LOOP-01 STT half)</name>
  <files>apps/achilles-terminal/src/audio/tts-playback.ts, apps/achilles-terminal/src/audio/stt-bridge.ts, apps/achilles-terminal/tests/audio/tts-playback.test.ts, apps/achilles-terminal/tests/audio/stt-bridge.test.ts</files>
  <read_first>
    - .planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md `<decisions>` rows "ffplay TTS playback (PLAY-01, PLAY-02)" + "HTTP 429 messaging (ERR-02)"
    - .planning/research/PITFALLS.md §7 — full ffplay flag tuning + backpressure + EPIPE handling
    - packages/voice-tts/src/stream-client.ts lines 1-200 — events$ shape, open()/appendText()/flush()/close() contract, webSocketCtor seam
    - packages/voice-protocol/src/tts-events.ts — TtsChunk + TtsStreamComplete + TtsEvent union shape
    - packages/voice-stt/src/realtime-client.ts lines 1-200 — webSocketCtor seam, getToken callback, RealtimeSttClient surface (start/stop/write/commit/events$)
    - apps/achilles-terminal/src/audio/mic-sox.ts — the upstream producer of Int16Array frames + RMS scalars that stt-bridge consumes
    - apps/achilles-terminal/src/session-events.ts (created in Plan 01 Task 1) — the SessionEvent discriminated union both modules emit on
    - apps/achilles-terminal/src/circuit-breaker.ts (created in Plan 01 Task 2) — the breaker both modules wrap their .open() calls in (CONTEXT.md "applies to voice-stt WSS connect AND voice-tts WSS connect")
  </read_first>
  <action>
Create apps/achilles-terminal/src/audio/tts-playback.ts. Export the const string `FFPLAY_ARGS` as a readonly tuple containing exactly: `["-loglevel", "quiet", "-nodisp", "-autoexit", "-fflags", "+nobuffer", "-flags", "+low_delay", "-framedrop", "-probesize", "32", "-analyzeduration", "0", "-f", "mp3", "-i", "pipe:0"]` — note `-f mp3` is added per PITFALLS.md "Integration Gotchas" row "ffplay subprocess: Pipe MP3 chunks without setting -f mp3"; the auto-detect can misidentify small initial chunks.

Export `createTtsPlayback(deps: { ttsFactory: (opts: { voiceId: string }) => import("@achilles/voice-tts").TtsStreamClient; voiceId: string; spawnImpl?: typeof import("node:child_process").spawn; circuitBreaker?: import("../circuit-breaker.js").CircuitBreaker; emit: (event: import("../session-events.js").SessionEvent) => void; logger?: import("../structured-logger.js").StructuredLogger }): TtsPlaybackHandle`. The handle exposes: `appendText(text: string): void` — synthesises through ttsClient.appendText; `flush(): Promise<void>`; `cancel(): Promise<void>` — closes voice-tts WSS + sends `stdin.end()` to ffplay + 200ms timeout then `proc.kill("SIGTERM")`; `dispose(): Promise<void>` — terminal teardown. Lifecycle: `start()` is called once per session and (a) calls ttsClient.open() routed through circuitBreaker.attempt() when provided; on circuit-exhausted it emits a SessionEvent { type: "error", payload: { classification: outcome.error.kind, message: ... } } and throws so Plan 04's session.ts catches it. (b) spawns ffplay via `(spawnImpl ?? spawn)("ffplay", FFPLAY_ARGS, { stdio: ["pipe", "ignore", "pipe"] })`. (c) attaches `ffplay.on("exit", (code) => { /* emit tts_drained then if code !== 0 emit error with classification=playback_lost */ })`. (d) starts the async iterator consumer: `for await (const ev of ttsClient.events$) { if (ev.type === "chunk") { await new Promise((resolve, reject) => ffplay.stdin.write(Buffer.from(ev.chunk.bytes), (err) => err ? reject(err) : resolve())); if (ev.chunk.isFinal) ffplay.stdin.end(); } else if (ev.type === "complete") { ffplay.stdin.end(); emit tts_drained after ffplay.exit fires; } }`. Catch EPIPE on the awaited stdin.write — when ffplay died mid-stream, emit error with classification="playback_lost" and terminate the iterator. Do NOT block the consumer loop on a single hung write — use `Promise.race` against a 5s timeout if needed.

Half-duplex (PLAY-02): the playback module does NOT own the mic gate. It owns ONLY the `tts_drained` SessionEvent emission. Plan 04's session.ts owns the SPEAKING_DEBOUNCE_MS=300 timer scheduling after tts_drained. The playback module's contract: emit tts_drained immediately when ffplay.on("exit") fires AND the consumer iterator has finished (use a Promise.all of both).

Create apps/achilles-terminal/src/audio/stt-bridge.ts. Export `createSttBridge(deps: { sttFactory: (opts: { getToken: import("@achilles/voice-stt").CreateRealtimeSttClientOptions["getToken"]; webSocketCtor?: import("@achilles/voice-stt").SttWebSocketCtor }) => import("@achilles/voice-stt").RealtimeSttClient; mintToken: () => Promise<{ token: string; expiresAt: string }>; webSocketCtor?: import("@achilles/voice-stt").SttWebSocketCtor; circuitBreaker?: import("../circuit-breaker.js").CircuitBreaker; emit: (event: import("../session-events.js").SessionEvent) => void; logger?: import("../structured-logger.js").StructuredLogger }): SttBridgeHandle`. The handle exposes: `start(): Promise<void>` — routes mintToken() through circuitBreaker.attempt() when provided; on circuit-exhausted emits error event with classification=auth/rate_limit/etc and throws; on success calls sttClient.start(). `write(frame: Int16Array): void` — forwards to sttClient.write(frame). `commit(): void` — calls sttClient.commit(). `stop(): Promise<void>`. `events$(): AsyncIterable<SttEvent>` — yields from sttClient.events$, mapping `partial` → emit { type: "stt_partial", payload: { text } } and `committed` → emit { type: "stt_committed", payload: { text } } on the session emitter via deps.emit BEFORE yielding upstream.

Create apps/achilles-terminal/tests/audio/tts-playback.test.ts. Tests: (1) "spawn ffplay with the locked args" — pass a recording spawnImpl, assert the args array equals FFPLAY_ARGS. (2) "writes MP3 chunks to ffplay stdin" — pass a mock spawnImpl returning a fake child with a recording stdin.write; pass a mock ttsFactory whose events$ yields three chunks then complete; assert all three chunks reach stdin.write in order; assert stdin.end() is called after complete. (3) "emits tts_drained on ffplay exit + iterator complete" — capture emit calls; assert exactly one { type: "tts_drained" } event after both signals fire. (4) "EPIPE on stdin.write surfaces as error event with classification=playback_lost" — the recording stdin.write rejects with new Error("EPIPE"); assert emit captures { type: "error", payload: { classification: "playback_lost" } }. (5) "cancel sends stdin.end then SIGTERM after 200ms" — use fake timers; call cancel(); assert stdin.end called immediately; advance 200ms; assert proc.kill called with "SIGTERM".

Create apps/achilles-terminal/tests/audio/stt-bridge.test.ts. Tests: (1) "constructs realtime client with injected webSocketCtor" — pass a recording sttFactory; call start(); assert sttFactory was called once with `{ getToken, webSocketCtor }`. (2) "mintToken failure routed through circuit breaker" — pass a circuitBreaker mock returning `{ error: { kind: "auth" }, exhausted: true, attemptCount: 1, consecutiveFailures: 1 }`; assert emit was called with `{ type: "error", payload: { classification: "auth", ... } }` and start() rejected. (3) "write forwards frames to sttClient" — pass an Int16Array; assert sttClient.write was called with the same buffer. (4) "events$ emits stt_partial and stt_committed on session emitter" — drive the mock sttClient.events$ to yield a partial then a committed event; assert emit was called twice with the right SessionEvent shapes.

NO emojis. NO modification of LOOP-02 paths. NO new top-level static imports in cli.ts (the new modules are imported lazily by session.ts).
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npm run typecheck --workspace apps/achilles-terminal &amp;&amp; npm test --workspace apps/achilles-terminal -- tests/audio/tts-playback.test.ts tests/audio/stt-bridge.test.ts &amp;&amp; grep -F '"-loglevel", "quiet", "-nodisp", "-autoexit", "-fflags", "+nobuffer", "-flags", "+low_delay"' apps/achilles-terminal/src/audio/tts-playback.ts | wc -l | grep -E '^[[:space:]]*[1-9]' &amp;&amp; grep -E 'createRealtimeSttClient' apps/achilles-terminal/src/audio/stt-bridge.ts | wc -l | grep -E '^[[:space:]]*[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - tts-playback.ts exports createTtsPlayback + TtsPlaybackHandle + FFPLAY_ARGS readonly tuple matching the CONTEXT.md-locked flag set + `-f mp3` (PITFALLS Integration Gotcha mitigation)
    - tts-playback.ts handles EPIPE on stdin.write by emitting error event (classification=playback_lost) without crashing the consumer iterator
    - tts-playback.ts respects backpressure: awaits the stdin.write callback before continuing the for-await iterator
    - stt-bridge.ts constructs a voice-stt realtime client via the package's webSocketCtor DI seam without modifying any file under packages/voice-stt/
    - stt-bridge.ts routes mintToken through circuitBreaker.attempt() when provided; on exhaustion emits the right SessionEvent error
    - 9 unit tests pass total (5 for tts-playback, 4 for stt-bridge) under vitest --pool=forks
    - LOOP-02: zero diff under packages/voice-stt/, packages/voice-tts/, packages/voice-protocol/
  </acceptance_criteria>
  <done>PLAY-01 + PLAY-02 substrate is on disk + tested; LOOP-01's STT half has a deterministic seam Plan 04 wires into session.ts. The two modules are independently constructable + disposable so the integration test in Plan 05 can swap in MOCK_LOOP fakes.</done>
</task>

<task type="auto">
  <name>Task 2: Stuck-thinking watchdog port (ERR-05) + child-exit watchdog (ERR-03/ERR-06 substrate)</name>
  <files>apps/achilles-terminal/src/stuck-thinking-watchdog.ts, apps/achilles-terminal/src/child-exit-watchdog.ts, apps/achilles-terminal/tests/stuck-thinking-watchdog.test.ts, apps/achilles-terminal/tests/child-exit-watchdog.test.ts</files>
  <read_first>
    - apps/achilles/src/main/stuck-thinking-watchdog.ts — full 301 LOC port source (factory + 4 lifecycle methods + 2 locked constants + threat model T-14-19/T-14-20)
    - apps/achilles/src/main/stuck-thinking-watchdog.test.ts — 507 LOC; mirror the SW1..SW7 cases (arm-then-fire, observeProgress-no-op-when-unarmed, clearForTurn-cancels-without-firing, disposed-late-fire-noop, onTimeout-receives-waitedMs, idempotent-re-arm, logger-redaction)
    - .planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md `<decisions>` row "Stuck-thinking watchdog (ERR-05)" — 60s default + reset-on-progress + locked announcement string
    - .planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md `<decisions>` row "Child-exit watchdog (ERR-03, ERR-06)" — 3 respawns in 10s sliding window + cap-exceeded transition to error + suspend/resume EIO via macOS SIGCONT
    - .planning/research/PITFALLS.md §6 (VAD reverberation), §4 (sox/ffmpeg device-open failure), Pitfall 10 (SIGINT propagation)
  </read_first>
  <action>
Create apps/achilles-terminal/src/stuck-thinking-watchdog.ts as a verbatim port of apps/achilles/src/main/stuck-thinking-watchdog.ts. Adjustments: (a) no import path changes needed (the v1.2 file has zero electron / IPC imports — confirm by `grep -E "^import" apps/achilles/src/main/stuck-thinking-watchdog.ts | grep -v "from \\\"node:" | wc -l` returning 0 for non-node imports); (b) exported names byte-for-byte: `STUCK_THINKING_DEFAULT_TIMEOUT_MS` (60_000), `STUCK_THINKING_ANNOUNCEMENT` ("Claude is still working — I'll let you know when it's done."), `StuckThinkingTimeoutEvent`, `StuckThinkingWatchdogOptions`, `StuckThinkingWatchdog`, `createStuckThinkingWatchdog`. Preserve the em-dash U+2014 inside STUCK_THINKING_ANNOUNCEMENT — it is not an emoji.

Create apps/achilles-terminal/src/child-exit-watchdog.ts as a NEW module (no v1.2 equivalent — v1.2 had Electron's powerMonitor; v1.3 polls subprocess exit codes). Public surface: `createChildExitWatchdog<TChild>({ label, child, respawnFactory, onError, logger?, nowImpl?, maxRespawns?, windowMs? }): ChildExitWatchdog` where:
- `label`: "sox" | "ffplay" — used in log lines + the "Audio device lost — restart Achilles" surfacing
- `child`: the initial ChildProcess instance from spawn(); the watchdog attaches an `on("exit", code => ...)` listener
- `respawnFactory: () => TChild` — returns a fresh ChildProcess on respawn; the watchdog re-attaches its exit listener
- `onError(message: string): void` — called when respawn cap is exceeded; Plan 04's session.ts wires this to dispatch INJECT_ERROR + emit SessionEvent { type: "error", payload: { classification: "mic_unavailable" | "playback_lost", message: "Audio device lost — restart Achilles" } }
- `maxRespawns`: default 3 (per CONTEXT.md "max 3 respawns in 10-second sliding window")
- `windowMs`: default 10_000
- `nowImpl`: () => Date.now() default
- `logger`: optional StructuredLogger; logs `{event: "child_exit", label, code, attempt}` per exit; logs `{event: "respawn_cap_exceeded", label, attempts, windowMs}` when exhausted

The handle exposes: `dispose(): void` — removes exit listener + clears any pending respawn timer. On every child exit: (1) push current timestamp onto an internal `recentExits: number[]` array; (2) drop entries older than windowMs from the head; (3) if length > maxRespawns, call onError with the locked "Audio device lost — restart Achilles" string and STOP respawning; (4) otherwise call respawnFactory() and re-attach the exit listener. Idempotent: dispose() during a respawn-in-flight cleanly cancels the next attempt.

Create apps/achilles-terminal/tests/stuck-thinking-watchdog.test.ts mirroring the v1.2 SW1..SW7 cases. Inject `setTimeoutImpl` + `clearTimeoutImpl` as recording fakes; assert: (SW1) armForTurn schedules the timer + onTimeout fires with waitedMs=60_000 after 60s elapses; (SW2) observeProgress when no timer is armed is a no-op; (SW3) clearForTurn cancels without firing; (SW4) dispose followed by a stale timer callback is a no-op; (SW5) onTimeout receives `{ waitedMs }` payload; (SW6) idempotent re-arm cancels the prior token; (SW7) logger receives `[achilles] stuck-thinking timer fired: waitedMs=60000` with NO transcript content.

Create apps/achilles-terminal/tests/child-exit-watchdog.test.ts. Tests: (1) "child exit triggers respawnFactory" — fake child emits "exit" with code 1; assert respawnFactory was called once. (2) "3 exits within windowMs triggers cap-exceeded" — simulate 4 exits 100ms apart; assert respawnFactory called exactly 3 times; assert onError called once with "Audio device lost — restart Achilles". (3) "exits spaced beyond windowMs do not trip cap" — simulate 2 exits then advance nowImpl by 11_000ms then 2 more exits; assert respawnFactory called 4 times; assert onError never called. (4) "dispose during respawn-in-flight cleanly cancels" — call dispose() between exit and respawn; assert respawnFactory never called after dispose. (5) "logger records exit + cap-exceeded events" — pass a mock StructuredLogger; assert the right NDJSON events appear.

NO emojis. NO modification of LOOP-02 paths.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npm run typecheck --workspace apps/achilles-terminal &amp;&amp; npm test --workspace apps/achilles-terminal -- tests/stuck-thinking-watchdog.test.ts tests/child-exit-watchdog.test.ts &amp;&amp; grep -E 'STUCK_THINKING_DEFAULT_TIMEOUT_MS\s*=\s*60_?000' apps/achilles-terminal/src/stuck-thinking-watchdog.ts | wc -l | grep -E '^[[:space:]]*[1-9]' &amp;&amp; grep -F 'Claude is still working' apps/achilles-terminal/src/stuck-thinking-watchdog.ts | wc -l | grep -E '^[[:space:]]*[1-9]' &amp;&amp; grep -E 'Audio device lost — restart Achilles|Audio device lost -- restart Achilles' apps/achilles-terminal/src/child-exit-watchdog.ts | wc -l | grep -E '^[[:space:]]*[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - stuck-thinking-watchdog.ts exports STUCK_THINKING_DEFAULT_TIMEOUT_MS=60_000 + STUCK_THINKING_ANNOUNCEMENT (em-dash U+2014 preserved) + the createStuckThinkingWatchdog factory + 4-method handle
    - SW1..SW7 test cases pass
    - child-exit-watchdog.ts implements 3-in-10s sliding window respawn cap; emits "Audio device lost — restart Achilles" on cap exceeded
    - 5 child-exit-watchdog tests pass (single-exit respawn, cap-exceeded, sliding-window roll-off, dispose-during-respawn, logger events)
    - LOOP-02 invariant: zero diff under packages/voice-* + packages/claude-code-bridge/ + packages/achilles-skill/skill/prompts/companion.md
  </acceptance_criteria>
  <done>ERR-05 (stuck-thinking) + ERR-03/ERR-06 (child-exit) substrate is on disk + tested. Plan 04 can wire both into session.ts via constructor injection without touching internals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tts-playback ↔ ffplay child stdin | Bytes from voice-tts events$ cross into a child process pipe. EPIPE on a dead child is the documented failure mode. |
| stt-bridge ↔ voice-stt WSS (ElevenLabs Scribe v2) | Token-based auth; the bridge owns no API key (the key lives in the token mint closure). The webSocketCtor seam means production wires globalThis.WebSocket and tests wire a fake. |
| child-exit-watchdog ↔ subprocess exit stream | Listener on `child.on("exit")` — a misbehaving subprocess that exits then immediately respawns (e.g., a runaway loop) is bounded by the 3-in-10s cap. |
| stuck-thinking-watchdog ↔ timer scheduler | Timer-based; an attacker who could drive the timer arbitrarily could suppress the announcement, but the timer is process-local with no external trigger. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Denial of service | tts-playback consumer loop blocked by a hung ffplay.stdin.write | mitigate | The Promise.race against 5s timeout on each write prevents indefinite block; on timeout we treat as EPIPE-equivalent and emit playback_lost. |
| T-17-07 | Denial of service | child-exit-watchdog unbounded respawn (runaway sox) | mitigate | 3-in-10s sliding window cap; cap-exceeded emits error event + stops respawning. |
| T-17-08 | Information disclosure | stuck-thinking-watchdog logger leaks transcript | mitigate | Logger sees waitedMs only; SW7 test pins this. The verify command greps for any non-waitedMs field in the log call. |
| T-17-09 | Tampering | EPIPE in middle of TTS playback mis-attributed | mitigate | The classifier emits classification="playback_lost" only when EPIPE / ffplay-exited; voice-tts auth/rate-limit errors flow through the circuit breaker's existing classifyHttpError. |
| T-17-10 | Spoofing | A fake spawnImpl could inject any args to ffplay/sox | accept | The DI seam is the test seam; production callers always pass node:child_process.spawn. Out of scope unless a downstream consumer takes the seam from untrusted input. |
| T-17-SC | Tampering | npm package installs | mitigate | No NEW package-manager installs in this plan — all deps were added in Plan 01. Plan 02 imports only from workspace-resolved @achilles/* packages already on disk. |
</threat_model>

<verification>
- All 9 + 5 + SW1..SW7 unit tests pass under `vitest --pool=forks`
- Phase 16's 129 existing tests + Plan 01's 33 tests + this plan's ~21 new tests = ~183 total
- LOOP-02 invariant verified by `git diff --name-only` against the 5 LOOP-02 paths returning zero modified files
- No new top-level static imports in apps/achilles-terminal/src/cli.ts (the four new modules are imported by session.ts lazily inside runVoice)
- The FFPLAY_ARGS const + the locked stuck-thinking announcement string + the locked "Audio device lost — restart Achilles" string are all greppable in their respective files
</verification>

<success_criteria>
- TTS playback module wired against voice-tts events$ async iterator + ffplay stdin with backpressure + EPIPE handling + drain-emit symmetry
- STT bridge wired against voice-stt webSocketCtor seam + circuit-breaker mintToken wrap + SessionEvent emit on partial/committed
- Stuck-thinking watchdog ported with SW1..SW7 invariants preserved (60s default + locked announcement + waitedMs-only logger)
- Child-exit watchdog implements 3-in-10s sliding window respawn cap with cap-exceeded error surfacing
- LOOP-02 invariant respected: zero diff under packages/voice-*, packages/claude-code-bridge/, packages/achilles-skill/skill/prompts/companion.md
- All ~21 new unit tests pass + Phase 16's 129 existing tests + Plan 01's ~33 tests still pass
</success_criteria>

<output>
Create `.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-02-SUMMARY.md` when done documenting: the exact FFPLAY_ARGS tuple shipped, the resolved voice-tts/voice-stt package versions (workspace-relative), the child-exit-watchdog's locked thresholds (3 respawns / 10_000ms window), the stuck-thinking announcement byte length (should be 53 chars to match v1.2), the test counts per module, and a confirmation line listing the five LOOP-02 paths with `(unchanged)` after each.
</output>
</content>
</invoke>