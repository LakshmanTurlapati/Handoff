---
phase: 17
plan: 04
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
  - 17-03
files_modified:
  - apps/achilles-terminal/src/session.ts
  - apps/achilles-terminal/src/graceful-shutdown.ts
  - apps/achilles-terminal/src/resume-session.ts
  - apps/achilles-terminal/src/latency-probe.ts
  - apps/achilles-terminal/src/cli.ts
  - apps/achilles-terminal/tests/session.test.ts
  - apps/achilles-terminal/tests/graceful-shutdown.test.ts
  - apps/achilles-terminal/tests/resume-session.test.ts
  - apps/achilles-terminal/tests/latency-probe.test.ts
  - apps/achilles-terminal/tests/cli.test.ts
autonomous: true
requirements:
  - LOOP-01
  - LOOP-05
  - LOOP-06
must_haves:
  truths:
    - "runVoice composes mic-sox + VAD + stt-bridge + claude-bridge + tts-playback + state machine via constructor injection"
    - "Session emitter fans out the SessionEvent discriminated union to the existing Phase 16 useAchillesState consumer without UI regressions"
    - "gracefulShutdown(reason) is registered via process.once (not on) so a second SIGINT during cleanup escalates"
    - "gracefulShutdown completes the 7-step teardown (block transitions, voice-tts.close, ffplay.stdin.end + 200ms SIGTERM, claude-bridge.cancel, voice-stt.close, mic.stop, emit shutdown) in under 1.5 seconds"
    - "achilles voice --resume <sid> reads ~/.achilles/sessions/<sid>.json and hydrates the new session"
    - "achilles latency --report prints P50/P95 from ~/.achilles/latency/*.json (ERR-07 substrate — wiring is Phase 18)"
    - "INIT-07 invariant preserved: cli.ts top-level static imports remain { node:fs/promises, node:url, node:path }"
  artifacts:
    - path: "apps/achilles-terminal/src/session.ts"
      provides: "Composition root replacing the Phase 16 stub; ports v1.2 session.ts orchestration with IPC envelopes stripped and direct EventEmitter calls"
      contains: "runVoice"
      min_lines: 400
    - path: "apps/achilles-terminal/src/graceful-shutdown.ts"
      provides: "Registers process.once SIGINT + SIGTERM handlers; coordinates the 7-step teardown with a 1.5s budget"
      exports: ["registerGracefulShutdown", "gracefulShutdown", "GracefulShutdownHandle"]
    - path: "apps/achilles-terminal/src/resume-session.ts"
      provides: "--resume <sid> lock-file semantics + prior-session hydration"
      exports: ["createResumeSession", "ACHILLES_HOME", "SESSION_DIR", "LOCK_FILE"]
    - path: "apps/achilles-terminal/src/latency-probe.ts"
      provides: "LOOP-06 latency probe port from v1.2 + achilles latency --report subcommand reader"
      exports: ["createLatencyProbe", "renderLatencyReport"]
    - path: "apps/achilles-terminal/src/cli.ts"
      provides: "Extended voice subcommand (--resume, --debug) + new latency subcommand entry point; INIT-07 preserved"
      contains: "if (argv[0] === \"voice\")"
  key_links:
    - from: "apps/achilles-terminal/src/session.ts runVoice"
      to: "Plan 02 createTtsPlayback + createSttBridge + Plan 03 createClaudeBridge"
      via: "constructor injection of factories into the Session class; MOCK_LOOP=1 substitutes factory implementations"
      pattern: "createTtsPlayback|createSttBridge|createClaudeBridge"
    - from: "apps/achilles-terminal/src/graceful-shutdown.ts"
      to: "process.once SIGINT registration"
      via: "second-signal escalation flag"
      pattern: "process\\.once\\(\"SIGINT\"|process\\.once\\(\"SIGTERM\""
    - from: "apps/achilles-terminal/src/cli.ts"
      to: "INIT-07 invariant: static top-level imports limited to { fs/promises, url, path }"
      via: "tests/cli.test.ts T8 grep gate"
      pattern: "^import.*from \"node:(fs/promises|url|path)\""
---

<objective>
Wave 3 — the composition root that wires every Wave-2 module into a working voice loop. Plan 04 has THREE responsibilities, one per task:

  1. **Session.ts port + runVoice replacement (Task 1)** — replace the Phase 16 stub at apps/achilles-terminal/src/session.ts with the ported v1.2 orchestration. v1.2 session.ts is 1820 LOC; the port strips the renderer-IPC envelope wrappers (broadcast / IPC_*), replaces them with direct EventEmitter calls on the session-events discriminated union, and consumes the four DI factories (sttFactory, ttsFactory, claudeBridgeFactory, micFactory, vadFactory) from Wave 2. Expected port size: 600-900 LOC.

  2. **gracefulShutdown + LOOP-06 latency probe (Task 2)** — register process.once SIGINT + SIGTERM handlers; coordinate the 7-step teardown in under 1.5s; port v1.2 latency-probe.ts (588 LOC) with the LOOP-06 stage taxonomy intact.

  3. **resume-session + cli.ts flag extensions (Task 3)** — ~/.achilles/ directory + lock file + session JSON persistence + --resume <sid> hydration; extend cli.ts to add `voice --resume <sid> --debug` flags AND a new `latency --report` subcommand; INIT-07 invariant preserved.

Output: 5 new/modified source files + 5 new test files. ZERO modifications under packages/voice-*, packages/claude-code-bridge/, or packages/achilles-skill/skill/prompts/companion.md (LOOP-02 invariant).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-VALIDATION.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-01-PLAN.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-02-PLAN.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-03-PLAN.md
@.planning/research/PITFALLS.md
@apps/achilles/src/main/session.ts
@apps/achilles/src/main/latency-probe.ts
@apps/achilles-terminal/src/cli.ts
@apps/achilles-terminal/src/session.ts
@apps/achilles-terminal/src/state/state-machine.ts
@apps/achilles-terminal/src/state/constants.ts
@apps/achilles-terminal/src/audio/mic-sox.ts
@apps/achilles-terminal/src/audio/vad-energy.ts
@apps/achilles-terminal/src/ui/VoiceShell.tsx
@apps/achilles-terminal/src/ui/useAchillesState.ts
@apps/achilles-terminal/src/ui/plain-text.ts
@apps/achilles-terminal/src/session-events.ts
@apps/achilles-terminal/src/structured-logger.ts
@apps/achilles-terminal/src/circuit-breaker.ts
@apps/achilles-terminal/src/audio/companion-md.ts
@apps/achilles-terminal/src/audio/tts-playback.ts
@apps/achilles-terminal/src/audio/stt-bridge.ts
@apps/achilles-terminal/src/audio/claude-bridge.ts
@apps/achilles-terminal/src/stuck-thinking-watchdog.ts
@apps/achilles-terminal/src/child-exit-watchdog.ts
@apps/achilles-terminal/src/sandwich-defence.ts
@apps/achilles-terminal/src/normalisation.ts

<interfaces>
<!-- Key contracts. Extracted from the codebase. -->

From apps/achilles-terminal/src/session.ts (the Phase 16 stub being replaced):
- `class Session extends EventEmitter` — keep the class name + extends EventEmitter so Phase 16 useAchillesState consumers continue to work unchanged
- `SessionEvents` interface from session.ts (Phase 16) declares: state-change, transcript-partial, amplitude, rms-sample, error-message — Phase 17 EXTENDS this with the full SessionEvent discriminated union from session-events.ts (preserve the 5 Phase 16 channels for back-compat; ADD the new typed events)
- `runVoice(argv: string[]): Promise<void>` — entry point invoked via `await import("./session.js")` from cli.ts
- Phase 16's runVoice currently parses --mock / --debug-vad / --plain via commander and either renders the Ink tree or routes to plain-text mode; Phase 17 EXTENDS the same commander parser with --resume <sid> and --debug

From apps/achilles-terminal/src/ui/useAchillesState.ts (Phase 16):
- The hook subscribes to `session.on("state-change", ...)` and `session.on("amplitude", ...)` and `session.on("rms-sample", ...)` — Phase 17 must continue to emit on these channels alongside the new SessionEvent stream

From apps/achilles-terminal/src/state/state-machine.ts (Phase 16):
- `createSessionStateController({ broadcast, getMode }): MockStateController`
- Reducer events used by Phase 17 production callers: STT_COMMITTED { transcript }, CLAUDE_RESULT_READY, TTS_PLAYBACK_DRAINED, CLAUDE_FAILURE_OVERRIDE { reason }, INJECT_ERROR { kind }, MUTE_TOGGLE, CIRCLE_CLICK (cancel path)

From apps/achilles-terminal/src/state/constants.ts:
- `SPEAKING_DEBOUNCE_MS = 300` — half-duplex tail timer schedule after tts_drained

From apps/achilles/src/main/session.ts (port source 1820 LOC):
- The orchestrator function shape ports as `createSession(deps): AchillesSession`; Phase 17 KEEPS the v1.2 closure-state pattern but REPLACES the IPC fanout (deps.sendIpc + window.webContents.send) with the EventEmitter pattern
- v1.2 lifecycle: onHotkeyPress, requestSttToken, onUtteranceCommit, onMicFrame, onTtsPlaybackComplete, onCancel, dispose, handleTypedPrompt, announceStuckThinking, onSuspend, onResume, onDeviceChange
- v1.3 lifecycle simplifies: VAD speech_start replaces onHotkeyPress; VAD speech_end + STT commit replaces onUtteranceCommit; the rest stay
- v1.2 consumeClaudeEvents (lines 950-1145) MOVES into Plan 03's createClaudeBridge.consume() — Phase 17 session.ts orchestrates the three handles (sttBridge, ttsPlayback, claudeBridge) without re-implementing extractAck or extractSpokenSummary

From apps/achilles/src/main/latency-probe.ts (port source 588 LOC):
- 7-stage LOOP-06 metric: stt_committed, claude_first_text_delta, claude_assistant_done, tts_first_chunk, tts_playback_start, tts_playback_complete (+ speech_end anchor)
- `createLatencyProbe(opts: { debugEnabled, writeSampleFile, sampleFilePath?, samplesCap=100 }): LatencyProbe` with methods markSpeechEnd, recordStage, finalizeSample, snapshot
- The probe writes NDJSON to ~/.achilles/latency/ (Phase 18 owns the writer; Phase 17 ports the in-memory probe; the file write seam is in-scope but the JSON-write path is documented for Phase 18 to flesh out)

From CONTEXT.md `<decisions>` row "Ctrl-C cancel chain (LOOP-05)":
- process.once (not process.on) for SIGINT + SIGTERM
- 7-step sequence in gracefulShutdown(reason):
  1. Mark session as shutting down (block new state transitions)
  2. voice-tts.close() — closes TTS WSS
  3. ffplayChild.stdin.end() then 200ms timeout then ffplay.kill("SIGTERM")
  4. claude-code-bridge.cancel() — package's existing chain (SIGINT 1s SIGTERM 2s SIGKILL)
  5. voice-stt.close() — closes STT WSS
  6. mic-sox.stop() — kills sox child
  7. Wait for all to exit; emit shutdown event; resolve runVoice() promise
- Total budget: under 1.5s; verified by MOCK_LOOP=1 integration test with synthetic delays

From CONTEXT.md `<decisions>` row "Lock file + resume (LOOP-06)":
- Lock file: ~/.achilles/voice.lock containing PID + start time
- On startup: read lock file; if PID alive, error "another achilles voice instance is running (PID X); use --resume <sid> to attach"
- On gracefulShutdown: unlink lock file
- Sessions stored at ~/.achilles/sessions/<sid>.json: state, last partial transcript, latency P50/P95
- --resume <sid> reads the session file and starts a new session with prior state hydrated
- Phase 17 IMPLEMENTS the file mechanics; Phase 18 adds the interactive picker UX + lock-conflict resolution UI

From CONTEXT.md `<specifics>` row 5:
- `~/.achilles/` directory creation is idempotent and lazy: `fs.mkdirSync(recursive: true)`; never errors if it already exists

From apps/achilles-terminal/src/cli.ts (Phase 16 baseline):
- Top-level static imports: { readFile from "node:fs/promises", fileURLToPath from "node:url", dirname/join from "node:path" } — INIT-07 LOCKED
- main() argv branches: --version/-v → read package.json + write to stdout; --latency-probe → cold-start measurement; first arg "voice" → dynamic import session.js then call runVoice(argv.slice(1))
- Phase 17 EXTENDS the voice branch by passing additional argv to the existing runVoice; ADDS a new "latency" first-arg branch for `achilles latency --report` via dynamic import of latency-probe.js
- INIT-07 verified by tests/cli.test.ts T8 (Phase 16 — grep gate on `^import` lines)

From PITFALLS.md §1 (silent-launch defence):
- Phase 17 must register top-level uncaughtException + unhandledRejection handlers (already in Phase 16 cli.ts lines 97-106 — preserve)
- gracefulShutdown's last act before process.exit is an unconditional structured-logger.flush() so the final state is on disk even after SIGKILL of the children

From PITFALLS.md §10:
- mic-sox / ffplay children SHARE our process group (we do NOT spawn them detached); Ctrl-C in the user's terminal reaches them as a fast-path
- claude child is DETACHED into its own group (handled in Plan 03's claude-bridge.ts)
- gracefulShutdown sends SIGTERM to mic-sox / ffplay as a no-op confirmation (they may already be dead from the terminal SIGINT)
- Register `process.once("exit", ...)` last-chance cleanup to unlink the lock file synchronously (unlinkSync) — async unlink not guaranteed to run before exit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session.ts port + runVoice replacement (LOOP-01 wiring)</name>
  <files>apps/achilles-terminal/src/session.ts, apps/achilles-terminal/tests/session.test.ts</files>
  <read_first>
    - apps/achilles/src/main/session.ts FULL FILE (1820 LOC) — the port target; read end-to-end before writing; identify the IPC fanout sites (deps.sendIpc, IPC_* constants) and strip them
    - apps/achilles/src/main/session.test.ts (2528 LOC) — the v1.2 test surface; mirror the SE1-SE28 cases adapted to the EventEmitter shape
    - apps/achilles-terminal/src/session.ts (Phase 16 stub — currently 422 LOC); preserve the SessionEvents back-compat channels (state-change, amplitude, rms-sample) while adding the new SessionEvent fanout
    - apps/achilles-terminal/src/state/state-machine.ts (Phase 16) for the createSessionStateController surface
    - apps/achilles-terminal/src/audio/mic-sox.ts (Phase 16) for the createMicSox handle shape
    - apps/achilles-terminal/src/audio/vad-energy.ts (Phase 16) for the VadHandle surface
    - apps/achilles-terminal/src/ui/VoiceShell.tsx (Phase 16) for the props interface the Ink tree expects
    - apps/achilles-terminal/src/ui/useAchillesState.ts (Phase 16) for the hook's subscription channels
    - All Wave-2 outputs: src/audio/tts-playback.ts (Plan 02 Task 1), src/audio/stt-bridge.ts (Plan 02 Task 1), src/audio/claude-bridge.ts (Plan 03 Task 2), src/stuck-thinking-watchdog.ts (Plan 02 Task 2), src/child-exit-watchdog.ts (Plan 02 Task 2)
    - .planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md `<decisions>` row "session.ts port shape" + "Sandwich-wrap pattern" + "ffplay TTS playback" + "Circuit breaker"
  </read_first>
  <action>
Replace apps/achilles-terminal/src/session.ts with a Phase 17 implementation. PRESERVE: the exported `class Session extends EventEmitter`, the `SessionEvents` Phase 16 interface (state-change, transcript-partial, amplitude, rms-sample, error-message), the `SessionOptions` shape extended with new fields, the `createSession(opts)` factory, the `runVoice(argv)` async function entry point. REPLACE: the stub orchestration with the real wiring.

New imports added to session.ts (NOT to cli.ts — cli.ts top-level imports stay locked at fs/promises + url + path):
  - From "@achilles/voice-stt": createRealtimeSttClient (the production sttFactory default)
  - From "@achilles/voice-tts": createTtsStreamClient (the production ttsFactory default)
  - From "@achilles/claude-code-bridge": createClaudeSession (production createSession default for claude-bridge)
  - From "./audio/tts-playback.js": createTtsPlayback, TtsPlaybackHandle
  - From "./audio/stt-bridge.js": createSttBridge, SttBridgeHandle
  - From "./audio/claude-bridge.js": createClaudeBridge, ClaudeBridgeHandle, FAILURE_OVERRIDE_PHRASE
  - From "./audio/companion-md.js": resolveCompanionPromptPath
  - From "./circuit-breaker.js": createCircuitBreaker
  - From "./structured-logger.js": createStructuredLogger
  - From "./stuck-thinking-watchdog.js": createStuckThinkingWatchdog, STUCK_THINKING_ANNOUNCEMENT
  - From "./child-exit-watchdog.js": createChildExitWatchdog
  - From "./session-events.js": SessionEvent
  - From "./state/constants.js": SPEAKING_DEBOUNCE_MS (already imported in Phase 16 stub)

Extend SessionOptions with: sttFactory? (defaults to createRealtimeSttClient — the unknown placeholder Phase 16 had gets typed as `import("@achilles/voice-stt").CreateRealtimeSttClientOptions`-shape factory), ttsFactory? (defaults to createTtsStreamClient), claudeBridgeFactory? (defaults to the Plan 03 createClaudeBridge factory which itself wraps createClaudeSession), apiKey? (the ELEVENLABS_API_KEY at session boot; production reads process.env.ELEVENLABS_API_KEY at runVoice level; tests inject), voiceId? (default reads process.env.ELEVENLABS_VOICE_ID with a v1.2 fallback string; verify v1.2 default from apps/achilles/src/main/index.ts), resume? (the optional --resume sid hydrating prior state), debug? (the --debug flag enabling latency-probe + debug logger), mockLoop? (the MOCK_LOOP=1 boolean read by Plan 05's integration test).

Inside the Session class, add per-instance fields: `sttBridge: SttBridgeHandle | null`, `ttsPlayback: TtsPlaybackHandle | null`, `claudeBridge: ClaudeBridgeHandle | null`, `sttCircuit: CircuitBreaker`, `ttsCircuit: CircuitBreaker`, `logger: StructuredLogger`, `latencyProbe: LatencyProbe | null` (from Plan 04 Task 2), `stuckWatchdog: StuckThinkingWatchdog`, `micWatchdog: ChildExitWatchdog | null`, `ffplayWatchdog: ChildExitWatchdog | null`, `shuttingDown: boolean = false`, `currentSpeakingTailToken: NodeJS.Timeout | null`.

The constructor wires: structured-logger (always-on per CONTEXT.md "Structured logger" row), sttCircuit + ttsCircuit (label "stt" / "tts" with CONTEXT.md-locked thresholds), stuck-thinking-watchdog (onTimeout → emits SessionEvent { type:"claude_partial", payload:{text: STUCK_THINKING_ANNOUNCEMENT} } AND routes the announcement through ttsPlayback.appendText after normaliseForTts).

Replace handleMockFrame + handlePcmFrame so VAD speech_start dispatches HOTKEY_PRESS (idle → listening) AND lazily starts sttBridge.start() (mints token, opens WSS). VAD speech_end dispatches STT_COMMITTED { transcript: "" } as the timing anchor AND calls sttBridge.commit() so the package's commit() forces Scribe to flush. The real STT committed event from sttBridge.events$ → emit on session as `stt_committed` SessionEvent + drive claudeBridge.send(rawTranscript) (rawTranscript NOT pre-wrapped here — the wrap happens inside claude-bridge.ts per Plan 03).

Drive claudeBridge.consume() concurrently; observe the SessionEvents it emits via the shared emit-callback deps.emit. On claude_ack: dispatch CLAUDE_RESULT_READY (processing → speaking); call ttsPlayback.appendText(payload.text). On claude_summary: ttsPlayback.appendText(payload.text). On claude_done with outcome.kind === "success": no state transition needed (ttsPlayback's tts_drained schedules the speaking-tail debounce). On claude_failed: emit error event with classification:"claude_failed"; the SPEAKING_DEBOUNCE_MS path still runs.

On tts_drained from ttsPlayback: schedule `setTimeout(() => { dispatch({type:"TTS_PLAYBACK_DRAINED"}); resetTurnLocals; }, SPEAKING_DEBOUNCE_MS)` — this is PLAY-02's half-duplex tail.

Mic gating: when state ∈ {processing, speaking}, drop frames at the mic source (the existing handlePcmFrame / handleMockFrame already short-circuits via the Phase 16 VAD self-trigger guard; the orchestrator additionally tracks `framesDroppedDuringSpeaking` + `framesDroppedDuringProcessing` metrics per the v1.2 split — port the metrics block from v1.2 session.ts lines 222-241).

Replace runVoice(argv) — keep the existing commander setup (--mock, --debug-vad, --plain) and ADD: `.option("--resume <sid>", "resume prior session by sid")` and `.option("--debug", "enable verbose latency-probe + line-trace logging")`. Inside the action: read process.env.ELEVENLABS_API_KEY (no key required when --mock is set — guard the missing-key error path); resolve companionPromptPath via resolveCompanionPromptPath(); construct Session with all factories; call session.start(); register gracefulShutdown via the Plan 04 Task 2 helper (NOT Phase 16's plain process.once SIGINT). The plain-text path and Ink path branches continue to work as in Phase 16.

If --resume <sid> is provided: dynamically import resume-session.js (Plan 04 Task 3) and call hydrateSession({sid}); pass the hydrated state to the Session constructor.

Create apps/achilles-terminal/tests/session.test.ts mirroring the v1.2 test surface adapted to the EventEmitter pattern. Test the 10 critical invariants from v1.2's SE1-SE28 (focus on the ones still applicable post-IPC-strip):
  (1) "VAD speech_start drives idle → listening + lazily starts sttBridge"
  (2) "VAD speech_end + sttBridge committed event drives listening → processing + calls claudeBridge.send with sandwich-wrapped transcript"
  (3) "claude_ack from claudeBridge drives processing → speaking + appendText to ttsPlayback"
  (4) "claude_failed drives processing → speaking via CLAUDE_FAILURE_OVERRIDE + plays FAILURE_OVERRIDE_PHRASE-prefixed summary"
  (5) "tts_drained → SPEAKING_DEBOUNCE_MS timer → TTS_PLAYBACK_DRAINED → state=idle"
  (6) "mic frames dropped during speaking — framesDroppedDuringSpeaking increments"
  (7) "mic frames dropped during processing — framesDroppedDuringProcessing increments"
  (8) "MUTE_TOGGLE flows through to VAD setMuted (Phase 16 invariant preserved)"
  (9) "INIT-07 invariant: tests/cli.test.ts T8 still passes (no new top-level static imports added to cli.ts)"
  (10) "MOCK_LOOP=1 path: factories accept injected mocks so production code never hits real WSS / spawn"

NO emojis. NO modification of LOOP-02 paths.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npm run typecheck --workspace apps/achilles-terminal &amp;&amp; npm test --workspace apps/achilles-terminal -- tests/session.test.ts tests/cli.test.ts &amp;&amp; grep -cE 'createSttBridge|createTtsPlayback|createClaudeBridge' apps/achilles-terminal/src/session.ts | grep -E '^[3-9]|^[1-9][0-9]+' &amp;&amp; bash -c "imports=\$(grep -E '^import' apps/achilles-terminal/src/cli.ts | grep -vE 'node:(fs/promises|url|path)' | wc -l); test \$imports -eq 0" &amp;&amp; bash -c "diff_count=\$(git diff --name-only -- 'packages/voice-protocol' 'packages/voice-stt' 'packages/voice-tts' 'packages/claude-code-bridge' 'packages/achilles-skill/skill/prompts/companion.md' | grep -v '^$' | wc -l); test \$diff_count -eq 0"</automated>
  </verify>
  <acceptance_criteria>
    - session.ts is between 600 and 900 LOC (CONTEXT.md scope target); the Phase 16 stub is replaced not deleted
    - SessionEvents Phase 16 channels (state-change, amplitude, rms-sample, error-message, transcript-partial) continue to fire so Phase 16 useAchillesState consumers + Phase 16 tests pass unchanged
    - session.ts imports createSttBridge, createTtsPlayback, createClaudeBridge from Wave-2 modules
    - runVoice composes the full chain: mic-sox → VAD → stt-bridge → claude-bridge → tts-playback with state machine driving transitions
    - tests/session.test.ts asserts the 10 invariants pass
    - cli.ts top-level static imports remain exactly { node:fs/promises, node:url, node:path } (INIT-07 verified by tests/cli.test.ts T8)
    - LOOP-02: zero diff under the five protected paths
  </acceptance_criteria>
  <done>The orchestrator wiring exists end-to-end. Plan 05's MOCK_LOOP integration test can exercise this surface by injecting fakes through the SessionOptions factory seams.</done>
</task>

<task type="auto">
  <name>Task 2: gracefulShutdown (LOOP-05) + latency-probe port (LOOP-06 substrate)</name>
  <files>apps/achilles-terminal/src/graceful-shutdown.ts, apps/achilles-terminal/src/latency-probe.ts, apps/achilles-terminal/tests/graceful-shutdown.test.ts, apps/achilles-terminal/tests/latency-probe.test.ts</files>
  <read_first>
    - apps/achilles/src/main/latency-probe.ts FULL FILE (588 LOC) — the port target; the 7-stage LOOP-06 taxonomy + samplesCap=100 FIFO + P50/P95 readers port unchanged
    - apps/achilles/src/main/latency-probe.test.ts (673 LOC) for the test surface
    - .planning/research/PITFALLS.md §10 (SIGINT propagation) — the 7-step gracefulShutdown sequence + process.once invariant
    - .planning/research/PITFALLS.md §1 + §9 — uncaughtException last-chance logger flush + final lock-file unlinkSync
    - .planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md `<decisions>` row "Ctrl-C cancel chain (LOOP-05)" — the 7-step sequence + 1.5s total budget + process.once registration
    - All Wave-2 handle surfaces: sttBridge.stop(), ttsPlayback.cancel(), claudeBridge.cancel(), micSox.stop() — needed to drive the teardown ordering
    - apps/achilles-terminal/src/session.ts (Plan 04 Task 1) — the Session class fields graceful-shutdown.ts needs to read
  </read_first>
  <action>
Create apps/achilles-terminal/src/graceful-shutdown.ts. Export `registerGracefulShutdown(deps: { session: Session; logger: StructuredLogger; lockFilePath: string; onShutdownComplete?: () => void; setTimeoutImpl?: typeof setTimeout; clearTimeoutImpl?: typeof clearTimeout; processOverride?: { once: typeof process.once; exit: typeof process.exit; kill: typeof process.kill } }): GracefulShutdownHandle`. The handle exposes a single `gracefulShutdown(reason: "sigint" | "sigterm" | "internal_error" | "dispose"): Promise<void>` async function.

Inside registerGracefulShutdown:
  - Register `process.once("SIGINT", () => { void gracefulShutdown("sigint") })` and `process.once("SIGTERM", () => { void gracefulShutdown("sigterm") })`. NEVER process.on — second-signal escalation depends on .once semantics.
  - Track `shuttingDown: boolean` and `forceful: boolean` flags. First signal sets shuttingDown=true and runs the teardown. Second signal (received during teardown via a separate process.on installed AFTER the first .once fires) flips forceful=true; the teardown's race against the 1.5s budget detects the flag and calls process.exit(130) immediately.
  - Register `process.once("exit", () => { unlinkSync(lockFilePath) })` as the synchronous last-chance lock cleanup (Pitfall 10).

The 7-step gracefulShutdown body:
  1. `if (shuttingDown) return; shuttingDown = true; session.shuttingDown = true; logger.info("graceful_shutdown_start", { reason })`
  2. `await Promise.race([session.ttsPlayback?.cancel() ?? Promise.resolve(), timeout(300)])` — voice-tts close + ffplay drain (PLAY playback) per the handle's own internal SIGTERM-after-200ms logic
  3. step 2 includes the ffplay stdin.end + 200ms SIGTERM (handled inside ttsPlayback.cancel per Plan 02)
  4. `await Promise.race([session.claudeBridge?.cancel() ?? Promise.resolve(), timeout(700)])` — the package's existing chain SIGINT 100ms SIGTERM 200ms SIGKILL (note: Phase 17 telescopes this from the 1s+2s default to 100ms+200ms in the cancel-on-shutdown path; verify the cancel() return value is observed within the bridge's existing surface; if the bridge doesn't expose telescoping, the gracefulShutdown's outer Promise.race against 700ms guarantees the budget)
  5. `await Promise.race([session.sttBridge?.stop() ?? Promise.resolve(), timeout(200)])` — STT WSS close 1000
  6. `await Promise.race([session.micSox?.stop() ?? Promise.resolve(), timeout(200)])` — sox SIGTERM
  7. `session.emit("shutdown" as any, { type: "shutdown", payload: { reason }, timestamp: Date.now() }); logger.info("graceful_shutdown_complete", { reason, durationMs: ... }); await logger.flush(); onShutdownComplete?.(); process.exit(reason === "internal_error" ? 1 : 0)`

The outer envelope wraps step 1-7 in `await Promise.race([allSteps, timeout(1500)])`; on outer timeout, log "graceful_shutdown_budget_exceeded" + `process.exit(130)` immediately.

Create apps/achilles-terminal/src/latency-probe.ts as a port of apps/achilles/src/main/latency-probe.ts. The 7-stage LOOP-06 taxonomy (stt_committed, claude_first_text_delta, claude_assistant_done, tts_first_chunk, tts_playback_start, tts_playback_complete + speech_end anchor) ports verbatim. Public surface: `createLatencyProbe({ debugEnabled, writeSampleFile?, sampleFilePath?, samplesCap=100, nowImpl?, logger? }): LatencyProbe`. The probe records timestamps per stage; markSpeechEnd anchors the sample at the user-perceived "speech end" tick; finalizeSample on first audible TTS byte (the LOOP-06 metric anchor) computes durations + appends to a 100-slot FIFO + (when writeSampleFile=true) writes NDJSON to sampleFilePath. Phase 18 owns the report-subcommand UX; Phase 17 exports `renderLatencyReport(samplePath?: string): Promise<string>` which reads the latency JSON files under ~/.achilles/latency/, computes P50/P95 across all samples, returns a formatted string with stage-by-stage stats. The CLI invokes this from the new `latency --report` branch in cli.ts (Task 3).

Create apps/achilles-terminal/tests/graceful-shutdown.test.ts. Tests: (1) "process.once SIGINT registered (not process.on)" — pass a recording processOverride.once + processOverride.on (the latter should NOT be called for SIGINT); assert once was called twice (SIGINT + SIGTERM). (2) "7-step teardown completes under 1.5s with synthetic 50ms delays per step" — pass mock session with handles whose cancel/stop return Promise resolving after 50ms; advance fake timers; assert all 4 handle methods were called in the documented order and the total budget consumed < 1.5s. (3) "second SIGINT during teardown flips forceful and calls process.exit(130)" — pass a session whose ttsPlayback.cancel() never resolves; advance to step 2; trigger a second SIGINT synthetically (call the registered handler directly); assert processOverride.exit was called with 130 within 100ms of the second signal. (4) "outer budget timeout at 1.5s forces process.exit(130)" — pass a session whose every handle hangs forever; advance fake timers by 1500ms; assert processOverride.exit was called with 130. (5) "process.once('exit') synchronously unlinks the lock file" — register; invoke the captured exit handler; assert unlinkSync was called with lockFilePath. (6) "logger.flush called before process.exit so the final state is on disk" — assert flush is in the call sequence before exit.

Create apps/achilles-terminal/tests/latency-probe.test.ts mirroring the v1.2 LP1-LPN test surface (cap to the most relevant 10 cases — stage-recording sequence, samplesCap FIFO eviction, P50/P95 math against fixture data, renderLatencyReport format).

NO emojis. NO modification of LOOP-02 paths.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npm run typecheck --workspace apps/achilles-terminal &amp;&amp; npm test --workspace apps/achilles-terminal -- tests/graceful-shutdown.test.ts tests/latency-probe.test.ts &amp;&amp; grep -cE 'process\.once\("SIGINT"' apps/achilles-terminal/src/graceful-shutdown.ts | grep -E '^[1-9]' &amp;&amp; grep -cE 'process\.once\("SIGTERM"' apps/achilles-terminal/src/graceful-shutdown.ts | grep -E '^[1-9]' &amp;&amp; bash -c "grep -cE 'process\.on\(\"SIGINT' apps/achilles-terminal/src/graceful-shutdown.ts | grep -E '^[[:space:]]*0[[:space:]]*$'" &amp;&amp; grep -cE 'unlinkSync' apps/achilles-terminal/src/graceful-shutdown.ts | grep -E '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - graceful-shutdown.ts registers process.once for SIGINT + SIGTERM; never process.on for shutdown
    - 7-step teardown sequence in the documented order with 1.5s outer budget
    - Second-SIGINT-during-teardown path calls process.exit(130) immediately
    - process.once("exit") synchronously unlinkSyncs the lock file
    - logger.flush in the teardown sequence before process.exit
    - latency-probe.ts ports the v1.2 7-stage taxonomy + samplesCap FIFO + P50/P95 + renderLatencyReport
    - 6 graceful-shutdown tests + ~10 latency-probe tests pass
    - LOOP-02: zero diff under the five protected paths
  </acceptance_criteria>
  <done>LOOP-05 (cancellation chain under 1.5s) and LOOP-06 (latency-probe substrate) are on disk + tested. Plan 05 integration test exercises gracefulShutdown end-to-end through the MOCK_LOOP harness.</done>
</task>

<task type="auto">
  <name>Task 3: Resume session + lock file + cli.ts flag extensions (LOOP-06 + INIT-07 preservation)</name>
  <files>apps/achilles-terminal/src/resume-session.ts, apps/achilles-terminal/src/cli.ts, apps/achilles-terminal/tests/resume-session.test.ts, apps/achilles-terminal/tests/cli.test.ts</files>
  <read_first>
    - .planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md `<decisions>` row "Lock file + resume (LOOP-06)" — the full lifecycle contract
    - apps/achilles-terminal/src/cli.ts (Phase 16 baseline) — the existing argv branches + the INIT-07 locked static imports
    - apps/achilles-terminal/tests/cli.test.ts (Phase 16 baseline) for the existing INIT-07 T8 grep gate + the --version + --latency-probe cases
    - .planning/research/PITFALLS.md §1 (cli.ts top-level handlers must remain — uncaughtException + unhandledRejection)
    - .planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md `<specifics>` row 5 (~/.achilles/ idempotent mkdirSync)
  </read_first>
  <action>
Create apps/achilles-terminal/src/resume-session.ts. Export const `ACHILLES_HOME = path.join(os.homedir(), ".achilles")`, `SESSION_DIR = path.join(ACHILLES_HOME, "sessions")`, `LOCK_FILE = path.join(ACHILLES_HOME, "voice.lock")`. Export `createResumeSession(deps?: { homeDir?: string; nowImpl?: () => number; logger?: StructuredLogger }): ResumeSessionHandle`. The handle exposes:
  - `ensureHome(): void` — `fs.mkdirSync(homeDir, { recursive: true, mode: 0o700 })` and same for SESSION_DIR (idempotent; never throws if dir exists)
  - `acquireLock(): { ok: true } | { ok: false; runningPid: number }` — reads LOCK_FILE; if missing or stale (kill(-0, pid) throws ESRCH), writes the new lock with current pid + start_time + mode 0o600; if a live PID is found, returns { ok: false, runningPid }
  - `releaseLock(): void` — `unlinkSync(LOCK_FILE)`; idempotent (ignores ENOENT)
  - `persistSessionState(sid: string, state: { lastTranscript?: string; latencyP50?: number; latencyP95?: number; status: "active"|"ended" }): void` — writes ~/.achilles/sessions/<sid>.json with the session payload (mode 0o600)
  - `hydrateSession(sid: string): SessionState | null` — reads ~/.achilles/sessions/<sid>.json; returns parsed state or null if file missing
  - `listSessions(): SessionSummary[]` — returns metadata for every JSON file in SESSION_DIR sorted by start_time descending (Phase 18 uses this for the picker UI; Phase 17 just exports the function)

Extend apps/achilles-terminal/src/cli.ts to add a new first-arg branch BEFORE the existing `if (argv[0] === "voice")` branch. The new branch handles `latency` first-arg: `if (argv[0] === "latency") { const { renderLatencyReport } = await import("./latency-probe.js"); const sub = argv[1]; if (sub === "--report" || sub === "report") { const report = await renderLatencyReport(); process.stdout.write(report + "\\n", () => process.exit(0)); return; } process.stderr.write("achilles latency: unknown subcommand. Try --report.\\n"); process.exit(1); return; }`. The existing voice branch stays unchanged at the source level — runVoice itself is what parses the new --resume and --debug flags (handled in Task 1's commander extension). Do NOT add any new top-level static import to cli.ts.

Update apps/achilles-terminal/tests/cli.test.ts to ADD: T9 "latency --report invokes renderLatencyReport via dynamic import" — spawn the CLI via tsx or node with argv ["latency", "--report"]; assert stdout contains the report header (e.g., "P50:" or "samples="); T10 "voice --resume <sid> propagates to runVoice" — spawn with argv ["voice", "--mock", "--resume", "test-sid"]; assert process exit code is 0 (assuming --mock allows missing API key; verify Phase 16 baseline). The existing T8 INIT-07 grep gate continues to pass — verify by inspecting the test source after the edit.

Create apps/achilles-terminal/tests/resume-session.test.ts. Tests: (1) "ensureHome creates ~/.achilles/ + sessions/ with 0o700" — use tmpdir as homeDir; assert directories exist with mode 0o700 (POSIX only — skip on Windows via test.skipIf). (2) "acquireLock writes PID + start_time" — assert file content parses as JSON with { pid, startTime }. (3) "acquireLock detects live PID and returns { ok: false }" — pre-write a lock with the current process's PID; assert subsequent acquireLock returns { ok: false, runningPid: process.pid }. (4) "acquireLock ignores stale PID via kill(-0)=ESRCH" — pre-write a lock with PID 99999 (unlikely to exist); mock kill to throw ESRCH; assert acquireLock returns { ok: true } and the lock is overwritten. (5) "releaseLock unlinks idempotently" — call twice; second call does not throw. (6) "persistSessionState writes JSON to sessions/<sid>.json with mode 0o600" — assert file exists with the right shape. (7) "hydrateSession returns null for missing sid". (8) "hydrateSession parses an existing session". (9) "listSessions returns sorted summaries".

NO emojis. NO modification of LOOP-02 paths. INIT-07: cli.ts top-level static imports MUST remain exactly the three node: imports.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npm run typecheck --workspace apps/achilles-terminal &amp;&amp; npm test --workspace apps/achilles-terminal -- tests/resume-session.test.ts tests/cli.test.ts &amp;&amp; bash -c "imports=\$(grep -E '^import' apps/achilles-terminal/src/cli.ts | grep -vE 'node:(fs/promises|url|path)' | wc -l); test \$imports -eq 0" &amp;&amp; grep -cE 'achilles-terminal/src/cli\.ts' apps/achilles-terminal/tests/cli.test.ts | grep -E '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - resume-session.ts exports ensureHome, acquireLock, releaseLock, persistSessionState, hydrateSession, listSessions
    - Lock file semantics: live-PID detection via kill -0; stale-PID overwrite; idempotent unlink
    - cli.ts adds `latency` first-arg branch via dynamic import (no static top-level import)
    - cli.ts top-level static imports remain exactly { node:fs/promises, node:url, node:path } — INIT-07 verified
    - tests/cli.test.ts T8 (INIT-07 grep gate) continues to pass
    - 9 resume-session tests + 2 new cli tests pass
    - LOOP-02: zero diff under the five protected paths
  </acceptance_criteria>
  <done>LOOP-06 lock file + resume substrate exists; CLI flag extensions (--resume, --debug, latency --report) wired without regressing INIT-07. Plan 05 integration test can exercise the lock + session-state round trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session in-process EventEmitter | Producer and consumer share memory; no IPC; the boundary is type-only (SessionEvent discriminated union). |
| graceful-shutdown ↔ process signals | The handler is the only path that should reach process.exit. A second SIGINT during cleanup goes to the .once-fallback installed during teardown, not back to gracefulShutdown. |
| resume-session ↔ filesystem (~/.achilles/) | Mode 0o700 on the directory and 0o600 on the files. Other users on a multi-user system cannot read the lock file or session JSON. |
| cli.ts ↔ runVoice dynamic import gate | INIT-07 invariant; --version path never loads session.js or any of its transitive imports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-16 | Denial of service | gracefulShutdown hangs indefinitely | mitigate | Outer 1.5s budget Promise.race; on timeout process.exit(130). Per-step inner timeouts (300ms TTS, 700ms claude, 200ms STT, 200ms mic) sum to budget. |
| T-17-17 | Tampering | A stale lock file blocks legitimate restart | mitigate | acquireLock checks kill(-0, pid); ESRCH means stale; overwrite is safe. |
| T-17-18 | Information disclosure | Session JSON contains transcript text readable by other users | mitigate | Mode 0o600 on every file write under ~/.achilles/. Phase 18 will add the opt-in --save-transcripts gate; Phase 17's session JSON carries only state metadata (status, latency P50/P95, lastTranscript preview capped at 60 chars). |
| T-17-19 | Elevation of privilege | A second SIGINT delivered after the first .once handler removed itself could leak to default Node behavior (immediate exit, no cleanup) | mitigate | After the first .once fires, gracefulShutdown installs a process.on("SIGINT") handler that flips forceful=true; the outer Promise.race observes the flag and exits clean. |
| T-17-20 | Repudiation | A crash without graceful shutdown leaves the lock file orphaned and the user cannot restart | mitigate | process.once("exit") synchronously unlinkSyncs the lock file. Defense-in-depth: acquireLock's stale-PID detection covers the case where even this fallback failed. |
| T-17-21 | Information disclosure | logger.flush writes the final structured log line which may carry classification kinds | accept | The structured-logger's default redaction patterns from Plan 01 Task 2 cover the documented secret shapes; reason/classification fields are NOT secrets. |
| T-17-SC | Tampering | npm package installs | mitigate | No NEW package-manager installs in Plan 04 — all workspace deps were added in Plan 01. |
</threat_model>

<verification>
- LOOP-02 invariant: zero diff under packages/voice-*, packages/claude-code-bridge/, packages/achilles-skill/skill/prompts/companion.md
- INIT-07 invariant: cli.ts top-level static imports remain exactly the three node: imports (verified by the existing tests/cli.test.ts T8 grep gate)
- LOOP-05: process.once is the registration mechanism; process.on count for shutdown signals is 0 (verified by grep)
- LOOP-06: lock file lifecycle covers acquire / release / stale-PID / persist / hydrate
- Phase 16's existing 129 tests + Plan 01's ~33 + Plan 02's ~21 + Plan 03's ~22 + Plan 04's ~37 = ~242 total tests passing under vitest --pool=forks
- gracefulShutdown 1.5s budget verified by the synthetic-delay test in graceful-shutdown.test.ts
</verification>

<success_criteria>
- session.ts wires the full LOOP-01 chain (mic-sox → VAD → stt-bridge → claude-bridge → tts-playback → state machine) via constructor injection
- graceful-shutdown.ts registers process.once handlers for the 7-step under-1.5s teardown
- resume-session.ts implements lock file + session state persistence with POSIX mode bits
- cli.ts adds latency subcommand dynamic-import branch; INIT-07 preserved (top-level imports unchanged)
- latency-probe.ts ports the v1.2 LOOP-06 7-stage taxonomy + renderLatencyReport
- All Phase 16 + Plan 01-03 tests still pass; Plan 04 adds ~37 new tests
</success_criteria>

<output>
Create `.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-04-SUMMARY.md` when done documenting: the final session.ts LOC count, the 7-step gracefulShutdown sequence with the exact per-step inner timeouts shipped, the LOOP-06 stage names ported, the ~/.achilles/ directory layout, the final cli.ts top-level static import list, the test count delta, and a confirmation line listing the five LOOP-02 paths with `(unchanged)` after each.
</output>
</content>
</invoke>