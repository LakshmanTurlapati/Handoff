---
phase: 17
plan: 05
type: execute
wave: 4
depends_on:
  - 17-04
files_modified:
  - apps/achilles-terminal/tests/integration/mock-loop.test.ts
  - apps/achilles-terminal/tests/integration/fixtures/mock-clients.ts
  - .github/workflows/achilles-terminal-ci.yml
autonomous: true
requirements:
  - LOOP-01
  - LOOP-02
must_haves:
  truths:
    - "MOCK_LOOP=1 integration test exists at apps/achilles-terminal/tests/integration/mock-loop.test.ts and exercises the full state machine cycle under 2s wall-clock"
    - "The test mocks Scribe v2 WSS, claude subprocess (stream-json synth), ElevenLabs WSS (MP3 frame synth), sox (mock-amplitude), ffplay (stdin consumer that exits on stdin.end)"
    - "On test completion, asserts no orphaned children (sox / ffplay / claude) remain in the process table"
    - "The CI workflow .github/workflows/achilles-terminal-ci.yml runs MOCK_LOOP=1 as a new step in the existing dual-runtime matrix"
    - "CI also enforces the LOOP-02 invariant via a git diff gate against the five protected paths"
  artifacts:
    - path: "apps/achilles-terminal/tests/integration/mock-loop.test.ts"
      provides: "End-to-end MOCK_LOOP=1 integration test asserting idle → listening → processing → speaking → idle within 2s; the upstream CI smoke gate for the v1.2 silent-launch defence"
      min_lines: 200
    - path: "apps/achilles-terminal/tests/integration/fixtures/mock-clients.ts"
      provides: "Reusable mock factories for the four DI seams (sttFactory, ttsFactory, claudeBridgeFactory, spawnImpl)"
      exports: ["createMockSttFactory", "createMockTtsFactory", "createMockClaudeFactory", "createMockSpawnImpl"]
    - path: ".github/workflows/achilles-terminal-ci.yml"
      provides: "CI gate runs MOCK_LOOP=1 vitest + LOOP-02 diff check on every PR"
      contains: "MOCK_LOOP=1"
  key_links:
    - from: "mock-loop.test.ts"
      to: "Plan 04 session.ts runVoice"
      via: "imported via @achilles/achilles-terminal-internal path or relative ../../src/session.js; SessionOptions factory seams swapped with mock-clients.ts fixtures"
      pattern: "createMockSttFactory|createMockTtsFactory|createMockClaudeFactory|MOCK_LOOP"
    - from: ".github/workflows/achilles-terminal-ci.yml"
      to: "LOOP-02 invariant"
      via: "new step running git diff --name-only against the five protected paths"
      pattern: "voice-protocol|voice-stt|voice-tts|claude-code-bridge|companion\\.md"
---

<objective>
Wave 4 — install the structural defence against v1.2's silent-launch failure shape. Two deliverables:

  1. **MOCK_LOOP=1 integration test (Task 1)** — a vitest integration test under apps/achilles-terminal/tests/integration/ that exercises Plan 04's runVoice with all four DI factories swapped for in-process mocks. The test drives the full state machine cycle (idle → listening → processing → speaking → idle), asserts the ack region was extracted, the spoken-summary was extracted, MP3 frames reached the mock ffplay stdin, and no orphaned children remain at test end. Total wall-clock under 2 seconds; the test is the upstream CI gate that v1.2's defect could not have passed.

  2. **CI workflow integration (Task 2)** — add a MOCK_LOOP=1 step to .github/workflows/achilles-terminal-ci.yml's existing dual-runtime matrix; add a LOOP-02 git diff gate that fails the build if any Phase 17 commit modifies a file under the five protected paths.

Output: 2 new test files + 1 modified workflow YAML. ZERO modifications under packages/voice-*, packages/claude-code-bridge/, packages/achilles-skill/skill/prompts/companion.md (LOOP-02 invariant — verified by the new CI gate itself).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-VALIDATION.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-01-PLAN.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-02-PLAN.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-03-PLAN.md
@.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-04-PLAN.md
@.planning/research/PITFALLS.md
@.github/workflows/achilles-terminal-ci.yml
@apps/achilles-terminal/vitest.config.ts
@apps/achilles/src/main/mock-loop-clients.ts
@apps/achilles-terminal/src/session.ts
@apps/achilles-terminal/src/audio/tts-playback.ts
@apps/achilles-terminal/src/audio/stt-bridge.ts
@apps/achilles-terminal/src/audio/claude-bridge.ts
@apps/achilles-terminal/src/state/constants.ts
@apps/achilles-terminal/src/session-events.ts

<interfaces>
<!-- Key contracts. Extracted from the codebase. -->

From .planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md `<decisions>` row "MOCK_LOOP=1 integration test":
- Test path: apps/achilles-terminal/tests/integration/mock-loop.test.ts
- Mocks needed:
  - Scribe v2 WSS — responds with synthetic transcript after 200ms
  - claude subprocess — synthesises stream-json (session_init then assistant_text_delta events building an ack + <spoken-summary> block then process_exit code=0)
  - ElevenLabs WSS — returns synthetic MP3 frames
  - sox — use mock-amplitude from Phase 16 (already exists in src/ui/mock-amplitude.ts)
  - ffplay — mock spawn that consumes stdin bytes and exits on stdin.end()
- Asserts: full state machine cycle (idle → listening → processing → speaking → idle) within 2 seconds; ack region extracted; spoken-summary extracted; TTS frames piped to ffplay stdin; no orphaned children at end
- The test is the upstream CI gate; PRs cannot merge if it fails

From apps/achilles/src/main/mock-loop-clients.ts (port reference — 588 LOC):
- MockClaudeHandle, MockTtsHandle from v1.2 — the shape ports to Phase 17's needs but the v1.2 file is renderer-IPC-flavored; Phase 17's fixture file simplifies to direct EventEmitter sources
- The v1.2 mock-loop has been the upstream CI gate for v1.2; the new file at apps/achilles-terminal/tests/integration/fixtures/mock-clients.ts adapts the same mocking philosophy to the in-process EventEmitter shape

From .github/workflows/achilles-terminal-ci.yml (Phase 15 baseline) — read for the existing matrix structure:
- `jobs.test` matrix: os ∈ [ubuntu-latest, macos-latest, windows-latest], runtime ∈ [bun, node]
- Steps: checkout → setup-node → setup-bun (conditional) → npm ci --include=optional --force → typecheck → lint → vitest under node OR bun
- The Phase 17 addition: a new step "Vitest MOCK_LOOP=1 integration" that runs ONLY on the integration test directory with MOCK_LOOP=1 env var set
- Additionally a new step "LOOP-02 invariant check" that runs `git diff --name-only HEAD^ HEAD -- packages/voice-protocol packages/voice-stt packages/voice-tts packages/claude-code-bridge packages/achilles-skill/skill/prompts/companion.md` and fails if any output appears

From apps/achilles-terminal/src/session.ts (Plan 04 Task 1):
- SessionOptions has factory seams: sttFactory, ttsFactory, claudeBridgeFactory, micFactory (already in Phase 16), vadOverride (Phase 16)
- MOCK_LOOP=1 enables the mockLoop path inside runVoice that auto-swaps factories with mocks if no explicit factories are passed
- For the integration test, the test ALWAYS passes explicit mock factories so the env var is just a documentation aid

From apps/achilles-terminal/vitest.config.ts:
- pool: "forks" — required for both Bun + Node parity
- The integration test runs under the same config; vitest auto-discovers tests/**/*.test.ts (the integration test path is already covered by the existing glob)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MOCK_LOOP=1 integration test + reusable mock-clients fixtures</name>
  <files>apps/achilles-terminal/tests/integration/mock-loop.test.ts, apps/achilles-terminal/tests/integration/fixtures/mock-clients.ts</files>
  <read_first>
    - apps/achilles-terminal/src/session.ts (Plan 04 Task 1) — the runVoice + Session + SessionOptions surface the test drives
    - apps/achilles-terminal/src/audio/tts-playback.ts (Plan 02 Task 1) — the events$ async iterable contract the mock TTS satisfies
    - apps/achilles-terminal/src/audio/stt-bridge.ts (Plan 02 Task 1) — the createSttBridge surface + getToken callback
    - apps/achilles-terminal/src/audio/claude-bridge.ts (Plan 03 Task 2) — the ClaudeBridgeHandle surface
    - apps/achilles-terminal/src/session-events.ts (Plan 01 Task 1) — the SessionEvent discriminated union
    - apps/achilles-terminal/src/state/constants.ts — SPEAKING_DEBOUNCE_MS (300ms tail the test must wait for)
    - apps/achilles/src/main/mock-loop-clients.ts (v1.2 reference; do NOT port verbatim — adapt to in-process EventEmitter shape)
    - packages/claude-code-bridge/src/event-schemas.ts — the ClaudeStreamEvent variants the mock claude emits (session_init, assistant_text_delta, process_exit shapes)
  </read_first>
  <action>
Create apps/achilles-terminal/tests/integration/fixtures/mock-clients.ts. Export four mock factory builders:

  - `createMockSttFactory(opts?: { commitDelayMs?: number; transcript?: string }): SttFactory` returns a factory that satisfies the createRealtimeSttClient signature. The returned client's start() resolves immediately; write(frame) is a no-op (the test does NOT exercise frame forwarding); commit() emits a `committed` event with payload `{ text: opts.transcript ?? "hello achilles" }` after `commitDelayMs ?? 200` ms via setTimeout-and-emit; events$ is an AsyncIterable that yields the synthesised events.

  - `createMockTtsFactory(opts?: { chunkCount?: number; chunkBytes?: Uint8Array }): TtsFactory` returns a factory whose client.open() resolves immediately; appendText(text) triggers the async iterator to yield `chunkCount ?? 3` chunks (each carrying `opts.chunkBytes ?? new Uint8Array([0x49, 0x44, 0x33, 0x04])` — a fake MP3 header) followed by a `complete` event. flush() and close() resolve.

  - `createMockClaudeFactory(opts?: { ackText?: string; summaryText?: string; exitCode?: number; processDelayMs?: number }): ClaudeFactory` — a factory satisfying claude-bridge.ts's `deps.createSession`. Returns a ClaudeSession whose events$ yields: { type: "session_init", session_id: "mock-sid" }, then 3-4 { type: "assistant_text_delta", text: "..." } events building up `(opts.ackText ?? "Working on that.") + " <spoken-summary>" + (opts.summaryText ?? "All clean, ready when you are.") + "</spoken-summary>"`, then { type: "process_exit", exit_code: opts.exitCode ?? 0, signal: null }. lastTurnText is populated as the events flow. outcome is set after process_exit per the v1.2 contract. send(text) is a no-op (only the events$ matters). cancel() resolves with a ProcessExitEvent. close() resolves.

  - `createMockSpawnImpl(opts?: { onSpawn?: (cmd: string, args: string[], options: unknown) => void; ffplayDrainMs?: number }): typeof import("node:child_process").spawn` returns a fake spawn that distinguishes by cmd. For `cmd === "ffplay"`: returns a fake ChildProcess with a Writable stdin that records bytes; stdin.end() triggers a setTimeout-and-emit "exit" event with code 0 after `opts.ffplayDrainMs ?? 50` ms. For any other cmd (e.g. accidental sox): returns a generic fake child that emits exit immediately. Calls onSpawn?.(cmd, args, options) on every invocation so the test can assert the args.

Each factory builder returns the factory function PLUS a side-channel `.controls` object exposing helpers (e.g., mockStt.controls.forceCommit(text), mockTts.controls.emittedChunks, mockClaude.controls.eventLog). This lets the integration test drive the mocks deterministically without sleeping.

Create apps/achilles-terminal/tests/integration/mock-loop.test.ts. Use vitest's `test()` (not describe()) with `it` blocks for the cycle subtests. Top-level setup:

  - `process.env.MOCK_LOOP = "1"` — documentation aid; the test always passes explicit factory mocks
  - Construct a Session with all four factory seams + the apiKey "mock-key-do-not-use" + voiceId "mock-voice" + mockLoop: true
  - Mock the gracefulShutdown's process.once registration by passing processOverride to the session constructor (Plan 04 Task 2 exposes the seam through registerGracefulShutdown deps; if not exposed in production, expose via SessionOptions for the integration test only)

The single integration test "full voice loop cycle: idle → listening → processing → speaking → idle within 2s":
  1. Construct the Session with mocks
  2. Start the session
  3. Drive VAD speech_start via the existing mock-amplitude / direct mockStt.controls.forceCommit("hello achilles") path
  4. Wait for the chain to fire: assert state transitions through listening → processing → speaking (use session.on("state-change", ...) to record the sequence)
  5. Wait for tts_drained event (the mock ffplay drains in 50ms after stdin.end)
  6. Wait for SPEAKING_DEBOUNCE_MS=300 to elapse → state should transition to idle
  7. Assert mockTts.controls.emittedChunks.length === 3 (the chunkCount)
  8. Assert mockClaude.controls.eventLog includes "process_exit" with exit_code 0
  9. Assert one claude_ack SessionEvent fired with text starting with "Working on that"
  10. Assert one claude_summary SessionEvent fired with text starting with "All clean"
  11. Assert total wall-clock duration < 2000ms (use Date.now() at start and end)
  12. Tear down via gracefulShutdown("dispose"); assert all four mock clients had their cancel/close/stop methods invoked exactly once

A second test "no orphaned children remain after teardown":
  - After step 12, assert mockSpawn.controls.spawned.every(child => child.exited) — every spawned child has emitted its exit event
  - Assert no setInterval / setTimeout tokens are still scheduled (vi.useRealTimers if needed)

A third test "MOCK_LOOP=1 env var documentation": assert that the integration test does not require process.env.MOCK_LOOP to be set (since the factories are passed explicitly); the env var is documented for the CI step but the test path is robust to either setting.

A fourth test "LOOP-04 invariant under mock failure: mockClaude with exit_code=2 triggers failure-override":
  - Construct a Session with mockClaude having opts.exitCode=2
  - Drive the cycle
  - Assert claude_failed SessionEvent fired with payload.reason === "exit_code"
  - Assert claude_summary SessionEvent fired with text starting with "I ran into a problem"
  - Assert mockTts.controls.appendedText[1] (the summary) starts with "I ran into a problem"

NO emojis. NO modification of LOOP-02 paths.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npm run typecheck --workspace apps/achilles-terminal &amp;&amp; MOCK_LOOP=1 npm test --workspace apps/achilles-terminal -- tests/integration/mock-loop.test.ts &amp;&amp; bash -c "test_count=\$(grep -cE 'it\\(|test\\(' apps/achilles-terminal/tests/integration/mock-loop.test.ts); test \$test_count -ge 4" &amp;&amp; bash -c "wall_clock_check=\$(grep -cE '< 2000|toBeLessThan\\(2000\\)' apps/achilles-terminal/tests/integration/mock-loop.test.ts); test \$wall_clock_check -ge 1" &amp;&amp; bash -c "diff_count=\$(git diff --name-only -- 'packages/voice-protocol' 'packages/voice-stt' 'packages/voice-tts' 'packages/claude-code-bridge' 'packages/achilles-skill/skill/prompts/companion.md' | grep -v '^$' | wc -l); test \$diff_count -eq 0"</automated>
  </verify>
  <acceptance_criteria>
    - mock-clients.ts exports the 4 factory builders + their controls side-channel
    - mock-loop.test.ts has at least 4 it/test blocks covering: full cycle + no orphans + env var docs + failure-override
    - Full cycle completes under 2000ms wall-clock (asserted via toBeLessThan)
    - On failure-override path (exit_code=2), claude_summary starts with "I ran into a problem" (no period — verified by toStartWith)
    - All 4 tests pass under vitest --pool=forks
    - LOOP-02: zero diff under the five protected paths
  </acceptance_criteria>
  <done>The MOCK_LOOP=1 integration test exists, passes locally, and exercises the full Plan 04 composition end-to-end. This is the structural CI gate the v1.2 silent-launch defect could not have passed.</done>
</task>

<task type="auto">
  <name>Task 2: CI workflow integration (MOCK_LOOP step + LOOP-02 diff gate)</name>
  <files>.github/workflows/achilles-terminal-ci.yml</files>
  <read_first>
    - .github/workflows/achilles-terminal-ci.yml — FULL current file; understand the matrix structure + the existing test step naming + the Phase 15 D-15-02 npm install command (`npm ci --include=optional --force`)
    - .planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-CONTEXT.md `<code_context>` row ".github/workflows/achilles-terminal-ci.yml" — "Phase 17 ADDS a step that runs MOCK_LOOP=1 npm test --workspace apps/achilles-terminal -- tests/integration/"
    - .planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-VALIDATION.md row "LOOP-02 grep gate in CI"
  </read_first>
  <action>
Edit .github/workflows/achilles-terminal-ci.yml in two surgical places:

  (A) Inside `jobs.test.steps`, AFTER the existing "Vitest under Node" and "Vitest under Bun" steps, ADD two new steps:

    - name: Vitest MOCK_LOOP=1 integration (Node)
      if: matrix.runtime == 'node'
      working-directory: apps/achilles-terminal
      env:
        MOCK_LOOP: "1"
      run: npx vitest run --pool=forks tests/integration/

    - name: Vitest MOCK_LOOP=1 integration (Bun)
      if: matrix.runtime == 'bun'
      working-directory: apps/achilles-terminal
      env:
        MOCK_LOOP: "1"
      run: bunx vitest run --pool=forks tests/integration/

  (B) Add a NEW job `loop-02-invariant` at the same level as `jobs.test` and `jobs.compile-binaries`. The job runs once per pull_request / push event (no matrix). Steps:

    - name: Check out repository with full history
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Assert no diff under LOOP-02 protected paths
      shell: bash
      run: |
        set -euo pipefail
        # The LOOP-02 invariant: Phase 17 may IMPORT packages/voice-* and
        # packages/claude-code-bridge at runtime but MUST NOT modify any
        # file under them. The companion.md SHA-256 source-of-truth check
        # in apps/achilles-terminal/scripts/check-source-of-truth.mjs is
        # the runtime enforcement; this CI step is the source-tree
        # enforcement against the merge base.
        BASE_SHA="${{ github.event.pull_request.base.sha || github.event.before }}"
        HEAD_SHA="${{ github.event.pull_request.head.sha || github.sha }}"
        echo "[ci] LOOP-02 check: base=$BASE_SHA head=$HEAD_SHA"
        DIFF=$(git diff --name-only "$BASE_SHA" "$HEAD_SHA" -- \
          packages/voice-protocol \
          packages/voice-stt \
          packages/voice-tts \
          packages/claude-code-bridge \
          packages/achilles-skill/skill/prompts/companion.md || true)
        if [ -n "$DIFF" ]; then
          echo "[ci] FAIL: LOOP-02 invariant violated. Modified files:"
          echo "$DIFF"
          exit 1
        fi
        echo "[ci] OK: LOOP-02 invariant holds across base..head"

    - name: Run companion.md SHA-256 source-of-truth check
      shell: bash
      working-directory: apps/achilles-terminal
      run: |
        set -euo pipefail
        # Plan 17-01 created scripts/check-source-of-truth.mjs; this CI
        # step invokes it to assert the embedded hash in companion-md.ts
        # matches the source-of-truth file on disk.
        node scripts/check-source-of-truth.mjs

The job needs a `runs-on: ubuntu-latest` and `permissions: contents: read` block matching the existing jobs. The job runs in parallel with `jobs.test` so the merge gate fires fast.

Do NOT modify the existing `compile-binaries` job; do NOT modify the existing `permissions` block; do NOT change the trigger paths or the concurrency block. The edit is additive only — two new steps inside the existing `test` job + one new top-level job.

NO emojis in the YAML.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; bash -c "grep -cE 'MOCK_LOOP' .github/workflows/achilles-terminal-ci.yml | grep -E '^[2-9]|^[1-9][0-9]+'" &amp;&amp; bash -c "grep -cE 'loop-02-invariant|LOOP-02' .github/workflows/achilles-terminal-ci.yml | grep -E '^[1-9]'" &amp;&amp; bash -c "grep -cE 'check-source-of-truth\\.mjs' .github/workflows/achilles-terminal-ci.yml | grep -E '^[1-9]'" &amp;&amp; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/achilles-terminal-ci.yml'))" &amp;&amp; bash -c "diff_count=\$(git diff --name-only -- 'packages/voice-protocol' 'packages/voice-stt' 'packages/voice-tts' 'packages/claude-code-bridge' 'packages/achilles-skill/skill/prompts/companion.md' | grep -v '^$' | wc -l); test \$diff_count -eq 0"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/achilles-terminal-ci.yml is valid YAML (python3 yaml.safe_load succeeds)
    - The file contains two new MOCK_LOOP=1 steps (one for Node, one for Bun) inside jobs.test.steps
    - The file contains a new top-level job `loop-02-invariant` that runs git diff against the 5 protected paths and exits 1 on any drift
    - The file contains a step that invokes scripts/check-source-of-truth.mjs
    - The existing test + compile-binaries jobs are untouched (no name changes, no matrix changes)
    - LOOP-02: zero diff under the five protected paths
  </acceptance_criteria>
  <done>CI gate is wired. Every PR runs MOCK_LOOP=1 on both Node and Bun matrices AND fails on any modification under the five protected paths. This is the structural defence against the v1.2 silent-launch shape reaching the merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mock-clients.ts ↔ test runner | The factories run in-process; the test asserts against their side-channel controls. No network, no real subprocesses. |
| MOCK_LOOP env var ↔ runtime branch | Documentation aid only; the actual mock injection happens via SessionOptions factory seams. A production binary launched with MOCK_LOOP=1 still requires explicit factory swaps — the env var alone does not auto-swap factories outside the integration test. |
| CI workflow ↔ merge base | The LOOP-02 diff gate uses the merge-base SHA from the PR event; on a force-push the diff may include older commits but the gate's invariant (zero changes under the 5 paths) is monotonic — false negatives are not possible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-22 | Tampering | A future PR weakens the LOOP-02 gate by editing the workflow | accept | The workflow itself can be modified by a PR; the structural defence is code review + the orchestrator-level CONTEXT.md decision. Phase 20 RBS asciicasts are the next-layer defence. |
| T-17-23 | Tampering | A future PR introduces a real Achilles invocation in the integration test bypassing the mocks | accept | The test passes explicit mocks; a PR that removes them would fail the global rule "no auto-running of `achilles voice` from any task". Code review enforces. |
| T-17-24 | Denial of service | The MOCK_LOOP test hangs indefinitely if a mock factory misbehaves | mitigate | Each it() block carries an explicit `expect(durationMs).toBeLessThan(2000)`; vitest's default per-test timeout (5s) is the outer guard. |
| T-17-25 | Information disclosure | Mock factories accidentally log real credential shapes | accept | The mock data is hardcoded ("mock-key-do-not-use", "mock-voice"); structured-logger's default redaction patterns from Plan 01 cover the sk-/xi-/Bearer shapes even if a future test introduces a fake key. |
| T-17-SC | Tampering | npm package installs in Plan 05 | mitigate | No NEW npm installs in Plan 05 — the workflow uses the existing `npm ci --include=optional --force` from Phase 15. |
</threat_model>

<verification>
- MOCK_LOOP=1 integration test exists at apps/achilles-terminal/tests/integration/mock-loop.test.ts with at least 4 it/test blocks
- Full state machine cycle completes under 2 seconds (asserted via toBeLessThan(2000))
- Failure-override invariant verified: exit_code=2 produces a summary starting with FAILURE_OVERRIDE_PHRASE
- CI workflow is valid YAML (python yaml.safe_load succeeds)
- CI workflow contains two new MOCK_LOOP=1 steps + a new loop-02-invariant job
- LOOP-02 invariant: zero diff under packages/voice-*, packages/claude-code-bridge/, packages/achilles-skill/skill/prompts/companion.md across the full Phase 17 commit range
- Phase 16's 129 + Plan 01-04 tests + Plan 05's 4 integration tests = ~246 total passing
</verification>

<success_criteria>
- MOCK_LOOP=1 integration test exists, passes locally, exercises the full LOOP-01 chain under 2s wall-clock with no orphans
- mock-clients.ts fixtures are reusable by future hardening tests (Phase 19/20 may extend)
- CI workflow runs MOCK_LOOP=1 on every PR (Node + Bun matrices)
- CI workflow's loop-02-invariant job fails any future PR that modifies the five protected paths
- companion.md SHA-256 source-of-truth check runs in CI alongside the LOOP-02 diff gate
- LOOP-02 invariant verified across the Phase 17 commit range
</success_criteria>

<output>
Create `.planning/phases/17-end-to-end-voice-loop-gracefulshutdown/17-05-SUMMARY.md` when done documenting: the MOCK_LOOP test's measured wall-clock duration (should be well under 2s), the exact mock factory shapes shipped, the CI workflow line ranges added (start + end line numbers of the two new steps + the new top-level job), the loop-02-invariant job's expected pass/fail behavior against a hypothetical mock-mode PR that touches packages/voice-stt/ (should fail), and a confirmation line listing the five LOOP-02 paths with `(unchanged)` after each.
</output>
</content>
</invoke>