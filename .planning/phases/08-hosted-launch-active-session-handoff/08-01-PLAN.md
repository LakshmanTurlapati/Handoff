---
phase: 08-hosted-launch-active-session-handoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - LAUNCH-01
  - SAFE-02
must_haves:
  truths:
    - "`POST /api/handoffs` mints a single-use Fly-hosted `/launch/<publicId>` URL bound to the active Codex thread and returns a short-lived expiry."
    - "`handoff codex-handoff --format json` (invoked by the `$handoff` Codex skill) returns the hosted descriptor with `launchUrl` and `qrText` for terminal QR rendering, derived from the active thread context only."
    - "Repeated invocations from the same active thread reuse the existing valid handoff record rather than minting a new public id."
  artifacts:
    - path: "apps/web/app/api/handoffs/route.ts"
      provides: "hosted handoff mint-or-reuse endpoint"
      contains: "publicId"
    - path: "apps/bridge/src/cli/codex-handoff.ts"
      provides: "Codex-side handoff helper bound to CODEX_THREAD_ID"
      contains: "missing_active_thread_context"
    - path: "apps/bridge/src/lib/qr.ts"
      provides: "terminal QR renderer fed by `qrText`"
      contains: "renderTerminalQr"
    - path: "apps/bridge/resources/codex/skills/handoff/SKILL.md"
      provides: "Codex skill manifest that invokes the helper"
      contains: "handoff codex-handoff --format json"
---

<objective>
Verify that the `$handoff` skill, via `handoff codex-handoff --format json`, mints a single-use Fly-hosted launch URL plus QR text from the active Codex thread context, and that subsequent invocations from the same thread reuse the existing valid record. This plan certifies success criterion 1 (`LAUNCH-01`, `SAFE-02`); it is verification-only.
</objective>

<verification>
- [ ] `apps/web/app/api/handoffs/route.ts` returns `{ threadId, sessionId, launchUrl, qrText, expiresAt, reused }` with `launchUrl = ${baseUrl}/launch/${publicId}` and `publicId` generated via `randomBytes(18).toString("base64url")`
- [ ] `apps/bridge/src/cli/codex-handoff.ts` resolves the thread/session from `CODEX_THREAD_ID` / `CODEX_SESSION_ID` (or explicit flags) and fails closed with `missing_active_thread_context` when absent, with no picker fallback
- [ ] `apps/bridge/resources/codex/skills/handoff/SKILL.md` directs Codex to invoke `handoff codex-handoff --format json` and to return only the launch URL, QR text, expiry, and reuse state
- [ ] `apps/web/tests/unit/handoff-route.test.ts` covers fresh-mint, reuse (same thread), replacement (expired), missing bootstrap token, revoked installation, user mismatch, and revoked-row branches
- [ ] `apps/bridge/tests/unit/codex-handoff-command.test.ts` covers env-derived thread binding, daemon_started/daemon_reused propagation, and CODEX_SESSION_ID preference
- [ ] `apps/bridge/tests/unit/codex-handoff-safety.test.ts` proves cross-thread non-reuse and same-thread reuse
</verification>
