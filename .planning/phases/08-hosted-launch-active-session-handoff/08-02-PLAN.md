---
phase: 08-hosted-launch-active-session-handoff
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - LAUNCH-02
  - LAUNCH-03
  - SAFE-02
must_haves:
  truths:
    - "Opening `/launch/<publicId>` on the hosted app resolves the publicId server-side and redirects to `/session/<sessionId>` without a GitHub sign-in."
    - "The redirect issues or reuses a durable `cm_device_session` cookie linked to the originating bridge installation before redirecting into the session route."
    - "`/launch/<publicId>` honours the forwarded public origin (Fly edge) when constructing the redirect target."
    - "Expired, revoked, unknown, or unauthorized launch ids fail closed by redirecting to `/launch/error?code=<code>`."
    - "Browser principal derivation on `/session/<sessionId>` and the device/session APIs reads from the device session and linked bridge installation only — no `auth()` round-trip."
  artifacts:
    - path: "apps/web/app/launch/[publicId]/route.ts"
      provides: "authless launch entrypoint"
      contains: "claimHandoffLaunch"
    - path: "apps/web/lib/handoff-launch.ts"
      provides: "publicId resolution + device-session issuance/reuse"
      contains: "findThreadHandoffByPublicId"
    - path: "apps/web/app/launch/error/page.tsx"
      provides: "fail-closed error surface for handoff_not_found / handoff_expired / handoff_revoked / handoff_not_authorized"
      contains: "$handoff"
    - path: "apps/web/lib/live-session/server.ts"
      provides: "device-session-only browser principal"
      contains: "requireRemotePrincipal"
---

<objective>
Verify that the hosted `/launch/<publicId>` route consumes the single-use publicId, mints or reuses a durable device session bound to the originating bridge installation, redirects directly to `/session/<sessionId>`, fails closed on terminal handoff states, and that the rest of the browser surface authenticates from the device session only. This plan certifies success criteria 2 and 3 (`LAUNCH-02`, `LAUNCH-03`, `SAFE-02`); it is verification-only.
</objective>

<verification>
- [ ] `apps/web/app/launch/[publicId]/route.ts` calls `claimHandoffLaunch` and redirects valid claims with HTTP 307 to `/session/<encoded-sessionId>` on the forwarded public origin
- [ ] `apps/web/lib/handoff-launch.ts` rejects with `handoff_not_found` / `handoff_revoked` / `handoff_expired` / `handoff_not_authorized` and revokes the handoff row after a successful claim
- [ ] `apps/web/app/launch/error/page.tsx` maps each error code to user-facing remediation copy referencing `$handoff`
- [ ] `apps/web/lib/live-session/server.ts::requireRemotePrincipal` rejects missing, mismatched, revoked, or expired device sessions and never calls `auth()`
- [ ] `apps/web/tests/unit/handoff-launch-route.test.ts` covers (a) valid launch redirect to the session route, (b) error-code redirect to `/launch/error`, (c) forwarded host honoured over internal origin
- [ ] `apps/web/tests/unit/remote-principal.test.ts` covers revoked, expired, user mismatch, and happy-path principal derivation with `lastSeenAt` touch
</verification>
