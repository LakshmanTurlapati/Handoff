---
phase: 05-multi-instance-routing-production-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - apps/relay/src/browser/browser-registry.ts
  - apps/relay/src/browser/session-router.ts
  - apps/relay/src/routes/ops.ts
  - apps/relay/src/routes/readyz.ts
  - apps/relay/src/server.ts
  - apps/relay/fly.toml
  - apps/relay/tests/unit/ops-route.test.ts
  - apps/relay/tests/unit/readyz.test.ts
  - apps/relay/tests/unit/ws-browser-reconnect.test.ts
autonomous: true
requirements:
  - OPS-04
  - SEC-04
must_haves:
  truths:
    - "Relay fanout remains bounded under slow-browser pressure and degrades best-effort live detail before critical state."
    - "Operators can inspect local ownership, queue pressure, drop counts, replay failures, and disconnect reasons without a dashboard product."
    - "Readiness for new traffic is separated from internal monitoring and only degrades when local routing ability is genuinely compromised."
    - "Phase 5 exposes production hardening checks without broadening the browser feature scope."
  artifacts:
    - path: "apps/relay/src/routes/ops.ts"
      provides: "compact operator-facing relay health and ownership snapshot"
      exports: ["registerOpsRoutes"]
    - path: "apps/relay/src/routes/readyz.ts"
      provides: "ownership/pressure-aware readiness gate"
      contains: "degraded"
    - path: "apps/relay/src/browser/browser-registry.ts"
      provides: "bounded per-browser pressure tracking and drop counters"
      contains: "MAX_PENDING_BROWSER_MESSAGES"
    - path: "apps/relay/fly.toml"
      provides: "top-level non-routing observability check for relay ops"
      contains: "[checks.relay_ops]"
  key_links:
    - from: "apps/relay/src/browser/session-router.ts"
      to: "apps/relay/src/routes/ops.ts"
      via: "session fanout pressure and disconnect counters feed the operator snapshot"
      pattern: "droppedBestEffortMessages"
    - from: "apps/relay/src/routes/readyz.ts"
      to: "apps/relay/src/routes/ops.ts"
      via: "readiness decisions reuse the same ownership and pressure counters visible to operators"
      pattern: "backpressuredSockets"
    - from: "apps/relay/fly.toml"
      to: "apps/relay/src/routes/ops.ts"
      via: "top-level Fly check monitors ops state without changing request routing"
      pattern: "/ops/relay"
---

<objective>
Add the production hardening and operator visibility layer for Phase 5.

Purpose: Once ownership and replay routing work, the relay still needs explicit pressure semantics and a compact operator surface. This plan adds bounded fanout behavior, exposes ownership/replay/pressure state in one relay snapshot, and wires Fly checks so routing health stays separate from internal observability.

Output: Priority-aware fanout pressure accounting, an `/ops/relay` snapshot, ownership/pressure-aware `readyz`, top-level Fly monitoring for relay ops, and regression coverage for ops/readyz behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-multi-instance-routing-production-hardening/05-CONTEXT.md
@.planning/phases/05-multi-instance-routing-production-hardening/05-RESEARCH.md
@.planning/phases/05-multi-instance-routing-production-hardening/05-VALIDATION.md
@.planning/phases/05-multi-instance-routing-production-hardening/05-01-PLAN.md
@.planning/phases/05-multi-instance-routing-production-hardening/05-02-PLAN.md
@apps/relay/src/browser/browser-registry.ts
@apps/relay/src/browser/session-router.ts
@apps/relay/src/routes/readyz.ts
@apps/relay/fly.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bounded browser fanout pressure accounting and drop policy</name>
  <files>apps/relay/src/browser/browser-registry.ts, apps/relay/src/browser/session-router.ts</files>
  <read_first>.planning/phases/05-multi-instance-routing-production-hardening/05-RESEARCH.md, apps/relay/src/browser/browser-registry.ts, apps/relay/src/browser/session-router.ts, apps/relay/src/browser/session-buffer.ts</read_first>
  <action>Extend `apps/relay/src/browser/browser-registry.ts` with these exact constants: `MAX_PENDING_BROWSER_MESSAGES = 50`, `PRESSURE_WARNING_THRESHOLD = 40`, and `MAX_BEST_EFFORT_DROPS = 25`. Add per-browser counters for `pendingMessages`, `droppedBestEffortMessages`, and `lastDisconnectReason`. Update `apps/relay/src/browser/session-router.ts` so each outbound live event is classified by a new helper `getEventPriority(event)` into `critical`, `important`, or `best_effort`. Use these exact rules: `session.ended`, `session.error`, and approval activities are `critical`; `session.reconnected` and `interrupt.finished` are `important`; all other live activity and replay detail are `best_effort`. When `pendingMessages >= 40`, increment a relay-wide `queuePressureCount`. When `pendingMessages > 50`, drop only `best_effort` events and increment `droppedBestEffortMessages`. If the socket still exceeds `50` pending messages after `25` best-effort drops, close it with code `1013` and the exact reason `backpressure`.</action>
  <verify>rg -n 'MAX_PENDING_BROWSER_MESSAGES|PRESSURE_WARNING_THRESHOLD|MAX_BEST_EFFORT_DROPS|pendingMessages|droppedBestEffortMessages|lastDisconnectReason|getEventPriority|critical|important|best_effort|backpressure' apps/relay/src/browser/browser-registry.ts apps/relay/src/browser/session-router.ts</verify>
  <acceptance_criteria>
    - `apps/relay/src/browser/browser-registry.ts` contains `MAX_PENDING_BROWSER_MESSAGES = 50`
    - `apps/relay/src/browser/browser-registry.ts` contains `PRESSURE_WARNING_THRESHOLD = 40`
    - `apps/relay/src/browser/session-router.ts` defines `getEventPriority`
    - `apps/relay/src/browser/session-router.ts` classifies `session.ended` as `critical`
    - `apps/relay/src/browser/session-router.ts` references `droppedBestEffortMessages`
    - `apps/relay/src/browser/browser-registry.ts` closes overloaded sockets with the exact reason `backpressure`
  </acceptance_criteria>
  <done>The relay now has explicit, bounded pressure behavior instead of silent unbounded fanout.</done>
</task>

<task type="auto">
  <name>Task 2: Expose compact relay ops state and gate readiness on severe local degradation</name>
  <files>apps/relay/src/routes/ops.ts, apps/relay/src/routes/readyz.ts, apps/relay/src/server.ts</files>
  <read_first>apps/relay/src/routes/readyz.ts, apps/relay/src/server.ts, apps/relay/src/ownership/ownership-service.ts, apps/relay/src/browser/browser-registry.ts, apps/relay/src/browser/session-router.ts, .planning/phases/05-multi-instance-routing-production-hardening/05-CONTEXT.md</read_first>
  <action>Create `apps/relay/src/routes/ops.ts` exporting `registerOpsRoutes(app)` and a pure `handleRelayOps()` helper. `GET /ops/relay` must return JSON with these exact top-level keys: `relayMachineId`, `relayRegion`, `activeBridgeCount`, `activeBrowserCount`, `activeLeaseCount`, `staleLeaseCount`, `queuePressureCount`, `backpressuredSockets`, `droppedBestEffortMessages`, `recentDisconnectReasons`, `recentReplayFailures`, and `readyzStatus`. Update `apps/relay/src/routes/readyz.ts` so it returns HTTP `200` with `{ status: "ready" }` when ownership lookup is healthy and `backpressuredSockets < 5`, and HTTP `503` with `{ status: "degraded" }` when ownership storage is unavailable or `backpressuredSockets >= 5`. Keep `/healthz` unchanged. Update `apps/relay/src/server.ts` to register the new ops route alongside existing relay routes.</action>
  <verify>rg -n 'registerOpsRoutes|handleRelayOps|relayMachineId|relayRegion|activeBridgeCount|activeBrowserCount|activeLeaseCount|staleLeaseCount|queuePressureCount|backpressuredSockets|droppedBestEffortMessages|recentDisconnectReasons|recentReplayFailures|readyzStatus|status.:.\"degraded\"' apps/relay/src/routes/ops.ts apps/relay/src/routes/readyz.ts apps/relay/src/server.ts</verify>
  <acceptance_criteria>
    - `apps/relay/src/routes/ops.ts` exports `registerOpsRoutes`
    - `apps/relay/src/routes/ops.ts` returns the exact key `recentReplayFailures`
    - `apps/relay/src/routes/ops.ts` returns the exact key `droppedBestEffortMessages`
    - `apps/relay/src/routes/readyz.ts` contains the exact status `degraded`
    - `apps/relay/src/routes/readyz.ts` references `backpressuredSockets`
    - `apps/relay/src/server.ts` registers the ops route
  </acceptance_criteria>
  <done>Operators now have one compact relay-state snapshot and readiness only degrades for real routing-impacting problems.</done>
</task>

<task type="auto">
  <name>Task 3: Add Fly top-level ops monitoring and regression coverage for ops and readiness behavior</name>
  <files>apps/relay/fly.toml, apps/relay/tests/unit/ops-route.test.ts, apps/relay/tests/unit/readyz.test.ts, apps/relay/tests/unit/ws-browser-reconnect.test.ts</files>
  <read_first>apps/relay/fly.toml, apps/relay/src/routes/ops.ts, apps/relay/src/routes/readyz.ts, .planning/phases/05-multi-instance-routing-production-hardening/05-RESEARCH.md, .planning/phases/05-multi-instance-routing-production-hardening/05-VALIDATION.md</read_first>
  <action>Update `apps/relay/fly.toml` to add a top-level Fly health check named `relay_ops` under `[checks]` with these exact values: `type = "http"`, `port = 8080`, `method = "get"`, `path = "/ops/relay"`, `interval = "30s"`, `grace_period = "20s"`, and `timeout = "5s"`. Keep the existing `/readyz` service-level check and `/healthz` machine check unchanged. Create `apps/relay/tests/unit/ops-route.test.ts` to assert `/ops/relay` returns all exact keys listed in the plan and reports newest disconnect reasons first. Create `apps/relay/tests/unit/readyz.test.ts` to assert `handleReadyz()` returns `status: "degraded"` at `backpressuredSockets >= 5` and `status: "ready"` below that threshold. Extend `apps/relay/tests/unit/ws-browser-reconnect.test.ts` to assert `bridge_unavailable` and `backpressure` disconnect reasons are surfaced in the relay ops state.</action>
  <verify>rg -n '\\[checks\\.relay_ops\\]|type = \"http\"|path = \"/ops/relay\"|status.:.\"degraded\"|relayMachineId|recentDisconnectReasons|bridge_unavailable|backpressure|describe\\(|it\\(' apps/relay/fly.toml apps/relay/tests/unit/ops-route.test.ts apps/relay/tests/unit/readyz.test.ts apps/relay/tests/unit/ws-browser-reconnect.test.ts</verify>
  <acceptance_criteria>
    - `apps/relay/fly.toml` contains `[checks.relay_ops]`
    - `apps/relay/fly.toml` contains the exact path `/ops/relay`
    - `apps/relay/tests/unit/ops-route.test.ts` contains `relayMachineId`
    - `apps/relay/tests/unit/ops-route.test.ts` contains `recentDisconnectReasons`
    - `apps/relay/tests/unit/readyz.test.ts` contains `status: "degraded"`
    - `apps/relay/tests/unit/ws-browser-reconnect.test.ts` references `backpressure`
  </acceptance_criteria>
  <done>Phase 5 production hardening now includes explicit monitoring checks and regression coverage for ops/readyz behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `vitest run apps/relay/tests/unit/ops-route.test.ts apps/relay/tests/unit/readyz.test.ts apps/relay/tests/unit/ws-browser-reconnect.test.ts` succeeds
- [ ] `rg -n 'MAX_PENDING_BROWSER_MESSAGES|backpressure|recentReplayFailures|status.:.\"degraded\"|\\[checks\\.relay_ops\\]' apps/relay/src/browser/browser-registry.ts apps/relay/src/routes/ops.ts apps/relay/src/routes/readyz.ts apps/relay/fly.toml` returns matches
- [ ] `/healthz` remains unchanged while `/readyz` and `/ops/relay` diverge in purpose
</verification>

<success_criteria>

- Relay fanout remains bounded under pressure and degrades best-effort detail first
- Operators can inspect ownership, replay failure, disconnect, and queue-pressure state without a dashboard build
- Readiness for new traffic is separated from internal observability
- Phase 5 closes the production-hardening gap required before broader rollout
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-instance-routing-production-hardening/05-03-SUMMARY.md`
</output>
