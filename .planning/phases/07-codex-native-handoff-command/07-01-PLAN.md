---
phase: 07-codex-native-handoff-command
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bridge/package.json
  - apps/bridge/src/cli.ts
  - apps/bridge/src/lib/codex-command-install.ts
  - apps/bridge/resources/codex/commands/handoff.md
  - apps/bridge/README.md
  - apps/bridge/tests/unit/codex-command-install.test.ts
autonomous: true
requirements:
  - CMD-01
  - SAFE-01
must_haves:
  truths:
    - "The published `handoff` package installs or updates a real Codex `/handoff` slash-command asset instead of asking the user to type raw bridge CLI commands inside Codex."
    - "The command asset delegates into the local `handoff` package and keeps the bridge outbound-only; it does not expose `codex app-server` or open a local inbound port."
    - "Command installation is idempotent and returns actionable repair guidance when the Codex command directory cannot be resolved."
    - "The packaged command markdown follows Codex slash-command conventions with explicit preflight, commands, verification, and summary sections."
  artifacts:
    - path: "apps/bridge/resources/codex/commands/handoff.md"
      provides: "packaged `/handoff` slash-command asset"
      contains: "## Preflight"
    - path: "apps/bridge/src/lib/codex-command-install.ts"
      provides: "Codex command install/update helper"
      exports: ["resolveCodexCommandDir", "installCodexHandoffCommand"]
    - path: "apps/bridge/src/cli.ts"
      provides: "CLI entrypoint for Codex command installation"
      contains: "install-codex-command"
    - path: "apps/bridge/package.json"
      provides: "published package surface that ships Codex command assets"
      contains: "resources/codex"
  key_links:
    - from: "apps/bridge/src/lib/codex-command-install.ts"
      to: "apps/bridge/resources/codex/commands/handoff.md"
      via: "installer copies the packaged slash-command asset into the resolved Codex command directory"
      pattern: "handoff.md"
    - from: "apps/bridge/src/cli.ts"
      to: "apps/bridge/src/lib/codex-command-install.ts"
      via: "CLI invokes the Codex command installer/update path"
      pattern: "installCodexHandoffCommand"
    - from: "apps/bridge/package.json"
      to: "apps/bridge/resources/codex/commands/handoff.md"
      via: "the npm package includes the packaged `/handoff` asset in its published files"
      pattern: "resources/codex"
---

<objective>
Install the Codex-facing command surface for `/handoff`.

Purpose: Phase 7 cannot bind `/handoff` to the active thread unless Codex can actually discover a packaged slash command after npm install. The bridge already owns the local runtime, but there is no command asset, no install/update path, and no published package entry that connects the `handoff` npm package to Codex command discovery.

Output: A packaged markdown slash-command asset, an installer/update helper that places it into the resolved Codex command directory, a CLI command that runs that installer, and unit coverage for the install path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-codex-native-handoff-command/07-CONTEXT.md
@.planning/phases/07-codex-native-handoff-command/07-RESEARCH.md
@.planning/phases/07-codex-native-handoff-command/07-VALIDATION.md
@apps/bridge/package.json
@apps/bridge/src/cli.ts
@apps/bridge/README.md
@apps/bridge/src/cli/launch.ts
@apps/bridge/src/lib/local-state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package a real `/handoff` slash-command asset inside the bridge package</name>
  <files>apps/bridge/resources/codex/commands/handoff.md</files>
  <read_first>.planning/phases/07-codex-native-handoff-command/07-RESEARCH.md, apps/bridge/README.md, /Users/lakshmanturlapati/.codex/.tmp/plugins/plugins/vercel/commands/deploy.md, /Users/lakshmanturlapati/.codex/.tmp/plugins/plugins/vercel/commands/_conventions.md</read_first>
  <action>Create `apps/bridge/resources/codex/commands/handoff.md` as the packaged Codex slash-command asset for `/handoff`. Add YAML frontmatter with the exact description `Start or reuse a thread-bound handoff from the current Codex session.`. Include these exact sections in order: `## Preflight`, `## Plan`, `## Commands`, `## Verification`, `## Summary`, and `## Next Steps`. In `## Commands`, instruct Codex to call the exact local helper command `handoff codex-handoff --format json` once the active thread context is available. Also state the exact failure rule that if the current thread cannot be resolved, the command must return `missing_active_thread_context` and must not call any generic session-picker flow such as `thread/list` for user selection.</action>
  <verify>rg -n '^description:|## Preflight|## Plan|## Commands|## Verification|## Summary|## Next Steps|handoff codex-handoff --format json|missing_active_thread_context|thread/list' apps/bridge/resources/codex/commands/handoff.md</verify>
  <acceptance_criteria>
    - `apps/bridge/resources/codex/commands/handoff.md` exists
    - `apps/bridge/resources/codex/commands/handoff.md` contains the exact heading `## Preflight`
    - `apps/bridge/resources/codex/commands/handoff.md` contains the exact command `handoff codex-handoff --format json`
    - `apps/bridge/resources/codex/commands/handoff.md` contains the exact error `missing_active_thread_context`
    - `apps/bridge/resources/codex/commands/handoff.md` explicitly forbids falling back to `thread/list` for user selection
  </acceptance_criteria>
  <done>The bridge package now ships a concrete `/handoff` slash-command asset instead of leaving command discovery implicit.</done>
</task>

<task type="auto">
  <name>Task 2: Add an installer/update helper that resolves the Codex command directory safely</name>
  <files>apps/bridge/src/lib/codex-command-install.ts, apps/bridge/src/cli.ts</files>
  <read_first>apps/bridge/src/cli.ts, apps/bridge/src/lib/local-state.ts, .planning/phases/07-codex-native-handoff-command/07-RESEARCH.md, /Users/lakshmanturlapati/.codex/config.toml</read_first>
  <action>Create `apps/bridge/src/lib/codex-command-install.ts` exporting the exact functions `resolveCodexCommandDir` and `installCodexHandoffCommand`. Implement `resolveCodexCommandDir` so it prefers `${CODEX_HOME}/commands` when `CODEX_HOME` is set and otherwise falls back to `${HOME}/.codex/commands`. Implement `installCodexHandoffCommand` so it creates the resolved directory with mode `0o700`, copies the packaged asset from `apps/bridge/resources/codex/commands/handoff.md` into the exact target file `handoff.md`, and returns one of the exact statuses `command_installed`, `command_updated`, or `command_install_skipped`. Update `apps/bridge/src/cli.ts` to register the exact top-level command `install-codex-command`, call `installCodexHandoffCommand`, and print the returned status line unchanged.</action>
  <verify>rg -n 'resolveCodexCommandDir|installCodexHandoffCommand|CODEX_HOME|\\.codex/commands|0o700|command_installed|command_updated|command_install_skipped|install-codex-command' apps/bridge/src/lib/codex-command-install.ts apps/bridge/src/cli.ts</verify>
  <acceptance_criteria>
    - `apps/bridge/src/lib/codex-command-install.ts` exports `resolveCodexCommandDir`
    - `apps/bridge/src/lib/codex-command-install.ts` exports `installCodexHandoffCommand`
    - `apps/bridge/src/lib/codex-command-install.ts` contains both `CODEX_HOME` and `.codex/commands`
    - `apps/bridge/src/lib/codex-command-install.ts` contains `0o700`
    - `apps/bridge/src/cli.ts` registers the exact command `install-codex-command`
    - `apps/bridge/src/cli.ts` prints one of `command_installed`, `command_updated`, or `command_install_skipped`
  </acceptance_criteria>
  <done>The bridge now has a deterministic, repeatable way to place the `/handoff` command asset where Codex can discover it.</done>
</task>

<task type="auto">
  <name>Task 3: Ship the command asset in the npm package and cover installation behavior with tests</name>
  <files>apps/bridge/package.json, apps/bridge/README.md, apps/bridge/tests/unit/codex-command-install.test.ts</files>
  <read_first>apps/bridge/package.json, apps/bridge/README.md, apps/bridge/tests/unit/launch-command.test.ts, .planning/phases/07-codex-native-handoff-command/07-VALIDATION.md</read_first>
  <action>Update `apps/bridge/package.json` so the published `files` array includes the exact path `resources/codex`. Update `apps/bridge/README.md` with an explicit Codex setup step that tells the user to run the exact command `handoff install-codex-command` after install. Add `apps/bridge/tests/unit/codex-command-install.test.ts` covering these exact behaviors: fresh install into `${CODEX_HOME}/commands`, fallback install into `${HOME}/.codex/commands` when `CODEX_HOME` is unset, and idempotent re-run that reports `command_updated` or `command_install_skipped` instead of creating duplicates.</action>
  <verify>rg -n 'resources/codex|handoff install-codex-command|command_installed|command_updated|command_install_skipped|describe\\(|it\\(' apps/bridge/package.json apps/bridge/README.md apps/bridge/tests/unit/codex-command-install.test.ts</verify>
  <acceptance_criteria>
    - `apps/bridge/package.json` contains the exact path `resources/codex`
    - `apps/bridge/README.md` contains the exact command `handoff install-codex-command`
    - `apps/bridge/tests/unit/codex-command-install.test.ts` exists
    - `apps/bridge/tests/unit/codex-command-install.test.ts` contains `describe(`
    - `apps/bridge/tests/unit/codex-command-install.test.ts` covers both `${CODEX_HOME}/commands` and `${HOME}/.codex/commands`
  </acceptance_criteria>
  <done>The npm package now actually ships the Codex command asset and has regression coverage for installing it safely.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run apps/bridge/tests/unit/codex-command-install.test.ts` succeeds
- [ ] `rg -n 'install-codex-command|resources/codex|handoff install-codex-command' apps/bridge/src/cli.ts apps/bridge/package.json apps/bridge/README.md` returns matches
- [ ] Re-running the installer updates or skips cleanly instead of duplicating `handoff.md`
</verification>

<success_criteria>

- The published `handoff` package ships a discoverable `/handoff` slash-command asset
- The bridge CLI can install or update that asset into the resolved Codex command directory
- The command asset delegates into the local handoff package instead of introducing a second command surface
- Phase 07-02 can now depend on a real `/handoff` entrypoint instead of an imagined one
</success_criteria>

<output>
After completion, create `.planning/phases/07-codex-native-handoff-command/07-01-SUMMARY.md`
</output>
