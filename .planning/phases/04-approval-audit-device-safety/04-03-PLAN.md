---
phase: 04-approval-audit-device-safety
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - packages/protocol/src/live-session.ts
  - apps/relay/src/routes/ws-browser.ts
  - apps/relay/src/browser/browser-registry.ts
  - apps/relay/src/browser/session-router.ts
  - apps/relay/src/routes/ws-bridge.ts
  - apps/web/lib/live-session/session-model.ts
  - apps/web/lib/live-session/reducer.ts
  - apps/web/lib/live-session/transport.ts
  - apps/web/app/session/[sessionId]/session-shell.tsx
  - apps/web/components/session/reconnect-banner.tsx
  - apps/web/components/session/session-ended-card.tsx
  - apps/web/components/session/composer.tsx
  - apps/web/components/session/approval-card.tsx
  - apps/relay/tests/unit/ws-browser-reconnect.test.ts
  - apps/relay/tests/unit/session-router-safety.test.ts
  - apps/web/tests/unit/session-shell-safety.test.tsx
  - apps/web/tests/device-safety-mobile.spec.ts
autonomous: true
requirements:
  - AUTH-04
  - SESS-06
  - LIVE-03
  - SEC-05
must_haves:
  truths:
    - "Transient disconnects can reconnect and backfill, but revoked or expired device sessions cannot silently resurrect."
    - "Bridge/Codex exit produces a terminal ended state instead of an infinite reconnect loop."
    - "The phone UI clearly distinguishes reconnecting from revoked or ended states and disables unsafe controls when trust is gone."
    - "Phase 4 does not broaden the remote command surface beyond prompt, steer, approval, and interrupt."
  artifacts:
    - path: "packages/protocol/src/live-session.ts"
      provides: "terminal end-reason contract for browser live sessions"
      contains: "device_session_revoked"
    - path: "apps/web/components/session/session-ended-card.tsx"
      provides: "terminal revoked/ended UI block"
      contains: "Session ended on your laptop"
    - path: "apps/web/lib/live-session/transport.ts"
      provides: "reconnect transport that stops on terminal reasons"
      contains: "device_session_expired"
    - path: "apps/relay/src/routes/ws-browser.ts"
      provides: "relay-side reconnect gating against durable device-session validity"
      contains: "device_session_expired"
  key_links:
    - from: "apps/relay/src/routes/ws-browser.ts"
      to: "packages/db/src/repositories/device-sessions.ts"
      via: "browser upgrade validation checks durable device-session state before attach"
      pattern: "findDeviceSessionForPrincipal"
    - from: "apps/relay/src/routes/ws-bridge.ts"
      to: "apps/relay/src/browser/session-router.ts"
      via: "bridge disconnects fan terminal end events into active browser sessions"
      pattern: "session.ended"
    - from: "apps/web/lib/live-session/transport.ts"
      to: "apps/web/components/session/session-ended-card.tsx"
      via: "terminal reasons stop reconnect and render explicit ended/revoked UI"
      pattern: "SessionEndedCard"
---

<objective>
Finish reconnect, resume, and bridge-health safety behavior for Phase 4.

Purpose: After durable device-session state exists, the remaining safety work is to distinguish transient reconnect from terminal trust loss. This plan hardens relay/browser reconnect gating, fans bridge-loss end events to the UI, renders explicit ended/revoked phone states, and adds regression tests that preserve the Codex-only control surface.

Output: Terminal end-reason contracts, relay/browser reconnect validity checks, bridge-health fanout, explicit revoked/ended UI states, and automated coverage for reconnect/backfill and command-surface safety.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-approval-audit-device-safety/04-CONTEXT.md
@.planning/phases/04-approval-audit-device-safety/04-RESEARCH.md
@.planning/phases/04-approval-audit-device-safety/04-VALIDATION.md
@.planning/phases/04-approval-audit-device-safety/04-01-PLAN.md
@.planning/phases/03-live-remote-ui-control/03-CONTEXT.md
@.planning/phases/03-live-remote-ui-control/03-UI-SPEC.md
@packages/protocol/src/live-session.ts
@apps/web/lib/live-session/transport.ts
@apps/web/lib/live-session/reducer.ts
@apps/web/lib/live-session/session-model.ts
@apps/web/app/session/[sessionId]/session-shell.tsx
@apps/relay/src/routes/ws-browser.ts
@apps/relay/src/routes/ws-bridge.ts
@apps/relay/src/browser/session-router.ts
@apps/bridge/src/daemon/bridge-daemon.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define terminal end reasons and block reconnect for revoked or expired device sessions</name>
  <files>packages/protocol/src/live-session.ts, apps/relay/src/routes/ws-browser.ts</files>
  <read_first>packages/protocol/src/live-session.ts, apps/relay/src/routes/ws-browser.ts, packages/db/src/repositories/device-sessions.ts, docs/adr/0001-phase-1-trust-boundary.md, .planning/phases/04-approval-audit-device-safety/04-RESEARCH.md</read_first>
  <action>Update `packages/protocol/src/live-session.ts` so `LiveSessionEndedEventSchema.reason` is an explicit `z.enum` containing these exact values: `device_session_revoked`, `device_session_expired`, `bridge_unavailable`, `codex_process_exited`, and `detached`. Keep `SessionCommandSchema` limited to `prompt`, `steer`, `approval`, and `interrupt`; do not add any `shell`, `terminal`, or arbitrary command kind. Update `apps/relay/src/routes/ws-browser.ts` so browser upgrade validation loads the durable device-session row with `findDeviceSessionForPrincipal` after ws-ticket verification and rejects revoked/expired rows with exact errors `device_session_revoked` or `device_session_expired` before any browser attach proceeds.</action>
  <verify>rg -n 'device_session_revoked|device_session_expired|bridge_unavailable|codex_process_exited|detached|kind: z\\.literal\\(\"prompt\"\\)|kind: z\\.literal\\(\"steer\"\\)|kind: z\\.literal\\(\"approval\"\\)|kind: z\\.literal\\(\"interrupt\"\\)|findDeviceSessionForPrincipal' packages/protocol/src/live-session.ts apps/relay/src/routes/ws-browser.ts</verify>
  <acceptance_criteria>
    - `packages/protocol/src/live-session.ts` contains the exact reason `device_session_revoked`
    - `packages/protocol/src/live-session.ts` contains the exact reason `bridge_unavailable`
    - `packages/protocol/src/live-session.ts` still contains only `prompt`, `steer`, `approval`, and `interrupt` command kinds
    - `apps/relay/src/routes/ws-browser.ts` references `findDeviceSessionForPrincipal`
    - `apps/relay/src/routes/ws-browser.ts` contains `device_session_expired`
  </acceptance_criteria>
  <done>Reconnect can now distinguish terminal trust loss from transient failures at the protocol and relay ingress layers.</done>
</task>

<task type="auto">
  <name>Task 2: Fan bridge-health end events to the browser and render explicit terminal phone states</name>
  <files>apps/relay/src/browser/browser-registry.ts, apps/relay/src/browser/session-router.ts, apps/relay/src/routes/ws-bridge.ts, apps/web/lib/live-session/session-model.ts, apps/web/lib/live-session/reducer.ts, apps/web/lib/live-session/transport.ts, apps/web/app/session/[sessionId]/session-shell.tsx, apps/web/components/session/reconnect-banner.tsx, apps/web/components/session/session-ended-card.tsx, apps/web/components/session/composer.tsx, apps/web/components/session/approval-card.tsx</files>
  <read_first>apps/bridge/src/daemon/bridge-daemon.ts, apps/relay/src/routes/ws-bridge.ts, apps/relay/src/browser/session-router.ts, apps/web/lib/live-session/transport.ts, apps/web/lib/live-session/reducer.ts, apps/web/app/session/[sessionId]/session-shell.tsx, .planning/phases/03-live-remote-ui-control/03-UI-SPEC.md</read_first>
  <action>Extend `apps/relay/src/browser/browser-registry.ts` and `apps/relay/src/browser/session-router.ts` so bridge close/error and `session.ended` notifications propagate terminal browser events to every affected browser socket before those sockets are closed. Update `apps/relay/src/routes/ws-bridge.ts` so an unexpected bridge disconnect emits `session.ended` with the exact reason `bridge_unavailable` for attached sessions instead of silently dropping the browser. Add `terminalState` and `terminalReason` fields to `apps/web/lib/live-session/session-model.ts`, handle them in `apps/web/lib/live-session/reducer.ts`, and update `apps/web/lib/live-session/transport.ts` so terminal reasons stop the reconnect loop while transient closes still schedule reconnect. Create `apps/web/components/session/session-ended-card.tsx` and render it from `apps/web/app/session/[sessionId]/session-shell.tsx` with the exact headings `Device revoked` and `Session ended on your laptop`. When `terminalState` is set, disable `Send Prompt`, `Steer`, `Interrupt`, and approval actions in `composer.tsx` and `approval-card.tsx`. Keep `ReconnectBanner` reserved for nonterminal reconnectable states only.</action>
  <verify>rg -n 'bridge_unavailable|terminalState|terminalReason|Device revoked|Session ended on your laptop|Send Prompt|Steer|Interrupt|disabled|ReconnectBanner|session\\.ended' apps/relay/src/browser/browser-registry.ts apps/relay/src/browser/session-router.ts apps/relay/src/routes/ws-bridge.ts apps/web/lib/live-session/session-model.ts apps/web/lib/live-session/reducer.ts apps/web/lib/live-session/transport.ts apps/web/app/session/[sessionId]/session-shell.tsx apps/web/components/session/reconnect-banner.tsx apps/web/components/session/session-ended-card.tsx apps/web/components/session/composer.tsx apps/web/components/session/approval-card.tsx</verify>
  <acceptance_criteria>
    - `apps/web/components/session/session-ended-card.tsx` contains the exact heading `Device revoked`
    - `apps/web/components/session/session-ended-card.tsx` contains the exact heading `Session ended on your laptop`
    - `apps/web/lib/live-session/session-model.ts` contains `terminalState`
    - `apps/web/lib/live-session/transport.ts` contains `device_session_revoked`
    - `apps/relay/src/routes/ws-bridge.ts` contains the exact reason `bridge_unavailable`
    - `apps/web/components/session/composer.tsx` disables controls when `terminalState` is present
  </acceptance_criteria>
  <done>The phone UI now reacts honestly to bridge loss and trust revocation instead of reconnecting forever.</done>
</task>

<task type="auto">
  <name>Task 3: Add reconnect, terminal-state, and command-surface regression coverage</name>
  <files>apps/relay/tests/unit/ws-browser-reconnect.test.ts, apps/relay/tests/unit/session-router-safety.test.ts, apps/web/tests/unit/session-shell-safety.test.tsx, apps/web/tests/device-safety-mobile.spec.ts</files>
  <read_first>apps/relay/src/routes/ws-browser.ts, apps/relay/src/routes/ws-bridge.ts, apps/web/lib/live-session/transport.ts, apps/web/components/session/session-ended-card.tsx, apps/web/tests/live-session-mobile.spec.ts, .planning/phases/04-approval-audit-device-safety/04-VALIDATION.md</read_first>
  <action>Create `apps/relay/tests/unit/ws-browser-reconnect.test.ts` to assert revoked or expired device sessions are rejected during browser reconnect and that bridge disconnect fanout emits `bridge_unavailable`. Create `apps/relay/tests/unit/session-router-safety.test.ts` to assert the live command router rejects any unknown command kind such as `{ kind: "shell" }`. Create `apps/web/tests/unit/session-shell-safety.test.tsx` to assert transient reconnect keeps the timeline visible, terminal reasons render the correct ended card, and controls/approval buttons are disabled once the session is revoked or ended. Create `apps/web/tests/device-safety-mobile.spec.ts` as a phone-sized smoke spec that verifies `Revoke device`, `Device revoked`, and `Session ended on your laptop` remain readable without desktop-only layout assumptions.</action>
  <verify>rg -n 'device_session_revoked|device_session_expired|bridge_unavailable|shell|Device revoked|Session ended on your laptop|Revoke device|describe\\(|it\\(|test\\(' apps/relay/tests/unit/ws-browser-reconnect.test.ts apps/relay/tests/unit/session-router-safety.test.ts apps/web/tests/unit/session-shell-safety.test.tsx apps/web/tests/device-safety-mobile.spec.ts</verify>
  <acceptance_criteria>
    - `apps/relay/tests/unit/ws-browser-reconnect.test.ts` contains `device_session_revoked`
    - `apps/relay/tests/unit/ws-browser-reconnect.test.ts` contains `bridge_unavailable`
    - `apps/relay/tests/unit/session-router-safety.test.ts` contains `shell`
    - `apps/web/tests/unit/session-shell-safety.test.tsx` contains `Device revoked`
    - `apps/web/tests/unit/session-shell-safety.test.tsx` contains `Session ended on your laptop`
    - `apps/web/tests/device-safety-mobile.spec.ts` contains `Revoke device`
  </acceptance_criteria>
  <done>Reconnect and terminal-session safety are now protected by automated regression coverage, including a boundary test for the command surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `vitest run --project phase-01-unit apps/relay/tests/unit/ws-browser-reconnect.test.ts apps/relay/tests/unit/session-router-safety.test.ts` succeeds
- [ ] `vitest run --project phase-03-web apps/web/tests/unit/session-shell-safety.test.tsx` succeeds
- [ ] `npx playwright test apps/web/tests/device-safety-mobile.spec.ts` succeeds or is explicitly documented behind a live-session smoke gate
- [ ] `rg -n 'device_session_revoked|bridge_unavailable|Session ended on your laptop|Device revoked' packages/protocol/src/live-session.ts apps/web/lib/live-session/transport.ts apps/web/components/session/session-ended-card.tsx apps/relay/src/routes/ws-bridge.ts` returns matches
</verification>

<success_criteria>

- Valid device sessions reconnect after transient drops without re-pairing
- Revoked, expired, bridge-ended, or Codex-ended sessions render explicit terminal states
- The reconnect loop stops when trust is gone or the upstream session is over
- Phase 4 preserves the Codex-only command surface and does not introduce a general remote shell
</success_criteria>

<output>
After completion, create `.planning/phases/04-approval-audit-device-safety/04-03-SUMMARY.md`
</output>
