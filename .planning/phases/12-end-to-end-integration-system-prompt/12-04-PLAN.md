---
phase: 12-end-to-end-integration-system-prompt
plan: 04
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
  - 12-03
files_modified:
  - apps/achilles/src/main/session.ts
  - apps/achilles/src/main/session.test.ts
  - apps/achilles/src/main/key-source.ts
  - apps/achilles/src/main/key-source.test.ts
  - apps/achilles/src/main/mock-loop-clients.ts
  - apps/achilles/src/main/index.ts
  - apps/achilles/src/main/ipc-bridge.ts
  - apps/achilles/src/main/ipc-bridge.test.ts
  - apps/achilles/src/main/store.ts
  - apps/achilles/src/main/store.test.ts
  - apps/achilles/src/main/state-machine.ts
  - apps/achilles/src/main/state-machine.test.ts
  - apps/achilles/test/integration/end-to-end-loop.test.ts
autonomous: true
requirements:
  - LOOP-05
tags:
  - achilles
  - orchestrator
  - session
  - half-duplex
  - integration
  - loop-05
  - prompt-05-enforcement

must_haves:
  truths:
    - "apps/achilles/src/main/session.ts ships createSession({deps}): AchillesSession that orchestrates one per-utterance voice loop: STT commit -> sandwich-defence wrap -> claude-code-bridge.send -> ack extraction -> TTS playback -> spoken-summary extraction -> TTS playback -> 300 ms debounce -> idle"
    - "On transition to 'speaking', the orchestrator calls micCapture.pauseFrameDelivery() AND closes the STT client write path (calls stt.close()) so mic frames stop reaching ElevenLabs (half-duplex per CONTEXT.md + PITFALLS #2)"
    - "After the last TTS chunk drains (onPlaybackComplete + onDrained), the orchestrator schedules a 300 ms debounce timer; on timer fire it transitions the state machine to 'idle' (NOT 'listening' — user must explicitly press hotkey for next turn, per CONTEXT.md half-duplex section)"
    - "Authoritative outcome path (PROMPT-05 + PITFALLS #17): the orchestrator reads session.outcome from @achilles/claude-code-bridge AFTER the bridge emits process_exit; when outcome.kind === 'failure', the spoken-summary text is OVERRIDDEN with `I ran into a problem. ${humanReason}` regardless of the LLM's emitted <spoken-summary> body — verified against MockLoopClients' failure_outcome fixture"
    - "Sandwich-defence is applied to every transcript before it crosses into claude-code-bridge.send — the orchestrator calls wrapTranscript(transcript) and then session.send(wrapped); detectManipulationTokens logs a warning when matched but does NOT strip"
    - "Pre-TTS normalisation is applied to BOTH the ack text AND the spoken-summary body before being routed to TTS — the orchestrator calls normaliseForTts(text) and feeds .normalised to the TTS client; the report counts are logged with the [achilles] prefix without exposing the redacted content"
    - "The ElevenLabs API key surface ships in apps/achilles/src/main/key-source.ts: readApiKey() reads from electron-store via safeStorage first; falls back to process.env.ELEVENLABS_API_KEY when the store value is absent; throws a typed MissingApiKeyError when both are absent"
    - "mock-loop-clients.ts provides deterministic fake STT, TTS, and Claude clients for end-to-end testing when MOCK_LOOP=1 env var is set; the integration test at test/integration/end-to-end-loop.test.ts exercises the full loop without live ElevenLabs or real Claude Code"
    - "apps/achilles/src/main/index.ts replaces the Plan 11-01 mock timers with the real session orchestrator while preserving the existing state-machine reducer + IPC bridge surface; createMockStateController is replaced by a new createSessionStateController that drives transitions from voice-loop signals instead of fixture timers"
    - "The state-machine reducer gains 3 new event tags: TTS_PLAYBACK_DRAINED (replaces MOCK_PLAYBACK_DONE in production), CLAUDE_RESULT_READY (drives processing -> speaking), STT_COMMITTED (drives listening -> processing); MOCK_* event tags stay for unit-test back-compat but are no longer dispatched in the production wiring"
    - "ipc-bridge.ts adds the 6 new Phase 12 IPC handlers: TTS_CHUNK forwarding (main -> renderer), TTS_PLAYBACK_COMPLETE (renderer -> main), UTTERANCE_COMMIT (renderer -> main), MIC_FRAME (renderer -> main), STT_TOKEN_REQUEST (renderer -> main), STT_TOKEN (main -> renderer)"
    - "All tests pass under the phase-12-unit Vitest project added in Plan 12-01; integration test runs only when MOCK_LOOP=1 is set"
  artifacts:
    - path: "apps/achilles/src/main/session.ts"
      provides: "createSession({deps}): AchillesSession — per-utterance lifecycle orchestrator composing voice-stt + claude-code-bridge + voice-tts + sandwich-defence + normalisation"
      exports: ["createSession", "AchillesSession", "AchillesSessionDeps", "SPEAKING_DEBOUNCE_MS"]
      min_lines: 200
    - path: "apps/achilles/src/main/session.test.ts"
      provides: "Tests covering the orchestrator state transitions, half-duplex gating, authoritative outcome path, sandwich-defence wiring, normalisation wiring, error paths"
      min_lines: 200
    - path: "apps/achilles/src/main/key-source.ts"
      provides: "readApiKey({store, env}): string + MissingApiKeyError — main-process API key source with safeStorage-via-store first + env fallback"
      exports: ["readApiKey", "MissingApiKeyError"]
    - path: "apps/achilles/src/main/key-source.test.ts"
      provides: "Tests for store-present, env-fallback, both-absent, both-present (store wins) paths"
      min_lines: 50
    - path: "apps/achilles/src/main/mock-loop-clients.ts"
      provides: "createMockStt(opts), createMockTts(opts), createMockClaude(opts) — deterministic fakes implementing the same interfaces as the real clients, used by the integration test under MOCK_LOOP=1"
      exports: ["createMockStt", "createMockTts", "createMockClaude"]
    - path: "apps/achilles/test/integration/end-to-end-loop.test.ts"
      provides: "MOCK_LOOP=1 gated end-to-end assertion: a synthetic mic frame stream -> mock STT commit -> mock Claude with successful + failing fixtures -> ack + spoken-summary -> mock TTS -> playback complete + 300 ms debounce -> idle"
      min_lines: 150
  key_links:
    - from: "apps/achilles/src/main/session.ts"
      to: "packages/achilles-skill (Plan 12-01) companionPromptPath"
      via: "import { companionPromptPath } from '@achilles/achilles-skill'"
      pattern: "companionPromptPath"
    - from: "apps/achilles/src/main/session.ts"
      to: "apps/achilles/src/main/sandwich-defence.ts (Plan 12-02) wrapTranscript + detectManipulationTokens"
      via: "import { wrapTranscript, detectManipulationTokens } from './sandwich-defence.js'"
      pattern: "wrapTranscript"
    - from: "apps/achilles/src/main/session.ts"
      to: "apps/achilles/src/main/normalisation.ts (Plan 12-02) normaliseForTts"
      via: "import { normaliseForTts } from './normalisation.js'"
      pattern: "normaliseForTts"
    - from: "apps/achilles/src/main/session.ts"
      to: "packages/claude-code-bridge createClaudeSession + extractAck + extractSpokenSummary + deriveOutcome"
      via: "import from '@achilles/claude-code-bridge'"
      pattern: "createClaudeSession"
    - from: "apps/achilles/src/main/session.ts"
      to: "packages/voice-tts createTtsStreamClient"
      via: "import { createTtsStreamClient } from '@achilles/voice-tts'"
      pattern: "createTtsStreamClient"
    - from: "apps/achilles/src/main/index.ts"
      to: "apps/achilles/src/main/session.ts"
      via: "createSessionStateController replaces createMockStateController"
      pattern: "createSession"

user_setup:
  - service: "ElevenLabs"
    why: "Voice loop uses ElevenLabs STT (Scribe v2 Realtime) and TTS (Flash v2.5)"
    env_vars:
      - name: "ELEVENLABS_API_KEY"
        source: "ElevenLabs Dashboard -> Profile -> API Keys (env fallback only; Phase 13 first-run wizard collects via safeStorage)"
    dashboard_config: []
---

<objective>
Plan 12-04 ships the main-process orchestrator at `apps/achilles/src/main/session.ts` that composes every Phase 09 / 10 / 11 / 12-01 / 12-02 / 12-03 deliverable into the real voice loop. It owns LOOP-05's authoritative half — playback ordering, half-duplex mic gating during speaking, the 300 ms debounce on drain, and the authoritative outcome override that converts a Claude failure into an honest "I ran into a problem" spoken summary (PROMPT-05 runtime enforcement; PROMPT-04 is enforced by Plan 12-03's playback-queue surface).

This plan converts Plan 11-01's mock-timer state controller into a real session controller wired to:
- @achilles/voice-stt (renderer-side client; orchestrator owns the lifecycle via the new IPC channels added in Plan 12-03)
- @achilles/claude-code-bridge createClaudeSession (subprocess spawn against companionPromptPath from Plan 12-01)
- @achilles/voice-tts createTtsStreamClient (main-side stream; chunks fan out via IPC_TTS_CHUNK to renderer playback-queue)
- apps/achilles/src/main/sandwich-defence.ts wrapTranscript + detectManipulationTokens (Plan 12-02)
- apps/achilles/src/main/normalisation.ts normaliseForTts (Plan 12-02)

It adds 3 new event tags to the state machine reducer for production wiring while preserving the MOCK_* tags for back-compat. It extends ipc-bridge.ts with the 6 new Phase 12 IPC handlers. It introduces apps/achilles/src/main/key-source.ts as the single read point for the ElevenLabs API key. It introduces apps/achilles/src/main/mock-loop-clients.ts so the MOCK_LOOP=1 integration test exercises the full loop deterministically without ElevenLabs or real Claude Code.

Purpose: LOOP-05 mandates TTS chunks play in arrival order via the renderer's AudioContext (Plan 12-03 delivers the renderer side); the mic is gated during playback (half-duplex); the mic re-enables 300 ms after the last chunk drains. The orchestrator is the single owner of these signals — it sequences STT close + mic-frame pause + TTS open in lockstep with the state-machine transitions.

Output: One new orchestrator module + one key source + one mock-clients module + extensions to 4 existing main-process files + 1 integration test gated by MOCK_LOOP=1. All tests run under the phase-12-unit Vitest project; the integration test runs under the same project but skips automatically when MOCK_LOOP is unset.

Phase boundary: Plan 12-04 does NOT modify the renderer audio modules (Plan 12-03 owns those). It does NOT modify the prompt body or the achilles-skill package (Plan 12-01). It does NOT modify the sandwich-defence or normalisation modules (Plan 12-02). It does NOT ship the npm CLI, install-skill subcommand, signed installers, or first-run wizard (Phase 13). It does NOT ship a latency probe or stuck-thinking timeout (Phase 14). It does NOT touch the Phase 11 Waveform component implementation (Plan 12-03's AnalyserBinding satisfies the existing AnalyserLike interface). NO real Electron launch in CI; NO live ElevenLabs in CI; NO real Claude Code in CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-end-to-end-integration-system-prompt/12-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@packages/claude-code-bridge/src/session.ts
@packages/claude-code-bridge/src/outcome.ts
@packages/claude-code-bridge/src/extractor.ts
@packages/voice-stt/src/index.ts
@packages/voice-stt/src/realtime-client.ts
@packages/voice-tts/src/index.ts
@packages/voice-tts/src/stream-client.ts
@packages/voice-tts/src/sequence-buffer.ts
@apps/achilles/src/main/index.ts
@apps/achilles/src/main/ipc-bridge.ts
@apps/achilles/src/main/state-machine.ts
@apps/achilles/src/main/store.ts
@apps/achilles/src/main/permission.ts
@apps/achilles/src/shared/constants.ts
@apps/achilles/src/shared/ipc-schemas.ts

<interfaces>
<!-- Phase 10 contracts the orchestrator composes against -->

From @achilles/claude-code-bridge:
- createClaudeSession(opts: {systemPromptFile: string, resumeSessionId?: string, cwd?: string, env?: NodeJS.ProcessEnv}, deps?): ClaudeSession
- ClaudeSession surface: sessionId, lastTurnText, outcome, events$, send(text), close(), cancel(), _internal
- extractAck(streamText): string | null  -- first sentence, <=120 chars, null when no terminator
- extractSpokenSummary(streamText): string | null  -- contents of FIRST <spoken-summary>...</spoken-summary>, null when markers absent, "" when present-but-empty
- deriveOutcome({exitCode, toolErrors, cancelled}): ClaudeOutcome
- ClaudeOutcome variants:
    {kind: "success"}
    {kind: "failure", reason: "exit_code" | "tool_error" | "cancelled" | "spawn_error", details?: string}
- ProcessExitEvent: {type: "process_exit", exit_code: number | null, signal: NodeJS.Signals | null}

From @achilles/voice-tts:
- createTtsStreamClient(opts: {keySource: KeySource, voiceId: string, outputFormat?: TtsOutputFormat}): TtsStreamClient
- TtsStreamClient surface: open(), appendText(text), flush(), close(), events$ (async iterable of TtsEvent — TtsChunk | TtsStreamComplete | TtsErrorEvent)
- TtsChunk: {seq: number, mime: "audio/mpeg" | "audio/pcm", bytes: Uint8Array, isFinal: boolean}
- SequenceBuffer is main-side; the orchestrator wires sequence-numbered chunks straight to the IPC fan-out

From @achilles/voice-stt:
- The orchestrator drives the renderer client via IPC; the renderer holds the WebSocket (per Phase 09 architecture)
- Orchestrator surface for STT lifecycle: send STT_TOKEN (after minting via @achilles/voice-stt/token-mint subpath) when renderer requests STT_TOKEN_REQUEST; mark mic frames received via MIC_FRAME as forwarded to STT; when state transitions to 'speaking' the orchestrator broadcasts a "close-stt" signal (via a new IPC channel? — see action item below)

From apps/achilles/src/main/state-machine.ts:
- The existing reducer + createMockStateController
- AchillesEvent union: HOTKEY_PRESS | HOTKEY_RELEASE | CIRCLE_CLICK | MOCK_VAD_COMMIT | MOCK_PROCESSING_COMPLETE | MOCK_PLAYBACK_DONE | INJECT_ERROR | ERROR_DISMISS | PERMISSION_CHANGED

From apps/achilles/src/main/store.ts:
- The existing electron-store wrapper with safeStorage encryption
- We add: readElevenlabsApiKey() : string | null and writeElevenlabsApiKey(key: string): void following the existing pattern with safeStorage encryption + plaintext fallback warning

From apps/achilles/src/main/ipc-bridge.ts:
- The wireIpcBridge composition with the existing 5 channels
- The bridge handle's broadcastPermissionState(state) pattern

Locked debounce constant per CONTEXT.md half-duplex section:
- SPEAKING_DEBOUNCE_MS = 300  // 300 ms after last TTS chunk drains before transitioning to idle

The phase-12-unit Vitest project's include glob (set in Plan 12-01) ALREADY covers apps/achilles/src/main/session.test.ts, apps/achilles/src/main/key-source.test.ts, and (extended below) apps/achilles/src/main/ipc-bridge.test.ts. The integration glob apps/achilles/test/integration/**/*.test.ts must be appended to phase-12-unit project's include array; Task 1 reads Plan 12-01's vitest.workspace.ts state and confirms the integration glob is present before relying on it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: key-source.ts + state-machine event extensions + electron-store API key methods</name>
  <files>apps/achilles/src/main/key-source.ts, apps/achilles/src/main/key-source.test.ts, apps/achilles/src/main/state-machine.ts, apps/achilles/src/main/state-machine.test.ts, apps/achilles/src/main/store.ts, apps/achilles/src/main/store.test.ts</files>
  <behavior>
    key-source tests:
    - Test K1: readApiKey({store: storeWithKey, env: {}}) returns the store's value (store wins).
    - Test K2: readApiKey({store: storeWithoutKey, env: {ELEVENLABS_API_KEY: "env-key"}}) returns "env-key" (env fallback).
    - Test K3: readApiKey({store: storeWithoutKey, env: {}}) throws MissingApiKeyError with message containing "ELEVENLABS_API_KEY".
    - Test K4: MissingApiKeyError is a typed Error subclass with name === "MissingApiKeyError".
    - Test K5: readApiKey logs a single [achilles] info-level message indicating source (store vs env) without logging the key itself.

    state-machine extensions:
    - Test S1: new event tag STT_COMMITTED transitions listening -> processing (mirrors MOCK_VAD_COMMIT for production).
    - Test S2: new event tag CLAUDE_RESULT_READY transitions processing -> speaking (mirrors MOCK_PROCESSING_COMPLETE for production).
    - Test S3: new event tag TTS_PLAYBACK_DRAINED transitions speaking -> idle (mirrors MOCK_PLAYBACK_DONE for production).
    - Test S4: MOCK_* event tags still work — back-compat preserved (the Phase 11 Playwright e2e specs still drive transitions through the MOCK_* path).
    - Test S5: a new event tag CLAUDE_FAILURE_OVERRIDE transitions processing -> speaking AND sets a transient flag on the reducer's return shape indicating the spoken summary should be the failure override (NOT the LLM body). Implementation: the reducer's return type stays AchillesState (a string literal) for back-compat; the failure-override signal is communicated via a SEPARATE per-event side payload that the orchestrator (Plan 12-04) tracks externally. State-machine.ts thus gains the event tag in the union but does NOT change the reducer's return type — the orchestrator owns the "next spoken summary is the failure override" flag in session.ts state.

    store extensions:
    - Test ST1: store.readElevenlabsApiKey() returns null when the key has never been written.
    - Test ST2: store.writeElevenlabsApiKey("test-key") + store.readElevenlabsApiKey() round-trips the value via safeStorage encryption.
    - Test ST3: when safeStorage is unavailable, the warning is emitted exactly once per process (matches the existing windowPosition/hotkeyKey warning pattern in Plan 11-01).
    - Test ST4: store.readElevenlabsApiKey returns null on safeStorage.decryptString throw (corrupted blob); the orchestrator falls back to env per K2 path.
  </behavior>
  <action>
    Create apps/achilles/src/main/key-source.ts:

    export class MissingApiKeyError extends Error {
      constructor() {
        super("ELEVENLABS_API_KEY is missing — set the env var or use the Phase 13 init wizard to store it via safeStorage.");
        this.name = "MissingApiKeyError";
      }
    }

    export interface KeySourceDeps {
      store: { readElevenlabsApiKey(): string | null };
      env: NodeJS.ProcessEnv;
      logger?: (msg: string) => void;
    }

    export function readApiKey(deps: KeySourceDeps): string {
      const fromStore = deps.store.readElevenlabsApiKey();
      const log = deps.logger ?? ((msg) => console.error(msg));
      if (fromStore !== null && fromStore.length > 0) {
        log("[achilles] elevenlabs api key sourced from store");
        return fromStore;
      }
      const fromEnv = deps.env.ELEVENLABS_API_KEY;
      if (fromEnv !== undefined && fromEnv.length > 0) {
        log("[achilles] elevenlabs api key sourced from env");
        return fromEnv;
      }
      throw new MissingApiKeyError();
    }

    Create apps/achilles/src/main/key-source.test.ts implementing K1-K5. Use vitest's spy on the logger injection seam; assert log messages never contain the literal key string.

    Modify apps/achilles/src/main/state-machine.ts:

    Extend the AchillesEvent union with 4 new tags (placed AFTER the existing MOCK_PLAYBACK_DONE entry):
      | { type: "STT_COMMITTED"; transcript: string }
      | { type: "CLAUDE_RESULT_READY" }
      | { type: "TTS_PLAYBACK_DRAINED" }
      | { type: "CLAUDE_FAILURE_OVERRIDE"; reason: string }

    Extend the reducer's switch statement with the 4 new cases:
      case "STT_COMMITTED": if (current === "listening") return "processing"; return current;
      case "CLAUDE_RESULT_READY": if (current === "processing") return "speaking"; return current;
      case "TTS_PLAYBACK_DRAINED": if (current === "speaking") return "idle"; return current;
      case "CLAUDE_FAILURE_OVERRIDE": if (current === "processing") return "speaking"; return current;
      // The orchestrator inspects the event payload separately to decide whether to override the LLM body.

    Update the reducer's behavior-table JSDoc to document the new tags. Keep the MOCK_* tags' behavior intact.

    Extend state-machine.test.ts with tests S1-S5. Do NOT delete or modify any existing Plan 11-01 test case.

    Modify apps/achilles/src/main/store.ts:

    Add two new public methods to the AchillesStore interface and the implementation:
      readElevenlabsApiKey(): string | null
      writeElevenlabsApiKey(key: string): void

    Implementation follows the existing windowPosition pattern: safeStorage.isEncryptionAvailable() check; encryptString -> store.set("elevenlabsApiKey", encrypted.toString("base64")); read = store.get -> Buffer.from(b64, "base64") -> decryptString. On safeStorage unavailable, the existing "[achilles] safeStorage unavailable; persisting plaintext (SAFE-01 follow-up in Phase 12)" warning is reused (do NOT emit a second warning per write — the once-per-process gate from Plan 11-01 is shared). On decryptString throw, return null and log "[achilles] elevenlabs key decryption failed; falling back to env" with no key content in the log.

    Extend store.test.ts with tests ST1-ST4. Use the existing safeStorage injection seam pattern from Plan 11-01.

    NO emojis. NO console.log of key content. The logger seam in key-source.ts defaults to console.error with the [achilles] prefix.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff && npx vitest run --project phase-12-unit apps/achilles/src/main/key-source.test.ts apps/achilles/src/main/state-machine.test.ts apps/achilles/src/main/store.test.ts</automated>
  </verify>
  <done>
    key-source.ts ships with K1-K5 passing tests. state-machine.ts adds 4 new event tags (STT_COMMITTED, CLAUDE_RESULT_READY, TTS_PLAYBACK_DRAINED, CLAUDE_FAILURE_OVERRIDE) with their reducer cases; MOCK_* tags intact. store.ts adds readElevenlabsApiKey / writeElevenlabsApiKey with safeStorage encryption + corruption-tolerance. All existing Plan 11-01 tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: session.ts orchestrator + mock-loop-clients.ts deterministic fakes</name>
  <files>apps/achilles/src/main/session.ts, apps/achilles/src/main/session.test.ts, apps/achilles/src/main/mock-loop-clients.ts</files>
  <behavior>
    Behaviour map for session.ts (each maps to a test):

    Lifecycle transitions:
    - Test SE1 (idle -> listening): onHotkeyPress() with permission=granted dispatches HOTKEY_PRESS to the reducer; state becomes "listening"; the orchestrator mints an STT token via opts.deps.mintSttToken() and sends IPC_STT_TOKEN to the renderer.
    - Test SE2 (listening -> processing): onUtteranceCommit({id, text, committedAt}) dispatches STT_COMMITTED(transcript); state becomes "processing"; the orchestrator wraps the transcript via wrapTranscript() and calls bridge.send(wrapped); detectManipulationTokens(text) result is logged when detected.
    - Test SE3 (processing -> speaking): when the Claude bridge emits an assistant_text_delta containing a sentence-terminating ack, the orchestrator extracts via extractAck, normalises via normaliseForTts, opens the TTS stream, and dispatches CLAUDE_RESULT_READY; state becomes "speaking"; the TTS stream's audio_chunk events are fanned out via IPC_TTS_CHUNK; sandwich-defence's mic gate fires: micCapture.pauseFrameDelivery() AND stt.close().
    - Test SE4 (speaking -> idle on drain): when the renderer signals IPC_TTS_PLAYBACK_COMPLETE, the orchestrator schedules a 300 ms (SPEAKING_DEBOUNCE_MS) timer; on timer fire it dispatches TTS_PLAYBACK_DRAINED; state becomes "idle"; micCapture.resumeFrameDelivery() is called.
    - Test SE5 (cancel during speaking): onHotkeyPress() while state === "speaking" calls bridge.cancel(), tts.close(), micCapture.resumeFrameDelivery(), and dispatches a CIRCLE_CLICK event to drive speaking -> idle; the cancelled outcome is observed but NOT spoken (no second TTS run).

    Authoritative outcome path:
    - Test SE6 (PROMPT-05 failure override): with MockClaude configured to emit a "Looking at the failing test. <spoken-summary>I have fixed everything.</spoken-summary>" stream BUT a process_exit with non-zero exit_code (so deriveOutcome returns failure), the orchestrator routes "I ran into a problem. exit_code: 1" (or equivalent locked reason format) to TTS — NOT the LLM's "I have fixed everything" body. The test asserts the TTS appendText was called with the override phrasing.
    - Test SE7 (tool_error override): with MockClaude configured to emit a successful assistant text but a tool_result event carrying is_error:true, deriveOutcome returns failure with reason "tool_error"; the orchestrator routes "I ran into a problem. tool_error" (or the equivalent humanReason). Override fires regardless of LLM body.
    - Test SE8 (success path): with MockClaude configured to emit a successful stream + exit_code:0 + no tool errors, the orchestrator routes the normalised <spoken-summary> body verbatim to TTS — NOT the override phrasing.

    Half-duplex gating:
    - Test SE9 (mic gated during speaking): from "speaking" state, when MIC_FRAME arrives, the orchestrator drops the frame (does NOT forward to stt.write); the frame counter increments a "frames_dropped_during_speaking" telemetry counter on the session handle.
    - Test SE10 (mic resumes after debounce): after TTS_PLAYBACK_DRAINED + 300 ms timer fire, micCapture.resumeFrameDelivery() is called; subsequent MIC_FRAME events from the next listening cycle are forwarded.

    Sandwich-defence wiring:
    - Test SE11: bridge.send is called with the wrapped transcript (the literal DELIM_START + transcript + DELIM_END + REMINDER_LINE shape); raw transcript is NEVER passed to bridge.send.

    Pre-TTS normalisation wiring:
    - Test SE12: with a Claude stream containing ANSI escapes + paths + secrets in the <spoken-summary> body, the orchestrator calls normaliseForTts on the extracted summary; the .normalised text (with the redactions applied) is what reaches tts.appendText; the report counts are logged with the [achilles] prefix.

    Logging discipline:
    - Test SE13: the orchestrator never logs the raw transcript content. The logger spy records only [achilles]-prefixed messages with counts + state names + outcome.kind — no transcript fragments, no key bytes, no Claude assistant content.

    Idempotency:
    - Test SE14: dispose() is idempotent — calling it twice does not throw and does not double-close clients.
  </behavior>
  <action>
    Create apps/achilles/src/main/mock-loop-clients.ts FIRST (no internal deps; session.ts imports types only at the type level):

    Define and export three factories:

      export interface MockSttFixture {
        committedTranscripts: Array<{id: string, text: string, committedAt: number}>;
        emitPartials?: boolean;
      }
      export function createMockStt(fixture: MockSttFixture): {
        emitFrame(frame: Int16Array): void;
        commit(): void;
        close(): void;
        readonly events$: AsyncIterable<{type: "committed", id: string, text: string} | {type: "partial", text: string}>;
      }

      export interface MockClaudeFixture {
        ackText: string;  // first sentence emitted as assistant_text_delta
        spokenSummaryBody: string;  // contents to wrap in <spoken-summary>...</spoken-summary>
        exitCode: number;
        toolErrors?: number;  // count; >0 triggers tool_result events with is_error:true
        deltaChunkSize?: number;  // default 30 chars per delta — exercises the wire-mapper buffering
      }
      export function createMockClaude(fixture: MockClaudeFixture): {
        send(text: string): void;
        readonly events$: AsyncIterable<ClaudeBridgeEvent>;
        readonly outcome: ClaudeOutcome | null;
        cancel(): Promise<ProcessExitEvent>;
        close(): Promise<void>;
      }
      (The shape mirrors ClaudeSession; the body synthesises session_init -> assistant_text_delta(ackText) -> assistant_text_delta(<spoken-summary> chunks) -> assistant_text_done -> tool_result (if toolErrors>0) -> process_exit. deriveOutcome is invoked at exit so .outcome reflects the success/failure status.)

      export interface MockTtsFixture {
        chunksPerSegment?: number;  // default 5 -- the number of TtsChunk events per appendText
        outOfOrderProbability?: number;  // default 0 -- when >0, deliberately scramble seq numbers within a segment
      }
      export function createMockTts(fixture: MockTtsFixture): {
        open(): Promise<void>;
        appendText(text: string): void;
        flush(): Promise<void>;
        close(): Promise<void>;
        readonly events$: AsyncIterable<TtsChunk | TtsStreamComplete>;
      }

    All three factories are pure event generators: no real I/O, no real network, no clock dependence (deterministic). They produce TypedArray bytes via deterministic generators (e.g., chunk N has bytes = Buffer.from(`mock-tts-chunk-${seq}`)).

    Create apps/achilles/src/main/session.ts:

    export const SPEAKING_DEBOUNCE_MS = 300;

    export interface AchillesSessionDeps {
      stateController: { dispatch(event: AchillesEvent): AchillesState; now(): AchillesState; cancelScheduledTransitions(): void };
      claudeFactory: (opts: {systemPromptFile: string, resumeSessionId?: string}) => ClaudeSession;
      ttsFactory: (opts: {voiceId: string}) => TtsStreamClient;
      mintSttToken: () => Promise<{token: string, expiresAt: string}>;
      micCapture: { pauseFrameDelivery(): void; resumeFrameDelivery(): void };
      sendIpc: (channel: string, payload: unknown) => void;
      readApiKey: () => string;
      voiceId: string;
      logger?: (msg: string) => void;
      now?: () => number;
      setTimeoutImpl?: (cb: () => void, ms: number) => unknown;
      clearTimeoutImpl?: (token: unknown) => void;
    }

    export interface AchillesSession {
      onHotkeyPress(): Promise<void>;
      onUtteranceCommit(payload: UtteranceCommitPayload): void;
      onMicFrame(payload: MicFramePayload): void;
      onTtsPlaybackComplete(): void;
      onCancel(): void;
      dispose(): void;
      readonly metrics: { framesDroppedDuringSpeaking: number };
    }

    export function createSession(deps: AchillesSessionDeps): AchillesSession

    Implementation outline:

    1. Internal state: currentClaudeSession: ClaudeSession | null; currentTtsClient: TtsStreamClient | null; debounceToken: unknown | null; pendingFailureOverride: {reason: string, humanReason: string} | null; metrics: {framesDroppedDuringSpeaking: 0}; disposed: false.

    2. onHotkeyPress: read current state; if "idle" dispatch HOTKEY_PRESS to reducer; await mintSttToken; sendIpc(IPC_STT_TOKEN, {token, expiresAt}); if "speaking" run onCancel.

    3. onUtteranceCommit(payload): assert state === "listening"; const manipulation = detectManipulationTokens(payload.text); if (manipulation.detected) log "[achilles] manipulation patterns detected: ${manipulation.matchedPatterns.join(',')}" — log shape only, NOT payload.text; const wrapped = wrapTranscript(payload.text); dispatch STT_COMMITTED(payload.text); currentClaudeSession = claudeFactory({systemPromptFile: companionPromptPath, resumeSessionId: lastSessionId ?? undefined}); currentClaudeSession.send(wrapped); start an async consumer of currentClaudeSession.events$ that drives the ack + summary + outcome path (described below).

    4. Claude event consumer:
       - Accumulate assistant text deltas internally.
       - On every delta, call extractAck(accumulatedText); when it first returns non-null AND TTS has not yet opened, normaliseForTts(ackText), open the TTS stream, appendText(normalised), dispatch CLAUDE_RESULT_READY (transitions processing -> speaking), call micCapture.pauseFrameDelivery() — this is the half-duplex entry gate; also send a new IPC channel? — see below note. The orchestrator drives the renderer-side STT close by sending an IPC signal via the existing IPC_STATE_CHANGED (the renderer's bridge subscriber observes state === "speaking" and the renderer's STT client closes its WebSocket). The orchestrator additionally directly invokes stt-client-close via the IPC bridge; the bridge already broadcasts state, which the renderer consumes to drive its own STT lifecycle. No new IPC channel is added — state-driven coordination is sufficient.
       - On process_exit event, compute outcome via the ClaudeSession's .outcome getter; when outcome.kind === "failure", construct humanReason = `${outcome.reason}${outcome.details ? ': ' + outcome.details : ''}`; the spoken summary text becomes `I ran into a problem. ${humanReason}` (the locked override). Otherwise extract via extractSpokenSummary(accumulatedText) — when null OR empty, fall back to the LLM's lastTurnText capped at 40 words; when present, normalise via normaliseForTts and route to TTS.
       - tts.appendText(normalisedSummary); tts.flush(); the tts client's events$ drives IPC_TTS_CHUNK fan-out (see step 5).

    5. TTS chunk consumer: an async iterator over currentTtsClient.events$ that calls sendIpc(IPC_TTS_CHUNK, {seq, mime, bytes: chunk.bytes.buffer.slice(...), isFinal}) for each chunk. The buffer is sliced so the IPC payload carries only the chunk's bytes (no over-read).

    6. onTtsPlaybackComplete: schedule a SPEAKING_DEBOUNCE_MS timer via setTimeoutImpl; on fire, dispatch TTS_PLAYBACK_DRAINED, call micCapture.resumeFrameDelivery(), clear pendingFailureOverride.

    7. onMicFrame: if state === "speaking" or "processing", drop the frame (metrics.framesDroppedDuringSpeaking++); otherwise forward (in the production wiring this means sending the frame to the renderer STT client via the existing renderer-side voice-stt client — for the Phase 12 orchestrator's purposes "forward" means do nothing because the renderer holds the STT client; the orchestrator's drop-during-speaking is the only gating action it owns. The MIC_FRAME IPC channel exists for symmetry but the renderer side calls the STT client directly without round-tripping through main. The MIC_FRAME channel is used only for the optional MOCK_LOOP=1 path where main+renderer share-process testing routes frames through main.).

    8. onCancel: clearTimeoutImpl(debounceToken); currentClaudeSession?.cancel(); currentTtsClient?.close(); micCapture.resumeFrameDelivery(); dispatch CIRCLE_CLICK.

    9. dispose: cancelScheduledTransitions(); currentClaudeSession?.close(); currentTtsClient?.close(); disposed = true; idempotent on second call.

    Logging: every log call uses the injected logger (default console.error with [achilles] prefix). Log statements include state name + outcome.kind + counts. NEVER include payload.text, accumulatedText, TTS bytes, or the API key.

    Create apps/achilles/src/main/session.test.ts implementing SE1-SE14. Each test injects mock-loop-clients fakes via the deps. Use vitest's vi.useFakeTimers for the 300 ms debounce assertion (SE4, SE10). For the failure-override tests SE6/SE7, configure MockClaude with exitCode !== 0 OR toolErrors > 0 and assert tts.appendText was called with a string starting with "I ran into a problem".

    NO emojis. NO console.log of payload content. The session.ts file's JSDoc references PITFALLS #2 (echo loop), #9 (prompt injection), #10 (re-utterance race), #16 (long completion), #17 (hallucinated success), #21 (secrets in TTS) — each pitfall mapped to the orchestrator code path that mitigates it.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff && npx vitest run --project phase-12-unit apps/achilles/src/main/session.test.ts</automated>
  </verify>
  <done>
    session.ts ships >=200 lines orchestrating the full voice loop. mock-loop-clients.ts provides deterministic fakes. session.test.ts: 14 passing tests covering lifecycle transitions, authoritative outcome path (PROMPT-05), half-duplex gating, sandwich-defence wiring, pre-TTS normalisation wiring, logging discipline, dispose idempotency. NO emojis, NO payload-content logs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire orchestrator into ipc-bridge.ts + main/index.ts + MOCK_LOOP integration test</name>
  <files>apps/achilles/src/main/ipc-bridge.ts, apps/achilles/src/main/ipc-bridge.test.ts, apps/achilles/src/main/index.ts, apps/achilles/test/integration/end-to-end-loop.test.ts</files>
  <behavior>
    ipc-bridge extensions:
    - Test IB1: IPC_UTTERANCE_COMMIT inbound parses with parseEnvelope and forwards to session.onUtteranceCommit(payload).
    - Test IB2: IPC_TTS_PLAYBACK_COMPLETE inbound forwards to session.onTtsPlaybackComplete().
    - Test IB3: IPC_MIC_FRAME inbound forwards to session.onMicFrame(payload); the existing sender-identity check is honoured.
    - Test IB4: IPC_STT_TOKEN_REQUEST inbound forwards to session.onHotkeyPress() (the renderer requests a token at start-of-listening; the orchestrator decides whether to mint and broadcast via IPC_STT_TOKEN).
    - Test IB5: outbound IPC_TTS_CHUNK is wired so session's sendIpc(channel, payload) routes through window.webContents.send after parseEnvelope serialisation.
    - Test IB6: outbound IPC_STT_TOKEN similar to IB5.
    - Test IB7: dispose tears down the 4 new inbound handlers (removeAllListeners on each new channel).

    main/index.ts wiring:
    - Replace createMockStateController with createSessionStateController: the controller's broadcast still drives IPC_STATE_CHANGED + amplitude swap, but the underlying transitions are driven by session.ts (not by Phase 11 fixture timers). The createMockStateController surface is kept INTACT for the Plan 11-* unit tests; the production main entry imports the new createSessionStateController which composes createSession + the existing reducer.
    - The IPC bridge composition passes the new session handle into wireIpcBridge.
    - The API key is read via readApiKey({store, env: process.env}); on MissingApiKeyError the main process logs a warning and proceeds in degraded mode (the orchestrator's onHotkeyPress fails fast with a typed error event the bridge translates into IPC_ERROR).

    Integration test (test/integration/end-to-end-loop.test.ts):
    - Test EE1 (MOCK_LOOP=1 only): construct an in-process composition: createMockStt + createMockClaude (success fixture) + createMockTts; instantiate createSession with the mocks; call session.onHotkeyPress(); simulate onUtteranceCommit({id: uuid, text: "refactor the auth module", committedAt: 0}); drain Claude events; assert TTS receives the normalised ack first ("Looking at the auth module."), then receives the normalised spoken-summary; assert IPC_TTS_CHUNK was invoked 5+ times; assert state transitions matched [idle, listening, processing, speaking]; advance the 300 ms debounce timer; assert state ends at idle.
    - Test EE2 (failure path): same as EE1 but MockClaude exitCode=1; assert TTS receives "I ran into a problem. exit_code" prefix; the override fires even though MockClaude's spokenSummaryBody is "I have completed the work."
    - Test EE3 (PROMPT-04 enforcement at the IPC boundary): assert that during the full loop, ONLY IPC_TTS_CHUNK payloads are routed through sendIpc with audio-bearing content; no other IPC channel carries TTS audio.
    - Test EE4 (sandwich-defence wiring): the mock Claude bridge captures what was sent to send(); assert the captured text equals the wrapped form (DELIM_START + transcript + DELIM_END + REMINDER_LINE).
    - All four tests gate via if (!process.env.MOCK_LOOP) it.skip(...) — when MOCK_LOOP is unset the tests skip cleanly, satisfying the CLAUDE.md no-live-network rule by default.
  </behavior>
  <action>
    Modify apps/achilles/src/main/ipc-bridge.ts:

    Extend the WireIpcBridgeOptions interface with `session?: AchillesSession` (optional so existing unit tests can construct the bridge without a session). When session is present, wire 4 new inbound handlers:

      ipcMainRef.on(IPC_UTTERANCE_COMMIT, withSenderCheck(IPC_UTTERANCE_COMMIT, (_event, payload) => {
        try { const parsed = parseEnvelope(IPC_UTTERANCE_COMMIT, payload) as UtteranceCommitPayload; session.onUtteranceCommit(parsed); }
        catch (err) { log(`[achilles] dropping invalid ${IPC_UTTERANCE_COMMIT} payload: ${(err as Error).message}`); }
      }));

    Apply the same pattern for IPC_TTS_PLAYBACK_COMPLETE, IPC_MIC_FRAME, IPC_STT_TOKEN_REQUEST. Extend the dispose() function to removeAllListeners for the 4 new channels.

    Extend ipc-bridge.test.ts with tests IB1-IB7. Inject a mock session handle via opts.session.

    Modify apps/achilles/src/main/index.ts:

    Create a new factory createSessionStateController at the top of the file (or a sibling module if cleaner):
      function createSessionStateController({broadcast, getMode, session}): { dispatch, now, scheduleMockTransitions, cancelScheduledTransitions }
      The dispatch function applies transition() and broadcasts; scheduleMockTransitions is a NO-OP in production (timers are driven by session.ts); cancelScheduledTransitions clears any pending debounce.

    Replace the createMockStateController instantiation in main/index.ts with createSessionStateController. Compose:
      const apiKey = (() => { try { return readApiKey({store, env: process.env}); } catch (err) { console.error(`[achilles] ${(err as Error).message}`); return null; } })();
      const session = apiKey !== null ? createSession({
        stateController,
        claudeFactory: (opts) => createClaudeSession({systemPromptFile: opts.systemPromptFile, resumeSessionId: opts.resumeSessionId}),
        ttsFactory: (opts) => createTtsStreamClient({keySource: () => apiKey, voiceId: opts.voiceId}),
        mintSttToken: () => mintSttToken({apiKey}),
        micCapture: { pauseFrameDelivery: () => window.webContents.send(IPC_STATE_CHANGED, {state: "speaking"}), resumeFrameDelivery: () => { /* state-driven */ } },
        // The mic capture handle lives in the renderer in production; the orchestrator drives gating
        // via state broadcasts so the renderer's mic-capture pauses by reading IPC_STATE_CHANGED.
        sendIpc: (channel, payload) => window.webContents.send(channel, payload),
        readApiKey: () => apiKey,
        voiceId: process.env.ELEVENLABS_VOICE_ID ?? DEFAULT_VOICE_ID,
        logger: (msg) => console.error(msg),
      }) : null;
      wireIpcBridge({ ..., session });

    The DEFAULT_VOICE_ID constant lives in shared/constants.ts — append it as `export const DEFAULT_VOICE_ID = "21m00Tcm4TlvDq8ikWAM"` (the locked v1.2 default ElevenLabs voice id per the locked decisions in REQUIREMENTS.md). If the value collides with an existing constant, use the existing one.

    Import mintSttToken from "@achilles/voice-stt/token-mint" (the separate subpath established in Phase 09 to keep the SAFE-01 boundary intact).

    Create apps/achilles/test/integration/end-to-end-loop.test.ts implementing EE1-EE4:
    - Place a `// @vitest-environment node` docblock at the top.
    - Wrap each `it()` with `(process.env.MOCK_LOOP === undefined ? it.skip : it)` so the suite skips cleanly without MOCK_LOOP.
    - Use vi.useFakeTimers for the debounce assertions.
    - Build the composition: createSession with mock-loop-clients fakes; route sendIpc to a recording spy; assert the spy received the expected channel + payload sequence.
    - For EE3, scan every recorded sendIpc call and assert that audio-bearing payloads ONLY appeared on IPC_TTS_CHUNK (no other channel carried bytes that decoded as audio — the test compares against a simple "contains a `bytes` field with ArrayBuffer instance" predicate).

    Add the integration glob to phase-12-unit's include array if not already present from Plan 12-01:
      "apps/achilles/test/integration/**/*.test.ts"

    NO emojis. NO console.log of payload content. NO real Electron launch. NO real ElevenLabs network. NO real Claude Code spawn.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff && MOCK_LOOP=1 npx vitest run --project phase-12-unit apps/achilles/src/main/ipc-bridge.test.ts apps/achilles/test/integration/end-to-end-loop.test.ts</automated>
  </verify>
  <done>
    ipc-bridge.ts handles the 4 new inbound channels with sender-identity checks + payload validation + dispose teardown. main/index.ts wires the session orchestrator into production, reading the API key with graceful degradation on missing-key. The MOCK_LOOP=1 integration test asserts the full success + failure loops end-to-end without ElevenLabs, without Claude Code, without Electron. MOCK_LOOP unset skips cleanly so default CI is unaffected.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer transcript commit → orchestrator | The transcript crosses from the renderer (untrusted) into the main process. sandwich-defence is applied at this boundary. |
| Claude assistant output → TTS appendText | The LLM's narration crosses from the bridge (untrusted-output) into the outbound TTS stream. pre-TTS normalisation + authoritative outcome override are applied here. |
| API key → orchestrator construction | The ElevenLabs key crosses from electron-store/env into the orchestrator's closure. The key never reaches the renderer; only single-use STT tokens cross via IPC_STT_TOKEN. |
| process_exit → spoken summary decision | The bridge's authoritative outcome flag overrides any LLM-emitted "I finished" — PROMPT-05 + PITFALLS #17. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-19 | Spoofing | rogue IPC payload claiming to be utterance-commit | mitigate | parseEnvelope on every inbound IPC; sender-identity check via withSenderCheck (Plan 11-01 pattern) |
| T-12-20 | Tampering | prompt injection via voice | mitigate | wrapTranscript at the orchestrator entry; detectManipulationTokens warning log (no silent strip); embedded companion.md reinforces contract |
| T-12-21 | Repudiation | "I have finished" hallucination on failed runs | mitigate | session.ts authoritative outcome override fires whenever deriveOutcome returns failure; verified by session.test.ts SE6/SE7 and integration EE2 |
| T-12-22 | Information disclosure | API key in renderer / logs / IPC | mitigate | key-source.ts reads via store + env in main only; renderer receives single-use tokens via IPC_STT_TOKEN; logger spy assertion (SE13) ensures key content never reaches log surface |
| T-12-23 | Information disclosure | self-trigger loop reading TTS output back into STT | mitigate | session.ts dispatches micCapture.pauseFrameDelivery on processing -> speaking transition AND drops MIC_FRAME during speaking state (SE9); 300 ms debounce on TTS drain (SE10); PITFALLS #2 |
| T-12-24 | Denial of service | unbounded TTS during error storm | mitigate | normaliseForTts 600-char cap (Plan 12-02) + sequence-buffer ordering (Plan 12-03) + per-session lifecycle on TTS stream (close on cancel) |
| T-12-25 | Tampering | drift between PROMPT-05 prompt instruction and orchestrator runtime override | mitigate | Plan 12-01's prompt-content.test.ts gates the "I ran into a problem" phrase in the prompt; session.test.ts SE6/SE7 gates the runtime override prefix; if either drifts, builds fail |
| T-12-26 | Repudiation | cancel-during-speaking failing to stop TTS | mitigate | session.ts onCancel calls tts.close() + bridge.cancel() + state CIRCLE_CLICK; SE5 verifies the cancellation sequence |

</threat_model>

<verification>
1. `npx vitest run --project phase-12-unit apps/achilles/src/main/key-source.test.ts apps/achilles/src/main/state-machine.test.ts apps/achilles/src/main/store.test.ts` passes (Task 1).
2. `npx vitest run --project phase-12-unit apps/achilles/src/main/session.test.ts` passes (Task 2).
3. `MOCK_LOOP=1 npx vitest run --project phase-12-unit apps/achilles/src/main/ipc-bridge.test.ts apps/achilles/test/integration/end-to-end-loop.test.ts` passes (Task 3).
4. `npx vitest run --project phase-12-unit apps/achilles/test/integration/end-to-end-loop.test.ts` (without MOCK_LOOP) results in 4 skipped tests, no failures.
5. `npx tsc -p apps/achilles/tsconfig.json --noEmit` typechecks the main process including session.ts.
6. `npx vitest run --project phase-11-unit apps/achilles/src/main/state-machine.test.ts` still passes (Plan 11-01 back-compat preserved by keeping MOCK_* tags).
7. `grep -v '^//' apps/achilles/src/main/session.ts | grep -c "I ran into a problem"` returns >= 1 (the locked override prefix appears in source as a constant).
8. `grep -v '^//' apps/achilles/src/main/session.ts | grep -c "SPEAKING_DEBOUNCE_MS = 300"` returns >= 1 (locked debounce constant).
9. `grep -E "process\.env\.ELEVENLABS_API_KEY" apps/achilles/src/main/session.ts apps/achilles/src/main/ipc-bridge.ts` returns zero matches (the key is read only via key-source.ts, never directly elsewhere).
</verification>

<success_criteria>
- session.ts ships the per-utterance orchestrator composing voice-stt + claude-code-bridge + voice-tts + sandwich-defence + normalisation behind the state machine.
- The half-duplex contract is mechanically enforced: micCapture.pauseFrameDelivery on processing -> speaking AND MIC_FRAME drop during speaking state.
- The 300 ms SPEAKING_DEBOUNCE_MS timer is the locked debounce gate between TTS drain and idle.
- PROMPT-05 runtime override: when deriveOutcome returns failure, the spoken summary begins with "I ran into a problem" regardless of LLM narration (verified in SE6, SE7, EE2).
- SAFE-04 sandwich-defence is the SINGLE entry point for transcript -> Claude (SE11, EE4).
- Pre-TTS normalisation is applied to both ack and summary before TTS routing (SE12).
- The API key never leaves the main process; readApiKey is the single read point with store-first + env-fallback semantics.
- mock-loop-clients.ts enables deterministic end-to-end testing without ElevenLabs or Claude Code (MOCK_LOOP=1 gate).
- main/index.ts replaces Plan 11-01 mock timers with the real session orchestrator; Plan 11-01 mock-timer unit tests still pass for back-compat.
- ipc-bridge.ts adds the 4 new inbound handlers with sender-identity checks + payload validation + dispose teardown.
- No emojis, no logging of payload content, no real Electron launch, no live ElevenLabs, no real Claude Code in CI.
- LOOP-05 is structurally enforced across orchestrator (gating + debounce + outcome) + renderer (playback queue + half-duplex pause).
</success_criteria>

<output>
On completion create `.planning/phases/12-end-to-end-integration-system-prompt/12-04-SUMMARY.md` documenting: created files, the session.ts public surface (createSession + AchillesSessionDeps + SPEAKING_DEBOUNCE_MS), the half-duplex gating sequence, the authoritative outcome override (PROMPT-05 runtime), the sandwich-defence wiring, the pre-TTS normalisation wiring, the API key surface (store + env fallback), the mock-loop-clients fixtures, the MOCK_LOOP=1 integration test results, the four state-machine event tags added, the six IPC handlers added, the back-compat preservation for Plan 11-01 mock-timer tests, the verification grep results.
</output>