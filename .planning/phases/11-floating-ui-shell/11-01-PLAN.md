---
phase: 11-floating-ui-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/achilles/package.json
  - apps/achilles/tsconfig.json
  - apps/achilles/tsconfig.node.json
  - apps/achilles/tsconfig.web.json
  - apps/achilles/electron.vite.config.ts
  - apps/achilles/playwright.config.ts
  - apps/achilles/.gitignore
  - apps/achilles/src/.gitignore
  - apps/achilles/src/main/index.ts
  - apps/achilles/src/main/window.ts
  - apps/achilles/src/main/window.test.ts
  - apps/achilles/src/main/hotkey.ts
  - apps/achilles/src/main/hotkey.test.ts
  - apps/achilles/src/main/store.ts
  - apps/achilles/src/main/store.test.ts
  - apps/achilles/src/main/state-machine.ts
  - apps/achilles/src/main/state-machine.test.ts
  - apps/achilles/src/main/mock-amplitude.ts
  - apps/achilles/src/main/mock-amplitude.test.ts
  - apps/achilles/src/main/ipc-bridge.ts
  - apps/achilles/src/preload/index.ts
  - apps/achilles/src/preload/global.d.ts
  - apps/achilles/src/shared/ipc-schemas.ts
  - apps/achilles/src/shared/ipc-schemas.test.ts
  - apps/achilles/src/shared/constants.ts
  - apps/achilles/src/renderer/index.html
  - apps/achilles/src/renderer/main.tsx
  - apps/achilles/src/renderer/bridge.ts
  - apps/achilles/test/mocks/mock-bridge.ts
  - apps/achilles/test/e2e/scaffold.spec.ts
  - tsconfig.base.json
  - vitest.workspace.ts
  - playwright.config.ts
autonomous: true
requirements:
  - UI-01
  - UI-06

must_haves:
  truths:
    - "The apps/achilles workspace package builds via electron-vite without errors and exposes a typed preload API on window.achilles"
    - "BrowserWindow is constructed with frame:false, transparent:true, alwaysOnTop:true, focusable:false, type:'panel', skipTaskbar:true, and webPreferences { contextIsolation:true, nodeIntegration:false, sandbox:true }"
    - "setVisibleOnAllWorkspaces(true, { visibleOnFullScreen: true }) and app.dock.hide() are invoked on macOS so the window survives Spaces and full-screen apps and never shows a dock icon"
    - "The mocked AchillesState machine in main exposes a transition(state, event) reducer that drives idle, listening, processing, speaking, error and is the single source of truth (renderer is a pure projection)"
    - "globalShortcut.register honours both 'toggle' and 'pushToTalk' hotkeyMode; mode and hotkey accelerator persist in electron-store under hotkeyMode and hotkeyKey"
    - "Renderer→Main and Main→Renderer IPC payloads parse cleanly through Zod schemas in apps/achilles/src/shared/ipc-schemas.ts and unknown fields are rejected by .strict()"
    - "vitest workspace project phase-11-unit picks up apps/achilles/src/**/*.test.ts and runs without launching Electron"
    - "playwright project achilles-renderer is registered against the headless Vite renderer bundle and asserts the scaffold renders the floating-shell root with viewport 260×260"
  artifacts:
    - path: "apps/achilles/package.json"
      provides: "Achilles workspace package manifest (private; electron + electron-vite + electron-store + zod + react)"
      contains: "@achilles/voice-protocol"
    - path: "apps/achilles/electron.vite.config.ts"
      provides: "electron-vite config with main, preload, renderer (production) and renderer.headless entries"
      contains: "renderer"
    - path: "apps/achilles/src/main/window.ts"
      provides: "createAchillesWindow factory locked to UI-01 BrowserWindow contract"
      contains: "type: \"panel\""
    - path: "apps/achilles/src/main/hotkey.ts"
      provides: "registerAchillesHotkey + setHotkeyMode helpers honouring toggle + PTT modes"
      contains: "pushToTalk"
    - path: "apps/achilles/src/main/store.ts"
      provides: "electron-store schema { windowPosition, hotkeyMode, hotkeyKey } with safeStorage-encrypted reads/writes"
      contains: "windowPosition"
    - path: "apps/achilles/src/main/state-machine.ts"
      provides: "Pure transition(state, event) reducer for the five AchillesState values driven by mocked timers"
      contains: "AchillesState"
    - path: "apps/achilles/src/shared/ipc-schemas.ts"
      provides: "Zod-validated IPC envelope schemas for state-changed, transcript-partial/committed, mic/tts-amplitude, permission-state, error, request-state, register-hotkey, open-system-settings, update-window-position, update-hotkey-config"
      contains: "AchillesStateSchema"
    - path: "apps/achilles/src/preload/index.ts"
      provides: "contextBridge.exposeInMainWorld('achilles', { ... }) typed bridge surface"
      contains: "exposeInMainWorld"
    - path: "tsconfig.base.json"
      provides: "Updated path aliases for @achilles/app shared/main/preload/renderer subpaths"
      contains: "@achilles/app"
    - path: "vitest.workspace.ts"
      provides: "phase-11-unit project for apps/achilles unit tests + workspaceAlias entries for @achilles/app"
      contains: "phase-11-unit"
    - path: "playwright.config.ts"
      provides: "achilles-renderer project against the headless Vite bundle with viewport 260×260"
      contains: "achilles-renderer"
  key_links:
    - from: "apps/achilles/src/main/index.ts"
      to: "apps/achilles/src/main/window.ts"
      via: "createAchillesWindow() called inside app.whenReady"
      pattern: "createAchillesWindow"
    - from: "apps/achilles/src/preload/index.ts"
      to: "apps/achilles/src/shared/ipc-schemas.ts"
      via: "Zod schema imports gate every ipcRenderer.send/invoke payload"
      pattern: "from \"../shared/ipc-schemas"
    - from: "apps/achilles/src/main/ipc-bridge.ts"
      to: "apps/achilles/src/main/state-machine.ts"
      via: "ipcMain handlers call transition() and broadcast state-changed"
      pattern: "transition"
    - from: "apps/achilles/src/renderer/main.tsx"
      to: "apps/achilles/src/renderer/bridge.ts"
      via: "Renderer subscribes to window.achilles events via the typed bridge wrapper"
      pattern: "window.achilles"
---

<objective>
Scaffold the `apps/achilles` Electron application and the Wave 1 substrate every other Phase 11 plan builds on: the BrowserWindow contract (UI-01), the mocked state machine that drives the five visible states, the global hotkey infrastructure honouring both press-to-toggle and push-to-talk modes (UI-06), the `electron-store` persistence schema for window position and hotkey settings, the Zod-validated IPC envelope schemas, the preload `contextBridge` surface, the minimal renderer bootstrap, the headless Vite renderer entry the Playwright suite drives, the `MockAchillesBridge` test seam, and all root workspace plumbing (tsconfig path aliases, `vitest.workspace.ts` phase-11-unit project, `playwright.config.ts` achilles-renderer project).

Purpose: Wave 1 fixes the architectural contracts the rest of the phase consumes — once the IPC schemas, state-machine reducer, window contract, and mock bridge are committed, Plans 11-02 (visual components) and 11-03 (drag persistence + permission overlay + settings popover + error banner) can compose against them in parallel without inventing parallel schemas or renegotiating window plumbing. The scope is deliberately ordered so the contracts ship before any component does.

Output: A compileable, testable `apps/achilles` workspace package whose main process owns the locked BrowserWindow + mocked state machine + IPC schemas + hotkey + `electron-store` config, whose preload exposes a typed bridge, whose minimal renderer bootstraps against a `MockAchillesBridge` in headless tests, and whose Playwright + Vitest projects are wired into the monorepo so Wave 2 only touches its own component subtree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-floating-ui-shell/11-CONTEXT.md
@.planning/phases/11-floating-ui-shell/11-UI-SPEC.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md
@packages/voice-protocol/package.json
@packages/voice-protocol/tsconfig.json
@packages/voice-protocol/src/index.ts
@packages/voice-protocol/src/ipc.ts
@packages/voice-protocol/src/stt-events.ts
@packages/voice-protocol/src/tts-events.ts
@packages/claude-code-bridge/package.json
@tsconfig.base.json
@vitest.workspace.ts
@playwright.config.ts
@package.json

<interfaces>
<!-- Key contracts this plan establishes and that Plans 11-02 and 11-03 consume. -->
<!-- Phase 09 analog: packages/voice-protocol — mirror its package.json, tsconfig.json, src/.gitignore shape verbatim where applicable. -->

Existing analog — packages/voice-protocol/package.json shape:
- type: "module"
- main: "dist/index.js"
- types: "dist/index.d.ts"
- exports: { ".": { types: "./dist/index.d.ts", default: "./dist/index.js" } }
- scripts: build (rm -rf dist .tsbuildinfo && tsc -p tsconfig.json), typecheck, test (vitest run)
- dependencies: { "zod": "4.3.6" }
- devDependencies: { "typescript": "5.7.3", "vitest": "2.1.8" }

Existing analog — packages/voice-protocol/tsconfig.json shape:
- extends "../../tsconfig.base.json"
- compilerOptions: { outDir: "dist", rootDir: "src", tsBuildInfoFile: "./.tsbuildinfo", declaration: true, declarationMap: true, sourceMap: true }
- include: ["src/**/*.ts"]
- exclude: ["node_modules", "dist", "src/**/*.test.ts", "test/**"]  (CR-06 fix from Phase 09)

Existing analog — packages/voice-protocol/src/.gitignore (CR-07 defensive guard):
  *.js
  *.d.ts
  *.js.map
  *.d.ts.map

State machine contract (consumed by 11-02 and 11-03):
  type AchillesState = 'idle' | 'listening' | 'processing' | 'speaking' | 'error'
  type PermissionState = 'granted' | 'not-determined' | 'denied' | 'restricted'
  type HotkeyMode = 'toggle' | 'pushToTalk'

  type AchillesEvent =
    | { type: 'HOTKEY_PRESS' }
    | { type: 'HOTKEY_RELEASE' }                 // PTT only
    | { type: 'CIRCLE_CLICK' }
    | { type: 'MOCK_VAD_COMMIT' }                // listening → processing
    | { type: 'MOCK_PROCESSING_COMPLETE' }       // processing → speaking
    | { type: 'MOCK_PLAYBACK_DONE' }             // speaking → idle
    | { type: 'INJECT_ERROR'; kind: 'mic_unavailable' | 'hotkey_collision' | 'persistence_failure' | 'unknown' }
    | { type: 'ERROR_DISMISS' }
    | { type: 'PERMISSION_CHANGED'; state: PermissionState }

  function transition(current: AchillesState, event: AchillesEvent, hotkeyMode: HotkeyMode): AchillesState

IPC envelope contract — Main → Renderer (consumed by 11-02 + 11-03):
  | { type: 'state-changed'; state: AchillesState }
  | { type: 'transcript-partial'; text: string }
  | { type: 'transcript-committed'; id: string; text: string; committedAt: number }
  | { type: 'mic-amplitude'; rms: number }      // 0..1; emitted only during listening
  | { type: 'tts-amplitude'; rms: number }      // 0..1; emitted only during speaking
  | { type: 'permission-state'; state: PermissionState }
  | { type: 'error'; message: string }

IPC envelope contract — Renderer → Main:
  | { type: 'request-state'; state: AchillesState }                 // test seam + cancel paths
  | { type: 'register-hotkey'; accelerator: string }
  | { type: 'open-system-settings' }
  | { type: 'update-window-position'; x: number; y: number }
  | { type: 'update-hotkey-config'; mode?: HotkeyMode; key?: string }

electron-store schema:
  { windowPosition: { x: number; y: number } | null,
    hotkeyMode: 'toggle' | 'pushToTalk',
    hotkeyKey: string  /* Electron accelerator; default 'CommandOrControl+Shift+A' */ }

Window constants (from CONTEXT.md + UI-SPEC.md §2):
  WINDOW_WIDTH = 260
  WINDOW_HEIGHT = 260
  DEFAULT_MARGIN_PX = 24       // top-right inset on first launch
  DRAG_HANDLE_HEIGHT_PX = 30

Preload surface (window.achilles) — exact named API consumed by 11-02 + 11-03:
  onStateChanged(cb: (s: AchillesState) => void): () => void
  onTranscriptPartial(cb: (text: string) => void): () => void
  onTranscriptCommitted(cb: (entry: { id: string; text: string; committedAt: number }) => void): () => void
  onMicAmplitude(cb: (rms: number) => void): () => void
  onTtsAmplitude(cb: (rms: number) => void): () => void
  onPermissionState(cb: (p: PermissionState) => void): () => void
  onError(cb: (msg: string) => void): () => void
  requestState(state: AchillesState): void
  openSystemSettings(): void
  updateWindowPosition(pos: { x: number; y: number }): void
  updateHotkeyConfig(cfg: { mode?: HotkeyMode; key?: string }): void

Mock bridge (test seam — consumed by 11-02 + 11-03 e2e tests):
  window.__mockBridge: {
    setState(s: AchillesState): void
    setPermission(p: PermissionState): void
    emitPartialTranscript(text: string): void
    emitCommittedTranscript(text: string): void
    emitMicAmplitude(rms: number): void
    emitTtsAmplitude(rms: number): void
    emitError(message: string): void
    __test_inject_error(kind: 'mic_unavailable' | 'hotkey_collision' | 'persistence_failure' | 'unknown'): void
    getLastEmittedIPC(): { type: string; payload: unknown }[]
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold apps/achilles package, electron-vite config, tsconfigs, .gitignore guards, and root workspace plumbing</name>
  <files>
    apps/achilles/package.json,
    apps/achilles/tsconfig.json,
    apps/achilles/tsconfig.node.json,
    apps/achilles/tsconfig.web.json,
    apps/achilles/electron.vite.config.ts,
    apps/achilles/playwright.config.ts,
    apps/achilles/.gitignore,
    apps/achilles/src/.gitignore,
    apps/achilles/src/shared/constants.ts,
    tsconfig.base.json,
    vitest.workspace.ts,
    playwright.config.ts
  </files>
  <behavior>
    - Test 1 (vitest): apps/achilles/src/shared/constants.ts exports WINDOW_WIDTH === 260, WINDOW_HEIGHT === 260, DEFAULT_MARGIN_PX === 24, DRAG_HANDLE_HEIGHT_PX === 30, DEFAULT_HOTKEY_ACCELERATOR === 'CommandOrControl+Shift+A'.
    - Test 2 (vitest): apps/achilles/src/shared/constants.ts exports IPC channel name constants (IPC_STATE_CHANGED, IPC_TRANSCRIPT_PARTIAL, IPC_TRANSCRIPT_COMMITTED, IPC_MIC_AMPLITUDE, IPC_TTS_AMPLITUDE, IPC_PERMISSION_STATE, IPC_ERROR, IPC_REQUEST_STATE, IPC_REGISTER_HOTKEY, IPC_OPEN_SYSTEM_SETTINGS, IPC_UPDATE_WINDOW_POSITION, IPC_UPDATE_HOTKEY_CONFIG) and each value is a kebab-case string with prefix 'achilles:'.
    - Test 3 (vitest): The tuple ACHILLES_STATES === ['idle', 'listening', 'processing', 'speaking', 'error'] as const has length 5 and contains every documented state.
    - Test 4 (vitest): The tuple HOTKEY_MODES === ['toggle', 'pushToTalk'] as const.
    - Test 5 (vitest): The tuple PERMISSION_STATES === ['granted', 'not-determined', 'denied', 'restricted'] as const.
  </behavior>
  <action>
    Create `apps/achilles/package.json` mirroring the packages/voice-protocol shape but adapted for an Electron app: `name: "@achilles/app"`, `private: true`, `version: "0.1.0"`, `type: "module"`, `main: "out/main/index.js"` (electron-vite default output), no `files` field (this app is not published — keep `private: true`). Scripts: `dev` (electron-vite dev), `build:main` (electron-vite build --mode production --config electron.vite.config.ts), `build:renderer` (electron-vite build --mode production --config electron.vite.config.ts and explicit renderer-headless target), `typecheck` (tsc -p tsconfig.json --noEmit), `lint` (placeholder echo), `test` (vitest run). Dependencies: `electron@42.3.3`, `electron-store@10.1.0`, `zod@4.3.6`, `react@19.2.4`, `react-dom@19.2.4`, `@achilles/voice-protocol@0.1.0` (workspace consumer). DevDependencies: `electron-vite@3.1.0`, `vite@5.4.11`, `@vitejs/plugin-react@4.3.4`, `typescript@5.7.3`, `vitest@2.1.8`, `@types/react@19.0.8`, `@types/react-dom@19.0.3`, `@playwright/test@1.58.2`.
    Create `apps/achilles/tsconfig.json` as the root project reference file: `extends "../../tsconfig.base.json"`, `references: [{ path: "./tsconfig.node.json" }, { path: "./tsconfig.web.json" }]`, `include: []`.
    Create `apps/achilles/tsconfig.node.json` for the main + preload + shared TypeScript: extends base; `compilerOptions: { module: "NodeNext", moduleResolution: "NodeNext", outDir: "out", rootDir: "src", tsBuildInfoFile: "./.tsbuildinfo.node", declaration: true, declarationMap: true, sourceMap: true, types: ["node", "electron"] }`; `include: ["src/main/**/*.ts", "src/preload/**/*.ts", "src/shared/**/*.ts"]`; `exclude: ["node_modules", "out", "src/**/*.test.ts", "src/**/*.test.tsx", "test/**"]` (Phase 09 CR-06 lesson — never ship test files in the build output).
    Create `apps/achilles/tsconfig.web.json` for the renderer: extends base; `compilerOptions: { module: "ESNext", moduleResolution: "Bundler", jsx: "react-jsx", outDir: "out", rootDir: "src", lib: ["ESNext", "DOM", "DOM.Iterable"], types: ["vite/client"] }`; `include: ["src/renderer/**/*.ts", "src/renderer/**/*.tsx", "src/shared/**/*.ts"]`; `exclude: ["node_modules", "out", "src/**/*.test.ts", "src/**/*.test.tsx", "test/**"]`.
    Create `apps/achilles/electron.vite.config.ts` using `defineConfig` from `electron-vite`. Three named entries: `main` (entry `src/main/index.ts`, target `node`, format `es`), `preload` (entry `src/preload/index.ts`, format `cjs` for Electron preload contextBridge compatibility), and `renderer` (root `src/renderer`, plugins `[react()]`, build outDir `out/renderer`, dev server port 5173). Add a second renderer mode `renderer.headless` (or expose a top-level `defineConfig` override) that publishes the same renderer bundle but loads `apps/achilles/test/mocks/mock-bridge.ts` as a Vite virtual entry so Playwright drives the renderer without a real preload bridge; bind the headless dev server to port 5174 (matches playwright config).
    Create `apps/achilles/playwright.config.ts` exporting a `defineConfig` with `testDir: "./test/e2e"`, `use: { baseURL: "http://localhost:5174", viewport: { width: 260, height: 260 }, trace: "retain-on-failure" }`, `webServer: { command: "npm --workspace @achilles/app run build:renderer:headless && npx vite preview --port 5174 --host", port: 5174, reuseExistingServer: !process.env.CI, timeout: 60000 }`, `projects: [{ name: "achilles-renderer", testMatch: "**/*.spec.ts" }]`. The webServer command MUST NOT launch Electron — only the Vite preview against the built renderer bundle (per CONTEXT.md test strategy and CLAUDE.md global "never run applications automatically").
    Create `apps/achilles/.gitignore` listing `out/`, `node_modules/`, `.tsbuildinfo*`, `dist/` (defensive — should not appear), `playwright-report/`, `.cache/`.
    Create `apps/achilles/src/.gitignore` with the Phase 09 CR-07 defensive guard: header comment "Compiled output never belongs inside src/ ..." followed by `*.js`, `*.d.ts`, `*.js.map`, `*.d.ts.map`, `*.jsx`, `*.tsx.js` (NB include `.jsx` because the renderer compiles tsx).
    Create `apps/achilles/src/shared/constants.ts` exporting the five window + token constants (WINDOW_WIDTH, WINDOW_HEIGHT, DEFAULT_MARGIN_PX, DRAG_HANDLE_HEIGHT_PX, DEFAULT_HOTKEY_ACCELERATOR), the IPC channel name constants (each prefixed with `achilles:`), and three `as const` tuples (ACHILLES_STATES, HOTKEY_MODES, PERMISSION_STATES). Adjacent type aliases `AchillesState`, `HotkeyMode`, `PermissionState` derived via `(typeof TUPLE)[number]`. Add a colocated `constants.test.ts` that asserts the literal values, tuple membership, and IPC prefix invariant.
    Modify `tsconfig.base.json` — add path aliases in the alphabetised cluster after `@achilles/claude-code-bridge/*`: `"@achilles/app": ["apps/achilles/src/shared/constants.ts"]` and subpath aliases `"@achilles/app/main/*": ["apps/achilles/src/main/*"]`, `"@achilles/app/preload/*": ["apps/achilles/src/preload/*"]`, `"@achilles/app/renderer/*": ["apps/achilles/src/renderer/*"]`, `"@achilles/app/shared/*": ["apps/achilles/src/shared/*"]`. The first form is the barrel; subpaths expose specific module trees. Mirror the Phase 09/10 alphabetised insertion convention.
    Modify `vitest.workspace.ts` — append a paired set of `workspaceAlias` entries for `@achilles/app` (literal pointing at `apps/achilles/src/shared/constants.ts`) and four subpath regexes for `^@achilles/app/main/(.+)$`, `^@achilles/app/preload/(.+)$`, `^@achilles/app/renderer/(.+)$`, `^@achilles/app/shared/(.+)$`, each rewriting to the matching `apps/achilles/src/*` path. Append a new project after `phase-10-unit` named `phase-11-unit` with `include: ["apps/achilles/src/**/*.test.ts", "apps/achilles/src/**/*.test.tsx"]`, `environment: "node"`, `passWithNoTests: true`. Add a header comment block noting the Phase 11 addition pattern (matches the Phase 09 + Phase 10 footer-comment convention).
    Modify root `playwright.config.ts` — add a new project entry `{ name: "achilles-renderer", testDir: "./apps/achilles/test/e2e", testMatch: "**/*.spec.ts", use: { baseURL: "http://localhost:5174", viewport: { width: 260, height: 260 }, trace: "retain-on-failure" } }` AND append the matching `webServer` entry as a sibling (or wrap the project under an `apps/achilles/playwright.config.ts` import — choose the cleaner option that does not break the existing phase-01-e2e-mobile project). If the existing config does not support nested webServers, keep the existing top-level `testMatch` and add the achilles testDir glob, then encapsulate the achilles-specific webServer + viewport in the project block via `use` override. Document any decision in the file header comment.
    Update root `package.json` `scripts` block: add `"test:phase-11:quick": "vitest run --project phase-11-unit"` and `"test:phase-11:full": "vitest run --project phase-11-unit && playwright test --project=achilles-renderer"`. Mirror the Phase 01 test script convention. Do NOT touch any other root script.
    Per CLAUDE.md: no emojis anywhere. Per locked decision: `apps/achilles` is private and NOT published (the npm CLI surface ships from `apps/achilles-cli` in Phase 13).
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff && npm install --workspaces --include-workspace-root && npx tsc -p tsconfig.base.json --noEmit && npx vitest run --project phase-11-unit -- src/shared/constants.test.ts</automated>
  </verify>
  <done>
    The new package exists, npm workspace install succeeds, tsconfig.base.json compiles with the new aliases resolved, constants.test.ts passes 5/5 assertions, `vitest --project phase-11-unit` runs without launching Electron, and `playwright.config.ts` lists the achilles-renderer project (verified via `npx playwright test --list --project=achilles-renderer` returning a non-error exit even when no specs match yet).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the BrowserWindow contract (UI-01), mocked state machine, hotkey + electron-store + IPC schemas (UI-06), preload bridge, minimal renderer bootstrap, and MockAchillesBridge test seam</name>
  <files>
    apps/achilles/src/main/window.ts,
    apps/achilles/src/main/window.test.ts,
    apps/achilles/src/main/hotkey.ts,
    apps/achilles/src/main/hotkey.test.ts,
    apps/achilles/src/main/store.ts,
    apps/achilles/src/main/store.test.ts,
    apps/achilles/src/main/state-machine.ts,
    apps/achilles/src/main/state-machine.test.ts,
    apps/achilles/src/main/mock-amplitude.ts,
    apps/achilles/src/main/mock-amplitude.test.ts,
    apps/achilles/src/main/ipc-bridge.ts,
    apps/achilles/src/main/index.ts,
    apps/achilles/src/preload/index.ts,
    apps/achilles/src/preload/global.d.ts,
    apps/achilles/src/shared/ipc-schemas.ts,
    apps/achilles/src/shared/ipc-schemas.test.ts,
    apps/achilles/src/renderer/index.html,
    apps/achilles/src/renderer/main.tsx,
    apps/achilles/src/renderer/bridge.ts,
    apps/achilles/test/mocks/mock-bridge.ts,
    apps/achilles/test/e2e/scaffold.spec.ts
  </files>
  <behavior>
    - Test S1 (state-machine.test.ts): transition('idle', { type: 'HOTKEY_PRESS' }, 'toggle') === 'listening' (UI-06 toggle entry).
    - Test S2: transition('listening', { type: 'HOTKEY_PRESS' }, 'toggle') === 'processing' (second press commits the in-flight utterance per UI-SPEC §4).
    - Test S3: transition('listening', { type: 'HOTKEY_RELEASE' }, 'pushToTalk') === 'processing' (PTT release commits).
    - Test S4: transition('listening', { type: 'HOTKEY_RELEASE' }, 'toggle') === 'listening' (release is a no-op in toggle mode).
    - Test S5: transition('processing', { type: 'MOCK_PROCESSING_COMPLETE' }, 'toggle') === 'speaking'.
    - Test S6: transition('speaking', { type: 'MOCK_PLAYBACK_DONE' }, 'toggle') === 'idle'.
    - Test S7: For every state s in ACHILLES_STATES, transition(s, { type: 'INJECT_ERROR', kind: 'unknown' }, 'toggle') === 'error' (the test seam unconditionally enters error).
    - Test S8: transition('error', { type: 'ERROR_DISMISS' }, 'toggle') === 'idle'.
    - Test S9: transition('speaking', { type: 'CIRCLE_CLICK' }, 'toggle') === 'idle' (UI-SPEC §4 click-on-circle-during-speaking = cancel → idle).
    - Test S10: Unknown event types throw an Error whose message names the event tag (defensive — the reducer is exhaustively switched).
    - Test IPC1 (ipc-schemas.test.ts): The state-changed schema accepts every value in ACHILLES_STATES and rejects 'unknown' as a state.
    - Test IPC2: The mic-amplitude schema accepts rms in [0, 1] and rejects rms < 0 or > 1 (defensive against bad mock generators).
    - Test IPC3: update-window-position schema requires integer x + y (rejects floats — Electron setPosition takes integers).
    - Test IPC4: update-hotkey-config schema accepts partial { mode } or { key } and rejects both fields omitted.
    - Test IPC5: open-system-settings schema is .strict() — extra fields are refused (no payload contract).
    - Test IPC6: The discriminated union (parseEnvelope helper) accepts every documented Main→Renderer and Renderer→Main type and rejects unknown 'type' literals.
    - Test W1 (window.test.ts): A stubbed BrowserWindow constructor receives the exact UI-01 contract object — frame:false, transparent:true, alwaysOnTop:true, focusable:false, type:'panel', skipTaskbar:true, width:260, height:260, webPreferences:{ contextIsolation:true, nodeIntegration:false, sandbox:true }. Verified by injecting a mock constructor and asserting the captured options.
    - Test W2: createAchillesWindow calls win.setVisibleOnAllWorkspaces(true, { visibleOnFullScreen: true }) and win.setAlwaysOnTop(true, 'screen-saver') exactly once each (per STACK.md and PITFALLS.md #15).
    - Test W3: On a stubbed `process.platform === 'darwin'`, createAchillesWindow triggers app.dock.hide() exactly once. On 'win32'/'linux' it must NOT call app.dock.hide().
    - Test H1 (hotkey.test.ts): registerAchillesHotkey('CommandOrControl+Shift+A', 'toggle', onPress, onRelease) calls globalShortcut.register exactly once with the documented accelerator; onPress invoked on each registered callback.
    - Test H2: In 'pushToTalk' mode, registerAchillesHotkey wires BOTH key-down and key-up via Electron's globalShortcut.register API plus a synthesised key-up watcher (Electron's globalShortcut historically fires on key-down only — use BrowserWindow webContents key events as the release seam OR a polling debounce. Document the chosen mechanism in code comments.) — assert via injected stubs that onRelease is invoked when the test simulates key-up.
    - Test H3: setHotkeyMode('pushToTalk') persists the change to the injected store mock under the key 'hotkeyMode'.
    - Test ST1 (store.test.ts): readWindowPosition() returns null when no position is persisted (default branch).
    - Test ST2: writeWindowPosition({ x: 100, y: 200 }) persists the integer pair via the injected electron-store mock and a subsequent readWindowPosition returns the same value.
    - Test ST3: writeHotkeyMode('pushToTalk') persists 'pushToTalk'; readHotkeyMode() returns 'pushToTalk'.
    - Test ST4: When safeStorage.isEncryptionAvailable() returns false (older OS / Linux fallback), the store writes the values in plaintext and logs a `[achilles]` warning naming SAFE-01 boundary (defence in depth — the key surface ships in Phase 12; Phase 11 logs and tolerates the absence rather than throwing).
    - Test MA1 (mock-amplitude.test.ts): createMockAmplitudeStream('listening').next() returns a value in [0, 1] and the sequence is deterministic for a fixed seed (LCG or hashed counter).
    - Test MA2: createMockAmplitudeStream('speaking') returns a different deterministic sequence than 'listening' (so e2e tests can distinguish the two amplitude sources).
    - Test MA3: For any state other than 'listening' or 'speaking', createMockAmplitudeStream returns a constant-zero iterator.
    - Test E2E (scaffold.spec.ts Playwright): The headless renderer renders a single element matching `[data-testid="floating-shell"]`; window.__mockBridge exposes the documented API surface (setState, setPermission, emitPartialTranscript, emitCommittedTranscript, emitMicAmplitude, emitTtsAmplitude, emitError, __test_inject_error, getLastEmittedIPC); window.__mockBridge.setState('listening') causes `[data-testid="reactive-circle"]` (a stub div rendered by the minimal renderer bootstrap) to receive `data-state="listening"`.
  </behavior>
  <action>
    Author `apps/achilles/src/main/state-machine.ts` exporting `AchillesEvent` discriminated union (see <interfaces> block), `transition(current: AchillesState, event: AchillesEvent, hotkeyMode: HotkeyMode): AchillesState` as a pure function (no side effects, no mutable state), and `createMockStateController(opts: { broadcast: (state: AchillesState) => void; emitAmplitude: (rms: number) => void; getMode: () => HotkeyMode })` returning `{ dispatch(event), now: () => AchillesState, scheduleMockTransitions(state) }`. The controller is the runtime wrapper around the pure reducer; `scheduleMockTransitions('listening')` starts a fixture timer that emits MOCK_VAD_COMMIT after 1200 ms (so e2e tests can drive listening → processing → speaking → idle deterministically). Locked timer values: LISTENING_VAD_DELAY_MS=1200, PROCESSING_DELAY_MS=800, SPEAKING_DELAY_MS=2000, ERROR_AUTO_DISMISS_MS=8000 (per UI-SPEC §8). Colocate `state-machine.test.ts` with all 10 behaviour tests.
    Author `apps/achilles/src/shared/ipc-schemas.ts` defining one Zod `.strict()` schema per IPC channel constant: `StateChangedPayloadSchema` with `state: z.enum(ACHILLES_STATES)`, `TranscriptPartialPayloadSchema` with `text: z.string().min(1)`, `TranscriptCommittedPayloadSchema` with `{ id: z.string().uuid(), text: z.string().min(1), committedAt: z.number().int().nonnegative() }`, `MicAmplitudePayloadSchema` with `rms: z.number().min(0).max(1)`, `TtsAmplitudePayloadSchema` with `rms: z.number().min(0).max(1)`, `PermissionStatePayloadSchema` with `state: z.enum(PERMISSION_STATES)`, `ErrorPayloadSchema` with `message: z.string().min(1)`. Renderer→Main schemas: `RequestStatePayloadSchema` with `state: z.enum(ACHILLES_STATES)`, `RegisterHotkeyPayloadSchema` with `accelerator: z.string().min(1)`, `OpenSystemSettingsPayloadSchema = z.object({}).strict()`, `UpdateWindowPositionPayloadSchema` with `{ x: z.number().int(), y: z.number().int() }`, `UpdateHotkeyConfigPayloadSchema` with `.refine((v) => v.mode !== undefined || v.key !== undefined, "at least one of mode or key required")` over `{ mode: z.enum(HOTKEY_MODES).optional(), key: z.string().min(1).optional() }`. Export `parseEnvelope(channel: string, payload: unknown)` and `serializeForChannel(channel, payload)` helpers + a `IPC_PAYLOAD_SCHEMAS` map keyed by channel name. Add `.strict()` to every schema per the SAFE-01 boundary precedent in `packages/voice-protocol/src/ipc.ts`. Colocate `ipc-schemas.test.ts` with the 6 behaviour tests above + per-channel happy-path assertions.
    Author `apps/achilles/src/main/window.ts` exporting `createAchillesWindow(opts: { BrowserWindowCtor?: typeof BrowserWindow; appRef?: { dock?: { hide(): void } }; initialPosition?: { x: number; y: number } | null }): BrowserWindow`. The optional `BrowserWindowCtor` + `appRef` injection seams exist purely for unit testing — production code calls with no arguments and uses the real Electron exports. The function constructs the window with the exact UI-01 contract (260×260, frame:false, transparent:true, backgroundColor: '#00000000', alwaysOnTop:true, focusable:false, type:'panel', skipTaskbar:true, webPreferences { preload: path.join(__dirname, '../preload/index.js'), contextIsolation:true, nodeIntegration:false, sandbox:true }), then calls win.setVisibleOnAllWorkspaces(true, { visibleOnFullScreen: true }), win.setAlwaysOnTop(true, 'screen-saver') (per STACK.md), and on darwin invokes appRef.dock?.hide(). Position the window from `initialPosition` if provided; otherwise place it top-right of the primary display with DEFAULT_MARGIN_PX inset using `screen.getPrimaryDisplay().workArea`. Colocate `window.test.ts` with W1/W2/W3 tests using injected BrowserWindow + app stubs.
    Author `apps/achilles/src/main/store.ts` wrapping `electron-store` with the locked schema. Export `createAchillesStore(opts?: { storeCtor?: typeof Store; safeStorage?: { isEncryptionAvailable(): boolean; encryptString(s: string): Buffer; decryptString(buf: Buffer): string }; logger?: (msg: string) => void })` — the optional ctor + safeStorage injection makes the store testable without Electron. Persist `windowPosition`, `hotkeyMode` (default 'toggle'), `hotkeyKey` (default DEFAULT_HOTKEY_ACCELERATOR). If safeStorage.isEncryptionAvailable() returns true, encrypt the serialized payload before write; otherwise log `[achilles] safeStorage unavailable; persisting plaintext (SAFE-01 follow-up in Phase 12)` and write plaintext. Export `readWindowPosition`, `writeWindowPosition`, `readHotkeyMode`, `writeHotkeyMode`, `readHotkeyKey`, `writeHotkeyKey`. Colocate `store.test.ts` with ST1/ST2/ST3/ST4 tests using an in-memory store mock.
    Author `apps/achilles/src/main/hotkey.ts` exporting `registerAchillesHotkey(accelerator: string, mode: HotkeyMode, onPress: () => void, onRelease: () => void, opts?: { globalShortcutRef?: { register(acc: string, cb: () => void): boolean; unregister(acc: string): void; isRegistered(acc: string): boolean }; webContentsKeySource?: { onBeforeInputEvent(cb: (event: { type: 'keyDown' | 'keyUp'; key: string }) => void): void } })`. In `'toggle'` mode, register the accelerator with `globalShortcut.register` and invoke `onPress` on each fire (onRelease is never invoked — assertion). In `'pushToTalk'` mode, register the accelerator for the down edge AND wire a key-up watcher via the injected `webContentsKeySource` (production: BrowserWindow.webContents.on('before-input-event', ...)) — when key-up fires for any of the accelerator's non-modifier components, invoke `onRelease`. Document in code comments that Electron's `globalShortcut.register` only fires on key-down (PITFALLS.md note); the key-up watcher exists to bridge that gap. Export `unregisterAchillesHotkey()`. Colocate `hotkey.test.ts` with H1/H2/H3 tests using the injected stubs.
    Author `apps/achilles/src/main/mock-amplitude.ts` exporting `createMockAmplitudeStream(state: AchillesState, opts?: { seed?: number; tickMs?: number }): { next(): number; reset(): void; stop(): void; emit(cb: (rms: number) => void): () => void }`. Deterministic LCG-based RMS sequence for 'listening' (e.g., sin-wave-ish pattern over [0.2, 0.95]); a different deterministic sequence for 'speaking' (slower modulation, slightly lower peak [0.15, 0.85] — distinguishable from listening so e2e tests can tell the source apart). Constant 0 for other states. `emit(cb)` starts a 50 ms tick (matches UI-SPEC §1 "RMS sampled at 20 fps") invoking cb with the next value; returns an unsubscribe function. Colocate `mock-amplitude.test.ts` with MA1/MA2/MA3 + determinism assertions.
    Author `apps/achilles/src/main/ipc-bridge.ts` exporting `wireIpcBridge(opts: { window: BrowserWindow; controller: ReturnType<typeof createMockStateController>; store: ReturnType<typeof createAchillesStore>; ipcMainRef?: typeof ipcMain })`. The function registers handlers for IPC_REQUEST_STATE, IPC_REGISTER_HOTKEY, IPC_OPEN_SYSTEM_SETTINGS, IPC_UPDATE_WINDOW_POSITION, IPC_UPDATE_HOTKEY_CONFIG — each handler parses the payload through the matching schema in `IPC_PAYLOAD_SCHEMAS` and ignores invalid payloads with a `[achilles]` warning log. IPC_OPEN_SYSTEM_SETTINGS is implemented as a stub in this plan (logs and returns null) — Plan 11-03 wires the real `shell.openExternal` call. Broadcasting Main→Renderer events happens via `window.webContents.send(channel, payload)`. Subscribe to controller state changes so every transition emits IPC_STATE_CHANGED. Plumb the mock amplitude stream to IPC_MIC_AMPLITUDE during listening and IPC_TTS_AMPLITUDE during speaking; stop emitting outside those states.
    Author `apps/achilles/src/main/index.ts` as the Electron app entry. On `app.whenReady()`, instantiate the store, read persisted windowPosition + hotkeyMode + hotkeyKey, create the window via createAchillesWindow with the persisted position, instantiate the mock state controller, wire the IPC bridge, register the global hotkey, and on `app.on('will-quit')` unregister the hotkey + tear down the controller. Set `process.title = 'achilles'` and log `[achilles] main process started` once on boot. NEVER log raw audio, transcripts, or keys (defence in depth — none of these are wired in Phase 11 yet; the log discipline is set now).
    Author `apps/achilles/src/preload/index.ts` invoking `contextBridge.exposeInMainWorld('achilles', api)` where `api` is the documented preload surface from <interfaces>. Every method goes through `ipcRenderer.send(channel, payload)` or `ipcRenderer.on(channel, handler)`; payloads are validated against the matching schema before being forwarded (defence in depth — the Zod parse rejects unknown fields a compromised renderer might inject, per `packages/voice-protocol/src/ipc.ts` SAFE-01 boundary precedent). Each `on*` method returns an unsubscribe function.
    Author `apps/achilles/src/preload/global.d.ts` declaring `interface Window { achilles: ReturnType<typeof exposePreload> }` so the renderer + tests get typed access without `any`.
    Author `apps/achilles/src/renderer/index.html` with `<!doctype html>`, `<meta charset="utf-8">`, `<meta name="viewport" content="width=260, height=260">`, transparent body styles (`background: transparent; margin: 0; padding: 0; overflow: hidden; width: 260px; height: 260px; pointer-events: none`), `<div id="root" data-testid="floating-shell"></div>`, and the Vite-injected module script entry for `./main.tsx`.
    Author `apps/achilles/src/renderer/main.tsx` as a minimal React 18 root that mounts a stub `<App />` component rendering `<div data-testid="floating-shell"><div data-testid="reactive-circle" data-state={state}>state: {state}</div></div>`. The stub subscribes via `window.achilles.onStateChanged` (or `window.__mockBridge` when present — see bridge.ts). Plans 11-02 + 11-03 will replace this stub with the real composition root; Plan 11-01 ships only enough to prove the bridge wires correctly. Add a top-of-file comment "// Replaced by Plan 11-02 (FloatingShell composition root)" so the executor in the next plan knows the boundary.
    Author `apps/achilles/src/renderer/bridge.ts` exporting `getBridge(): AchillesBridge` — returns `window.__mockBridge` if it exists (Playwright headless mode), otherwise wraps `window.achilles`. The wrapper presents a single typed surface so renderer components do not branch on bridge identity.
    Author `apps/achilles/test/mocks/mock-bridge.ts` as a Vite-virtual-entry module that, when loaded by the headless renderer bundle, attaches `window.__mockBridge` with the documented test API. The mock maintains an internal state machine snapshot, dispatches state changes to subscribers registered via the renderer's `getBridge().onStateChanged(...)`, records every IPC payload it would have sent to main in an internal array exposed via `getLastEmittedIPC()`, and supports the four `__test_inject_error` kinds.
    Author `apps/achilles/test/e2e/scaffold.spec.ts` exercising the E2E behaviour test: navigate to the headless renderer, assert the floating-shell + reactive-circle stub elements render, call `page.evaluate(() => window.__mockBridge.setState('listening'))` and assert `[data-testid="reactive-circle"]` gains `data-state="listening"`, repeat for the other four states. Document at the top of the file that this is the Plan 11-01 scaffold spec; Plan 11-02 will add the full state-distinctness spec.
    Compile contract: every test file ends with `.test.ts` and is excluded by tsconfig.node.json / tsconfig.web.json (the CR-06 fix from Phase 09); the headless renderer bundle does NOT include `mock-bridge.ts` outside of the headless build mode. Per CLAUDE.md global: no emojis anywhere in source, tests, or logs.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff && npx vitest run --project phase-11-unit && npx tsc -p apps/achilles/tsconfig.node.json --noEmit && npx tsc -p apps/achilles/tsconfig.web.json --noEmit && npm --workspace @achilles/app run build:renderer && npx playwright test --project=achilles-renderer --grep scaffold</automated>
  </verify>
  <done>
    All 10 state-machine tests pass; all 6+ IPC schema tests pass; all 3 window tests pass with injected BrowserWindow stubs; all 3 hotkey tests pass with injected globalShortcut stubs; all 4 store tests pass with an in-memory electron-store mock; all 3+ mock-amplitude tests pass; the headless renderer Vite bundle builds; the Playwright `scaffold.spec.ts` passes asserting `[data-testid="floating-shell"]` renders, `window.__mockBridge` exposes the documented API surface, and `setState` for each of the five values updates `data-state` on the reactive-circle stub. The Phase 09 CR-06 + CR-07 hygiene holds: no `*.js`/`*.d.ts` files appear inside `apps/achilles/src/`, and `tsc -p tsconfig.node.json --listFiles | grep -v '^#' | grep -c '\.test\.ts'` returns 0 (test files are excluded from the build set).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main IPC | Untrusted renderer payloads cross into main; a compromised renderer could try to escalate IPC channels |
| OS→main (hotkey + permission) | Global hotkey events and macOS TCC status both originate outside the app process |
| disk persistence | electron-store values land on the local FS; safeStorage encrypts on capable OSes only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing | renderer→main IPC | mitigate | Every IPC channel parses payloads through a `.strict()` Zod schema in `apps/achilles/src/shared/ipc-schemas.ts`; unknown fields rejected, unknown channels logged + dropped |
| T-11-02 | Tampering | apps/achilles/package.json install | mitigate | electron@42.3.3, electron-store@10.1.0, zod@4.3.6, react@19.2.4 — all four are already in the monorepo OR locked at their last-stable versions per STACK.md. No `[ASSUMED]` packages introduced in this plan. |
| T-11-03 | Repudiation | mocked state transitions | accept | Mocked state machine is in-process and intentionally test-driven; no audit log required in Phase 11 (Phase 14 owns operational logging) |
| T-11-04 | Information Disclosure | store.ts safeStorage absent | mitigate | When safeStorage.isEncryptionAvailable() returns false, log a `[achilles]` warning naming the SAFE-01 boundary and continue plaintext (Phase 12 owns the API-key surface; Phase 11 persists only windowPosition + hotkeyMode + hotkeyKey, none of which are secret) |
| T-11-05 | Denial of Service | runaway IPC traffic during listening/speaking | mitigate | Mock amplitude tick is fixed at 50 ms (20 fps) per UI-SPEC; preload validates payload shape before forwarding to renderer; renderer subscribes via the bridge wrapper which is unsubscribe-on-unmount safe |
| T-11-06 | Elevation of Privilege | nodeIntegration in renderer | mitigate | createAchillesWindow locks webPreferences to { contextIsolation: true, nodeIntegration: false, sandbox: true } per UI-01 contract; window.test.ts asserts the exact options object |
</threat_model>

<verification>
- `npx tsc -p tsconfig.base.json --noEmit` exits 0 with the new aliases in place
- `npm --workspace @achilles/app run typecheck` exits 0
- `npx vitest run --project phase-11-unit` reports all tests green
- `npm --workspace @achilles/app run build:renderer` produces a Vite bundle under `apps/achilles/out/renderer/`
- `npx playwright test --project=achilles-renderer --grep scaffold` passes (the Playwright webServer launches Vite preview, NOT Electron)
- `grep -RIn 'achilles:' apps/achilles/src/shared/constants.ts | grep -v '^#' | grep -c "achilles:" >= 12` (every IPC channel constant carries the prefix)
- `grep -RIn 'process.env' apps/achilles/src/main apps/achilles/src/preload | grep -v '^#' | wc -l` returns a small bounded count (the store/hotkey code MUST NOT read environment variables — Phase 09 KeySource pattern; this guards against future regressions)
- `find apps/achilles/src -name '*.js' -o -name '*.d.ts' | wc -l` returns 0 (the src/.gitignore CR-07 guard holds)
</verification>

<success_criteria>
- apps/achilles workspace package exists, `npm install` succeeds at the repo root, `tsc -p tsconfig.base.json --noEmit` exits 0
- Every behaviour test in the 8 test files passes under `vitest run --project phase-11-unit`
- The BrowserWindow contract matches UI-01 verbatim (verified by window.test.ts against the captured constructor options)
- The mocked state-machine reducer covers all 5 states + the 8 documented event types with deterministic transitions
- The global hotkey path honours both toggle and PTT modes via the injected stub seams (UI-06 substrate)
- The `electron-store` schema persists windowPosition, hotkeyMode, hotkeyKey; safeStorage is consulted when available and gracefully falls back with a logged warning when absent
- The IPC Zod schemas reject unknown channels + unknown fields + out-of-range values (SAFE-01 precedent applied to the renderer↔main boundary)
- The preload bridge exposes the documented API surface via `contextBridge.exposeInMainWorld('achilles', ...)` with `contextIsolation: true` and `nodeIntegration: false` enforced by the window contract
- The minimal renderer bootstrap renders a stub that subscribes via the bridge wrapper so Plans 11-02 + 11-03 can replace `<App />` with the real composition root without touching `main.tsx` again
- The `MockAchillesBridge` test seam exposes the full documented surface so Plans 11-02 + 11-03 e2e tests can drive scenarios without launching Electron
- Workspace plumbing (`tsconfig.base.json` path aliases, `vitest.workspace.ts` phase-11-unit project + workspaceAlias entries, `playwright.config.ts` achilles-renderer project) is committed
- The Playwright scaffold spec passes against the headless Vite renderer bundle; NO Electron app launches during testing or CI
- Phase 09 CR-06 (tsconfig test-exclude) and CR-07 (`src/.gitignore` defensive guard) both applied to `apps/achilles`
- No emojis anywhere in source, tests, configs, or logs (CLAUDE.md global)
</success_criteria>

<output>
Create `.planning/phases/11-floating-ui-shell/11-01-SUMMARY.md` when done. Record: the locked BrowserWindow contract, the state-machine reducer signature, the IPC channel constants + Zod schema names, the preload `window.achilles` API, the `MockAchillesBridge` test seam, the workspace-plumbing additions (tsconfig.base.json aliases, vitest.workspace.ts project + aliases, playwright.config.ts project), Phase 11 hygiene applied (CR-06 + CR-07), and which downstream plans consume which contracts.
</output>
