---
phase: 11-floating-ui-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - apps/achilles/src/renderer/components/FloatingShell.tsx
  - apps/achilles/src/renderer/components/FloatingShell.test.tsx
  - apps/achilles/src/renderer/components/ReactiveCircle.tsx
  - apps/achilles/src/renderer/components/ReactiveCircle.test.tsx
  - apps/achilles/src/renderer/components/Waveform.tsx
  - apps/achilles/src/renderer/components/Waveform.test.tsx
  - apps/achilles/src/renderer/components/TranscriptOverlay.tsx
  - apps/achilles/src/renderer/components/TranscriptOverlay.test.tsx
  - apps/achilles/src/renderer/components/MockAnalyser.ts
  - apps/achilles/src/renderer/components/MockAnalyser.test.ts
  - apps/achilles/src/renderer/styles/tokens.css
  - apps/achilles/src/renderer/styles/components.css
  - apps/achilles/src/renderer/state/useAchillesState.ts
  - apps/achilles/src/renderer/state/useAchillesState.test.ts
  - apps/achilles/src/renderer/main.tsx
  - apps/achilles/test/e2e/state-distinctness.spec.ts
  - apps/achilles/test/e2e/circle-amplitude.spec.ts
  - apps/achilles/test/e2e/waveform.spec.ts
  - apps/achilles/test/e2e/transcript.spec.ts
  - apps/achilles/test/fixtures/amplitude-fixtures.ts
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-04
  - LOOP-02

must_haves:
  truths:
    - "Each of the 5 AchillesStates renders the reactive circle with a distinct accent color token and a distinct DOM data-state attribute so a Playwright assertion can confirm visual distinctness without flaky pixel diffs"
    - "Idle renders a slow breathing animation (CSS keyframe scaling 0.95↔1.05 over 2000ms) that PAUSES when document.visibilityState !== 'visible'"
    - "Listening scales the circle by 0.9 + rms * 0.5 from the mic amplitude stream; the waveform bars are driven by the same source"
    - "Speaking scales the circle by 0.9 + rms * 0.5 from the TTS amplitude stream; the waveform bars are driven by the TTS source"
    - "Processing renders an indeterminate 270deg rotating ring at one rotation per 1200ms; the circle does NOT scale (visually distinct from listening/speaking)"
    - "Error renders a static stroke-only red circle with a 600ms dampened shake; the waveform shows a flat baseline at the error accent at 0.3 opacity"
    - "The waveform Canvas2D renders exactly 32 bars (configurable via prop) with bar width 4px, gap 2px, max bar height 22px per UI-SPEC §2"
    - "Partial transcripts render at opacity 0.7, committed transcripts at opacity 1.0, and committed transcripts auto-fade after 15000ms of idle (LOOP-02)"
    - "Newest transcripts appear at the bottom; oldest scrolled out; max 3 visible lines (UI-SPEC §1 listening transcript row)"
    - "All visual decisions are encoded as CSS custom properties in tokens.css so Phase 12+ can override the palette without touching components"
  artifacts:
    - path: "apps/achilles/src/renderer/components/FloatingShell.tsx"
      provides: "Top-level layout container; positions circle, waveform, transcript region per UI-SPEC §2 pixel grid; routes overlay slots for Plan 11-03"
      contains: "FloatingShell"
    - path: "apps/achilles/src/renderer/components/ReactiveCircle.tsx"
      provides: "96px SVG circle with state-driven color tokens, breathing animation, amplitude-driven scale + glow"
      contains: "ReactiveCircle"
    - path: "apps/achilles/src/renderer/components/Waveform.tsx"
      provides: "32-bar Canvas2D visualizer subscribed to an AnalyserNode (or MockAnalyser)"
      contains: "Waveform"
    - path: "apps/achilles/src/renderer/components/TranscriptOverlay.tsx"
      provides: "LOOP-02 partial+committed transcript renderer with 0.7/1.0 opacity rules and 15s auto-fade"
      contains: "TranscriptOverlay"
    - path: "apps/achilles/src/renderer/components/MockAnalyser.ts"
      provides: "MockAnalyser class shape-matching AnalyserNode (frequencyBinCount + getByteFrequencyData) driven by the Plan 11-01 mock amplitude stream"
      contains: "MockAnalyser"
    - path: "apps/achilles/src/renderer/styles/tokens.css"
      provides: "CSS custom properties for spacing, typography, color (5 state accents + neutrals), motion durations, breathing period; reduced-motion media query"
      contains: "--achilles-listening"
    - path: "apps/achilles/src/renderer/styles/components.css"
      provides: "Component CSS for the floating shell layout grid, circle keyframes (breathing, spin, shake), waveform canvas sizing, transcript opacity rules"
      contains: "@keyframes spin"
    - path: "apps/achilles/src/renderer/state/useAchillesState.ts"
      provides: "useReducer + Context hook exposing { state, permissionState, micAmplitude, ttsAmplitude, partial, committed, error, dispatch } to all renderer components"
      contains: "useAchillesState"
    - path: "apps/achilles/test/e2e/state-distinctness.spec.ts"
      provides: "Playwright test asserting each of the 5 AchillesStates renders distinct data-state + distinct computed accent color"
      contains: "state-distinctness"
  key_links:
    - from: "apps/achilles/src/renderer/components/FloatingShell.tsx"
      to: "apps/achilles/src/renderer/state/useAchillesState.ts"
      via: "useAchillesState() hook supplies state, amplitudes, transcripts"
      pattern: "useAchillesState"
    - from: "apps/achilles/src/renderer/components/ReactiveCircle.tsx"
      to: "apps/achilles/src/renderer/styles/tokens.css"
      via: "CSS custom properties --achilles-{state} drive the accent color"
      pattern: "--achilles-listening"
    - from: "apps/achilles/src/renderer/components/Waveform.tsx"
      to: "apps/achilles/src/renderer/components/MockAnalyser.ts"
      via: "Waveform consumes AnalyserNode | MockAnalyser via the analyser prop"
      pattern: "MockAnalyser"
    - from: "apps/achilles/src/renderer/main.tsx"
      to: "apps/achilles/src/renderer/components/FloatingShell.tsx"
      via: "main.tsx renders <FloatingShell /> at the React root (replaces the Plan 11-01 stub)"
      pattern: "FloatingShell"
---

<objective>
Implement the four renderer components that make Phase 11 visually verifiable: `FloatingShell` (the layout grid composition root that positions the circle, waveform, and transcript area per UI-SPEC §2), `ReactiveCircle` (the 96px SVG circle whose state-driven color + breathing/amplitude/spin/shake animations are the dominant state cue), `Waveform` (the 32-bar Canvas2D visualizer subscribed to an `AnalyserNode` or `MockAnalyser`), and `TranscriptOverlay` (the LOOP-02 partial+committed transcript renderer with the documented opacity rules and 15-second auto-fade). Wire the renderer state container (`useAchillesState`) that consumes the Plan 11-01 preload bridge / mock bridge and exposes a typed React hook to every component. Ship the design tokens + component CSS so all five states are visually distinct under the UI-SPEC color palette without depending on framer-motion. Replace the Plan 11-01 minimal renderer stub at `main.tsx` with the real composition root. Wire four focused Playwright specs against the headless Vite bundle that prove the five states render with distinct accent colors (UI-02), the circle scales with the mock amplitude during listening + speaking (UI-03), the waveform renders 32 bars driven by the AnalyserNode-shaped source (UI-04), and partial/committed transcripts honour the LOOP-02 opacity + fade contract.

Purpose: Plan 11-01 set the contracts; Plan 11-02 is the surface that proves the contracts are usable. After this plan, a human reviewer driving the headless renderer through `window.__mockBridge.setState(...)` can identify each state from a glance and confirm transcripts surface as documented. Plan 11-03 layers in the drag-persistence wiring, the macOS permission flow, the settings popover, and the error banner; both Wave 2 plans land on disjoint component sets (FloatingShell delegates overlay rendering to children supplied by Plan 11-03 via the renderer composition).

Output: A fully visual Achilles renderer covering UI-02, UI-03, UI-04, LOOP-02 driven by the Plan 11-01 mock bridge, verified by Vitest unit tests for pure helpers + reducers and four Playwright headless specs against the built renderer bundle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-floating-ui-shell/11-CONTEXT.md
@.planning/phases/11-floating-ui-shell/11-UI-SPEC.md
@.planning/phases/11-floating-ui-shell/11-01-PLAN.md
@apps/achilles/src/shared/constants.ts
@apps/achilles/src/shared/ipc-schemas.ts
@apps/achilles/src/renderer/bridge.ts
@apps/achilles/test/mocks/mock-bridge.ts

<interfaces>
<!-- Contracts from Plan 11-01 this plan consumes. -->

Bridge surface (apps/achilles/src/renderer/bridge.ts → getBridge()):
  onStateChanged(cb: (s: AchillesState) => void): () => void
  onTranscriptPartial(cb: (text: string) => void): () => void
  onTranscriptCommitted(cb: (entry: { id: string; text: string; committedAt: number }) => void): () => void
  onMicAmplitude(cb: (rms: number) => void): () => void
  onTtsAmplitude(cb: (rms: number) => void): () => void
  onPermissionState(cb: (p: PermissionState) => void): () => void
  onError(cb: (msg: string) => void): () => void
  requestState(state: AchillesState): void

Constants from @achilles/app/shared/constants.ts:
  WINDOW_WIDTH = 260, WINDOW_HEIGHT = 260
  ACHILLES_STATES = ['idle','listening','processing','speaking','error'] as const
  type AchillesState = typeof ACHILLES_STATES[number]

UI-SPEC §1 visual contract per state:
  idle:        accent = --achilles-idle (#5F6471), circle 96px fill solid 0.4 opacity, breathing 0.95↔1.05 over 2000ms, waveform static 32 bars at 2px
  listening:   accent = --achilles-listening (#3DD68C), radial gradient fill, scale 0.9 + rms*0.5, glow 0.7 + rms*0.3, waveform bars from mic AnalyserNode
  processing:  accent = --achilles-processing (#F5A623), transparent fill, 270deg rotating ring 1200ms per rotation, scale 1.0 (no amplitude), waveform shimmer
  speaking:    accent = --achilles-speaking (#4A9EFF), radial gradient, scale 0.9 + ttsRms*0.5, waveform bars from TTS AnalyserNode
  error:       accent = --achilles-error (#FF4D4F), stroke-only no fill, 600ms dampened shake, waveform flat at 0.3 opacity

UI-SPEC §2 layout pixel positions:
  drag handle:        top:0  left:0   size 260×30  (UI-SPEC owned by Plan 11-03 for drag persistence wiring)
  circle:             center x:130 y:98 size 96×96
  waveform:           top:148 left:35 size 190×22  (32 bars × 4px + 31 gaps × 2px = 190px)
  transcript area:    top:176 left:16 right:16 size 228×64  (3 lines visible)
  settings affordance:bottom:8 right:12 size 16×16  (owned by Plan 11-03)
  error banner:       top:0 left:0 size 260×90  (owned by Plan 11-03)
  permission overlay: top:0 left:0 size 260×260 (owned by Plan 11-03)

LOOP-02 transcript contract:
  partial:     opacity 0.7 (--achilles-text-dim), at most one partial visible at a time, replaced by next partial event
  committed:   opacity 1.0 (--achilles-text), newest at bottom, oldest scrolled out, max 3 lines visible
  auto-fade:   committed lines fade 1.0 → 0 over 1500ms linear after 15000ms of idle (state === 'idle' duration > 15s)
  hidden:      in 'speaking' state, transcripts fade out 1s into the state (user is listening, not reading)
  hidden:      in 'error' state, transcripts hidden (error banner takes focus — banner rendered by Plan 11-03)

Component test seam (consumed by Plan 11-03 + e2e specs):
  FloatingShell exposes <slot> props for overlay rendering: `slot:permissionOverlay`, `slot:errorBanner`, `slot:settingsPopover` rendered as optional children.
  React composition: <FloatingShell> {permissionOverlay && <PermissionOverlay/>} {errorBanner && <ErrorBanner/>} </FloatingShell>
  Plan 11-03 owns the actual overlay components; Plan 11-02 ships the slot wiring and the conditional render placeholders (null-render is OK in 11-02; 11-03 supplies the real components).

data-testid selectors locked by UI-SPEC §10:
  floating-shell, reactive-circle, waveform, transcript-overlay, transcript-partial, transcript-committed
  data-state attribute on reactive-circle root
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Design tokens + renderer state container + MockAnalyser + ReactiveCircle + Waveform</name>
  <files>
    apps/achilles/src/renderer/styles/tokens.css,
    apps/achilles/src/renderer/styles/components.css,
    apps/achilles/src/renderer/state/useAchillesState.ts,
    apps/achilles/src/renderer/state/useAchillesState.test.ts,
    apps/achilles/src/renderer/components/MockAnalyser.ts,
    apps/achilles/src/renderer/components/MockAnalyser.test.ts,
    apps/achilles/src/renderer/components/ReactiveCircle.tsx,
    apps/achilles/src/renderer/components/ReactiveCircle.test.tsx,
    apps/achilles/src/renderer/components/Waveform.tsx,
    apps/achilles/src/renderer/components/Waveform.test.tsx,
    apps/achilles/test/fixtures/amplitude-fixtures.ts
  </files>
  <behavior>
    - Test US1 (useAchillesState.test.ts): The initial reducer state is `{ state: 'idle', permissionState: 'granted', micAmplitude: 0, ttsAmplitude: 0, partial: '', committed: [], error: null }`.
    - Test US2: A `STATE_CHANGED` action with state='listening' sets state='listening' and resets micAmplitude to 0 (fresh stream).
    - Test US3: A `TRANSCRIPT_PARTIAL` action replaces the previous partial (not appended) per UI-SPEC §1.
    - Test US4: A `TRANSCRIPT_COMMITTED` action appends the new entry to `committed` and clears `partial`.
    - Test US5: When `committed.length > 3`, the reducer trims to the most recent 3 (max visible lines).
    - Test US6: A `MIC_AMPLITUDE` action clamps rms to [0,1] (defensive — guards against MockAnalyser bugs in Wave 2 dev).
    - Test US7: An `ERROR` action sets `error: { message }`; a `STATE_CHANGED` to 'idle' clears the error.
    - Test MA1 (MockAnalyser.test.ts): new MockAnalyser({ state: 'listening', amplitudeSource: fakeStream }).frequencyBinCount === 32 (matches the Waveform bar count default).
    - Test MA2: getByteFrequencyData(array: Uint8Array) writes 32 values in [0,255]; values track the supplied amplitude source (high rms → high mean across bars).
    - Test MA3: When the supplied state is 'idle' or 'processing' or 'error', getByteFrequencyData fills the array with a state-specific static or shimmer pattern (deterministic for unit testability).
    - Test MA4: stop() halts the internal tick so subsequent getByteFrequencyData calls return the last-emitted snapshot without further mutation.
    - Test RC1 (ReactiveCircle.test.tsx): For each of the 5 states, the rendered root element carries `data-testid="reactive-circle"` and `data-state="{state}"`.
    - Test RC2: In 'idle' state, the rendered element has CSS class containing 'breathing'; in 'processing' it contains 'spinning'; in 'error' it contains 'shake'; in 'listening' / 'speaking' it has 'amplitude-driven'.
    - Test RC3: When the amplitude prop changes during listening, the inline CSS `--circle-scale` custom property updates to `0.9 + amplitude * 0.5` (verified by reading `getComputedStyle` or by direct style attribute assertion).
    - Test RC4: When `document.visibilityState !== 'visible'` (simulated via Object.defineProperty + dispatchEvent), the breathing class is removed from the idle root (CPU-save per UI-SPEC §1).
    - Test RC5: Clicking the circle emits the prop `onClick`; right-clicking emits `onRightClick` with the React.MouseEvent.
    - Test WF1 (Waveform.test.tsx): renders a `<canvas>` element with `data-testid="waveform"` of size 190×22 (per UI-SPEC §2).
    - Test WF2: Given barCount=32, the component schedules a draw call (via a stubbed requestAnimationFrame) that reads 32 values from the analyser.getByteFrequencyData and computes 32 bar positions.
    - Test WF3: When the analyser prop is null, the canvas renders a flat static baseline (no rAF loop, no analyser polling).
    - Test WF4: When the state prop is 'idle', the canvas color matches `--achilles-text-dim`; when 'listening' it matches `--achilles-listening`; when 'speaking' it matches `--achilles-speaking`; when 'error' it matches `--achilles-error` at 0.3 opacity.
  </behavior>
  <action>
    Author `apps/achilles/src/renderer/styles/tokens.css` declaring at `:root` every documented CSS custom property from UI-SPEC §design-system: spacing (`--space-xs:4px` through `--space-xl:32px`), typography (`--font-family`, `--font-size-body:13px`, `--font-size-label:11px`, `--font-size-heading:15px`, line-heights, weights), neutral colors (`--achilles-bg: rgba(20,22,28,0.85)`, `--achilles-surface: rgba(28,30,38,0.92)`, `--achilles-text: #E8EAED`, `--achilles-text-dim: rgba(232,234,237,0.7)`, `--achilles-border: rgba(255,255,255,0.08)`), the 5 state accents (`--achilles-idle: #5F6471`, `--achilles-listening: #3DD68C`, `--achilles-processing: #F5A623`, `--achilles-speaking: #4A9EFF`, `--achilles-error: #FF4D4F`), destructive (`--achilles-destructive: #FF4D4F` — equal to error per UI-SPEC), motion easings + durations (`--motion-ease-out`, `--motion-ease-in-out`, `--motion-duration-fast:150ms`, `--motion-duration-default:250ms`, `--motion-duration-state:350ms`, `--breathing-period:2000ms`). Append a `@media (prefers-reduced-motion: reduce)` rule that collapses every motion duration + breathing-period to `0ms` (UI-SPEC §3 + §5).
    Author `apps/achilles/src/renderer/styles/components.css` covering: window-root layout (260×260, pointer-events:none default, pointer-events:auto on the visible children — drag-handle, circle, waveform, transcript, settings affordance, overlays); `.floating-shell` flex/grid following UI-SPEC §2 exact pixel positions (use `position: absolute` per region; the 260×260 is small enough that absolute positioning is the cleanest fit); `.reactive-circle` base styles (width:96px height:96px border-radius:50% display:flex absolute centered at x:130 y:98); `@keyframes breathing` (transform: scale(0.95) → scale(1.05) → scale(0.95) over 100% of `var(--breathing-period)`); `@keyframes spin` (rotate 0 → 360deg over 1200ms linear); `@keyframes shake` (translateX -2px / 2px / -1px / 1px / 0 over 600ms with dampening); `.reactive-circle.breathing { animation: breathing var(--breathing-period) infinite ease-in-out }`; `.reactive-circle.spinning::before` (the 270deg arc as a pseudo-element); `.reactive-circle.shake { animation: shake 600ms ease-out }`; `.reactive-circle.amplitude-driven { transform: scale(var(--circle-scale, 1)) }`; `.waveform { width:190px height:22px position:absolute top:148px left:35px }`; `.transcript-overlay` (top:176 left:16 right:16 height:64 overflow:hidden); `.transcript-partial { opacity:0.7 color: var(--achilles-text-dim) }`; `.transcript-committed { opacity:1.0 color: var(--achilles-text) }`; `.transcript-committed.fading { animation: transcript-fade 1500ms linear forwards }` with the keyframe `@keyframes transcript-fade { from { opacity:1 } to { opacity:0 } }`. Every selector is scoped under a `.floating-shell` ancestor where appropriate so the styles do not leak.
    Author `apps/achilles/src/renderer/state/useAchillesState.ts` exporting an `AchillesReducerState` interface, an `AchillesAction` discriminated union (`STATE_CHANGED`, `TRANSCRIPT_PARTIAL`, `TRANSCRIPT_COMMITTED`, `MIC_AMPLITUDE`, `TTS_AMPLITUDE`, `PERMISSION_CHANGED`, `ERROR`, `ERROR_DISMISS`), a pure `reducer(state, action)` function, an `AchillesStateProvider` React component that wires the reducer to `getBridge()` via the documented `on*` subscriptions in `useEffect`, and a `useAchillesState()` hook returning `{ state, permissionState, micAmplitude, ttsAmplitude, partial, committed, error, dispatch }`. The reducer is colocated with the hook so unit tests can exercise it without React rendering. Apply the US1–US7 behaviour rules exactly. Colocate `useAchillesState.test.ts` with the 7 behaviour tests.
    Author `apps/achilles/src/renderer/components/MockAnalyser.ts` exporting a `MockAnalyser` class implementing the AnalyserNode-shape contract: `readonly frequencyBinCount: number`, `getByteFrequencyData(array: Uint8Array): void`. The constructor accepts `{ state: AchillesState; amplitudeSource?: () => number; barCount?: number; seed?: number }`. The class spawns an internal 50ms-tick (clearable via `stop()`) that updates an internal `Uint8Array` of length `frequencyBinCount` (default 32). For 'listening' / 'speaking', the bars track the supplied amplitudeSource (the Plan 11-01 mock amplitude stream — re-imported under the hood as `createMockAmplitudeStream` from the shared module, BUT keeping the renderer-side analyser independent of `electron`'s main process: the Plan 11-01 mock-amplitude file lives under `src/main/`, and Plan 11-02 must NOT import from main — instead, this file inlines an equivalent deterministic generator using the same LCG seed convention). For 'processing', emit a stagger-shimmer (low fixed height with subtle 4-bar phase offset). For 'idle' / 'error', emit a flat baseline (all 2/255 ≈ value `2`). Document in code that the MockAnalyser is a Phase 11 testability seam; Phase 12 will replace it with a real AnalyserNode wired off `getUserMedia` + TTS playback. Colocate `MockAnalyser.test.ts` with MA1–MA4 behaviour tests.
    Author `apps/achilles/src/renderer/components/ReactiveCircle.tsx` exporting `ReactiveCircleProps` (per UI-SPEC §9 component contract: `state, amplitude, onClick, onRightClick`) and the `ReactiveCircle` React component. The component renders an SVG `<circle>` inside a `<div role="status" aria-live="polite" aria-label="Achilles {state}" data-testid="reactive-circle" data-state={state}>`. Class list is computed deterministically from `state` + the `document.visibilityState` listener (state==='idle' → 'breathing' WHEN visible; state==='processing' → 'spinning'; state==='speaking' or 'listening' → 'amplitude-driven'; state==='error' → 'shake'). For amplitude-driven states, set `style={{ ['--circle-scale' as string]: 0.9 + amplitude * 0.5 }}` inline so the keyframe-less transform scaling works under reduced-motion as well (UI-SPEC §5 amplitude exemption). For 'processing', the 270deg rotating ring is a CSS pseudo-element controlled by the `.spinning` class. For 'error', wire the `.shake` class to auto-remove after 600ms via a setTimeout so the shake fires once per error entry. Use a `useEffect` with the documented visibilitychange listener for the idle CPU-save path. Click / right-click are forwarded to `props.onClick` / `props.onRightClick` (the latter receives the React.MouseEvent so Plan 11-03 can position the settings popover). Colocate `ReactiveCircle.test.tsx` with RC1–RC5 behaviour tests using `@testing-library/react`.
    Author `apps/achilles/src/renderer/components/Waveform.tsx` exporting `WaveformProps` (per UI-SPEC §9: `state, analyser, barCount?: number = 32, barWidth?: number = 4, barGap?: number = 2, maxBarHeight?: number = 22`) and the `Waveform` React component. Render a `<canvas data-testid="waveform" width={190} height={22}>` and acquire a 2D context. Use `useEffect` to start a `requestAnimationFrame` loop that polls the analyser at most every 50ms (20fps) and redraws all bars. For each bar `i` in `[0, barCount)`, compute height from `byteFrequencyData[i] / 255 * maxBarHeight` and fill at `x = i * (barWidth + barGap)`. Color is the state-dependent token: idle → `--achilles-text-dim`; listening → `--achilles-listening`; processing → `--achilles-processing` at 0.5 opacity; speaking → `--achilles-speaking`; error → `--achilles-error` at 0.3 opacity. When `analyser === null` or state is 'idle'/'error', skip the rAF loop and render a static baseline. Read the CSS custom property via `getComputedStyle(document.documentElement).getPropertyValue('--achilles-{state}')` for the fill style. Clean up the rAF on unmount. Colocate `Waveform.test.tsx` with WF1–WF4 behaviour tests (use a 2D canvas mock if jsdom lacks one; jsdom 26 ships a basic Canvas2D shim).
    Author `apps/achilles/test/fixtures/amplitude-fixtures.ts` exporting two deterministic 100-sample fixture arrays — `LISTENING_FIXTURE: readonly number[]` and `SPEAKING_FIXTURE: readonly number[]` — that match the seeded LCG output of `createMockAmplitudeStream('listening')` / `createMockAmplitudeStream('speaking')` for seed 42. The fixture exists so renderer e2e tests (Plan 11-02 Task 2 below) can pre-compute the expected `--circle-scale` values without re-running the generator in the page context. Document the generator → fixture relationship in a code comment.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff && npx vitest run --project phase-11-unit -- 'apps/achilles/src/renderer'</automated>
  </verify>
  <done>
    All 7 useAchillesState reducer tests pass; all 4 MockAnalyser tests pass; all 5 ReactiveCircle tests pass under jsdom + @testing-library/react; all 4 Waveform tests pass with the Canvas 2D shim; tokens.css declares all 5 state accent variables and the prefers-reduced-motion override; components.css defines breathing, spin, shake, transcript-fade keyframes; no test file imports from `src/main/` (renderer-main separation lock).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FloatingShell composition root + TranscriptOverlay (LOOP-02) + main.tsx wiring + 4 Playwright specs proving UI-02/03/04 + LOOP-02</name>
  <files>
    apps/achilles/src/renderer/components/FloatingShell.tsx,
    apps/achilles/src/renderer/components/FloatingShell.test.tsx,
    apps/achilles/src/renderer/components/TranscriptOverlay.tsx,
    apps/achilles/src/renderer/components/TranscriptOverlay.test.tsx,
    apps/achilles/src/renderer/main.tsx,
    apps/achilles/test/e2e/state-distinctness.spec.ts,
    apps/achilles/test/e2e/circle-amplitude.spec.ts,
    apps/achilles/test/e2e/waveform.spec.ts,
    apps/achilles/test/e2e/transcript.spec.ts
  </files>
  <behavior>
    - Test FS1 (FloatingShell.test.tsx): When useAchillesState supplies state='listening' and partial='hello', the rendered tree contains `[data-testid="floating-shell"]`, `[data-testid="reactive-circle"]` with data-state='listening', `[data-testid="waveform"]`, `[data-testid="transcript-overlay"]`, and `[data-testid="transcript-partial"]` with textContent='hello'.
    - Test FS2: FloatingShell accepts optional `permissionOverlay`, `errorBanner`, `settingsPopover` children-as-slots props; when supplied, each renders as a sibling of the layout regions; when omitted, only the core layout is rendered (slot props are how Plan 11-03 plugs in its overlays).
    - Test FS3: When state==='error', FloatingShell does NOT render the transcript-overlay (UI-SPEC §1 — error overlay takes focus). When state==='speaking' for > 1000ms, transcript-overlay receives a CSS class 'speaking-hide' that fades it out.
    - Test FS4: When permissionState is 'denied' or 'restricted' AND the permissionOverlay slot is supplied, FloatingShell hides the circle + waveform + transcript regions (the permission overlay takes the full window per UI-SPEC §2).
    - Test TO1 (TranscriptOverlay.test.tsx): With committed=[{id:'a',text:'first',committedAt:T0}, {id:'b',text:'second',committedAt:T0+10}], rendering shows both lines with newest at the bottom; assert via order of `[data-testid="transcript-committed"]` matches.
    - Test TO2: With committed.length === 5, only the last 3 render (max visible lines per UI-SPEC).
    - Test TO3 (uses vi.useFakeTimers): After state===' idle' for 15000ms, every committed line gains the CSS class 'fading'; after a further 1500ms the class persists (the animation drives opacity to 0 via the keyframe, not removal — assertion is class presence).
    - Test TO4: A new TRANSCRIPT_COMMITTED resets the fade timer (any line committed within the last 15s is NOT marked fading).
    - Test TO5: When state==='speaking' for > 1000ms, the partial element is hidden via the speaking-hide class (UI-SPEC §1 speaking row "fades out 1 second into speaking state").
    - Test TO6: Empty partial (empty string) renders no `[data-testid="transcript-partial"]` element (avoid the 0.7-opacity orphan box).
    - E2E1 (state-distinctness.spec.ts): For each of the 5 states ['idle','listening','processing','speaking','error'], `window.__mockBridge.setState(s)` followed by `expect(page.locator('[data-testid="reactive-circle"]')).toHaveAttribute('data-state', s)`. THEN read the computed `background-color` of the circle (or its inner gradient stop, or the box-shadow color — pick whichever is reliably distinct per state) via `getComputedStyle` evaluated in the page context, store all five colors, and assert all 5 are pairwise distinct (the strict UI-02 distinctness contract).
    - E2E2 (circle-amplitude.spec.ts): setState('listening') THEN emit 5 deterministic amplitude values (LISTENING_FIXTURE[0..4]) one per 100ms via `window.__mockBridge.emitMicAmplitude(v)`; after each, assert `getComputedStyle(reactiveCircle).getPropertyValue('--circle-scale')` equals `0.9 + v * 0.5` within a 0.001 tolerance. Repeat for setState('speaking') with `emitTtsAmplitude` + SPEAKING_FIXTURE — confirming the circle tracks BOTH amplitude sources (UI-03).
    - E2E3 (waveform.spec.ts): setState('listening'); assert `[data-testid="waveform"]` is a `<canvas>` of width 190 height 22; sample pixel colors at known bar centers via `page.evaluate` reading the canvas 2D context's getImageData (NB the headless canvas may be a no-op in some Vite preview modes — fall back to asserting the canvas DOM is present + the analyser-side MockAnalyser was constructed by the FloatingShell). At minimum: assert (a) one `<canvas>` element exists with the correct testid, (b) the renderer has instantiated a MockAnalyser with frequencyBinCount === 32 (expose this via `window.__achilles_debug` set in dev/headless mode only — gated by a build-time flag in the Vite config so it never ships in production).
    - E2E4 (transcript.spec.ts): setState('listening'); emitPartialTranscript('hello wor'); assert `[data-testid="transcript-partial"]` has CSS `opacity` === '0.7' (LOOP-02). emitCommittedTranscript('hello world'); assert at least one `[data-testid="transcript-committed"]` exists with CSS `opacity` === '1' AND textContent contains 'hello world'. Wait 15s wall-clock OR use `page.clock.fastForward(15000)` (Playwright clock API) and assert the committed line picks up the 'fading' class.
  </behavior>
  <action>
    Author `apps/achilles/src/renderer/components/FloatingShell.tsx` exporting `FloatingShellProps` (per UI-SPEC §9) and the `FloatingShell` React component. The component pulls everything from `useAchillesState()` rather than props (the props in UI-SPEC §9 are documented for clarity; Plan 11-02 uses the Context hook to avoid prop-drilling). FloatingShell renders:
      <div className="floating-shell" data-testid="floating-shell">
        <DragHandle />  // owned by Plan 11-03 — Plan 11-02 ships an empty 260×30 div with `-webkit-app-region: drag` and a data-testid="drag-handle" stub
        <ReactiveCircle state={state} amplitude={state==='listening' ? micAmplitude : state==='speaking' ? ttsAmplitude : 0} onClick={...} onRightClick={...} />
        <Waveform state={state} analyser={analyser} />
        {state !== 'error' && permissionState === 'granted' && <TranscriptOverlay state={state} partial={partial} committed={committed} />}
        {props.permissionOverlay}   // Plan 11-03 supplies the real PermissionOverlay component
        {props.errorBanner}          // Plan 11-03 supplies ErrorBanner
        {props.settingsPopover}      // Plan 11-03 supplies SettingsPopover
      </div>
    The `analyser` is constructed once via `useMemo(() => new MockAnalyser({ state, amplitudeSource: ... }), [state])`. Wire the `onClick` handler to call `bridge.requestState('processing')` when state==='listening' (commits the in-flight utterance) per UI-SPEC §4. Wire `onRightClick` to call a `props.onSettingsOpen?.(event.clientX, event.clientY)` callback (Plan 11-03 wires the popover position). Apply the FS3 / FS4 visibility rules (state==='error' hides transcript-overlay; state==='speaking' for >1000ms applies 'speaking-hide' via a useEffect timer; permission overlay full-screen hides core regions). Colocate `FloatingShell.test.tsx` with FS1–FS4 behaviour tests, using `@testing-library/react` and a mocked `getBridge()` returning a stub.
    Author `apps/achilles/src/renderer/components/TranscriptOverlay.tsx` exporting `TranscriptOverlayProps` (per UI-SPEC §9) and the `TranscriptOverlay` React component. The component slices committed to the last `maxVisibleLines` (default 3), renders newest-at-bottom, applies `transcript-partial`/`transcript-committed` CSS classes, and runs a `setInterval(check, 1000)` that — when state==='idle' and the most-recent commit was > fadeAfterMs ms ago — adds 'fading' to every committed line. Returns no partial element when partial is empty (TO6). When state==='speaking' and the speaking duration > 1000ms, adds 'speaking-hide' to the partial AND the most-recent committed (TO5). Cleanup the interval on unmount. Colocate `TranscriptOverlay.test.tsx` with TO1–TO6 behaviour tests using `vi.useFakeTimers()` for the timer-driven assertions.
    Modify `apps/achilles/src/renderer/main.tsx` — replace the Plan 11-01 stub with the real composition root: `createRoot(document.getElementById('root')!).render(<AchillesStateProvider><FloatingShell /></AchillesStateProvider>)`. Import the tokens.css + components.css. Add a build-time conditional `if (import.meta.env.MODE === 'headless') { window.__achilles_debug = { ... } }` exposing the live `MockAnalyser` reference so e2e3 can assert frequencyBinCount === 32 without breaking the prod build (Vite tree-shakes the branch when MODE !== 'headless'). The headless Vite mode comes from the `apps/achilles/electron.vite.config.ts` headless renderer target Plan 11-01 wired.
    Author `apps/achilles/test/e2e/state-distinctness.spec.ts` (E2E1 above). Iterate over the 5 states, for each: call `window.__mockBridge.setState(s)`, await DOM update, snapshot the circle's computed accent color via `getComputedStyle(reactiveCircle).boxShadow` (or `backgroundColor` of the circle's inner span — pick whichever style this plan's CSS guarantees is distinct per state; for 'processing' which has no fill, the rotating ring's border-color via the pseudo-element comes from the parent's `--circle-color-current` custom property). Maintain a `Set<string>` of observed values; at the end assert `set.size === 5` (UI-02 strict distinctness).
    Author `apps/achilles/test/e2e/circle-amplitude.spec.ts` (E2E2 above). Setup: read the fixtures from `test/fixtures/amplitude-fixtures.ts` into the page via `page.addInitScript({ content: 'window.__FIXTURES = ...' })` OR by `page.evaluate(({ listening }) => { ... }, { listening: LISTENING_FIXTURE })`. For each fixture sample, dispatch the amplitude event via `__mockBridge.emitMicAmplitude(v)`, then `expect.poll` the computed `--circle-scale` custom property until it equals `0.9 + v * 0.5` within tolerance 0.001. Repeat for speaking. Document a known-limitation comment for any value that depends on rAF — use Playwright clock if available.
    Author `apps/achilles/test/e2e/waveform.spec.ts` (E2E3 above). Assert canvas DOM present + viewport width/height match UI-SPEC §2. Assert `window.__achilles_debug.analyser.frequencyBinCount === 32`. Add a non-strict pixel sample if the headless preview supports canvas readback; otherwise document the structural assertion as the sufficient proof.
    Author `apps/achilles/test/e2e/transcript.spec.ts` (E2E4 above). Use `page.clock.install()` + `page.clock.fastForward(15000)` for the auto-fade assertion; if Playwright clock API does not cover all scenarios, fall back to a 16s real wait gated on the `process.env.CI` flag (CI mode uses real wait to honour the strict 15s contract).
    All four e2e files include a top-of-file comment: `// Phase 11 Plan 11-02 — proves UI-{number} against the headless renderer bundle. No real Electron app launches in CI.`
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff && npx vitest run --project phase-11-unit -- 'apps/achilles/src/renderer/components' && npm --workspace @achilles/app run build:renderer && npx playwright test --project=achilles-renderer --grep "state-distinctness|circle-amplitude|waveform|transcript"</automated>
  </verify>
  <done>
    All 4+4+6 = 14 unit tests pass; the headless Vite build emits a renderer bundle that renders FloatingShell with the real composition root; 4 Playwright specs pass against the headless bundle proving the 5 states render with distinct accents (UI-02), the circle scales with both mic and TTS amplitude streams from the LISTENING/SPEAKING fixtures (UI-03), the Waveform renders 32 bars driven by the MockAnalyser (UI-04), and partial/committed transcripts honour the 0.7/1.0 opacity + 15s auto-fade contract (LOOP-02). No Electron app launches during the test run. Phase 09 CR-06 + CR-07 hygiene still holds (find apps/achilles/src -name '*.js' returns 0).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main IPC | Click and right-click on the circle dispatch through the bridge — payloads cross the SAFE-01 boundary established in Plan 11-01 |
| amplitude stream | Mocked amplitude values are deterministic but flow through React state — defence in depth: clamp to [0,1] in the reducer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Information Disclosure | Transcript rendering | mitigate | TranscriptOverlay receives sanitized text via the IPC schema (text:z.string().min(1)). No HTML allowed; React's default text-node rendering is XSS-safe. No copy-to-clipboard affordance in Phase 11 (CONTEXT.md decision). |
| T-11-08 | Spoofing | Amplitude streams | mitigate | useAchillesState reducer clamps mic/tts amplitude to [0,1] regardless of source (defence-in-depth against a buggy MockAnalyser or a future malicious bridge). |
| T-11-09 | Denial of Service | rAF loop on Waveform | mitigate | The rAF loop is gated by a 50ms tick (20fps); when analyser is null OR state is idle/error, the loop is not started, so the Waveform consumes ~0 CPU outside of amplitude states. |
| T-11-10 | Tampering | MockAnalyser exposed via __achilles_debug | mitigate | The debug surface is behind `import.meta.env.MODE === 'headless'` so Vite tree-shakes the branch in production. window.test.ts (Plan 11-01) asserts contextIsolation; this seam exists only in the headless build target. |
| T-11-11 | Information Disclosure | Transcript auto-fade timing | accept | The 15s auto-fade is a UX contract, not a security control. A user reading their floating UI from across the room sees committed transcripts vanish after 15s — this is a privacy nudge, not a guarantee. Phase 14 owns the formal "no transcript persistence" guarantee. |
</threat_model>

<verification>
- `npx vitest run --project phase-11-unit` reports all unit tests green
- `npm --workspace @achilles/app run build:renderer` produces a Vite bundle
- `npx playwright test --project=achilles-renderer` passes the state-distinctness, circle-amplitude, waveform, and transcript specs
- `grep -RIn 'import.*from.*src/main' apps/achilles/src/renderer | wc -l` returns 0 (renderer-main process separation lock — renderer never reaches into main)
- `grep -RIn 'framer-motion' apps/achilles | wc -l` returns 0 (UI-SPEC §design-system declares CSS-only motion; no framer-motion dependency added in Phase 11)
- The CSS tokens.css declares exactly 5 state-accent custom properties matching the UI-SPEC hex values
- `find apps/achilles/src -name '*.js' -o -name '*.d.ts' -o -name '*.jsx' | wc -l` returns 0 (CR-07 guard holds)
</verification>

<success_criteria>
- All 5 AchillesStates render with distinct accent treatments verified by E2E1 (UI-02)
- The reactive circle scales with the mocked mic amplitude during listening AND the mocked TTS amplitude during speaking; idle breathes when visible and pauses when hidden (UI-03)
- The 32-bar Canvas2D waveform polls the AnalyserNode-shaped source at 20fps and renders with state-distinct accent colors (UI-04)
- Partial transcripts render at opacity 0.7; committed at 1.0; max 3 visible lines; auto-fade after 15s idle (LOOP-02)
- All visual decisions encoded in tokens.css as CSS custom properties; Phase 12+ can override the palette via `:root` theme files without touching component source
- The MockAnalyser is the renderer-side Phase 11 testability seam; Phase 12 will replace it with a real AnalyserNode without touching component interfaces
- The renderer never imports from `src/main/` (process separation lock)
- All four Playwright specs pass against the headless Vite renderer bundle; no Electron app launches
- The FloatingShell slot wiring is in place so Plan 11-03 can supply `permissionOverlay`, `errorBanner`, `settingsPopover` without touching FloatingShell.tsx again
- No emojis anywhere (CLAUDE.md global)
- Phase 09 CR-06 + CR-07 hygiene applied (test files excluded from build set; src/.gitignore guards)
</success_criteria>

<output>
Create `.planning/phases/11-floating-ui-shell/11-02-SUMMARY.md` when done. Record: the locked design tokens (5 accent hex values + motion durations), the FloatingShell slot wiring contract for Plan 11-03, the useAchillesState reducer + action union, the MockAnalyser → Phase 12 AnalyserNode swap-out plan, the 4 Playwright e2e proofs against UI-02/03/04 + LOOP-02, and any divergences from UI-SPEC §2 pixel positions discovered during implementation.
</output>
