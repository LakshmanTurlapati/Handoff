---
phase: 01-identity-pairing-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [apps/web/app/api/healthz/route.ts, apps/relay/src/routes/readyz.ts, apps/web/Dockerfile, apps/relay/Dockerfile, apps/web/fly.toml, apps/relay/fly.toml, .github/workflows/fly-deploy.yml, README.md]
autonomous: true
requirements: [OPS-01]
user_setup:
  - service: fly
    why: "Fly app creation, Postgres attachment, and secret provisioning require user credentials"
    env_vars:
      - name: FLY_API_TOKEN
        source: "Fly dashboard -> Personal Access Tokens"
    dashboard_config:
      - task: "Create the Fly apps and attach Postgres"
        location: "Fly dashboard or `fly launch` per app"
        details: "Provision one app for `apps/web`, one app for `apps/relay`, and attach shared Postgres credentials as secrets"
    local_dev:
      - "Run `fly auth login` before invoking deploy scripts"
must_haves:
  truths:
    - "Both public services can run on Fly.io with TLS and explicit health checks."
    - "The web app and relay service have reproducible deploy configuration rather than ad hoc local commands."
    - "Secrets and callback URLs required for GitHub auth and pairing are documented for operators."
  artifacts:
    - path: "apps/web/fly.toml"
      provides: "Fly deployment config for the web app"
      contains: "internal_port"
    - path: "apps/relay/fly.toml"
      provides: "Fly deployment config for the relay service"
      contains: "internal_port"
    - path: ".github/workflows/fly-deploy.yml"
      provides: "Deploy workflow for web and relay services"
      contains: "flyctl"
    - path: "README.md"
      provides: "Operator setup and deployment instructions"
      contains: "Fly.io"
  key_links:
    - from: "apps/web/fly.toml"
      to: "apps/web/app/api/healthz/route.ts"
      via: "health check path points at the web health endpoint"
      pattern: "healthz"
    - from: "apps/relay/fly.toml"
      to: "apps/relay/src/routes/readyz.ts"
      via: "readiness path points at the relay readiness endpoint"
      pattern: "readyz"
    - from: ".github/workflows/fly-deploy.yml"
      to: "apps/web/fly.toml"
      via: "workflow deploys the web app using its Fly config"
      pattern: "apps/web/fly.toml"
---

<objective>
Deploy the Phase 1 services to Fly.io with operational basics.

Purpose: Satisfy the public-edge requirement so the web app and the future relay exist with TLS, secrets, and health checks before bridge and remote-session work expands in Phase 2.
Output: Health endpoints, Dockerfiles, Fly manifests, deploy workflow, and operator documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-pairing-foundation/01-RESEARCH.md
@.planning/phases/01-identity-pairing-foundation/01-VALIDATION.md
@.planning/phases/01-identity-pairing-foundation/01-02-SUMMARY.md
@apps/relay/src/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add web and relay health endpoints plus container entrypoints</name>
  <files>apps/web/app/api/healthz/route.ts, apps/relay/src/routes/readyz.ts, apps/web/Dockerfile, apps/relay/Dockerfile</files>
  <read_first>.planning/phases/01-identity-pairing-foundation/01-RESEARCH.md, .planning/phases/01-identity-pairing-foundation/01-02-SUMMARY.md, apps/relay/src/server.ts</read_first>
  <action>Create `apps/web/app/api/healthz/route.ts` returning a simple JSON payload with at least `status` and `service`. Add `apps/relay/src/routes/readyz.ts` returning readiness JSON. Create Dockerfiles for both apps using Node 22, `npm ci`, and explicit startup commands for the web and relay services. Ensure both containers expose the internal port that their Fly config will use and include an HTTP healthcheck command or documented health endpoint path.</action>
  <verify>rg -n 'healthz|readyz|node:22|EXPOSE|status' apps/web/app/api/healthz/route.ts apps/relay/src/routes/readyz.ts apps/web/Dockerfile apps/relay/Dockerfile</verify>
  <acceptance_criteria>
    - `apps/web/app/api/healthz/route.ts` contains `status`
    - `apps/relay/src/routes/readyz.ts` contains `status`
    - `apps/web/Dockerfile` contains `node:22`
    - `apps/relay/Dockerfile` contains `node:22`
    - each Dockerfile contains `EXPOSE`
  </acceptance_criteria>
  <done>Both services expose health endpoints and have explicit container entrypoints for Fly.</done>
</task>

<task type="auto">
  <name>Task 2: Add Fly manifests and deployment workflow</name>
  <files>apps/web/fly.toml, apps/relay/fly.toml, .github/workflows/fly-deploy.yml</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-identity-pairing-foundation/01-RESEARCH.md, .planning/phases/01-identity-pairing-foundation/01-02-SUMMARY.md</read_first>
  <action>Create `apps/web/fly.toml` and `apps/relay/fly.toml` with separate app names, explicit `internal_port` values, and HTTP health checks targeting `/api/healthz` for the web app and `/readyz` for the relay. Create `.github/workflows/fly-deploy.yml` that installs `flyctl`, deploys `apps/web` and `apps/relay`, and documents required secrets `FLY_API_TOKEN`, `AUTH_GITHUB_ID`, `AUTH_GITHUB_SECRET`, `SESSION_COOKIE_SECRET`, `PAIRING_TOKEN_SECRET`, `WS_TICKET_SECRET`, and `DATABASE_URL`.</action>
  <verify>rg -n 'internal_port|healthz|readyz|flyctl|PAIRING_TOKEN_SECRET|DATABASE_URL' apps/web/fly.toml apps/relay/fly.toml .github/workflows/fly-deploy.yml</verify>
  <acceptance_criteria>
    - `apps/web/fly.toml` contains `internal_port` and `healthz`
    - `apps/relay/fly.toml` contains `internal_port` and `readyz`
    - `.github/workflows/fly-deploy.yml` contains `flyctl`
    - `.github/workflows/fly-deploy.yml` contains `PAIRING_TOKEN_SECRET`
    - `.github/workflows/fly-deploy.yml` contains `DATABASE_URL`
  </acceptance_criteria>
  <done>The repo has versioned Fly configuration and a repeatable deploy workflow for both services.</done>
</task>

<task type="auto">
  <name>Task 3: Document operator setup for local dev and Fly deployment</name>
  <files>README.md, .env.example</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-identity-pairing-foundation/01-RESEARCH.md, .planning/phases/01-identity-pairing-foundation/01-02-SUMMARY.md, .env.example</read_first>
  <action>Write `README.md` sections `## Local Development`, `## Authentication Setup`, `## Pairing Flow`, and `## Fly.io Deployment`. Document the exact env vars from `.env.example`, the GitHub OAuth callback setup, the need to provision Postgres, and the expected health endpoints `/api/healthz` and `/readyz`. State that the local machine uses outbound connectivity only and that the relay is a hosted service, not a tunnel for general shell access.</action>
  <verify>rg -n '## Local Development|## Authentication Setup|## Pairing Flow|## Fly.io Deployment|/api/healthz|/readyz|outbound connectivity only' README.md</verify>
  <acceptance_criteria>
    - `README.md` contains `## Local Development`
    - `README.md` contains `## Fly.io Deployment`
    - `README.md` contains `/api/healthz` and `/readyz`
    - `README.md` contains `outbound connectivity only`
  </acceptance_criteria>
  <done>An operator can configure auth, secrets, Postgres, and Fly deployment from repo docs without reverse-engineering the code.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:phase-01:full` succeeds
- [ ] `curl` or fetch against local `/api/healthz` and `/readyz` returns success
- [ ] Fly configs validate with `fly config validate` or the current Fly CLI equivalent
</verification>

<success_criteria>

- All tasks completed
- Web and relay services have health checks, container entrypoints, and deploy manifests
- Operator documentation covers secrets, GitHub OAuth setup, and Fly deployment
- Phase 1 satisfies the Fly deployment baseline without exposing the local machine inbound
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-pairing-foundation/01-03-SUMMARY.md`
</output>
