---
phase: 01-identity-pairing-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [apps/web/auth.config.ts, apps/web/auth.ts, apps/web/middleware.ts, apps/web/app/sign-in/page.tsx, "apps/web/app/pair/[pairingId]/page.tsx", apps/web/app/api/pairings/route.ts, "apps/web/app/api/pairings/[pairingId]/redeem/route.ts", "apps/web/app/api/pairings/[pairingId]/confirm/route.ts", apps/web/lib/device-session.ts, apps/web/lib/pairing-service.ts, apps/bridge/src/cli/pair.ts, apps/bridge/src/lib/qr.ts, apps/bridge/src/lib/pairing-client.ts, apps/relay/src/server.ts, apps/relay/src/routes/health.ts, apps/web/tests/auth-pairing.spec.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, PAIR-01, PAIR-02, PAIR-03, PAIR-04, PAIR-05, SEC-01, SEC-06]
user_setup:
  - service: github
    why: "GitHub OAuth is the initial v1 sign-in provider for developer users"
    env_vars:
      - name: AUTH_GITHUB_ID
        source: "GitHub Developer Settings -> OAuth Apps -> Client ID"
      - name: AUTH_GITHUB_SECRET
        source: "GitHub Developer Settings -> OAuth Apps -> Client secret"
    dashboard_config:
      - task: "Register local and Fly callback URLs"
        location: "GitHub Developer Settings -> OAuth Apps -> Authorization callback URL"
        details: "Add both local development and Fly deployment callback URLs used by Auth.js"
    local_dev:
      - "Set AUTH_GITHUB_ID and AUTH_GITHUB_SECRET in local env before running auth flows"
must_haves:
  truths:
    - "A signed-in browser user can begin pairing from a phone-sized web UI."
    - "The local terminal can request a pairing, show a QR code plus fallback code, and wait for confirmation."
    - "A device session is issued only after both the browser and terminal see the same verification phrase and the terminal explicitly approves."
    - "All browser and pairing routes use short-lived or cookie-based credentials rather than query-string bearer tokens."
  artifacts:
    - path: "apps/web/app/sign-in/page.tsx"
      provides: "Mobile sign-in screen for the web app"
      min_lines: 30
      contains: "Continue with GitHub"
    - path: "apps/web/app/api/pairings/[pairingId]/confirm/route.ts"
      provides: "Pairing confirmation and device-session issuance"
      exports: ["POST"]
    - path: "apps/bridge/src/cli/pair.ts"
      provides: "Terminal pairing command that prints QR and waits for approval"
      min_lines: 40
      contains: "renderTerminalQr"
    - path: "apps/web/lib/device-session.ts"
      provides: "Server-side cookie issuance and validation"
      exports: ["WEB_SESSION_COOKIE_NAME", "DEVICE_SESSION_COOKIE_NAME", "issueDeviceSession"]
  key_links:
    - from: "apps/bridge/src/lib/pairing-client.ts"
      to: "apps/web/app/api/pairings/route.ts"
      via: "bridge starts a pairing by calling the hosted API"
      pattern: "/api/pairings"
    - from: "apps/web/app/pair/[pairingId]/page.tsx"
      to: "apps/web/app/api/pairings/[pairingId]/confirm/route.ts"
      via: "redeem screen submits the verification phrase confirmation"
      pattern: "confirm"
    - from: "apps/web/app/api/pairings/[pairingId]/confirm/route.ts"
      to: "apps/web/lib/device-session.ts"
      via: "confirmation route issues the browser device cookie"
      pattern: "issueDeviceSession|cm_device_session"
---

<objective>
Implement the user-facing identity and pairing flow for Phase 1.

Purpose: Turn the Phase 1 trust model into a working browser login, pairing API, QR-based terminal flow, and device-session issuance path.
Output: Web auth shell, pairing routes, device-session issuance, terminal QR command, and a minimal relay health service.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-identity-pairing-foundation/01-RESEARCH.md
@.planning/phases/01-identity-pairing-foundation/01-VALIDATION.md
@.planning/phases/01-identity-pairing-foundation/01-01-SUMMARY.md
@resources/gsd-2/web/lib/auth.ts
@resources/gsd-2/web/proxy.ts
@resources/gsd-2/src/resources/extensions/remote-questions/remote-command.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build browser auth and pairing UI surfaces</name>
  <files>apps/web/auth.config.ts, apps/web/auth.ts, apps/web/middleware.ts, apps/web/app/sign-in/page.tsx, apps/web/app/pair/[pairingId]/page.tsx, apps/web/lib/device-session.ts</files>
  <read_first>AGENTS.md, .planning/phases/01-identity-pairing-foundation/01-RESEARCH.md, .planning/phases/01-identity-pairing-foundation/01-01-SUMMARY.md, resources/gsd-2/web/lib/auth.ts, resources/gsd-2/web/proxy.ts</read_first>
  <action>Configure Auth.js in `apps/web/auth.config.ts` and `apps/web/auth.ts` with GitHub as the only enabled provider for Phase 1. Create a mobile-first `apps/web/app/sign-in/page.tsx` that includes the exact CTA text `Continue with GitHub`. In `apps/web/middleware.ts`, guard the pairing UI so unauthenticated users are redirected to `/sign-in`. In `apps/web/lib/device-session.ts`, implement `WEB_SESSION_COOKIE_NAME = "cm_web_session"` and `DEVICE_SESSION_COOKIE_NAME = "cm_device_session"`, with HttpOnly + Secure + SameSite=Lax cookie options and a 7-day absolute device-session expiry. In `apps/web/app/pair/[pairingId]/page.tsx`, render the pairing status, fallback code, and verification phrase using mobile-safe layout text rather than desktop-terminal chrome.</action>
  <verify>rg -n 'Continue with GitHub|cm_web_session|cm_device_session|SameSite|GitHub' apps/web/auth.config.ts apps/web/auth.ts apps/web/middleware.ts apps/web/app/sign-in/page.tsx apps/web/app/pair/[pairingId]/page.tsx apps/web/lib/device-session.ts</verify>
  <acceptance_criteria>
    - `apps/web/app/sign-in/page.tsx` contains `Continue with GitHub`
    - `apps/web/lib/device-session.ts` contains `cm_web_session` and `cm_device_session`
    - `apps/web/lib/device-session.ts` contains `sameSite: "lax"` or the equivalent cookie option
    - `apps/web/middleware.ts` references `/sign-in`
    - `apps/web/app/pair/[pairingId]/page.tsx` contains `verificationPhrase` or `Verification phrase`
  </acceptance_criteria>
  <done>The browser can authenticate users and has a real pairing screen optimized for mobile display.</done>
</task>

<task type="auto">
  <name>Task 2: Implement hosted pairing routes and device-session issuance</name>
  <files>apps/web/app/api/pairings/route.ts, apps/web/app/api/pairings/[pairingId]/redeem/route.ts, apps/web/app/api/pairings/[pairingId]/confirm/route.ts, apps/web/lib/pairing-service.ts, apps/web/lib/device-session.ts, apps/web/tests/auth-pairing.spec.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-identity-pairing-foundation/01-RESEARCH.md, .planning/phases/01-identity-pairing-foundation/01-VALIDATION.md, .planning/phases/01-identity-pairing-foundation/01-01-SUMMARY.md</read_first>
  <action>Create `POST /api/pairings` to create a pending pairing with fields `pairingId`, `pairingUrl`, `userCode`, and `expiresAt` using a 5-minute expiry window. Create `POST /api/pairings/[pairingId]/redeem` to attach the authenticated user and generate a `verificationPhrase`. Create `POST /api/pairings/[pairingId]/confirm` to validate the terminal confirmation and issue the `cm_device_session` cookie only when the pairing is still `pending` or `redeemed` and the verification phrase matches. Record audit rows for `pairing.created`, `pairing.redeemed`, `pairing.confirmed`, and `pairing.expired`. Add Playwright coverage in `apps/web/tests/auth-pairing.spec.ts` for login redirect, redeem flow, and confirmation failure on expired pairing.</action>
  <verify>rg -n 'pairing\\.created|pairing\\.redeemed|pairing\\.confirmed|expiresAt|cm_device_session|verificationPhrase' apps/web/app/api/pairings/route.ts apps/web/app/api/pairings/[pairingId]/redeem/route.ts apps/web/app/api/pairings/[pairingId]/confirm/route.ts apps/web/lib/pairing-service.ts apps/web/tests/auth-pairing.spec.ts</verify>
  <acceptance_criteria>
    - `apps/web/app/api/pairings/route.ts` contains `expiresAt`
    - `apps/web/app/api/pairings/[pairingId]/redeem/route.ts` contains `verificationPhrase`
    - `apps/web/app/api/pairings/[pairingId]/confirm/route.ts` contains `cm_device_session`
    - `apps/web/lib/pairing-service.ts` contains all audit event strings `pairing.created`, `pairing.redeemed`, and `pairing.confirmed`
    - `apps/web/tests/auth-pairing.spec.ts` contains an expired-pairing assertion
  </acceptance_criteria>
  <done>The hosted app can create, redeem, and confirm pairings and can issue a 7-day device session only after confirmation.</done>
</task>

<task type="auto">
  <name>Task 3: Add the terminal pairing command and minimal relay service</name>
  <files>apps/bridge/src/cli/pair.ts, apps/bridge/src/lib/qr.ts, apps/bridge/src/lib/pairing-client.ts, apps/relay/src/server.ts, apps/relay/src/routes/health.ts</files>
  <read_first>.planning/phases/01-identity-pairing-foundation/01-RESEARCH.md, .planning/phases/01-identity-pairing-foundation/01-01-SUMMARY.md, resources/gsd-2/src/resources/extensions/remote-questions/remote-command.ts, resources/gsd-2/src/headless.ts</read_first>
  <action>Implement a bridge CLI command in `apps/bridge/src/cli/pair.ts` that calls the hosted `POST /api/pairings` endpoint, prints the fallback `userCode`, renders the QR with `renderTerminalQr`, polls or subscribes for the pairing state, shows the `verificationPhrase`, and requires explicit local approval before the server is told to finalize pairing. Implement `apps/bridge/src/lib/pairing-client.ts` with a typed client that calls the exact route `/api/pairings`. Create `apps/relay/src/server.ts` and `apps/relay/src/routes/health.ts` with `GET /healthz` and `GET /readyz` so the relay service exists in Phase 1 and can be deployed before live bridge traffic is added in Phase 2.</action>
  <verify>rg -n 'renderTerminalQr|/api/pairings|verificationPhrase|healthz|readyz' apps/bridge/src/cli/pair.ts apps/bridge/src/lib/qr.ts apps/bridge/src/lib/pairing-client.ts apps/relay/src/server.ts apps/relay/src/routes/health.ts</verify>
  <acceptance_criteria>
    - `apps/bridge/src/cli/pair.ts` contains `renderTerminalQr`
    - `apps/bridge/src/lib/pairing-client.ts` contains the string `/api/pairings`
    - `apps/bridge/src/cli/pair.ts` contains `verificationPhrase`
    - `apps/relay/src/server.ts` contains `healthz` and `readyz`
    - `apps/relay/src/routes/health.ts` exports handlers or functions for both health endpoints
  </acceptance_criteria>
  <done>The local machine can begin pairing from the terminal, and the future relay service already has deployable health endpoints.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:phase-01:quick` succeeds
- [ ] `npm run test:phase-01:full` succeeds
- [ ] Authenticated pairing flow passes in Playwright mobile viewport
</verification>

<success_criteria>

- All tasks completed
- Browser auth, pairing creation, redeem, terminal confirmation, and device-session issuance work together
- Pairing is single-use and time-bounded
- No public route stores or accepts long-lived auth in query parameters or localStorage
- A minimal relay service with health endpoints exists for Fly deployment
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-pairing-foundation/01-02-SUMMARY.md`
</output>
