---
phase: 01-identity-pairing-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.base.json, .env.example, apps/web/package.json, apps/relay/package.json, apps/bridge/package.json, packages/protocol/package.json, packages/protocol/src/pairing.ts, packages/protocol/src/session.ts, packages/auth/package.json, packages/auth/src/device-session.ts, packages/auth/src/ws-ticket.ts, packages/db/package.json, packages/db/drizzle.config.ts, packages/db/src/schema.ts, docs/adr/0001-phase-1-trust-boundary.md, vitest.workspace.ts, playwright.config.ts]
autonomous: true
requirements: [AUTH-01, PAIR-03, PAIR-04, SEC-01, SEC-06]
user_setup: []
must_haves:
  truths:
    - "The repo has one workspace layout for web, relay, bridge, and shared packages."
    - "Session state is modeled as short-lived web sessions plus 7-day device sessions, not one catch-all bearer token."
    - "Pairing is modeled as a single-use server-side record with expiry and a verification phrase."
    - "Phase 1 trust boundaries explicitly keep codex app-server and the local machine off the public internet."
  artifacts:
    - path: "package.json"
      provides: "Workspace root scripts and package manager configuration"
      contains: "\"workspaces\""
    - path: "packages/db/src/schema.ts"
      provides: "Database schema for users, sessions, pairings, and audit"
      min_lines: 60
      contains: "pairing_sessions"
    - path: "packages/protocol/src/pairing.ts"
      provides: "Pairing request and response contracts"
      min_lines: 40
      exports: ["PairingStatus", "PairingCreateResponse", "PairingConfirmResponse"]
    - path: "packages/auth/src/device-session.ts"
      provides: "Cookie and device-session helpers"
      min_lines: 40
      exports: ["DEVICE_SESSION_COOKIE_NAME", "createDeviceSession", "verifyDeviceSession"]
    - path: "docs/adr/0001-phase-1-trust-boundary.md"
      provides: "Trust-boundary ADR for browser, web app, relay, bridge, and Codex"
      contains: "No direct public exposure of codex app-server"
  key_links:
    - from: "packages/auth/src/device-session.ts"
      to: "packages/db/src/schema.ts"
      via: "session helper depends on device_sessions and web_sessions tables"
      pattern: "device_sessions|web_sessions"
    - from: "packages/protocol/src/pairing.ts"
      to: "packages/db/src/schema.ts"
      via: "protocol states align with persisted pairing status values"
      pattern: "pending|redeemed|confirmed|expired"
    - from: "docs/adr/0001-phase-1-trust-boundary.md"
      to: "packages/auth/src/ws-ticket.ts"
      via: "ADR defines short-lived derived credentials for live channels"
      pattern: "60-second|single-use"
---

<objective>
Create the execution foundation for Phase 1.

Purpose: Give later plans a concrete workspace, schema, protocol, and trust model so pairing and deployment work are built on explicit security rules rather than ad hoc code.
Output: Workspace scaffolding, shared packages, database schema, auth/ticket helpers, trust-boundary ADR, and test harness stubs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/SUMMARY.md
@.planning/phases/01-identity-pairing-foundation/01-RESEARCH.md
@resources/gsd-2/package.json
@resources/gsd-2/web/package.json
@resources/gsd-2/web/lib/auth.ts
@resources/gsd-2/web/proxy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Codex Mobile workspace and root scripts</name>
  <files>package.json, tsconfig.base.json, .env.example, apps/web/package.json, apps/relay/package.json, apps/bridge/package.json, packages/protocol/package.json, packages/auth/package.json, packages/db/package.json, vitest.workspace.ts, playwright.config.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/research/STACK.md, .planning/phases/01-identity-pairing-foundation/01-RESEARCH.md, resources/gsd-2/package.json, resources/gsd-2/web/package.json</read_first>
  <action>Create the root `package.json` using npm workspaces for `apps/*` and `packages/*`. Add scripts `build`, `typecheck`, `lint`, `test:phase-01:quick`, `test:phase-01:full`, and `db:generate`. Add `tsconfig.base.json` path aliases for `@codex-mobile/protocol`, `@codex-mobile/auth`, and `@codex-mobile/db`. Add `.env.example` keys `DATABASE_URL=`, `AUTH_GITHUB_ID=`, `AUTH_GITHUB_SECRET=`, `SESSION_COOKIE_SECRET=`, `PAIRING_TOKEN_SECRET=`, `WS_TICKET_SECRET=`, `NEXTAUTH_URL=`, `FLY_APP_NAME_WEB=`, and `FLY_APP_NAME_RELAY=`. Create package manifests for `apps/web`, `apps/relay`, `apps/bridge`, `packages/protocol`, `packages/auth`, and `packages/db` with TypeScript-based build and test scripts.</action>
  <verify>rg -n '"workspaces"|test:phase-01:quick|PAIRING_TOKEN_SECRET|@codex-mobile/protocol' package.json tsconfig.base.json .env.example apps/web/package.json apps/relay/package.json apps/bridge/package.json packages/protocol/package.json packages/auth/package.json packages/db/package.json</verify>
  <acceptance_criteria>
    - `package.json` contains `"workspaces"` and the script name `"test:phase-01:quick"`
    - `tsconfig.base.json` contains the alias string `"@codex-mobile/protocol"`
    - `.env.example` contains `PAIRING_TOKEN_SECRET=` and `WS_TICKET_SECRET=`
    - `apps/web/package.json` contains `"next"`
    - `apps/relay/package.json` contains `"fastify"`
    - `apps/bridge/package.json` contains a CLI entry for the bridge package
  </acceptance_criteria>
  <done>The repo can install as a single workspace and Phase 1 scripts and env contracts are visible from the root.</done>
</task>

<task type="auto">
  <name>Task 2: Define shared schema, protocol contracts, and auth primitives</name>
  <files>packages/db/drizzle.config.ts, packages/db/src/schema.ts, packages/protocol/src/pairing.ts, packages/protocol/src/session.ts, packages/auth/src/device-session.ts, packages/auth/src/ws-ticket.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-identity-pairing-foundation/01-RESEARCH.md, resources/gsd-2/web/lib/auth.ts, resources/gsd-2/web/proxy.ts</read_first>
  <action>In `packages/db/src/schema.ts`, define tables named `users`, `oauth_accounts`, `web_sessions`, `device_sessions`, `pairing_sessions`, and `audit_events`. In `packages/protocol/src/pairing.ts`, export `PairingStatus` with the exact values `pending`, `redeemed`, `confirmed`, `expired`, and `cancelled`; export `PairingCreateResponse` with `pairingId`, `pairingUrl`, `userCode`, and `expiresAt`; export `PairingConfirmResponse` with `verificationPhrase` and `deviceSessionId`. In `packages/auth/src/device-session.ts`, implement `DEVICE_SESSION_COOKIE_NAME = "cm_device_session"` and helpers `createDeviceSession`, `verifyDeviceSession`, and `rotateDeviceSession`. In `packages/auth/src/ws-ticket.ts`, implement `WS_TICKET_TTL_SECONDS = 60` and helpers to mint and verify a single-use `cm_ws_ticket` derived from the authenticated browser session.</action>
  <verify>rg -n 'users|device_sessions|pairing_sessions|PairingStatus|cm_device_session|WS_TICKET_TTL_SECONDS = 60' packages/db/src/schema.ts packages/protocol/src/pairing.ts packages/auth/src/device-session.ts packages/auth/src/ws-ticket.ts</verify>
  <acceptance_criteria>
    - `packages/db/src/schema.ts` contains `device_sessions` and `pairing_sessions`
    - `packages/protocol/src/pairing.ts` contains all five status strings `pending`, `redeemed`, `confirmed`, `expired`, and `cancelled`
    - `packages/auth/src/device-session.ts` contains `cm_device_session`
    - `packages/auth/src/ws-ticket.ts` contains `WS_TICKET_TTL_SECONDS = 60`
    - `packages/protocol/src/session.ts` exports the ticket or session payload types used by the web and relay apps
  </acceptance_criteria>
  <done>The database, protocol, and auth packages describe the Phase 1 trust model in code and can be consumed by later plans without redefining names or lifetimes.</done>
</task>

<task type="auto">
  <name>Task 3: Document the Phase 1 trust boundary and validation harness</name>
  <files>docs/adr/0001-phase-1-trust-boundary.md, vitest.workspace.ts, playwright.config.ts</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-identity-pairing-foundation/01-RESEARCH.md, .planning/phases/01-identity-pairing-foundation/01-VALIDATION.md</read_first>
  <action>Write `docs/adr/0001-phase-1-trust-boundary.md` with explicit sections for `Browser`, `Web App`, `Relay`, `Local Bridge`, and `Codex`. State the exact rule `No direct public exposure of codex app-server`. State that browser sessions use cookies, pairing tokens are single-use, and live channels use derived tickets with 60-second expiry. Create `vitest.workspace.ts` and `playwright.config.ts` with project names `phase-01-unit` and `phase-01-e2e-mobile` so later plans can add tests without changing harness names.</action>
  <verify>rg -n 'No direct public exposure of codex app-server|60-second|phase-01-unit|phase-01-e2e-mobile' docs/adr/0001-phase-1-trust-boundary.md vitest.workspace.ts playwright.config.ts</verify>
  <acceptance_criteria>
    - `docs/adr/0001-phase-1-trust-boundary.md` contains the sentence `No direct public exposure of codex app-server`
    - `docs/adr/0001-phase-1-trust-boundary.md` contains the phrase `60-second`
    - `vitest.workspace.ts` contains `phase-01-unit`
    - `playwright.config.ts` contains `phase-01-e2e-mobile`
  </acceptance_criteria>
  <done>The repo has written trust-boundary guidance and named test harness entry points for the rest of Phase 1.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install` succeeds at the workspace root
- [ ] `npm run typecheck` succeeds
- [ ] `npm run test:phase-01:quick` succeeds or prints explicit pending/stub coverage for the newly created harness
</verification>

<success_criteria>

- All tasks completed
- Shared schema, protocol, and auth packages exist with the exact Phase 1 names and lifetimes
- The trust-boundary ADR explicitly forbids public app-server exposure
- The workspace is ready for web auth, pairing APIs, and deployment work without renaming core concepts
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-pairing-foundation/01-01-SUMMARY.md`
</output>
