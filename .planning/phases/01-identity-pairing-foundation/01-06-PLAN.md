---
phase: 01-identity-pairing-foundation
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .github/workflows/fly-deploy.yml
  - apps/web/Dockerfile
  - apps/relay/Dockerfile
  - README.md
autonomous: true
requirements: [OPS-01, SEC-01]
must_haves:
  truths:
    - "No shell command line in .github/workflows/fly-deploy.yml interpolates a ${{ secrets.X }} expression. Secrets flow into the step via env: block and the script only references $VAR names."
    - "Neither apps/web/Dockerfile nor apps/relay/Dockerfile contains `|| true` on a build RUN line. A failing `next build` or `tsc` now fails the image build."
    - "apps/web/Dockerfile explicitly asserts `.next/` exists at the end of the build stage."
    - "apps/relay/Dockerfile explicitly asserts `dist/` exists at the end of the build stage (or `apps/relay/src/index.ts` exists as a fallback, matching the current --experimental-strip-types runtime entry)."
    - "The workflow declares `permissions: contents: read` at the top level."
    - "README.md Fly.io Deployment section carries an INFO callout that the Phase 1 in-memory pairing store is single-machine only, so apps/web must run with min_machines_running >= 1 until a Drizzle-backed store lands."
  artifacts:
    - path: ".github/workflows/fly-deploy.yml"
      provides: "Hardened Fly deploy workflow with env-block secrets and minimum-privilege permissions"
      contains: "permissions:"
    - path: "apps/web/Dockerfile"
      provides: "Web Dockerfile that fails the build when next build fails"
      contains: "test -d"
    - path: "apps/relay/Dockerfile"
      provides: "Relay Dockerfile that fails the build when tsc fails"
      contains: "test -d"
    - path: "README.md"
      provides: "Fly.io Deployment section documents the single-machine pairing constraint"
      contains: "in-memory pairing store"
  key_links:
    - from: ".github/workflows/fly-deploy.yml"
      to: ".github/workflows/fly-deploy.yml"
      via: "step-level env: block is the only place ${{ secrets.X }} expressions are evaluated; run: scripts reference $VAR"
      pattern: "env:"
    - from: "apps/web/Dockerfile"
      to: "apps/web/fly.toml"
      via: "Dockerfile build assertion protects the `.next/` output that Fly will boot with `npm start --workspace @codex-mobile/web`"
      pattern: ".next"
---

<objective>
Close CR-02 (GitHub Actions script injection via secret interpolation), CR-03 (Dockerfile `|| true` masking build failures), WR-04 (missing permissions block), and bake an INFO callout into the README warning operators that the Phase 1 in-memory pairing store is single-machine only.

Purpose: The deploy workflow must not be a shell injection vector and the production images must not be allowed to ship a broken build silently. These are deploy-safety gaps, not runtime bugs — nothing in this plan touches application code.
Output: A hardened fly-deploy.yml with an env: block, a top-level permissions: scope, and tightened Dockerfiles that fail loudly when a build step fails.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-identity-pairing-foundation/01-VERIFICATION.md
@.planning/phases/01-identity-pairing-foundation/01-REVIEW.md
@.planning/phases/01-identity-pairing-foundation/01-03-SUMMARY.md
@.github/workflows/fly-deploy.yml
@apps/web/Dockerfile
@apps/relay/Dockerfile
@apps/web/fly.toml
@apps/relay/fly.toml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CR-02 — move fly-deploy.yml secrets into env: blocks and add top-level permissions scope</name>
  <files>.github/workflows/fly-deploy.yml</files>
  <read_first>.github/workflows/fly-deploy.yml, .planning/phases/01-identity-pairing-foundation/01-REVIEW.md</read_first>
  <action>
  Modify `.github/workflows/fly-deploy.yml`:

  1. Add a top-level `permissions:` block immediately AFTER the `concurrency:` block (around line 78) and BEFORE the `jobs:` block. The exact text to insert:
     ```yaml
     # Minimum privilege: this workflow only needs to check out the repo
     # and call flyctl via an externally-scoped FLY_API_TOKEN. It does not
     # write to the repo, push images, or post comments. Scoping to
     # contents: read prevents a compromised step from abusing GITHUB_TOKEN.
     # (Addresses WR-04 from 01-REVIEW.md.)
     permissions:
       contents: read

     ```
     (Keep the blank line after so the `jobs:` block stays visually separated.)

  2. Rewrite the "Push web app secrets to Fly" step (currently at lines 93-109). REPLACE the existing step block with:
     ```yaml
           - name: Push web app secrets to Fly
             # Secrets must NEVER be interpolated into the run: command
             # string — GitHub's expression engine substitutes before the
             # shell parses, which is a textbook script-injection vector.
             # Instead: map secrets to env vars on the step, and let the
             # shell expand $VAR references at runtime. (CR-02 fix.)
             env:
               FLY_API_TOKEN:         ${{ secrets.FLY_API_TOKEN }}
               AUTH_GITHUB_ID:        ${{ secrets.AUTH_GITHUB_ID }}
               AUTH_GITHUB_SECRET:    ${{ secrets.AUTH_GITHUB_SECRET }}
               SESSION_COOKIE_SECRET: ${{ secrets.SESSION_COOKIE_SECRET }}
               PAIRING_TOKEN_SECRET:  ${{ secrets.PAIRING_TOKEN_SECRET }}
               WS_TICKET_SECRET:      ${{ secrets.WS_TICKET_SECRET }}
               DATABASE_URL:          ${{ secrets.DATABASE_URL }}
             run: |
               flyctl secrets set \
                 --stage \
                 --config apps/web/fly.toml \
                 AUTH_GITHUB_ID="$AUTH_GITHUB_ID" \
                 AUTH_GITHUB_SECRET="$AUTH_GITHUB_SECRET" \
                 SESSION_COOKIE_SECRET="$SESSION_COOKIE_SECRET" \
                 PAIRING_TOKEN_SECRET="$PAIRING_TOKEN_SECRET" \
                 WS_TICKET_SECRET="$WS_TICKET_SECRET" \
                 DATABASE_URL="$DATABASE_URL"
     ```

  3. Rewrite the "Push relay secrets to Fly" step (currently at lines 134-147). REPLACE with the equivalent env-block structure:
     ```yaml
           - name: Push relay secrets to Fly
             # CR-02 fix: env-block indirection prevents shell injection.
             env:
               FLY_API_TOKEN:         ${{ secrets.FLY_API_TOKEN }}
               SESSION_COOKIE_SECRET: ${{ secrets.SESSION_COOKIE_SECRET }}
               PAIRING_TOKEN_SECRET:  ${{ secrets.PAIRING_TOKEN_SECRET }}
               WS_TICKET_SECRET:      ${{ secrets.WS_TICKET_SECRET }}
               DATABASE_URL:          ${{ secrets.DATABASE_URL }}
             run: |
               flyctl secrets set \
                 --stage \
                 --config apps/relay/fly.toml \
                 SESSION_COOKIE_SECRET="$SESSION_COOKIE_SECRET" \
                 PAIRING_TOKEN_SECRET="$PAIRING_TOKEN_SECRET" \
                 WS_TICKET_SECRET="$WS_TICKET_SECRET" \
                 DATABASE_URL="$DATABASE_URL"
     ```

  4. Leave the existing "Deploy apps/web" and "Deploy apps/relay" steps unchanged — they already pass `FLY_API_TOKEN` via an `env:` block and their `run:` scripts only reference static `flyctl deploy` command lines (no secret expressions).

  5. After making the changes, the rule is: grep MUST NOT find any line that contains BOTH `${{ secrets.` AND a shell command (`flyctl`, `echo`, `set`, `export`, etc.) on the same line. `${{ secrets.X }}` expressions may only appear inside a step-level `env:` block.
  </action>
  <verify>
    <automated>grep -n "permissions:" .github/workflows/fly-deploy.yml &amp;&amp; grep -n "env:" .github/workflows/fly-deploy.yml | head -20 &amp;&amp; ! grep -nE '\$\{\{ *secrets\.[A-Z_]+ *\}\}' .github/workflows/fly-deploy.yml | grep -E '(flyctl|echo|export |set )'</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/fly-deploy.yml` contains a top-level `permissions:` block with `contents: read`
    - The top-level `permissions:` block appears BEFORE the first `jobs:` key (verify with line numbers via `grep -n`)
    - Both "Push web app secrets to Fly" and "Push relay secrets to Fly" steps now have an `env:` block containing every secret they previously interpolated
    - Neither step's `run:` script contains `${{ secrets.` — grep for the literal `${{ secrets.` inside any `run:` block returns only occurrences that are inside `env:` blocks
    - No line in the file contains BOTH `${{ secrets.` AND one of the shell tokens `flyctl`, `echo`, `export `, `set ` (these would indicate a leftover interpolation in a run: line)
    - The "Deploy apps/web" and "Deploy apps/relay" steps are unchanged
    - The `name:`, `on:`, and `concurrency:` blocks are unchanged
  </acceptance_criteria>
  <done>Fly secrets are pushed through runner env vars instead of being interpolated into shell command strings, and the workflow runs with the minimum GITHUB_TOKEN scope it needs.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CR-03 — drop `|| true` from both Dockerfiles and add post-build existence assertions</name>
  <files>apps/web/Dockerfile, apps/relay/Dockerfile</files>
  <read_first>apps/web/Dockerfile, apps/relay/Dockerfile, apps/web/fly.toml, apps/relay/fly.toml, .planning/phases/01-identity-pairing-foundation/01-REVIEW.md</read_first>
  <action>
  Modify `apps/web/Dockerfile`:

  1. Replace line 68 which currently reads:
     ```
     RUN npm run build --workspace @codex-mobile/web --if-present || true
     ```
     With:
     ```
     # CR-03 fix: no `|| true`. --if-present still returns 0 when the
     # script is genuinely absent, but preserves the real exit code when
     # the script runs and fails. A broken `next build` now fails the image.
     RUN npm run build --workspace @codex-mobile/web --if-present
     ```
  2. Immediately AFTER that RUN line, add a build-time assertion that the Next.js output exists. This catches the silent-failure mode where a future change removes the `build` script entirely:
     ```
     # Fail the build if `.next/` was not produced. This is the exact
     # directory `npm start --workspace @codex-mobile/web` (i.e. `next start`)
     # will read from at container boot. A container without `.next/` would
     # crash on the first request in production — we prefer a loud build
     # failure over a green deploy that serves 500s.
     RUN test -d /repo/apps/web/.next \
       || (echo "next build did not produce /repo/apps/web/.next" && exit 1)
     ```
  3. Leave every other line of the Dockerfile (deps stage, runtime stage, COPY commands, USER, EXPOSE, CMD) unchanged.

  Modify `apps/relay/Dockerfile`:

  1. Replace line 65 which currently reads:
     ```
     RUN npm run build --workspace @codex-mobile/relay --if-present || true
     ```
     With:
     ```
     # CR-03 fix: no `|| true`. If `tsc` fails the image build fails.
     RUN npm run build --workspace @codex-mobile/relay --if-present
     ```
  2. Immediately AFTER that RUN line, add a build-time assertion:
     ```
     # The relay still uses `node --experimental-strip-types` at runtime
     # (see the CMD line below) so a compiled `dist/` tree is not STRICTLY
     # required for the container to boot. However, the source entry point
     # MUST exist, otherwise `node --experimental-strip-types apps/relay/src/index.ts`
     # will crash on start. If a future plan lands a real tsc build, change
     # this assertion to `test -d /repo/apps/relay/dist`.
     RUN test -f /repo/apps/relay/src/index.ts \
       || (echo "relay entry file apps/relay/src/index.ts missing after build" && exit 1)
     ```
     NOTE: we use `test -f apps/relay/src/index.ts` rather than `test -d /repo/apps/relay/dist` because the current Dockerfile CMD is `node --experimental-strip-types apps/relay/src/index.ts` — a `dist/` directory does not exist yet at Phase 1. The comment documents the intended future swap so the next plan knows what to tighten.
  3. Leave every other line unchanged.

  Do NOT touch anything outside lines 65 and 68 of the respective Dockerfiles, and do not modify the deps stage, COPY instructions, USER directive, EXPOSE line, or CMD.
  </action>
  <verify>
    <automated>! grep -n "|| true" apps/web/Dockerfile &amp;&amp; ! grep -n "|| true" apps/relay/Dockerfile &amp;&amp; grep -n "test -d /repo/apps/web/.next" apps/web/Dockerfile &amp;&amp; grep -n "test -f /repo/apps/relay/src/index.ts" apps/relay/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/Dockerfile` contains ZERO occurrences of the literal string `|| true`
    - `apps/relay/Dockerfile` contains ZERO occurrences of the literal string `|| true`
    - `apps/web/Dockerfile` contains the literal `test -d /repo/apps/web/.next`
    - `apps/relay/Dockerfile` contains the literal `test -f /repo/apps/relay/src/index.ts`
    - `apps/web/Dockerfile` CMD is still `["npm", "start", "--workspace", "@codex-mobile/web"]` (unchanged)
    - `apps/relay/Dockerfile` CMD is still `["node", "--experimental-strip-types", "apps/relay/src/index.ts"]` (unchanged)
    - Both Dockerfiles still contain `EXPOSE 3000` / `EXPOSE 8080` respectively
    - Both Dockerfiles still contain `FROM node:22-alpine AS deps` (the deps stage is untouched)
  </acceptance_criteria>
  <done>Both Dockerfiles now fail loudly on a broken build instead of shipping a runtime image that crashes on first request. The web build also asserts `.next/` exists so Phase 1 cannot silently produce a runtime-broken image.</done>
</task>

<task type="auto">
  <name>Task 3: Add README INFO callout about single-machine pairing store</name>
  <files>README.md</files>
  <read_first>README.md, apps/web/fly.toml, apps/relay/fly.toml, apps/web/lib/pairing-service.ts, .planning/phases/01-identity-pairing-foundation/01-REVIEW.md</read_first>
  <action>
  Modify `README.md`:

  Find the line `Both services are built with multi-stage \`node:22-alpine\` Dockerfiles that \`COPY\` the repo root as the build context so the npm workspaces layout stays intact inside the image. The build context is set implicitly by \`fly.toml\` because the \`dockerfile = "apps/<service>/Dockerfile"\` path is relative to the repo root.` (around line 259 in the current README).

  Immediately AFTER that paragraph (before the `### Prerequisites` subsection), insert a NEW subsection:

  ```markdown
  ### IMPORTANT: Single-machine pairing store constraint (Phase 1)

  Phase 1 ships with an **in-memory pairing store** (`apps/web/lib/pairing-service.ts` `InMemoryPairingStore`) for the `pairing_sessions` state machine. A pairing created on machine A is NOT visible to machine B, so `apps/web` **must run on exactly one Fly machine** until a Drizzle-backed store lands in a later phase.

  Required Fly configuration until the Drizzle-backed adapter ships:

  - `apps/web/fly.toml`: set `min_machines_running = 1` under `[http_service]` (the current value is `0`, which allows Fly to scale to zero and then cold-start a fresh second machine that has no pairing state).
  - `apps/web/fly.toml`: keep `auto_start_machines` as-is (`true` is acceptable because only ONE machine will ever run).
  - `apps/relay/fly.toml`: already sets `min_machines_running = 1` and `auto_start_machines = false`. No change needed.

  Symptom if this constraint is violated: the bridge CLI creates a pairing on machine A, the browser redeems it against machine B, and machine B returns `pairing_not_found` because its process-local Map has never seen the row. Operators will see confused "pairing disappeared" errors.

  This constraint is also why the in-memory rate limiter at `apps/web/lib/rate-limit.ts` is per-machine only; a multi-machine deploy needs a Redis-backed counter before scaling horizontally. Track both under the Phase 1 follow-ups.
  ```

  Do NOT change any existing README content above or below the insertion point.
  </action>
  <verify>
    <automated>grep -n "Single-machine pairing store" README.md &amp;&amp; grep -n "in-memory pairing store" README.md &amp;&amp; grep -n "min_machines_running = 1" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains the exact heading `### IMPORTANT: Single-machine pairing store constraint (Phase 1)`
    - `README.md` contains the exact string `in-memory pairing store`
    - `README.md` contains the exact string `min_machines_running = 1`
    - `README.md` contains the exact string `pairing_not_found`
    - The new section is inserted between the existing `Both services are built with multi-stage` paragraph and the `### Prerequisites` subsection
    - Existing README content above and below the insertion point is byte-for-byte unchanged
  </acceptance_criteria>
  <done>Operators reading the Fly deployment docs see a clear, load-bearing callout that Phase 1 is single-machine-only until the Drizzle-backed pairing store lands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.github/workflows/fly-deploy.yml` has a top-level `permissions: contents: read` block
- [ ] Both secrets-push steps use `env:` indirection; no `${{ secrets.X }}` appears in any `run:` line
- [ ] Neither Dockerfile contains `|| true`
- [ ] Both Dockerfiles contain a post-build existence assertion
- [ ] README.md has the new IMPORTANT section about the single-machine pairing store constraint
- [ ] No task attempts `npm install`, `next build`, `docker build`, or `fly deploy`
- [ ] No application code under `apps/web/lib/`, `apps/web/app/`, `apps/bridge/src/`, or `apps/relay/src/` is touched by this plan
</verification>

<success_criteria>
- All tasks completed
- CR-02 (script injection) is closed — secrets only cross the boundary via env block
- CR-03 (build failures masked) is closed — both Dockerfiles fail loudly on bad builds and also assert the runtime-critical artifact exists
- WR-04 (missing workflow permissions) is closed
- README.md warns operators about the single-machine pairing constraint so nobody scales apps/web to 2+ machines before the Drizzle store lands
</success_criteria>

<deferred>
Gaps NOT addressed in this plan:
- Production image still ships devDependencies (WR-07 / original numbering) — out of gap inventory scope
- Relay still runs `node --experimental-strip-types` (IN-06) — tracked for Plan 02-01 swap to compiled JS
- Raw uptimeSeconds in healthz responses (IN-03) — out of gap inventory scope
- InMemoryPairingStore production gate (WR-08 code-level fix) — this plan only documents the constraint; the code-level fix (either crashing on boot in production or landing the Drizzle adapter) is deferred to a later phase
</deferred>

<output>
After completion, create `.planning/phases/01-identity-pairing-foundation/01-06-SUMMARY.md`
</output>
