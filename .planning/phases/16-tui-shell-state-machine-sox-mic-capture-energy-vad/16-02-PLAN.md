---
phase: 16-tui-shell-state-machine-sox-mic-capture-energy-vad
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/achilles-terminal/src/state/constants.ts
  - apps/achilles-terminal/src/state/state-machine.ts
  - apps/achilles-terminal/tests/state/state-machine.test.ts
autonomous: true
requirements:
  - CAP-03

must_haves:
  truths:
    - "ACHILLES_STATES exports the locked 6-state tuple ['idle','listening','processing','speaking','error','muted'] per RESEARCH.md Option A (muted as a 6th state, not an orchestrator-level boolean)"
    - "MUTE_TOGGLE event transitions: idle <-> muted, listening -> muted, muted -> idle (returns to the rest state, NOT to listening — Phase 17 wires real listening re-entry; Phase 16 keeps the reducer's responsibility narrow)"
    - "SPEAKING_DEBOUNCE_MS = 300 const is exported from constants.ts at the workspace's first canonical site (was buried in v1.2 session.ts line 112; Phase 16 surfaces it for reuse)"
    - "Pure transition() reducer is total over the 6-state x event-tag matrix with an exhaustiveness guard (the unreachable default branch throws an Error so adding a new event tag without updating the switch is a compile-time + runtime error)"
    - "createMockStateController + createSessionStateController production seam ports verbatim from v1.2 with import paths repointed to constants.ts only (no behavior change for the 5 original states)"
  artifacts:
    - path: "apps/achilles-terminal/src/state/constants.ts"
      provides: "Locked 6-state tuple + HOTKEY_MODES + PERMISSION_STATES + 5 timing constants from v1.2 + SPEAKING_DEBOUNCE_MS"
      exports: ["ACHILLES_STATES", "AchillesState", "HOTKEY_MODES", "HotkeyMode", "PERMISSION_STATES", "PermissionState", "LISTENING_VAD_DELAY_MS", "PROCESSING_DELAY_MS", "SPEAKING_DELAY_MS", "ERROR_AUTO_DISMISS_MS", "SPEAKING_DEBOUNCE_MS"]
    - path: "apps/achilles-terminal/src/state/state-machine.ts"
      provides: "Pure transition reducer + createMockStateController + createSessionStateController ported from v1.2 with muted substate added (Option A)"
      exports: ["transition", "AchillesEvent", "createMockStateController", "createSessionStateController", "MockStateController", "MockStateControllerOptions"]
    - path: "apps/achilles-terminal/tests/state/state-machine.test.ts"
      provides: "Reducer behavior table tests covering all 5 original states + the new muted substate + MUTE_TOGGLE transitions + exhaustiveness guard"
      contains: "MUTE_TOGGLE"
  key_links:
    - from: "apps/achilles-terminal/src/state/state-machine.ts"
      to: "apps/achilles-terminal/src/state/constants.ts"
      via: "import type { AchillesState, HotkeyMode, PermissionState } and import { LISTENING_VAD_DELAY_MS, PROCESSING_DELAY_MS, SPEAKING_DELAY_MS, ERROR_AUTO_DISMISS_MS }"
      pattern: "from \"./constants\\.js\""
    - from: "apps/achilles/src/main/state-machine.ts"
      to: "apps/achilles-terminal/src/state/state-machine.ts"
      via: "verbatim port with two adjustments — import paths repointed and muted as 6th state"
      pattern: "transition.*AchillesEvent"
---

<objective>
Port the v1.2 state machine to `apps/achilles-terminal/src/state/state-machine.ts` with the minimum changes required for the v1.3 architecture: import paths repoint to the new `constants.ts`, and the 6th state `muted` is added per Option A from `16-RESEARCH.md` (RESEARCH.md Open Question #1 — the muted substate lives in the state machine, not as an orchestrator-level boolean). Surface `SPEAKING_DEBOUNCE_MS = 300` from v1.2 `apps/achilles/src/main/session.ts:112` into the new constants module so Phase 17 can import it from a canonical site instead of re-deriving it.

Purpose: Satisfies CAP-03 (muted state in the state machine reducer; Plan 03's StatusRow.tsx renders the visible MUTED indicator + VoiceShell.tsx wires the `m` keypress that dispatches MUTE_TOGGLE). Also locks the state-machine substrate Plan 04's `session.ts` composition root and Phase 17's full-loop wiring will consume.

Output: 3 files — `src/state/constants.ts` (new), `src/state/state-machine.ts` (ported), `tests/state/state-machine.test.ts` (ported + extended). Zero touches to Phase 15 configs. The new files are `.ts` (not `.tsx`) so the Phase 15 NodeNext + strict tsconfig include glob already covers them — no config extensions needed in Plan 02.

LOOP-02 invariant: zero runtime imports from any of the four voice packages or `achilles-skill`. Type-only imports from `voice-protocol` would be acceptable per CONTEXT.md `<code_context>` row 1, but Phase 16 does not need them — the state machine is self-contained.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-CONTEXT.md
@.planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-RESEARCH.md
@.planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-VALIDATION.md
@apps/achilles/src/main/state-machine.ts
@apps/achilles/src/shared/constants.ts
@apps/achilles-terminal/tsconfig.json

<interfaces>
<!-- Key contracts from the v1.2 port source, repointed for the v1.3 layout. -->

From apps/achilles/src/main/state-machine.ts (port source — 337 LOC read end-to-end per RESEARCH.md):
- `transition(current: AchillesState, event: AchillesEvent, hotkeyMode: HotkeyMode): AchillesState` — pure reducer with exhaustive switch over event.type
- `AchillesEvent` union (v1.2 tags): HOTKEY_PRESS, HOTKEY_RELEASE, CIRCLE_CLICK, MOCK_VAD_COMMIT, MOCK_PROCESSING_COMPLETE, MOCK_PLAYBACK_DONE, STT_COMMITTED (with transcript), CLAUDE_RESULT_READY, TTS_PLAYBACK_DRAINED, CLAUDE_FAILURE_OVERRIDE (with reason), INJECT_ERROR (with kind), ERROR_DISMISS, PERMISSION_CHANGED (with state)
- `AchillesErrorKind` union: "mic_unavailable" | "hotkey_collision" | "persistence_failure" | "unknown"
- `MockStateControllerOptions` interface: broadcast, emitAmplitude?, getMode, now?, setTimeoutImpl?, clearTimeoutImpl?
- `MockStateController` interface: dispatch, now, scheduleMockTransitions, cancelScheduledTransitions
- `createMockStateController(opts): MockStateController` and `createSessionStateController(opts): MockStateController` factories
- The reducer's CIRCLE_CLICK case still exists in v1.2 source — keep it in the port (the legacy event tag is harmless in the terminal context where there is no click surface; Phase 17 will simply never dispatch it. Deleting it would constitute a behavior change to the pure reducer Phase 16 is supposed to port verbatim.)

From apps/achilles/src/shared/constants.ts (port source — only the relevant subset is needed):
- ACHILLES_STATES = ["idle", "listening", "processing", "speaking", "error"] as const
- HOTKEY_MODES = ["toggle", "pushToTalk"] as const
- PERMISSION_STATES = ["granted", "not-determined", "denied", "restricted"] as const
- LISTENING_VAD_DELAY_MS = 1200
- PROCESSING_DELAY_MS = 800
- SPEAKING_DELAY_MS = 2000
- ERROR_AUTO_DISMISS_MS = 8000
- Type derivations: `(typeof TUPLE)[number]` pattern for AchillesState, HotkeyMode, PermissionState
- IPC channel constants, WINDOW_WIDTH/HEIGHT, DEFAULT_MARGIN_PX, init wizard channels, etc. are v1.2-Electron-specific and DO NOT port

From apps/achilles/src/main/session.ts line 112 (Bash grep result):
- `export const SPEAKING_DEBOUNCE_MS = 300;` — the half-duplex tail constant. Phase 16 surfaces this in constants.ts at the canonical workspace site so Phase 17 can `import { SPEAKING_DEBOUNCE_MS } from "../state/constants.js"` without round-tripping through session.ts.

From 16-RESEARCH.md §"State machine port (Phase 16 import-path adjustment only)" lines 896-908:
- Adjustment 1: import paths repoint to new constants.ts
- Adjustment 2: muted as 6th state per Option A. Transitions: `idle <-> muted`, `listening -> muted`, `muted -> idle` (return to idle, NOT to listening — Phase 17 wires VAD re-arm on unmute which logically re-enters listening as a separate flow)
- The reducer's exhaustiveness `_exhaustive: never` guard at the default branch ports unchanged

From RESEARCH.md Open Question #1 (Option A vs B) RECOMMENDATION:
- Option A (add muted to ACHILLES_STATES). Reasons: (1) CONTEXT.md `<domain>` row says "state machine transitions to muted substate"; (2) screen-reader wording table in CONTEXT.md already includes muted; (3) one switch case + one event tag is trivial.

Locked Per-state screen-reader wording (CONTEXT.md `<decisions>` Accessibility — referenced here for Plan 03 alignment, NOT consumed in Plan 02):
- idle/listening/processing/speaking/error/muted each have one explicit string in colors.ts (Plan 03)

LOOP-02 invariant: zero runtime imports from voice-*, claude-code-bridge, or achilles-skill in Plan 02. Verified by grep.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port constants.ts subset from v1.2 shared/constants.ts + surface SPEAKING_DEBOUNCE_MS</name>
  <files>
    apps/achilles-terminal/src/state/constants.ts
  </files>
  <read_first>
    apps/achilles/src/shared/constants.ts,
    apps/achilles/src/main/session.ts,
    .planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-CONTEXT.md,
    .planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1 (state tuple membership): import ACHILLES_STATES from the new constants and assert it equals the readonly tuple ["idle", "listening", "processing", "speaking", "error", "muted"] (length 6, last element "muted", order locked)
    - Test 2 (type derivation): a value of type AchillesState ("muted") compiles without an "as never" cast at runtime; this is checked indirectly by Test 1 succeeding under `tsc -p` strict mode
    - Test 3 (HOTKEY_MODES + PERMISSION_STATES port): both tuples are exported with the v1.2 values verbatim
    - Test 4 (timing constants port): LISTENING_VAD_DELAY_MS === 1200, PROCESSING_DELAY_MS === 800, SPEAKING_DELAY_MS === 2000, ERROR_AUTO_DISMISS_MS === 8000, SPEAKING_DEBOUNCE_MS === 300
  </behavior>
  <action>
    Create apps/achilles-terminal/src/state/constants.ts. This is a fresh file — it does NOT extend or import the v1.2 constants.ts. It is the new canonical site for the SUBSET v1.3 needs.

    Top comment block explains the file scope: "v1.3 state-machine substrate constants. Subset of v1.2 apps/achilles/src/shared/constants.ts — only the symbols the new terminal architecture consumes are ported. The Electron-only constants (WINDOW_WIDTH/HEIGHT, IPC channel names, init-wizard channels, default hotkey accelerator, error copy map) are intentionally left behind because the v1.3 architecture has no renderer process, no IPC bridge, and a different onboarding model (Phase 18 owns config + wizard). SPEAKING_DEBOUNCE_MS is surfaced here from its v1.2 home at apps/achilles/src/main/session.ts:112 so Phase 17's session.ts port has a canonical import site for the half-duplex tail constant."

    Export `ACHILLES_STATES = ["idle", "listening", "processing", "speaking", "error", "muted"] as const` (6-tuple — Option A per RESEARCH.md Open Question #1). Export `type AchillesState = (typeof ACHILLES_STATES)[number]`. Each new state has a sibling JSDoc comment summarizing its role (idle: rest; listening: VAD or PTT armed; processing: claude -p running; speaking: TTS playback active; error: surfaceable failure; muted: VAD off, sox still running so unmute is instant — CONTEXT.md `<decisions>` Mute control row).

    Export `HOTKEY_MODES = ["toggle", "pushToTalk"] as const` and `type HotkeyMode = (typeof HOTKEY_MODES)[number]`. Add a JSDoc note that v1.3 does NOT use hotkeys (the PTT path is removed by CAP-02 per CONTEXT.md), but the reducer still carries the HotkeyMode parameter to preserve the v1.2 transition() function signature byte-for-byte; production callers always pass "toggle".

    Export `PERMISSION_STATES = ["granted", "not-determined", "denied", "restricted"] as const` and `type PermissionState = (typeof PERMISSION_STATES)[number]`. JSDoc note that v1.3 reads OS mic permission via the sox EPERM path (PITFALLS.md §3 — Phase 18 owns the init wizard prompt; Phase 16 only emits a hint to stderr at sox-spawn time, but the type is exported here so the existing PERMISSION_CHANGED event tag in state-machine.ts compiles).

    Export the four timing constants exactly as v1.2 has them: LISTENING_VAD_DELAY_MS = 1200, PROCESSING_DELAY_MS = 800, SPEAKING_DELAY_MS = 2000, ERROR_AUTO_DISMISS_MS = 8000. JSDoc note that these drive createMockStateController's fixture timeline (used by the deterministic state-machine tests and the optional `--mock` shape that drives mock state transitions; Plan 04's mock-amplitude generator is a separate concern from these timers per RESEARCH.md Open Question #2).

    Export `SPEAKING_DEBOUNCE_MS = 300`. JSDoc note that this is surfaced from v1.2 session.ts line 112 — the v1.2 site stays untouched (LOOP-02 — Phase 16 must not modify apps/achilles/), and Phase 17's session.ts port will import from this new site instead of re-deriving the value.

    DO NOT export AchillesErrorKind here (it stays in state-machine.ts per the v1.2 layout). DO NOT export ERROR_COPY (the v1.2 message map is Electron-renderer-bound; Phase 19 ERR-01 owns the v1.3 inline error banner copy). DO NOT export AMPLITUDE_TICK_MS (the 20fps render tick lives in Plan 03's VoiceShell.tsx setInterval, where it is locally scoped because it is a UI concern not a state-machine concern). DO NOT export DEFAULT_VOICE_ID (Phase 18 owns voice selection per CONTEXT.md `<deferred>`).

    No emojis (CLAUDE.md global). No imports at all — this file is self-contained constants.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; node -e "import('./apps/achilles-terminal/src/state/constants.ts').then(m => { const exp = ['ACHILLES_STATES','HOTKEY_MODES','PERMISSION_STATES','LISTENING_VAD_DELAY_MS','PROCESSING_DELAY_MS','SPEAKING_DELAY_MS','ERROR_AUTO_DISMISS_MS','SPEAKING_DEBOUNCE_MS']; for (const e of exp) if (!(e in m)) throw new Error('missing ' + e); if (m.ACHILLES_STATES.length !== 6) throw new Error('state count'); if (m.ACHILLES_STATES[5] !== 'muted') throw new Error('muted'); if (m.SPEAKING_DEBOUNCE_MS !== 300) throw new Error('debounce'); console.log('OK'); }).catch(e => { console.error(e.message); process.exit(1); });" 2>&amp;1 || echo "Direct ts-import not available — Task 2 vitest will verify via static import"</automated>
  </verify>
  <acceptance_criteria>
    - apps/achilles-terminal/src/state/constants.ts exists and exports ACHILLES_STATES (length 6, last element "muted"), AchillesState type, HOTKEY_MODES, HotkeyMode type, PERMISSION_STATES, PermissionState type, LISTENING_VAD_DELAY_MS (1200), PROCESSING_DELAY_MS (800), SPEAKING_DELAY_MS (2000), ERROR_AUTO_DISMISS_MS (8000), SPEAKING_DEBOUNCE_MS (300)
    - File contains NO imports (self-contained constants module)
    - File contains NO Electron-only symbols: grep -E "WINDOW_WIDTH|WINDOW_HEIGHT|IPC_|DEFAULT_HOTKEY_ACCELERATOR|ACHILLES_MODE_INIT|DEFAULT_VOICE_ID|ERROR_COPY|AMPLITUDE_TICK_MS|DEFAULT_MARGIN_PX|DRAG_HANDLE_HEIGHT_PX" apps/achilles-terminal/src/state/constants.ts returns 0 lines
    - `grep -E "voice-protocol|voice-stt|voice-tts|claude-code-bridge|achilles-skill" apps/achilles-terminal/src/state/constants.ts` returns 0 lines (LOOP-02)
    - `grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" apps/achilles-terminal/src/state/constants.ts` returns 0 lines (no emojis)
    - `npm run typecheck --workspace apps/achilles-terminal` exits 0
  </acceptance_criteria>
  <done>
    constants.ts ships with the 6-state tuple + supporting type derivations + 5 timing constants + SPEAKING_DEBOUNCE_MS, all under the existing Phase 15 NodeNext + strict tsconfig. The single canonical site for the state-machine substrate is established for Plan 04 and Phase 17 to consume.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port state-machine.ts from v1.2 with MUTE_TOGGLE event + muted-state transitions</name>
  <files>
    apps/achilles-terminal/src/state/state-machine.ts,
    apps/achilles-terminal/tests/state/state-machine.test.ts
  </files>
  <read_first>
    apps/achilles/src/main/state-machine.ts,
    apps/achilles-terminal/src/state/constants.ts,
    .planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1 (port baseline — idle → listening on HOTKEY_PRESS): transition("idle", { type: "HOTKEY_PRESS" }, "toggle") === "listening" — preserves v1.2 behavior
    - Test 2 (port baseline — listening → processing on STT_COMMITTED): transition("listening", { type: "STT_COMMITTED", transcript: "hi" }, "toggle") === "processing"
    - Test 3 (port baseline — processing → speaking on CLAUDE_RESULT_READY): transition("processing", { type: "CLAUDE_RESULT_READY" }, "toggle") === "speaking"
    - Test 4 (port baseline — speaking → idle on TTS_PLAYBACK_DRAINED): transition("speaking", { type: "TTS_PLAYBACK_DRAINED" }, "toggle") === "idle"
    - Test 5 (port baseline — failure-override is processing → speaking): transition("processing", { type: "CLAUDE_FAILURE_OVERRIDE", reason: "test" }, "toggle") === "speaking"
    - Test 6 (port baseline — INJECT_ERROR routes anything → error): transition("listening", { type: "INJECT_ERROR", kind: "unknown" }, "toggle") === "error" AND transition("speaking", { type: "INJECT_ERROR", kind: "mic_unavailable" }, "toggle") === "error"
    - Test 7 (port baseline — ERROR_DISMISS routes error → idle): transition("error", { type: "ERROR_DISMISS" }, "toggle") === "idle"
    - Test 8 (port baseline — PTT release): transition("listening", { type: "HOTKEY_RELEASE" }, "pushToTalk") === "processing" AND transition("listening", { type: "HOTKEY_RELEASE" }, "toggle") === "listening"
    - Test 9 (NEW — MUTE_TOGGLE idle ↔ muted): transition("idle", { type: "MUTE_TOGGLE" }, "toggle") === "muted" AND transition("muted", { type: "MUTE_TOGGLE" }, "toggle") === "idle"
    - Test 10 (NEW — MUTE_TOGGLE listening → muted): transition("listening", { type: "MUTE_TOGGLE" }, "toggle") === "muted"
    - Test 11 (NEW — MUTE_TOGGLE during processing/speaking/error is ignored): transition("processing", { type: "MUTE_TOGGLE" }, "toggle") === "processing" (and same for speaking, error). The reducer cannot mute mid-utterance; the user must wait for the loop to return to idle. CONTEXT.md `<decisions>` Mute control row scopes mute to "VAD off without exiting the session" — semantically meaningful only when VAD could be armed, i.e., from idle or listening.
    - Test 12 (NEW — speech_start from muted is suppressed by VAD, not state machine): the state machine's response to STT_COMMITTED from "muted" is "muted" (event ignored). The self-trigger guard at the VAD layer prevents the speech_start dispatch in the first place per CONTEXT.md, but the state machine remains pure regardless. transition("muted", { type: "STT_COMMITTED", transcript: "x" }, "toggle") === "muted"
    - Test 13 (mock controller — listening schedules MOCK_VAD_COMMIT after LISTENING_VAD_DELAY_MS): given a deterministic setTimeoutImpl mock, calling scheduleMockTransitions("listening") schedules a 1200ms timer; firing it dispatches MOCK_VAD_COMMIT and the broadcast callback receives "processing"
    - Test 14 (session controller — scheduleMockTransitions is a no-op): createSessionStateController returns a controller whose setTimeoutImpl is the locked () => null; calling scheduleMockTransitions("listening") does NOT cause any state transition
    - Test 15 (exhaustiveness guard): passing an object with an unknown type field (cast as never) into transition() throws an Error containing "Unknown AchillesEvent"
  </behavior>
  <action>
    Create apps/achilles-terminal/src/state/state-machine.ts and apps/achilles-terminal/tests/state/state-machine.test.ts. Tests first per TDD.

    apps/achilles-terminal/src/state/state-machine.ts: port verbatim from apps/achilles/src/main/state-machine.ts with two adjustments per RESEARCH.md §"State machine port" lines 898-908:

    Adjustment 1 (import paths): replace the v1.2 imports `import { ERROR_AUTO_DISMISS_MS, LISTENING_VAD_DELAY_MS, PROCESSING_DELAY_MS, SPEAKING_DELAY_MS } from "../shared/constants.js"; import type { AchillesState, HotkeyMode, PermissionState } from "../shared/constants.js"` with the v1.3 equivalents from `"./constants.js"`. NO other module imports change; the file is otherwise self-contained.

    Adjustment 2 (add MUTE_TOGGLE event + muted transitions): extend the AchillesEvent union to include `| { type: "MUTE_TOGGLE" }` (a no-payload event). In the transition() switch, add a `case "MUTE_TOGGLE":` branch that returns "muted" if current === "idle" OR current === "listening", returns "idle" if current === "muted", and returns current otherwise (processing/speaking/error ignore MUTE_TOGGLE per Test 11 behavior). Add a JSDoc note above the case explaining: "muted is a substate of idle in the v1.3 architecture — VAD is gated off but sox keeps running so unmute is instant per CONTEXT.md `<decisions>` Mute control row. Only idle and listening accept the toggle because mid-utterance mute would orphan the loop; processing/speaking/error remain transparent to the toggle (the user must wait for the loop to drain to idle, OR for the error to dismiss)."

    Adjustment 2b: when the current state is "muted", every event tag other than MUTE_TOGGLE returns "muted" unchanged. This is structurally NEW — v1.2 had no muted handling. Concretely: STT_COMMITTED, MOCK_VAD_COMMIT, HOTKEY_PRESS/RELEASE, CIRCLE_CLICK, MOCK_PROCESSING_COMPLETE/PLAYBACK_DONE, CLAUDE_RESULT_READY, TTS_PLAYBACK_DRAINED, CLAUDE_FAILURE_OVERRIDE, PERMISSION_CHANGED — all return "muted" when current === "muted". INJECT_ERROR still routes to "error" (the user can still surface a fatal error from any state). ERROR_DISMISS is irrelevant from muted. Implement this by adding an early-return guard at the top of transition(): `if (current === "muted" && event.type !== "MUTE_TOGGLE" && event.type !== "INJECT_ERROR") return current`. This single guard keeps the rest of the switch unchanged from the v1.2 port (no per-case "if muted" branches sprinkled through).

    Update the JSDoc header comment block to reflect the v1.3 layout (state machine now lives at apps/achilles-terminal/src/state/, MUTE_TOGGLE added per CONTEXT.md). Keep the behavior table prose comment intact (v1.2's "Behaviour table" comment block ports verbatim — the prose is documentation of the reducer's behavior and adding muted is a single-line addition to that block).

    Port AchillesErrorKind exactly. Port MockStateControllerOptions and MockStateController interfaces exactly. Port createMockStateController and createSessionStateController exactly with the import-path adjustment only.

    LOOP-02 invariant: zero imports from voice-*, claude-code-bridge, achilles-skill. The v1.2 source file had no such imports either, so the port preserves this.

    apps/achilles-terminal/tests/state/state-machine.test.ts: vitest test suite with describe blocks "transition reducer (v1.2 port)", "transition reducer (MUTE_TOGGLE)", "transition reducer (muted state behavior)", "createMockStateController", "createSessionStateController", "exhaustiveness". Each block holds the corresponding tests from the behavior section. Use vitest's `vi.fn()` for the broadcast callback. For setTimeoutImpl injection, capture the scheduled callback and the delay via a `Map<unknown, { cb: () => void; ms: number }>` so tests can drive the fixture timeline deterministically without real timers. For exhaustiveness test, use `expect(() => transition("idle", { type: "BOGUS" } as never, "toggle")).toThrow(/Unknown AchillesEvent/)`.

    Run `npm test --workspace apps/achilles-terminal -- --pool=forks tests/state/state-machine.test.ts` to confirm all 15 tests pass.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npm test --workspace apps/achilles-terminal -- --pool=forks tests/state/state-machine.test.ts 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/achilles-terminal/src/state/state-machine.ts exports `transition`, `AchillesEvent`, `AchillesErrorKind`, `createMockStateController`, `createSessionStateController`, `MockStateController`, `MockStateControllerOptions`
    - state-machine.ts AchillesEvent union contains the literal string "MUTE_TOGGLE" exactly once and includes all 13 v1.2 event tags (HOTKEY_PRESS, HOTKEY_RELEASE, CIRCLE_CLICK, MOCK_VAD_COMMIT, MOCK_PROCESSING_COMPLETE, MOCK_PLAYBACK_DONE, STT_COMMITTED, CLAUDE_RESULT_READY, TTS_PLAYBACK_DRAINED, CLAUDE_FAILURE_OVERRIDE, INJECT_ERROR, ERROR_DISMISS, PERMISSION_CHANGED) — 14 tags total
    - state-machine.ts transition() includes a `case "MUTE_TOGGLE":` branch that handles idle/listening → muted, muted → idle, and other states return current
    - state-machine.ts contains an early-return guard for the muted-state passthrough: `if (current === "muted" && event.type !== "MUTE_TOGGLE" && event.type !== "INJECT_ERROR") return current` (or equivalent boolean shape that produces identical behavior)
    - All 15 tests in tests/state/state-machine.test.ts pass under `npm test --workspace apps/achilles-terminal`
    - `npm run typecheck --workspace apps/achilles-terminal` exits 0 (the exhaustiveness `_exhaustive: never` guard still type-checks with the muted addition because MUTE_TOGGLE is now in the union)
    - `grep -E "voice-protocol|voice-stt|voice-tts|claude-code-bridge|achilles-skill" apps/achilles-terminal/src/state/state-machine.ts apps/achilles-terminal/tests/state/state-machine.test.ts` returns 0 lines (LOOP-02)
    - `grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" apps/achilles-terminal/src/state/state-machine.ts apps/achilles-terminal/tests/state/state-machine.test.ts` returns 0 lines (no emojis)
    - The v1.2 source file at apps/achilles/src/main/state-machine.ts is UNCHANGED (verified by `git diff apps/achilles/src/main/state-machine.ts` returning no output)
  </acceptance_criteria>
  <done>
    state-machine.ts ports verbatim from v1.2 with the muted substate added per Option A. createMockStateController + createSessionStateController preserve their v1.2 surface for the deterministic-fixture test path Phase 17 will leverage. The exhaustiveness guard remains type-safe under strict mode. CAP-03 reducer substrate complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Event dispatch (caller → reducer) | The reducer is a pure function; untrusted input is bounded to the AchillesEvent union shape via TypeScript's exhaustiveness check, plus a runtime throw for unknown tags |
| Timer injection (test → controller) | setTimeoutImpl / clearTimeoutImpl seam is trusted in production (defaults to setTimeout / clearTimeout); mocked deterministically in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-event-tampering | Tampering | apps/achilles-terminal/src/state/state-machine.ts transition() | mitigate | The reducer's switch is exhaustive over event.type with a `_exhaustive: never` assertion at the default branch that produces a compile-time error on missing cases AND a runtime throw on unknown tags. New event tags require updating the switch — no silent acceptance. Verified by Test 15 (exhaustiveness guard). |
| T-16-muted-bypass | Tampering | apps/achilles-terminal/src/state/state-machine.ts muted-state passthrough | mitigate | The early-return guard for muted state ignores all events except MUTE_TOGGLE and INJECT_ERROR. Even if the VAD layer's self-trigger guard fails open and dispatches a spurious STT_COMMITTED, the state machine cannot exit muted via that path. Defense-in-depth between the VAD layer and the state machine layer per CONTEXT.md `<decisions>` row "self-trigger guard at VAD layer, NOT state machine layer." Verified by Test 12. |
</threat_model>

<verification>
After both tasks complete:
- `npm test --workspace apps/achilles-terminal -- --pool=forks tests/state/` exits 0 with all 15 tests passing (Task 2 covers Task 1's constants via static imports too)
- `npm run typecheck --workspace apps/achilles-terminal` exits 0
- `npm run lint --workspace apps/achilles-terminal -- --max-warnings 0` exits 0 (no new ESLint warnings; Phase 15 baseline rules apply unchanged)
- `git diff apps/achilles/src/main/state-machine.ts apps/achilles/src/shared/constants.ts apps/achilles/src/main/session.ts` returns no output (LOOP-02 — v1.2 sources untouched)
- `grep -rE "voice-protocol|voice-stt|voice-tts|claude-code-bridge|companion.md" apps/achilles-terminal/src/state/ apps/achilles-terminal/tests/state/` returns 0 lines
- `grep -rP "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" apps/achilles-terminal/src/state/ apps/achilles-terminal/tests/state/` returns 0 lines (no emojis)
</verification>

<success_criteria>
CAP-03 reducer substrate: ACHILLES_STATES contains the 6th "muted" state; MUTE_TOGGLE event tag handles idle/listening → muted, muted → idle. Verified by Tests 9, 10, 11.
v1.2 behavior preserved: all 5 original state transitions (idle/listening/processing/speaking/error) behave identically under the original event tags. Verified by Tests 1-8.
SPEAKING_DEBOUNCE_MS surfaced: the v1.2 session.ts:112 constant is now exported from the new constants.ts canonical site. Verified by Test 4 of Task 1.
LOOP-02: zero references to voice-*/claude-code-bridge/companion.md in any new file; v1.2 source files (state-machine.ts, constants.ts, session.ts) are UNCHANGED on disk.
PITFALLS.md compatibility: the muted-state early-return guard is defense-in-depth above the VAD layer's self-trigger guard; both layers must fail for a state-machine exit-from-muted via a spurious STT_COMMITTED to occur.
</success_criteria>

<output>
Create `.planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-02-SUMMARY.md` summarizing: 3 files created (1 new constants + 1 ported state machine + 1 new test suite), test count (15), the Option A vs Option B decision recorded (Option A adopted per RESEARCH.md recommendation), and any deviations from the v1.2 port encountered.
</output>
</content>
</invoke>