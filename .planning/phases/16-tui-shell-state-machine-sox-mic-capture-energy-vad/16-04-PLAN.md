---
phase: 16-tui-shell-state-machine-sox-mic-capture-energy-vad
plan: 04
type: execute
wave: 3
depends_on:
  - 16-01
  - 16-02
  - 16-03
files_modified:
  - apps/achilles-terminal/src/cli.ts
  - apps/achilles-terminal/src/session.ts
  - apps/achilles-terminal/src/ui/useAchillesState.ts
  - apps/achilles-terminal/src/ui/VoiceShell.tsx
  - apps/achilles-terminal/src/store-stub.ts
  - apps/achilles-terminal/tests/cli.test.ts
  - apps/achilles-terminal/tests/session.test.ts
  - apps/achilles-terminal/tests/ui/voice-shell.test.tsx
autonomous: true
requirements:
  - CAP-03
  - TUI-06

must_haves:
  truths:
    - "cli.ts adds a `voice` subcommand via commander with three flags: --mock, --debug-vad, --plain — argv parse for --version / -v / --latency-probe runs BEFORE any voice subcommand wiring or dynamic import, preserving the INIT-07 invariant established in Phase 15"
    - "session.ts is the in-process composition root: extends EventEmitter, owns the state machine (Plan 02 createSessionStateController), wires the mic source (mic-sox from Plan 01 OR mock-amplitude from Plan 03 when --mock), wires the VAD (createEnergyVad from Plan 01), and exposes toggleMute() + bindings for the UI tier — but does NOT import voice-stt / voice-tts / claude-code-bridge (LOOP-02 — Phase 17 wires those)"
    - "VoiceShell.tsx is the Ink root component: mounts Blob + Sparkline + StatusRow + ScreenReader children depending on isScreenReaderActive(); registers useInput('m') for mute toggle WITHOUT swallowing Ctrl-C (per RESEARCH Assumption A3 — full callback signature with key.ctrl + key.meta guards); drives a 20fps tick via setInterval(50ms); subscribes to session.state and session.amplitude via useSyncExternalStore (useAchillesState hook)"
    - "useAchillesState.ts is the React adapter — useSyncExternalStore hooking session.on('state-change') and session.on('amplitude') / 'rms-sample' — exposed as exports useAchillesState, useAmplitude, useRingBuffer (returns the Float32Array(80) + writeIndex pair Sparkline needs)"
    - "store-stub.ts ships loadSettings() returning DEFAULT_VAD_CONFIG defaults — Phase 18 ships the real ~/.achilles/settings.json reader (per CONTEXT.md <decisions> row 'All four overridable via settings.json — Phase 18 owns the settings store; Phase 16 reads via a stub')"
    - "render() is called WITHOUT { exitOnCtrlC: false } — the Ink default true is preserved (RESEARCH Assumption A3 + Pitfall 6); the SIGINT handler at process-level is a minimum `process.once('SIGINT', () => micSox?.stop())` placeholder that Phase 17's gracefulShutdown chain will replace"
    - "When !process.stdout.isTTY OR --plain flag is set, cli.ts routes to startPlainMode() (from Plan 03's plain-text.ts) — Ink is NEVER mounted in plain mode (per Pitfall 4 — mounting Ink to non-TTY produces garbage)"
    - "The `voice` subcommand emits a clean exit on SIGINT within 1.5s with no orphan sox processes (verified by tests/cli.test.ts integration test spawning the child + sending SIGINT + asserting exit code + ps lookup)"
  artifacts:
    - path: "apps/achilles-terminal/src/cli.ts"
      provides: "Phase 15-extended CLI router: --version + -v + --latency-probe argv-first branches preserved, new `voice` subcommand with --mock / --debug-vad / --plain flags registered via commander"
      exports: []
    - path: "apps/achilles-terminal/src/session.ts"
      provides: "In-process composition root — EventEmitter producer; owns state machine + mic source + VAD; exposes toggleMute(), start(), stop(); does NOT touch voice-stt/voice-tts/claude-code-bridge (LOOP-02)"
      exports: ["Session", "SessionEvents", "SessionOptions", "createSession", "runVoice"]
    - path: "apps/achilles-terminal/src/store-stub.ts"
      provides: "Settings loader stub returning DEFAULT_VAD_CONFIG defaults — Phase 18 ships the real ~/.achilles/settings.json reader"
      exports: ["loadSettings", "AchillesSettings"]
    - path: "apps/achilles-terminal/src/ui/useAchillesState.ts"
      provides: "React adapter — useSyncExternalStore hooks for state, amplitude scalar, and RMS ring buffer"
      exports: ["useAchillesState", "useAmplitude", "useRingBuffer"]
    - path: "apps/achilles-terminal/src/ui/VoiceShell.tsx"
      provides: "Ink root component composing Blob + Sparkline + StatusRow + ScreenReader; setInterval(50) tick; useInput('m') mute toggle WITHOUT Ctrl-C swallowing; --debug-vad stderr stream"
      exports: ["VoiceShell"]
  key_links:
    - from: "apps/achilles-terminal/src/cli.ts"
      to: "src/session.ts + src/ui/VoiceShell.tsx + src/ui/plain-text.ts"
      via: "dynamic import gated by argv parse — pipeline-boot modules ONLY load after --version / -v / --latency-probe branches early-return (INIT-07)"
      pattern: "voice|--mock|--debug-vad|--plain"
    - from: "apps/achilles-terminal/src/session.ts"
      to: "src/state/state-machine.ts (Plan 02) + src/audio/mic-sox.ts + src/audio/vad-energy.ts (Plan 01) + src/ui/mock-amplitude.ts (Plan 03) + src/store-stub.ts"
      via: "imported as runtime dependencies — composition root wires them all together"
      pattern: "createSession|createEnergyVad|createMicSox|createMockAmplitudeStream|loadSettings"
    - from: "apps/achilles-terminal/src/ui/VoiceShell.tsx"
      to: "ink render() (default exitOnCtrlC=true preserved) + useInput hook + useSyncExternalStore via useAchillesState"
      via: "Ink + React composition"
      pattern: "useInput|render|useSyncExternalStore"
---

<objective>
Wire the audio + state + UI primitives from Plans 01-03 into a working `achilles voice` subcommand that mounts the Ink tree in a real terminal, drives the 20fps render via a `setInterval` tick, captures the `m` mute key without swallowing Ctrl-C, and auto-degrades to plain-text mode on `!process.stdout.isTTY` or `--plain`. The composition root is `session.ts` (an EventEmitter that owns the state machine, mic source, and VAD); the React adapter is `useAchillesState.ts` (useSyncExternalStore subscriptions); the Ink root is `VoiceShell.tsx`. cli.ts gains the `voice` subcommand + three flags (`--mock`, `--debug-vad`, `--plain`) extending the Phase 15 router without regressing the INIT-07 argv-first invariant.

Purpose: Wires CAP-03 (the `m` key -> state.MUTE_TOGGLE -> VAD.setMuted(true) -> status row shows MUTED — Plan 03 has the rendering; Plan 04 wires the keypress) and TUI-06 (plain-text fallback routing — Plan 03 has the formatter; Plan 04 wires the router decision based on isTTY + --plain). The other 10 requirement IDs from Phase 16 are wired transitively: TUI-01/02/03/04 = render Plan 03's components; ACC-01/02 = isScreenReaderActive() branch in VoiceShell; CAP-01 = construct createMicSox in non-mock mode; CAP-02 + CAP-04 = construct createEnergyVad with loadSettings() defaults, --debug-vad streams snapshot() JSON lines to stderr.

Output: 5 new source files + 3 test files. Extends `cli.ts` with the voice subcommand. No new package installs (Plan 03 handled all package additions). The 8 files listed in `files_modified` constitute the entire Plan 04 surface.

LOOP-02 invariant — Phase 16's hardest line. Plan 04 imports NO runtime symbol from `@achilles/voice-protocol`, `@achilles/voice-stt`, `@achilles/voice-tts`, `@achilles/claude-code-bridge`, or `@achilles/achilles-skill`. Phase 17's session.ts port will add factory-function parameters (sttFactory, ttsFactory, claudeBridgeFactory) per RESEARCH.md Open Question #4 — Plan 04's session.ts is structured to accept those factories as optional constructor params that default to undefined (so Phase 17 only needs to pass real factories, not refactor the constructor signature).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-CONTEXT.md
@.planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-RESEARCH.md
@.planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-VALIDATION.md
@.planning/research/PITFALLS.md
@apps/achilles-terminal/package.json
@apps/achilles-terminal/tsconfig.json
@apps/achilles-terminal/src/cli.ts
@apps/achilles-terminal/src/state/constants.ts
@apps/achilles-terminal/src/state/state-machine.ts
@apps/achilles-terminal/src/audio/mic-sox.ts
@apps/achilles-terminal/src/audio/vad-energy.ts
@apps/achilles-terminal/src/audio/braille.ts
@apps/achilles-terminal/src/ui/colors.ts
@apps/achilles-terminal/src/ui/Blob.tsx
@apps/achilles-terminal/src/ui/Sparkline.tsx
@apps/achilles-terminal/src/ui/StatusRow.tsx
@apps/achilles-terminal/src/ui/ScreenReader.tsx
@apps/achilles-terminal/src/ui/plain-text.ts
@apps/achilles-terminal/src/ui/mock-amplitude.ts
@apps/achilles-terminal/tests/cli.test.ts

<interfaces>
<!-- Key contracts the executor needs. -->

From Phase 15 apps/achilles-terminal/src/cli.ts current state (read directly):
- The file has top-level static imports of node:fs/promises, node:url, node:path only.
- main() reads argv.slice(2) and branches: `--version`/`-v` -> read package.json -> process.stdout.write(version + "\n", () => process.exit(0)).
- `--latency-probe` branch — hrtime stub.
- argv[0] === "voice" currently writes "TUI not implemented in Phase 15. Phase 16 ships this." to stderr and exits 1 — Plan 04 REPLACES this branch.
- Top-level fatal handlers: process.on("uncaughtException", ...) and process.on("unhandledRejection", ...) before main() invocation.
- main().catch() top-level catch.
- INIT-07 invariant: argv parse precedes any dynamic import of pipeline-boot modules.

Plan 04 EXTENSION rules:
- KEEP the existing --version/-v/--latency-probe argv-first branches at the TOP of main() unchanged. They MUST execute before any dynamic import.
- KEEP the existing fatal handlers.
- REPLACE the "voice" stub branch with a dynamic import gate: `if (argv[0] === "voice") { const { runVoice } = await import("./session.js"); await runVoice(argv.slice(1)); return; }`. This dynamic import is the ONLY new import path; it loads commander, session, VoiceShell etc. lazily so --version still has zero pipeline-boot cost.

DECISION (planner): Inline the voice-routing logic into a NEW exported function `runVoice` in src/session.ts. Plan 04's cli.ts dynamically imports `runVoice` from `./session.js` and the runVoice function in session.ts is the single entry point that (a) parses the voice subcommand flags via commander, (b) decides isTTY + --plain routing, (c) constructs the Session, (d) either mounts Ink (TTY mode) or starts plain mode. This keeps the file count tight and the composition root in one place.

From Plan 03 Task 3 (Plan 04 must consume):
- `formatPlainLine(input: PlainLineInput): string` and `startPlainMode(events): Promise<void>` from src/ui/plain-text.ts
- `createMockAmplitudeStream({ seed, onFrame, intervalMs })` from src/ui/mock-amplitude.ts
- `idleBreathingAmplitude(tickMs)`, `processingPulseAmplitude(tickMs)`, `STATE_COLORS`, `SCREEN_READER_WORDING`, `colorize`, `isScreenReaderActive` from src/ui/colors.ts

From Plan 02 (Plan 04 must consume):
- `transition`, `AchillesEvent`, `createSessionStateController` from src/state/state-machine.ts
- `AchillesState`, `HotkeyMode`, `SPEAKING_DEBOUNCE_MS` from src/state/constants.ts

From Plan 01 (Plan 04 must consume):
- `createMicSox`, `MicSoxHandle` from src/audio/mic-sox.ts
- `createEnergyVad`, `VadHandle`, `VadConfig`, `DEFAULT_VAD_CONFIG` from src/audio/vad-energy.ts
- (Plan 03's Sparkline already consumes braille.ts; Plan 04 does not import braille.ts directly)

From 16-RESEARCH.md §"Pattern 1" lines 305-322 — verbatim shape for session.ts:
- `interface SessionEvents { "state-change": [AchillesState]; "transcript-partial": [string]; "amplitude": [number]; "rms-sample": [number] }`
- `class Session extends EventEmitter` is the composition root that owns state-machine + mic + VAD

From 16-RESEARCH.md §"Pattern 4: useSyncExternalStore" lines 391-406 — verbatim shape for useAchillesState.ts:
- `useAchillesState(session): AchillesState` calls `useSyncExternalStore` with subscribe = (cb) => { session.on("state-change", cb); return () => session.off("state-change", cb) }; getSnapshot = () => session.currentState; getServerSnapshot = () => session.currentState

From 16-RESEARCH.md §"Pattern 2: useInput" + Assumption A3 — verbatim shape for the m-key handler in VoiceShell.tsx:
- `useInput((input, key) => { if (input === "m" && !key.ctrl && !key.meta) session.toggleMute() })`
- Ctrl-C is NOT in this callback — Ink's exitOnCtrlC default handles it
- CRITICAL: do NOT pass `{ exitOnCtrlC: false }` to the root `render()` call. The default true is what we want; Phase 17's gracefulShutdown chain will wrap Ink's default handler.

From 16-RESEARCH.md §"TTY detection precedence" lines 858-893 — verbatim shape for runVoice's routing:
- `const usePlain = args.plain || !process.stdout.isTTY`
- `if (usePlain) { await startPlainMode(session, args); return; }`
- `const { unmount, waitUntilExit } = render(<VoiceShell session={session} debugVad={args.debugVad} />); await waitUntilExit()`

From 16-RESEARCH.md §"Settings loader stub" lines 910-944 — verbatim shape for store-stub.ts:
- `interface AchillesSettings { vad: VadConfig }` — Phase 18 will expand
- `loadSettings(): AchillesSettings = () => ({ vad: structuredClone(DEFAULT_VAD_CONFIG) })` — fresh copy per call

From 16-RESEARCH.md §"Pitfall 6: Ink's Ctrl-C handler" — the Phase 16 minimum SIGINT handler:
- `process.once("SIGINT", () => { micSox?.stop(); /* Ink unmounts via default */ })` — use `process.once`, not `process.on`. Phase 17 REPLACES this with the full gracefulShutdown chain.

From CONTEXT.md `<decisions>` Mic capture row + 16-RESEARCH.md §"Pitfall 7: sox device-died":
- On sox exit code != 0: emit a state transition to `error` via the orchestrator (dispatch INJECT_ERROR with kind "mic_unavailable"). Write the captured stderr to process.stderr.
- Do NOT restart sox in Phase 16 — that's Phase 19 ERR-03. Phase 16 fails visibly.
- On sox EPERM (exit code 126 OR stderr contains "EPERM" / "Permission denied"), resolve parent emulator via `child_process.execSync("ps -p " + process.ppid + " -o comm=")` (with a try/catch — failure to resolve is non-fatal) and write to stderr: `"achilles: sox cannot open the microphone. On macOS, grant Microphone access to your terminal (parent: <emulator>). Open System Settings > Privacy & Security > Microphone."`

From RESEARCH.md Open Questions #4 and #2:
- session.ts constructor accepts OPTIONAL `sttFactory`, `ttsFactory`, `claudeBridgeFactory` parameters (typed as `unknown` in Plan 04, defaulted to undefined). Phase 16 ignores them — they exist solely so Phase 17 can drop in real factories without a constructor refactor.
- `--mock` mode swaps mic-sox for createMockAmplitudeStream. The mock-amplitude generator emits amplitude scalars directly (no PCM frames). session.ts's mock branch feeds the amplitude AS RMS into both the "amplitude" event (Blob) AND the ring buffer for the "rms-sample" event (Sparkline) AND vad.observe(amplitude, 20) so the VAD can fire speech_start on the speech-window peaks.
- The v1.2 createMockStateController fixture-timer constants (LISTENING_VAD_DELAY_MS etc.) are NOT used by Plan 04 — createSessionStateController is the production controller used in both --mock and real modes.

LOOP-02 invariant: zero runtime imports from voice-* / claude-code-bridge / achilles-skill in any of the 8 new files. Verified by grep.

From RESEARCH.md §"Pitfall 5: Bun stdout flush":
- For long-running processes (the Ink tree until SIGINT), `process.stderr.write` and `process.stdout.write` are fine WITHOUT the explicit callback form. The callback form is only required at process-exit paths. Ink's render() owns the long-running stdout writes.
- For --debug-vad stderr stream: plain `process.stderr.write(JSON.stringify(snapshot) + "\n")` is fine.
- For plain-mode startPlainMode (Plan 03's function): same applies — long-running, no callback needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement store-stub.ts + useAchillesState.ts + session.ts (composition root)</name>
  <files>
    apps/achilles-terminal/src/store-stub.ts,
    apps/achilles-terminal/src/ui/useAchillesState.ts,
    apps/achilles-terminal/src/session.ts,
    apps/achilles-terminal/tests/session.test.ts
  </files>
  <read_first>
    apps/achilles-terminal/src/state/state-machine.ts,
    apps/achilles-terminal/src/state/constants.ts,
    apps/achilles-terminal/src/audio/mic-sox.ts,
    apps/achilles-terminal/src/audio/vad-energy.ts,
    apps/achilles-terminal/src/ui/mock-amplitude.ts,
    apps/achilles-terminal/src/ui/colors.ts,
    .planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-RESEARCH.md,
    .planning/research/PITFALLS.md
  </read_first>
  <behavior>
    - Test 1 (loadSettings returns defaults): loadSettings() returns an object with `.vad` deep-equal to DEFAULT_VAD_CONFIG from Plan 01. Two successive calls return distinct object references (structuredClone — mutating the returned object does NOT mutate the next call's return).
    - Test 2 (Session construction default state idle): createSession() has `.currentState === "idle"` immediately after construction. The state machine inside the session uses Plan 02's createSessionStateController.
    - Test 3 (Session.toggleMute round-trip): from currentState === "idle", calling session.toggleMute() emits a "state-change" event with payload "muted" and currentState becomes "muted". A second toggleMute() returns to "idle".
    - Test 4 (Session.toggleMute drives VAD mute flag): session.toggleMute() calls VAD.setMuted(true) under the hood (verified via spies — inject a custom VAD via a private __test seam OR via the SessionOptions.vadOverride field added solely for testability). A second toggle calls setMuted(false).
    - Test 5 (Session.start in --mock mode): session.start() with constructor opt mock=true does NOT call createMicSox (spawnImpl mock never invoked); the mock amplitude stream is started; with vi.useFakeTimers(), advance 600ms (30 frames at 20ms) and verify at least one "amplitude" event with amplitude > 0.4 was emitted (speech-window peak).
    - Test 6 (Session.start in real mode wires mic-sox + VAD): session.start() with mock=false calls createMicSox with the injected spawnImpl. A fake 640-byte PCM chunk delivered to the mock child stdout produces an "amplitude" event AND vad.observe is called with the computed RMS scalar. When the injected VAD returns "speech_start", a "state-change" event fires with payload "listening" (idle -> listening transition via dispatch({ type: "HOTKEY_PRESS" })).
    - Test 7 (sox onExit triggers error state + stderr): when the mock spawnImpl's fake child emits exit code 1 with stderr "no default device", session emits "state-change" with payload "error" AND process.stderr.write was called with the captured stderr text plus a per-emulator hint (verify via vi.spyOn(process.stderr, "write")).
    - Test 8 (useAchillesState subscribes correctly): rendering a test component that uses useAchillesState(session) returns the current state. Calling session.toggleMute() triggers a re-render via the useSyncExternalStore subscription with the new state value. Use ink-testing-library's render() to mount the test component.
    - Test 9 (--debug-vad emission shape): when constructed with debugVad=true and started in --mock mode, every VAD observe() call (which happens for each mock frame) leads to one process.stderr.write call with a JSON string matching the locked CONTEXT.md `<specifics>` row 4 shape `{"t":<number>,"energy":<number>,"noiseFloor":<number>,"threshold":<number>,"state":"silence"|"voice"}` plus optional warmupRemaining field.
    - Test 10 (LOOP-02 import gate): the source of session.ts contains NO import lines referencing `@achilles/voice-protocol`, `@achilles/voice-stt`, `@achilles/voice-tts`, `@achilles/claude-code-bridge`, or `@achilles/achilles-skill` (verified by grep -E pattern returning 0 lines).
  </behavior>
  <action>
    Create four files. Tests are written first per TDD; commit all together once they pass.

    apps/achilles-terminal/src/store-stub.ts: implement per RESEARCH.md §"Settings loader stub" lines 910-944. Imports: `import { DEFAULT_VAD_CONFIG, type VadConfig } from "./audio/vad-energy.js"`. Export interface `AchillesSettings { vad: VadConfig }`. Export const `DEFAULTS: AchillesSettings = { vad: DEFAULT_VAD_CONFIG }`. Export `loadSettings(): AchillesSettings = () => ({ vad: structuredClone(DEFAULT_VAD_CONFIG) })` (or equivalent — each call returns a fresh structuredClone so mutation isolation is guaranteed). JSDoc top comment: "Phase 16 stub — Phase 18 ships the real ~/.achilles/settings.json reader per CONTEXT.md <decisions> row 'Phase 18 owns the settings store; Phase 16 reads via a stub'. The four VAD threshold knobs (voice_threshold, silence_threshold, voice_hold_ms, silence_hold_ms) become user-overridable in Phase 18; Phase 16 returns DEFAULT_VAD_CONFIG unconditionally."

    apps/achilles-terminal/src/ui/useAchillesState.ts: imports `import { useSyncExternalStore } from "react"; import type { AchillesState } from "../state/constants.js"; import type { Session } from "../session.js"`. Export hook `useAchillesState(session: Session): AchillesState` per RESEARCH.md §"Pattern 4" verbatim shape: `useSyncExternalStore((cb) => { session.on("state-change", cb); return () => session.off("state-change", cb); }, () => session.currentState, () => session.currentState)`. Export hook `useAmplitude(session: Session): number` — same pattern subscribed to "amplitude" events; snapshot returns `session.currentAmplitude`. Export hook `useRingBuffer(session: Session): { ring: Float32Array; writeIndex: number }` — subscribed to "rms-sample" events; snapshot returns a stable object reference `session.currentRingBuffer` (the session caches the object reference and only constructs a new one when writeIndex advances, so React's referential-equality semantics produce stable re-renders).

    apps/achilles-terminal/src/session.ts: the composition root. Top imports: `import { EventEmitter } from "node:events"; import { execSync } from "node:child_process"; import { createSessionStateController, type MockStateController } from "./state/state-machine.js"; import type { AchillesState, HotkeyMode } from "./state/constants.js"; import { createMicSox, type MicSoxHandle } from "./audio/mic-sox.js"; import { createEnergyVad, type VadHandle } from "./audio/vad-energy.js"; import { createMockAmplitudeStream } from "./ui/mock-amplitude.js"; import { loadSettings, type AchillesSettings } from "./store-stub.js"`.

    Export interface `SessionEvents { "state-change": [AchillesState]; "transcript-partial": [string]; "amplitude": [number]; "rms-sample": [number]; "error-message": [string] }`. Export interface `SessionOptions { mock?: boolean; debugVad?: boolean; settings?: AchillesSettings; spawnImpl?: typeof import("node:child_process").spawn; mockSeed?: number; vadOverride?: VadHandle; sttFactory?: unknown; ttsFactory?: unknown; claudeBridgeFactory?: unknown }`. (The vadOverride field is for testability; the three factory fields are Phase 17 hooks ignored by Plan 04.)

    Export class `Session extends EventEmitter`. Private fields: `private state: AchillesState = "idle"`, `private amplitude: number = 0`, `private ring: Float32Array = new Float32Array(80)`, `private writeIndex: number = 0`, `private ringSnapshot: { ring: Float32Array; writeIndex: number } = { ring: this.ring, writeIndex: 0 }`, `private controller: MockStateController`, `private vad: VadHandle`, `private micSox: MicSoxHandle | undefined`, `private mockStream: { stop: () => void } | undefined`, `private debugVad: boolean`, `private lastFrameTime: number = 0`, `private opts: SessionOptions`. Public getters: `get currentState(): AchillesState { return this.state }`, `get currentAmplitude(): number { return this.amplitude }`, `get currentRingBuffer(): { ring: Float32Array; writeIndex: number } { return this.ringSnapshot }`.

    Constructor: resolve settings = opts.settings ?? loadSettings(); store debugVad = opts.debugVad ?? false; construct controller via createSessionStateController({ broadcast: (s) => { this.state = s; this.emit("state-change", s) }, getMode: () => "toggle" as HotkeyMode }); construct vad = opts.vadOverride ?? createEnergyVad(settings.vad); store opts.

    Method `start(): void`: if (opts.mock) { this.mockStream = createMockAmplitudeStream({ seed: opts.mockSeed ?? 42, onFrame: (amp) => this.handleMockFrame(amp), intervalMs: 20 }) } else { this.micSox = createMicSox({ onFrame: (frame) => this.handlePcmFrame(frame), onExit: (code, stderr) => this.handleSoxExit(code, stderr), ...(opts.spawnImpl !== undefined ? { spawnImpl: opts.spawnImpl } : {}) }) }.

    Private `handleMockFrame(amplitude: number): void`: this.amplitude = amplitude; this.emit("amplitude", amplitude); this.ring[this.writeIndex] = amplitude; this.writeIndex = (this.writeIndex + 1) % 80; this.ringSnapshot = { ring: this.ring, writeIndex: this.writeIndex }; this.emit("rms-sample", amplitude); const event = this.vad.observe(amplitude, 20); if (this.debugVad) process.stderr.write(JSON.stringify({ t: Date.now(), ...this.vad.snapshot() }) + "\n"); if (event === "speech_start") this.controller.dispatch({ type: "HOTKEY_PRESS" }); else if (event === "speech_end") this.controller.dispatch({ type: "STT_COMMITTED", transcript: "" }).

    Private `handlePcmFrame(frame: Int16Array): void`: compute RMS via `let sum = 0; for (let i = 0; i < frame.length; i++) sum += frame[i]! * frame[i]!; const rms = Math.sqrt(sum / frame.length) / 32768` (normalize Int16 to [0,1]). const now = Date.now(); const dt = this.lastFrameTime === 0 ? 20 : now - this.lastFrameTime; this.lastFrameTime = now; this.amplitude = rms; this.emit("amplitude", rms); this.ring[this.writeIndex] = rms; this.writeIndex = (this.writeIndex + 1) % 80; this.ringSnapshot = { ring: this.ring, writeIndex: this.writeIndex }; this.emit("rms-sample", rms); const event = this.vad.observe(rms, dt); if (this.debugVad) process.stderr.write(JSON.stringify({ t: now, ...this.vad.snapshot() }) + "\n"); if (event === "speech_start") this.controller.dispatch({ type: "HOTKEY_PRESS" }); else if (event === "speech_end") this.controller.dispatch({ type: "STT_COMMITTED", transcript: "" }).

    Private `handleSoxExit(code: number | null, stderr: string): void`: if (code !== 0 && code !== null) { let emulator = "unknown"; try { emulator = execSync(`ps -p ${process.ppid} -o comm=`, { encoding: "utf8", timeout: 1000 }).trim() } catch { /* non-fatal */ }; const hint = `achilles: sox exited (code ${code}). On macOS, grant Microphone access to your terminal (parent: ${emulator}). Open System Settings > Privacy & Security > Microphone.\n${stderr}\n`; process.stderr.write(hint); this.controller.dispatch({ type: "INJECT_ERROR", kind: "mic_unavailable" }); this.emit("error-message", hint) }.

    Public `toggleMute(): void`: const newState = this.controller.dispatch({ type: "MUTE_TOGGLE" }); this.vad.setMuted(newState === "muted").

    Public async `stop(): Promise<void>`: await this.micSox?.stop(); this.mockStream?.stop(); this.controller.cancelScheduledTransitions().

    Export `createSession(opts: SessionOptions = {}): Session { return new Session(opts) }` (also exported as the factory shape per the must_haves artifacts entry).

    Export `async function runVoice(argv: string[]): Promise<void>`: use commander Command — `const program = new Command(); program.command("voice").option("--mock").option("--debug-vad").option("--plain").action(async (opts) => { ... })`. INSIDE the action: const usePlain = opts.plain || !process.stdout.isTTY; const session = createSession({ mock: opts.mock, debugVad: opts.debugVad }); session.start(); process.once("SIGINT", async () => { await session.stop(); process.exit(0) }); if (usePlain) { const { startPlainMode } = await import("./ui/plain-text.js"); await startPlainMode({ onStateChange: (cb) => { session.on("state-change", cb); return () => session.off("state-change", cb) }, onTranscriptPartial: (cb) => { session.on("transcript-partial", cb); return () => session.off("transcript-partial", cb) }, getState: () => session.currentState, getTranscript: () => "" }) } else { const { render } = await import("ink"); const { VoiceShell } = await import("./ui/VoiceShell.js"); const { jsx } = await import("react/jsx-runtime"); const element = jsx(VoiceShell, { session, debugVad: opts.debugVad ?? false }); const { waitUntilExit } = render(element); await waitUntilExit() }. Use program.parseAsync(["dummy-node", "dummy-cli", "voice", ...argv]) so commander's argv parsing picks up the subcommand correctly. NOTE: do NOT pass `{ exitOnCtrlC: false }` to render() — preserve default.

    apps/achilles-terminal/tests/session.test.ts: vitest suite covering behavior Tests 1-7, 9, 10. (Test 8 lives in tests/ui/voice-shell.test.tsx since it requires a React mount.) Use vi.fn() for the mock spawnImpl (build a fakeChild EventEmitter as in Plan 01 mic-sox.test.ts). For mock-amplitude tests, use vi.useFakeTimers() and advance the clock by N * 20ms; await microtasks via `vi.runAllTimersAsync()` after advancing. For Test 4 (toggleMute drives VAD), inject a stub vad via opts.vadOverride = { observe: vi.fn(() => null), setMuted: vi.fn(), setSelfTriggerGuard: vi.fn(), reset: vi.fn(), snapshot: vi.fn(() => ({ rms: 0, noiseFloor: 0.005, threshold: 0.015, state: "silence" as const, warmupRemaining: 25 })) }; assert that after toggleMute, vadOverride.setMuted was called with true; after a second toggleMute, with false. For Test 7, fakeChild.stderr.emit("data", Buffer.from("no default device")) then fakeChild.emit("exit", 1, null); assert process.stderr.write was called with a string containing "no default device" AND state is "error". For Test 10, fs.readFileSync the source + assert grep returns no matches.

    CLAUDE.md: NO emojis. NO auto-running achilles voice. LOOP-02 import gate enforced.

    Run `npm test --workspace apps/achilles-terminal -- --pool=forks tests/session.test.ts` to confirm all 10 tests pass.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npm test --workspace apps/achilles-terminal -- --pool=forks tests/session.test.ts 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/achilles-terminal/src/session.ts exports `Session` class, `SessionEvents` interface, `SessionOptions` interface, `createSession` factory, `runVoice` async function
    - apps/achilles-terminal/src/store-stub.ts exports `loadSettings` and `AchillesSettings`; loadSettings() returns a fresh structuredClone on each call (verified by Test 1)
    - apps/achilles-terminal/src/ui/useAchillesState.ts exports `useAchillesState`, `useAmplitude`, `useRingBuffer` — all three are useSyncExternalStore-based React hooks
    - session.ts source contains zero matches for `@achilles/voice-protocol`, `@achilles/voice-stt`, `@achilles/voice-tts`, `@achilles/claude-code-bridge`, `@achilles/achilles-skill` (LOOP-02 verified by Test 10 grep)
    - session.ts handleSoxExit dispatches INJECT_ERROR with kind "mic_unavailable" AND writes stderr hint (verified by Test 7)
    - session.ts SessionOptions includes sttFactory, ttsFactory, claudeBridgeFactory typed as `unknown` (Phase 17 hooks — Plan 04 ignores them)
    - session.ts runVoice() does NOT pass `{ exitOnCtrlC: false }` to render() — verified by `grep -F 'exitOnCtrlC' apps/achilles-terminal/src/session.ts` returning 0 matches
    - All 10 tests in tests/session.test.ts pass under `npm test --workspace apps/achilles-terminal`
    - `npm run typecheck --workspace apps/achilles-terminal` exits 0
    - `grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" apps/achilles-terminal/src/session.ts apps/achilles-terminal/src/ui/useAchillesState.ts apps/achilles-terminal/src/store-stub.ts apps/achilles-terminal/tests/session.test.ts` returns 0 lines (no emojis)
  </acceptance_criteria>
  <done>
    Session composition root ships with state machine + VAD + mic source wiring (both real + mock branches), toggleMute synchronized between state machine and VAD, sox exit error path surfacing to stderr + state, --debug-vad JSON stderr stream, useSyncExternalStore React adapter, settings loader stub. runVoice() entry point ready for cli.ts to dynamically import. Zero voice-package runtime imports.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement VoiceShell.tsx (Ink root component) with 20fps tick, m-key mute, screen-reader branch</name>
  <files>
    apps/achilles-terminal/src/ui/VoiceShell.tsx,
    apps/achilles-terminal/tests/ui/voice-shell.test.tsx
  </files>
  <read_first>
    apps/achilles-terminal/src/session.ts,
    apps/achilles-terminal/src/ui/useAchillesState.ts,
    apps/achilles-terminal/src/ui/Blob.tsx,
    apps/achilles-terminal/src/ui/Sparkline.tsx,
    apps/achilles-terminal/src/ui/StatusRow.tsx,
    apps/achilles-terminal/src/ui/ScreenReader.tsx,
    apps/achilles-terminal/src/ui/colors.ts,
    .planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1 (default tree composition): render(<VoiceShell session={stubSession} debugVad={false} />) with isScreenReaderActive() falsy mounts Blob + Sparkline + StatusRow children; lastFrame() contains block characters AND braille characters AND the state tag
    - Test 2 (screen-reader-mode composition): with process.env.INK_SCREEN_READER stubbed to "true", the same render mounts ScreenReader + StatusRow only (no Blob, no Sparkline); lastFrame() contains "Achilles ready." and NO U+2580-U+259F block characters and NO U+2800-U+28FF braille characters
    - Test 3 (m-key dispatches toggleMute): mount the shell, spy on stubSession.toggleMute, simulate `m` keypress via the render result's `stdin.write` API, assert stubSession.toggleMute was called exactly once
    - Test 4 (m-key with ctrl is ignored per RESEARCH A3): simulate Ctrl-M via stdin.write("" is Ctrl-C — DO NOT use that; use stdin.write(String.fromCharCode(13)) which is Ctrl-M = carriage return) — assert stubSession.toggleMute was NOT called; the input + key.ctrl guard from RESEARCH A3 must be in effect
    - Test 5 (uppercase M is ignored — only lowercase m toggles): simulate "M" keypress — assert toggleMute was NOT called (the literal `input === "m"` check is case-sensitive). This is a strict reading of CONTEXT.md `<decisions>` Mute control row which specifies lowercase m.
    - Test 6 (20fps tick increments amplitude over time): with vi.useFakeTimers(), mount the shell with a stubSession in state "idle" + currentAmplitude=0; advance 600ms; assert the lastFrame() at t=600ms differs from the frame at t=0ms (because idleBreathingAmplitude(600) > idleBreathingAmplitude(0)); specifically assert that the rendered blob characters at t=600ms include at least one non-space character beyond what t=0 had (the breathing amplitude has risen above the lowest non-empty ramp threshold)
    - Test 7 (processing pulse envelope at peak): with stubSession in state "processing" + currentAmplitude=0, advance 100ms; assert lastFrame() contains block character "▒" or denser (intensity from processingPulseAmplitude(100) >= 0.64 maps to ramp index >= 2)
    - Test 8 (StatusRow always mounted): both default and screen-reader modes include the StatusRow output — lastFrame() in both Test 1 and Test 2 contains "[idle]" substring
    - Test 9 (exitOnCtrlC NEVER appears in any Plan 04 source): grep -rF 'exitOnCtrlC' apps/achilles-terminal/src/ apps/achilles-terminal/tests/ returns 0 matches (RESEARCH A3 invariant — verified across the entire workspace)
  </behavior>
  <action>
    Create apps/achilles-terminal/src/ui/VoiceShell.tsx and apps/achilles-terminal/tests/ui/voice-shell.test.tsx. Tests first per TDD.

    apps/achilles-terminal/src/ui/VoiceShell.tsx: Imports: `import { Box, useInput } from "ink"; import { useEffect, useState } from "react"; import { Blob } from "./Blob.js"; import { Sparkline } from "./Sparkline.js"; import { StatusRow } from "./StatusRow.js"; import { ScreenReader } from "./ScreenReader.js"; import { isScreenReaderActive, idleBreathingAmplitude, processingPulseAmplitude } from "./colors.js"; import { useAchillesState, useAmplitude, useRingBuffer } from "./useAchillesState.js"; import type { Session } from "../session.js"`.

    Export the component `VoiceShell({ session, debugVad = false }: { session: Session; debugVad?: boolean })`. The debugVad prop is currently unused inside the component — debug-vad emission lives in session.ts's handleFrame methods per Plan 04 Task 1 — but the prop is kept on the component signature so Phase 17 can wire UI-side debug display if needed. Mark it with an explicit void usage: `void debugVad`.

    Inside the component:
    - `const state = useAchillesState(session)`
    - `const liveAmplitude = useAmplitude(session)`
    - `const { ring, writeIndex } = useRingBuffer(session)`
    - `const [tickMs, setTickMs] = useState(0)`
    - `useEffect(() => { const id = setInterval(() => setTickMs((t) => t + 50), 50); return () => clearInterval(id); }, [])` — single 50ms interval per VoiceShell mount (Pitfall 1 — pre-compute strings outside React per tick is handled by Blob.tsx's blobFrame and Sparkline.tsx's sparklineFromRing; here we just bump tick state once per tick)
    - Compute effective amplitude: `let amp = liveAmplitude; if (state === "idle" || state === "muted") amp = idleBreathingAmplitude(tickMs); else if (state === "processing") amp = processingPulseAmplitude(tickMs); else if (state === "speaking") amp = 0; // Phase 17 wires TTS amplitude`
    - useInput hook per RESEARCH A3 verbatim shape: `useInput((input, key) => { if (input === "m" && !key.ctrl && !key.meta) { session.toggleMute(); } })` — DO NOT add a Ctrl-C handler; Ink default exitOnCtrlC=true handles it. The check is case-sensitive (`input === "m"` not `input.toLowerCase() === "m"`) per CONTEXT.md `<decisions>` row "Key: `m` (lowercase)".
    - Render tree: `const sr = isScreenReaderActive(); return <Box flexDirection="column">{sr ? <ScreenReader state={state} /> : <><Blob amplitude={amp} /><Sparkline ring={ring} writeIndex={writeIndex} /></>}<StatusRow state={state} transcript="" transcriptsActive={false} /></Box>`. Phase 16: transcript is always "" because no STT is wired (Phase 17 wires the real transcript-partial events stream); transcriptsActive is always false (Phase 18 wires the --save-transcripts flag).

    Per CONTEXT.md `<decisions>` Accessibility row "suppress Blob and Sparkline entirely" — the conditional render does exactly that; StatusRow stays mounted because it is helpful for sighted co-watchers and CONTEXT.md does not explicitly suppress it.

    apps/achilles-terminal/tests/ui/voice-shell.test.tsx: vitest + ink-testing-library tests for Tests 1-9. Build a stubSession helper `function createStubSession(initial: { state: AchillesState; amplitude: number }) { ... }` that returns an EventEmitter-like object with `currentState`, `currentAmplitude`, `currentRingBuffer`, `on`, `off`, `toggleMute` (as vi.fn()). For Test 1, mount with `process.env.INK_SCREEN_READER` unset (vi.stubEnv("INK_SCREEN_READER", "")); assert lastFrame contains a block character from U+2580-U+259F set AND a braille character from U+2800-U+28FF set. For Test 2, stub env to "true" before mount and assert no chars from those ranges. For Test 3, use the render result's `stdin.write("m")`; await a microtask via `await Promise.resolve()` or `await new Promise(r => setImmediate(r))` so React processes the input event; assert toggleMute was called. For Test 4 simulate carriage-return via `stdin.write("\r")` and assert toggleMute NOT called (key.ctrl will be true for control chars OR — more concretely — ink only emits the m-keypress when input === literal "m"; the literal char received from "\r" is not "m"). For Test 5 stdin.write("M") and assert toggleMute NOT called. For Tests 6+7, use vi.useFakeTimers() + vi.advanceTimersByTime(600 or 100); the lastFrame() string comparison reveals the change. For Test 9, fs.readFileSync the src/ tree + tests/ tree recursively (use child_process.execSync of a recursive grep) and assert "exitOnCtrlC" substring count === 0.

    CLAUDE.md: NO emojis. LOOP-02: no voice-* imports.

    Run `npm test --workspace apps/achilles-terminal -- --pool=forks tests/ui/voice-shell.test.tsx` to confirm all 9 tests pass.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npm test --workspace apps/achilles-terminal -- --pool=forks tests/ui/voice-shell.test.tsx 2>&amp;1 | tail -30 &amp;&amp; (grep -rF 'exitOnCtrlC' apps/achilles-terminal/src/ apps/achilles-terminal/tests/ 2>/dev/null | grep -v '^Binary file' || echo "RESEARCH A3 invariant OK (no exitOnCtrlC matches anywhere)")</automated>
  </verify>
  <acceptance_criteria>
    - apps/achilles-terminal/src/ui/VoiceShell.tsx exports `VoiceShell` component
    - VoiceShell.tsx source contains the literal substring `!key.ctrl` AND the literal substring `!key.meta` AND the literal substring `input === "m"` (RESEARCH A3 verbatim signature)
    - VoiceShell.tsx contains exactly one setInterval call (grep -c 'setInterval' apps/achilles-terminal/src/ui/VoiceShell.tsx === 1)
    - VoiceShell.tsx branches on isScreenReaderActive() — sr=true mounts ScreenReader only; sr=false mounts Blob + Sparkline
    - Composition tree always includes StatusRow regardless of screen-reader mode
    - VoiceShell.tsx applies envelope helpers idleBreathingAmplitude(tickMs) for state="idle" OR state="muted" AND processingPulseAmplitude(tickMs) for state="processing"
    - VoiceShell.tsx source does NOT contain the literal string "exitOnCtrlC" (RESEARCH A3 invariant)
    - Plan 04 workspace-wide invariant: `grep -rF 'exitOnCtrlC' apps/achilles-terminal/src/ apps/achilles-terminal/tests/` returns 0 lines (no source file passes the disabled option)
    - All 9 tests in tests/ui/voice-shell.test.tsx pass
    - `npm run typecheck --workspace apps/achilles-terminal` exits 0
    - `grep -E "voice-protocol|voice-stt|voice-tts|claude-code-bridge|achilles-skill" apps/achilles-terminal/src/ui/VoiceShell.tsx apps/achilles-terminal/tests/ui/voice-shell.test.tsx` returns 0 lines (LOOP-02)
    - `grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" apps/achilles-terminal/src/ui/VoiceShell.tsx apps/achilles-terminal/tests/ui/voice-shell.test.tsx` returns 0 lines (no emojis)
  </acceptance_criteria>
  <done>
    VoiceShell.tsx ships the Ink root component with 20fps tick + RESEARCH A3-compliant m-key handling (no Ctrl-C / Meta-M / uppercase-M false-fires) + screen-reader-mode branching + idle/processing envelope wiring + StatusRow always-mounted. The RESEARCH A3 invariant "no exitOnCtrlC" is locked workspace-wide. CAP-03 (UI keypress wiring) complete.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend cli.ts with `voice` subcommand + --mock / --debug-vad / --plain flags + integration tests</name>
  <files>
    apps/achilles-terminal/src/cli.ts,
    apps/achilles-terminal/tests/cli.test.ts
  </files>
  <read_first>
    apps/achilles-terminal/src/cli.ts,
    apps/achilles-terminal/tests/cli.test.ts,
    apps/achilles-terminal/src/session.ts,
    apps/achilles-terminal/src/ui/plain-text.ts,
    .planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1 (Phase 15 regression — --version unchanged): spawning cli.ts with --version still exits 0 and prints the package.json version string (existing Phase 15 assertion preserved)
    - Test 2 (Phase 15 regression — -v unchanged): same for the short form
    - Test 3 (Phase 15 regression — argv-first INIT-07): --version with env.ELEVENLABS_API_KEY explicitly deleted still exits 0
    - Test 4 (Phase 15 regression — unknown command exits 1): spawning cli.ts with `["other-bogus"]` (NOT "voice") still exits 1 with stderr matching /achilles: unknown command/
    - Test 5 (Phase 15 regression — shebang line preserved): src/cli.ts first line is still "#!/usr/bin/env node"
    - Test 6 (NEW — `voice --plain --mock` exits cleanly on SIGINT in plain mode): spawn cli.ts with `["voice", "--plain", "--mock"]`; after 300ms send SIGINT; child exits within 1500ms with exit code 0 (or 130 — both acceptable on POSIX); stdout contains at least one line matching the regex /^\[\d{4}-\d{2}-\d{2}T/ (ISO timestamp prefix from plain-text formatter)
    - Test 7 (NEW — `voice --mock --debug-vad --plain` emits debug-vad JSON to stderr): spawn cli.ts with all three flags; after 300ms send SIGINT; child stderr contains at least one line matching the regex /^\{"t":\d+,"energy":/ (the locked --debug-vad JSON line shape from CONTEXT.md `<specifics>` row 4)
    - Test 8 (NEW — INIT-07 source-budget preserved): readFileSync(cli.ts).match for top-level imports — the only `^import` lines must be the Phase 15 set: node:fs/promises, node:url, node:path. The new dynamic `await import("./session.js")` MUST be inside a function body, not at module top level. Verified by parsing the source: collect all lines matching `/^import /` (after the shebang), assert each line's specifier is in the allowed set { "node:fs/promises", "node:url", "node:path" }.
    - Test 9 (NEW — voice branch dispatches to session.runVoice): spawn cli.ts with `["voice", "--plain", "--mock"]` and immediately send SIGINT (no delay); child exits without hanging (the dynamic import + runVoice + plain mode + SIGINT handler chain works end-to-end). This is a smoke test that the wiring exists.
  </behavior>
  <action>
    Edit apps/achilles-terminal/src/cli.ts in place and extend apps/achilles-terminal/tests/cli.test.ts.

    apps/achilles-terminal/src/cli.ts edit shape: PRESERVE all existing structure verbatim — the shebang, the existing top-level static imports (node:fs/promises, node:url, node:path), the HERE constant, the existing `process.on("uncaughtException")` and `process.on("unhandledRejection")` registrations, the `main().catch(...)` invocation. INSIDE main(): preserve the existing --version/-v/-latency-probe branches at the top exactly as written. REPLACE the existing `if (argv[0] === "voice") { ... stub ... }` block with the dynamic-import gate per the Phase 16 plan:
    - `if (argv[0] === "voice") { const { runVoice } = await import("./session.js"); await runVoice(argv.slice(1)); return; }`
    This is the ONLY new logic added to cli.ts. The dynamic import ensures session.ts (which transitively imports ink, react, chalk, mic-sox, vad-energy, etc.) is NEVER loaded when the user runs --version — preserving INIT-07. KEEP the existing fallthrough that writes "achilles: unknown command. Try --version." to stderr and exits 1.

    Update the JSDoc comment block at the top of cli.ts to reflect Phase 16's addition: add a sentence (preserving the existing INIT-07 / Pitfall 5 / Pitfall 1 commentary) — "Phase 16 extends the `voice` branch from a stub-write-to-stderr-and-exit to a dynamic import of session.ts's runVoice(); the --version / -v / --latency-probe argv-first branches stay at the top of main() so INIT-07 is structurally preserved — Ink, React, chalk, sox, and VAD never load when the user runs --version."

    CRITICAL: keep the cli.ts top-level static-import budget at exactly { node:fs/promises, node:url, node:path }. Do NOT add a static import for commander, session, ink, etc. — all those load dynamically via the await import("./session.js") branch. The runVoice function in session.ts owns commander parsing.

    NO emojis. LOOP-02: cli.ts adds NO voice-* imports.

    apps/achilles-terminal/tests/cli.test.ts: EXTEND the existing 5 Phase 15 tests with the 4 new Plan 04 tests (Tests 6, 7, 8, 9 — Tests 1-5 are the existing Phase 15 set). KEEP all 5 existing tests VERBATIM. For Tests 6 + 7 + 9 (integration tests), use childProcess.spawn (NOT spawnSync) with explicit SIGINT timing via setTimeout. The pattern:
    - `import { spawn } from "node:child_process"`
    - `const child = spawn(process.execPath, ["--import", "tsx", CLI_SRC, "voice", "--plain", "--mock"], { encoding: "utf8", stdio: ["pipe", "pipe", "pipe"] })`
    - `let stdout = ""; let stderr = ""; child.stdout.on("data", (d) => stdout += d.toString()); child.stderr.on("data", (d) => stderr += d.toString())`
    - `setTimeout(() => child.kill("SIGINT"), 300)`
    - `await new Promise<void>((resolve, reject) => { child.on("exit", () => resolve()); setTimeout(() => { child.kill("SIGKILL"); reject(new Error("integration test timeout")) }, 5000) })`
    - assert exit code, stdout regex, stderr regex per test

    For Test 8 (source budget), use `readFileSync(CLI_SRC, "utf8").split("\n").filter(line => /^import /.test(line))` to extract the top-level import lines; verify each line's specifier (extracted via regex /from\s+["']([^"']+)["']/) is one of the three allowed values.

    For Test 9 (the smoke-without-delay), the same spawn pattern as Test 6 but send SIGINT immediately (within 50ms of process start) — the child should still exit cleanly because the SIGINT handler is registered synchronously inside runVoice() (or as close to synchronously as the dynamic import allows — under tsx + Bun the dynamic import is fast enough that the SIGINT handler is registered well within the 50ms delay).

    CLAUDE.md: NO auto-running of achilles voice OUTSIDE of vitest. The integration tests spawn the child process under vitest's control — that is the allowed pattern per CLAUDE.md global rule "Vitest is OK; do NOT launch `achilles voice` to verify visually."

    Run `npm test --workspace apps/achilles-terminal -- --pool=forks tests/cli.test.ts` to confirm all 9 tests pass (5 from Phase 15 + 4 new from Plan 04). Note that integration tests are slow (~500ms each x 3 = ~1.5s additional) so the total test runtime grows but stays well under the vitest pool timeout.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npm test --workspace apps/achilles-terminal -- --pool=forks tests/cli.test.ts 2>&amp;1 | tail -40 &amp;&amp; node -e "const src = require('fs').readFileSync('apps/achilles-terminal/src/cli.ts', 'utf8'); const topImports = src.split('\n').filter(l => /^import /.test(l)); const allowed = ['node:fs/promises','node:url','node:path']; for (const line of topImports) { const ok = allowed.some(a => line.includes('\"' + a + '\"') || line.includes(\"'\" + a + \"'\")); if (!ok) throw new Error('INIT-07 regression: unexpected top-level import: ' + line); } console.log('INIT-07 top-level static import budget OK (' + topImports.length + ' imports, all in allowed set)');"</automated>
  </verify>
  <acceptance_criteria>
    - apps/achilles-terminal/src/cli.ts contains a dynamic-import gate: `if (argv[0] === "voice") { const { runVoice } = await import("./session.js"); await runVoice(argv.slice(1)); return; }` (or equivalent — the literal substring `await import("./session.js")` MUST appear inside main())
    - apps/achilles-terminal/src/cli.ts top-level static imports are EXACTLY { node:fs/promises, node:url, node:path } — no additions, no removals (INIT-07 invariant preserved)
    - apps/achilles-terminal/src/cli.ts first line is STILL "#!/usr/bin/env node" (Phase 15 invariant preserved)
    - apps/achilles-terminal/src/cli.ts still registers process.on("uncaughtException", ...) and process.on("unhandledRejection", ...) before main() invocation (Phase 15 silent-launch defence preserved)
    - cli.ts removes the Phase 15 stub message "TUI not implemented in Phase 15" — verified by grep returning 0 matches
    - All 9 tests in tests/cli.test.ts pass under `npm test --workspace apps/achilles-terminal` (5 preserved Phase 15 + 4 new Plan 04)
    - `npm run typecheck --workspace apps/achilles-terminal` exits 0
    - `npm run lint --workspace apps/achilles-terminal -- --max-warnings 0` exits 0
    - `grep -E "voice-protocol|voice-stt|voice-tts|claude-code-bridge|achilles-skill" apps/achilles-terminal/src/cli.ts apps/achilles-terminal/tests/cli.test.ts` returns 0 lines (LOOP-02)
    - `grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" apps/achilles-terminal/src/cli.ts apps/achilles-terminal/tests/cli.test.ts` returns 0 lines (no emojis)
  </acceptance_criteria>
  <done>
    cli.ts extends Phase 15 with the voice subcommand dynamic-import gate. INIT-07 invariant locked structurally — the only static top-level imports remain node:fs/promises, node:url, node:path. All Phase 15 cli.test.ts assertions still pass; 4 new Plan 04 integration tests verify the SIGINT exit + --plain + --debug-vad end-to-end paths. TUI-06 plain-text routing decision wired into runVoice. Phase 16 deliverable complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv (user shell -> CLI process) | Untrusted command-line input enters cli.ts; commander shape-validates the `voice` subcommand flags |
| dynamic import path (cli.ts -> ./session.js) | Trusted in production (relative module specifier); cannot be tampered without modifying the binary on disk |
| sox child stdout (sox -> session.ts) | Untrusted binary output; bounded by Plan 01 mic-sox.ts frame-extraction contract; backpressure via silent frame drop |
| Ink render tree (component tree -> stdout TTY) | Ink owns raw-mode toggling, ANSI escape coalescing, SIGWINCH handling, Ctrl-C exit — Phase 16 does not bypass Ink |
| SIGINT (terminal -> process) | Phase 16 minimum handler kills sox + lets Ink unmount via its default handler; Phase 17 will wrap with gracefulShutdown chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-argv-injection | Tampering | apps/achilles-terminal/src/cli.ts argv parse | mitigate | commander 13.x shape-validates the `voice` subcommand and its three flags (--mock, --debug-vad, --plain). Unknown flags fail the parse loudly (Test 6 in Task 3 acceptance criteria refers to commander's "unknown option" error). The argv-first branches for --version / -v / --latency-probe execute BEFORE any dynamic import — argv-injection in those branches is bounded to package.json read or hrtime probe, neither of which has side effects beyond stdout write. |
| T-16-spawn-args | Tampering | apps/achilles-terminal/src/audio/mic-sox.ts argv via session.ts createMicSox call | mitigate (inherited from Plan 01) | The sox argv is HARD-CODED in mic-sox.ts — no user input flows into it. session.ts passes only the spawnImpl DI seam from SessionOptions which defaults to the real node:child_process.spawn. Verified by Plan 01 Test 1 + Test 2 (exact argv assertion). |
| T-16-silent-exit | Information disclosure (absence of) | session.ts handleSoxExit + cli.ts uncaughtException/unhandledRejection handlers | mitigate | sox child uses `stdio: ["ignore", "pipe", "pipe"]` (Plan 01). On non-zero exit, session.ts writes the captured stderr + parent-emulator hint to process.stderr AND dispatches INJECT_ERROR to the state machine. The Phase 15 uncaughtException/unhandledRejection handlers remain registered in cli.ts. Verified by Task 1 Test 7 and the preserved Phase 15 cli.test.ts assertions (Task 3 Tests 1-5). |
| T-16-debug-vad-disclosure | Information disclosure | session.ts --debug-vad stderr stream | accept | --debug-vad emits RMS + noiseFloor + threshold scalars to stderr. These are amplitude measurements over time — they contain NO semantic information about user speech (no transcripts, no audio samples). Documenting this in `--help` text (the per-flag commander description) is the mitigation; explicit user opt-in via the flag is the access control. Low-value target. |
| T-16-loop-02-runtime-import | Tampering (architectural boundary) | apps/achilles-terminal/src/session.ts + src/cli.ts | mitigate | Plan 04 acceptance criteria assert grep -E '@achilles/voice-*|@achilles/claude-code-bridge|@achilles/achilles-skill' returns 0 lines across all 8 new/edited files. This is the structural enforcement of LOOP-02 — the four voice packages stay byte-for-byte unchanged across Phase 16 because Phase 16 has zero runtime touch points. Phase 17 will add the runtime touch points via the sttFactory/ttsFactory/claudeBridgeFactory hooks Plan 04 left as opaque `unknown`-typed optional parameters. |
</threat_model>

<verification>
After all 3 tasks complete:
- `npm test --workspace apps/achilles-terminal -- --pool=forks` exits 0 with ALL prior tests passing PLUS the new tests:
  - 5 Phase 15 cli.test.ts assertions (preserved verbatim by Task 3)
  - 27 Plan 01 audio tests (mic-sox + vad-energy + braille)
  - 15 Plan 02 state tests
  - 23 Plan 03 UI tests (blob + sparkline + status-row + screen-reader + plain-text + mock-amplitude)
  - 10 Plan 04 session.test.ts tests (Task 1)
  - 9 Plan 04 voice-shell.test.tsx tests (Task 2)
  - 4 Plan 04 NEW cli.test.ts tests (Task 3) = total 9 cli.test.ts tests with the Phase 15 5
  Total Phase 16: 93+ tests
- `npm run typecheck --workspace apps/achilles-terminal` exits 0
- `npm run lint --workspace apps/achilles-terminal -- --max-warnings 0` exits 0
- `grep -rE "voice-protocol|voice-stt|voice-tts|claude-code-bridge|companion.md" apps/achilles-terminal/src/ apps/achilles-terminal/tests/` returns 0 lines (LOOP-02 enforced across the entire workspace's Phase 16 deliverables)
- `grep -rF 'exitOnCtrlC' apps/achilles-terminal/src/ apps/achilles-terminal/tests/` returns 0 lines (RESEARCH A3 invariant — no Plan 04 file passes the disabled option, no Plan 03 file does either)
- `grep -rP "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" apps/achilles-terminal/src/ apps/achilles-terminal/tests/` returns 0 lines (no emojis)
- apps/achilles-terminal/src/cli.ts top-level static imports are EXACTLY { node:fs/promises, node:url, node:path } (INIT-07 invariant — verified by the node -e script in Task 3 verify block)
- `git diff apps/achilles/ packages/voice-protocol/ packages/voice-stt/ packages/voice-tts/ packages/claude-code-bridge/ packages/achilles-skill/skill/prompts/companion.md` returns no output (LOOP-02 — Phase 16 leaves all six locked surfaces byte-for-byte unchanged)
</verification>

<success_criteria>
CAP-03 (full wiring): m-key in VoiceShell.tsx dispatches session.toggleMute() which dispatches MUTE_TOGGLE to the state machine which transitions to "muted" which calls vad.setMuted(true) which renders the MUTED tag in StatusRow. Verified by Task 1 Tests 3-4, Task 2 Tests 3-5, transitively by Plans 02-03 acceptance criteria.
TUI-06 (full wiring): runVoice() in session.ts checks `!process.stdout.isTTY || args.plain` and routes to startPlainMode (NEVER mounts Ink in plain mode per Pitfall 4). Verified by Task 3 Test 6 (integration: `voice --plain --mock` emits ISO-prefixed log lines).
TUI-01..04 (transitively wired): VoiceShell.tsx mounts Blob + Sparkline + StatusRow + ScreenReader from Plan 03; the 20fps tick + idle/processing envelopes drive Blob amplitude; live ring buffer drives Sparkline. Verified by Task 2 Tests 1, 6, 7, 8.
ACC-01 (transitively wired): chalk under colors.ts in Plan 03 honors NO_COLOR + FORCE_COLOR natively; Plan 04 mounts those colors via StatusRow.
ACC-02 (transitively wired): isScreenReaderActive() from Plan 03 (using `=== "true"` per A1) gates the screen-reader branch in VoiceShell.tsx; ScreenReader uses aria-label + aria-role per A2 (NOT aria-live). Verified by Task 2 Test 2.
CAP-01 (transitively wired): createMicSox from Plan 01 is invoked in session.start() with mock=false; exit codes surface to stderr + state machine.
CAP-02 + CAP-04 (transitively wired): createEnergyVad from Plan 01 is constructed with loadSettings() VAD defaults; --debug-vad stream emits JSON lines to stderr per CONTEXT.md `<specifics>` row 4. Verified by Task 1 Test 9.
INIT-07 preserved: cli.ts top-level static imports limited to { node:fs/promises, node:url, node:path }; dynamic import gates the voice subcommand path. Verified by Task 3 Test 8.
RESEARCH Assumptions A1, A2, A3 honored: `=== "true"` in colors.ts (Plan 03), aria-label + aria-role in ScreenReader.tsx (Plan 03), full useInput callback signature with key.ctrl + key.meta guards in VoiceShell.tsx (Plan 04), no `exitOnCtrlC: false` anywhere in the workspace (Plan 03 + Plan 04).
LOOP-02 invariant: zero voice-package runtime imports in any Phase 16 file; v1.2 source files (apps/achilles/, packages/voice-*, packages/claude-code-bridge/, companion.md) are byte-for-byte unchanged.
</success_criteria>

<output>
Create `.planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-04-SUMMARY.md` summarizing: 8 files touched (5 source + 3 test + 1 cli.ts extension), full Phase 16 test count (~93 tests), the three RESEARCH.md Assumption Log corrections honored (A1 string "true", A2 aria-label+aria-role NOT aria-live, A3 full useInput signature + no exitOnCtrlC), the INIT-07 invariant preserved (cli.ts static imports limited to 3 node: modules), and any deviations from the plan encountered. Cross-reference SUMMARYs 16-01, 16-02, 16-03 for the transitively wired requirement IDs.
</output>
</content>
</invoke>