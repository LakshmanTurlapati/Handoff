---
phase: 16-tui-shell-state-machine-sox-mic-capture-energy-vad
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
files_modified:
  - apps/achilles-terminal/package.json
  - apps/achilles-terminal/tsconfig.json
  - apps/achilles-terminal/eslint.config.js
  - apps/achilles-terminal/vitest.config.ts
  - apps/achilles-terminal/src/ui/colors.ts
  - apps/achilles-terminal/src/ui/Blob.tsx
  - apps/achilles-terminal/src/ui/Sparkline.tsx
  - apps/achilles-terminal/src/ui/StatusRow.tsx
  - apps/achilles-terminal/src/ui/ScreenReader.tsx
  - apps/achilles-terminal/src/ui/plain-text.ts
  - apps/achilles-terminal/src/ui/mock-amplitude.ts
  - apps/achilles-terminal/tests/ui/blob.test.tsx
  - apps/achilles-terminal/tests/ui/sparkline.test.tsx
  - apps/achilles-terminal/tests/ui/status-row.test.tsx
  - apps/achilles-terminal/tests/ui/screen-reader.test.tsx
  - apps/achilles-terminal/tests/ui/plain-text.test.ts
  - apps/achilles-terminal/tests/ui/mock-amplitude.test.ts
autonomous: true
requirements:
  - TUI-01
  - TUI-02
  - TUI-03
  - TUI-04
  - TUI-05
  - TUI-06
  - ACC-01
  - ACC-02

must_haves:
  truths:
    - "Blob.tsx renders a 7-row, 7-column grid of Unicode block-shade characters (U+0020 / U+2591 / U+2592 / U+2593 / U+2588 — 5-step ramp per RESEARCH.md §'Unicode block ramp for the blob') driven by a center-weighted intensity kernel (center 1.0, ring 1 0.75, ring 2 0.5, ring 3 0.25)"
    - "Sparkline.tsx renders a 40-character braille string by consuming a Float32Array(80) ring + writeIndex via sparklineFromRing() from Plan 01"
    - "StatusRow.tsx renders `[state] <last 60 chars of transcript>` with appended REC tag when transcriptsActive=true AND appended MUTED tag when state === 'muted'; state name is colorized via the locked STATE_COLORS palette"
    - "ScreenReader.tsx renders ONLY a single <Text aria-label={SCREEN_READER_WORDING[state]} aria-role='status'> per state — Ink 7 does NOT support aria-live (RESEARCH.md Assumption A2); the screen reader announces via state-change-driven re-renders with a 200ms debounce per CONTEXT.md <specifics> row 3"
    - "colors.ts isScreenReaderActive() returns true ONLY when process.env.INK_SCREEN_READER === 'true' (the string 'true', NOT '1' — RESEARCH.md Assumption A1)"
    - "plain-text.ts emits ISO-timestamped log lines `[ISO][state] partial-transcript` with no ANSI escapes; this is the !isTTY OR --plain fallback (TUI-06)"
    - "mock-amplitude.ts emits a deterministic seeded 1.5s speech-like ramp + 1.5s silence loop at 20ms cadence for the `--mock` runtime path (Plan 04 wires it into session.ts)"
    - "TUI-05 (CPU < 10% over 10-minute idle animation on Windows Terminal v1.18 / iTerm2 / Ghostty / Terminal.app) is a MANUAL-ONLY verification per 16-VALIDATION.md — Plan 03 ships the components whose perf budget TUI-05 measures (Blob + Sparkline + setInterval(50) tick architecture); Pitfall 1 defence (pre-compute blob + sparkline strings outside React via blobFrame and sparklineFromRing pure helpers) is the structural mitigation; the actual CPU measurement is captured by an operator running achilles voice --mock for 10 minutes per terminal emulator and is deferred to the Phase 20 RBS asciicasts per CONTEXT.md <deferred>"
  artifacts:
    - path: "apps/achilles-terminal/src/ui/colors.ts"
      provides: "STATE_COLORS palette, SCREEN_READER_WORDING table, colorize() helper, isScreenReaderActive() resolver"
      exports: ["STATE_COLORS", "SCREEN_READER_WORDING", "colorize", "isScreenReaderActive"]
    - path: "apps/achilles-terminal/src/ui/Blob.tsx"
      provides: "7x7 Unicode block-shade blob component + blobFrame() pure helper for pre-computed render strings (Pitfall 1 defence)"
      exports: ["Blob", "blobFrame", "rampChar", "RAMP"]
    - path: "apps/achilles-terminal/src/ui/Sparkline.tsx"
      provides: "40-cell braille sparkline component reading Float32Array(80) + writeIndex props"
      exports: ["Sparkline"]
    - path: "apps/achilles-terminal/src/ui/StatusRow.tsx"
      provides: "Single-line status renderer: [state] transcript + REC + MUTED tags"
      exports: ["StatusRow"]
    - path: "apps/achilles-terminal/src/ui/ScreenReader.tsx"
      provides: "Screen-reader-only state announcer using aria-label + aria-role (NOT aria-live — Ink 7 unsupported)"
      exports: ["ScreenReader"]
    - path: "apps/achilles-terminal/src/ui/plain-text.ts"
      provides: "Non-TTY / --plain log-line emitter (no Ink, no ANSI)"
      exports: ["formatPlainLine", "startPlainMode"]
    - path: "apps/achilles-terminal/src/ui/mock-amplitude.ts"
      provides: "Deterministic seeded mock amplitude generator for `--mock` flag"
      exports: ["createMockAmplitudeStream"]
  key_links:
    - from: "apps/achilles-terminal/src/ui/Sparkline.tsx"
      to: "apps/achilles-terminal/src/audio/braille.ts (Plan 01)"
      via: "import { sparklineFromRing } from \"../audio/braille.js\""
      pattern: "sparklineFromRing"
    - from: "apps/achilles-terminal/src/ui/Blob.tsx + StatusRow.tsx + ScreenReader.tsx"
      to: "apps/achilles-terminal/src/state/constants.ts (Plan 02)"
      via: "import type { AchillesState } from \"../state/constants.js\""
      pattern: "AchillesState"
    - from: "apps/achilles-terminal/src/ui/colors.ts"
      to: "chalk@5.6.x (NO_COLOR-aware natively)"
      via: "import chalk from \"chalk\""
      pattern: "from \"chalk\""
---

<objective>
Build the seven UI-tier primitives Phase 16 needs in isolation from the composition root: colors / palette / screen-reader detection (`colors.ts`), the 7×7 reactive Blob component (`Blob.tsx`), the 40-cell braille Sparkline component (`Sparkline.tsx`), the status row renderer (`StatusRow.tsx`), the screen-reader-only announcer (`ScreenReader.tsx`), the non-TTY/--plain log emitter (`plain-text.ts`), and the deterministic seeded mock amplitude stream for the `--mock` flag (`mock-amplitude.ts`). Plus the Wave 0 substrate to make `.tsx` files compile + lint + test: extend `package.json` (add ink, react, @types/react, ink-testing-library), `tsconfig.json` (broaden include glob to cover `**/*.tsx`), `eslint.config.js` (broaden files glob to cover `.tsx`), and `vitest.config.ts` (broaden include glob to cover `tests/**/*.test.tsx`).

Purpose: Satisfies TUI-01 (7×7 blob), TUI-02 (40-cell braille sparkline rendering — consumes Plan 01's braille.ts), TUI-03 (5-state colors + idle breathing + processing pulse envelopes — locked in colors.ts and consumed by Blob.tsx envelopes), TUI-04 (status row with state + transcript + REC + MUTED tags), TUI-06 (plain-text fallback), ACC-01 (NO_COLOR + FORCE_COLOR honored via chalk's native behavior), ACC-02 (screen-reader mode suppresses Blob + Sparkline + emits state announcements via aria-label per RESEARCH.md A2 correction). Reflects the three Ink 7 API corrections from RESEARCH.md Assumption Log (A1 `=== "true"`, A2 `aria-label` + `aria-role`, A3 the useInput callback signature lives in Plan 04 not here).

Output: 11 new files under `apps/achilles-terminal/src/ui/` and `apps/achilles-terminal/tests/ui/` PLUS 4 Wave 0 config extensions. Does NOT mount Ink at the binary level — that wiring is Plan 04. This plan delivers the COMPONENTS in isolation, each unit-testable via `ink-testing-library` for the `.tsx` components and plain vitest for the `.ts` modules.

LOOP-02 invariant: zero runtime imports from any of the four voice packages or `achilles-skill`. The state machine import from Plan 02 is type-only (`import type { AchillesState }`) where possible to keep the runtime surface minimal. The audio import from Plan 01 (`braille.ts` for Sparkline.tsx) is runtime — it is a pure-function helper, not voice-package code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-CONTEXT.md
@.planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-RESEARCH.md
@.planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-VALIDATION.md
@.planning/phases/15-workspace-scaffold-bun-build-pipeline/15-DEVIATIONS.md
@.planning/research/PITFALLS.md
@apps/achilles-terminal/package.json
@apps/achilles-terminal/tsconfig.json
@apps/achilles-terminal/eslint.config.js
@apps/achilles-terminal/vitest.config.ts
@apps/achilles-terminal/src/audio/braille.ts
@apps/achilles-terminal/src/state/constants.ts

<interfaces>
<!-- Key contracts from RESEARCH.md code examples + Plan 01/02 outputs. -->

From 16-RESEARCH.md §"Standard Stack" + §"Installation":
- Production runtime deps to add: `ink@^7.0.5`, `react@^19.2.7`
- Dev deps to add: `@types/react@^19.2.17`, `ink-testing-library@^4.0.0`
- chalk is NOT in the current achilles-terminal/package.json — Plan 03 adds it: `chalk@^5.6.2` (Standard Stack core table). This is a NEW prod dep; chalk 5 is ESM-only matching the workspace's "type": "module" lock.
- All five packages are HIGH-confidence per the Package Legitimacy Audit (slopcheck OK). No threat_model checkpoint needed for these specific installs.

From 16-RESEARCH.md §"Recommended Project Structure" lines 261-295:
- ui/Blob.tsx — 7x7 grid component
- ui/Sparkline.tsx — 40-cell braille component
- ui/StatusRow.tsx — state + transcript + REC + MUTED
- ui/screen-reader.tsx — Phase 16's RESEARCH naming uses lowercase "screen-reader.tsx"; Plan 03 uses `ScreenReader.tsx` (PascalCase) for component-file consistency with Blob/Sparkline/StatusRow — minor naming improvement, no semantic change
- ui/colors.ts — palette + NO_COLOR/FORCE_COLOR + INK_SCREEN_READER detection
- ui/plain-text.ts — !isTTY/--plain fallback emitter
- ui/mock-amplitude.ts — synthetic stream for --mock
- src/ui/useAchillesState.ts is in the RESEARCH file list but is part of Plan 04's composition root (the useSyncExternalStore adapter wires the session EventEmitter into React; it is component-glue, not a primitive). Plan 03 does NOT create useAchillesState.ts.

From 16-RESEARCH.md §"NO_COLOR / FORCE_COLOR / INK_SCREEN_READER precedence" lines 793-852 — verbatim shape for colors.ts:
- STATE_COLORS: idle="gray", listening="green", processing="yellow", speaking="blue", error="red", muted="redBright"
- SCREEN_READER_WORDING: per-state explicit wording per CONTEXT.md `<decisions>` Accessibility row
- colorize(state, text): `(chalk as any)[STATE_COLORS[state]](text)` — chalk auto-no-ops on NO_COLOR
- isScreenReaderActive(): returns `process.env["INK_SCREEN_READER"] === "true"` (NOTE: the string "true" — RESEARCH Assumption A1 corrects CONTEXT.md's "1")

From 16-RESEARCH.md §"Unicode block ramp for the blob" lines 624-672 — verbatim shape for Blob.tsx:
- RAMP = [" ", "░", "▒", "▓", "█"] (codepoints U+0020, U+2591, U+2592, U+2593, U+2588)
- rampChar(intensity): `RAMP[Math.min(4, Math.max(0, Math.round(intensity * 4)))]`
- blobFrame(amplitude: number): readonly string[] — 7 rows, center-weighted ring kernel (center 1.0, ring 1 0.75, ring 2 0.5, ring 3 0.25); ring = Math.min(3, Math.floor(Math.hypot(x - 3, y - 3)))
- blobFrame is a PURE function; Blob.tsx component just receives `amplitude` prop and calls `blobFrame(amplitude)` inside the render. Per Pitfall 1 defence: the pre-built string array is the memoized output, but since blobFrame is fast and deterministic, the React tree can call it inline per render — the bottleneck is Ink reconciler tree depth, not the math.

From CONTEXT.md `<decisions>` Visual surface block:
- Idle breathing: amplitude = 0.3 + 0.1·sin(t/600)
- Processing pulse: amplitude = 0.5 + 0.3·sin(t/200)
- Listening: live mic RMS (clamped 0-1) — driven by orchestrator in Plan 04
- Speaking: live TTS amplitude — Phase 16 stub returns 0; Phase 17 wires real TTS amplitude
- Render rate: 20fps (50ms setInterval driving `tick` state — Plan 04's VoiceShell.tsx owns the setInterval; Plan 03's Blob.tsx accepts amplitude as a prop and renders synchronously)

The envelope helpers (idle breathing curve, processing pulse curve) live in colors.ts as exported functions so Plan 04's composition root can compose `amplitude = envelopeFor(state, tick)` cleanly. Plan 03 exports:
- `idleBreathingAmplitude(tickMs: number): number = 0.3 + 0.1 * Math.sin(tickMs / 600)`
- `processingPulseAmplitude(tickMs: number): number = 0.5 + 0.3 * Math.sin(tickMs / 200)`
Both are pure helpers used by Plan 04's session.ts to feed the Blob's amplitude prop per tick.

From CONTEXT.md `<decisions>` Mute control row + StatusRow:
- StatusRow format: `[state] <transcript>` where state is the colorized state name in brackets
- Append " [REC]" tag (colorized red — the literal text "REC" in chalk.red bracketed form) when transcriptsActive prop is true (Phase 16 surface; Plan 04 passes the prop, Phase 18 wires the real `--save-transcripts` flag)
- Append " [MUTED]" tag (colorized chalk.redBright bracketed form) when state === "muted"
- Truncate transcript to last 60 chars (CONTEXT.md `<domain>` row 1) — use `transcript.slice(-60)` to take the tail

From 16-RESEARCH.md §"Pattern 2: useInput" — Plan 03 only includes the screen-reader component; the `m` keypress useInput handler lives in Plan 04's VoiceShell.tsx (where the session.toggleMute() function is in scope). Plan 03's StatusRow.tsx receives `state` and `muted` as props.

From RESEARCH.md Assumption Log A2 — verbatim correction:
- Ink 7 does NOT support `aria-live`. Supported ARIA attributes: aria-label, aria-hidden, aria-role, aria-state.
- ScreenReader.tsx renders a single `<Text aria-label={SCREEN_READER_WORDING[state]} aria-role="status">{SCREEN_READER_WORDING[state]}</Text>` (the text content is what the screen reader reads; the aria-label is the explicit hint for screen-reader announcers; aria-role="status" matches the semantic intent of "live state announcer").
- The 200ms debounce from CONTEXT.md `<specifics>` row 3 is implemented in Plan 03 via a React `useEffect` that schedules the state-text change with `setTimeout(setDisplayedState, 200)` and clears on re-trigger. The screen reader announces only after 200ms of state stability.

From 15-DEVIATIONS.md D-15-02 (active):
- `npm install --include=optional --force` is the required install invocation for any new dep additions. Plan 03's Task 1 includes this exact invocation.

From PITFALLS.md §6 (Ink reconciliation thrash) — Plan 03 substrate, Plan 04 verification:
- Pre-compute blob string OUTSIDE the React tree per tick (blobFrame is the pure function)
- Pre-compute sparkline string OUTSIDE the React tree per tick (sparklineFromRing from Plan 01 is the pure function)
- Use ONE setState({tick}) per render cycle (Plan 04 owns the setInterval)
- The 10-minute CPU < 10% measurement is MANUAL (per CONTEXT.md `<deferred>` Phase 20 row + 16-VALIDATION.md row "TUI-05 — manual-only")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — add ink/react/chalk/ink-testing-library + extend tsconfig/eslint/vitest configs to cover .tsx</name>
  <files>
    apps/achilles-terminal/package.json,
    apps/achilles-terminal/tsconfig.json,
    apps/achilles-terminal/eslint.config.js,
    apps/achilles-terminal/vitest.config.ts
  </files>
  <read_first>
    apps/achilles-terminal/package.json,
    apps/achilles-terminal/tsconfig.json,
    apps/achilles-terminal/eslint.config.js,
    apps/achilles-terminal/vitest.config.ts,
    .planning/phases/15-workspace-scaffold-bun-build-pipeline/15-DEVIATIONS.md,
    .planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-RESEARCH.md
  </read_first>
  <action>
    Extend the four Phase 15 lock files to add the four new packages and broaden the `.tsx` glob coverage. CRITICAL: do NOT change the package.json `name` field (still "achilles-terminal" per D-15-01); do NOT rename the bin entry; do NOT touch the existing 5 sibling optionalDependencies; do NOT add `react-dom` (Ink uses its own host config).

    apps/achilles-terminal/package.json: edit-in-place. Add to dependencies (sorted alphabetically): `"chalk": "^5.6.2"`, `"ink": "^7.0.5"`, `"react": "^19.2.7"` — three new prod deps. The existing `"commander": "13.1.0"` stays. Do not change optionalDependencies (5 sibling packages unchanged). Add to devDependencies (sorted alphabetically): `"@types/react": "^19.2.17"`, `"ink-testing-library": "^4.0.0"` — two new dev deps. The existing devDependencies (esbuild, eslint, eslint-config-prettier, tsx, typescript, typescript-eslint, vitest, @types/node) stay unchanged.

    apps/achilles-terminal/tsconfig.json: edit-in-place. Add JSX support — extend compilerOptions with `"jsx": "react-jsx"` (the modern JSX transform that does not require explicit React imports in every .tsx file). Broaden include glob from `["src/**/*.ts", "tests/**/*.ts", "scripts/**/*.mjs"]` to `["src/**/*.ts", "src/**/*.tsx", "tests/**/*.ts", "tests/**/*.tsx", "scripts/**/*.mjs"]`. Add `"react-jsx"` to lib? — NO, lib stays ["ES2024"]; the react-jsx transform is a compiler-level concern, not a lib concern. Do not change module/moduleResolution/target/strict — they all stay at their Phase 15 values.

    apps/achilles-terminal/eslint.config.js: edit-in-place. Broaden the first config block's `files` glob from `["src/**/*.ts", "tests/**/*.ts"]` to `["src/**/*.ts", "src/**/*.tsx", "tests/**/*.ts", "tests/**/*.tsx"]`. The recommendedTypeChecked block now covers .tsx as well. Keep the parserOptions.project pointing at "./tsconfig.json" (which now includes the .tsx files via Task 1's tsconfig edit). Keep prettier last. Keep the Phase 19 rule slot comment block intact (the slot is still empty — Phase 19 owns the `stdio: "ignore"` no-restricted-syntax rule activation).

    apps/achilles-terminal/vitest.config.ts: edit-in-place. Broaden the test.include glob from `["tests/**/*.test.ts"]` to `["tests/**/*.test.ts", "tests/**/*.test.tsx"]`. Pool stays "forks" (Pitfall 9). Environment stays "node" (Ink testing library runs in node env per RESEARCH.md — no jsdom needed).

    After all four config edits, run `npm install --include=optional --force` at the repo root (D-15-02 invocation shape). This installs the four new packages (ink, react, chalk, @types/react, ink-testing-library) per the Phase 15 lockfile-update workflow. Verify the install completes successfully and lockfile entries are added for all five new packages.

    NO emojis (CLAUDE.md global). LOOP-02: no voice-* or claude-code-bridge deps added.

    CRITICAL — do NOT auto-run `achilles voice` to verify the install worked (CLAUDE.md global). Verification is via `npm install` exit code + `npm run typecheck` + the existing Phase 15 vitest tests still passing.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npm install --include=optional --force 2>&amp;1 | tail -10 &amp;&amp; node -e "const pkg = require('./apps/achilles-terminal/package.json'); for (const d of ['ink','react','chalk']) if (!pkg.dependencies[d]) throw new Error('missing dep: ' + d); for (const d of ['@types/react','ink-testing-library']) if (!pkg.devDependencies[d]) throw new Error('missing devDep: ' + d); if (pkg.name !== 'achilles-terminal') throw new Error('name regressed past D-15-01'); console.log('deps OK');" &amp;&amp; npm run typecheck --workspace apps/achilles-terminal 2>&amp;1 | tail -5 &amp;&amp; npm test --workspace apps/achilles-terminal -- --pool=forks tests/cli.test.ts 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/achilles-terminal/package.json dependencies include ink@^7.0.5, react@^19.2.7, chalk@^5.6.2 (3 NEW), commander@13.1.0 (Phase 15, unchanged)
    - apps/achilles-terminal/package.json devDependencies include @types/react@^19.2.17 and ink-testing-library@^4.0.0 (2 NEW)
    - apps/achilles-terminal/package.json `name` field is STILL "achilles-terminal" (D-15-01 invariant preserved)
    - apps/achilles-terminal/package.json optionalDependencies block is UNCHANGED (5 platform siblings still listed)
    - apps/achilles-terminal/tsconfig.json compilerOptions includes `"jsx": "react-jsx"` AND include glob covers `src/**/*.tsx` and `tests/**/*.tsx`
    - apps/achilles-terminal/eslint.config.js first block's `files` covers `.tsx`
    - apps/achilles-terminal/vitest.config.ts test.include covers `tests/**/*.test.tsx`
    - `npm install --include=optional --force` exits 0 with no errors
    - `npm run typecheck --workspace apps/achilles-terminal` exits 0 (the existing Phase 15 cli.ts still typechecks under the broadened tsconfig)
    - `npm test --workspace apps/achilles-terminal -- --pool=forks tests/cli.test.ts` still passes all 5 Phase 15 INIT-07 assertions (Phase 15 regression check — Plan 03 must NOT break cli.ts)
    - `grep -E "voice-protocol|voice-stt|voice-tts|claude-code-bridge|achilles-skill|react-dom" apps/achilles-terminal/package.json` returns 0 lines (LOOP-02 + no react-dom)
  </acceptance_criteria>
  <done>
    Workspace can compile, lint, and test .tsx files. ink + react + chalk + ink-testing-library + @types/react installed. Phase 15 invariants (D-15-01 package name, INIT-07 cli.test.ts, optional sibling deps) preserved. The substrate for Tasks 2-3 is locked.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement colors.ts (palette + screen reader detection + envelope helpers) + Blob.tsx + Sparkline.tsx + tests</name>
  <files>
    apps/achilles-terminal/src/ui/colors.ts,
    apps/achilles-terminal/src/ui/Blob.tsx,
    apps/achilles-terminal/src/ui/Sparkline.tsx,
    apps/achilles-terminal/tests/ui/blob.test.tsx,
    apps/achilles-terminal/tests/ui/sparkline.test.tsx
  </files>
  <read_first>
    .planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-CONTEXT.md,
    .planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-RESEARCH.md,
    apps/achilles-terminal/src/audio/braille.ts,
    apps/achilles-terminal/src/state/constants.ts
  </read_first>
  <behavior>
    - Test 1 (colors.ts STATE_COLORS): STATE_COLORS deep-equals { idle: "gray", listening: "green", processing: "yellow", speaking: "blue", error: "red", muted: "redBright" } — exactly 6 keys (one per AchillesState including muted from Plan 02)
    - Test 2 (colors.ts SCREEN_READER_WORDING): SCREEN_READER_WORDING deep-equals the locked table from CONTEXT.md `<decisions>` Accessibility row { idle: "Achilles ready.", listening: "Achilles listening.", processing: "Achilles processing your request.", speaking: "Achilles speaking.", error: "Achilles encountered an error.", muted: "Achilles muted." }
    - Test 3 (colors.ts isScreenReaderActive — A1): with process.env.INK_SCREEN_READER set to "true", isScreenReaderActive() returns true; with it unset OR set to any other value ("1", "TRUE", "false", ""), isScreenReaderActive() returns false
    - Test 4 (colors.ts colorize honors NO_COLOR): with process.env.NO_COLOR="1", colorize("error", "x") returns the literal string "x" (no ANSI codes); with process.env.NO_COLOR unset and FORCE_COLOR="1", colorize("error", "x") returns a string containing ANSI escape codes ([)
    - Test 5 (colors.ts envelope helpers): idleBreathingAmplitude(0) === 0.3 (sin(0) === 0; 0.3 + 0.1*0); idleBreathingAmplitude(300 * Math.PI) is within 0.001 of 0.3 (sin(πk) === 0 for integer k); processingPulseAmplitude(0) === 0.5; processingPulseAmplitude(100 * Math.PI) within 0.001 of 0.5
    - Test 6 (Blob.tsx blobFrame center-weighted kernel): blobFrame(1.0) returns 7 strings each of length 7; the center cell (row 3, col 3) is "█" (U+2588 — full block); ring-1 cells (row 3 col 2, row 2 col 3, etc. — distance 1) are "▓" (U+2593 — dark shade, intensity 0.75 rounds to 3/4 = "▓"); corner cells (distance √18 ≈ 4.24 → floor 4 → clamped ring 3 → 0.25) are "░" (U+2591 — light shade)
    - Test 7 (Blob.tsx blobFrame at amplitude 0): blobFrame(0) returns 7 strings of 7 space characters each (every cell is U+0020)
    - Test 8 (Blob.tsx Ink renders the 7 rows): render(<Blob amplitude={0.5} />) lastFrame() contains the seven row strings produced by blobFrame(0.5), each on its own line; no extra ANSI codes beyond what Ink/chalk emits per the chalk state mapping (Plan 03's Blob does NOT colorize internally — color is applied at the StatusRow + by VoiceShell.tsx's wrapping component in Plan 04; Blob.tsx renders monochrome block characters)
    - Test 9 (Blob.tsx screen-reader mode suppresses entirely): render(<Blob amplitude={0.5} isScreenReader={true} />) lastFrame() is empty / contains no block characters U+2580–U+259F (the entire blob is replaced by null render OR the absence of the Blob mount)
    - Test 10 (Sparkline.tsx renders 40-cell braille string): given a Float32Array(80) ring filled with 0.5 and writeIndex = 0, render(<Sparkline ring={ring} writeIndex={0} />) lastFrame() contains a string of length 40 where every character is in the U+2800-U+28FF range; the specific cell value at intensity 0.5*4 = 2 matches sparklineFromRing(ring, 0)
    - Test 11 (Sparkline.tsx screen-reader mode suppresses entirely): render(<Sparkline ring={ring} writeIndex={0} isScreenReader={true} />) lastFrame() contains no braille characters U+2800-U+28FF
  </behavior>
  <action>
    Create five files. Tests are written first per TDD; commit all together once they pass.

    apps/achilles-terminal/src/ui/colors.ts: implement per 16-RESEARCH.md §"NO_COLOR / FORCE_COLOR / INK_SCREEN_READER precedence" lines 793-852. Import chalk from "chalk" (default import). Import type { AchillesState } from "../state/constants.js". Export STATE_COLORS as a const-asserted object with exactly the 6 keys per behavior Test 1. Export SCREEN_READER_WORDING as a const-asserted object with exactly the 6 keys per behavior Test 2 (the literal strings from CONTEXT.md `<decisions>` Accessibility row). Export `colorize(state: AchillesState | "muted", text: string): string` that resolves the chalk color via `(chalk as any)[STATE_COLORS[state]](text)` — note Plan 02 includes "muted" in AchillesState, but the | "muted" union is kept for caller clarity; the colors.ts compile-time check confirms STATE_COLORS has a "muted" entry. Export `isScreenReaderActive(): boolean = process.env["INK_SCREEN_READER"] === "true"` per RESEARCH Assumption A1. Export `idleBreathingAmplitude(tickMs: number): number = 0.3 + 0.1 * Math.sin(tickMs / 600)` and `processingPulseAmplitude(tickMs: number): number = 0.5 + 0.3 * Math.sin(tickMs / 200)` — pure helpers used by Plan 04's session.ts.

    apps/achilles-terminal/src/ui/Blob.tsx: implement per 16-RESEARCH.md §"Unicode block ramp for the blob" lines 624-672. Top of file: `import { Box, Text } from "ink"`. Export const `RAMP = [" ", "░", "▒", "▓", "█"] as const` (5-step shade ramp — U+0020 / U+2591 / U+2592 / U+2593 / U+2588). Export `rampChar(intensity: number): string` that clamps intensity*4 to 0..4 via Math.round, returns RAMP[clamped]. Export pure helper `blobFrame(amplitude: number): readonly string[]` that builds 7 rows of 7 characters using the center-weighted ring kernel: `const cx = 3, cy = 3; for y in 0..7, for x in 0..7: const ring = Math.min(3, Math.floor(Math.hypot(x - cx, y - cy))); const ringScale = ring === 0 ? 1.0 : ring === 1 ? 0.75 : ring === 2 ? 0.5 : 0.25; row += rampChar(amplitude * ringScale)`. Export the component `Blob({ amplitude, isScreenReader = false }: { amplitude: number; isScreenReader?: boolean }) { if (isScreenReader) return null; const rows = blobFrame(amplitude); return <Box flexDirection="column">{rows.map((row, i) => <Text key={i}>{row}</Text>)}</Box>; }`. The screen-reader gate is at the component boundary — Blob.tsx returns null in screen-reader mode so the Ink reconciler removes the entire subtree (per CONTEXT.md `<decisions>` Accessibility row: "suppress Blob and Sparkline (don't render them at all — not just hide)").

    apps/achilles-terminal/src/ui/Sparkline.tsx: implement per 16-RESEARCH.md §"Architectural Responsibility Map" row 2. `import { Text } from "ink"; import { sparklineFromRing } from "../audio/braille.js"`. Export the component `Sparkline({ ring, writeIndex, isScreenReader = false }: { ring: Float32Array; writeIndex: number; isScreenReader?: boolean }) { if (isScreenReader) return null; return <Text>{sparklineFromRing(ring, writeIndex)}</Text>; }`. The single <Text> wraps the 40-character braille string emitted by Plan 01's pure function.

    apps/achilles-terminal/tests/ui/blob.test.tsx: vitest test suite. Imports: describe, it, expect from vitest; render from ink-testing-library; React (the JSX is transformed, but `import React from "react"` may be needed for older transform compatibility — use the react-jsx transform from Task 1 which removes the need for explicit React imports in JSX files; verify by ensuring Task 1's tsconfig has `"jsx": "react-jsx"`). Tests 1-7 cover STATE_COLORS, SCREEN_READER_WORDING, isScreenReaderActive, NO_COLOR/FORCE_COLOR, envelope helpers, blobFrame kernel, blobFrame zero. For env-var tests use vitest's vi.stubEnv("NO_COLOR", "1") + vi.unstubAllEnvs() teardown. Test 8 renders <Blob amplitude={0.5} /> via ink-testing-library and inspects the returned `lastFrame()` string — split by newline, assert 7 row strings each of length 7, assert specific characters at known positions. Test 9 renders <Blob amplitude={0.5} isScreenReader={true} /> and asserts lastFrame() contains no characters in [U+2580-U+259F] range (specifically: no "█", "▓", "▒", "░"). For NO_COLOR / FORCE_COLOR tests, also test colorize: with NO_COLOR set, colorize("error", "x") === "x" (no escape codes); with FORCE_COLOR set, colorize("error", "x") contains the literal string "[".

    apps/achilles-terminal/tests/ui/sparkline.test.tsx: vitest test suite. Tests 10-11 use ink-testing-library render() to mount <Sparkline ring={ring} writeIndex={0} /> with a Float32Array(80) ring; assert lastFrame() length === 40 (or 40 after stripping newlines) and per-char codepoint range. For screen-reader test, mount with isScreenReader={true} and assert no braille codepoints in the frame.

    CLAUDE.md global rule: NO emojis (the block characters U+2580-U+259F and braille U+2800-U+28FF are NOT in the emoji ranges checked by the grep pattern; they are explicitly used Unicode block + braille codepoints). LOOP-02: zero imports from voice-* / claude-code-bridge / achilles-skill.

    Run `npm test --workspace apps/achilles-terminal -- --pool=forks tests/ui/blob.test.tsx tests/ui/sparkline.test.tsx` to confirm 11 tests pass.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npm test --workspace apps/achilles-terminal -- --pool=forks tests/ui/blob.test.tsx tests/ui/sparkline.test.tsx 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/achilles-terminal/src/ui/colors.ts exports STATE_COLORS (6 keys), SCREEN_READER_WORDING (6 keys with locked strings), colorize, isScreenReaderActive, idleBreathingAmplitude, processingPulseAmplitude
    - colors.ts isScreenReaderActive() source compares `process.env["INK_SCREEN_READER"] === "true"` (the string "true", NOT "1" — RESEARCH Assumption A1)
    - apps/achilles-terminal/src/ui/Blob.tsx exports Blob (component), blobFrame (pure helper), rampChar, RAMP (const ramp array)
    - Blob.tsx returns `null` when the isScreenReader prop is true (CONTEXT.md `<decisions>` Accessibility row "don't render them at all")
    - apps/achilles-terminal/src/ui/Sparkline.tsx imports `sparklineFromRing` from "../audio/braille.js" (Plan 01 dependency)
    - Sparkline.tsx returns `null` when isScreenReader is true
    - All 11 tests across tests/ui/blob.test.tsx + tests/ui/sparkline.test.tsx pass under `npm test --workspace apps/achilles-terminal`
    - `npm run typecheck --workspace apps/achilles-terminal` exits 0 (the .tsx files compile under the broadened tsconfig from Task 1)
    - `grep -E "voice-protocol|voice-stt|voice-tts|claude-code-bridge|achilles-skill" apps/achilles-terminal/src/ui/colors.ts apps/achilles-terminal/src/ui/Blob.tsx apps/achilles-terminal/src/ui/Sparkline.tsx apps/achilles-terminal/tests/ui/blob.test.tsx apps/achilles-terminal/tests/ui/sparkline.test.tsx` returns 0 lines (LOOP-02)
    - `grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" apps/achilles-terminal/src/ui/colors.ts apps/achilles-terminal/src/ui/Blob.tsx apps/achilles-terminal/src/ui/Sparkline.tsx apps/achilles-terminal/tests/ui/blob.test.tsx apps/achilles-terminal/tests/ui/sparkline.test.tsx` returns 0 lines (no pictograph emojis)
    - colors.ts STATE_COLORS has the literal string "muted" as a key with value "redBright" (CAP-03 substrate — Plan 04 colorizes the MUTED tag using this)
  </acceptance_criteria>
  <done>
    colors.ts ships the locked 5+1 state palette, screen-reader wording table, screen-reader detector with the corrected `=== "true"` check, NO_COLOR/FORCE_COLOR honored via chalk, and the two envelope helpers Plan 04 will compose. Blob.tsx ships the 7×7 reactive blob with the verbatim center-weighted ring kernel, screen-reader suppression at component boundary. Sparkline.tsx ships the 40-cell braille component consuming Plan 01's encoder. TUI-01 substrate, TUI-02 substrate, TUI-03 substrate (palette + envelopes), ACC-01 substrate (chalk handles), ACC-02 substrate (screen-reader detection + suppression) all complete.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement StatusRow.tsx + ScreenReader.tsx + plain-text.ts + mock-amplitude.ts + tests</name>
  <files>
    apps/achilles-terminal/src/ui/StatusRow.tsx,
    apps/achilles-terminal/src/ui/ScreenReader.tsx,
    apps/achilles-terminal/src/ui/plain-text.ts,
    apps/achilles-terminal/src/ui/mock-amplitude.ts,
    apps/achilles-terminal/tests/ui/status-row.test.tsx,
    apps/achilles-terminal/tests/ui/screen-reader.test.tsx,
    apps/achilles-terminal/tests/ui/plain-text.test.ts,
    apps/achilles-terminal/tests/ui/mock-amplitude.test.ts
  </files>
  <read_first>
    .planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-CONTEXT.md,
    .planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-RESEARCH.md,
    apps/achilles-terminal/src/ui/colors.ts,
    apps/achilles-terminal/src/state/constants.ts
  </read_first>
  <behavior>
    - Test 1 (StatusRow default — state idle, no transcript, no REC, no muted): render(<StatusRow state="idle" transcript="" transcriptsActive={false} />) lastFrame() contains "[idle]" (state-name brackets) but does NOT contain "REC" or "MUTED"
    - Test 2 (StatusRow transcript truncation to last 60 chars): given a 200-char transcript, render(<StatusRow state="listening" transcript={longString} transcriptsActive={false} />) lastFrame() includes only the last 60 characters of longString (and NOT the first 140)
    - Test 3 (StatusRow REC tag appears when transcriptsActive=true): render(<StatusRow state="idle" transcript="hi" transcriptsActive={true} />) lastFrame() contains the literal substring "[REC]"
    - Test 4 (StatusRow MUTED tag appears when state="muted"): render(<StatusRow state="muted" transcript="" transcriptsActive={false} />) lastFrame() contains the literal substring "[MUTED]"
    - Test 5 (StatusRow REC and MUTED both appear when applicable): render with state="muted" + transcriptsActive=true contains BOTH "[REC]" AND "[MUTED]" substrings
    - Test 6 (ScreenReader.tsx renders one Text with aria-label per A2 correction): render(<ScreenReader state="listening" />) lastFrame() contains the literal SCREEN_READER_WORDING.listening text "Achilles listening."; the component does NOT contain the literal string "aria-live" anywhere in its source (A2 correction — aria-label + aria-role only)
    - Test 7 (ScreenReader.tsx state transition triggers re-render): with vitest fake timers, render <ScreenReader state="idle" /> then rerender <ScreenReader state="processing" /> and advance 200ms — lastFrame() now contains "Achilles processing your request."
    - Test 8 (plain-text.ts formatPlainLine format): formatPlainLine({ timestamp: new Date("2026-06-08T12:00:00Z"), state: "listening", transcript: "hello" }) === "[2026-06-08T12:00:00.000Z] [listening] hello"
    - Test 9 (plain-text.ts no ANSI escapes): formatPlainLine() output passed through a regex /\[/ returns no matches (no ANSI codes in plain mode)
    - Test 10 (mock-amplitude.ts deterministic seed): two createMockAmplitudeStream({seed: 42}) instances pumped for the same number of frames emit identical amplitude sequences (deterministic — same input seed → same output)
    - Test 11 (mock-amplitude.ts speech/silence loop shape): the first 30 frames (1.5s at 20ms cadence — speech ramp window) include at least one amplitude > 0.4 (the speech-like envelope rises to 0.7); the next 30 frames (1.5s silence window) all have amplitude < 0.1 (silence + small noise floor)
    - Test 12 (mock-amplitude.ts dispose stops emission): calling stream.stop() ceases emission — no further frames arrive within 100ms after stop()
  </behavior>
  <action>
    Create eight files. Tests are written first per TDD; commit all together once they pass.

    apps/achilles-terminal/src/ui/StatusRow.tsx: `import { Text, Box } from "ink"; import { colorize, STATE_COLORS } from "./colors.js"; import type { AchillesState } from "../state/constants.js"`. Export the component `StatusRow({ state, transcript, transcriptsActive }: { state: AchillesState; transcript: string; transcriptsActive: boolean })`. Inside: compose the segments — `const stateTag = colorize(state, "[" + state + "]")` (e.g., "[idle]" in gray); `const truncated = transcript.slice(-60)`; `const recTag = transcriptsActive ? colorize("error", " [REC]") : ""` (REC uses error/red color per CONTEXT.md "REC tag (colorized red)"); `const mutedTag = state === "muted" ? colorize("muted", " [MUTED]") : ""`. Render `<Text>{stateTag}{truncated ? " " + truncated : ""}{recTag}{mutedTag}</Text>`. NO use of aria-live (RESEARCH A2). NO `dangerouslySetInnerHTML` equivalents — Ink doesn't have one. The colorize calls run chalk under the hood, which auto-no-ops on NO_COLOR (ACC-01).

    apps/achilles-terminal/src/ui/ScreenReader.tsx: `import { Text } from "ink"; import { useEffect, useState } from "react"; import { SCREEN_READER_WORDING } from "./colors.js"; import type { AchillesState } from "../state/constants.js"`. Export the component `ScreenReader({ state }: { state: AchillesState })`. Inside: use `useState<AchillesState>(state)` for displayedState. `useEffect` watches `state` prop; when it changes, schedule `setTimeout(() => setDisplayedState(state), 200)` (200ms debounce per CONTEXT.md `<specifics>` row 3); on re-trigger or unmount, clearTimeout. Render `<Text aria-label={SCREEN_READER_WORDING[displayedState]} aria-role="status">{SCREEN_READER_WORDING[displayedState]}</Text>` — per RESEARCH A2: aria-label and aria-role only, NO aria-live. The aria-role="status" is the closest Ink 7-supported role to the polite-live-region semantic intent.

    apps/achilles-terminal/src/ui/plain-text.ts: pure-function module — no Ink import, no React. Export interface `PlainLineInput { timestamp: Date; state: AchillesState; transcript: string }`. Export `formatPlainLine(input: PlainLineInput): string` returning the literal format `[ISO][state] transcript` — e.g., `[2026-06-08T12:00:00.000Z] [listening] hello`. ISO timestamp via `input.timestamp.toISOString()`. NO chalk imports — plain mode emits NO ANSI escapes per TUI-06 row "format: [ISO][state] partial-transcript, no ANSI". Export an async function `startPlainMode(events: { onStateChange: (cb: (s: AchillesState) => void) => () => void; onTranscriptPartial: (cb: (t: string) => void) => () => void; getState: () => AchillesState; getTranscript: () => string }): Promise<void>` — this is the Plan 04 integration seam. The implementation subscribes to events and writes formatPlainLine output to process.stdout.write() on each state-change OR transcript-partial event. Per PITFALLS.md §5 (Bun stdout flush): use the write-with-callback form `process.stdout.write(line + "\n", () => {})` (no exit needed — long-running). The function never resolves on its own; Plan 04's signal handlers terminate the process.

    apps/achilles-terminal/src/ui/mock-amplitude.ts: pure-function module — no Ink, no React. Per CONTEXT.md `<specifics>` row 5: emit a 1.5s "speech-like" amplitude pattern (rising envelope 0-0.7-0-0.5-0.3-0-0 over 30 frames at 20ms cadence) followed by 1.5s silence (random noise at 0.02 amplitude), repeating. The pattern is deterministic via seeded PRNG. Export interface `MockAmplitudeOptions { seed: number; onFrame: (amplitude: number) => void; intervalMs?: number }` (intervalMs default 20). Export `createMockAmplitudeStream(opts: MockAmplitudeOptions): { stop: () => void }` factory. Inside: implement a small mulberry32 or splitmix32 PRNG seeded with opts.seed (closed-form algorithms, no external deps). Maintain a frameIndex 0..59 wrapping. For frames 0..29 (speech window): use the locked envelope shape — frame i maps to amplitude via a piecewise linear table: 0→0, 5→0.7, 10→0, 15→0.5, 20→0.3, 25→0, 29→0 (interpolate between table points). For frames 30..59 (silence window): emit prng.next() * 0.02 (noise floor at 0-0.02 range). Drive emission via `setInterval(() => { opts.onFrame(amplitude); frameIndex = (frameIndex + 1) % 60; }, opts.intervalMs ?? 20)`. The stop() method calls clearInterval. LOOP-02: zero voice-* imports.

    apps/achilles-terminal/tests/ui/status-row.test.tsx: ink-testing-library tests for the 5 StatusRow behaviors (Tests 1-5 from behavior block). Use the env-var stubbing technique from Task 2 (vi.stubEnv("NO_COLOR", "1")) so the assertions on substring content can be made against plain text without ANSI code interference. Mount each StatusRow variant, capture lastFrame(), assert substring presence.

    apps/achilles-terminal/tests/ui/screen-reader.test.tsx: ink-testing-library + vitest fake timers (vi.useFakeTimers()). Tests 6 + 7 cover the aria-label rendering and the 200ms debounce. For Test 6, also assert via fs.readFileSync that apps/achilles-terminal/src/ui/ScreenReader.tsx does NOT contain the literal substring "aria-live" (RESEARCH A2 invariant). For Test 7, render <ScreenReader state="idle" />, capture frame, call rerender <ScreenReader state="processing" />, advance vi.advanceTimersByTime(199) and assert frame still shows "Achilles ready.", then advance 1 more ms (total 200) and assert frame shows "Achilles processing your request.".

    apps/achilles-terminal/tests/ui/plain-text.test.ts: pure unit tests for formatPlainLine (Tests 8-9). No Ink, no ink-testing-library. Just `import { formatPlainLine } from "../../src/ui/plain-text.js"` and assert the format string + the absence of ANSI escape pattern.

    apps/achilles-terminal/tests/ui/mock-amplitude.test.ts: tests using vitest fake timers. Build an onFrame mock, construct the stream, advance the fake clock by N intervals, inspect mock.calls. Tests 10-12 cover determinism, envelope shape, and stop().

    CLAUDE.md global rule: NO emojis (the block chars and braille are NOT emojis). LOOP-02: zero voice-*/claude-code-bridge imports.

    Run `npm test --workspace apps/achilles-terminal -- --pool=forks tests/ui/` to confirm all 12 new tests pass (in addition to the 11 from Task 2 — full suite should be 23 ui tests).
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npm test --workspace apps/achilles-terminal -- --pool=forks tests/ui/ 2>&amp;1 | tail -35</automated>
  </verify>
  <acceptance_criteria>
    - apps/achilles-terminal/src/ui/StatusRow.tsx exports StatusRow component; rendered output contains state tag, last-60-char transcript, optional [REC] tag (when transcriptsActive=true), optional [MUTED] tag (when state="muted")
    - apps/achilles-terminal/src/ui/ScreenReader.tsx exports ScreenReader component; source does NOT contain the literal string "aria-live" (RESEARCH A2 — verified by grep -F 'aria-live' apps/achilles-terminal/src/ui/ScreenReader.tsx returning 0 lines)
    - ScreenReader.tsx source contains the literal strings "aria-label" AND "aria-role" (the corrected ARIA shape)
    - apps/achilles-terminal/src/ui/plain-text.ts exports formatPlainLine + startPlainMode; formatPlainLine output contains NO ANSI escape codes (no `[` substring)
    - apps/achilles-terminal/src/ui/mock-amplitude.ts exports createMockAmplitudeStream; the stream is deterministic given a seed (verified by Test 10) and includes both speech-window peaks > 0.4 and silence-window levels < 0.1 (verified by Test 11)
    - All 12 NEW tests across tests/ui/status-row.test.tsx + tests/ui/screen-reader.test.tsx + tests/ui/plain-text.test.ts + tests/ui/mock-amplitude.test.ts pass
    - Combined with Task 2 tests, all 23 tests/ui/ tests pass under `npm test --workspace apps/achilles-terminal -- tests/ui/`
    - `npm run typecheck --workspace apps/achilles-terminal` exits 0
    - `grep -F 'aria-live' apps/achilles-terminal/src/ui/` returns 0 matches (RESEARCH A2 invariant)
    - `grep -E "voice-protocol|voice-stt|voice-tts|claude-code-bridge|achilles-skill" apps/achilles-terminal/src/ui/ apps/achilles-terminal/tests/ui/` returns 0 lines (LOOP-02)
    - `grep -P "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" apps/achilles-terminal/src/ui/ apps/achilles-terminal/tests/ui/` returns 0 lines (no pictograph emojis)
  </acceptance_criteria>
  <done>
    StatusRow.tsx ships with state + transcript + REC + MUTED rendering per CONTEXT.md. ScreenReader.tsx ships with the corrected aria-label + aria-role shape (NO aria-live — RESEARCH A2). plain-text.ts ships the ANSI-free format helper. mock-amplitude.ts ships the deterministic seeded speech/silence loop. TUI-04, TUI-06, ACC-02 substrate, and the `--mock` flag substrate all complete. All 23 UI tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env access (env vars → render decision) | INK_SCREEN_READER, NO_COLOR, FORCE_COLOR are read as strings, never eval'd; trusted via OS env-var semantics |
| chalk color application (chalk → ANSI string) | chalk natively respects NO_COLOR — defense-in-depth above colors.ts; no manual ANSI string construction in Phase 16 source |
| Ink render tree (component tree → terminal stdout) | Ink owns raw-mode toggling, ANSI escape coalescing, SIGWINCH handling; Phase 16 does not bypass Ink |
| npm install (registry → developer machine) | Untrusted package fetch for ink, react, chalk, @types/react, ink-testing-library — but all are HIGH-confidence per RESEARCH.md Package Legitimacy Audit (slopcheck OK) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-env-injection | Tampering | apps/achilles-terminal/src/ui/colors.ts isScreenReaderActive() | mitigate | The env-var read uses strict string equality (`=== "true"`) — no regex, no `eval`, no interpolation. Even if an attacker controls the env (e.g., `INK_SCREEN_READER='$(rm -rf /)'`), the JS code path treats the value as an opaque string never executed. RESEARCH Assumption A1 verified by Test 3 (any value other than the literal "true" returns false). |
| T-16-supply-chain | Tampering | npm install of ink@7.0.5, react@19.2.7, chalk@5.6.2, @types/react@19.2.17, ink-testing-library@4.0.0 | mitigate | All five packages are `[OK]` per the RESEARCH.md Package Legitimacy Audit (slopcheck approved; ~900K weekly downloads for ink; React + DefinitelyTyped well-known; chalk known despite the 2024 supply-chain incident now mitigated by pinning to 5.6.x; ink-testing-library has been stable since 2.x). NO postinstall scripts permitted (RESEARCH.md anti-pattern). Plan 04's CI matrix runs `npm ci --include=optional --force` (D-15-02 + D-15-03) which enforces lockfile equality. |
| T-16-stdout-tampering | Information disclosure / Tampering | apps/achilles-terminal/src/ui/plain-text.ts formatPlainLine | mitigate | formatPlainLine produces a fixed-format string with NO untrusted interpolation outside the transcript content. The state field is constrained to AchillesState (6-tuple membership). The transcript is user content but is written as plain text — no shell, no SQL, no eval target downstream. The 60-char truncation in StatusRow happens UPSTREAM of plain-text.ts; plain-text.ts itself emits the full transcript via its caller (Plan 04 owns the truncation policy for plain mode if needed — current design emits full transcript to plain-mode log lines). |
</threat_model>

<verification>
After all 3 tasks complete:
- `npm test --workspace apps/achilles-terminal -- --pool=forks` exits 0 with all Phase 15 cli.test.ts tests (5) + all Plan 01 audio tests (27) + all Plan 02 state tests (15) + all Plan 03 ui tests (23) passing — 70 tests total
- `npm run typecheck --workspace apps/achilles-terminal` exits 0
- `npm run lint --workspace apps/achilles-terminal -- --max-warnings 0` exits 0
- `grep -rF 'aria-live' apps/achilles-terminal/src/ui/` returns 0 matches (RESEARCH A2 invariant locked)
- `grep -rE "voice-protocol|voice-stt|voice-tts|claude-code-bridge|companion.md" apps/achilles-terminal/src/ui/ apps/achilles-terminal/tests/ui/` returns 0 lines (LOOP-02)
- `grep -rP "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" apps/achilles-terminal/src/ui/ apps/achilles-terminal/tests/ui/` returns 0 lines (no emojis)
- apps/achilles-terminal/package.json `name` field is still "achilles-terminal" (D-15-01 invariant)
- apps/achilles-terminal/package.json does NOT include react-dom in dependencies or devDependencies (Ink uses its own host config)
</verification>

<success_criteria>
TUI-01: 7×7 reactive blob component ships with center-weighted ring kernel + 5-step Unicode block ramp + screen-reader suppression at component boundary. Verified by Tests 6-9.
TUI-02: 40-cell braille sparkline component ships consuming Plan 01's sparklineFromRing pure helper. Verified by Tests 10-11.
TUI-03: 5-state colors locked in STATE_COLORS palette; idle breathing + processing pulse curves exported as pure helpers. Verified by Tests 1, 5.
TUI-04: StatusRow renders [state] transcript with truncation, REC, MUTED tags. Verified by Tests 1-5 of Task 3.
TUI-06: plain-text.ts ships formatPlainLine + startPlainMode; ANSI-free output. Verified by Tests 8-9.
ACC-01: NO_COLOR + FORCE_COLOR honored via chalk native behavior. Verified by Test 4.
ACC-02: screen-reader detection uses `=== "true"` per A1; ScreenReader component uses aria-label + aria-role (NOT aria-live) per A2; Blob + Sparkline return null in screen-reader mode. Verified by Tests 3, 6, 7, 9, 11.
RESEARCH Assumption A1: `INK_SCREEN_READER === "true"` (string "true") locked in colors.ts source.
RESEARCH Assumption A2: NO `aria-live` anywhere; aria-label + aria-role only. Verified by `grep -rF 'aria-live'` returning empty.
LOOP-02 invariant: no voice-*/claude-code-bridge/companion.md references in any new file; D-15-01 preserved.
</success_criteria>

<output>
Create `.planning/phases/16-tui-shell-state-machine-sox-mic-capture-energy-vad/16-03-SUMMARY.md` summarizing the 17 files touched (4 config edits + 7 source + 6 test), test count (23 UI tests + Phase 15/Plan 01/Plan 02 regression untouched), all three RESEARCH.md Assumption Log corrections applied (A1 string "true", A2 aria-label+aria-role NOT aria-live, A3 deferred to Plan 04 where useInput lives), and any deviations encountered.
</output>
</content>
</invoke>