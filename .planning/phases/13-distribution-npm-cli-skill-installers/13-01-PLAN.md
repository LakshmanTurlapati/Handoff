---
phase: 13-distribution-npm-cli-skill-installers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/achilles-cli/package.json
  - apps/achilles-cli/tsconfig.json
  - apps/achilles-cli/src/.gitignore
  - apps/achilles-cli/src/cli.ts
  - apps/achilles-cli/src/cli.test.ts
  - apps/achilles-cli/src/commands/launch.ts
  - apps/achilles-cli/src/commands/launch.test.ts
  - apps/achilles-cli/src/commands/transcripts.ts
  - apps/achilles-cli/src/commands/transcripts.test.ts
  - apps/achilles-cli/src/electron-binary-locator.ts
  - apps/achilles-cli/src/electron-binary-locator.test.ts
  - apps/achilles-cli/README.md
  - tsconfig.base.json
  - vitest.workspace.ts
  - package.json
autonomous: true
requirements:
  - DIST-01
tags:
  - achilles
  - cli
  - npm-package
  - distribution
  - commander
  - dist-01

must_haves:
  truths:
    - "User can run `npm install -g achilles` (with the tarball produced by `npm pack`) and the `achilles` command resolves on macOS, Windows, and Linux PATH without admin elevation"
    - "Running `achilles` (no args) or `achilles launch` locates the bundled Electron binary on the host OS and execs it as a detached child so the CLI exits and the Electron app stays running"
    - "Running `achilles --version` prints the package version from package.json and exits 0"
    - "Running `achilles --help` prints the documented top-level commands (launch, install-skill, init, transcripts) and exits 0"
    - "Running `achilles transcripts purge` exits 0 with a 'not yet implemented' message (Phase 14 stub) and does NOT silently delete user data"
    - "The CLI tarball produced by `npm pack` in apps/achilles-cli contains dist/, README.md, and package.json and is publish-ready (`publishConfig.access: \"public\"`) but is NOT published in CI"
  artifacts:
    - path: "apps/achilles-cli/package.json"
      provides: "Published npm package manifest: name `achilles`, bin entry, publishConfig public, workspace dep on @achilles/achilles-skill"
      contains: "\"name\": \"achilles\""
    - path: "apps/achilles-cli/src/cli.ts"
      provides: "commander-based CLI entrypoint; routes `achilles [launch|install-skill|init|transcripts purge]` and the no-arg default to launch"
      exports: ["runCli"]
    - path: "apps/achilles-cli/src/commands/launch.ts"
      provides: "Locates the bundled Electron binary and execs it detached so the CLI exits while the app stays running"
      exports: ["launchCommand"]
    - path: "apps/achilles-cli/src/commands/transcripts.ts"
      provides: "Phase 14-deferred `transcripts purge` stub that exits 0 with a clearly-marked-deferred message; no filesystem mutation"
      exports: ["transcriptsCommand"]
    - path: "apps/achilles-cli/src/electron-binary-locator.ts"
      provides: "Resolves the bundled Electron binary path per platform (darwin: dist/Achilles.app/Contents/MacOS/Achilles; win32: dist/Achilles.exe; linux: dist/linux/achilles); throws ElectronBinaryMissingError when absent"
      exports: ["locateElectronBinary", "ElectronBinaryMissingError"]
    - path: "apps/achilles-cli/tsconfig.json"
      provides: "Extends tsconfig.base.json with rootDir:src + outDir:dist + NodeNext module resolution; excludes test files (Phase 09 CR-07 hygiene)"
    - path: "apps/achilles-cli/src/.gitignore"
      provides: "CR-07 defensive guard: ignore *.js, *.d.ts, *.js.map, *.d.ts.map under src/ to catch mis-built artefacts"
    - path: "vitest.workspace.ts"
      provides: "New `phase-13-unit` project includes apps/achilles-cli/src/**/*.test.ts so tests run via the workspace runner"
      contains: "phase-13-unit"
    - path: "tsconfig.base.json"
      provides: "Added paths alias for the `achilles-cli` workspace (achilles-cli, achilles-cli/* → apps/achilles-cli/src/*) so workspace imports resolve under tsc"
      contains: "achilles-cli"
  key_links:
    - from: "apps/achilles-cli/src/cli.ts"
      to: "apps/achilles-cli/src/commands/launch.ts"
      via: "commander action handler import"
      pattern: "from \"./commands/launch"
    - from: "apps/achilles-cli/src/commands/launch.ts"
      to: "apps/achilles-cli/src/electron-binary-locator.ts"
      via: "locateElectronBinary call resolves bundled Electron path before spawn"
      pattern: "locateElectronBinary"
    - from: "package.json (root)"
      to: "apps/achilles-cli"
      via: "workspaces glob already covers apps/*; phase-13 vitest script wires to the new project"
      pattern: "test:phase-13:quick"
---

<objective>
Scaffold `apps/achilles-cli` — the publishable npm package that ships the `achilles` global command (DIST-01). This plan delivers the package manifest, the commander entrypoint, the no-arg default + explicit `launch` command, the platform-aware Electron binary locator, the `transcripts purge` Phase-14-deferred stub, and the workspace plumbing (tsconfig.base.json path aliases + vitest.workspace.ts `phase-13-unit` project + top-level package.json script). The `install-skill` and `init` commands are owned by Plan 13-02 and 13-03; this plan provides the cli.ts surface they extend.

Purpose: A user runs `npm install -g achilles` and gets a global `achilles` binary that, with no args, opens the floating UI from any directory. The platform-aware locator path handles macOS (.app bundle), Windows (.exe), and Linux (AppImage-extracted binary). Detached spawn keeps the Electron app running after the CLI process exits (the CLI is not the GUI's parent — npm-CLI launch from a terminal must not block the terminal).

Output: The `apps/achilles-cli/` package directory at npm-publishable shape (private:false, publishConfig.access:public, files: ["dist", "README.md"]), the commander surface for four top-level commands (two of which delegate to placeholders that 13-02 / 13-03 fill in), and the deferred `transcripts purge` Phase 14 stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-distribution-npm-cli-skill-installers/13-CONTEXT.md
@.planning/phases/12-end-to-end-integration-system-prompt/12-04-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@CLAUDE.md

@apps/achilles/package.json
@packages/voice-stt/package.json
@packages/achilles-skill/package.json
@apps/achilles/src/main/key-source.ts
@vitest.workspace.ts
@tsconfig.base.json
@package.json
@apps/achilles/src/.gitignore

<interfaces>
<!-- Existing workspace conventions Plan 13-01 must conform to. -->

From packages/voice-stt/package.json (analog for a published workspace package):
```json
{
  "name": "@achilles/voice-stt",
  "private": false,
  "publishConfig": { "access": "public" },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "scripts": {
    "build": "rm -rf dist .tsbuildinfo && tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "prepack": "npm run build",
    "test": "vitest run"
  }
}
```

The achilles-cli package differs in five ways:
- `"name": "achilles"` (NOT scoped — this is the public top-level CLI)
- adds `"bin": { "achilles": "./dist/cli.js" }`
- adds `"description"` and `"license": "MIT"`
- depends on `commander@13.x` + workspace `@achilles/achilles-skill` (the latter pulls the skill body into the tarball via the npm workspaces resolution — Plan 13-02 finalises the SKILL.md body; Plan 13-04 verifies the diff)
- ships a `README.md` (Plan 13-01 ships a minimal README; Plan 13-02 extends it with install-skill copy; Plan 13-03 extends with init copy)

From apps/achilles/src/.gitignore (CR-07 hygiene pattern Plan 13-01 mirrors):
```gitignore
# Compiled output never belongs inside src/. The build emits to ../out/
*.js
*.d.ts
*.js.map
*.d.ts.map
*.jsx
```

From vitest.workspace.ts (existing phase-12-unit project shape — Plan 13-01 adds a `phase-13-unit` entry mirroring this):
```ts
{
  resolve: { alias: workspaceAlias },
  test: {
    name: "phase-12-unit",
    include: [/* file globs */],
    environment: "node",
    passWithNoTests: true,
  },
}
```

From tsconfig.base.json paths (existing aliases — Plan 13-01 adds two new entries):
```jsonc
"paths": {
  // ... existing
  "@achilles/voice-stt": ["packages/voice-stt/src/index.ts"],
  "@achilles/voice-stt/*": ["packages/voice-stt/src/*"]
  // Plan 13-01 ADDS:
  // "achilles-cli": ["apps/achilles-cli/src/cli.ts"],
  // "achilles-cli/*": ["apps/achilles-cli/src/*"]
}
```

From CLAUDE.md (project-wide globals — enforced):
- NO emojis anywhere in code, tests, logs, or markdown unless the user explicitly asks
- Never run applications automatically; only run them when explicitly asked
- Phase 09 CR-07 hygiene: tsconfig MUST exclude *.test.ts; src/.gitignore guards mis-built artefacts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold apps/achilles-cli package + tsconfig + src/.gitignore + workspace plumbing</name>
  <files>
    apps/achilles-cli/package.json,
    apps/achilles-cli/tsconfig.json,
    apps/achilles-cli/src/.gitignore,
    apps/achilles-cli/README.md,
    tsconfig.base.json,
    vitest.workspace.ts,
    package.json
  </files>
  <behavior>
    - Test 1 (package.json contract): `apps/achilles-cli/package.json` parses as JSON; `name` field equals the string `achilles` (no scope, no leading @); `private` is `false`; `publishConfig.access` is `"public"`; `bin.achilles` is `"./dist/cli.js"`; `main` is `"dist/cli.js"`; `type` is `"module"`; `files` includes `"dist"` and `"README.md"`; `engines.node` is `">=22.0.0"` (matches root); `dependencies.commander` is the string `"13.1.0"` (locked exact version, no caret); `dependencies` includes `"@achilles/achilles-skill": "0.1.0"` (workspace dep that resolves to the npm-pack tarball at publish time per Pitfall #14). NO occurrence of any other production dependency in this plan.
    - Test 2 (scripts block): the `scripts` object includes `build`, `typecheck`, `lint`, `prepack`, and `test` keys; `build` runs `rm -rf dist .tsbuildinfo && tsc -p tsconfig.json`; `prepack` runs `npm run build` (so `npm pack` always builds first); `test` runs `vitest run`; `typecheck` runs `tsc --noEmit`. Plan 13-04 will ADD `dist:*` keys to this scripts block; do NOT add them in this plan.
    - Test 3 (tsconfig contract): `apps/achilles-cli/tsconfig.json` extends `../../tsconfig.base.json`; `compilerOptions.rootDir` is `"src"`; `compilerOptions.outDir` is `"dist"`; `compilerOptions.module` is `"NodeNext"`; `compilerOptions.moduleResolution` is `"NodeNext"`; `compilerOptions.types` is `["node"]`; `include` is `["src/**/*.ts"]`; `exclude` includes `"src/**/*.test.ts"` (Phase 09 CR-07 hygiene); the file parses as valid JSON.
    - Test 4 (CR-07 guard): `apps/achilles-cli/src/.gitignore` exists; the file body contains the literal lines `*.js`, `*.d.ts`, `*.js.map`, `*.d.ts.map` (one per line; comment header optional).
    - Test 5 (workspace alias): `tsconfig.base.json`'s `compilerOptions.paths` contains a key whose string form is `achilles-cli` mapping to `["apps/achilles-cli/src/cli.ts"]` AND a key `achilles-cli/*` mapping to `["apps/achilles-cli/src/*"]`. The existing `@achilles/*` and `@codex-mobile/*` entries remain unchanged.
    - Test 6 (vitest project): `vitest.workspace.ts` exports a workspace config that includes a project entry whose `test.name` is `"phase-13-unit"`; that project's `test.include` array contains the glob `"apps/achilles-cli/src/**/*.test.ts"`; the project sets `environment: "node"` and `passWithNoTests: true`; the existing phase-01/03/09/10/11/12 projects remain unchanged.
    - Test 7 (root scripts): root `package.json` `scripts` includes a `test:phase-13:quick` key whose value runs `vitest run --project phase-13-unit`.
    - Test 8 (README hygiene): `apps/achilles-cli/README.md` exists; the body opens with an H1 `# achilles` line, includes a `## Install` section listing the literal command `npm install -g achilles`, includes a `## Commands` section enumerating the four commands (`launch` / `install-skill` / `init` / `transcripts purge`) with one-line descriptions; contains NO emojis (literal codepoint regex check covers U+1F000-U+1FFFF and U+2600-U+27FF) per CLAUDE.md global.
  </behavior>
  <action>
    Create the `apps/achilles-cli/` directory with the four files (package.json, tsconfig.json, src/.gitignore, README.md). Mirror the @achilles/voice-stt package shape but with `"name": "achilles"` (UNSCOPED — this is the public top-level package that resolves under `npm install -g achilles`), `"private": false`, and the additional `"bin"` and `"description"` fields. Pin `commander` to `"13.1.0"` (exact version per `<context>` interfaces — research locked commander 13.x as the small, well-maintained CLI parser; exact pin is the workspace convention for vendor packages). Declare `@achilles/achilles-skill` as a workspace dependency at version `"0.1.0"` (npm workspaces resolves this to a tarball reference at publish time so the skill body is inlined into the published achilles tarball — addresses Pitfall #14 monorepo workspace symlinks broken under `npm install -g`).

    The tsconfig extends `../../tsconfig.base.json`. Set `module: "NodeNext"` and `moduleResolution: "NodeNext"` (the achilles-cli runs in Node, not Electron; matches the @achilles/voice-tts main-process pattern). Set `outDir: "dist"` (npm package tradition) instead of the Electron app's `outDir: "out"`. The `exclude` array MUST drop `src/**/*.test.ts` (Phase 09 CR-07 hygiene — tsc must not see the tests).

    The src/.gitignore mirrors apps/achilles/src/.gitignore verbatim: a one-line comment header explaining the guard, followed by `*.js`, `*.d.ts`, `*.js.map`, `*.d.ts.map`, and `*.jsx` ignore lines.

    Update `tsconfig.base.json`: add two new entries to `compilerOptions.paths`. The literal `"achilles-cli"` maps to `["apps/achilles-cli/src/cli.ts"]`; the glob `"achilles-cli/*"` maps to `["apps/achilles-cli/src/*"]`. Place the new entries AFTER the existing `@achilles/voice-tts/*` block in the same dictionary; preserve existing entries verbatim.

    Update `vitest.workspace.ts`: append a new project entry to the `defineWorkspace([])` array (after `phase-12-unit`) with `test.name: "phase-13-unit"`, `test.include: ["apps/achilles-cli/src/**/*.test.ts"]`, `test.environment: "node"`, `test.passWithNoTests: true`, and `resolve: { alias: workspaceAlias }`. Do NOT touch any other existing project entry. The header comment block at the bottom of the file is augmented with a one-line "v1.2 phase-13 addition" note matching the existing phase-9..12 pattern.

    Update the root `package.json` scripts block: add `"test:phase-13:quick": "vitest run --project phase-13-unit"` after the existing `test:phase-11:full` entry. Do NOT bump version, do NOT touch workspaces glob (the existing `apps/*` already covers the new directory).

    The README is intentionally minimal — Plans 13-02 / 13-03 / 13-04 will extend the Commands section with specifics as those features land. The H1 reads `# achilles`. The body has three sections: `## Install` (one fenced bash block with `npm install -g achilles`), `## Commands` (a four-row bulleted list naming launch, install-skill, init, transcripts purge), `## Privacy` (one paragraph noting that the ElevenLabs API key lives only in the main-process OS keystore and that no audio leaves the developer's machine except to ElevenLabs — links the README to SAFE-01 and SAFE-03). NO emojis anywhere per CLAUDE.md global.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff && node -e "const p = require('./apps/achilles-cli/package.json'); if (p.name !== 'achilles') process.exit(1); if (p.private !== false) process.exit(1); if (p.publishConfig?.access !== 'public') process.exit(1); if (p.bin?.achilles !== './dist/cli.js') process.exit(1); if (p.dependencies?.commander !== '13.1.0') process.exit(1); if (p.dependencies?.['@achilles/achilles-skill'] !== '0.1.0') process.exit(1); console.log('OK');" && node -e "const t = require('./apps/achilles-cli/tsconfig.json'); if (t.extends !== '../../tsconfig.base.json') process.exit(1); if (t.compilerOptions.rootDir !== 'src') process.exit(1); if (!t.exclude?.includes('src/**/*.test.ts')) process.exit(1); console.log('OK');" && grep -q 'achilles-cli' tsconfig.base.json && grep -q 'phase-13-unit' vitest.workspace.ts && grep -q 'test:phase-13:quick' package.json && grep -v '^#' apps/achilles-cli/src/.gitignore | grep -q '\*\.d\.ts' && grep -q '^# achilles' apps/achilles-cli/README.md</automated>
  </verify>
  <done>
    apps/achilles-cli/package.json, tsconfig.json, src/.gitignore, README.md exist with the contracts above. tsconfig.base.json + vitest.workspace.ts + root package.json each have exactly one new entry. Running `npx vitest run --project phase-13-unit` from the repo root passes with zero tests (the project resolves; the include glob has no matches yet because Task 2 is the first to add a test). `npm run typecheck --workspace apps/achilles-cli` exits 0. No `.js` or `.d.ts` files exist under apps/achilles-cli/src/. No emoji codepoints (U+1F000-U+1FFFF or U+2600-U+27FF) appear in any new file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: electron-binary-locator + launch command + transcripts purge stub + cli.ts commander wiring</name>
  <files>
    apps/achilles-cli/src/electron-binary-locator.ts,
    apps/achilles-cli/src/electron-binary-locator.test.ts,
    apps/achilles-cli/src/commands/launch.ts,
    apps/achilles-cli/src/commands/launch.test.ts,
    apps/achilles-cli/src/commands/transcripts.ts,
    apps/achilles-cli/src/commands/transcripts.test.ts,
    apps/achilles-cli/src/cli.ts,
    apps/achilles-cli/src/cli.test.ts
  </files>
  <behavior>
    - Test L1 (locator: darwin path): with `platform: 'darwin'` and an injected `fileExistsAt: (p) => p === '/pkg/dist/Achilles.app/Contents/MacOS/Achilles'`, `locateElectronBinary({ pkgRoot: '/pkg', platform: 'darwin', fileExistsAt })` returns the absolute path `'/pkg/dist/Achilles.app/Contents/MacOS/Achilles'`.
    - Test L2 (locator: win32 path): with `platform: 'win32'` and an injected `fileExistsAt` truthy only for `'/pkg/dist/Achilles.exe'`, the function returns that path.
    - Test L3 (locator: linux path): with `platform: 'linux'` and `fileExistsAt` truthy only for `'/pkg/dist/linux/achilles'`, the function returns that path.
    - Test L4 (locator: missing binary throws typed error): with `fileExistsAt: () => false`, the function throws `ElectronBinaryMissingError`; the error message names the platform AND the expected path; `err instanceof Error` is true.
    - Test L5 (locator: unknown platform throws): with `platform: 'aix'`, the function throws an Error whose message contains "Unsupported platform" and names the value `'aix'`.
    - Test LC1 (launch happy path): given an injected `locate: () => '/pkg/dist/Achilles.app/Contents/MacOS/Achilles'`, an injected `spawn: (cmd, args, opts) => { spawnCalls.push({cmd, args, opts}); return { unref: vi.fn(), pid: 4242 }; }`, and an injected `processExitImpl: (code) => exitCode = code`, calling `launchCommand({ locate, spawn, processExitImpl, env: {} })` captures one spawn call whose `cmd` equals the path returned by locate, `args` is `[]`, `opts.detached` is `true`, `opts.stdio` is the string `'ignore'`, the spawned child's `unref` method was called, and the function does NOT call processExitImpl (the CLI exits 0 by returning, not by explicit exit).
    - Test LC2 (launch: locator throws): when `locate` throws `ElectronBinaryMissingError`, `launchCommand` calls `processExitImpl(1)` AND writes a diagnostic to an injected `stderr.write` seam that contains the substring `"Electron binary not found"` and the platform name; NO spawn call is made.
    - Test LC3 (launch: env passthrough): when `launchCommand` is invoked with `env: { ACHILLES_MODE: 'init', PATH: '/usr/bin' }`, the captured spawn call's `opts.env` equals exactly that object (env passthrough is the contract Plan 13-03 uses to route `init` mode to the wizard).
    - Test T1 (transcripts purge stub): `transcriptsCommand('purge', { stdout, processExitImpl })` writes a message to the stdout seam containing the literal substring "not yet implemented" AND the literal substring "Phase 14"; calls `processExitImpl(0)`; performs NO filesystem operation (an injected `fs.rm` spy is never called).
    - Test T2 (transcripts unknown subcommand): `transcriptsCommand('save', { stdout, processExitImpl })` writes a message containing the substring "Unknown subcommand" to stdout AND calls `processExitImpl(2)` (commander-style misuse exit code).
    - Test C1 (cli: parses --version): given argv `['achilles', '--version']` and an injected stdout seam, `runCli({ argv: ['achilles', '--version'], stdout, stderr, processExitImpl, deps })` writes the version string from package.json AND calls `processExitImpl(0)`. The version string is dynamically read from the package.json at module load (NOT hardcoded into cli.ts).
    - Test C2 (cli: --help lists four commands): given argv `['achilles', '--help']`, the captured stdout contains the substrings `"launch"`, `"install-skill"`, `"init"`, AND `"transcripts"`.
    - Test C3 (cli: no-args defaults to launch): given argv `['achilles']` (no command), `runCli` routes to `launchCommand` (an injected `launchCommand` spy is called exactly once with no positional args).
    - Test C4 (cli: explicit `launch` routes to launchCommand): given argv `['achilles', 'launch']`, the injected `launchCommand` spy is called exactly once.
    - Test C5 (cli: `install-skill` routes to placeholder): given argv `['achilles', 'install-skill']`, an injected `installSkillCommand` spy is called exactly once. The actual install-skill implementation is owned by Plan 13-02; this plan ships the placeholder import + commander wiring only.
    - Test C6 (cli: `init` routes to placeholder): given argv `['achilles', 'init']`, an injected `initCommand` spy is called exactly once. Plan 13-03 owns the real implementation.
    - Test C7 (cli: `transcripts purge` routes correctly): given argv `['achilles', 'transcripts', 'purge']`, the injected `transcriptsCommand` spy is called exactly once with the first positional argument equal to `'purge'`.
    - Test C8 (cli: unknown command exits 1): given argv `['achilles', 'nonexistent']`, the captured stderr contains the substring "unknown command" or "Unknown command" (commander's default error string) AND `processExitImpl(1)` was called.
    - Test C9 (cli: shebang): `apps/achilles-cli/src/cli.ts` starts with `#!/usr/bin/env node` (the npm-published bin needs the shebang for Linux/macOS; Windows shim handles this separately).
  </behavior>
  <action>
    Implement `electron-binary-locator.ts` as a pure function. Export `locateElectronBinary(opts: { pkgRoot: string; platform: NodeJS.Platform; fileExistsAt: (path: string) => boolean }): string` and the typed error class `ElectronBinaryMissingError extends Error`. Inside the function, map `'darwin' | 'win32' | 'linux'` to the three documented relative paths under `dist/` (per CONTEXT.md "Locates the bundled Electron binary by"): macOS uses `dist/Achilles.app/Contents/MacOS/Achilles`, Windows uses `dist/Achilles.exe`, Linux uses `dist/linux/achilles`. Resolve to absolute via `path.posix.join`/`path.win32.join` keyed by the platform string. Call `fileExistsAt(absolutePath)`; if false, throw `new ElectronBinaryMissingError(message)` where the message names BOTH the platform AND the expected path. Throw a plain `Error` with `"Unsupported platform: <value>"` for unknown platforms. The default production-runtime resolves `pkgRoot` via `import.meta.url` + `fileURLToPath` walked up two directories (`dist/cli.js` → `..` → `..` lands at package root; the same walk that packages/achilles-skill/src/index.ts uses); the seam stays injected so tests run without filesystem.

    Implement `commands/launch.ts` as `launchCommand({ locate, spawn, processExitImpl, stderr, env }): void`. The contract: call `locate()` to obtain the binary path; if it throws `ElectronBinaryMissingError`, write a stderr line `"[achilles] Electron binary not found for platform <platform> at <expected-path>. Did you run `npm install`?"` and call `processExitImpl(1)`. On success, call `spawn(binaryPath, [], { detached: true, stdio: 'ignore', env })`; immediately call `child.unref()` so the parent (the CLI) does not wait on the GUI; return without calling processExitImpl (Node exits naturally with code 0 after the event loop drains). The seams are typed as `LaunchDeps` so the test file injects fakes without touching `node:child_process`. The real `spawn` is sourced from `node:child_process` and bound at module load; the real `processExitImpl` is `(code) => process.exit(code)`.

    Implement `commands/transcripts.ts` as `transcriptsCommand(subcommand: string, { stdout, processExitImpl }): void`. The contract: when subcommand is `'purge'`, write a stdout line `"[achilles] transcripts purge — not yet implemented (Phase 14 — Hardening, Privacy, Resilience)."` and call `processExitImpl(0)`. When subcommand is any other string, write `"[achilles] Unknown subcommand: <value>. Supported: purge."` to stdout (NOT stderr — invalid command usage is informational at this stage, not an error) and call `processExitImpl(2)` (commander misuse code). The implementation MUST NOT touch the filesystem (no `fs.*` import in this file); a future Phase 14 patch will replace the body with the real purge.

    Implement `cli.ts` as the commander entrypoint. Open with the shebang `#!/usr/bin/env node` on line 1 (no blank lines before). Import `Command` from `commander`. Import `launchCommand`, `installSkillCommand` (the Plan-13-02 hook — Plan 13-01 ships a placeholder import + ALSO ships a one-line placeholder `commands/install-skill.ts` that exports a function which writes `"[achilles] install-skill: placeholder — Plan 13-02 implements this"` to stdout and exits 1. Plan 13-02 REPLACES the placeholder body with the real implementation), `initCommand` (Plan-13-03 hook — same placeholder pattern), and `transcriptsCommand`. Read the version dynamically from package.json via `import { readFileSync } from 'node:fs'` + `JSON.parse` resolved from `import.meta.url`; this avoids drift between cli.ts and package.json. Build the commander program:
      - `.name('achilles')`
      - `.description('Achilles voice companion for Claude Code')`
      - `.version(version)`
      - `.command('launch').description('Open the floating Achilles UI').action(() => launchCommand(deps))`
      - `.command('install-skill').description('Install the Achilles Claude Code skill').option('--force', 'Overwrite existing skill destination').action((opts) => installSkillCommand({ force: opts.force ?? false, ...deps }))`
      - `.command('init').description('Run the first-run wizard (API key, mic permission, smoke test)').action(() => initCommand(deps))`
      - `.command('transcripts <subcommand>').description('Manage transcript persistence (Phase 14 — currently `purge` is a stub)').action((sub) => transcriptsCommand(sub, deps))`
    The default action (no subcommand) MUST route to `launchCommand` — commander supports this via `.action()` at the program level. Export `runCli({ argv, stdout, stderr, processExitImpl, deps })` as the testable seam; the real `process.argv` / `process.stdout.write` / `process.stderr.write` / `process.exit` are bound only when `cli.ts` is executed directly (the bottom of the file checks `import.meta.url` against the resolved entry path and calls `runCli({ argv: process.argv, stdout: process.stdout, stderr: process.stderr, processExitImpl: process.exit, deps })`).

    Place the placeholder install-skill and init commands at `apps/achilles-cli/src/commands/install-skill.ts` and `apps/achilles-cli/src/commands/init.ts`. Each is a 5-line file: a single exported function that writes a placeholder message to stdout and exits with code 1 marking the command as "not yet wired (this plan: 13-01)". Plan 13-02 and 13-03 each REPLACE their respective placeholder file with the full implementation; the import path in cli.ts does NOT change between waves.

    Every public function is typed; named exports only (no default exports — workspace convention per .planning/STATE.md decisions). The cli.ts file is the only place that touches `commander`; all commands are pure functions over injected deps so tests run without commander. NO console.log; use the injected stdout/stderr seams. NO emoji codepoints in any source string, log message, or comment.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff && npx vitest run --project phase-13-unit apps/achilles-cli/src/electron-binary-locator.test.ts apps/achilles-cli/src/commands/launch.test.ts apps/achilles-cli/src/commands/transcripts.test.ts apps/achilles-cli/src/cli.test.ts && head -1 apps/achilles-cli/src/cli.ts | grep -q '^#!/usr/bin/env node' && grep -v '^//' apps/achilles-cli/src/commands/transcripts.ts | grep -v '^\s*\*' | grep -c "require(\\?['\"]node:fs\\?['\"]\\?\\|from ['\"]node:fs['\"]" | grep -q '^0$' && npm run typecheck --workspace apps/achilles-cli && find apps/achilles-cli/src -name '*.js' -o -name '*.d.ts' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>
    All tests in L1-L5, LC1-LC3, T1-T2, C1-C9 pass. cli.ts opens with the shebang on line 1. transcripts.ts contains zero imports from node:fs (verified by grep). `npm run typecheck --workspace apps/achilles-cli` exits 0. No mis-built `.js` / `.d.ts` files exist under apps/achilles-cli/src. The achilles-cli `phase-13-unit` test count is at least 21 (L1-L5 + LC1-LC3 + T1-T2 + C1-C9 = 21 minimum).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal → achilles CLI | The user's shell invokes the global `achilles` binary; argv crosses the trust boundary |
| achilles CLI → Electron binary | The CLI spawns the bundled Electron app as a detached child; env vars cross the boundary |
| npm registry → user machine | At `npm install -g achilles`, the tarball contents land in the user's global node_modules — untrusted-publisher attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | published tarball — modified `dist/cli.js` shim or substituted Electron binary | mitigate | Plan 13-04 owns the release-time tarball scan + source-of-truth diff check; Plan 13-01 ensures the build path is reproducible (rm -rf dist; tsc -p tsconfig.json) so a tampered build is detectable by comparing the tarball checksum to a known-good `tsc` output |
| T-13-02 | Information Disclosure | `achilles --help` text or error messages leaking secret env vars | mitigate | Plan 13-01: the cli.ts command surfaces NEVER read or log env values; error messages name only platform identifiers and expected paths (locator throws). Verified by the test C8 (unknown command path uses stderr but writes only the user-supplied command name) and LC2 (launch failure names path, not env contents) |
| T-13-03 | Tampering | npm package installs (the tree under apps/achilles-cli/node_modules) | mitigate | Only one production dependency added in this plan (`commander@13.1.0`, exact pin). Workspace dep `@achilles/achilles-skill@0.1.0` is private + resolves to the local source until the publish-time pack step inlines it. RESEARCH.md SUMMARY lists commander as a HIGH-confidence well-maintained CLI parser; the version is exact-pinned, not caret-range, so a malicious 13.1.1 cannot transparently land |
| T-13-04 | Denial of Service | a malicious or broken Electron binary causes the spawned child to consume CPU forever, leaving the user's terminal with a dangling background process | accept | launchCommand calls child.unref() and exits the CLI; the user retains full terminal control. The Electron-side window-lifetime is owned by Phase 11 (close button + dock hide); a runaway Electron process is killed by the OS task manager. Not Phase 13's problem to enforce. |
| T-13-05 | Elevation of Privilege | `npm install -g achilles` on Windows requesting admin elevation due to shim placement | mitigate | package.json's `bin: { achilles: './dist/cli.js' }` uses the npm-generated `.cmd` shim mechanism (NOT a hand-rolled `.ps1`); npm 9+ places the shim under the user's npm prefix without admin elevation (Pitfall #13). The cli.ts shebang is `#!/usr/bin/env node` so Linux/macOS resolve via PATH without root |
| T-13-06 | Spoofing | a typosquat package `achiless` or similar published to npm pretending to be Achilles | accept | Out of Plan 13-01 scope. The locked name `achilles` is the canonical publish target. Mitigation belongs to the publisher (the human releasing v1.2). Plan 13-04 owns the package-legitimacy gate audit before publish. |

Package legitimacy: commander@13.1.0 is on npm (`npmjs.com/package/commander`); maintained by TJ Holowaychuk + maintainers; weekly downloads >150M; verified to be a legitimate, widely-used CLI parser. No `[ASSUMED]` / `[SUS]` flag from RESEARCH.md PITFALLS audit. No human checkpoint required.
</threat_model>

<verification>
- `npx vitest run --project phase-13-unit` exits 0 with at least 21 passing tests (L1-L5 + LC1-LC3 + T1-T2 + C1-C9).
- `npm run typecheck --workspace apps/achilles-cli` exits 0.
- `find apps/achilles-cli/src -name '*.js' -o -name '*.d.ts' -o -name '*.js.map' -o -name '*.d.ts.map'` is empty (CR-07 hygiene).
- `head -1 apps/achilles-cli/src/cli.ts` is exactly `#!/usr/bin/env node`.
- `grep -v '^#' apps/achilles-cli/src/.gitignore | grep -c '\*\.d\.ts'` is `>= 1`.
- `node -e "const p = require('./apps/achilles-cli/package.json'); if (p.name !== 'achilles' || p.private !== false || p.publishConfig?.access !== 'public' || p.bin?.achilles !== './dist/cli.js') process.exit(1);"` exits 0.
- `grep -c 'phase-13-unit' vitest.workspace.ts` is `>= 1`.
- `grep -c 'achilles-cli' tsconfig.base.json` is `>= 2` (the literal alias + the glob alias).
- `grep -c 'test:phase-13:quick' package.json` is `>= 1`.
- `cd apps/achilles-cli && npm run build` succeeds AND produces `dist/cli.js` with the shebang preserved on line 1 (tsc preserves shebangs when present in the source).
- No emoji codepoint in U+1F000-U+1FFFF or U+2600-U+27FF appears in any file modified by this plan (grep `-P "[\\x{1F000}-\\x{1FFFF}\\x{2600}-\\x{27FF}]"` returns nothing).
- `apps/achilles-cli/src/commands/install-skill.ts` and `apps/achilles-cli/src/commands/init.ts` exist as 5-line placeholder files (verified by line count); Plan 13-02 / 13-03 REPLACE these files in their respective tasks.
</verification>

<success_criteria>
1. The achilles-cli package exists at the publish-ready shape with `name: "achilles"`, `bin: { achilles: "./dist/cli.js" }`, `publishConfig.access: "public"`, `commander@13.1.0` as the only added production dep.
2. The commander entrypoint routes the four documented commands (launch / install-skill / init / transcripts) and the no-arg default (defaults to launch).
3. The Electron binary locator returns absolute paths for darwin / win32 / linux and throws a typed `ElectronBinaryMissingError` when the binary is absent (Plan 13-04 wires the locator into the verifiable `npm pack` flow).
4. The `transcripts purge` Phase-14-deferred stub exits 0 with a clear deferred message; performs NO filesystem mutation.
5. The workspace plumbing (tsconfig.base.json + vitest.workspace.ts + root package.json) integrates the new package without breaking any of the existing phase-01/03/09/10/11/12 projects.
6. CR-07 + CLAUDE.md global hygiene: src/.gitignore guards mis-built artefacts; no `.js` / `.d.ts` under src/; no emojis anywhere.
</success_criteria>

<output>
Create `.planning/phases/13-distribution-npm-cli-skill-installers/13-01-SUMMARY.md` when done — list files created, files modified, test counts (target ≥ 21 new tests + zero regressions in phase-01/03/09/10/11/12), the typecheck result, the CR-07 grep results, and any deviations from the plan with rationale.
</output>
