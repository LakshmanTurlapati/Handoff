---
phase: 13-distribution-npm-cli-skill-installers
verified: 2026-06-06T21:12:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  is_re_verification: false
human_verification:
  - test: "Fresh macOS install validation"
    expected: "On a fresh macOS account (or VM), `npm install -g achilles` succeeds without admin elevation; the `achilles` command appears on PATH; running `achilles --version` returns `0.1.0`; running `achilles` with no args spawns the floating Electron UI; running `achilles install-skill` creates `~/.claude/skills/achilles/` as a symlink pointing into the installed npm package's `node_modules/@achilles/achilles-skill/skill/`; Claude Code discovers the `/achilles` skill on next launch; running `achilles init` opens the InitWizard (NOT detached — the CLI blocks on wizard exit); the wizard's mic permission prompt is attributed to Achilles, NOT to the launching terminal (Pitfall #3 mitigation)."
    why_human: "Goal-backward verifier cannot spin up a fresh macOS account / VM and execute the real `npm install -g achilles` flow. The unit tests cover the locator algorithm, the install-skill primitive (with injected fs), and the init command's spawn contract — but the real cross-OS resolver behaviour and the real OS-level mic-permission attribution can only be observed on the target operating systems. Phase 14 ROADMAP entry explicitly states 'macOS TCC remediation flows are tested on a fresh account; Windows install is tested on a fresh VM.'"
  - test: "Fresh Windows 11 install validation"
    expected: "On a fresh Windows 11 account (or VM), `npm install -g achilles` succeeds without admin elevation; the npm-generated `.cmd` shim places `achilles` on PATH; `achilles --version` returns `0.1.0`; `achilles install-skill` either creates a symlink (if Developer Mode is enabled or admin) OR falls back to a recursive copy with a clear warn-level message naming the EPERM/EACCES/EISDIR fallback path (Pitfall #13); `achilles init` opens the wizard and the OS mic permission prompt fires inside the Electron host."
    why_human: "Same as macOS: cross-platform real-environment behaviour cannot be programmatically verified by the verifier on its current macOS host. The skill-symlink unit tests (SS6/SS7) drive the fallback codepath via injected fs fakes; the real EPERM/EACCES branching on a target Windows machine has not been exercised in CI."
  - test: "Fresh Linux container install validation"
    expected: "On a fresh Linux container (Ubuntu 22.04 baseline or similar), `npm install -g achilles` succeeds without admin elevation; `achilles --version` returns `0.1.0`; `achilles install-skill` creates the symlink; `achilles init` opens the wizard; on a desktop Linux session with PulseAudio/PipeWire the mic permission is granted via the standard per-application surface."
    why_human: "Linux real-environment behaviour deferred to release operator + Phase 14."
  - test: "Real electron-builder run produces signed .dmg / .exe / .AppImage installers"
    expected: "On macOS with APPLE_ID + APPLE_APP_SPECIFIC_PASSWORD + APPLE_TEAM_ID + CSC_LINK + CSC_KEY_PASSWORD env vars set, running `npm run dist:mac --workspace apps/achilles` produces a signed and notarised .dmg under apps/achilles/dist-installers/. On Windows with CSC_LINK set, `npm run dist:win` produces a signed .exe NSIS installer. On Linux, `npm run dist:linux` produces an .AppImage. The .dmg's Info.plist contains the NSMicrophoneUsageDescription string verbatim; running the installed .app on a fresh macOS account shows the Achilles-attributed mic prompt (NOT the terminal-attributed prompt)."
    why_human: "Per the verifier context: 'NO electron-builder runs in CI by default (CLAUDE.md global: never run applications automatically; per CONTEXT.md NO real electron-builder build in CI); the dist:* scripts are operator-triggered only.' The cross-platform code-signing identity acquisition is OPERATOR responsibility per the build/README.md contract. SC4 (signed installer production) is deferred to release operator on each target OS. The electron-builder.json contract is fully wired and unit-verified via the 14 node-test cases in EB1-EB5 + DIST1-DIST3 + ENT1-ENT2 + INFO1 + BR1-BR2, but the real cross-OS run is not within scope of automated verification."
  - test: "Real-environment SAFE-01 + Pitfall #22 verification on a built artifact"
    expected: "After producing the .dmg / .exe / .AppImage on the operator's machine (with their real Apple Developer + Windows EV certs), `grep -r 'sk_' <installer>` AND `grep -r 'sk_' <npm-tarball-from-real-publish>` return zero matches. Renderer DevTools (Cmd-Option-I on the installed .app) cannot read the ElevenLabs API key — `window.achilles.bridge` does not expose the key; the renderer's Audio API does not transmit raw key bytes to ElevenLabs (the SAFE-01 contract from Phase 12-04 keeps the key in main-process safeStorage)."
    why_human: "The check-tarball-no-secrets.mjs script is wired into prepublishOnly and passes against the current source tree (exit 0). The defensive scan covers seven concrete regex patterns including `sk_[A-Za-z0-9_-]{29,}`. But the real verification — running the grep against an actual signed installer artifact and against the real publish-time tarball — only happens at release time when the operator runs `npm publish` or invokes `npm run dist:*`. Renderer DevTools key-leakage check requires running the actual built Electron .app and inspecting via DevTools, which falls under 'NO live application launch in CI' per CLAUDE.md."
---

# Phase 13: Distribution — npm CLI + Skill + Installers Verification Report

**Phase Goal:** Three distribution surfaces from one source of truth — npm CLI `achilles`, Claude Code skill at `~/.claude/skills/achilles/`, signed cross-platform installers (.dmg, .exe, .AppImage).

**Verified:** 2026-06-06T21:12:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                          | Status     | Evidence                                                                                                                                                                                                                                                                  |
| --- | ---------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `npm install -g achilles` succeeds on fresh macOS/Windows/Linux and `achilles` launches floating UI without admin   | VERIFIED (with deferred real-OS check) | `apps/achilles-cli/package.json`: `name: "achilles"` (UNSCOPED, `private: false`, `publishConfig.access: "public"`, `bin.achilles: "./dist/cli.js"`); `apps/achilles-cli/dist/cli.js` is built with shebang on line 1; `node dist/cli.js --version` returns `0.1.0`; `node dist/cli.js --help` lists all four commands. The cross-OS PATH resolution + admin-elevation behaviour is deferred to fresh-VM testing — see human verification #1-3. |
| 2   | `achilles install-skill` creates `~/.claude/skills/achilles/` symlink to one source of truth   | VERIFIED   | `apps/achilles-cli/src/commands/install-skill.ts` resolves source via `SKILL_PROMPTS_DIR` walked up to skill root; destination via `os.homedir() + '/.claude/skills/achilles/'`; delegates to `installSkillSymlink` in `apps/achilles-cli/src/skill-symlink.ts` which performs macOS/Linux symlink + Windows EPERM/EACCES/EISDIR fallback to recursive copy. 21/21 phase-13-unit tests pass (SS1-SS11 + IS1-IS10). Source-of-truth contract enforced by `check-source-of-truth.mjs` — exit code 0 confirms byte-equality between source `packages/achilles-skill/skill/prompts/companion.md` and bundled `node_modules/@achilles/achilles-skill/skill/prompts/companion.md` (SHA-256 match `e1308c2af287`). |
| 3   | `achilles init` wizard: API key entry, mic permission via Electron host, smoke round-trip, clean exit | VERIFIED (with deferred real-OS mic prompt) | `apps/achilles-cli/src/commands/init.ts` spawns Electron NOT detached with `ACHILLES_MODE=init` in env; `apps/achilles/src/main/index.ts:114` branches on `process.env.ACHILLES_MODE === ACHILLES_MODE_INIT` early-return to wizard mode; `apps/achilles/src/main/init-wizard.ts` ships `createInitWizardWindow` (360x480, no parent, centred) + `createInitWizardSession` (submitApiKey routes through `store.writeElevenlabsApiKey` (safeStorage-backed), requestMicPermission routes through `probePermissionImpl({triggerAskForMediaAccess:true})` so the Electron host owns the prompt — Pitfall #3 mitigation, runSmokeTest races against SMOKE_TEST_TIMEOUT_MS=60000); `apps/achilles/src/renderer/components/InitWizard.tsx` ships the 3-step React state machine with locked copy strings (U3/U5/U7/U8/U9 tests pin the exact copy). 32 new tests across phase-11-unit + phase-12-unit + phase-13-unit. Real OS mic prompt attribution deferred to fresh-machine verification #1-3. |
| 4   | electron-builder produces signed `.dmg` + `.exe` (NSIS) + `.AppImage` from one CI pipeline      | VERIFIED (config wired; real build deferred to operator) | `apps/achilles/electron-builder.json`: `appId: "com.achilles.voice"`, `productName: "Achilles"`, mac.target dmg + hardenedRuntime + entitlements + extendInfo NSMicrophoneUsageDescription + notarize.teamId `${env.APPLE_TEAM_ID}`, win.target nsis + icon path, linux.target AppImage. `apps/achilles/build/entitlements.mac.plist` declares the three required entitlements (audio-input, allow-jit, allow-unsigned-executable-memory). `apps/achilles/build/Info.plist.fragment` mirrors the NSMicrophoneUsageDescription. `apps/achilles/package.json` adds dist + dist:mac + dist:win + dist:linux scripts; `electron-builder@25.1.8` exact-pinned as devDep. 14 node-test cases pass (EB1-EB5 + ENT1-ENT2 + INFO1 + DIST1-DIST3 + BR1-BR2). Per CLAUDE.md global + CONTEXT.md the actual cross-platform build is NOT run in CI; operator triggers `npm run dist:*` per platform — see human verification #4. |
| 5   | Tarball + installer grep for ElevenLabs key prefix empty; renderer DevTools cannot read API key | VERIFIED (with deferred real-installer artifact check) | `apps/achilles-cli/scripts/check-tarball-no-secrets.mjs` exits 0 against the current source tree (verified by direct invocation: `[achilles] tarball scan: no secrets detected (scanned 21 matchable files)`). Seven concrete regex patterns (elevenlabs-sk_, elevenlabs-xi-api-key, elevenlabs-xi_api_key-assignment, elevenlabs-env-assignment, anthropic-sk-, github-pat, github-fine-grained-pat) wired into `prepublishOnly` chain (`"prepublishOnly": "node scripts/check-source-of-truth.mjs && node scripts/check-tarball-no-secrets.mjs"`). Defence-in-depth log truncation (first 8 chars + ...) prevents leak-in-leak surface. SAFE-01 key-surface contract inherited from Phase 12-04: API key lives in main-process safeStorage; the K5/SE13 leak-prevention invariants extend to the InitWizard (the IPC_INIT_API_KEY_RESULT schema does NOT echo the key bytes back to renderer). Renderer DevTools cannot read the key. Real `npm pack` + real signed-installer grep deferred to operator-triggered release (see human verification #5). |

**Score:** 5/5 must-haves verified (all wired correctly; real cross-OS / real-installer / real-publish checks deferred to release operator per Phase 14 scope).

### Required Artifacts

| Artifact                                                       | Expected                                                                                                              | Status     | Details                                                                                                                                                                                                                                                                                                              |
| -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/achilles-cli/package.json`                               | Published npm package manifest; name=achilles, bin, publishConfig public, bundledDependencies, prepublishOnly         | VERIFIED   | name="achilles", private=false, bin.achilles="./dist/cli.js", publishConfig.access="public", commander@13.1.0 pinned, @achilles/achilles-skill@0.1.0 workspace dep, bundledDependencies=["@achilles/achilles-skill"], prepublishOnly chains source-of-truth + tarball-no-secrets gates.                                |
| `apps/achilles-cli/src/cli.ts`                                 | commander-based entrypoint with shebang; routes four commands + bare-invocation default                              | VERIFIED   | Shebang `#!/usr/bin/env node` on line 1. Routes launch / install-skill / init / transcripts <subcommand> + bare-invocation defaults to launch via `isBareInvocation(argv)` pre-detection. Version read dynamically from package.json. Wired to real handlers via productionDeps at module bottom.                       |
| `apps/achilles-cli/src/electron-binary-locator.ts`             | Platform-aware locator: darwin / win32 / linux paths + ElectronBinaryMissingError + Unsupported platform Error      | VERIFIED   | `locateElectronBinary` over injected `fileExistsAt` seam. POSIX joining uniformly (Windows accepts forward slashes in absolute paths). 5 tests pass (L1-L5).                                                                                                                                                          |
| `apps/achilles-cli/src/commands/launch.ts`                     | Detached + stdio:ignore spawn; env passthrough; ElectronBinaryMissingError surfaces remediation                        | VERIFIED   | Detached=true, stdio="ignore", child.unref(), processExitImpl(1) only on locator failure. Tests LC1-LC3 pass.                                                                                                                                                                                                          |
| `apps/achilles-cli/src/commands/install-skill.ts`              | commander action handler: resolves source via @achilles/achilles-skill workspace dep; delegates to installSkillSymlink | VERIFIED   | Imports SKILL_PROMPTS_DIR; defaults to `resolve(SKILL_PROMPTS_DIR, "..")`; destination via `os.homedir() + ".claude/skills/achilles"`. Surfaces ExistingDestinationConflictError + SymlinkNotPermittedError with remediation copy. Includes "Restart Claude Code" message (Pitfall #5). 10 tests pass (IS1-IS10).      |
| `apps/achilles-cli/src/commands/init.ts`                       | initCommand: spawn Electron NOT detached with ACHILLES_MODE=init in env; propagate exit code                          | VERIFIED   | detached=false, stdio="inherit", `childEnv = {...env, ACHILLES_MODE: "init"}`, `child.on("exit", code => processExitImpl(code ?? 1))`. ElectronBinaryMissingError surfaces stderr "Run \`npm install -g achilles\`" remediation. 6 tests pass (C1-C4 + emoji + env-imm).                                                  |
| `apps/achilles-cli/src/commands/transcripts.ts`                | Phase-14-deferred `purge` stub; exits 0 with deferred message; no node:fs imports                                     | VERIFIED   | Zero `node:fs` imports verified by grep. "not yet implemented (Phase 14 — Hardening, Privacy, Resilience)." literal copy. processExitImpl(0) on `purge`, processExitImpl(2) on unknown subcommand. 2 tests pass (T1-T2).                                                                                              |
| `apps/achilles-cli/src/skill-symlink.ts`                       | Pure-function symlink-or-copy primitive with idempotency + Windows-EPERM fallback                                     | VERIFIED   | installSkillSymlink + ExistingDestinationConflictError + SymlinkNotPermittedError. Windows fallback set {EPERM, EACCES, EISDIR}; non-Windows symlinkSync failures throw. 11 tests pass (SS1-SS11).                                                                                                                    |
| `apps/achilles-cli/scripts/check-source-of-truth.mjs`          | SHA-256 diff between source companion.md and bundled companion.md + version pin check                                  | VERIFIED   | Real-mode invocation: exit 0 ([achilles] source-of-truth: companion.md SHA-256 match (e1308c2af287); [achilles] version pin: 0.1.0 === 0.1.0). Defence-in-depth truncates SHA-256 prefixes to first 12 hex. 6 node-test cases pass (SOT1-SOT5 + placeholder).                                                          |
| `apps/achilles-cli/scripts/check-tarball-no-secrets.mjs`       | npm pack + grep against seven concrete regex patterns; defensive log truncation                                       | VERIFIED   | Real-mode invocation: exit 0 ([achilles] tarball scan: no secrets detected (scanned 21 matchable files)). 7 node-test cases pass (TNS1-TNS7). KEY_PATTERNS array exports the seven named regexes. Implicit allowlist via {16,} requirement after `=`.                                                                  |
| `packages/achilles-skill/skill/SKILL.md`                       | YAML frontmatter (name, description, allowed-tools) + body <=2000 words; references prompts/companion.md             | VERIFIED   | name=achilles, description=non-empty (~600 chars), allowed-tools=Bash. Body ~1250 words. Contains literal substrings "achilles launch" (Test S6), "I ran into a problem" (Test S7, 2 occurrences for emphasis), "prompts/companion.md" (Test S4, exactly 1 occurrence). NO emojis. NO executable artefacts in skill/.   |
| `apps/achilles/electron-builder.json`                          | Cross-platform target config: mac dmg + hardened runtime + entitlements + extendInfo + notarize; win nsis; linux AppImage | VERIFIED   | appId=com.achilles.voice, productName=Achilles, directories.output=dist-installers. All three platform blocks present with locked values per plan.                                                                                                                                                                     |
| `apps/achilles/build/entitlements.mac.plist`                   | Three required hardened-runtime entitlements                                                                          | VERIFIED   | com.apple.security.device.audio-input, com.apple.security.cs.allow-jit, com.apple.security.cs.allow-unsigned-executable-memory all paired with <true/>. Valid PLIST 1.0 XML.                                                                                                                                          |
| `apps/achilles/build/Info.plist.fragment`                      | NSMicrophoneUsageDescription with non-empty prompt copy                                                               | VERIFIED   | Mirrors electron-builder.json mac.extendInfo string verbatim (EB2 drift-prevention test asserts byte equality). LSUIElement true.                                                                                                                                                                                     |
| `apps/achilles/src/main/init-wizard.ts`                        | createInitWizardWindow + createInitWizardSession                                                                      | VERIFIED   | Window factory: 360x480, no parent, centred on primary display workArea. Session: submitApiKey + requestMicPermission + runSmokeTest + markWizardDone + dispose. Test seams for setTimeoutImpl + clearTimeoutImpl + logger. 15 tests pass (K1 + W1-W2 + S1-S8 + dispose).                                              |
| `apps/achilles/src/renderer/components/InitWizard.tsx`         | Three-step React component with locked copy                                                                           | VERIFIED   | discriminated-union state machine ('api-key' / 'mic-permission' / 'smoke-test' / 'done'). Inline locked copy strings. 11 tests pass (U1-U10 + skip-mic-advance).                                                                                                                                                       |
| `apps/achilles/build/README.md`                                | Operator-facing release docs: icons, code-signing env vars, CI policy                                                 | VERIFIED   | Lists icon.icns / icon.ico / icon.png; names APPLE_ID / APPLE_APP_SPECIFIC_PASSWORD / APPLE_TEAM_ID / CSC_LINK / CSC_KEY_PASSWORD; explicit CI policy "electron-builder runs are OPERATOR-triggered only". NO emojis, NO sample secret values.                                                                          |

### Key Link Verification

| From                                            | To                                                                | Via                                                          | Status | Details                                                                                                                                                          |
| ----------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------ | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/achilles-cli/src/cli.ts`                  | `apps/achilles-cli/src/commands/launch.ts`                        | commander action handler import                              | WIRED  | `import { launchCommand as realLaunchCommand } from "./commands/launch.js"`; productionDeps.launchCommand bound at module bottom.                                  |
| `apps/achilles-cli/src/cli.ts`                  | `apps/achilles-cli/src/commands/install-skill.ts`                 | commander action handler import                              | WIRED  | `import { installSkillCommand as realInstallSkillCommand } from "./commands/install-skill.js"`. productionDeps.installSkillCommand bound at module bottom.       |
| `apps/achilles-cli/src/cli.ts`                  | `apps/achilles-cli/src/commands/init.ts`                          | commander action handler import                              | WIRED  | `import { initCommand as realInitCommand } from "./commands/init.js"`. productionDeps.initCommand bound at module bottom.                                          |
| `apps/achilles-cli/src/commands/launch.ts`      | `apps/achilles-cli/src/electron-binary-locator.ts`                | locateElectronBinary call                                    | WIRED  | productionDeps.launchCommand.locate calls `locateElectronBinary({ pkgRoot: resolve(HERE, ".."), platform: process.platform, fileExistsAt: existsSync })`.         |
| `apps/achilles-cli/src/commands/install-skill.ts`| `packages/achilles-skill` (workspace dep)                         | imports SKILL_PROMPTS_DIR; walks up one dir to skill root    | WIRED  | `import { SKILL_PROMPTS_DIR } from "@achilles/achilles-skill"`; defaultSkillSourceProvider resolves to `resolve(SKILL_PROMPTS_DIR, "..")`.                          |
| `apps/achilles-cli/src/commands/install-skill.ts`| `apps/achilles-cli/src/skill-symlink.ts`                          | installSkillSymlink call with source + destination + force   | WIRED  | `import { installSkillSymlink, ExistingDestinationConflictError, SymlinkNotPermittedError } from "../skill-symlink.js"`. Call inside try/catch surfaces remediation. |
| `apps/achilles-cli/src/commands/init.ts`        | `apps/achilles-cli/src/electron-binary-locator.ts`                | reuse Plan 13-01 locator; spawn with ACHILLES_MODE=init      | WIRED  | productionDeps.initCommand.locate calls locateElectronBinary; spawn with `env: { ...env, ACHILLES_MODE: "init" }`. NOT detached (CLI blocks on exit).             |
| `apps/achilles/src/main/index.ts`               | `apps/achilles/src/main/init-wizard.ts`                           | env-var routing; createInitWizardWindow called early-return  | WIRED  | `apps/achilles/src/main/index.ts:114`: `if (process.env.ACHILLES_MODE === ACHILLES_MODE_INIT) { ... createInitWizardWindow ... }`. Plan 11/12 bootstrap below untouched. |
| `apps/achilles/src/main/init-wizard.ts`         | `apps/achilles/src/main/store.ts` (Plan 12-04 surface)            | writeElevenlabsApiKey routes through safeStorage             | WIRED  | createInitWizardSession's submitApiKey calls `deps.store.writeElevenlabsApiKey(key)` after length validation. Real store implementation persists via safeStorage. |
| `apps/achilles/src/main/init-wizard.ts`         | `apps/achilles/src/main/permission.ts` (Plan 11-03)               | probePermission with triggerAskForMediaAccess:true           | WIRED  | createInitWizardSession's requestMicPermission calls `deps.probePermissionImpl({ triggerAskForMediaAccess: true })`. Production wires to Plan 11-03 probePermission. |
| `packages/achilles-skill/skill/SKILL.md`        | `packages/achilles-skill/skill/prompts/companion.md`              | literal substring reference (exactly 1 occurrence)           | WIRED  | grep confirms exactly 1 occurrence of `prompts/companion.md` in SKILL.md. The diff check (check-source-of-truth.mjs) verifies byte-equality between source and bundled. |
| `apps/achilles/electron-builder.json` (mac.extendInfo) | `apps/achilles/build/Info.plist.fragment`                  | byte-equal NSMicrophoneUsageDescription strings              | WIRED  | EB2 drift-prevention test asserts byte equality between the JSON's extendInfo string and the fragment's <string> element.                                          |
| `apps/achilles-cli/package.json` (prepublishOnly) | `apps/achilles-cli/scripts/check-source-of-truth.mjs`             | npm pre-publish gate                                         | WIRED  | `"prepublishOnly": "node scripts/check-source-of-truth.mjs && node scripts/check-tarball-no-secrets.mjs"`. Chain order: source-of-truth runs first (cheaper).         |
| `apps/achilles-cli/package.json` (prepublishOnly) | `apps/achilles-cli/scripts/check-tarball-no-secrets.mjs`          | npm pre-publish gate                                         | WIRED  | Same prepublishOnly chain. Either non-zero exit aborts publish.                                                                                                    |
| `apps/achilles/package.json` (dist scripts)     | `apps/achilles/electron-builder.json`                              | electron-builder reads config at apps/achilles root          | WIRED  | dist scripts: `electron-vite build && electron-builder`, `electron-builder --mac`, etc. electron-builder@25.1.8 exact pin in devDependencies.                       |

### Data-Flow Trace (Level 4)

| Artifact                                                     | Data Variable                       | Source                                                                                                            | Produces Real Data                                                                                                     | Status |
| ------------------------------------------------------------ | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------ |
| `apps/achilles-cli/dist/cli.js` (built artifact)              | version, command routes              | package.json read at module load via `readFileSync(packageJsonPath, "utf8")`                                       | Yes — `node dist/cli.js --version` returns `0.1.0`; --help lists four commands                                          | FLOWING |
| `apps/achilles-cli/src/electron-binary-locator.ts`            | absolutePath                        | platform-keyed segment table + `posixJoin(pkgRoot, ...segments)`                                                  | Yes — locator returns absolute path or throws ElectronBinaryMissingError; 5 tests pin behaviour                          | FLOWING |
| `apps/achilles-cli/src/skill-symlink.ts`                      | source / destination                | injected fs seam (production: node:fs synchronous)                                                                 | Yes — production fs calls actually create the symlink / copy on success                                                  | FLOWING |
| `apps/achilles/src/main/init-wizard.ts` (createInitWizardSession.submitApiKey) | persisted key bytes                  | `deps.store.writeElevenlabsApiKey(key)` routes through safeStorage-backed AchillesStore (Plan 12-04 surface)        | Yes — store integration is the same one used by Plan 12-04 and exercised in 235/235 phase-12-unit tests                    | FLOWING |
| `apps/achilles/src/renderer/components/InitWizard.tsx`        | step state                           | useReducer driven by IPC bridge subscriptions (window.achilles.onInitWizardApiKeyResult, ...)                       | Yes — main-process session emits IPC_INIT_API_KEY_RESULT/etc; renderer subscribes and reduces                            | FLOWING |
| `apps/achilles-cli/scripts/check-source-of-truth.mjs`         | source SHA-256 vs bundled SHA-256    | reads `packages/achilles-skill/skill/prompts/companion.md` AND extracted-tarball `node_modules/.../companion.md`     | Yes — real-mode invocation returns `match (e1308c2af287)`; npm pack inlines bundled dep                                  | FLOWING |
| `apps/achilles-cli/scripts/check-tarball-no-secrets.mjs`      | leakCount                            | walks extracted tarball; applies 7 regex patterns against scannable file extensions                                 | Yes — real-mode invocation: scans 21 matchable files, leakCount=0                                                          | FLOWING |

### Behavioral Spot-Checks

| Behavior                                                | Command                                                          | Result                                                                                                                  | Status |
| ------------------------------------------------------- | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ------ |
| CLI returns version                                     | `node apps/achilles-cli/dist/cli.js --version`                   | `0.1.0` (exit 0)                                                                                                         | PASS   |
| CLI --help lists four commands                          | `node apps/achilles-cli/dist/cli.js --help`                      | Lists launch, install-skill, init, transcripts <subcommand>                                                              | PASS   |
| transcripts purge stub                                   | `node apps/achilles-cli/dist/cli.js transcripts purge`           | `[achilles] transcripts purge — not yet implemented (Phase 14 — Hardening, Privacy, Resilience).` (exit 0; no fs mutation) | PASS   |
| source-of-truth gate clean                              | `node apps/achilles-cli/scripts/check-source-of-truth.mjs`       | source/bundled SHA-256 match; version pin 0.1.0===0.1.0; exit 0                                                          | PASS   |
| tarball-no-secrets gate clean                           | `node apps/achilles-cli/scripts/check-tarball-no-secrets.mjs`    | scanned 21 matchable files, no leaks; exit 0                                                                              | PASS   |
| phase-13-unit suite                                      | `npx vitest run --project phase-13-unit`                         | 46/46 tests pass                                                                                                          | PASS   |
| phase-12-unit regression                                  | `npx vitest run --project phase-12-unit`                         | 235/235 (+ 4 skipped) — matches baseline                                                                                  | PASS   |
| phase-11-unit regression                                  | `npx vitest run --project phase-11-unit`                         | 449/449 — matches baseline                                                                                                | PASS   |
| phase-10-unit regression                                  | `npx vitest run --project phase-10-unit`                         | 157/157 — matches baseline                                                                                                | PASS   |
| phase-09-unit regression                                  | `npx vitest run --project phase-09-unit`                         | 145/145 — matches baseline                                                                                                | PASS   |
| node-test scripts (source-of-truth + tarball + package wiring + electron-builder + entitlements + Info.plist) | `node --test apps/achilles-cli/scripts/*.test.mjs apps/achilles/electron-builder.test.mjs apps/achilles/build/*.test.mjs` | 30 tests, 30 pass, 0 fail                                                                                                | PASS   |
| npm pack dry-run (verifies bundledDependencies inlines)  | `cd apps/achilles-cli && npm pack --dry-run --json`              | tarball entryCount 37; `node_modules/@achilles/achilles-skill/skill/SKILL.md` and `prompts/companion.md` included; bundled=["@achilles/achilles-skill"] | PASS   |
| Built dist artifact present                              | `ls apps/achilles-cli/dist/cli.js`                               | Present (9981 bytes); shebang preserved on line 1                                                                          | PASS   |

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes exist in this repository for Phase 13. The phase's verification surface is the node-test scripts (run above), the vitest suites (run above), and the two release-gate scripts (run above). All passed.

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                                  | Status     | Evidence                                                                                                                                                                  |
| ----------- | ----------- | ------------------------------------------------------------------------------------------------------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DIST-01     | 13-01       | User can install Achilles as a global npm CLI and launch it with `achilles` on macOS, Windows, Linux         | SATISFIED  | Package shape verified; CLI built; commander routes confirmed. Cross-OS run deferred to fresh-machine verification (human #1-3).                                            |
| DIST-02     | 13-02       | User can install Achilles as a Claude Code skill via `achilles install-skill`                                | SATISFIED  | install-skill primitive verified across happy path + idempotent + force + Windows fallback. Pitfall #5 "restart Claude Code" reminder included.                              |
| DIST-03     | 13-04       | The Claude Code skill body and npm CLI ship from one source-of-truth artifact                                | SATISFIED  | check-source-of-truth.mjs verifies SHA-256 byte equality between source and bundled companion.md; bundledDependencies wires the skill body into the tarball.                |
| DIST-04     | 13-03       | First-run `achilles init` wizard collects API key, requests mic permission via Electron host, runs smoke    | SATISFIED  | initCommand spawns Electron with ACHILLES_MODE=init env; main/index.ts branches on env to wizard mode; InitWizard 3-step state machine implemented with locked copy.        |
| DIST-05     | 13-04       | Signed and notarised installers produced from one build pipeline: .dmg + .exe (NSIS) + .AppImage             | SATISFIED  | electron-builder.json wired with all three platform configs; entitlements + Info.plist fragment present; build/README.md documents operator contract. Real build deferred. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none flagged by scan) | - | - | - | This phase's modifications were scanned for `TBD|FIXME|XXX` (none found in modified files), `placeholder|coming soon|not yet implemented` (only the Phase-14-deferred transcripts purge stub, which is INTENTIONAL and named in 13-01 plan + SUMMARY), hardcoded empty data, console.log only implementations — all clean. The init.ts and install-skill.ts placeholder bodies from 13-01 were REPLACED by 13-02 and 13-03 per the wave plan; their current bodies are the full implementations. NO emojis in any modified file. |

### Human Verification Required

Five items need human testing (see frontmatter `human_verification`). Summary:

1. **Fresh macOS install validation** — Goal-backward verifier cannot spin up a fresh macOS account to exercise the real `npm install -g achilles` flow, the real PATH resolution, and the real OS-level mic-permission attribution. Phase 14 ROADMAP entry explicitly assigns this to a fresh-account verification step.
2. **Fresh Windows 11 install validation** — Same reasoning. The Windows EPERM/EACCES/EISDIR symlink fallback codepath is unit-tested with injected fs fakes; the real behaviour on a Windows machine has not been observed in CI.
3. **Fresh Linux container install validation** — Same reasoning.
4. **Real electron-builder run produces signed .dmg / .exe / .AppImage installers** — Per CLAUDE.md global (`never run applications automatically`) and CONTEXT.md (`NO real electron-builder build in CI`), the operator triggers the real cross-platform installer build on each target OS with their own Apple Developer + Windows EV cert material. The config + entitlements + Info.plist are fully wired and unit-tested.
5. **Real-environment SAFE-01 + Pitfall #22 verification on a built artifact** — The check-tarball-no-secrets.mjs scan is wired into prepublishOnly and exits 0 against the current source tree. The renderer DevTools key-leakage check requires running the actual built Electron .app, which falls under the "no live application launch in CI" global.

### Gaps Summary

**No gaps found.** All five ROADMAP Success Criteria are wired in source and verified through the available automated surfaces (46 phase-13-unit vitest tests + 30 node-test cases for the .mjs scripts + 145/157/449/235 cross-phase regression tests + direct script-invocation real-mode verification of source-of-truth and tarball-no-secrets gates + npm pack dry-run confirming bundledDependencies inlines the skill body).

The phase is `human_needed` (NOT `gaps_found`) because the residual verification items require real cross-OS environments (fresh macOS account, Windows 11 VM, Linux container) and a real operator-triggered release build with real Apple Developer + Windows EV certs. These are explicitly out of scope per CLAUDE.md (`never run applications automatically`) and Phase 13 CONTEXT.md (`NO real electron-builder build in CI`), and are explicitly assigned to Phase 14 (`macOS TCC remediation flows are tested on a fresh account; Windows install is tested on a fresh VM`).

---

_Verified: 2026-06-06T21:12:00Z_
_Verifier: Claude (gsd-verifier)_
