---
phase: 10-claude-code-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/claude-code-bridge/package.json
  - packages/claude-code-bridge/tsconfig.json
  - packages/claude-code-bridge/src/.gitignore
  - packages/claude-code-bridge/src/constants.ts
  - packages/claude-code-bridge/src/errors.ts
  - packages/claude-code-bridge/src/errors.test.ts
  - packages/claude-code-bridge/src/event-schemas.ts
  - packages/claude-code-bridge/src/event-schemas.test.ts
  - packages/claude-code-bridge/src/extractor.ts
  - packages/claude-code-bridge/src/extractor.test.ts
  - packages/claude-code-bridge/src/types.ts
  - packages/claude-code-bridge/src/index.ts
  - tsconfig.base.json
  - vitest.workspace.ts
autonomous: true
requirements:
  - LOOP-04
must_haves:
  truths:
    - "Package @achilles/claude-code-bridge is resolvable from the monorepo (workspace alias + package.json + tsconfig in place)"
    - "ClaudeBridgeEvent typed union (9 variants) is exported from src/index.ts and validates known shapes via Zod parse"
    - "ClaudeVersionError carries actualVersion, requiredVersion, and an install-or-upgrade hint message"
    - "extractAck(text) returns the first sentence capped at 120 chars or null when no sentence terminator is present"
    - "extractSpokenSummary(text) returns the inner text of <spoken-summary>...</spoken-summary> or null when the markers are absent or unclosed"
    - "MIN_CLAUDE_VERSION, MAX_LINE_BYTES, and the LOCKED_FLAGS list are exported constants from src/constants.ts"
    - "Building this package emits dist/ artifacts only; no .js/.d.ts pollution under src/"
  artifacts:
    - path: packages/claude-code-bridge/package.json
      provides: "Workspace package manifest mirroring voice-protocol shape"
      contains: '"name": "@achilles/claude-code-bridge"'
    - path: packages/claude-code-bridge/tsconfig.json
      provides: "NodeNext TS build config excluding test files from dist"
      contains: 'src/**/*.test.ts'
    - path: packages/claude-code-bridge/src/.gitignore
      provides: "Guard against compiled-artifact pollution in src/"
      contains: '*.js'
    - path: packages/claude-code-bridge/src/constants.ts
      provides: "MIN_CLAUDE_VERSION, MAX_LINE_BYTES, LOCKED_FLAGS"
      contains: 'MIN_CLAUDE_VERSION'
    - path: packages/claude-code-bridge/src/errors.ts
      provides: "ClaudeVersionError class"
      contains: 'class ClaudeVersionError'
    - path: packages/claude-code-bridge/src/event-schemas.ts
      provides: "Zod ClaudeStreamEventSchema discriminated union"
      contains: 'z.discriminatedUnion'
    - path: packages/claude-code-bridge/src/extractor.ts
      provides: "extractAck + extractSpokenSummary pure functions"
      contains: 'extractAck'
    - path: packages/claude-code-bridge/src/types.ts
      provides: "TypeScript ClaudeBridgeEvent union + helper types"
      contains: 'ClaudeBridgeEvent'
    - path: packages/claude-code-bridge/src/index.ts
      provides: "Single barrel exporting public API"
      exports:
        - "ClaudeBridgeEvent"
        - "ClaudeVersionError"
        - "MIN_CLAUDE_VERSION"
        - "extractAck"
        - "extractSpokenSummary"
    - path: tsconfig.base.json
      provides: "Path alias resolution for @achilles/claude-code-bridge"
      contains: '@achilles/claude-code-bridge'
    - path: vitest.workspace.ts
      provides: "phase-10-unit vitest project covering the new package tests"
      contains: 'phase-10-unit'
  key_links:
    - from: packages/claude-code-bridge/src/index.ts
      to: packages/claude-code-bridge/src/event-schemas.ts
      via: "barrel re-export of Zod schema + inferred event types"
      pattern: "export \\* from \\./event-schemas"
    - from: packages/claude-code-bridge/src/index.ts
      to: packages/claude-code-bridge/src/extractor.ts
      via: "barrel re-export of extractAck + extractSpokenSummary"
      pattern: "export \\* from \\./extractor"
    - from: packages/claude-code-bridge/src/index.ts
      to: packages/claude-code-bridge/src/errors.ts
      via: "barrel re-export of ClaudeVersionError"
      pattern: "export \\* from \\./errors"
    - from: tsconfig.base.json
      to: packages/claude-code-bridge/src/index.ts
      via: "path alias mapping @achilles/claude-code-bridge -> src/index.ts"
      pattern: "@achilles/claude-code-bridge"
    - from: vitest.workspace.ts
      to: packages/claude-code-bridge/src
      via: "phase-10-unit include glob + workspaceAlias entry"
      pattern: "packages/claude-code-bridge/src"
---

<objective>
Lay down the @achilles/claude-code-bridge package scaffold and stable type surface that the Wave 2 runtime tasks will consume without renegotiating contracts. This is the interface-first task: types, constants, Zod schemas, error class, and pure-function extractors ship FIRST so that the spawner (Plan 10-02) and the cancellation primitive (Plan 10-03) build against a frozen ABI.

Purpose: LOOP-04 (the NDJSON-line-by-line parse + the ack and <spoken-summary> extractor scaffolding) is delivered as a typed event vocabulary and two pure extractor functions in this plan; Plan 10-02 wires the parser that consumes those types, and Phase 12 wires the prompt body that drives the extractor markers.
Output: A buildable @achilles/claude-code-bridge package with no runtime spawn yet — only stable types, schemas, constants, the version-error class, and the two pure extractors, plus workspace plumbing (tsconfig.base.json alias + vitest workspace project) so the package is reachable from typecheck and test runs across the monorepo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-claude-code-bridge/10-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@packages/voice-protocol/package.json
@packages/voice-protocol/tsconfig.json
@packages/voice-protocol/src/.gitignore
@packages/voice-protocol/src/index.ts
@packages/voice-protocol/src/stt-events.ts
@tsconfig.base.json
@vitest.workspace.ts

<interfaces>
<!-- Phase 09 analog: packages/voice-protocol exports its public API via a single barrel
     with submodule re-exports. Mirror this exact shape. The Zod schemas live next to
     their inferred TypeScript types so callers import both from a single specifier. -->

From packages/voice-protocol/src/index.ts:
- export * from "./stt-events.js"
- export * from "./tts-events.js"
- export * from "./ipc.js"
- export * from "./transport.js"

From packages/voice-protocol/src/stt-events.ts (pattern to replicate):
- z.discriminatedUnion("type", [...]) — fixed-string discriminator pattern
- export const SCHEMA_NAME = z.object({...}).strict() — every schema uses .strict()
- export type X = z.infer<typeof SCHEMA_NAME> — type alias next to the schema
- Top-of-file JSDoc explains lifecycle, security notes, and ties events to PITFALL IDs

From packages/voice-protocol/package.json (shape to mirror):
- "name": "@achilles/<name>", "version": "0.1.0", "type": "module", "files": ["dist"]
- "main": "dist/index.js", "types": "dist/index.d.ts"
- scripts.build: "rm -rf dist .tsbuildinfo && tsc -p tsconfig.json"
- scripts.test: "vitest run"
- devDependencies: typescript 5.7.3, vitest 2.1.8 (pin to existing monorepo versions)
- dependencies: zod 4.3.6 (single-line entry)

From packages/voice-protocol/tsconfig.json (Phase 09 CR-06 fix to replicate):
- extends "../../tsconfig.base.json"
- compilerOptions: outDir "dist", rootDir "src", explicit declaration/sourceMap
- exclude: ["node_modules", "dist", "src/**/*.test.ts", "test/**"]

From packages/voice-protocol/src/.gitignore (Phase 09 CR-07 fix to replicate verbatim):
- "*.js", "*.d.ts", "*.js.map", "*.d.ts.map"

From tsconfig.base.json (existing path aliases to extend, NOT replace):
- "@achilles/voice-protocol": ["packages/voice-protocol/src/index.ts"]
- "@achilles/voice-protocol/*": ["packages/voice-protocol/src/*"]

From vitest.workspace.ts (existing phase-09-unit project as the template for phase-10-unit):
- workspaceAlias entries: { find: "@achilles/voice-protocol", replacement: <abs path>/src/index.ts }
- and a regex entry for the submodule subpath
- phase-09-unit project: { name: "phase-09-unit", include: ["packages/voice-*/src/**/*.test.ts"], environment: "node", passWithNoTests: true }
</interfaces>

<event_vocabulary>
<!-- The ClaudeStreamEventSchema discriminated union variants Plan 10-01 must define.
     Field names below are the Achilles-side stable vocabulary; Plan 10-02 will map
     Claude Code's actual stream-json field names INTO these types. Plan 10-01 does
     NOT need to call Claude — it only ships the schemas and the type union. -->

Variant 1: session_init
  Fields: type ("session_init"), session_id (string), model (string), claude_code_version (string)

Variant 2: assistant_text_delta
  Fields: type ("assistant_text_delta"), text (string)

Variant 3: assistant_text_done
  Fields: type ("assistant_text_done"), full_text (string)

Variant 4: tool_use
  Fields: type ("tool_use"), id (string), name (string), input (z.unknown())

Variant 5: tool_result
  Fields: type ("tool_result"), tool_use_id (string), content (string), is_error (z.boolean().optional())

Variant 6: permission_request
  Fields: type ("permission_request"), id (string), action (string), details (z.record(z.string(), z.unknown()).optional())

Variant 7: assistant_done
  Fields: type ("assistant_done")

Variant 8: parse_error
  Fields: type ("parse_error"), error (string), raw_line (z.string().optional())

Variant 9: unknown_event
  Fields: type ("unknown_event"), raw (z.unknown())

Note: process_exit ({ type: "process_exit", exit_code: number | null, signal: string | null }) is
emitted by the bridge runtime, not parsed from NDJSON. Include it in the TypeScript ClaudeBridgeEvent
union but NOT in the Zod ClaudeStreamEventSchema (which validates wire-format NDJSON lines only).
</event_vocabulary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold @achilles/claude-code-bridge package + workspace plumbing</name>
  <files>packages/claude-code-bridge/package.json, packages/claude-code-bridge/tsconfig.json, packages/claude-code-bridge/src/.gitignore, packages/claude-code-bridge/src/index.ts, tsconfig.base.json, vitest.workspace.ts</files>
  <read_first>
    - packages/voice-protocol/package.json (mirror this shape exactly: name, type module, files dist, scripts build/test/typecheck/lint/prepack, deps zod 4.3.6, devDeps typescript 5.7.3 + vitest 2.1.8)
    - packages/voice-protocol/tsconfig.json (mirror exactly; the exclude list of src/**/*.test.ts + test/** is the Phase 09 CR-06 fix)
    - packages/voice-protocol/src/.gitignore (replicate verbatim — CR-07 defence)
    - tsconfig.base.json (existing @achilles/voice-* path entries are the model — add @achilles/claude-code-bridge entries in the same alphabetised cluster)
    - vitest.workspace.ts (existing phase-09-unit project + workspaceAlias entries for @achilles/voice-* are the model — add @achilles/claude-code-bridge alias entries AND a phase-10-unit project)
    - .planning/phases/10-claude-code-bridge/10-CONTEXT.md (the "Package layout" section)
  </read_first>
  <action>
    Create `packages/claude-code-bridge/package.json` mirroring `packages/voice-protocol/package.json`. Name `@achilles/claude-code-bridge`. Version `0.1.0`. `private: false`. Description: "Subprocess wrapper around `claude -p --output-format stream-json` for Achilles: NDJSON parsing, typed event union, authoritative success/failure signal, session-id resume, SIGINT cancellation primitive, and ack + spoken-summary extractor scaffolding for the voice loop." `type: module`. `files: ["dist"]`. `publishConfig.access: public`. `main: dist/index.js`. `types: dist/index.d.ts`. Single `exports` entry for `.` mapping to `dist/index.{js,d.ts}` (no multi-subpath exports in v1.2 per CONTEXT.md). Scripts: `build` runs `rm -rf dist .tsbuildinfo && tsc -p tsconfig.json`; `typecheck` runs `tsc --noEmit`; `lint` is the placeholder echo string used by the voice-protocol package; `prepack` runs `npm run build`; `test` runs `vitest run`. Dependencies: `zod` pinned to `4.3.6` (single dep). devDependencies: `typescript` pinned to `5.7.3`, `vitest` pinned to `2.1.8`.

    Create `packages/claude-code-bridge/tsconfig.json` mirroring `packages/voice-protocol/tsconfig.json` verbatim: `extends ../../tsconfig.base.json`, `compilerOptions.outDir: "dist"`, `rootDir: "src"`, `tsBuildInfoFile: "./.tsbuildinfo"`, `declaration: true`, `declarationMap: true`, `sourceMap: true`. `include: ["src/**/*.ts"]`. `exclude: ["node_modules", "dist", "src/**/*.test.ts", "test/**"]` — the test exclude is the Phase 09 CR-06 fix and is non-negotiable.

    Create `packages/claude-code-bridge/src/.gitignore` with the same four-line guard as `packages/voice-protocol/src/.gitignore`: `*.js`, `*.d.ts`, `*.js.map`, `*.d.ts.map` (CR-07 defence — compiled artifacts must never appear in `src/`).

    Create `packages/claude-code-bridge/src/index.ts` as a barrel file. Initially export only what Task 2 + Task 3 will produce: `export * from "./constants.js";`, `export * from "./errors.js";`, `export * from "./event-schemas.js";`, `export * from "./extractor.js";`, `export * from "./types.js";`. NodeNext `.js` import specifiers per the monorepo convention from CONVENTIONS.md. Add a top-of-file JSDoc block describing the package's public surface mirroring the structure of `packages/voice-protocol/src/index.ts`.

    Edit `tsconfig.base.json`. Add two new entries to `compilerOptions.paths` immediately after the existing `@achilles/voice-tts/*` block, alphabetised:
    `"@achilles/claude-code-bridge": ["packages/claude-code-bridge/src/index.ts"]`
    `"@achilles/claude-code-bridge/*": ["packages/claude-code-bridge/src/*"]`
    Place the cluster ABOVE the voice-* cluster so the path aliases stay alphabetised by package name. Do not remove or reorder existing entries.

    Edit `vitest.workspace.ts`. Append two new entries to the `workspaceAlias` array AFTER the existing `@achilles/voice-tts` block (alphabetisation in this file is by insertion order; keep new entries grouped at the bottom of the voice-* cluster so they remain visually adjacent):
    `{ find: "@achilles/claude-code-bridge", replacement: resolve(ROOT_DIR, "packages/claude-code-bridge/src/index.ts") }`
    `{ find: /^@achilles\/claude-code-bridge\/(.+)$/, replacement: resolve(ROOT_DIR, "packages/claude-code-bridge/src/$1.ts") }`
    Append a new project to the `defineWorkspace` array AFTER the `phase-09-unit` project: name `phase-10-unit`, `resolve.alias: workspaceAlias`, `test.include: ["packages/claude-code-bridge/src/**/*.test.ts"]`, `test.environment: "node"`, `test.passWithNoTests: true`. Mirror the inline `// v1.2 phase-09 addition` footer comment with a matching `// v1.2 phase-10 addition: workspace aliases + phase-10-unit project for @achilles/claude-code-bridge` comment at the end of the file.

    Do NOT install dependencies in this task. Do NOT run `npm install`. The scaffold must be lexically valid and the next task will pull deps via the existing monorepo install path. Per CLAUDE.md global, no emojis anywhere in files, log lines, or comments.
  </action>
  <verify>
    <automated>
      cd packages/claude-code-bridge &amp;&amp; test -f package.json &amp;&amp; test -f tsconfig.json &amp;&amp; test -f src/.gitignore &amp;&amp; test -f src/index.ts &amp;&amp; node -e "const p=require('./package.json'); if(p.name!=='@achilles/claude-code-bridge')process.exit(1); if(p.type!=='module')process.exit(2); if(!p.dependencies.zod)process.exit(3); if(!p.exports['.'])process.exit(4);" &amp;&amp; grep -q 'src/\*\*/\*.test.ts' tsconfig.json &amp;&amp; grep -q '\*\.js' src/.gitignore &amp;&amp; cd ../.. &amp;&amp; grep -q '@achilles/claude-code-bridge' tsconfig.base.json &amp;&amp; grep -q 'phase-10-unit' vitest.workspace.ts &amp;&amp; grep -q '@achilles/claude-code-bridge' vitest.workspace.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `packages/claude-code-bridge/package.json` exists with `name: "@achilles/claude-code-bridge"`, `type: "module"`, `dependencies.zod: "4.3.6"`, `devDependencies.typescript: "5.7.3"`, `devDependencies.vitest: "2.1.8"`, and an `exports["."]` map with both `types` and `default` keys.
    - `packages/claude-code-bridge/tsconfig.json` extends `../../tsconfig.base.json`, sets `outDir: "dist"` and `rootDir: "src"`, and excludes `src/**/*.test.ts` (CR-06 guard).
    - `packages/claude-code-bridge/src/.gitignore` contains the four CR-07 patterns: `*.js`, `*.d.ts`, `*.js.map`, `*.d.ts.map`.
    - `packages/claude-code-bridge/src/index.ts` is a barrel with five `export *` lines pointing at `./constants.js`, `./errors.js`, `./event-schemas.js`, `./extractor.js`, `./types.js`.
    - `tsconfig.base.json` contains two new path entries for `@achilles/claude-code-bridge` (bare + subpath wildcard) without disturbing existing entries.
    - `vitest.workspace.ts` contains a `phase-10-unit` project whose `include` glob is `packages/claude-code-bridge/src/**/*.test.ts` and two new `workspaceAlias` entries for `@achilles/claude-code-bridge`.
    - No `*.js`, `*.d.ts`, `*.js.map`, or `*.d.ts.map` files are present under `packages/claude-code-bridge/src/` after this task runs.
  </acceptance_criteria>
  <done>Task 1 acceptance criteria all satisfied; the verify command exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Implement constants, ClaudeVersionError, Zod event schemas, and the public type union</name>
  <files>packages/claude-code-bridge/src/constants.ts, packages/claude-code-bridge/src/errors.ts, packages/claude-code-bridge/src/errors.test.ts, packages/claude-code-bridge/src/event-schemas.ts, packages/claude-code-bridge/src/event-schemas.test.ts, packages/claude-code-bridge/src/types.ts</files>
  <read_first>
    - packages/voice-protocol/src/stt-events.ts (mirror this exact file structure for event-schemas.ts: top-of-file JSDoc, named const SCHEMA + z.infer type alias adjacent, `.strict()` on every object, discriminated union at the bottom, `STT_EVENT_TYPES` const-tuple pattern for the discriminator list)
    - packages/voice-protocol/src/stt-events.test.ts (mirror this test-file shape: import Schema + type, parse valid + invalid fixtures, assert error.path for invalid keys)
    - .planning/phases/10-claude-code-bridge/10-CONTEXT.md (sections "Event shapes (typed unions)", "Version check (pitfall #24)" — these define the 9 wire event variants and the version-check semantics)
    - .planning/research/PITFALLS.md (Pitfall #24 — skill assumes specific Claude Code version)
  </read_first>
  <action>
    Create `packages/claude-code-bridge/src/constants.ts`. Export the following named constants:
    - `MIN_CLAUDE_VERSION = "2.0.0"` as a string literal (do NOT use a SemVer object — keep it a plain string and let Plan 10-02 do the comparison; CONTEXT.md pins this conservative floor).
    - `MAX_LINE_BYTES = 1_048_576` (1 MiB watchdog cap referenced by Plan 10-02's LDJSON parser; defined here so both plans agree on the constant).
    - `LOCKED_FLAGS` as a `readonly string[]` containing exactly the locked subprocess flag set: `["-p", "--output-format", "stream-json", "--include-partial-messages", "--append-system-prompt-file", "--resume"]`. Add a top-of-file JSDoc comment noting that flag VALUES (the path after `--append-system-prompt-file`, the session id after `--resume`) are caller-provided at runtime and are NOT part of this constant — the constant captures only the flag NAMES so a single source of truth exists for the Phase 12 wiring.
    - `SKIP_VERSION_CHECK_ENV_VAR = "ACHILLES_SKIP_CLAUDE_VERSION_CHECK"` as a string literal — Plan 10-02 will read `process.env[SKIP_VERSION_CHECK_ENV_VAR]` to decide whether to bypass.
    Use `as const` on the array literals so the inferred types are tuple-literal-typed.

    Create `packages/claude-code-bridge/src/errors.ts`. Export `class ClaudeVersionError extends Error`. Constructor signature: `constructor(actualVersion: string, requiredVersion: string)`. The constructor MUST: set `this.name = "ClaudeVersionError"`, store both versions on `this.actualVersion` and `this.requiredVersion` as readonly fields, and set `this.message` to a single-line string of the exact form `"Claude Code ${requiredVersion} or newer is required, found ${actualVersion}. Upgrade with: npm install -g @anthropic-ai/claude-code"`. Add JSDoc tying the error to Pitfall #24 and the conservative MIN_CLAUDE_VERSION constant. Do not log secrets, do not embed env vars, do not embed cwd in the message.

    Create `packages/claude-code-bridge/src/errors.test.ts`. Cover at minimum:
    (a) `new ClaudeVersionError("1.5.3", "2.0.0").message` matches the exact format above.
    (b) `err.name === "ClaudeVersionError"` so `instanceof` checks and serialised stack traces are distinguishable.
    (c) `err.actualVersion === "1.5.3"` and `err.requiredVersion === "2.0.0"` (readonly fields preserved).
    (d) `err instanceof Error === true` and `err instanceof ClaudeVersionError === true`.

    Create `packages/claude-code-bridge/src/event-schemas.ts`. Top-of-file JSDoc describes: the 9-variant typed event union, the discriminator field `type`, the mapping from Claude Code wire-format (Plan 10-02 handles the actual mapping) to these Achilles-side stable names, and the pitfall ties (#7 non-interactive `-p` mode, #8 LDJSON watchdog, #17 authoritative success). For EACH of the 9 variants listed in the `<event_vocabulary>` section above, export a `<NAME>Schema = z.object({ ... }).strict()` plus `export type <Name> = z.infer<typeof <NAME>Schema>`. The `type` discriminator must be `z.literal("session_init")` etc. The `tool_use.input` field is `z.unknown()`. The `tool_result.is_error` field is `z.boolean().optional()`. The `permission_request.details` field is `z.record(z.string(), z.unknown()).optional()`. The `parse_error.raw_line` field is `z.string().optional()`. The `unknown_event.raw` field is `z.unknown()`. Export a const-tuple `CLAUDE_STREAM_EVENT_TYPES` containing the 9 type-string literals in the same order as the schemas. At the bottom of the file, export `ClaudeStreamEventSchema = z.discriminatedUnion("type", [<list of all 9 schemas>])` and `export type ClaudeStreamEvent = z.infer<typeof ClaudeStreamEventSchema>`. This schema validates ONLY wire-format NDJSON lines; the runtime-emitted `process_exit` event is defined in `types.ts` and is intentionally NOT in this Zod union.

    Create `packages/claude-code-bridge/src/event-schemas.test.ts`. For EACH of the 9 variants:
    (a) A "valid happy path" test: `<NAME>Schema.safeParse(<minimal valid object>).success === true`, and the parsed value's `type` matches the discriminator literal.
    (b) A "missing required field" test: `safeParse({ type: "<discriminator>" }).success === false` and `error.issues[0].path` includes a required-field name.
    (c) A "wrong discriminator" test: `<NAME>Schema.safeParse({ ...validFields, type: "wrong" }).success === false`.
    Add one additional union-level test: `ClaudeStreamEventSchema.safeParse({ type: "session_init", session_id: "sid-1", model: "claude-sonnet-4-5", claude_code_version: "2.0.1" }).success === true` and the parsed value narrows to the `SessionInit` variant on the `type` discriminator.
    Add one rejection test for the discriminator: `ClaudeStreamEventSchema.safeParse({ type: "not_in_union", x: 1 }).success === false`.

    Create `packages/claude-code-bridge/src/types.ts`. Re-export the inferred event types from `event-schemas.ts` AS PART OF a richer public union. Define `export interface ProcessExitEvent { type: "process_exit"; exit_code: number | null; signal: string | null; }`. Define `export type ClaudeBridgeEvent = ClaudeStreamEvent | ProcessExitEvent;` — this is the TypeScript-level event surface that Plan 10-02 will emit on `events$`. Define `export interface CreateClaudeSessionOptions { systemPromptFile: string; resumeSessionId?: string; cwd?: string; env?: Record<string, string>; }` — these are the public options Plan 10-02 will accept. Define `export interface ClaudeOutcome { kind: "success" | "failure"; reason?: "exit_code" | "tool_error" | "cancelled"; exitCode?: number | null; details?: string; }` — the authoritative outcome shape used by Phase 12. Use `interface` over `type` for the options + outcome objects per CONVENTIONS.md.

    Do NOT add live Claude calls. Do NOT add any subprocess spawning. Do NOT add the parser. Those belong to Plan 10-02. This task ships only types, constants, the error class, and Zod schemas + tests.
  </action>
  <verify>
    <automated>
      cd packages/claude-code-bridge &amp;&amp; npx --no-install tsc --noEmit -p tsconfig.json &amp;&amp; cd ../.. &amp;&amp; npx --no-install vitest run --project phase-10-unit packages/claude-code-bridge/src/errors.test.ts packages/claude-code-bridge/src/event-schemas.test.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `MIN_CLAUDE_VERSION === "2.0.0"`, `MAX_LINE_BYTES === 1048576`, `LOCKED_FLAGS` is a frozen-like array of 6 strings matching the locked flag set exactly, and `SKIP_VERSION_CHECK_ENV_VAR === "ACHILLES_SKIP_CLAUDE_VERSION_CHECK"`.
    - `new ClaudeVersionError("1.5.3", "2.0.0")` produces `message === "Claude Code 2.0.0 or newer is required, found 1.5.3. Upgrade with: npm install -g @anthropic-ai/claude-code"`, `name === "ClaudeVersionError"`, and `actualVersion`/`requiredVersion` readonly fields populated.
    - `ClaudeStreamEventSchema` is a `z.discriminatedUnion` over `type` with exactly 9 variants and `CLAUDE_STREAM_EVENT_TYPES` is a 9-element const tuple of the variant string literals.
    - `event-schemas.test.ts` has at least 27 assertions (9 variants × 3 basic tests) plus the 2 union-level tests, and the vitest run reports green for `phase-10-unit`.
    - `ClaudeBridgeEvent` union in `types.ts` includes the 9 stream events AND `ProcessExitEvent` (which is NOT in the Zod schema, per CONTEXT.md).
    - `tsc --noEmit` exits 0 with no errors.
    - No `tool_result.is_error` validation path silently coerces — the Zod schema must keep it `optional()` so `tool_result` lines without `is_error` parse cleanly (Plan 10-02 derives success/failure from this field's presence + value).
  </acceptance_criteria>
  <done>Task 2 acceptance criteria all satisfied; both verify commands exit 0; the 9 schemas + ClaudeVersionError + types are exportable from the barrel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement extractAck + extractSpokenSummary pure functions with TDD test suite</name>
  <files>packages/claude-code-bridge/src/extractor.ts, packages/claude-code-bridge/src/extractor.test.ts</files>
  <read_first>
    - .planning/phases/10-claude-code-bridge/10-CONTEXT.md (the "Ack + spoken-summary extractor" section is the contract source of truth)
    - .planning/research/PITFALLS.md (Pitfall #16 — long / symbol-heavy spoken completion; Pitfall #17 — hallucinated success)
    - .planning/REQUIREMENTS.md (PROMPT-02 caps ack at <=12 words ~= 120 char ceiling; PROMPT-03 caps spoken-summary at <=40 words)
    - packages/voice-protocol/src/stt-events.test.ts (mirror this test-table shape: `describe`/`it` blocks with positive + negative + edge cases per behavior)
  </read_first>
  <behavior>
    - Test 1: extractAck("Got it, listing the files.") returns "Got it, listing the files."
    - Test 2: extractAck("Got it, listing the files. Now I will run ls.") returns "Got it, listing the files." (first sentence only)
    - Test 3: extractAck("Hello? Are you there!") returns "Hello?" (handles ?, !, . as terminators)
    - Test 4: extractAck("No terminator here") returns null
    - Test 5: extractAck("") returns null
    - Test 6: extractAck("   ") returns null (whitespace-only)
    - Test 7: extractAck on a 200-character sentence with terminator returns the first 120 characters (cap)
    - Test 8: extractAck preserves leading whitespace trimming — "  Got it.  " returns "Got it."
    - Test 9: extractSpokenSummary("Some text <spoken-summary>I finished the task.</spoken-summary> more text") returns "I finished the task."
    - Test 10: extractSpokenSummary("no markers at all") returns null
    - Test 11: extractSpokenSummary("<spoken-summary>unclosed marker") returns null (unclosed = null per CONTEXT.md)
    - Test 12: extractSpokenSummary("<spoken-summary></spoken-summary>") returns "" (empty inner — explicit null vs empty distinction; per CONTEXT.md spec, empty markers return the empty string and null is reserved for "markers not present")
    - Test 13: extractSpokenSummary handles the markers across newline boundaries: "<spoken-summary>\nI did it.\n</spoken-summary>" returns "I did it." (with the inner trimmed of surrounding whitespace)
    - Test 14: extractSpokenSummary returns ONLY the first occurrence's inner text when multiple `<spoken-summary>` blocks appear
    - Test 15: extractAck and extractSpokenSummary are PURE — calling them twice with the same input returns identical results and produces no side effects on input
  </behavior>
  <action>
    Apply RED-GREEN-REFACTOR. Write `packages/claude-code-bridge/src/extractor.test.ts` FIRST. Use `import { describe, it, expect } from "vitest";` and `import { extractAck, extractSpokenSummary } from "./extractor.js";`. Group tests with `describe("extractAck", ...)` and `describe("extractSpokenSummary", ...)`. Each test from the `<behavior>` list above is a separate `it(...)` block. Run `npx vitest run --project phase-10-unit packages/claude-code-bridge/src/extractor.test.ts` to confirm RED (the import fails because extractor.ts does not yet exist).

    Then write `packages/claude-code-bridge/src/extractor.ts`. Top-of-file JSDoc cites: CONTEXT.md "Ack + spoken-summary extractor" section, PROMPT-02 (<=12-word ack), PROMPT-03 (<=40-word spoken-summary), Pitfall #16 (symbol-heavy long completion), Pitfall #17 (authoritative success). Note that these are pure functions over accumulated text and DO NOT call ElevenLabs — Phase 12 wires the TTS routing.

    Export `function extractAck(streamText: string): string | null`. Behavior:
    1. If `streamText` is null/undefined/empty after `.trim()` → return null.
    2. Find the first occurrence of any sentence terminator (`.`, `?`, `!`) using a single regex `/[.!?]/`.
    3. If no terminator found → return null.
    4. Slice from start of trimmed text up to AND INCLUDING the terminator.
    5. Cap the result at 120 characters: if the sliced sentence is longer, take `slice(0, 120)` (do not preserve the terminator in the truncated form — the cap is bytewise, not semantic).
    6. Return the result.
    Use a single regex match, no String.prototype.split (which would allocate the full split array on long inputs).

    Export `function extractSpokenSummary(streamText: string): string | null`. Behavior:
    1. If `streamText` is null/undefined → return null.
    2. Match the regex `/<spoken-summary>([\s\S]*?)<\/spoken-summary>/` (non-greedy across all characters including newlines).
    3. If no match → return null. This includes the "open marker without close marker" case (the regex won't match).
    4. If matched, return the captured group with `.trim()` applied (so leading/trailing whitespace inside the markers is removed but inner whitespace is preserved verbatim).
    5. Empty inner content → return the empty string `""` (NOT null — CONTEXT.md is explicit that null is reserved for "markers absent").
    6. Only the FIRST occurrence is returned (regex without the `g` flag).

    Mark both functions as pure: no module-level mutable state, no side effects, no `console.*`, no Date.now(), no Math.random(). Add explicit `// pure function — no side effects` comment above each export.

    After the implementation, re-run `npx vitest run --project phase-10-unit packages/claude-code-bridge/src/extractor.test.ts`. Confirm all 15 tests pass (GREEN). No REFACTOR step required unless the implementation is non-obvious — keep both functions under ~25 lines each.

    Once tests are green, ensure `src/index.ts` already re-exports `./extractor.js` (the barrel was set up in Task 1; if it is missing, add `export * from "./extractor.js";`).
  </action>
  <verify>
    <automated>
      cd /Users/lakshmanturlapati/Documents/Codes/Handoff &amp;&amp; npx --no-install vitest run --project phase-10-unit packages/claude-code-bridge/src/extractor.test.ts &amp;&amp; cd packages/claude-code-bridge &amp;&amp; npx --no-install tsc --noEmit -p tsconfig.json
    </automated>
  </verify>
  <acceptance_criteria>
    - All 15 behaviors in the `<behavior>` block above pass as separate vitest cases.
    - `extractAck("Got it, listing the files. Now I will run ls.") === "Got it, listing the files."` (first sentence only).
    - `extractAck("No terminator here")`, `extractAck("")`, `extractAck("   ")` all return `null`.
    - `extractAck` on a 200-char sentence with terminator returns exactly 120 chars.
    - `extractSpokenSummary("Some text <spoken-summary>I finished the task.</spoken-summary> more text") === "I finished the task."`.
    - `extractSpokenSummary("<spoken-summary>unclosed marker")` returns `null` (unclosed markers do not match).
    - `extractSpokenSummary("<spoken-summary></spoken-summary>") === ""` (empty inner is the empty string, not null).
    - `extractSpokenSummary` handles inner newlines correctly: `"<spoken-summary>\nI did it.\n</spoken-summary>"` returns `"I did it."` after trim.
    - Both functions are pure: calling either twice with the same input returns identical results, neither mutates the input string, and neither has observable side effects.
    - `tsc --noEmit` exits 0; the vitest run reports all green for `phase-10-unit` extractor tests.
    - Both functions are exported from `src/index.ts` via the barrel and are importable as `import { extractAck, extractSpokenSummary } from "@achilles/claude-code-bridge"` (alias resolution from `tsconfig.base.json`).
  </acceptance_criteria>
  <done>Task 3 acceptance criteria all satisfied; the verify command exits 0; the two extractors are ready for Phase 12 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> bridge API (this plan) | Caller-provided `systemPromptFile` path, `resumeSessionId`, env vars, and cwd will be passed to subprocess spawn in Plan 10-02. This plan only declares the types, so the boundary surface is the type definitions in `types.ts`. |
| npm install boundary | The package adds no new npm dependencies in this plan (zod is the only runtime dep, already in the monorepo). |
| filesystem -> tsc build output | The src/.gitignore guard prevents tsc misconfiguration from leaking artifacts into src/. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | tsconfig.json exclude list | mitigate | Phase 09 CR-06 fix replicated: `exclude: ["src/**/*.test.ts", "test/**"]` keeps test files out of the dist tarball even if a downstream consumer mishandles the build. |
| T-10-02 | Information Disclosure | ClaudeVersionError.message | mitigate | Error message is a fixed template carrying only the two version strings; no env vars, no cwd, no secrets. Verified by the explicit `errors.test.ts` assertion on `.message` format. |
| T-10-03 | Tampering | src/ compiled-artifact pollution | mitigate | CR-07 fix replicated: `src/.gitignore` blocks `*.js`/`*.d.ts`/`*.js.map`/`*.d.ts.map`, preventing accidental tsc misconfiguration from leaking compiled artifacts into git. |
| T-10-04 | Spoofing | LOCKED_FLAGS constant | accept | The constant captures only flag NAMES, not values. Caller-provided paths (the `<companion.md>` path and `<sid>` resume id) are the runtime tamper surface, mitigated by Plan 10-02's argument-passing model. This plan ships the immutable flag-name list. |
| T-10-05 | Tampering | extractAck / extractSpokenSummary inputs | accept | Extractors are pure functions over accumulated assistant text. The text itself comes from Claude Code's NDJSON stream parsed by Plan 10-02; sandwich-defence wrapping of upstream transcripts is Phase 12's responsibility per CONTEXT.md scope. This plan accepts the input as untrusted and does not interpret it as a control channel — extractors are read-only. |
| T-10-SC | Tampering | npm/cargo/pip install boundary | accept | This plan adds zero new npm packages (the existing monorepo's `zod@4.3.6`, `typescript@5.7.3`, and `vitest@2.1.8` are already installed and audited). Package Legitimacy Gate is not triggered. No new install tasks; no `[ASSUMED]`/`[SUS]` packages introduced. |
</threat_model>

<verification>
- `cd packages/claude-code-bridge && npx tsc --noEmit -p tsconfig.json` exits 0.
- `npx vitest run --project phase-10-unit` exits 0 with the extractor + errors + event-schemas tests all reporting green.
- `cd /Users/lakshmanturlapati/Documents/Codes/Handoff && npx tsc -p tsconfig.base.json --noEmit` succeeds (root typecheck recognises the new path alias and the existing aliases still resolve).
- `find packages/claude-code-bridge/src -name '*.js' -o -name '*.d.ts' -o -name '*.js.map' -o -name '*.d.ts.map' | grep -v '^$' | wc -l` returns 0 (CR-07 guard holding).
- `node -e "const { ClaudeStreamEventSchema, ClaudeVersionError, MIN_CLAUDE_VERSION, MAX_LINE_BYTES, extractAck, extractSpokenSummary } = require('./packages/claude-code-bridge/dist/index.js'); if (MIN_CLAUDE_VERSION!=='2.0.0') process.exit(1); if (MAX_LINE_BYTES!==1048576) process.exit(2); if (typeof extractAck!=='function') process.exit(3); if (typeof extractSpokenSummary!=='function') process.exit(4); if (typeof ClaudeVersionError!=='function') process.exit(5); if (!ClaudeStreamEventSchema) process.exit(6); console.log('barrel OK');"` exits 0 after `npm run build --workspace @achilles/claude-code-bridge` (post-build sanity).
</verification>

<success_criteria>
- The @achilles/claude-code-bridge package is scaffolded and resolvable from anywhere in the monorepo via the `@achilles/claude-code-bridge` alias.
- The 9-variant `ClaudeStreamEventSchema` Zod discriminated union is exported, with each variant individually exported as a named schema + inferred type.
- `ClaudeVersionError` is exported and carries the documented message template, with readonly `actualVersion` and `requiredVersion` fields.
- `extractAck` and `extractSpokenSummary` pure functions are exported and satisfy all 15 documented behaviors.
- Workspace plumbing is complete: `tsconfig.base.json` has both alias entries and `vitest.workspace.ts` has both workspaceAlias entries plus a `phase-10-unit` project.
- No emojis anywhere; no live Claude calls; no subprocess spawning in this plan; no LOOP-03 / LOOP-07 logic (those are owned by Plans 10-02 and 10-03).
</success_criteria>

<output>
Create `.planning/phases/10-claude-code-bridge/10-01-SUMMARY.md` when done. Include:
- Files created (full list)
- Workspace plumbing changes (tsconfig.base.json paths added, vitest.workspace.ts project added)
- Type vocabulary shipped (the 9 stream events + ProcessExitEvent + CreateClaudeSessionOptions + ClaudeOutcome)
- Constants shipped (MIN_CLAUDE_VERSION, MAX_LINE_BYTES, LOCKED_FLAGS, SKIP_VERSION_CHECK_ENV_VAR)
- Extractor behaviours covered (the 15 test cases)
- Anything Plan 10-02 will need to consume from this scaffold (the schemas, the types, the constants)
</output>
