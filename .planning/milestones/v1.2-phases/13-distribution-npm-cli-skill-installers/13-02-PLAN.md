---
phase: 13-distribution-npm-cli-skill-installers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/achilles-skill/SKILL.md
  - packages/achilles-skill/package.json
  - apps/achilles-cli/src/skill-symlink.ts
  - apps/achilles-cli/src/skill-symlink.test.ts
  - apps/achilles-cli/src/commands/install-skill.ts
  - apps/achilles-cli/src/commands/install-skill.test.ts
autonomous: true
requirements:
  - DIST-02
tags:
  - achilles
  - cli
  - skill
  - distribution
  - install-skill
  - dist-02

must_haves:
  truths:
    - "User can run `achilles install-skill` and the command creates `~/.claude/skills/achilles/` containing the SKILL.md body + companion.md (or copies them on Windows when symlink permissions are absent)"
    - "The destination `~/.claude/skills/achilles/` resolves to the SAME source as `packages/achilles-skill/skill/` (one source of truth — symlink on macOS/Linux; copy on Windows; either way the destination's SKILL.md byte-equals the source)"
    - "Re-running `achilles install-skill` is idempotent: a second run does NOT corrupt or duplicate the destination; existing-symlink-pointing-at-our-source is silently kept; existing-non-symlink-or-wrong-target prompts the user (unless `--force` is passed)"
    - "The SKILL.md body is <= 2000 words (Pitfall #11 skill bundle scope creep guard); contains the required frontmatter (name, description, allowed-tools); references prompts/companion.md as the embedded system prompt"
    - "The skill body does NOT ship any executable binary inside the skill directory; no .exe, no .sh that contains non-shim logic, no compiled .js — only markdown + the prompts/ directory (Pitfall #11 + community security guidance)"
    - "On Windows, when `fs.symlinkSync` throws EPERM, the command falls back to a recursive copy; a one-line warning is printed naming the fallback path"
  artifacts:
    - path: "packages/achilles-skill/SKILL.md"
      provides: "The Claude Code skill manifest: YAML frontmatter (name, description, allowed-tools) + body (<= 2000 words) instructing Claude how to launch Achilles and referencing prompts/companion.md as the embedded system prompt"
      contains: "name: achilles"
      min_lines: 30
    - path: "apps/achilles-cli/src/skill-symlink.ts"
      provides: "Pure-function symlink-or-copy primitive: install({ source, destination, force, fs, platform, logger }) returns { mode: 'symlink' | 'copy' | 'already-installed' | 'skipped' } per the contract"
      exports: ["installSkillSymlink", "ExistingDestinationConflictError", "SymlinkNotPermittedError"]
    - path: "apps/achilles-cli/src/commands/install-skill.ts"
      provides: "Commander action handler that resolves the source (the workspace-resolved @achilles/achilles-skill/skill directory) and the destination (~/.claude/skills/achilles/) and invokes installSkillSymlink; surfaces errors to the user with remediation copy"
      exports: ["installSkillCommand"]
    - path: "packages/achilles-skill/package.json"
      provides: "Stays private:true (the skill body ships INSIDE the achilles npm tarball via the workspace dep in Plan 13-01; it is NOT independently published). The `files` field includes `skill/` so npm-pack inlines the directory into the achilles tarball."
      contains: "\"private\": true"
  key_links:
    - from: "apps/achilles-cli/src/commands/install-skill.ts"
      to: "packages/achilles-skill/skill"
      via: "import { SKILL_PROMPTS_DIR } from '@achilles/achilles-skill'; walk up one directory to obtain the skill root"
      pattern: "SKILL_PROMPTS_DIR"
    - from: "apps/achilles-cli/src/commands/install-skill.ts"
      to: "apps/achilles-cli/src/skill-symlink.ts"
      via: "installSkillSymlink call with the resolved source + destination + force flag"
      pattern: "installSkillSymlink"
    - from: "packages/achilles-skill/SKILL.md"
      to: "packages/achilles-skill/skill/prompts/companion.md"
      via: "the SKILL.md body explicitly references the relative path prompts/companion.md as the embedded system prompt (Plan 13-04's source-of-truth diff check verifies that this reference matches what the CLI's --append-system-prompt-file resolves to)"
      pattern: "prompts/companion.md"
---

<objective>
Deliver the `achilles install-skill` command + the finalised `packages/achilles-skill/SKILL.md` body so that running the CLI from the global npm install creates `~/.claude/skills/achilles/` pointing at one source-of-truth artifact (DIST-02). This plan owns:

1. `packages/achilles-skill/SKILL.md` — the Claude Code skill manifest with YAML frontmatter (name, description, allowed-tools) and a body bounded at <= 2000 words (Pitfall #11) that references prompts/companion.md as the embedded system prompt.
2. `apps/achilles-cli/src/skill-symlink.ts` — pure-function symlink/copy primitive with idempotency + Windows-EPERM fallback (Pitfall #13: Windows global install pain — symlink requires admin or Developer Mode; we fall back to recursive copy with a clear warning).
3. `apps/achilles-cli/src/commands/install-skill.ts` — the commander action handler the Plan 13-01 cli.ts already wires; resolves the skill source via `@achilles/achilles-skill`'s exported `SKILL_PROMPTS_DIR` and the destination via `os.homedir() + '/.claude/skills/achilles/'`.
4. `packages/achilles-skill/package.json` — keep `private: true` (the skill body ships INSIDE the achilles tarball via the workspace dep declared in Plan 13-01); ensure `files` includes both `dist` (existing — typed exports) and `skill` (must be present so `npm pack achilles` inlines the skill directory).

Purpose: Closes DIST-02 (the install-skill subcommand) AND the SKILL.md side of DIST-03 (one source of truth). The companion.md body shipped in Plan 12-01 is the single source of truth; Plan 13-02 wraps it in the SKILL.md manifest so Claude Code can discover the skill, and Plan 13-02 wires the install path so the destination IS the source (symlink on macOS/Linux). Plan 13-04 enforces the byte-identical contract with the CI diff check.

Output: A user with the global `achilles` CLI installed can run `achilles install-skill` and immediately see `/achilles` available on next Claude Code restart. The skill body shells out to the locally-installed `achilles` CLI (the npm-installed binary) — the skill itself contains NO executable code (Pitfall #11), only markdown + the prompts/ directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-distribution-npm-cli-skill-installers/13-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md

@packages/achilles-skill/package.json
@packages/achilles-skill/src/index.ts
@packages/achilles-skill/skill/prompts/companion.md
@apps/achilles/src/main/key-source.ts

<interfaces>
<!-- The skill source-of-truth exports the resolved skill prompts directory path; install-skill walks ONE level up to get the skill root. -->

From packages/achilles-skill/src/index.ts (Plan 12-01 deliverable):
```typescript
/**
 * Absolute resolved filesystem path to the directory shipping the
 * embedded prompt assets.
 *   <package-root>/skill/prompts/companion.md
 */
export const SKILL_PROMPTS_DIR: string;

/**
 * Absolute resolved filesystem path to the embedded companion system
 * prompt body. PROMPT-01 single source of truth.
 */
export const companionPromptPath: string;
```

The skill root is `path.resolve(SKILL_PROMPTS_DIR, '..')` — i.e. `<package-root>/skill/`. This directory contains `prompts/companion.md`; Plan 13-02 ADDS `SKILL.md` at the package root (NOT inside skill/), and the install-skill command symlinks the `skill/` directory plus places SKILL.md at the destination root.

CORRECTION on layout: the Claude Code skill convention places `SKILL.md` at the skill root (the directory symlinked into `~/.claude/skills/achilles/`). So SKILL.md MUST live at `packages/achilles-skill/skill/SKILL.md` (alongside `prompts/`), NOT at the package root. The symlink target is then the skill/ directory itself: `~/.claude/skills/achilles -> packages/achilles-skill/skill`. This matches `RESEARCH.md ARCHITECTURE.md > Recommended Project Structure > packages/achilles-skill/`. Plan 13-02 creates `packages/achilles-skill/skill/SKILL.md` so the layout is:

  packages/achilles-skill/skill/
  ├── SKILL.md          (NEW in Plan 13-02)
  └── prompts/
      └── companion.md  (Plan 12-01)

The destination of `achilles install-skill` is therefore `~/.claude/skills/achilles` symlinked AT THE FILESYSTEM LEVEL to the resolved `<installed-achilles>/node_modules/@achilles/achilles-skill/skill/` directory inside the npm-installed achilles package.

From Pitfall #11 (the SKILL.md scope guard — quotation from PITFALLS.md):
> Target SKILL.md body at 1,500-2,000 words per Anthropic's published guidance; push details into `references/`.
> Never include executable artifacts in the skill ZIP if it can be avoided; community security guidance treats bundled executables as a red flag.

From Pitfall #13 (Windows global install — pertinent excerpt):
> On Windows, symlink may require admin privileges — fall back to copy with warning.

The Plan 13-02 implementation honours both: SKILL.md is ~1500-2000 words of pure markdown; the skill directory contains NO executables; on Windows the install falls back from symlink to a recursive copy that warns the user.

From Plan 12-01 companion.md (Phase 12 deliverable — pinned phrases that SKILL.md references):
- Spoken acknowledgement <= 12 words (PROMPT-02)
- `<spoken-summary>` block <= 40 words (PROMPT-03)
- Failure override phrase: `"I ran into a problem"` (PROMPT-05)

These phrases live in companion.md; SKILL.md references companion.md as the embedded prompt source — SKILL.md does NOT duplicate the contract (drift prevention).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalise packages/achilles-skill/skill/SKILL.md + ensure package.json ships the skill directory</name>
  <files>
    packages/achilles-skill/skill/SKILL.md,
    packages/achilles-skill/skill/SKILL.test.ts,
    packages/achilles-skill/package.json
  </files>
  <behavior>
    - Test S1 (frontmatter present): the SKILL.md file body begins with a YAML frontmatter block delimited by `---` lines; after parsing the frontmatter, the resulting object has a `name` field whose string value is exactly `achilles`; a `description` field whose value is a non-empty string of length 1..1024 characters (Anthropic's documented description cap); an `allowed-tools` field that is a non-empty string OR a non-empty array of strings.
    - Test S2 (word cap): the SKILL.md body (post-frontmatter prose only — frontmatter is excluded; fenced code blocks excluded; H1/H2 markdown markers excluded) contains <= 2000 words split on whitespace. The test reads the file, slices off the frontmatter via the two `---` delimiters, applies a regex strip of fenced ``` blocks, and counts whitespace-delimited tokens. The pass threshold is documented in the test message: "Pitfall #11 skill bundle scope creep — SKILL.md body must be <= 2000 words".
    - Test S3 (no emoji): the SKILL.md file contains zero codepoints in U+1F000-U+1FFFF or U+2600-U+27FF (CLAUDE.md global).
    - Test S4 (references companion.md): the SKILL.md body contains the literal string `prompts/companion.md` exactly once (the body references the embedded prompt by relative path so Plan 13-04's diff check can scan a single canonical token).
    - Test S5 (no executable artefacts in skill/): the test enumerates `packages/achilles-skill/skill/` recursively and asserts that no file has the extension `.exe`, `.dll`, `.so`, `.dylib`, `.bin`, `.cmd`, `.bat`, `.ps1`, `.js`, `.mjs`, `.cjs`, `.ts`, `.sh`. Only `.md` files and the `prompts/` directory are allowed. Documented contract in the test: "Pitfall #11 — skill bundle must be pure markdown + the prompts/ directory; community security guidance treats bundled executables as a red flag."
    - Test S6 (mandates the launch command): the SKILL.md body contains the literal substring `achilles launch` (the skill body's launch instruction names the CLI command shipped by Plan 13-01; verified by simple `.includes`).
    - Test S7 (mentions the failure-override phrase): the SKILL.md body contains the substring `I ran into a problem` (PROMPT-05 — the skill body informs users that this is the failure-mode spoken phrase so they can interpret what they hear; this is INFORMATIONAL prose, NOT a re-statement of the contract — the contract lives in companion.md).
    - Test S8 (no claude-version pin without value): IF SKILL.md mentions a minimum Claude Code version (per PITFALLS.md #24), the version string MUST be of the form `>= <semver>` with a concrete semver (e.g., `>= 2.1.0`). The test pattern matches `claude.*version` case-insensitively; if found, the matched line must include a `\d+\.\d+\.\d+` semver. If not found at all, the test passes (the skill SKILL.md is allowed to defer the minimum-version assertion to runtime).
    - Test P1 (package.json ships skill/): `packages/achilles-skill/package.json` `files` array contains both `"dist"` (existing) AND `"skill"` (the latter ensures `npm pack` includes the directory recursively so the npm-installed achilles tarball — which depends on @achilles/achilles-skill via Plan 13-01's workspace dep — inlines the skill body).
    - Test P2 (package.json stays private): `packages/achilles-skill/package.json` `private` field equals `true` (the skill package is consumed INSIDE the achilles tarball via the workspace dep — it is not independently npm-published; CONTEXT.md "Mark `packages/achilles-skill/package.json` as still private").
  </behavior>
  <action>
    Author `packages/achilles-skill/skill/SKILL.md` with the following structure:

    YAML frontmatter (delimited by `---` on dedicated lines, top-of-file):
    - `name: achilles`
    - `description: <one-paragraph 256-512 char description naming Achilles as a voice companion for Claude Code, the trigger conditions (user says they want to talk, /achilles, asks to use voice), and the launch behaviour (non-blocking spawn of the local Electron app). Names ElevenLabs as the STT/TTS vendor. Mentions that the local achilles CLI must be installed via npm install -g achilles before the skill works.>`
    - `allowed-tools: Bash`

    Body (Markdown, <= 2000 words):
    - **H1: `# Achilles voice companion for Claude Code`** — one-paragraph orientation.
    - **H2: `## What it does`** — three-paragraph plain-prose explanation: floating UI lives on the user's machine, ElevenLabs handles STT and TTS, Claude Code handles the actual coding work. Names PROMPT-01 (single source-of-truth embedded prompt at `prompts/companion.md`). Names PROMPT-04 (only the spoken acknowledgement + `<spoken-summary>` block are routed to TTS — everything else is silent and visible in the terminal). Names PROMPT-05 (when the run fails, the spoken summary begins with `"I ran into a problem"` — INFORMATIONAL, not contractual).
    - **H2: `## Prerequisites`** — short bulleted list (NO emoji bullets — use `-`):
      - The achilles CLI must already be installed (`npm install -g achilles`)
      - An ElevenLabs API key must be configured via `achilles init` (or via the `ELEVENLABS_API_KEY` env var as fallback per the SAFE-01 key surface in apps/achilles/src/main/key-source.ts)
      - Microphone permission must be granted to the Electron host (the `achilles init` wizard owns the grant flow; on macOS the host calls `systemPreferences.askForMediaAccess('microphone')` per UI-07)
    - **H2: `## How to launch`** — single short paragraph instructing Claude to run `achilles launch` via the Bash tool when the user invokes the skill. Explicitly states that the launch is non-blocking — Achilles spawns its own window process and Claude Code's interactive session is left undisturbed. The skill body does NOT include a `${CLAUDE_SKILL_DIR}/scripts/launch.sh` reference because Plan 13-02 chooses to shell out directly to the npm-installed `achilles` binary (per CONTEXT.md "ships the `bin: { achilles: './dist/cli.js' }` entry" — the binary IS the entrypoint; no per-skill launch.sh needed).
    - **H2: `## How the spoken interaction works`** — two short paragraphs naming PROMPT-02 (<= 12-word ack), PROMPT-03 (<= 40-word `<spoken-summary>`), PROMPT-04 (only the two regions are spoken; everything else is silent and visible in the terminal). Includes the literal substring `prompts/companion.md` exactly once with the prose: "The exact contract is in `prompts/companion.md` — Achilles passes that file to Claude via `--append-system-prompt-file` so the contract is identical across the CLI launch path and the skill launch path."
    - **H2: `## When the run fails`** — one short paragraph naming PROMPT-05: "When work fails (non-zero exit, tool error, refused permission), the spoken summary begins with the phrase `I ran into a problem` regardless of what the LLM narrated. The orchestrator inspects the Claude run's exit code and tool_result events to determine failure — the LLM's narration is not authoritative." INFORMATIONAL only; do NOT re-state the contract.
    - **H2: `## Privacy`** — short paragraph: "Achilles holds the ElevenLabs API key only in the user's main-process OS keystore (macOS Keychain / Windows DPAPI / libsecret). Audio leaves the user's machine only to ElevenLabs endpoints. No transcripts are persisted by default; Phase 14 owns the opt-in `--save-transcripts` flag." (SAFE-01, SAFE-03 references.)

    Word-count target: 1500-2000 words. Test S2 enforces <= 2000. If the draft exceeds 2000 after stripping the frontmatter and fenced code, tighten the prose. Do NOT add a `references/` subdirectory in this plan — the SKILL.md body is sufficient at the listed scope.

    Update `packages/achilles-skill/package.json`: the `files` array currently lists `["dist", "skill"]` per the existing manifest. VERIFY that `skill` is present (the file already lists it per the read in `<context>`). If `skill` is absent, ADD it. Do NOT change `private: true` (the package is consumed via the workspace dep declared by Plan 13-01; never independently npm-published).

    Create the dedicated test file `packages/achilles-skill/skill/SKILL.test.ts` — placement at the `skill/` directory level so the test's read-relative path is `./SKILL.md`. Add the file to the `phase-12-unit` include glob in vitest.workspace.ts ONLY IF the existing glob `packages/achilles-skill/src/**/*.test.ts` does not pick it up; per the existing workspace config the project glob is `packages/achilles-skill/src/**/*.test.ts` (only `src/`) — so this plan must EITHER place the test inside `src/` (the cleanest choice; the test imports SKILL.md via the resolved package-root walk that mirrors `companionPromptPath`) OR extend the include glob. CHOICE: place the test at `packages/achilles-skill/src/skill-content.test.ts` (matching the existing companion.md test naming pattern from Plan 12-01); the test imports `SKILL_PROMPTS_DIR` from the package barrel, walks one directory up to find the skill root, then reads `SKILL.md` from there. This avoids touching vitest.workspace.ts.

    REVISED file path: `packages/achilles-skill/src/skill-content.test.ts` (NOT `packages/achilles-skill/skill/SKILL.test.ts` — the latter would not be picked up by the existing test glob). Update the `<files>` block above and the verify command accordingly when implementing.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff && npx vitest run --project phase-12-unit packages/achilles-skill/src/skill-content.test.ts && node -e "const p = require('./packages/achilles-skill/package.json'); if (!p.files?.includes('skill')) process.exit(1); if (p.private !== true) process.exit(1); console.log('OK');" && test -f packages/achilles-skill/skill/SKILL.md && head -1 packages/achilles-skill/skill/SKILL.md | grep -q '^---$' && grep -v '^---' packages/achilles-skill/skill/SKILL.md | grep -q 'prompts/companion\.md' && grep -v '^---' packages/achilles-skill/skill/SKILL.md | grep -q 'achilles launch' && grep -v '^---' packages/achilles-skill/skill/SKILL.md | grep -q 'I ran into a problem' && find packages/achilles-skill/skill -type f \( -name '*.exe' -o -name '*.dll' -o -name '*.sh' -o -name '*.js' -o -name '*.ts' \) | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>
    SKILL.md exists at `packages/achilles-skill/skill/SKILL.md` with valid YAML frontmatter, <= 2000 words in the body, contains the literal strings `prompts/companion.md`, `achilles launch`, and `I ran into a problem`, and has zero emojis. Tests S1-S8 + P1-P2 (10 tests minimum) pass in the phase-12-unit project. No executable file types exist anywhere under `packages/achilles-skill/skill/`. The existing Plan 12-01 companion.md test continues to pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: skill-symlink primitive + install-skill command (cross-platform with Windows-EPERM fallback)</name>
  <files>
    apps/achilles-cli/src/skill-symlink.ts,
    apps/achilles-cli/src/skill-symlink.test.ts,
    apps/achilles-cli/src/commands/install-skill.ts,
    apps/achilles-cli/src/commands/install-skill.test.ts
  </files>
  <behavior>
    - Test SS1 (symlink: happy path on macOS): given `platform: 'darwin'`, `source: '/pkg/skill'`, `destination: '/home/user/.claude/skills/achilles'`, an in-memory fs fake where the destination's parent (`/home/user/.claude/skills`) does NOT yet exist and the destination itself does NOT exist, calling `installSkillSymlink({ source, destination, force: false, fs, platform, logger })` returns `{ mode: 'symlink' }`; the fs fake records a `mkdirSync('/home/user/.claude/skills', { recursive: true })` call; the fs fake records exactly one `symlinkSync('/pkg/skill', '/home/user/.claude/skills/achilles', 'dir')` call; the logger receives a one-line success message containing the substring `"symlinked"` AND the destination path.
    - Test SS2 (idempotent: existing symlink pointing at correct source): when the fs fake reports that the destination already exists AND `readlinkSync(destination)` returns the source path (correctly pointing at our source already), the function returns `{ mode: 'already-installed' }`; NO `symlinkSync` call is recorded; NO `rmSync` / `unlinkSync` call is recorded; the logger receives a one-line info message containing the substring `"already installed"`.
    - Test SS3 (existing symlink pointing at WRONG source, force=false): when the fs fake reports the destination exists AND `readlinkSync(destination)` returns a different path (e.g., `/wrong/source`), the function throws `ExistingDestinationConflictError`; the error message names BOTH the existing target AND the source we wanted to install; NO `symlinkSync` / `rmSync` call is recorded.
    - Test SS4 (existing destination, force=true): same setup as SS3 but `force: true`, the function records ONE `rmSync(destination, { recursive: true, force: true })` followed by ONE `symlinkSync(source, destination, 'dir')`; returns `{ mode: 'symlink' }`.
    - Test SS5 (existing destination is a real directory, not a symlink, force=false): the fs fake reports destination exists AND `lstatSync(destination).isSymbolicLink()` returns false (it's a real directory, possibly from a previous failed install). The function throws `ExistingDestinationConflictError` whose message names the destination AND the recommended remediation (`"pass --force to overwrite"`).
    - Test SS6 (Windows symlink EPERM fallback): given `platform: 'win32'` and an fs fake whose `symlinkSync` throws an Error whose `code` is `'EPERM'`, the function falls back: records a `mkdirSync(destination, { recursive: true })` call AND a `cpSync(source, destination, { recursive: true })` call (Node 16.7+ `fs.cpSync`); returns `{ mode: 'copy' }`; logger receives a TWO-line message: a warning containing the substring `"symlink not permitted"` and a follow-up `"copied"` info line. The success result still reports the destination path so the user knows where it landed.
    - Test SS7 (Windows symlink EISDIR fallback): same as SS6 but with `code: 'EISDIR'` (some Windows configurations report different codes). The function ALSO falls back to copy. The fallback is triggered for the set `{'EPERM', 'EACCES', 'EISDIR'}` matched on `err.code`.
    - Test SS8 (non-Windows symlink failure throws SymlinkNotPermittedError): on darwin or linux, if `symlinkSync` throws ANY error, the function does NOT fall back to copy; it re-throws as `SymlinkNotPermittedError` (typed; instanceof Error true; message contains the platform name + the underlying errno code if present). Rationale: a symlink failure on macOS/Linux is a real bug (the user's homedir is unwritable or similar) and silently copying would mask it.
    - Test IS1 (install-skill command: resolves source from @achilles/achilles-skill): the install-skill command imports `SKILL_PROMPTS_DIR` from the workspace dep and walks ONE directory up via `path.resolve(SKILL_PROMPTS_DIR, '..')` to obtain the skill root. The unit test passes an injected `skillSourceProvider: () => '/pkg/skill'` so the test does not need the real filesystem; the production path uses the real exported SKILL_PROMPTS_DIR.
    - Test IS2 (install-skill command: resolves destination via os.homedir()): the test injects `homedir: () => '/home/alice'`; the resolved destination is exactly `'/home/alice/.claude/skills/achilles'` (forward slashes preserved on test fake; production uses `path.join` which picks the OS separator).
    - Test IS3 (install-skill command: force flag passed through): when commander invokes the action handler with `--force`, `installSkillCommand({ force: true, ...deps })` calls `installSkillSymlink` with `force: true`. When `--force` is absent, `force: false` is passed.
    - Test IS4 (install-skill command: ExistingDestinationConflictError surfaces remediation copy): when the underlying primitive throws ExistingDestinationConflictError, the command writes a stderr line containing the substring `"already installed at a different path"` AND `"pass --force to overwrite"` AND calls `processExitImpl(1)`. NO process.exit(0) is called.
    - Test IS5 (install-skill command: success prints the restart-claude-code reminder): on a clean install (mode === 'symlink' or 'copy'), the command writes a stdout line containing `"restart Claude Code"` (Pitfall #5 anti-pattern from PITFALLS.md: "Skills installed mid-session are picked up via live change detection, but a new top-level skills directory created mid-session requires Claude Code restart").
  </behavior>
  <action>
    Implement `apps/achilles-cli/src/skill-symlink.ts`:
      - Export named types: `InstallSkillSymlinkOptions` (the input contract) and `InstallSkillSymlinkResult` (`{ mode: 'symlink' | 'copy' | 'already-installed' }`).
      - Export named error classes: `ExistingDestinationConflictError extends Error` and `SymlinkNotPermittedError extends Error`. Each has a `name` property set to the class name.
      - Export `installSkillSymlink({ source, destination, force, fs, platform, logger }): InstallSkillSymlinkResult`.

      Algorithm:
      1. Ensure destination parent exists: `fs.mkdirSync(path.dirname(destination), { recursive: true })`.
      2. If destination exists (via `fs.lstatSync(destination)` succeeding):
         a. If it's a symbolic link: read its target via `fs.readlinkSync(destination)`. If the target equals source (use `path.resolve` to normalise both sides for comparison), return `{ mode: 'already-installed' }` and log "already installed".
         b. If it's a symbolic link with a DIFFERENT target AND `force === false`: throw `ExistingDestinationConflictError(`Destination ${destination} already points to ${target}; pass --force to overwrite with ${source}.`)`.
         c. If it's a symbolic link with a DIFFERENT target AND `force === true`: continue to step 3 after `fs.rmSync(destination, { recursive: true, force: true })`.
         d. If it's NOT a symbolic link (real directory or file) AND `force === false`: throw `ExistingDestinationConflictError(`Destination ${destination} exists but is not a symlink; pass --force to overwrite (this will DELETE the existing path).`)`.
         e. If not a symlink AND `force === true`: `fs.rmSync(destination, { recursive: true, force: true })` and continue.
      3. Try `fs.symlinkSync(source, destination, 'dir')`. On success, log `"[achilles] skill symlinked at ${destination} -> ${source}"` and return `{ mode: 'symlink' }`.
      4. On symlinkSync failure: branch on platform:
         - `platform === 'win32'` AND `err.code` is one of `'EPERM' | 'EACCES' | 'EISDIR'`: log a one-line warning `"[achilles] symlink not permitted on this Windows configuration (need admin or Developer Mode); falling back to a recursive copy. Future updates require re-running 'achilles install-skill'."` then `fs.cpSync(source, destination, { recursive: true })` and log `"[achilles] skill copied to ${destination}"`. Return `{ mode: 'copy' }`.
         - Any other platform OR any other error code: throw `new SymlinkNotPermittedError(`Failed to create symlink at ${destination} -> ${source}: ${err.code ?? err.message}`)`.

      The fs seam type is the minimal subset used: `{ mkdirSync, lstatSync, readlinkSync, symlinkSync, rmSync, cpSync }`. The production path uses `node:fs` defaults; the test path injects a recording fake. NO real filesystem touch in the test.

    Implement `apps/achilles-cli/src/commands/install-skill.ts`:
      - REPLACE the Plan-13-01 placeholder body with the real implementation.
      - Import `SKILL_PROMPTS_DIR` from `@achilles/achilles-skill` (the dependency declared in Plan 13-01's package.json).
      - Import `installSkillSymlink, ExistingDestinationConflictError, SymlinkNotPermittedError` from `../skill-symlink.js`.
      - Export `installSkillCommand({ force, homedir, platform, fs, stdout, stderr, processExitImpl, skillSourceProvider, logger })`. The seams default to the production values; the test injects fakes.
      - Algorithm:
        1. Resolve source = `skillSourceProvider()` (default: `path.resolve(SKILL_PROMPTS_DIR, '..')` so the source is the `<package-root>/skill/` directory containing both SKILL.md and prompts/).
        2. Resolve destination = `path.join(homedir(), '.claude', 'skills', 'achilles')` (default homedir: `os.homedir`).
        3. Try `installSkillSymlink({ source, destination, force, fs, platform, logger })`.
        4. On success (any of the three success modes), write to stdout:
           - For mode `symlink` or `copy`: `[achilles] Skill installed at ${destination}.` followed by `Restart Claude Code to discover the /achilles skill.`
           - For mode `already-installed`: `[achilles] Skill already installed at ${destination}; nothing to do.`
           Return without calling processExitImpl (success exits naturally with 0).
        5. On `ExistingDestinationConflictError`: write to stderr the error's `message` followed by the line `Pass --force to overwrite.` Call `processExitImpl(1)`.
        6. On `SymlinkNotPermittedError`: write to stderr the error's `message` followed by the line `On Windows you may need to enable Developer Mode (Settings > Update & Security > For Developers).` Call `processExitImpl(1)`.
        7. On any other error: write a generic `[achilles] install-skill failed: ${err.message}` line to stderr; call `processExitImpl(1)`.

    The `logger` seam in the primitive defaults to `(msg) => stdout.write(msg + '\n')` when invoked from the command. The command itself does NOT log to stderr inside the primitive seam — it routes errors through the command's stderr seam so the user sees concise remediation copy on misuse.

    Update `apps/achilles-cli/README.md` (Plan 13-01 shipped the minimal version): add a `## install-skill` subsection under `## Commands` describing what `achilles install-skill` does (creates the symlink, references `prompts/companion.md`), names the `--force` flag, names the Windows fallback behaviour. Keep the addition concise (under 200 words).

    NO emoji codepoints in any output string, log message, comment, or markdown. NO console.log; use the injected seams everywhere.
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Handoff && npx vitest run --project phase-13-unit apps/achilles-cli/src/skill-symlink.test.ts apps/achilles-cli/src/commands/install-skill.test.ts && npm run typecheck --workspace apps/achilles-cli && find apps/achilles-cli/src -name '*.js' -o -name '*.d.ts' | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>
    Tests SS1-SS8 + IS1-IS5 (13 tests minimum) pass. The install-skill primitive is pure (no real filesystem touch in tests); the command path is integration-shaped only via the injected `homedir` + `skillSourceProvider` + `fs` seams. Typecheck exits 0 for the achilles-cli workspace. CR-07 hygiene preserved (no compiled artefacts under src/).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| achilles CLI process → user homedir | install-skill creates a symlink (or copy) under `~/.claude/skills/achilles/` — file-system mutation outside the npm install tree |
| symlinked skill body → Claude Code skill discovery | Claude Code reads the symlinked SKILL.md + prompts/companion.md at next launch — Achilles' source-of-truth markdown becomes Claude's system prompt |
| installed npm tarball → skill source | The achilles tarball inlines the @achilles/achilles-skill workspace dep; the skill body shipped to the user is the same source Achilles ships from |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | a malicious skill body injected by a typosquat tarball or compromised mirror | mitigate | Plan 13-04 owns the source-of-truth diff check + tarball scan; Plan 13-02 ensures SKILL.md is shipped INSIDE the achilles tarball (via the workspace dep declared in Plan 13-01) so a single integrity check covers both surfaces |
| T-13-08 | Spoofing | an attacker on the user's filesystem replaces the symlinked skill destination with a different target between `achilles install-skill` runs | accept | Out of scope. The user's homedir is their security domain. The idempotent check (SS2) verifies the symlink still points at our source; SS3 surfaces a conflict for the user to investigate. We do NOT cryptographically verify the symlink target each run. |
| T-13-09 | Tampering | force-overwrite path (`--force`) destroying a user's customised local skill body that they had hand-edited | mitigate | The `--force` flag is opt-in and named. The command writes a clear stderr message before requiring it. The destructive path is gated behind `force=true` AND the SS4/SS5 tests pin the rmSync-before-symlink sequence so the user knows their existing data was removed |
| T-13-10 | Information Disclosure | the SKILL.md body containing an ElevenLabs API key prefix or path that would leak via the published tarball | mitigate | Plan 13-04 owns the tarball scan for ElevenLabs key prefixes (`sk_`, `xi-api-key`). Plan 13-02's SKILL.md body is text-only and authored without referencing any concrete key; the test S3/S4 verify no executable artefacts ship in the skill dir |
| T-13-11 | Tampering | bundled executables inside the skill directory exfiltrating data when Claude calls Bash | mitigate | Test S5 asserts that the skill/ directory contains NO file with `.exe`, `.dll`, `.sh`, `.js`, `.ts`, or similar executable extension. Only `.md` files and the `prompts/` subdirectory are allowed. The skill body's launch instruction shells out to the npm-installed `achilles` binary; if the user has installed a tampered npm package the install-skill threat surface inherits that risk (covered by 13-04's tarball scan) |
| T-13-12 | Spoofing | the user's homedir resolution returning an unexpected path (HOME env var poisoning) | accept | Defer to OS. The user's HOME is the user's security domain. `os.homedir()` is the Node stdlib choice. Plan 13-02 does NOT independently resolve HOME or read other env vars to compute the destination |

Package legitimacy: Plan 13-02 adds zero new npm dependencies (uses node:fs, node:path, node:os and the workspace dep on @achilles/achilles-skill declared in Plan 13-01). No legitimacy gate needed.
</threat_model>

<verification>
- `npx vitest run --project phase-13-unit apps/achilles-cli/src/skill-symlink.test.ts apps/achilles-cli/src/commands/install-skill.test.ts` exits 0 with ≥ 13 passing tests (SS1-SS8 + IS1-IS5).
- `npx vitest run --project phase-12-unit packages/achilles-skill/src/skill-content.test.ts` exits 0 with ≥ 10 passing tests (S1-S8 + P1-P2).
- `npm run typecheck --workspace apps/achilles-cli` exits 0.
- `npm run typecheck --workspace packages/achilles-skill` exits 0 (the new SKILL.md is asset-only; the test file is .ts).
- `find apps/achilles-cli/src -name '*.js' -o -name '*.d.ts'` is empty.
- `find packages/achilles-skill/skill -type f \( -name '*.exe' -o -name '*.dll' -o -name '*.so' -o -name '*.dylib' -o -name '*.bin' -o -name '*.cmd' -o -name '*.bat' -o -name '*.ps1' -o -name '*.js' -o -name '*.mjs' -o -name '*.cjs' -o -name '*.ts' -o -name '*.sh' \)` returns nothing (Pitfall #11 enforced).
- `grep -c 'I ran into a problem' packages/achilles-skill/skill/SKILL.md` is `>= 1` (informational reference to PROMPT-05).
- `grep -c 'prompts/companion\.md' packages/achilles-skill/skill/SKILL.md` is exactly `1` (single canonical reference for Plan 13-04's diff check).
- The SKILL.md body word count (excluding YAML frontmatter and fenced code blocks) is `<= 2000` (test S2 enforces this).
- No emoji codepoint in U+1F000-U+1FFFF or U+2600-U+27FF appears in SKILL.md or any new .ts file.
- The Plan 12-04 phase-12-unit suite (200/200 under MOCK_LOOP=1; 196/200 default) continues to pass with the new SKILL.md (the new test file ADDS to the suite; existing tests are not modified).
- The Plan 13-01 phase-13-unit suite (≥ 21 tests) continues to pass; Plan 13-02 ADDS ≥ 13 new tests, taking the project total to ≥ 34.
</verification>

<success_criteria>
1. `packages/achilles-skill/skill/SKILL.md` exists with valid YAML frontmatter (name, description, allowed-tools), <= 2000 words in the body, references prompts/companion.md exactly once, and names the failure-override phrase ("I ran into a problem") informationally.
2. The skill directory `packages/achilles-skill/skill/` contains ONLY markdown files (SKILL.md, prompts/companion.md) — no executable artefacts (Pitfall #11 enforced by test S5).
3. The `installSkillSymlink` primitive is idempotent (already-installed mode), force-aware, and falls back to a recursive copy on Windows when symlinkSync throws EPERM/EACCES/EISDIR.
4. The `achilles install-skill` command resolves source via `@achilles/achilles-skill`'s `SKILL_PROMPTS_DIR` (walked up to the skill root) and destination via `os.homedir()` + `.claude/skills/achilles`.
5. The command surfaces conflict errors with concrete remediation copy (`--force`, "enable Developer Mode" on Windows) and the success path includes the "restart Claude Code" reminder (Pitfall #5).
6. The package.json `private: true` constraint is preserved; the achilles npm tarball will inline the skill body via the workspace dep declared in Plan 13-01.
</success_criteria>

<output>
Create `.planning/phases/13-distribution-npm-cli-skill-installers/13-02-SUMMARY.md` when done — list files created/modified, test counts (target ≥ 13 new tests in phase-13-unit, ≥ 10 new tests in phase-12-unit, zero regressions in any other phase), the Pitfall #11 verification result, the typecheck result, and any deviations from the plan with rationale.
</output>
